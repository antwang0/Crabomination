# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Card images are auto-downloaded from Scryfall on first run by
# `crabomination_client::scryfall::ensure_card_images`; tracking them just
# bloats the repo and goes stale when the catalog grows. Fonts stay tracked
# (they're not auto-fetched and the UI needs them at startup), and the
# cardback at `crabomination_client/assets/cardback.png` is tracked because
# it's a single small file the renderer needs at startup before the
# prefetcher has a chance to run.
#
# Both paths are listed because `cfg.paths.asset_dir` resolves through
# `CARGO_MANIFEST_DIR` (→ `crabomination_client/assets/`), but a stale
# workspace-root copy can linger from earlier runs that resolved against
# the cwd instead.
crabomination_client/assets/cards/
/assets/cards/

# Per-user Claude Code harness settings (allow-lists, hooks, etc.).
# Each contributor maintains their own.
.claude/settings.local.json

# Python bytecode caches generated by `scripts/*.py` runs.
scripts/__pycache__/
