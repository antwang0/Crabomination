[package]
name = "crabomination_server"
version = "0.1.0"
edition = "2024"

[dependencies]
# No allocator swap here: a `#[global_allocator]` is the program's call,
# and neither of these is the simulator.
crabomination = { path = "../crabomination", default-features = false }
serde_json = "1.0.150"
