--------------------------------------------------------------------------------
Profile data file 'cg.base.out' (creator: callgrind-3.22.0)
--------------------------------------------------------------------------------
I1 cache: 
D1 cache: 
LL cache: 
Timerange: Basic block 0 - 612561719
Trigger: Program termination
Profiled target:  target/profiling-fast/bot_ladder --a gang --b gang --games 6 --threads 1 --seed 1 --decks fixed (PID 24569, part 1)
Events recorded:  Ir
Events shown:     Ir
Event sort order: Ir
Thresholds:       98
Include dirs:     
User annotated:   
Auto-annotation:  on

--------------------------------------------------------------------------------
Ir                     
--------------------------------------------------------------------------------
3,116,508,803 (100.0%)  PROGRAM TOTALS

--------------------------------------------------------------------------------
Ir                    file:function
--------------------------------------------------------------------------------
173,617,646 ( 5.57%)  ./malloc/./malloc/malloc.c:_int_malloc [/usr/lib/x86_64-linux-gnu/libc.so.6]
142,757,444 ( 4.58%)  ./malloc/./malloc/malloc.c:_int_free [/usr/lib/x86_64-linux-gnu/libc.so.6]
118,987,081 ( 3.82%)  ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6]
105,771,695 ( 3.39%)  ./malloc/./malloc/malloc.c:malloc [/usr/lib/x86_64-linux-gnu/libc.so.6]
 79,393,764 ( 2.55%)  crabomination/src/game/mod.rs:crabomination::game::GameState::gather_continuous_effects_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 73,606,254 ( 2.36%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 64,022,272 ( 2.05%)  ./malloc/./malloc/malloc.c:free [/usr/lib/x86_64-linux-gnu/libc.so.6]
 53,708,762 ( 1.72%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::gather_continuous_effects_inner
 39,356,112 ( 1.26%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 39,257,436 ( 1.26%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::gather_continuous_effects_inner
 32,840,478 ( 1.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
 31,247,602 ( 1.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 29,886,418 ( 0.96%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 28,147,517 ( 0.90%)  ./malloc/./malloc/malloc.c:_int_free_merge_chunk [/usr/lib/x86_64-linux-gnu/libc.so.6]
 27,564,244 ( 0.88%)  crabomination/src/game/layers.rs:crabomination::game::layers::compute_permanent_pass [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 27,351,776 ( 0.88%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::gather_continuous_effects_inner
 26,836,470 ( 0.86%)  crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 26,113,094 ( 0.84%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
 25,151,087 ( 0.81%)  ./malloc/./malloc/arena.c:free
 24,769,664 ( 0.79%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter_nested.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 24,732,719 ( 0.79%)  ./malloc/./malloc/malloc.c:malloc_consolidate [/usr/lib/x86_64-linux-gnu/libc.so.6]
 23,470,875 ( 0.75%)  ./malloc/./malloc/malloc.c:unlink_chunk.isra.0 [/usr/lib/x86_64-linux-gnu/libc.so.6]
 23,120,340 ( 0.74%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 22,169,056 ( 0.71%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::dispatch_triggers_for_events
 21,673,290 ( 0.70%)  crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 20,895,424 ( 0.67%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::dispatch_triggers_for_events
 20,140,545 ( 0.65%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
 18,906,130 ( 0.61%)  crabomination_base/src/card.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 18,855,320 ( 0.61%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::gather_continuous_effects_inner
 17,873,304 ( 0.57%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/alloc/unix.rs:__rustc::__rdl_alloc
 17,193,872 ( 0.55%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::make_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 16,310,484 ( 0.52%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
 14,646,619 ( 0.47%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:alloc::sync::Arc<T,A>::drop_slow
 14,393,938 ( 0.46%)  crabomination_base/src/card.rs:<crabomination_base::card::Keyword as core::cmp::PartialEq>::eq [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 13,856,986 ( 0.44%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 13,701,388 ( 0.44%)  crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 13,486,310 ( 0.43%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::find_card_anywhere
 13,366,162 ( 0.43%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 13,352,074 ( 0.43%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
 13,135,760 ( 0.42%)  crabomination/src/player.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 12,565,994 ( 0.40%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 11,974,366 ( 0.38%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 11,841,472 ( 0.38%)  crabomination_base/src/effect/abilities.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 11,713,282 ( 0.38%)  crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 11,587,302 ( 0.37%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 11,362,282 ( 0.36%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::computed_permanent
 11,197,236 ( 0.36%)  crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 11,130,146 ( 0.36%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
 10,875,454 ( 0.35%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::computed_permanent
 10,819,596 ( 0.35%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of [/home/user/Crabomination/target/profiling-fast/bot_ladder]
 10,624,886 ( 0.34%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
 10,502,588 ( 0.34%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::sync::Arc<T,A>::drop_slow'2
 10,375,364 ( 0.33%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:alloc::sync::Arc<T,A>::drop_slow'2
 10,168,975 ( 0.33%)  ./malloc/./malloc/malloc.c:_int_free_maybe_consolidate [/usr/lib/x86_64-linux-gnu/libc.so.6]
 10,123,754 ( 0.32%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_color_set [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  9,989,606 ( 0.32%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  9,962,100 ( 0.32%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
  9,806,474 ( 0.31%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  9,725,724 ( 0.31%)  crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  9,443,976 ( 0.30%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::raw_vec::RawVecInner<A>::finish_grow [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  9,224,526 ( 0.30%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::clone_from_ref_in [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  8,997,532 ( 0.29%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
  8,601,748 ( 0.28%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  7,970,518 ( 0.26%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  7,963,452 ( 0.26%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::dispatch_board_scan
  7,858,958 ( 0.25%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  7,765,172 ( 0.25%)  crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_board_scan [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  7,439,336 ( 0.24%)  ./malloc/./malloc/malloc.c:realloc [/usr/lib/x86_64-linux-gnu/libc.so.6]
  7,250,668 ( 0.23%)  crabomination/src/game/layers.rs:crabomination::game::layers::compute_permanent [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  7,100,096 ( 0.23%)  crabomination/src/game/mod.rs:crabomination::game::GameState::same_team [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  7,030,196 ( 0.23%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/function.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  7,018,200 ( 0.23%)  crabomination/src/game/actions.rs:crabomination::game::actions::effect_produces_color [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  6,896,148 ( 0.22%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
  6,800,848 ( 0.22%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::scale_damage_to
  6,773,970 ( 0.22%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
  6,750,792 ( 0.22%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::layers::compute_permanent
  6,739,932 ( 0.22%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  6,712,992 ( 0.22%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
  6,475,270 ( 0.21%)  crabomination/src/server/bot.rs:crabomination::server::bot::available_mana [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  6,448,215 ( 0.21%)  ./malloc/./malloc/malloc.c:_int_realloc [/usr/lib/x86_64-linux-gnu/libc.so.6]
  6,213,335 ( 0.20%)  ./malloc/./malloc/arena.c:malloc
  6,127,818 ( 0.20%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter_nested.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  6,046,964 ( 0.19%)  crabomination/src/game/layers.rs:crabomination::game::layers::affected_includes_gated [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  6,003,450 ( 0.19%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::raw_vec::RawVec<T,A>::grow_one [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  5,870,702 ( 0.19%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::dispatch_board_scan
  5,768,486 ( 0.19%)  crabomination_base/src/card.rs:crabomination::game::GameState::computed_permanent
  5,760,498 ( 0.18%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/set_len_on_drop.rs:crabomination::game::GameState::dispatch_triggers_for_events
  5,597,760 ( 0.18%)  crabomination/src/game/actions.rs:crabomination::game::actions::is_mana_ability::mana_compatible [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  5,586,622 ( 0.18%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  5,325,868 ( 0.17%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::has_keyword
  5,273,998 ( 0.17%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_creature
  5,255,724 ( 0.17%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
  5,232,018 ( 0.17%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  5,196,822 ( 0.17%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  5,189,162 ( 0.17%)  crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  5,096,266 ( 0.16%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
  5,039,476 ( 0.16%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
  5,038,370 ( 0.16%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  4,944,344 ( 0.16%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut
  4,939,634 ( 0.16%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  4,938,892 ( 0.16%)  ./malloc/./malloc/malloc.c:calloc [/usr/lib/x86_64-linux-gnu/libc.so.6]
  4,798,212 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::GameState::computed_permanent
  4,796,257 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::drop_slow [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  4,759,768 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::sync::Arc<T,A>::drop_slow
  4,684,766 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
  4,667,666 ( 0.15%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:<hashbrown::raw::RawTable<T,A> as core::ops::drop::Drop>::drop [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  4,632,796 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::scale_damage_to
  4,632,012 ( 0.15%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::layers::compute_permanent_pass
  4,494,440 ( 0.14%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::trigger_grant_sources
  4,493,950 ( 0.14%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
  4,427,408 ( 0.14%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
  4,367,568 ( 0.14%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone
  4,303,418 ( 0.14%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  4,281,178 ( 0.14%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::GameState::computed_permanent
  4,165,182 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_creature
  4,165,182 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_creature
  4,146,004 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
  4,096,720 ( 0.13%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  4,068,888 ( 0.13%)  crabomination_base/src/card.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
  4,065,842 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  4,055,918 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
  4,054,286 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/take.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  4,015,740 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:alloc::sync::Arc<T,A>::drop_slow
  4,015,342 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
  3,996,834 ( 0.13%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,974,880 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::GameState::computed_permanent
  3,921,260 ( 0.13%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut
  3,894,398 ( 0.12%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,888,706 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
  3,858,790 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/alloc/layout.rs:alloc::sync::arcinner_layout_for_value_layout
  3,798,678 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::printed_color_set
  3,738,736 ( 0.12%)  crabomination/src/game/actions.rs:crabomination::game::actions::is_mana_ability::produces_mana [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,707,600 ( 0.12%)  crabomination_base/src/card.rs:crabomination::game::GameState::find_card_anywhere
  3,654,304 ( 0.12%)  crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,646,514 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:alloc::sync::Arc<T,A>::make_mut
  3,642,048 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::layers::compute_permanent_pass
  3,637,352 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields
  3,611,674 ( 0.12%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
  3,523,903 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:alloc::raw_vec::RawVecInner<A>::finish_grow
  3,509,584 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  3,508,688 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan
  3,472,444 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
  3,416,548 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<alloc::boxed::Box<alloc::vec::Vec<crabomination_base::card::Keyword>>>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,404,218 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
  3,391,152 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
  3,361,608 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::layers::compute_permanent_pass
  3,284,442 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  3,280,752 ( 0.11%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  3,254,420 ( 0.10%)  crabomination/src/game/mod.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  3,248,554 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::trigger_grant_sources
  3,226,498 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
  3,208,726 ( 0.10%)  crabomination/src/game/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
  3,147,540 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::layers::compute_permanent_pass
  3,099,844 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::trigger_grant_sources
  3,083,890 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
  3,051,500 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop
  3,044,538 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::gather_continuous_effects_inner
  3,014,684 ( 0.10%)  crabomination/src/server/bot.rs:crabomination::server::bot::cast_candidates [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,992,872 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/map.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  2,985,144 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,974,860 ( 0.10%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation
  2,944,308 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
  2,933,242 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::GameState::dispatch_triggers_for_events
  2,921,252 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  2,899,942 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
  2,851,824 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::game::layers::compute_permanent_pass
  2,834,994 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
  2,817,772 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
  2,816,870 ( 0.09%)  crabomination/src/game/mod.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,811,768 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::drop_slow'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,806,065 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,805,710 ( 0.09%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,773,652 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
  2,773,396 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  2,762,520 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan
  2,742,644 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  2,735,280 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::layers::compute_permanent
  2,732,408 ( 0.09%)  crabomination/src/game/types.rs:crabomination::game::GameState::perform_action_inner
  2,714,000 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_blocks_inner
  2,707,171 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,701,394 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
  2,699,032 ( 0.09%)  crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,692,134 ( 0.09%)  crabomination/src/game/actions.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  2,672,208 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
  2,668,624 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan
  2,660,804 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::GameState::dispatch_triggers_for_events
  2,656,748 ( 0.09%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map
  2,648,422 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::dispatch_board_scan
  2,630,342 ( 0.08%)  crabomination_base/src/effect/abilities.rs:crabomination_base::effect::abilities::ActivatedAbility::is_free [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,625,852 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields
  2,611,592 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::gather_continuous_effects_inner
  2,584,118 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
  2,568,466 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
  2,548,290 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  2,542,434 ( 0.08%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,530,874 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
  2,528,458 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
  2,514,720 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
  2,510,498 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop
  2,505,242 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}}
  2,485,037 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:alloc::raw_vec::RawVecInner<A>::finish_grow
  2,484,952 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,454,062 ( 0.08%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,438,746 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::ops::drop::Drop>::drop [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,432,998 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,421,210 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  2,392,125 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::raw_vec::RawVecInner<A>::reserve::do_reserve_and_handle [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,368,824 ( 0.08%)  crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,363,142 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
  2,352,602 ( 0.08%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::cost_reduction_for_spell_full
  2,324,452 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::combat_damage_prevented_from
  2,319,198 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::layers::compute_permanent_pass
  2,286,495 ( 0.07%)  ???:__rustc::__rust_no_alloc_shim_is_unstable_v2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,286,494 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/alloc/unix.rs:__rustc::__rdl_dealloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,286,494 ( 0.07%)  ???:__rustc::__rust_dealloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,274,300 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::layers::AffectedPermanents> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,245,180 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::abilities::ActivatedAbility> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,234,163 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/alloc.rs:__rustc::__rdl_alloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,234,163 ( 0.07%)  ???:__rustc::__rust_alloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,226,268 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
  2,219,812 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::option::Option<T>::or_else [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,217,632 ( 0.07%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_creature
  2,189,324 ( 0.07%)  crabomination_base/src/card.rs:<crabomination_base::card::Keyword as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,149,762 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,123,124 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:alloc::sync::Arc<T,A>::drop_slow'2
  2,086,056 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  2,065,088 ( 0.07%)  crabomination/src/game/stack.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  2,062,160 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_blocks_inner
  2,054,140 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::combat_damage_prevented_to_self
  2,047,188 ( 0.07%)  ./malloc/./malloc/arena.c:realloc
  2,029,028 ( 0.07%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState>
  2,005,108 ( 0.06%)  crabomination/src/game/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  2,002,404 ( 0.06%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  2,001,022 ( 0.06%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::clear_end_of_turn_effects [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,989,676 ( 0.06%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,974,146 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
  1,958,752 ( 0.06%)  crabomination/src/server/bot.rs:crabomination::server::bot::eval_material_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,958,108 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
  1,931,752 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_land
  1,925,968 ( 0.06%)  crabomination/src/server/bot.rs:crabomination::server::bot::accumulate_mana_colors [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,915,380 ( 0.06%)  crabomination/src/game/effects/events.rs:crabomination::game::effects::events::event_matches_spec [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,910,226 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,901,416 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut
  1,899,342 ( 0.06%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,880,548 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::extra_cost_for_spell
  1,876,676 ( 0.06%)  crabomination/src/server/bot.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,875,704 ( 0.06%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,871,182 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,860,552 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::board_keyword_in_scope
  1,859,452 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::layers::compute_permanent_pass
  1,854,680 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation
  1,848,822 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:alloc::sync::Arc<T,A>::drop_slow
  1,845,520 ( 0.06%)  crabomination_base/src/mana.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  1,835,676 ( 0.06%)  crabomination/src/server/bot.rs:crabomination::server::bot::main_phase_action_with [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,790,808 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  1,785,696 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::GameState::same_team
  1,777,280 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
  1,774,172 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
  1,746,012 ( 0.06%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,742,002 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for
  1,729,350 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,725,042 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::dispatch_triggers_for_events
  1,724,498 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  1,723,858 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::GameState::computed_permanent
  1,721,962 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::permanents_with_abilities_removed
  1,715,484 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:alloc::raw_vec::RawVecInner<A>::finish_grow
  1,715,278 ( 0.06%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
  1,712,404 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}}
  1,690,680 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::printed_color_set
  1,686,352 ( 0.05%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,678,446 ( 0.05%)  crabomination/src/game/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,670,358 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  1,667,136 ( 0.05%)  crabomination_base/src/effect.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,663,564 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::layers::compute_permanent_pass
  1,660,008 ( 0.05%)  crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,649,368 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  1,627,984 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::can_block_attacker_computed
  1,627,096 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
  1,619,538 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,612,542 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::spell_kind
  1,611,928 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut
  1,607,588 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,603,124 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:alloc::sync::Arc<T,A>::make_mut
  1,595,000 ( 0.05%)  crabomination/src/server/bot.rs:crabomination::server::bot::sim_step [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,586,070 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::computed_permanent
  1,569,960 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller
  1,569,616 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::mana::ManaCost::cmc
  1,552,262 ( 0.05%)  crabomination_base/src/card.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  1,546,268 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan
  1,539,840 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::extra_cost_for_spell
  1,532,640 ( 0.05%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::is_empty [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,519,612 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::perform_action_inner
  1,509,496 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::cost_reduction_for_spell_full
  1,492,614 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut
  1,491,113 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:alloc::sync::Arc<T,A>::drop_slow
  1,490,580 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/sync/mutex/futex.rs:crabomination::game::GameState::computed_permanent
  1,482,982 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::board_keyword_in_scope
  1,482,844 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameEvent> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,480,590 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell
  1,478,488 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  1,477,058 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
  1,472,398 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_land
  1,472,398 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_land
  1,465,296 ( 0.05%)  crabomination/src/game/mod.rs:crabomination::game::GameState::continue_trigger_ordering [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,459,612 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::layers::affected_includes_gated
  1,455,182 ( 0.05%)  crabomination/src/game/effects/targeting.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_targets_for_effect_all_slots_kicked [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,444,886 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/map.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  1,429,492 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::Keyword> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,425,072 ( 0.05%)  crabomination/src/game/mod.rs:crabomination::game::GameState::permanents_with_abilities_removed [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,421,622 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::cast_candidates
  1,411,836 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::GameState::frozen_effects
  1,410,778 ( 0.05%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::GameState::dispatch_triggers_for_events
  1,390,918 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  1,389,626 ( 0.04%)  crabomination/src/game/actions.rs:crabomination::game::actions::colored_spell_tax_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,388,682 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::draw_one
  1,374,858 ( 0.04%)  crabomination/src/game/mod.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,370,786 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::computed_permanent
  1,369,698 ( 0.04%)  crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,366,720 ( 0.04%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  1,358,194 ( 0.04%)  crabomination/src/game/actions.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,354,970 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for
  1,352,368 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
  1,343,766 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone
  1,338,096 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
  1,327,959 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,320,416 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::GameState::frozen_effects
  1,318,308 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
  1,305,370 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::trigger_grant_sources
  1,302,548 ( 0.04%)  crabomination/src/game/stack.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,296,212 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
  1,291,684 ( 0.04%)  crabomination/src/game/actions.rs:crabomination::game::actions::cost_reduction_for_spell_full [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,290,016 ( 0.04%)  crabomination_base/src/card.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
  1,289,060 ( 0.04%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,286,242 ( 0.04%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::pay [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,282,632 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/hash/sip.rs:<core::hash::sip::Hasher<S> as core::hash::Hasher>::write [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,282,018 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
  1,279,074 ( 0.04%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,278,780 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs:crabomination::game::GameState::gather_continuous_effects_inner
  1,274,482 ( 0.04%)  crabomination/src/game/mod.rs:crabomination::game::GameState::scale_damage_to [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,271,892 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::has_keyword
  1,268,616 ( 0.04%)  crabomination/src/game/mod.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell::{{closure}}
  1,267,658 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::all_damage_to_player_prevented
  1,264,842 ( 0.04%)  crabomination_base/src/card.rs:crabomination::game::GameState::gather_continuous_effects_inner
  1,263,122 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/alloc/unix.rs:__rustc::__rdl_realloc
  1,257,382 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::relax_cost_colors
  1,255,968 ( 0.04%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,255,484 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::permanent_value
  1,255,228 ( 0.04%)  crabomination/src/server/bot.rs:crabomination::server::bot::sim_spell_action_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,255,110 ( 0.04%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::intrinsic_land_mana_abilities_with [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,248,808 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::draft::DraftNotes> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,248,562 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  1,246,480 ( 0.04%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:alloc::sync::Arc<T,A>::drop_slow
  1,245,600 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::layers::ComputedPermanent> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,236,240 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
  1,234,724 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::draw_one
  1,229,276 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::cost_reduction_for_spell_full
  1,222,494 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,219,506 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
  1,218,082 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::colored_spell_tax_for_spell
  1,216,024 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
  1,206,646 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:core::option::Option<T>::or_else
  1,205,652 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/borrow.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
  1,203,920 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
  1,196,884 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
  1,190,464 ( 0.04%)  crabomination/src/team.rs:crabomination::game::GameState::same_team
  1,187,406 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for
  1,186,986 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::push_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,184,946 ( 0.04%)  crabomination/src/server/bot.rs:crabomination::server::bot::simulate_attack_outcome_once [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,177,074 ( 0.04%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,172,870 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::colored_spell_tax_for_spell
  1,170,874 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::main_phase_action_with
  1,165,896 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::layers::affected_includes_gated
  1,160,848 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::perform_action_inner
  1,158,190 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
  1,155,988 ( 0.04%)  crabomination/src/server/bot.rs:crabomination::server::bot::HeuristicBot::next_action_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,155,688 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
  1,151,576 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::sync::Arc<T,A>::drop_slow'2
  1,145,622 ( 0.04%)  crabomination/src/game/layers.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
  1,141,156 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/take.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
  1,139,324 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::main_phase_action_with
  1,135,802 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
  1,133,548 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
  1,131,656 ( 0.04%)  crabomination/src/game/mod.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
  1,127,034 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::perform_action_inner
  1,124,016 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
  1,123,118 ( 0.04%)  crabomination_base/src/mana.rs:crabomination_base::card::CardDefinition::printed_color_set
  1,122,814 ( 0.04%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,116,760 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::ManaPayload> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,108,816 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,099,944 ( 0.04%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::permanent_value
  1,090,716 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
  1,084,904 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::layers::compute_permanent_pass
  1,078,096 ( 0.03%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:hashbrown::map::HashMap<K,V,S,A>::insert
  1,074,958 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::graveyard_exile_redirects
  1,074,260 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::layers::Modification> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,068,704 ( 0.03%)  crabomination/src/game/mod.rs:crabomination::game::GameState::drain_trigger_queue [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,065,994 ( 0.03%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,059,708 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
  1,059,310 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
  1,055,340 ( 0.03%)  crabomination/src/game/layers.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
  1,049,494 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
  1,045,770 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination_base::card::CardDefinition::printed_color_set
  1,041,884 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/string.rs:<alloc::string::String as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,035,808 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
  1,020,590 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::base_power
  1,020,590 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::base_toughness
  1,018,372 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw
  1,016,576 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:core::option::Option<T>::or_else
  1,011,442 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
  1,006,684 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::perform_action_inner
  1,006,610 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
  1,005,942 ( 0.03%)  crabomination/src/game/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
  1,004,464 ( 0.03%)  crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value [/home/user/Crabomination/target/profiling-fast/bot_ladder]
  1,003,454 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
  1,002,362 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::cast_candidates
  1,001,624 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:alloc::sync::Arc<T,A>::drop_slow'2
  1,000,575 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:alloc::raw_vec::RawVec<T,A>::grow_one
  1,000,575 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:alloc::raw_vec::RawVec<T,A>::grow_one
  1,000,544 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2
    993,720 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sync/poison.rs:crabomination::game::GameState::computed_permanent
    992,884 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::base_power
    992,884 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::base_toughness
    990,344 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_extend.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    982,680 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_uninit.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    982,680 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    981,998 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::game::GameState::dispatch_triggers_for_events
    977,492 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    972,874 ( 0.03%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    969,648 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    966,162 ( 0.03%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    956,262 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::sync::Arc<T,A>::drop_slow
    950,758 ( 0.03%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    950,608 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::layers::compute_permanent_pass
    945,812 ( 0.03%)  /rust/deps/hashbrown-0.16.1/src/map.rs:crabomination::game::GameState::dispatch_triggers_for_events
    943,026 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    935,760 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::effect_produces_color
    932,460 ( 0.03%)  crabomination/src/server/bot.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    931,576 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::GameState::computed_permanent
    929,916 ( 0.03%)  crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    928,204 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    923,294 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardInstance::has_keyword
    923,294 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardInstance::has_keyword
    922,832 ( 0.03%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
    922,760 ( 0.03%)  crabomination/src/draft.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    919,498 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    919,112 ( 0.03%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::spell_kind [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    918,708 ( 0.03%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_land
    916,006 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    914,874 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::computed_permanent
    914,332 ( 0.03%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    912,346 ( 0.03%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    911,808 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::etb_trigger_multiplier
    909,048 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap
    907,064 ( 0.03%)  crabomination_base/src/effect.rs:<crabomination_base::effect::PlayerRef as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    904,554 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell
    898,646 ( 0.03%)  crabomination_base/src/card.rs:crabomination::game::GameState::dispatch_triggers_for_events
    895,146 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:crabomination::game::GameState::gather_continuous_effects_inner
    893,580 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::name_locked_by_a_permanent
    890,490 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/alignment.rs:alloc::sync::arcinner_layout_for_value_layout [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    878,910 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    868,884 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::cost_reduction_for_spell_full
    868,524 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::adjust_life
    864,864 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    864,718 ( 0.03%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_ping [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    864,518 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination_base::effect::abilities::ActivatedAbility::is_free
    862,530 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::blocker_can_block_attacker
    860,522 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2
    858,866 ( 0.03%)  crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    858,540 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::all_damage_to_player_prevented
    857,887 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    854,572 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    854,256 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::GameState::gather_continuous_effects_inner
    852,528 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::printed_color_set
    850,694 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::available_mana
    850,560 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination_base::card::CardInstance::has_keyword
    848,066 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::combat_damage_prevented_from
    846,160 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x
    842,858 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for
    842,184 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::effect_produces_color
    839,200 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::available_mana
    838,266 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    837,382 ( 0.03%)  ./malloc/./malloc/arena.c:calloc
    835,734 ( 0.03%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked::eff_find [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    835,600 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x
    833,094 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::blocker_can_block_attacker
    832,990 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw
    831,644 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    829,716 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::combat_damage_prevented_to_self
    825,744 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    817,586 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    816,266 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::trigger_grant_sources
    815,650 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    815,378 ( 0.03%)  crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    812,692 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::find_card_anywhere_mut
    810,928 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/function.rs:core::ops::function::FnOnce::call_once [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    808,788 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:alloc::sync::Arc<T,A>::drop_slow'2
    807,258 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    806,736 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    805,882 ( 0.03%)  crabomination/src/game/layers.rs:core::ops::function::FnOnce::call_once [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    800,968 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::Vec<T,A> as core::ops::drop::Drop>::drop
    799,502 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    799,474 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::available_mana
    799,312 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::simulate_attack_outcome_once
    798,972 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    798,106 ( 0.03%)  crabomination_base/src/card.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}}
    795,950 ( 0.03%)  crabomination/src/recommend.rs:crabomination::recommend::play_one_game_traced [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    795,590 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::graveyard_exile_redirects
    792,848 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    779,190 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack
    779,166 ( 0.03%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    770,712 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::adjust_life [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    767,268 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::GameState::gather_continuous_effects_inner
    766,320 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::empty_mana_pools [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    756,996 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop
    745,536 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::accumulate_mana_colors
    743,862 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    743,182 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:core::iter::adapters::flatten::and_then_or_clear [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    743,072 ( 0.02%)  /rust/deps/hashbrown-0.16.1/src/map.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    737,612 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    736,216 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::GameState::scale_damage_to
    735,478 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::scale_damage_to
    732,270 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination_base::card::CardInstance::toughness
    726,966 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::available_mana
    721,784 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/alloc.rs:__rustc::__rdl_realloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    721,252 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    720,466 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::relax_cost_colors
    714,094 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    711,760 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    711,272 ( 0.02%)  /rust/deps/hashbrown-0.16.1/src/map.rs:hashbrown::map::HashMap<K,V,S,A>::insert [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    709,128 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell::{{closure}}
    708,786 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    707,068 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    706,912 ( 0.02%)  crabomination/src/game/layers.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    705,640 ( 0.02%)  /rust/deps/hashbrown-0.16.1/src/set.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    705,472 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::LayerFreeze> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    705,146 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    701,820 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::effect_produces_color
    700,104 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    700,031 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/fuse.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    698,218 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
    697,298 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::trigger_grant_sources
    695,266 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::spell_kind
    695,248 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::continue_ability_resolution_x [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    693,278 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2
    691,432 ( 0.02%)  /rust/deps/hashbrown-0.16.1/src/control/tag.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    690,984 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    686,472 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    684,778 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    684,066 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::trigger_grant_sources [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    683,452 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::toughness
    682,692 ( 0.02%)  crabomination_base/src/card.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    680,316 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<[crabomination::game::types::TriggerCandidate]> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    680,316 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller
    680,316 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/alloc/unix.rs:__rustc::__rdl_alloc_zeroed
    679,556 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::etb_trigger_multiplier
    679,430 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::GameState::board_keyword_in_scope
    676,174 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::available_mana
    674,672 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    663,588 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    662,636 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<[(crabomination_base::card::CardId,crabomination_base::effect::Effect,usize,core::option::Option<crabomination_base::effect::Predicate>)]> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    662,314 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    659,724 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::mana::ManaCost::has_x
    659,628 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::counter_drain_cost [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    657,834 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
    656,812 ( 0.02%)  crabomination/src/game/effects/mod.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map
    653,158 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::permanents_with_abilities_removed
    653,136 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::find_card_anywhere
    650,750 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop
    644,252 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
    642,974 ( 0.02%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::base_power
    642,974 ( 0.02%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::base_toughness
    639,520 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::GameState::scale_damage_to
    635,700 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::etb_trigger_multiplier [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    635,456 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::adjust_life
    634,714 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::PlayerRef> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    632,100 ( 0.02%)  crabomination_base/src/card.rs:crabomination::server::bot::pick_blocks_inner
    631,952 ( 0.02%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_graveyard_target [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    631,476 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
    631,212 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::GameState::gather_continuous_effects_inner
    622,590 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:alloc::vec::Vec<T,A>::push_mut
    621,352 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    619,434 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_uninit.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
    617,628 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::combat_damage_prevented_to_self
    617,092 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut
    615,248 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    614,886 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2
    614,856 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::gather_continuous_effects_inner
    613,082 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::dispatch_board_scan
    613,068 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
    612,850 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    612,080 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::graveyard_granted_abilities
    609,126 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    608,608 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/set_len_on_drop.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
    604,828 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::damage_redirect_target
    595,232 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::same_team
    594,936 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::combat_damage_prevented_from
    593,576 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:alloc::sync::Arc<T,A>::clone_from_ref_in
    589,760 ( 0.02%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_sacrifice_value [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    587,826 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size
    586,014 ( 0.02%)  crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    581,474 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    581,302 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    580,136 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    578,612 ( 0.02%)  crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    577,510 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::can_block_attacker_computed
    577,258 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::cast_candidates
    575,728 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
    575,546 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::apply_spell_cost_floor
    573,116 ( 0.02%)  crabomination/src/game/combat.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    572,884 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/function.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    565,664 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    562,608 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::GameState::perform_action_inner
    560,306 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::colored_cost_reduction_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    558,450 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::can_block_attacker_computed [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    553,664 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs:crabomination::game::GameState::dispatch_triggers_for_events
    553,408 ( 0.02%)  crabomination_base/src/mana.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    552,474 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::intrinsic_land_mana_abilities_with
    551,744 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::GameState::perform_action_inner
    551,170 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::blocker_can_block_attacker [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    549,862 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan
    548,914 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    548,080 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_removal_ping
    546,238 ( 0.02%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::is_indestructible [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    546,186 ( 0.02%)  crabomination/src/server/bot.rs:crabomination::server::bot::can_afford_in_state [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    546,158 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::find_card_anywhere
    545,404 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap
    544,758 ( 0.02%)  crabomination/src/game/actions.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    543,248 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    542,596 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::iter::traits::iterator::Iterator>::try_fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    542,530 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::counter_count
    539,700 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker
    534,948 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
    534,814 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    534,436 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
    534,402 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::permanents_with_abilities_removed
    534,378 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation
    534,352 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::drain_trigger_queue
    532,860 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    532,648 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::extra_cost_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    529,868 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::pick_removal_ping
    529,650 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    524,596 ( 0.02%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked::sel_find [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    524,508 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    524,494 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    523,884 ( 0.02%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_destroy [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    523,486 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    521,160 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::name_locked_by_a_permanent
    516,624 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::spell_granted_convoke
    515,530 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    514,416 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::colored_cost_reduction_for_spell
    513,338 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
    513,182 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    512,478 ( 0.02%)  crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    511,512 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs:crabomination::game::GameState::gather_continuous_effects_inner
    507,860 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    506,412 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2
    505,886 ( 0.02%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_sacrifice [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    505,800 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source
    504,638 ( 0.02%)  crabomination_base/src/card.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields
    500,778 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    499,044 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::graveyard_granted_abilities [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    495,156 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/sync/mutex/futex.rs:crabomination::game::GameState::frozen_effects
    495,034 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    493,566 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack
    492,544 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/stable/mod.rs:crabomination::game::layers::compute_permanent_pass
    490,222 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map
    489,946 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::board_keyword_in_scope
    489,456 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::colored_cost_reduction_for_spell
    489,114 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    488,180 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::toughness
    488,120 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::ally_trigger_extra_fires
    483,474 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::dispatch_triggers_for_events
    481,808 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
    480,982 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<core::option::Option<crabomination_base::card::SelectionRequirement>>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    479,756 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::effective_life
    479,708 ( 0.02%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_attacks_inner [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    479,598 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::can_block_attacker_computed
    478,800 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<crabomination::game::layers::AffectedPermanents>
    478,425 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:alloc::raw_vec::RawVecInner<A>::reserve::do_reserve_and_handle
    477,114 ( 0.02%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    475,704 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    475,500 ( 0.02%)  crabomination_base/src/card.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    475,304 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination::game::layers::compute_permanent_pass
    475,068 ( 0.02%)  crabomination/src/game/mod.rs:crabomination::game::GameState::relax_cost_colors_known [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    474,400 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement
    474,006 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    472,524 ( 0.02%)  crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_player [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    469,998 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace
    469,412 ( 0.02%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    464,874 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    462,568 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::process_cumulative_upkeep
    460,002 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    459,650 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    459,354 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    458,740 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
    458,426 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    455,430 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    454,112 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_land_to_play [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    452,962 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    452,062 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    451,560 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
    450,428 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    446,816 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::mana::ManaPool::pay
    446,754 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::server::bot::available_mana
    444,762 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_targets_for_effect_all_slots_kicked
    443,898 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:core::iter::traits::iterator::Iterator::partition [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    442,924 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::spell_kind
    442,340 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::draw_one [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    440,420 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    439,586 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/take.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    435,972 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::can_afford_from [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    431,760 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_planeswalker
    431,760 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_planeswalker
    430,436 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size
    430,016 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    429,534 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    429,522 ( 0.01%)  crabomination/src/game/combat.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    428,256 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    425,984 ( 0.01%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    425,636 ( 0.01%)  crabomination/src/game/types.rs:<crabomination::game::types::GameEvent as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    425,626 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/bool.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    424,812 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::game::GameState::computed_permanent
    424,650 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    421,432 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone
    421,070 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    417,060 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    417,008 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    416,376 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:alloc::sync::Arc<T,A>::drop_slow
    414,656 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::mana::ManaCost::colors
    413,560 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/set_len_on_drop.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    413,550 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:crabomination::game::GameState::perform_action_inner
    412,080 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sync/poison.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    412,080 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/sync/mutex/futex.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    412,006 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/in_place_collect.rs:alloc::vec::in_place_collect::from_iter_in_place [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    408,820 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::colors
    408,708 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::GameState::perform_action_inner
    407,614 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    404,286 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    403,224 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    401,562 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::opponent_has_static [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    400,422 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    398,364 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_from [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    398,116 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    397,604 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields
    397,178 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    393,240 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
    393,168 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::board_keyword_in_scope [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    392,804 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    390,488 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::GameState::scale_damage_to
    390,322 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_attacks_inner
    390,176 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    389,890 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    389,158 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::find_card_anywhere
    387,208 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::GameState::scale_damage_to
    385,066 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    384,474 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::blocker_side_gates_allow_block
    384,192 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    383,634 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:crabomination::game::GameState::gather_continuous_effects_inner
    383,634 ( 0.01%)  crabomination/src/cow.rs:crabomination::game::GameState::gather_continuous_effects_inner
    382,848 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::ally_trigger_extra_fires
    382,000 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_for [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    382,000 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_with [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    379,592 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    378,536 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::extend_trusted [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    377,616 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::base_power
    377,616 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::base_toughness
    377,032 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    376,230 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::extra_cost_for_spell
    374,344 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement
    374,108 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/../../stdarch/crates/core_arch/src/x86/sse2.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    373,500 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::computed_permanent
    371,592 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_stack_response [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    371,296 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_blocks_inner::{{closure}}
    371,204 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<core::iter::adapters::flatten::FlatMap<alloc::vec::into_iter::IntoIter<core::option::Option<usize>>,alloc::vec::Vec<crabomination::game::types::GameAction>,crabomination::server::bot::cast_candidates::{{closure}}::{{closure}}>>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    368,570 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::HeuristicBot::next_action_inner
    367,720 ( 0.01%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::ability_spend_kind [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    366,950 ( 0.01%)  crabomination/src/server/bot.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    365,340 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
    365,136 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    364,964 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::spell_granted_convoke [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    364,382 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::perform_action
    364,214 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    363,974 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    361,440 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    361,416 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
    357,616 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::game::GameState::empty_mana_pools
    357,056 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    356,482 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::GameState::scale_damage_to
    355,668 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    354,586 ( 0.01%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    354,432 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    353,564 ( 0.01%)  ./malloc/./malloc/malloc.c:alloc_perturb [/usr/lib/x86_64-linux-gnu/libc.so.6]
    353,350 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    351,540 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::GameState::perform_action_inner
    350,794 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    350,128 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    350,058 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    349,926 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::simulate_through_combat
    349,334 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::ability_spend_kind
    347,624 ( 0.01%)  crabomination/src/game/effects/mod.rs:crabomination::game::GameState::continue_ability_resolution_x
    346,338 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_creature_spells
    346,302 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::GameState::all_damage_to_player_prevented
    345,400 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::eval_material_inner
    345,192 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    344,498 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::GameState::with_frozen_layers'2
    341,092 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::layers::compute_permanent_pass
    340,574 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::copied::Copied<I> as core::iter::traits::iterator::Iterator>::try_fold
    340,406 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    339,952 ( 0.01%)  crabomination/src/game/effects/targeting.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    338,652 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::append_elements [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    337,896 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    336,824 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
    336,752 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    334,918 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    333,706 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    333,118 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::attack_block_keyword_tax [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    332,764 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    331,794 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    331,530 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone
    331,430 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    330,104 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sync/poison.rs:crabomination::game::GameState::frozen_effects
    329,876 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::spell_kind
    329,560 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/collections/vec_deque/mod.rs:<alloc::collections::vec_deque::VecDeque<T,A> as core::ops::drop::Drop>::drop [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    328,940 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    328,492 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    328,300 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    327,740 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::blocker_side_gates_allow_block [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    325,220 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    324,512 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::cast_candidates
    320,684 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    319,266 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    319,140 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    318,650 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::server::bot::pick_removal_ping
    318,010 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    317,896 ( 0.01%)  crabomination/src/cow.rs:crabomination::game::GameState::dispatch_triggers_for_events
    317,830 ( 0.01%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    317,702 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash
    317,482 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::opponent_has_static
    314,282 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    313,992 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller
    313,992 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    313,992 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/alloc.rs:__rustc::__rdl_alloc_zeroed [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    313,476 ( 0.01%)  crabomination_base/src/card.rs:crabomination::server::bot::permanent_value
    313,212 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::mana::ManaCost::has_x
    312,870 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination_base::card::CardInstance::power
    312,744 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
    311,788 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_reach_burn::reach_amount [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    307,756 ( 0.01%)  crabomination/src/game/combat.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    307,634 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::base_power [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    307,634 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::base_toughness [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    306,560 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    303,832 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:core::slice::sort::shared::smallsort::insert_tail
    303,056 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    302,418 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/map.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    300,556 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::chosen_type_etb_counter_specs
    300,466 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_blocks_inner::{{closure}}
    300,250 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::attacker_damage_value [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    299,221 ( 0.01%)  ./elf/../sysdeps/x86_64/dl-machine.h:_dl_relocate_object
    298,980 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::chosen_type_etb_counter_specs [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    298,106 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_prepare_response [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    297,616 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::same_team
    297,616 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::same_team
    296,991 ( 0.01%)  ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6]
    296,830 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::arcinner_layout_for_value_layout
    296,192 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    295,496 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    295,194 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    294,966 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::continue_spell_resolution [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    293,894 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner::{{closure}}
    293,244 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::GameState::with_frozen_layers'2
    293,168 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::counter_drain_cost
    293,142 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
    292,806 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/int_macros.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
    292,012 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::power
    290,860 ( 0.01%)  crabomination/src/cow.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
    290,746 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_removal_ping
    290,604 ( 0.01%)  crabomination_base/src/card.rs:core::option::Option<T>::or_else
    290,400 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::apply_soulbond_pairing
    290,308 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    290,308 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    290,262 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter_nested.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
    289,552 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    287,860 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::can_block [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    287,860 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::can_block
    287,268 ( 0.01%)  crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::on_left_battlefield [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    287,055 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:alloc::raw_vec::RawVecInner<A>::reserve::do_reserve_and_handle
    286,736 ( 0.01%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::place_card_at_resolved_zone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    286,632 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace
    286,488 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_aura
    286,488 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_aura
    284,860 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2
    284,256 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    284,176 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::printed_color_set
    284,100 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    282,948 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for
    281,794 ( 0.01%)  crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::damage_redirect_target [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    281,008 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::cost_reduction_for_spell_full
    280,890 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::intrinsic_land_mana_abilities_with
    280,252 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::layers::compute_permanent_pass
    277,670 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone
    277,426 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::graveyard_granted_abilities
    277,420 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::GameState::frozen_effects
    276,800 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:core::ptr::drop_in_place<crabomination::game::layers::ComputedPermanent>
    276,800 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:core::ptr::drop_in_place<crabomination::game::layers::ComputedPermanent>
    276,800 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:core::ptr::drop_in_place<crabomination::game::layers::ComputedPermanent>
    276,742 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_attacks_inner
    276,406 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::pick_removal_sacrifice
    274,616 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_self_pump_counter
    274,476 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:crabomination::game::GameState::dispatch_triggers_for_events
    274,180 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::string::String as core::clone::Clone>::clone
    274,024 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source
    273,856 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::GameState::effective_life
    272,850 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::intrinsic_land_mana_abilities_with
    272,850 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    272,292 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::relax_cost_colors
    270,450 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::creature_redirects_damage_to_controller
    269,954 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_to_self [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    269,812 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    268,816 ( 0.01%)  crabomination/src/decision.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    268,184 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone
    267,584 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_damage_computed
    267,346 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::attacker_damage_value
    266,958 ( 0.01%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::reduce_generic [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    266,760 ( 0.01%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::pay_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    266,742 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::relax_cost_colors [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    266,230 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::spell_granted_convoke
    265,934 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    265,068 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    264,280 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    264,012 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::game::can_block_attacker_computed
    263,938 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::apply_umbra_armor
    263,840 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    263,678 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::score_candidate [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    263,358 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::graveyard_exile_redirects [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    263,172 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
    263,000 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::draw_cap_for
    262,528 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::place_land_card
    261,660 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    261,660 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller
    261,570 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    260,656 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_targets
    260,264 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets
    259,888 ( 0.01%)  crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::affordance_probe_template [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    259,590 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
    259,462 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2
    259,232 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_noncreature_spells
    259,024 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/alloc.rs:<hashbrown::raw::RawTable<T,A> as core::clone::Clone>::clone
    258,906 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    258,750 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/shared/smallsort.rs:core::slice::sort::shared::smallsort::insert_tail [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    258,262 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::apply_spell_cost_floor
    256,736 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone
    256,646 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::server::bot::permanent_value
    256,640 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::apply_soulbond_pairing [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    256,116 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_player_triggers
    255,938 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/control/bitmask.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    255,856 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner::{{closure}}
    255,756 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::GameState::gather_continuous_effects_inner
    255,756 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination::game::GameState::gather_continuous_effects_inner
    255,032 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::try_fold
    254,892 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:crabomination::game::layers::compute_permanent_pass
    254,478 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::available_mana
    254,236 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::apply_soulbond_pairing
    254,182 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    254,140 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash
    251,654 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_targets_for_effect_all_slots_kicked
    251,516 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::pick_removal_destroy
    251,320 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    250,780 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    250,704 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::actions::apply_spell_cost_floor
    249,562 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    248,996 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::computed_permanent
    247,882 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    247,664 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::GameState::dispatch_triggers_for_events
    247,054 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::GameState::blocker_can_block_attacker
    247,008 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::noncreature_spell_cast_locked
    246,560 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::simulate_attack_outcome_once
    244,530 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::filter::Filter<I,P> as core::iter::traits::iterator::Iterator>::count
    244,336 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone
    244,302 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap
    243,974 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::main_phase_action_with
    242,788 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::damage_redirect_target
    242,656 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::iter::adapters::flatten::and_then_or_clear
    242,642 ( 0.01%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::can_block
    241,224 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::effect::abilities::TriggeredAbility>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    241,118 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/set_len_on_drop.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    240,964 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::pick_removal_ping
    239,400 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::GameState::gather_continuous_effects_inner
    239,394 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_ability_counter_response [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    239,378 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_sacrifice_value
    238,410 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    238,266 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
    237,972 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::GameState::dispatch_triggers_for_events
    237,398 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/control_flow.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    236,882 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    235,716 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    235,214 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    234,538 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_targets_for_effect_all_slots_kicked
    234,008 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    233,940 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::effect_produces_color
    233,940 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::game::actions::effect_produces_color
    233,712 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::noncreature_spell_cast_locked [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    233,688 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_locked_to_sorcery_timing
    233,034 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    231,648 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::compute_permanents [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    231,392 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    231,136 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    230,972 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_token_maker [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    230,542 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::fire_self_etb_triggers
    230,506 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::cast_candidates
    229,896 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaPool::pay
    229,896 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::empty_mana_pools
    229,474 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::pick_blocks_inner
    228,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    228,636 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::can_afford_in_state
    228,570 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    228,220 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    227,358 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardInstance::can_block
    225,536 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    225,322 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_removal_destroy
    224,628 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::source_exile_draw_pile_for
    224,520 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::main_phase_action_with
    224,340 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::trade_counters_for_damage
    224,070 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    223,868 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::source_damage_to_color_prevented
    223,472 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_creature_triggers
    223,352 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::ManaPayload>
    222,934 ( 0.01%)  crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    222,694 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone
    222,684 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
    222,636 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_instants_or_sorceries
    221,822 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::effective_poison
    221,582 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap
    221,370 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_artifact
    220,724 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::server::bot::pick_blocks_inner
    220,400 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    219,888 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::combat_damage_from_matching_prevented
    219,830 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::chosen_type_etb_counter_specs
    219,614 ( 0.01%)  crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    219,424 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword
    219,344 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<alloc::string::String as core::clone::Clone>::clone
    219,344 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::string::String as core::clone::Clone>::clone
    218,858 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_self_pump_counter [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    218,832 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::crew_cost
    218,430 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::fold
    217,852 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    217,642 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
    217,216 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::draw_cap_for
    217,012 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    216,182 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    216,166 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    215,880 ( 0.01%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_planeswalker
    215,564 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    215,454 ( 0.01%)  crabomination/src/server/bot.rs:core::iter::adapters::flatten::and_then_or_clear
    215,432 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::combat_damage_from_matching_prevented
    215,054 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw
    215,028 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::chosen_type_etb_counter_specs
    214,292 ( 0.01%)  crabomination/src/server/bot.rs:<crabomination::server::bot::HeuristicBot as crabomination::server::bot::Bot>::next_action [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    214,282 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::dying_snapshot
    213,830 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    213,478 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::copied::Copied<I> as core::iter::traits::iterator::Iterator>::try_fold
    212,990 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_token_maker
    212,812 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    212,518 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:alloc::sync::Arc<T,A>::make_mut
    212,436 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode
    212,212 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::process_cumulative_upkeep
    212,034 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::try_fold
    211,872 ( 0.01%)  crabomination/src/player.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
    211,842 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::clamp_damage_to_life_floor
    211,380 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::pay_life_cost [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    211,058 ( 0.01%)  crabomination/src/game/layers.rs:crabomination::game::GameState::blocker_side_gates_allow_block
    210,978 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    210,628 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
    209,614 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::perform_action_inner
    209,368 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::layers::compute_permanent_pass
    209,328 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    209,328 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/set.rs:crabomination::game::GameState::dispatch_triggers_for_events
    208,936 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::try_fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    208,516 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::layers::affected_includes_gated
    208,516 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::layers::affected_includes_gated
    208,516 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::layers::affected_includes_gated
    208,260 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_reach_burn
    207,878 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    206,262 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_player_triggers
    205,710 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
    205,696 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_targets
    205,414 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::perform_action_inner::{{closure}}
    205,392 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::effective_life
    205,324 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::next
    204,998 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_defensive_removal [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    204,940 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
    204,398 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::play_land_with_face
    204,304 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:hashbrown::rustc_entry::<impl hashbrown::map::HashMap<K,V,S,A>>::rustc_entry
    204,284 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut
    203,056 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    202,820 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
    202,716 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    201,834 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
    201,834 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of
    201,612 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/set.rs:<crabomination::game::GameState as core::clone::Clone>::clone
    201,336 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::graveyard_exile_redirects
    201,222 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_creature_triggers
    201,210 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::server::bot::pick_removal_sacrifice
    200,932 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    200,382 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::game::GameState::drain_trigger_queue
    199,962 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere_mut
    199,166 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter_map.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    197,736 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::decision::ScriptedDecider> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    197,420 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::permanent_value
    197,292 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::pick_sacrifice_value
    197,248 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::simulate_attack_outcome_once
    196,888 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_instant
    196,348 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_noncreature_spells
    195,712 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_land_to_play
    195,534 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter_map.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    195,272 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardInstance::toughness
    195,138 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell
    195,092 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::recommend::play_one_game_traced
    194,346 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_reach_burn [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    194,050 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    192,378 ( 0.01%)  ./elf/./elf/do-rel.h:_dl_relocate_object
    192,192 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::slice::sort::shared::smallsort::insert_tail
    192,120 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::actions::apply_spell_cost_floor
    191,952 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::team_of [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    191,768 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::available_mana
    191,576 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::eval_material_inner
    191,498 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    191,370 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:alloc::raw_vec::RawVecInner<A>::reserve::do_reserve_and_handle
    190,530 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::can_afford_in_state
    190,274 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::damage_prevented_by_protection
    189,868 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
    189,640 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:core::ptr::drop_in_place<core::option::Option<alloc::boxed::Box<alloc::vec::Vec<crabomination_base::card::Keyword>>>>
    188,680 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::cost_reduction_for_spell_full
    187,770 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
    187,722 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::power
    187,634 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::attack_block_keyword_tax
    187,372 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    187,058 ( 0.01%)  crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    186,640 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/shared/smallsort.rs:crabomination::game::layers::compute_permanent_pass
    186,384 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::accumulate_mana_colors
    186,384 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::server::bot::accumulate_mana_colors
    186,126 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    186,022 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    185,930 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::draw_one
    185,602 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    185,220 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_under_opponent_control
    184,250 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::strive_cost_for_spell [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    184,094 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::ally_trigger_extra_fires [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    183,940 ( 0.01%)  crabomination_base/src/effect/abilities.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::cmp::PartialEq>::eq [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    183,860 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::ability_spend_kind
    183,860 ( 0.01%)  crabomination/src/game/stack.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability
    183,220 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>>
    183,196 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    183,096 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    182,800 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_source_dealt_damage_watchers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    182,324 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/map.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    182,244 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::place_land_card
    182,196 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
    182,046 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_reach_burn
    181,366 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for
    180,760 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
    180,246 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_removal_sacrifice
    179,734 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_to_graveyard_with_triggers
    178,090 ( 0.01%)  crabomination_base/src/card.rs:crabomination::game::GameState::find_card_anywhere_mut
    178,062 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_land_to_play
    177,840 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode
    177,354 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell
    177,096 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_artifact
    177,096 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_artifact
    176,662 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::dispatch_board_scan
    176,150 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/rustc_entry.rs:hashbrown::rustc_entry::<impl hashbrown::map::HashMap<K,V,S,A>>::rustc_entry [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    176,098 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
    175,918 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::effect_uses_x [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    175,386 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::next
    175,180 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::fold
    174,882 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    174,860 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:alloc::vec::in_place_collect::from_iter_in_place
    174,720 ( 0.01%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_player_triggers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    174,534 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::can_afford_from
    173,564 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::clamp_damage_to_life_floor [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    173,388 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::ward_gate_ok [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    173,386 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::layers::ComputedPermanent>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    172,344 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    172,042 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::adjust_life
    170,578 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    170,244 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::GameState::frozen_effects
    170,112 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut
    169,598 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::player_cannot_gain_life_now
    169,510 ( 0.01%)  crabomination/src/game/actions.rs:crabomination::game::actions::or_pay_cost_symbols [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    169,422 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:crabomination_base::mana::ManaPool::pay
    169,168 ( 0.01%)  ???:0x00000000088fb6b0 [???]
    169,104 ( 0.01%)  crabomination/src/game/mod.rs:crabomination::game::GameState::process_suspend_accelerants [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    169,050 ( 0.01%)  ./malloc/../string/bits/string_fortified.h:_int_realloc
    168,948 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::mana::ManaPool::pay_for_spell
    168,608 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::cost_reduction_for_spell_full
    168,502 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_instants_or_sorceries
    167,982 ( 0.01%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks_inner::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    167,744 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    167,720 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::server::bot::pick_removal_ping
    167,436 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    167,420 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::blocker_side_gates_allow_block
    166,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::server::bot::pick_removal_destroy
    166,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::server::bot::pick_removal_sacrifice
    166,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::server::bot::pick_sacrifice_value
    166,816 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::fire_self_etb_triggers
    166,602 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone
    166,018 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::scale_damage_to
    165,802 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
    165,802 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
    165,770 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::cast_candidates
    165,474 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::ability_spend_kind
    164,928 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup
    164,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    164,808 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::DelayedTrigger>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    163,520 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<core::iter::adapters::filter::Filter<I,P> as core::iter::traits::iterator::Iterator>::count [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    163,186 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::next [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    163,068 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
    162,912 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::draw_cap_for
    162,888 ( 0.01%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:core::ptr::drop_in_place<crabomination::draft::DraftNotes>
    161,844 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::look_instead_of_drawing
    161,550 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::note_creature_death
    161,442 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::trade_counters_for_damage
    161,364 ( 0.01%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    160,822 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
    160,576 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::cost_reduction_for_spell_full
    160,460 ( 0.01%)  crabomination/src/game/mod.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone
    160,216 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_crack_lander
    160,088 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::effective_life
    160,088 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::effective_life
    160,056 ( 0.01%)  crabomination_base/src/mana.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state
    159,880 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::GameState::scale_damage_to
    159,012 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>>
    159,000 ( 0.01%)  crabomination/src/game/combat.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    158,790 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    158,568 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    158,500 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::try_fold
    158,464 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::pick_removal_destroy
    158,420 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    157,854 ( 0.01%)  crabomination/src/player.rs:crabomination::player::PlayerData::send_to_graveyard [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    157,584 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell::{{closure}}
    157,232 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::draw_cap_for
    157,128 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::creature_redirects_damage_to_controller
    156,996 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::continue_trigger_ordering
    156,996 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    156,996 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    156,996 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller
    156,432 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:alloc::vec::Vec<T,A>::push_mut
    156,178 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/shared/smallsort.rs:core::slice::sort::shared::smallsort::insertion_sort_shift_left [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    155,988 ( 0.01%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::apply_umbra_armor
    155,750 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_creature_triggers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    154,858 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_instant
    154,858 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_instant
    154,770 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::card::AdditionalCastCost>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    154,712 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_crack_lander
    154,460 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/fuse.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    153,538 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    153,494 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    153,264 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::empty_mana_pools
    152,982 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    152,760 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::opponents_of [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    152,400 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::decision::DecisionAnswer> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    152,090 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    151,768 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_basic
    150,880 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::noncreature_spell_cast_locked
    150,656 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    150,042 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination_base::mana::ManaCost::cmc
    150,042 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination_base::mana::ManaCost::cmc
    149,942 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_targets [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    149,730 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash
    149,364 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_self_pump_counter
    149,364 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_token_maker
    148,980 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::GameState::draw_one
    148,974 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardInstance::is_indestructible
    148,930 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for
    148,558 ( 0.00%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::distinct_target_count [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    147,784 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_crack_lander [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    147,462 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::GameState::damage_prevented_by_protection
    147,400 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_source_table [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    147,090 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    147,088 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    147,088 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    147,088 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    146,970 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/macros/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    146,850 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    146,732 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::perform_action_inner
    146,622 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/sync/mutex/futex.rs:crabomination::game::GameState::with_frozen_layers'2
    146,368 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::continue_ability_resolution_x
    146,194 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::pick_blocks_inner
    145,432 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::blockers_of [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    145,404 ( 0.00%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    145,288 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    145,184 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    145,170 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::can_afford_in_state
    145,056 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::colored_spell_tax_for_spell
    145,038 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    145,038 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    145,024 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination_base::mana::ManaPool::pay
    145,014 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    144,798 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
    144,620 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    143,748 ( 0.00%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::has_x
    143,710 ( 0.00%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    143,670 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_crew_vehicle [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    143,304 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::source_damage_to_color_prevented
    143,280 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for
    142,912 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::has_first_strikers::{{closure}}::{{closure}}
    142,592 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::trade_counters_for_damage [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    142,520 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::graveyard_granted_abilities
    142,446 ( 0.00%)  crabomination_base/src/turn_step.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
    142,170 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::GameState::blocker_side_gates_allow_block
    142,056 ( 0.00%)  crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_on_card [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    141,618 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    141,600 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:alloc::sync::Arc<T,A>::drop_slow'2
    141,460 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x
    141,212 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_creature_spells
    141,120 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone
    140,644 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::GameState::combat_damage_prevented_to_self
    140,480 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:crabomination::server::bot::cast_candidates
    140,364 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    140,256 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    139,796 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    139,374 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    139,348 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    139,158 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step'2
    138,866 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword
    137,964 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::recommend::play_one_game_traced
    137,946 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::main_phase_action_with
    137,118 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
    136,928 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::effective_life
    136,928 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:crabomination::game::GameState::effective_life
    136,528 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::fire_self_etb_triggers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    135,798 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_creature_spells [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    135,792 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    135,688 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
    135,536 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::pick_removal_sacrifice
    135,494 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::source_exile_draw_pile_for
    135,212 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_turn_face_up [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    135,194 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_card_draw_ability
    135,004 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::look_instead_of_drawing
    134,870 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
    134,440 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState>
    134,416 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace
    134,408 ( 0.00%)  crabomination/src/decision.rs:<crabomination::decision::AutoDecider as crabomination::decision::Decider>::kind [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    134,224 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    134,104 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::GameState::dispatch_triggers_for_events
    134,010 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::blocker_matching_restriction_bars
    133,588 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::drain_trigger_queue
    133,588 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::drain_trigger_queue
    133,508 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::play_land_with_face [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    133,016 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
    132,980 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    132,960 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::CardDefinition> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    132,802 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::damage_prevented_by_protection
    132,564 ( 0.00%)  crabomination/src/game/mod.rs:<core::iter::adapters::filter::Filter<I,P> as core::iter::traits::iterator::Iterator>::count
    131,880 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::apply_fading_vanishing_etb
    131,300 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw
    131,144 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::life_gain_becomes_draw_now
    130,948 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_copies [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    130,792 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
    130,240 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    130,158 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    130,036 ( 0.00%)  crabomination/src/game/actions.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
    129,912 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size
    129,860 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/hash/sip.rs:crabomination::game::stack::<impl crabomination::game::GameState>::loop_fingerprint
    129,814 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
    129,704 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination_base::card::CardInstance::clear_end_of_turn_effects
    129,440 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:alloc::vec::Vec<T,A>::extend_trusted
    129,332 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers::{{closure}}
    129,070 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    128,520 ( 0.00%)  crabomination/src/game/effects/mod.rs:crabomination::game::effects::EffectContext::for_ability [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    128,510 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_to_graveyard_with_triggers
    128,368 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone
    128,338 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    128,270 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::game::GameState::blocker_can_block_attacker
    128,234 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::simulate_through_combat
    128,072 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:crabomination::game::GameState::continue_ability_resolution_x
    127,826 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::pick_blocks_inner
    127,510 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    127,134 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:crabomination::server::bot::permanent_value
    127,046 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
    127,020 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::server::bot::can_afford_in_state
    127,020 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::game::GameState::with_frozen_layers'2
    126,804 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::next
    126,804 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::effective_poison [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    126,752 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
    126,736 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::GameState::effective_poison
    126,632 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::actions::ally_trigger_extra_fires
    126,562 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::name_locked_by_a_permanent
    126,432 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::affordance_probe_template
    126,432 ( 0.00%)  crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::spliceable_hand_cards_on [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    126,336 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::prevent_combat_to_target [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    125,932 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::dying_snapshot
    125,660 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    125,660 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    125,660 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
    125,476 ( 0.00%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::requires_target [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    125,118 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::server::bot::attacker_damage_value
    125,102 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::reduce_generic
    124,744 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
    124,498 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    124,418 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    124,322 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::actions::ally_trigger_extra_fires
    123,480 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_under_opponent_control
    122,918 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::opponent_has_static
    122,362 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_combat_trick [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    121,898 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
    121,626 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack
    121,552 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination::server::bot::pick_removal_ping
    121,162 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    120,680 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:crabomination::game::layers::compute_permanent_pass
    120,576 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::effective_ring_bearer [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    120,517 ( 0.00%)  ./malloc/./malloc/arena.c:_int_free_maybe_consolidate
    120,432 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::extra_cost_for_spell
    120,178 ( 0.00%)  crabomination/src/fxhash.rs:hashbrown::map::HashMap<K,V,S,A>::insert
    120,134 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:alloc::vec::in_place_collect::from_iter_in_place
    119,792 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::GameState::apply_soulbond_pairing
    119,722 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::clamp_damage_to_life_floor
    119,700 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:core::ptr::drop_in_place<crabomination::game::layers::AffectedPermanents>
    119,700 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:core::ptr::drop_in_place<crabomination::game::layers::AffectedPermanents>
    119,576 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::actions::etb_trigger_multiplier
    119,490 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_card_draw_ability
    119,020 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_opponent_graveyard_watchers [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    118,884 ( 0.00%)  crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    118,828 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
    118,630 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::GameState::with_frozen_layers
    118,556 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<crabomination::game::GameState::with_frozen_layers::Unfreeze as core::ops::drop::Drop>::drop
    118,482 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_crew_vehicle
    118,482 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_sacrifice_value
    118,246 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash
    118,048 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    117,920 ( 0.00%)  crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::reduce_by_cost [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    117,752 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::block_barred_by_protection_filter
    117,470 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::iter::adapters::flatten::FlatMap<core::option::IntoIter<alloc::sync::Arc<crabomination::game::layers::ComputedPermanent>>,alloc::vec::Vec<crabomination_base::card::Keyword>,crabomination::game::GameState::damage_from_source_prevented_by_keyword::{{closure}}>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    117,062 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::server::bot::simulate_attack_outcome_once
    116,680 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/borrow.rs:crabomination::server::bot::pick_removal_ping
    116,592 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::permanent_has_keyword [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    116,460 ( 0.00%)  crabomination/src/game/effects/movement.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
    116,186 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::effective_poison
    116,130 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::abilities::ActivatedAbility>
    115,696 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
    115,672 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement
    115,672 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement
    115,596 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state
    115,284 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardDefinition::is_sorcery
    115,230 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static
    115,094 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect
    114,938 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2
    114,936 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone
    114,854 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::process_cumulative_upkeep
    114,688 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::server::bot::pick_sacrifice_value
    114,318 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::server::bot::can_afford_in_state
    114,318 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::can_afford_in_state
    114,318 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::server::bot::can_afford_in_state
    114,318 ( 0.00%)  crabomination/src/game/affordances.rs:crabomination::server::bot::can_afford_in_state
    114,304 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::iter::adapters::flatten::Flatten<alloc::vec::into_iter::IntoIter<alloc::vec::Vec<(crabomination_base::card::CardId,crabomination_base::effect::Effect,usize,core::option::Option<crabomination_base::effect::Predicate>,bool)>>>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    114,214 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
    113,826 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_basic
    113,826 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_basic
    113,588 ( 0.00%)  crabomination/src/game/effects/movement.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
    113,414 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
    112,992 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    112,986 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::<impl crabomination::game::GameState>::apply_as_enters_effect
    112,986 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::effects::<impl crabomination::game::GameState>::apply_enters_as_copy
    112,508 ( 0.00%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
    112,284 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::ops::drop::Drop>::drop
    111,556 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_aura
    111,404 ( 0.00%)  crabomination_base/src/mana.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    111,128 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:alloc::vec::Vec<T,A>::extend_trusted
    111,018 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    110,880 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::new [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    110,124 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::GameState::damage_prevented_by_protection
    110,004 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/bool.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner
    110,004 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs:core::iter::adapters::flatten::FlattenCompat<I,U>::iter_fold
    109,776 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for
    109,530 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
    109,180 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    109,140 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    109,140 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    109,066 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x
    109,018 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}}
    108,836 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
    108,636 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap
    108,506 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardInstance::counter_count
    108,506 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardInstance::counter_count
    108,506 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::counter_count
    108,364 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::main_phase_action_with
    108,066 ( 0.00%)  crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    107,940 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    107,246 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::server::bot::eval_material_inner
    107,064 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::can_afford_in_state
    106,992 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::GameState::combat_damage_prevented_from
    106,944 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/range.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
    106,800 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    106,568 ( 0.00%)  crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::return_persist_undying [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    106,546 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
    106,086 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination::game::GameState::dispatch_triggers_for_events
    105,936 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority
    105,712 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/fuse.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter'2
    105,616 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::mana::ManaCost::colors
    105,544 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>>
    105,466 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::simulate_through_combat
    104,932 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::server::bot::pick_removal_ping
    104,872 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
    104,664 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::continue_trigger_ordering
    104,664 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::GameState::push_ordered_trigger_candidates
    104,664 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/stable/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events
    104,384 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
    104,306 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size
    104,212 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::apply_umbra_armor [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    103,940 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs:<core::iter::adapters::chain::Chain<A,B> as core::iter::traits::iterator::Iterator>::fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    103,924 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:alloc::sync::Arc<T,A>::drop_slow
    103,526 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_aura
    103,180 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::apply_spell_cost_floor [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    102,848 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<core::option::Option<crabomination_base::card::SelectionRequirement>>>
    102,848 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation
    102,764 ( 0.00%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:crabomination::server::bot::simulate_attack_outcome_once
    102,708 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs:crabomination::server::bot::available_mana
    102,352 ( 0.00%)  crabomination/src/game/effects/movement.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner
    101,932 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_removal_destroy
    101,932 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::pick_removal_sacrifice
    101,794 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::server::bot::pick_turn_face_up
    101,616 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::server::bot::can_afford_from
    101,448 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs:crabomination::game::GameState::with_frozen_layers
    101,358 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with
    101,220 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter_map.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
    101,134 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers
    100,968 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::player_cannot_gain_life_now
    100,932 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::reset_case [/home/user/Crabomination/target/profiling-fast/bot_ladder]
    100,760 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
    100,716 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
    100,482 ( 0.00%)  crabomination/src/game/mod.rs:core::ops::function::impls::<impl core::ops::function::FnMut<A> for &mut F>::call_mut
    100,376 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::note_creature_death
    100,136 ( 0.00%)  crabomination/src/team.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone
     99,934 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_control [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     99,568 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::pick_attacks_inner
     99,470 ( 0.00%)  crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::is_adapt [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     99,316 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::is_indestructible
     99,176 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::process_cumulative_upkeep
     99,062 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:<core::iter::adapters::filter_map::FilterMap<I,F> as core::iter::traits::iterator::Iterator>::next
     98,644 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/take.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
     98,468 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::place_land_card [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     98,426 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
     98,376 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::recommend::play_one_game_traced
     98,336 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::server::bot::cast_candidates
     97,636 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardInstance::toughness
     97,434 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::GameState::process_cumulative_upkeep
     97,380 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:crabomination_base::mana::ManaCost::colors
     97,088 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_source_table
     96,976 ( 0.00%)  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs:hashbrown::rustc_entry::<impl hashbrown::map::HashMap<K,V,S,A>>::rustc_entry
     96,928 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_instant_speed [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     96,786 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::server::bot::HeuristicBot::next_action_inner
     96,510 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::GameState::dispatch_board_scan
     96,240 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::resolve_zone_change [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     96,152 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/map.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions
     96,118 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     96,060 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::apply_spell_cost_floor
     95,828 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
     95,685 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:alloc::raw_vec::RawVecInner<A>::reserve::do_reserve_and_handle
     95,496 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_aura [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     95,414 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement>'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     95,140 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::server::bot::attacker_damage_value
     95,058 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::reduce_generic
     95,036 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::with_frozen_layers'2
     94,786 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::play_land_with_face
     94,710 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
     94,534 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::sim_spell_action_inner
     94,508 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_locked_to_sorcery_timing
     94,484 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
     93,884 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::layers::compute_permanent_pass
     93,576 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<(usize,crabomination_base::card::SelectionRequirement)>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     93,576 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_from_matching_prevented [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     93,466 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::armed_attack_mandate_for [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     93,368 ( 0.00%)  crabomination_base/src/effect.rs:<crabomination_base::effect::Value as core::clone::Clone>::clone [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     93,284 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any
     93,194 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::server::bot::pick_removal_ping
     92,944 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::life_gain_becomes_draw_now
     92,880 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::find_card_anywhere_mut [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     92,840 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_damage_computed
     92,668 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
     92,536 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner::{{closure}}
     91,962 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_locked_to_sorcery_timing [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     91,780 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::has_first_strikers::{{closure}}::{{closure}} [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     91,706 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardDefinition::is_sorcery
     91,706 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination_base::card::CardDefinition::is_sorcery
     91,660 ( 0.00%)  crabomination_base/src/card.rs:crabomination::server::bot::pick_blocks_inner::{{closure}}
     91,624 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::GameState::effective_life
     91,560 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<(alloc::vec::Vec<crabomination_base::card::CounterType>,u32,crabomination_base::card::SelectionRequirement)>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     91,536 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets
     91,312 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::affordance_probe_template
     90,880 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     90,792 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
     90,762 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<core::iter::adapters::flatten::FlatMap<I,U,F> as core::iter::traits::iterator::Iterator>::next
     90,460 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::server::bot::main_phase_action_with
     90,316 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
     90,312 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/exact_size.rs:<alloc::vec::Vec<T,A1> as alloc::vec::spec_extend::SpecExtend<T,alloc::vec::into_iter::IntoIter<T,A2>>>::spec_extend
     90,223 ( 0.00%)  ???:__rustc::__rust_realloc [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     90,038 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::server::bot::pick_card_draw_ability [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     89,882 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
     89,718 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::life_gain_becomes_draw_now [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     89,486 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::GameState::combat_damage_prevented_from
     89,472 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::name_locked_by_a_permanent [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     89,084 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardInstance::record_damage_from [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     89,000 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword
     88,946 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<alloc::boxed::Box<crabomination::game::ResolvingSpell>>> [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     88,920 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state
     88,914 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::GameState::relax_cost_colors
     88,802 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode
     88,696 ( 0.00%)  crabomination_base/src/mana.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::fold
     88,548 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_artifact
     88,476 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs:<core::hash::sip::Hasher<S> as core::hash::Hasher>::write
     88,432 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::stack::<impl crabomination::game::GameState>::advance_step
     87,902 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::main_phase_action_with
     87,658 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs:crabomination::server::bot::pick_sacrifice_value
     87,524 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/copied.rs:<core::iter::adapters::copied::Copied<I> as core::iter::traits::iterator::Iterator>::try_fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     87,326 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::fold [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     87,010 ( 0.00%)  crabomination/src/server/bot.rs:<alloc::vec::into_iter::IntoIter<T,A> as core::iter::traits::iterator::Iterator>::try_fold
     86,876 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::can_afford_from
     86,802 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:hashbrown::raw::RawTable<T,A>::reserve_rehash
     86,772 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers
     86,616 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::route_to_graveyard [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     86,564 ( 0.00%)  crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone'2 [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     86,496 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination_base::card::CardDefinition::spell_kind
     86,460 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination_base::card::CardDefinition::spell_kind
     86,314 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat
     86,292 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::source_exile_draw_pile_for [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     86,208 ( 0.00%)  crabomination/src/game/layers.rs:crabomination::game::GameState::damage_prevented_by_protection
     86,035 ( 0.00%)  ./elf/./elf/dl-tunables.c:__GI___tunables_init [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
     85,912 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
     85,802 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::server::bot::pick_blocks_inner
     85,728 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers
     85,704 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::KeywordCounters::clear [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     85,404 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::on_left_battlefield
     85,398 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size
     84,760 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::server::bot::pick_blocks_inner::{{closure}}
     84,744 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:<alloc::collections::vec_deque::VecDeque<T,A> as core::ops::drop::Drop>::drop
     84,732 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     84,732 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::has_x
     84,660 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::creature_redirects_damage_to_controller [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     84,582 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs:crabomination::game::actions::ally_trigger_extra_fires
     84,552 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:core::iter::traits::iterator::Iterator::partition
     84,552 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast
     84,552 ( 0.00%)  crabomination/src/game/actions.rs:crabomination::game::actions::converge_count
     84,288 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:alloc::vec::Vec<T,A>::extend_trusted
     84,116 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::iter::adapters::chain::and_then_or_clear [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     84,060 ( 0.00%)  crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_instant
     84,024 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell
     83,852 ( 0.00%)  crabomination/src/game/mod.rs:crabomination::game::GameState::look_instead_of_drawing [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     83,696 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner
     83,656 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/cloned.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter_nested::SpecFromIterNested<T,I>>::from_iter
     83,546 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks
     83,440 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::server::bot::pick_removal_destroy
     83,440 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::server::bot::pick_removal_sacrifice
     83,440 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs:crabomination::server::bot::pick_sacrifice_value
     83,432 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::card::CardInstance::power
     83,006 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:crabomination::game::GameState::notion_thief_for_draw
     82,788 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::extend_desugared [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     82,550 ( 0.00%)  crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_damage_computed [/home/user/Crabomination/target/profiling-fast/bot_ladder]
     82,536 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded
     82,420 ( 0.00%)  crabomination/src/server/bot.rs:crabomination::game::GameState::with_frozen_layers
     82,348 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:core::ptr::drop_in_place<crabomination::game::LayerFreeze>
     82,330 ( 0.00%)  crabomination_base/src/card.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
     82,198 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land
     82,060 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::score_candidate
     81,962 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
     81,920 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::apply_umbra_armor
     81,818 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs:<core::iter::adapters::map::Map<I,F> as core::iter::traits::iterator::Iterator>::next
     81,652 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs:crabomination_base::card::CardDefinition::spell_kind
     81,622 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::apply_fading_vanishing_etb
     81,158 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke
     81,152 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T> as alloc::vec::spec_from_iter::SpecFromIter<T,I>>::from_iter
     81,096 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target
     81,016 ( 0.00%)  crabomination_base/src/mana.rs:crabomination_base::card::CardDefinition::spell_kind
     81,000 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode
     80,976 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
     80,976 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
     80,976 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
     80,976 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect
     80,418 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap
     80,390 ( 0.00%)  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/effects/eval.rs
--------------------------------------------------------------------------------
Ir                 

-- line 127 ----------------------------------------
        .                                   matches!(s.effect, crate::effect::StaticEffect::DevotionBonus)
        .                               })
        .                           })
        .                           .count() as i32
        .                   };
        .                   pips + bonus
        .               }
        .           
   14,872 ( 0.00%)      pub(crate) fn evaluate_value(&self, v: &Value, ctx: &EffectContext) -> i32 {
    6,760 ( 0.00%)          match v {
        .                       Value::HalfLibrarySizeRoundedUp(who) => self
        .                           .resolve_player(who, ctx)
        .                           .map(|p| ((self.players[p].library.len() as i32) + 1) / 2)
        .                           .unwrap_or(0),
        .                       Value::HalfLifeRoundedUp(who) => self
        .                           .resolve_player(who, ctx)
        .                           .map(|p| (self.players[p].life.max(0) + 1) / 2)
        .                           .unwrap_or(0),
-- line 144 ----------------------------------------
-- line 179 ----------------------------------------
        .                               self.players[p]
        .                                   .graveyard
        .                                   .iter()
        .                                   .map(|c| c.definition.cost.cmc() as i32)
        .                                   .max()
        .                                   .unwrap_or(0)
        .                           })
        .                           .unwrap_or(0),
    1,258 ( 0.00%)              Value::Const(n) => *n,
        .                       Value::CountOf(s) => self.resolve_selector(s, ctx).len() as i32,
        .                       Value::PartyCount => {
        .                           use crate::card::{CreatureType as CT, Keyword};
        .                           let roles = [CT::Cleric, CT::Rogue, CT::Warrior, CT::Wizard];
        .                           // Per creature you control, which of the four roles it can fill
        .                           // (a Changeling fills all — CR 702.73). One creature fills at
        .                           // most one slot, so party size is a max bipartite matching.
        .                           let creatures: Vec<[bool; 4]> = self
-- line 195 ----------------------------------------
-- line 257 ----------------------------------------
        .                           .map(|id| self.blocker_count_of(id) as i32)
        .                           .sum(),
        .                       Value::CreaturesBlockedBy(s) => self
        .                           .resolve_selector(s, ctx)
        .                           .iter()
        .                           .filter_map(|e| e.as_permanent_id())
        .                           .map(|id| self.attackers_blocked_by(id).len() as i32)
        .                           .sum(),
      470 ( 0.00%)              Value::PowerOf(s) => self.resolve_selector(s, ctx).iter()
   25,482 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (94x)
        .                           .filter_map(|e| {
        .                               // `as_card_id` (not `as_permanent_id`): a dies-trigger
        .                               // subject arrives as `EntityRef::Card` once the creature
        .                               // is in the graveyard (Anax's "if its power was 4+").
        .                               let cid = e.as_card_id()?;
        .                               // CR 603.10 — a leaves-battlefield trigger ("when this
        .                               // dies, deals damage equal to its power") reads the
        .                               // dying object's last-known power, counters/pumps
-- line 273 ----------------------------------------
-- line 282 ----------------------------------------
        .                               // hand for cards that have changed zones but whose
        .                               // power is still being read (e.g. Lorehold
        .                               // Excavation's "X = its power" rider where the
        .                               // target is in graveyard at evaluation time, before
        .                               // it gets exiled). Non-battlefield zones return the
        .                               // printed power from `CardDefinition.power` since
        .                               // counters don't apply off the battlefield.
        .                               if let Some(c) = self.battlefield_find(cid) {
       76 ( 0.00%)                          return Some(c.power());
    4,636 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (76x)
        .                               }
        .                               if let Some(c) = self.exile.iter().find(|c| c.id == cid) {
        2 ( 0.00%)                          return Some(c.definition.power);
        .                               }
       32 ( 0.00%)                      for p in &self.players {
        .                                   if let Some(c) = p.graveyard.iter().find(|c| c.id == cid) {
        .                                       return Some(c.definition.power);
        .                                   }
        .                                   if let Some(c) = p.hand.iter().find(|c| c.id == cid) {
        .                                       return Some(c.definition.power);
        .                                   }
        .                               }
        .                               None
-- line 303 ----------------------------------------
-- line 1454 ----------------------------------------
        .                       Value::IfPred { pred, then, else_ } => {
        .                           if self.evaluate_predicate(pred, ctx) {
        .                               self.evaluate_value(then, ctx)
        .                           } else {
        .                               self.evaluate_value(else_, ctx)
        .                           }
        .                       }
        .                   }
   12,262 ( 0.00%)      }
        .           
        .               /// CR 702.43 — distinct basic land types among lands `seat` controls (0–5).
        .               pub(crate) fn domain_count(&self, seat: usize) -> usize {
        .                   use crate::card::LandType::*;
        .                   [Plains, Island, Swamp, Mountain, Forest]
        .                       .into_iter()
        .                       .filter(|lt| {
        .                           self.battlefield.iter().any(|c| {
-- line 1470 ----------------------------------------
-- line 1491 ----------------------------------------
        .                                   Some(&**card)
        .                               }
        .                               _ => None,
        .                           })
        .                       })
        .                       .map(|c| c.definition.printed_colors())
        .               }
        .           
    1,710 ( 0.00%)      pub fn evaluate_predicate(&self, p: &Predicate, ctx: &EffectContext) -> bool {
    1,140 ( 0.00%)          match p {
        .                       Predicate::True => true,
        .                       Predicate::False => false,
        .                       Predicate::Not(q) => !self.evaluate_predicate(q, ctx),
        .                       Predicate::All(qs) => qs.iter().all(|q| self.evaluate_predicate(q, ctx)),
        .                       Predicate::Any(qs) => qs.iter().any(|q| self.evaluate_predicate(q, ctx)),
        .                       Predicate::SelectorExists(s) => !self.resolve_selector(s, ctx).is_empty(),
        .                       Predicate::SelectorCountAtLeast { sel, n } => {
        .                           self.resolve_selector(sel, ctx).len() as i32 >= self.evaluate_value(n, ctx)
-- line 1508 ----------------------------------------
-- line 1610 ----------------------------------------
        .                       Predicate::ActivePlayerControls(sel) => self
        .                           .resolve_selector(sel, ctx)
        .                           .into_iter()
        .                           .filter_map(|e| e.as_permanent_id())
        .                           .any(|cid| {
        .                               self.battlefield_find(cid).map(|c| c.controller) == Some(self.active_player_idx)
        .                           }),
        .                       Predicate::CurrentStepIs(step) => self.step == *step,
      380 ( 0.00%)              Predicate::EntityMatches { what, filter } => self
      950 ( 0.00%)                  .resolve_selector(what, ctx)
   76,099 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (190x)
        .                           .into_iter()
      380 ( 0.00%)                  .all(|e| match e {
        .                               EntityRef::Permanent(cid) | EntityRef::Card(cid) => {
    1,710 ( 0.00%)                          self.evaluate_requirement_static(filter, &Target::Permanent(cid), ctx.controller, ctx.source)
   86,358 ( 0.00%)  => crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (186x)
        .                               }
        .                               EntityRef::Player(_) => matches!(filter, SelectionRequirement::Player),
        .                           }),
        .                       Predicate::EntityMatchesAny { what, filter } => self
        .                           .resolve_selector(what, ctx)
        .                           .into_iter()
        .                           .any(|e| match e {
        .                               EntityRef::Permanent(cid) | EntityRef::Card(cid) => {
-- line 1631 ----------------------------------------
-- line 1846 ----------------------------------------
        .                                           self.evaluate_requirement_on_card(filter, c, c.controller)
        .                                       })
        .                                   })
        .                           })
        .                       }
        .                       Predicate::SourceIsCreature => ctx
        .                           .source
        .                           .and_then(|cid| self.computed_permanent(cid))
       16 ( 0.00%)                  .map(|c| c.card_types.contains(&crate::card::CardType::Creature))
        .                           .unwrap_or(false),
        .                       Predicate::SourceSaddled => ctx
        .                           .source
        .                           .and_then(|cid| self.battlefield.iter().find(|c| c.id == cid))
        .                           .map(|c| c.saddled)
        .                           .unwrap_or(false),
        .                       Predicate::SourceCastFromEscape => ctx
        .                           .source
-- line 1862 ----------------------------------------
-- line 2941 ----------------------------------------
        .                           // and self-pumps.
        .                           let (p, t) = self
        .                               .computed_permanent(source_id)
        .                               .map(|cp| (cp.power, cp.toughness))
        .                               .unwrap_or_else(|| (source_card.power(), source_card.toughness()));
        .                           (mana_spent as i32 > p) || (mana_spent as i32 > t)
        .                       }
        .                   }
    1,710 ( 0.00%)      }
        .           
        .               // ── Requirement evaluation (unchanged API) ──────────────────────────────
        .           
        .               /// Last-known-information-aware power of `src`: its live battlefield power
        .               /// if present, else the `leaves_bf_lki` snapshot of a just-died source.
        .               /// Powers source-power-relative filters that resolve in a death trigger.
        .               pub(crate) fn source_power_lki(&self, src: CardId) -> Option<i32> {
        .                   if let Some(c) = self.battlefield_find(src) {
-- line 2957 ----------------------------------------
-- line 3050 ----------------------------------------
        .                       };
        .                       if let Some(seat) = defender {
        .                           seats.insert(seat);
        .                       }
        .                   }
        .                   seats.len() as u32
        .               }
        .           
1,515,920 ( 0.05%)      pub fn evaluate_requirement_static(
        .                   &self,
        .                   req: &SelectionRequirement,
        .                   target: &Target,
        .                   controller: usize,
        .                   source: Option<CardId>,
        .               ) -> bool {
        .                   use SelectionRequirement as R;
  909,552 ( 0.03%)          match req {
        .                       R::Any => true,
        .                       R::Player => matches!(target, Target::Player(_)),
        .                       // Fire and Brimstone — "target player who attacked this turn".
        .                       R::PlayerAttackedThisTurn => {
        .                           matches!(target, Target::Player(p) if self.players[*p].attacked_this_turn)
        .                       }
        .                       R::OpponentPlayer => {
        .                           matches!(target, Target::Player(p) if !self.same_team(*p, controller))
-- line 3074 ----------------------------------------
-- line 3081 ----------------------------------------
        .                           }
        .                           let diff = if *fewer {
        .                               self.player_tally(controller, *what) - self.player_tally(*p, *what)
        .                           } else {
        .                               self.player_tally(*p, *what) - self.player_tally(controller, *what)
        .                           };
        .                           diff >= *by as i64
        .                       }
   39,942 ( 0.00%)              R::And(a, b) => self.evaluate_requirement_static(a, target, controller, source)
8,808,893 ( 0.28%)  => crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2 (9,546x)
   10,548 ( 0.00%)                  && self.evaluate_requirement_static(b, target, controller, source),
        .                       R::Or(a, b) => self.evaluate_requirement_static(a, target, controller, source)
        .                           || self.evaluate_requirement_static(b, target, controller, source),
    3,896 ( 0.00%)              R::Not(inner) => !self.evaluate_requirement_static(inner, target, controller, source),
   82,044 ( 0.00%)  => crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static'2 (190x)
        .                       R::ControlledByYou => match target {
        .                           // A `Target::Permanent` can also address a spell on the stack
        .                           // (a "copy target spell you control" ability); its caster is
        .                           // its controller. When the object has left the battlefield
        .                           // (a die-trigger reading "a creature you control dies" off a
        .                           // graveyard source), fall back to the CR 603.10 last-known
        .                           // controller in `died_card_snapshots`.
        .                           Target::Permanent(cid) => self
-- line 3101 ----------------------------------------
-- line 3250 ----------------------------------------
        .                               Target::Permanent(cid) => {
        .                                   self.battlefield_find(*cid).map(|c| c.controller).unwrap_or(controller)
        .                               }
        .                               Target::Player(p) => *p,
        .                           };
        .                           self.players[owner].poison_counters >= 3
        .                       }
        .                       _ => {
  276,544 ( 0.01%)                  let Target::Permanent(cid) = target else { return false; };
        .                           // Look on the battlefield first; fall through to graveyards,
        .                           // exile, and the stack so reanimate-style spells (Goryo's
        .                           // Vengeance, Reanimate, Animate Dead) can validate their
        .                           // targets, and so counter-style spells (Mystical Dispute,
        .                           // Force of Negation) can read the colors of a target stack
        .                           // spell.
  165,320 ( 0.01%)                  let stack_card = self.stack.iter().find_map(|si| match si {
   14,516 ( 0.00%)                      StackItem::Spell { card, .. } if card.id == *cid => Some(&**card),
        .                               _ => None,
        .                           });
        .                           let card = self
  133,156 ( 0.00%)                      .battlefield_find(*cid)
        .                               // Dies-trigger filters (Felisa's "with a +1/+1 counter on
        .                               // it") read the dying object's last-known battlefield
        .                               // state, not the counter-stripped graveyard copy
        .                               // (CR 603.10 LKI; CR 122.2 cleared the counters on the
        .                               // zone change). Consulted before the terminal zones; the
        .                               // cache only holds cards from the in-flight die batch.
        .                               .or_else(|| self.died_card_snapshots.get(cid))
  532,624 ( 0.02%)                      .or_else(|| {
  577,154 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::option::Option<T>::or_else (122,570x)
        .                                   self.resolving_lki_source
        .                                       .filter(|s| s == cid)
        .                                       .and_then(|_| self.leaves_bf_lki.get(cid))
        .                               })
  665,780 ( 0.02%)                      .or_else(|| self.players.iter().find_map(|p| p.graveyard.iter().find(|c| c.id == *cid)))
3,098,326 ( 0.10%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::option::Option<T>::or_else (122,570x)
        .                               .or_else(|| self.exile.iter().find(|c| c.id == *cid))
        .                               .or(stack_card)
        .                               // Library / hand: needed by "look at top of library"
        .                               // predicates (Lurking Predators: "if it's a creature
        .                               // card, …"), discard-from-hand pickers, and any future
        .                               // hidden-zone filter check. Library cards have hidden
        .                               // info for opponents in real play, but the engine is
        .                               // permission-checked at the call site (effects target
        .                               // the controller's own library).
  665,780 ( 0.02%)                      .or_else(|| self.players.iter().find_map(|p| p.library.iter().find(|c| c.id == *cid)))
  490,280 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::option::Option<T>::or_else (122,570x)
  532,624 ( 0.02%)                      .or_else(|| self.players.iter().find_map(|p| p.hand.iter().find(|c| c.id == *cid)));
  490,280 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:core::option::Option<T>::or_else (122,570x)
  266,312 ( 0.01%)                  let Some(card) = card else { return false; };
        .                           // Layer-4-aware card types for battlefield permanents
        .                           // (CR 613.2): an artifact-ized creature (Phyrexian
        .                           // Scriptures I), an animated land, or a devotion-gated god
        .                           // must filter by its *computed* types, not the printed ones.
        .                           // Off-battlefield cards keep the printed definition.
        .                           let computed: Option<std::sync::Arc<crate::game::layers::ComputedPermanent>> =
  266,312 ( 0.01%)                      if self.in_layer_gather.load(std::sync::atomic::Ordering::Relaxed) {
        .                                   None // mid-recompute: printed types (reentrancy guard)
        .                               } else {
  206,768 ( 0.01%)                          self.battlefield_find(*cid).and_then(|_| self.computed_permanent(*cid))
69,722,245 ( 2.24%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (93,612x)
        .                               };
   47,934 ( 0.00%)                  let has_type = |t: crate::card::CardType| match &computed {
        .                               Some(cp) => cp.card_types.contains(&t),
        .                               None => card.definition.card_types.contains(&t),
   31,956 ( 0.00%)                  };
        .                           // CR 613.2 layer-4 — a creature that gained a type from a
        .                           // continuous effect (Jenova's Mutant grant) matches by its
        .                           // *computed* subtypes on the battlefield; off-battlefield cards
        .                           // (incl. die-snapshots, whose grants were stamped in at death)
        .                           // fall back to the definition.
        .                           // Mid-gather the full computed view is off-limits, but stored
        .                           // layer-4 type changes are already in `continuous_effects` —
        .                           // read them shallowly so a "as long as it's a Wall" gate sees a
        .                           // retyped permanent (CR 613.8; Mistform Wall).
        .                           let shallow_types = computed
        .                               .is_none()
  506,124 ( 0.02%)                      .then(|| self.shallow_creature_types(*cid))
        .                               .flatten();
  532,624 ( 0.02%)                  let has_ctype = |ct: &crate::card::CreatureType| match (&computed, &shallow_types) {
        .                               (Some(cp), _) => cp.subtypes.creature_types.contains(ct),
        .                               (None, Some(types)) => types.contains(ct),
        .                               (None, None) => card.definition.subtypes.creature_types.contains(ct),
        .                           };
        .                           // CR 613.2 layer-4 — subtypes/supertypes a permanent gained (or
        .                           // lost) from a continuous effect (Vraska's Treasure, Song of the
        .                           // Dryads' Forest, Sugar Coat's Food, the Ring-bearer's Legendary)
        .                           // read from the *computed* type line on the battlefield.
-- line 3332 ----------------------------------------
-- line 3338 ----------------------------------------
        .                               Some(cp) => cp.subtypes.land_types.contains(lt),
        .                               None => card.definition.subtypes.land_types.contains(lt),
        .                           };
        .                           let has_stype = |st: &Supertype| match &computed {
        .                               Some(cp) => cp.supertypes.contains(st),
        .                               None => card.definition.supertypes.contains(st),
        .                           };
        .                           use crate::card::CardType as CT;
  798,936 ( 0.03%)                  match req {
        .                               // CR 604.3 — Grist is a creature everywhere but the
        .                               // battlefield.
        .                               R::Creature => {
   95,868 ( 0.00%)                          has_type(CT::Creature)
  207,276 ( 0.01%)  => crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static::{{closure}} (7,340x)
   16,488 ( 0.00%)                              || (card.definition.creature_off_battlefield && computed.is_none())
        .                               }
        .                               R::Artifact => has_type(CT::Artifact),
        .                               R::Enchantment => has_type(CT::Enchantment),
        .                               R::Planeswalker => has_type(CT::Planeswalker),
        .                               R::Permanent => card.definition.is_permanent(),
        .                               R::Land => has_type(CT::Land),
        .                               R::Nonland => !has_type(CT::Land),
        .                               R::Noncreature => !has_type(CT::Creature),
-- line 3359 ----------------------------------------
-- line 3379 ----------------------------------------
        .                                       self.battlefield_find(s)
        .                                           .or_else(|| self.leaves_bf_lki.get(&s))
        .                                           .or_else(|| self.died_card_snapshots.get(&s))
        .                                   })
        .                                   .is_some_and(|src| src.damaged_by_this_turn.contains(&card.id)),
        .                               // CR 105.2/202.2 — color is the union of the mana cost's
        .                               // colors and the color indicator (tokens, DFC backs), and
        .                               // empty under Devoid. `printed_colors` folds all three in.
    1,758 ( 0.00%)                      R::HasColor(c) => card.definition.printed_colors().contains(c),
  412,775 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_colors (1,758x)
        .                               R::HasKeyword(kw) => card.has_keyword(kw),
        .                               R::HasToxic => card.has_toxic(),
        .                               R::HasModular => card.has_modular(),
        .                               R::HasMutate => card.definition.mutate.is_some(),
        .                               R::HasMorphAbility => card.definition.keywords.iter().any(|k| matches!(
        .                                   k,
        .                                   crate::card::Keyword::Morph(_)
        .                                       | crate::card::Keyword::MorphCost(_)
-- line 3395 ----------------------------------------
-- line 3578 ----------------------------------------
        .                                   self.attacking.len() == 1
        .                                       && self.attacking.iter().any(|a| a.attacker == card.id)
        .                               }
        .                               // CR 506.5: blocking alone = card is in block_map keys
        .                               // AND there is exactly one declared blocker.
        .                               R::IsBlockingAlone => {
        .                                   self.block_map.len() == 1 && self.block_map.contains_key(&card.id)
        .                               }
  146,226 ( 0.00%)                      R::IsSpellOnStack => self.stack.iter().any(|si| matches!(si, StackItem::Spell { card: c, .. } if c.id == card.id)),
        .                               // CR 115 — a stack spell that targets the chooser or a
        .                               // permanent they control (Hindering Light).
        .                               R::SpellTargetsControllerOrControlled => self.stack.iter().any(|si| {
        .                                   let StackItem::Spell { card: c, target, additional_targets, .. } = si else { return false };
        .                                   if c.id != card.id { return false; }
        .                                   target.iter().chain(additional_targets.iter()).any(|t| match t {
        .                                       crate::game::types::Target::Player(p) => *p == controller,
        .                                       crate::game::types::Target::Permanent(id) => self
-- line 3594 ----------------------------------------
-- line 3806 ----------------------------------------
        .                                       <= self.sacrificed_mana_value.unwrap_or(0) + *off
        .                               }
        .                               R::ManaValueLessThanEventAmount => {
        .                                   card.definition.cost.cmc() < self.trigger_event_amount_scratch
        .                               }
        .                               R::HasCardType(ct) => {
        .                                   // CR 715 / 702.183 — an Adventure/Omen card is its
        .                                   // instant/sorcery half on the stack; report those types.
      380 ( 0.00%)                          if let Some(half) = card.alt_spell_half() {
        .                                       half.card_types.contains(ct)
        .                                   } else {
        .                                       card.definition.card_types.contains(ct)
        .                                   }
        .                               }
        .                               R::Multicolored => card.definition.cost.distinct_colors() >= 2,
        .                               R::SharesMostCommonColor => {
        .                                   let top = self.most_common_permanent_colors();
-- line 3822 ----------------------------------------
-- line 3824 ----------------------------------------
        .                               }
        .                               R::ManaValueEqualsChosenNumber => {
        .                                   card.definition.cost.cmc() == self.chosen_number_this_resolution
        .                               }
        .                               R::HasNonManaActivatedAbility => card
        .                                   .definition
        .                                   .activated_abilities
        .                                   .iter()
    1,948 ( 0.00%)                          .any(|a| !crate::game::actions::is_mana_ability_public(&a.effect)),
        .                               R::SharesNameWithAnotherPermanent => self
        .                                   .battlefield
        .                                   .iter()
        .                                   .any(|c| c.id != card.id && c.definition.name == card.definition.name),
        .                               R::NameNotSharedWithYourPermanents => !self.battlefield.iter().any(|c| {
        .                                   c.controller == controller && c.definition.name == card.definition.name
        .                               }),
        .                               // CR 702.114 — Devoid CDA: colorless despite colored pips.
-- line 3840 ----------------------------------------
-- line 3951 ----------------------------------------
        .                                   let cand_pow = cand.power();
        .                                   !self.battlefield.iter().any(|other| {
        .                                       other.id != *cid
        .                                           && other.definition.is_creature()
        .                                           && other.power() > cand_pow
        .                                   })
        .                               }
        .                               R::HasName(name) => {
      562 ( 0.00%)                          card.definition.name == name.as_str()
        .                                       || self.has_all_creature_names(card.id, name)
        .                               }
        .                               R::OriginallyPrintedIn(set) => set.contains(card.definition.name),
        .                               R::ManaValueAtMostControllerHand => {
        .                                   card.definition.cost.cmc() as usize <= self.players[controller].hand.len()
        .                               }
        .                               R::ManaValueAtMostControlledCount(inner) => {
        .                                   let count = self
-- line 3967 ----------------------------------------
-- line 4148 ----------------------------------------
        .                                       })
        .                               }
        .                               // Zone-agnostic atoms (spell/enchantment subtype, token
        .                               // flags, …) the battlefield walker doesn't special-case
        .                               // are evaluated against the located card. Keeps the two
        .                               // requirement walkers from drifting apart (TODO P3).
        .                               _ => self.evaluate_requirement_on_card(req, card, controller),
        .                           }
  133,156 ( 0.00%)              }
        .                   }
1,212,736 ( 0.04%)      }
        .           
        .               /// CR 201.4a — evaluate a `SelectionRequirement` against a bare
        .               /// `CardDefinition` (a *name*, not an object in any zone), for the
        .               /// namespace restriction on "choose a [kind] card name".
        .               pub fn definition_matches_requirement(
        .                   &self,
        .                   def: &crate::card::CardDefinition,
        .                   req: &SelectionRequirement,
-- line 4166 ----------------------------------------
-- line 4168 ----------------------------------------
        .               ) -> bool {
        .                   let scratch = CardInstance::new(CardId(u32::MAX), def.clone(), controller);
        .                   self.evaluate_requirement_on_card(req, &scratch, controller)
        .               }
        .           
        .               /// Evaluate a `SelectionRequirement` directly against a `CardInstance`
        .               /// without requiring it to be on the battlefield. Used for library searches.
        .               /// Battlefield-only predicates (Tapped, IsAttacking, etc.) return false.
   43,872 ( 0.00%)      pub fn evaluate_requirement_on_card(
        .                   &self,
        .                   req: &SelectionRequirement,
        .                   card: &CardInstance,
        .                   controller: usize,
        .               ) -> bool {
        .                   use SelectionRequirement as R;
   32,904 ( 0.00%)          match req {
        .                       R::Any => true,
        .                       R::ManaValueEqualsTriggerAmount => {
        .                           card.definition.cost.cmc() == self.trigger_event_amount_scratch
        .                       }
        .                       // Source-less path: the counter count needs the ability's source,
        .                       // which only `evaluate_requirement_static` carries.
        .                       R::ManaValueEqualsCountersOnSource(_) | R::PowerAtMostSourceCounters(_) => false,
        .                       R::Player
-- line 4191 ----------------------------------------
-- line 4193 ----------------------------------------
        .                       | R::YouPlayer
        .                       | R::OpponentTallyDiffers { .. }
        .                       | R::PlayerAttackedThisTurn => false,
        .                       R::And(a, b) => {
        .                           self.evaluate_requirement_on_card(a, card, controller)
        .                               && self.evaluate_requirement_on_card(b, card, controller)
        .                       }
        .                       R::Or(a, b) => {
      264 ( 0.00%)                  self.evaluate_requirement_on_card(a, card, controller)
        .                               || self.evaluate_requirement_on_card(b, card, controller)
        .                       }
        .                       R::Not(inner) => !self.evaluate_requirement_on_card(inner, card, controller),
        .                       R::ControlledByYou => card.controller == controller,
        .                       R::ControlledByOpponent => !self.same_team(card.controller, controller),
        .                       R::ControlledByActivePlayer => card.controller == self.active_player_idx,
        .                       R::HasAwaken => card.definition.alternative_cost.as_ref().is_some_and(|a| a.awaken),
        .                       R::PutIntoGraveyardFromBattlefieldThisTurn => {
-- line 4209 ----------------------------------------
-- line 4240 ----------------------------------------
        .                       R::SharesNameWithControllerGraveyardCard => self.players[controller]
        .                           .graveyard
        .                           .iter()
        .                           .any(|c| c.id != card.id && c.definition.name == card.definition.name),
        .                       R::ControllerDrewAtLeastThisTurn(n) => {
        .                           self.players[controller].cards_drawn_this_turn >= *n
        .                       }
        .                       R::ControllerSacrificedArtifactThisTurn => {
    2,068 ( 0.00%)                  self.players[card.controller].artifacts_sacrificed_this_turn > 0
        .                       }
        .                       R::ControllersTurn => self.active_player_idx == card.controller,
        .                       R::ControllerCorrupted => self.players[card.controller].poison_counters >= 3,
      264 ( 0.00%)              R::Land => card.definition.is_land(),
    3,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (264x)
        .                       R::Nonland => !card.definition.is_land(),
        .                       R::Noncreature => !card.definition.is_creature(),
        .                       // CR 105.2/202.2 — color is the union of the mana cost's colors
        .                       // (incl. hybrid {R/W}, Phyrexian {R/P}, mono-hybrid {2/W}) and the
        .                       // color indicator (tokens, DFC backs), and empty under Devoid.
        .                       // `printed_colors` folds all three in — matching the battlefield
        .                       // path (`colors_from_card`) for cards in hidden zones too.
        .                       R::HasColor(c) => card.definition.printed_colors().contains(c),
-- line 4260 ----------------------------------------
-- line 4337 ----------------------------------------
        .                       }
        .                       R::EnteredFromExileThisTurn => {
        .                           self.entered_from_exile_this_turn.contains(&card.id)
        .                       }
        .                       R::SameNameAsAPermanent => {
        .                           self.battlefield.iter().any(|p| p.definition.name == card.definition.name)
        .                       }
        .                       R::HasPhyrexianManaInCost => card.definition.cost.has_phyrexian(),
   15,660 ( 0.00%)              R::IsBasicLand => card.definition.is_land() && card.definition.supertypes.contains(&Supertype::Basic),
   77,316 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (5,220x)
        .                       R::IsNonbasicLand => card.definition.is_land() && !card.definition.supertypes.contains(&Supertype::Basic),
        .                       R::ProducesColorless => card.definition.produces_colorless(),
        .                       R::IsSnow => card.definition.is_snow(),
        .                       R::ManaValueAtMost(n) => card.definition.cost.cmc() <= *n,
        .                       R::ManaValueAtMostOpponentsAttackedThisCombat => {
        .                           card.definition.cost.cmc() <= self.opponents_attacked_this_combat()
        .                       }
        .                       // Need the ability's source or the live trigger context, which
-- line 4353 ----------------------------------------
-- line 4705 ----------------------------------------
        .                       | R::DealtDamageToControllerThisTurn | R::IsBestowed
        .                       | R::EquippedByAtLeast(_) | R::IsModified | R::DealtDamageThisTurn
        .                       | R::DamagedBySourceThisTurn | R::DealtDamageToSourceThisTurn
        .                       | R::BlockingOrBlockedBySource
        .                       | R::BlockedBySourceThisTurn
        .                       | R::PlayerDamagedBySourceThisTurn
        .                       | R::SaddledSourceThisTurn => false,
        .                   }
   43,872 ( 0.00%)      }
        .           }

1,515,898 ( 0.05%)  <counts for unidentified lines in crabomination/src/game/effects/eval.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/effects/movement.rs
--------------------------------------------------------------------------------
Ir               

-- line 21 ----------------------------------------
      .                       .iter()
      .                       .find(|(c, _)| *c == cid)
      .                       .and_then(|(_, to)| to.as_permanent_id())
      .                       .filter(|id| self.battlefield_find(*id).is_some())
      .               }
      .           
      .               /// (Palisade Giant), return the redirecting permanent. The redirector
      .               /// never re-redirects its own damage.
 11,448 ( 0.00%)      pub(crate) fn damage_redirect_target(&self, ent: EntityRef) -> Option<crate::card::CardId> {
      .                   use crate::effect::StaticEffect;
      .                   // Karona's Zealot — a turn-scoped "all damage to it is dealt to that
      .                   // creature instead" outranks the standing Palisade-Giant statics.
  8,604 ( 0.00%)          if let EntityRef::Permanent(c) = ent
      .                       && let Some(to) = self.turn_damage_redirect_for(c)
      .                   {
      .                       return Some(to);
      .                   }
      .                   let protected = match ent {
  2,856 ( 0.00%)              EntityRef::Player(p) => p,
      4 ( 0.00%)              EntityRef::Permanent(c) => self.battlefield_find(c)?.controller,
      .                       EntityRef::Card(_) => return None,
      .                   };
      .                   let aimed_at = match ent {
      .                       EntityRef::Permanent(c) => Some(c),
      .                       _ => None,
      .                   };
      .                   // Pariah's Shield — a player's damage is dealt to the equipped
      .                   // creature instead. Only player-directed damage redirects.
      .                   if let EntityRef::Player(p) = ent
  5,712 ( 0.00%)              && let Some(cid) = self.battlefield.iter().find_map(|c| {
113,588 ( 0.00%)                  (c.controller == p
      .                               && c.definition.static_abilities.iter().any(|sa| {
      .                                   matches!(
      .                                       sa.effect,
      .                                       StaticEffect::RedirectControllerDamageToEquippedCreature
      .                                   )
      .                               }))
      .                           .then_some(c.attached_to)
      .                           .flatten()
      .                       })
      .                   {
      .                       return Some(cid);
      .                   }
  5,720 ( 0.00%)          if let Some(cid) = self.battlefield.iter().find_map(|c| {
113,756 ( 0.00%)              (c.controller == protected
     80 ( 0.00%)                  && Some(c.id) != aimed_at
      .                           && c.definition
      .                               .static_abilities
      .                               .iter()
      .                               .any(|sa| matches!(sa.effect, StaticEffect::RedirectDamageToSelf)))
      .                       .then_some(c.id)
      .                   }) {
      .                       return Some(cid);
      .                   }
      .                   // Gideon's Sacrifice — a one-shot "all damage to you and your
      .                   // permanents this turn goes to the chosen permanent instead", so long
      .                   // as it's still on the battlefield and isn't itself the aimed-at card.
      .                   self.damage_redirect_this_turn.iter().find_map(|(p, to)| {
      .                       (*p == protected && Some(*to) != aimed_at && self.battlefield_find(*to).is_some())
      .                           .then_some(*to)
      .                   })
 14,310 ( 0.00%)      }
      .           
      .               /// CR 614.9 — the permanent `seat`'s *combat* damage is redirected to
      .               /// (Turn the Tables), if any. Narrower than `damage_redirect_target`, which
      .               /// also covers noncombat damage and permanents.
      .               pub(crate) fn combat_damage_redirect_for(&self, seat: usize) -> Option<crate::card::CardId> {
      .                   self.combat_damage_redirect_this_turn
      .                       .iter()
      .                       .find(|(p, to)| *p == seat && self.battlefield_find(*to).is_some())
-- line 90 ----------------------------------------
-- line 96 ----------------------------------------
      .               pub(crate) fn source_damage_fully_prevented(&self, source: crate::card::CardId) -> bool {
      .                   !self.damage_cant_be_prevented_this_turn
      .                       && self.damage_prevented_sources.iter().any(|sh| sh.source == source)
      .               }
      .           
      .               /// CR 120.6 — how much more damage would be lethal to `cid` right now,
      .               /// with damage already marked subtracted. A deathtouch source needs only 1
      .               /// (CR 702.2c). `None` when the permanent isn't a creature.
     48 ( 0.00%)      pub(crate) fn lethal_damage_needed(&self, cid: CardId, deathtouch: bool) -> Option<u32> {
     10 ( 0.00%)          let cp = self.computed_permanent(cid)?;
 10,906 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (6x)
      8 ( 0.00%)          if !cp.card_types.contains(&crate::card::CardType::Creature) {
      .                       return None;
      .                   }
      .                   if deathtouch {
      .                       return Some(1);
      .                   }
      4 ( 0.00%)          let prior = self.battlefield_find(cid).map(|c| c.damage).unwrap_or(0);
      4 ( 0.00%)          Some((cp.toughness.max(0) as u32).saturating_sub(prior))
     36 ( 0.00%)      }
      .           
      .               /// CR 615.1 / 615.7 / 615.12 — apply prevention shields to a pending
      .               /// damage event aimed at `ent`. "Prevent all" shields zero the event;
      .               /// "prevent next N" shields soak up to N and then expire. The whole
      .               /// step is bypassed while `damage_cant_be_prevented_this_turn` is set.
      .               /// Emits `GameEvent::DamagePrevented` for the prevented portion
      .               /// (CR 615.13) and returns the unprevented remainder.
131,616 ( 0.00%)      pub fn apply_prevention_shields(
      .                   &mut self,
      .                   ent: EntityRef,
      .                   amount: u32,
      .                   source: Option<crate::card::CardId>,
      .                   events: &mut Vec<GameEvent>,
      .               ) -> u32 {
      .                   use crate::game::types::PreventionTarget;
 14,624 ( 0.00%)          if self.damage_cant_be_prevented_this_turn || self.damage_cant_be_prevented_now() {
      .                       return amount;
      .                   }
      .                   // Dark Sphere — "prevent half that damage, rounded down" from the next
      .                   // source to hit you this turn. One shield, one damage event.
      .                   if let EntityRef::Player(p) = ent
  8,568 ( 0.00%)              && self.players[p].half_damage_shields > 0
      .                   {
      .                       self.players[p].half_damage_shields -= 1;
      .                       let prevented = amount / 2;
      .                       if prevented > 0 {
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount: prevented,
      .                               to_player: Some(p),
      .                               to_card: None,
      .                           });
      .                       }
      .                       return self.apply_prevention_shields(ent, amount - prevented, source, events);
      .                   }
      .                   // CR 615.12 (scoped) — Questing Beast: combat damage dealt by creatures
      .                   // the controller controls can't be prevented. Bypass shields when the
      .                   // damage source is a creature whose controller has the static.
 17,480 ( 0.00%)          if let Some(src_id) = source
      .                       && let Some(src) = self.battlefield_find(src_id)
 28,584 ( 0.00%)              && src.definition.is_creature()
 92,898 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,146x)
      .                   {
      .                       let ctrl = src.controller;
      .                       let unpreventable = self.battlefield.iter().any(|c| {
      .                           c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(sa.effect, crate::effect::StaticEffect::CombatDamageCantBePrevented)
      .                                   || (c.controller == ctrl
      .                                       && matches!(
      .                                           sa.effect,
-- line 162 ----------------------------------------
-- line 177 ----------------------------------------
      .                           })
      .                       })
      .                   {
      .                       return amount;
      .                   }
      .                   // CR 615 — damage from a spell or ability that TARGETS the recipient
      .                   // (Silhouette's turn shield, Bronze Horse's static). The resolution's
      .                   // own target list is the discriminator.
 26,392 ( 0.00%)          if let (EntityRef::Permanent(tgt), Some(_)) = (ent, source)
      .                       && self.resolution_targets.contains(&tgt)
      .                   {
      6 ( 0.00%)              let shielded = self.targeting_damage_prevented_this_turn.contains(&tgt)
     40 ( 0.00%)                  || self.battlefield_find(tgt).is_some_and(|c| {
      .                               c.definition.static_abilities.iter().any(|sa| {
      .                                   matches!(
      .                                       sa.effect,
      .                                       crate::effect::StaticEffect::PreventTargetingDamageWhileYouControlAnotherCreature
      .                                   )
      .                               }) && self
      .                                   .battlefield
      .                                   .iter()
      .                                   .any(|o| o.id != tgt && o.controller == c.controller && o.definition.is_creature())
     32 ( 0.00%)                  });
      8 ( 0.00%)              if shielded {
      .                           if amount > 0 {
      .                               events.push(GameEvent::DamagePrevented {
      .                                   amount,
      .                                   to_player: None,
      .                                   to_card: Some(tgt),
      .                               });
      .                           }
      .                           return 0;
      .                       }
      .                   }
      .                   // CR 615 — "prevent all damage that would be dealt to this creature by
      .                   // creatures it's blocking" (Wall of Vapor). Combat and noncombat alike,
      .                   // so it sits in the funnel rather than the combat resolver.
      .                   if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
      .                       && (self.damage_from_blocked_prevented(tgt, src)
 40,104 ( 0.00%)                  || self.combat_damage_from_matching_prevented(tgt, src))
1,010,638 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_from_matching_prevented (4,456x)
      .                   {
      .                       if amount > 0 {
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount,
      .                               to_player: None,
      .                               to_card: Some(tgt),
      .                           });
      .                       }
      .                       return 0;
      .                   }
      .                   // CR 615.7 — "prevent all damage [source] would deal this turn"
      .                   // (Burrenton Forge-Tender's chosen source, Hallow, Awe Strike). Lives
      .                   // here rather than in `deal_damage_to_from` so the combat path — which
      .                   // reaches this funnel directly — honours it too.
  7,312 ( 0.00%)          if let Some(src) = source
  7,312 ( 0.00%)              && let Some(shield) = self
      .                           .damage_prevented_sources
      .                           .iter()
      .                           .find(|sh| {
      .                               sh.source == src
      .                                   // "…would deal damage to you" (New Way Forward) only
      .                                   // soaks damage aimed at the shield's owner.
      .                                   && sh.to_player.is_none_or(|p| ent == EntityRef::Player(p))
      .                           })
-- line 240 ----------------------------------------
-- line 284 ----------------------------------------
      .                                   events.append(&mut sub);
      .                               }
      .                           }
      .                       }
      .                       return 0;
      .                   }
      .                   // Protection from everything (The One Ring) — all damage to the
      .                   // player is prevented until their next turn.
  7,312 ( 0.00%)          if let EntityRef::Player(p) = ent
  8,568 ( 0.00%)              && self.players[p].protected_from_everything
      .                   {
      .                       if amount > 0 {
      .                           events.push(GameEvent::DamagePrevented { amount, to_player: Some(p), to_card: None });
      .                       }
      .                       return 0;
      .                   }
      .                   // CR 615.10 — "if a [filter] source would deal damage to you, prevent
      .                   // N of that damage" (Sphere of Purity). Per-event, controller-scoped.
      .                   let mut amount = amount;
 11,424 ( 0.00%)          if let (EntityRef::Player(p), Some(src)) = (ent, source) {
  2,856 ( 0.00%)              let shaved: u32 = self
      .                           .battlefield
      .                           .iter()
113,588 ( 0.00%)                  .filter(|c| c.controller == p)
      .                           .flat_map(|c| c.definition.static_abilities.iter())
      .                           .filter_map(|sa| match &sa.effect {
      .                               crate::effect::StaticEffect::ReduceDamageToControllerFromSource {
      .                                   filter,
      .                                   amount,
      .                               } => Some((filter.clone(), *amount)),
      .                               _ => None,
      .                           })
-- line 315 ----------------------------------------
-- line 319 ----------------------------------------
      .                                   &crate::game::types::Target::Permanent(src),
      .                                   p,
      .                                   Some(src),
      .                               )
      .                           })
      .                           .map(|(_, n)| n)
      .                           .sum();
      .                       let soaked = shaved.min(amount);
  5,712 ( 0.00%)              if soaked > 0 {
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount: soaked,
      .                               to_player: Some(p),
      .                               to_card: None,
      .                           });
      .                           amount -= soaked;
      .                           if amount == 0 {
      .                               return 0;
      .                           }
      .                       }
      .                   }
      .                   // Callous Giant — "if a source would deal N or less damage to this,
      .                   // prevent that damage": all-or-nothing on the whole event.
 19,080 ( 0.00%)          if let EntityRef::Permanent(cid) = ent
 13,368 ( 0.00%)              && amount > 0
      .                       && self.battlefield_find(cid).is_some_and(|c| {
      .                           c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(sa.effect,
      .                                   crate::effect::StaticEffect::PreventSmallDamageToThis { max } if amount <= max)
      .                           })
      .                       })
      .                   {
      .                       events.push(GameEvent::DamagePrevented {
-- line 350 ----------------------------------------
-- line 353 ----------------------------------------
      .                           to_card: Some(cid),
      .                       });
      .                       return 0;
      .                   }
      .                   // CR 702.64 — Absorb N on the damaged creature prevents N of this
      .                   // event's damage per instance (each instance applies separately).
      .                   if let EntityRef::Permanent(cid) = ent {
      .                       let absorbed: u32 = self
 13,368 ( 0.00%)                  .computed_permanent(cid)
15,629,522 ( 0.50%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,456x)
 13,362 ( 0.00%)                  .map(|cp| {
      .                               cp.keywords
      .                                   .iter()
  7,462 ( 0.00%)                          .filter_map(|k| match k {
      .                                       crate::card::Keyword::Absorb(n) => Some(*n),
      .                                       _ => None,
      .                                   })
      .                                   .sum()
 17,816 ( 0.00%)                  })
      .                           .unwrap_or(0);
      .                       let soaked = absorbed.min(amount);
  8,908 ( 0.00%)              if soaked > 0 {
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount: soaked,
      .                               to_player: None,
      .                               to_card: Some(cid),
      .                           });
      .                           amount -= soaked;
      .                           if amount == 0 {
      .                               return 0;
      .                           }
      .                       }
      .                   }
      .                   // Shield of the Avatar — an attached source prevents N of the damage,
      .                   // where N counts the equipment controller's matching permanents.
  4,456 ( 0.00%)          if let EntityRef::Permanent(cid) = ent
  4,456 ( 0.00%)              && amount > 0
      .                   {
  4,456 ( 0.00%)              let shields: Vec<_> = self
      .                           .battlefield
      .                           .iter()
      .                           .filter(|c| c.attached_to == Some(cid))
      .                           .flat_map(|c| {
      .                               c.definition.static_abilities.iter().filter_map(move |sa| match &sa.effect {
      .                                   crate::effect::StaticEffect::PreventDamageToAttachedPerPermanent {
      .                                       filter,
      .                                   } => Some((c.controller, filter.clone())),
-- line 398 ----------------------------------------
-- line 411 ----------------------------------------
      .                                           &crate::game::types::Target::Permanent(c.id),
      .                                           *ctrl,
      .                                       )
      .                                   })
      .                                   .count() as u32
      .                           })
      .                           .sum();
      .                       let soaked = soak.min(amount);
  8,912 ( 0.00%)              if soaked > 0 {
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount: soaked,
      .                               to_player: None,
      .                               to_card: Some(cid),
      .                           });
      .                           amount -= soaked;
      .                           if amount == 0 {
      .                               return 0;
      .                           }
      .                       }
 22,280 ( 0.00%)          }
115,856 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<(usize,crabomination_base::card::SelectionRequirement)>> (4,456x)
      .                   // Ajani Steadfast's emblem — "prevent all but 1 of that damage" to its
      .                   // owner and to planeswalkers they control.
 16,076 ( 0.00%)          if amount > 1 {
 18,824 ( 0.00%)              let protected_seat = match ent {
      .                           EntityRef::Player(p) => Some(p),
  3,552 ( 0.00%)                  EntityRef::Permanent(cid) => self
      .                               .battlefield_find(cid)
  3,552 ( 0.00%)                      .filter(|c| c.definition.is_planeswalker())
 56,832 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (3,552x)
      .                               .map(|c| c.controller),
      .                           EntityRef::Card(_) => None,
      .                       };
      .                       if let Some(seat) = protected_seat
  2,308 ( 0.00%)                  && self.players[seat].emblems.iter().any(|e| {
      .                               e.statics.iter().any(|sa| {
      .                                   matches!(
      .                                       sa.effect,
      .                                       crate::effect::StaticEffect::PreventAllButOneDamageToYouAndYourPlaneswalkers
      .                                   )
      .                               })
      .                           })
      .                       {
-- line 451 ----------------------------------------
-- line 458 ----------------------------------------
      .                               },
      .                           });
      .                           return 1;
      .                       }
      .                   }
      .                   // "If damage would be dealt to this while it has a [kind] counter,
      .                   // prevent that damage and remove that many counters" (Polukranos,
      .                   // Unchained). Prevents the whole event; removes min(amount, counters).
 23,536 ( 0.00%)          if let EntityRef::Permanent(cid) = ent
  8,912 ( 0.00%)              && amount > 0
 44,540 ( 0.00%)              && let Some((kind, single)) = self.battlefield_find(cid).and_then(|c| {
      .                           c.definition.static_abilities.iter().find_map(|sa| match sa.effect {
      .                               crate::effect::StaticEffect::PreventDamageByRemovingCounters {
      .                                   kind,
      .                                   single,
      .                               } => (c.counter_count(kind) > 0).then_some((kind, single)),
      .                               _ => None,
      .                           })
 44,540 ( 0.00%)              })
      .                   {
      .                       events.push(GameEvent::DamagePrevented {
      .                           amount,
      .                           to_player: None,
      .                           to_card: Some(cid),
      .                       });
      .                       if let Some(c) = self.battlefield_find_mut(cid) {
      .                           c.remove_counters(kind, if single { 1 } else { amount });
      .                       }
      .                       return 0;
      .                   }
      .                   // Phyrexian Vindicator — "If damage would be dealt to this creature,
      .                   // prevent that damage. When damage is prevented this way, this
      .                   // creature deals that much damage to any other target" (auto-picked).
  8,912 ( 0.00%)          if let EntityRef::Permanent(cid) = ent
      .                       && amount > 0
      .                       && self.battlefield_find(cid).is_some_and(|c| {
      .                           c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(
      .                                   sa.effect,
      .                                   crate::effect::StaticEffect::PreventDamageToThisRedirect
      .                               )
      .                           })
-- line 499 ----------------------------------------
-- line 516 ----------------------------------------
      .                               .build(),
      .                       );
      .                       return 0;
      .                   }
      .                   // Energy Field — "prevent all damage that would be dealt to you by
      .                   // sources you don't control." The source's controller is read from the
      .                   // battlefield, falling back to the resolving spell's caster.
      .                   if let EntityRef::Player(p) = ent
  5,712 ( 0.00%)              && amount > 0
 14,280 ( 0.00%)              && self.battlefield.iter().any(|c| {
559,584 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (2,856x)
113,588 ( 0.00%)                  c.controller == p
      .                               && c.definition.static_abilities.iter().any(|sa| {
      .                                   matches!(
      .                                       sa.effect,
      .                                       crate::effect::StaticEffect::PreventAllDamageToControllerFromOthersSources
      .                                   )
      .                               })
      .                       })
      .                   {
-- line 534 ----------------------------------------
-- line 537 ----------------------------------------
      .                           events.push(GameEvent::DamagePrevented {
      .                               amount,
      .                               to_player: Some(p),
      .                               to_card: None,
      .                           });
      .                           return 0;
      .                       }
      .                   }
 21,936 ( 0.00%)          if self.prevention_shields.is_empty() {
      .                       return amount;
      .                   }
      .                   let (to_player, to_card, key) = match ent {
      .                       EntityRef::Player(p) => (Some(p), None, PreventionTarget::Player(p)),
      .                       EntityRef::Permanent(c) => (None, Some(c), PreventionTarget::Permanent(c)),
      .                       EntityRef::Card(_) => return amount,
      .                   };
      .                   // Refraction Trap — a team shield's shared pool also covers the seat's
-- line 553 ----------------------------------------
-- line 734 ----------------------------------------
      .                                   .or_else(|| self.died_card_snapshots.get(&src).map(|c| c.controller))
      .                           })
      .                           .or(reflect_ctrl);
      .                       if let Some(ctrl) = ctrl {
      .                           self.deal_damage_to_from(EntityRef::Player(ctrl), reflected, None, events);
      .                       }
      .                   }
      .                   remaining
 58,496 ( 0.00%)      }
      .           
      .               /// Damage delivery with the source's identity threaded through, so
      .               /// CR 702.90b (Infect) can convert player damage into poison
      .               /// counters when the source has the Infect keyword. `source` is
      .               /// the `CardId` of the damaging permanent (typically `ctx.source`).
      .               /// Combat damage uses a separate path in `combat.rs` that already
      .               /// honors infect for combat damage.
  2,262 ( 0.00%)      pub fn deal_damage_to_from(
      .                   &mut self,
      .                   ent: EntityRef,
      .                   amount: u32,
      .                   source: Option<crate::card::CardId>,
      .                   events: &mut Vec<GameEvent>,
      .               ) {
      .                   // CR 120.8 — "If a source would deal 0 damage, it does not deal
      .                   // damage at all. That means abilities that trigger on damage
-- line 758 ----------------------------------------
-- line 760 ----------------------------------------
      .                   // effects that would increase the damage dealt by that source,
      .                   // or would have that source deal that damage to a different
      .                   // object or player, have no event to replace, so they have no
      .                   // effect." We bail out of the entire damage-delivery sequence
      .                   // when `amount == 0`, so no `GameEvent::DamageDealt`,
      .                   // `LifeLost`, `PoisonAdded`, or `LoyaltyChanged` event is
      .                   // emitted. Damage-watching triggered abilities won't fire on
      .                   // 0-damage events.
    348 ( 0.00%)          if amount == 0 {
      .                       return;
      .                   }
      .                   // CR 615 — Kiora's +1 locks a permanent out of dealing *and* taking
      .                   // damage until her controller's next turn.
    522 ( 0.00%)          if !self.damage_locked_until_turn_of.is_empty() {
      .                       let locked = |id: crate::card::CardId| {
      .                           self.damage_locked_until_turn_of.iter().any(|(c, _)| *c == id)
      .                       };
      .                       if source.is_some_and(locked)
      .                           || matches!(ent, EntityRef::Permanent(id) if locked(id))
      .                       {
      .                           return;
      .                       }
      .                   }
      .                   // Damage-source attribution: the source permanent's controller, or
      .                   // the resolving spell's caster (mirrors `scale_damage_to`).
      .                   let from_controller = source.and_then(|s| {
      .                       self.battlefield_find(s)
     32 ( 0.00%)                  .map(|c| c.controller)
  1,494 ( 0.00%)                  .or_else(|| match &self.resolving_source {
      .                               Some((id, caster, _, _)) if *id == s => Some(*caster),
      .                               _ => None,
      .                           })
      .                   });
      .                   // CR 614.9 — Mirrorwood Treefolk's one-shot: the next damage aimed at
      .                   // a registered permanent goes somewhere else instead. Consumed here.
    696 ( 0.00%)          if !self.in_damage_redirect
      6 ( 0.00%)              && let EntityRef::Permanent(hit) = ent
      .                       && let Some(idx) = self.next_damage_redirect.iter().position(|(c, _)| *c == hit)
      .                   {
      .                       let (_, to) = self.next_damage_redirect.remove(idx);
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(to, amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // CR 614.9 — Blood of the Martyr: damage to *any* creature may be
      .                   // dealt to the martyr instead. One redirect per event.
      .                   if !self.in_damage_redirect
      .                       && let EntityRef::Permanent(cid) = ent
     12 ( 0.00%)              && self.battlefield_find(cid).is_some_and(|c| c.definition.is_creature())
     52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
      .                       && let Some(martyr) =
      .                           (0..self.players.len()).find(|&p| self.players[p].creature_damage_to_you_this_turn)
      .                   {
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(EntityRef::Player(martyr), amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // CR 614.9 — redirect the whole event to a Palisade-Giant-style
      .                   // permanent. One redirect per event (CR 614.5; the flag also stops
      .                   // two redirectors ping-ponging).
    522 ( 0.00%)          if !self.in_damage_redirect
  1,044 ( 0.00%)              && let Some(redirect) = self.damage_redirect_target(ent)
 35,696 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::damage_redirect_target (174x)
      .                   {
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(EntityRef::Permanent(redirect), amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // "Prevent all damage that would be dealt to this by [filter] sources"
      .                   // (Argothian Treefolk / Pixies, Artifact Ward's host).
  1,236 ( 0.00%)          if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
      .                       && self
     18 ( 0.00%)                  .computed_permanent(tgt)
 10,906 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (6x)
      .                           .into_iter()
      .                           .flat_map(|cp| cp.keywords.to_vec())
      .                           .any(|k| match k {
      .                               crate::card::Keyword::PreventDamageFromMatching(f) => self
      .                                   .evaluate_requirement_static(&f, &crate::game::types::Target::Permanent(src), self.battlefield_find(tgt).map_or(0, |c| c.controller), Some(src)),
      .                               _ => false,
      6 ( 0.00%)                  })
    102 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::iter::adapters::flatten::FlatMap<core::option::IntoIter<alloc::sync::Arc<crabomination::game::layers::ComputedPermanent>>,alloc::vec::Vec<crabomination_base::card::Keyword>,crabomination::game::GameState::damage_from_source_prevented_by_keyword::{{closure}}>> (6x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 702.16e — protection from the source's color prevents the whole
      .                   // damage event to a permanent (noncombat damage path).
      .                   if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
     30 ( 0.00%)              && self.damage_prevented_by_protection(src, tgt)
 13,424 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (6x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Heart of Light: the enchanted creature deals no damage
      .                   // either (the receiving half rides
      .                   // `permanent_prevents_all_damage_to_self`).
    336 ( 0.00%)          if let Some(src) = source
      .                       && (self.damage_sealed_by_aura(src, false)
      .                           || (!self.damage_cant_be_prevented_this_turn
    174 ( 0.00%)                      && self.all_damage_prevented_by_this_turn.contains(&src)))
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Light of Sanction: "prevent all damage to creatures you
      .                   // control by sources you control."
    354 ( 0.00%)          if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
      .                       && self.damage_from_your_source_to_your_creature_prevented(src, tgt)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Indentured Oaf: this source prevents its own damage to
      .                   // creatures of a chosen color.
      .                   if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
     30 ( 0.00%)              && self.source_damage_to_color_prevented(src, tgt)
  1,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::source_damage_to_color_prevented (6x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Well-Laid Plans: creatures never hurt creatures of a
      .                   // shared colour.
      .                   if let (EntityRef::Permanent(tgt), Some(src)) = (ent, source)
     24 ( 0.00%)              && self.shared_color_creature_damage_prevented(src, tgt)
  1,926 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::shared_color_creature_damage_prevented (6x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 614.9 — Martyrs of Korlis: while it's untapped, artifact damage
      .                   // aimed at its controller lands on it instead.
  1,914 ( 0.00%)          if !self.in_damage_redirect
    168 ( 0.00%)              && let EntityRef::Player(victim) = ent
  1,008 ( 0.00%)              && source.is_some_and(|s| self.source_is_artifact(s))
 30,624 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::source_is_artifact (168x)
      .                       && let Some(martyr) = self.battlefield.iter().find(|c| {
      .                           c.controller == victim
      .                               && !c.tapped
      .                               && c.definition.static_abilities.iter().any(|sa| {
      .                                   matches!(
      .                                       self.active_static(&sa.effect, c),
      .                                       Some(crate::effect::StaticEffect::
      .                                           RedirectArtifactDamageToSourceWhileUntapped)
-- line 894 ----------------------------------------
-- line 899 ----------------------------------------
      .                       let to = martyr.id;
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(EntityRef::Permanent(to), amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // CR 614.9 — Reverberation: everything the targeted spell would deal
      .                   // this turn hits that spell's controller instead.
    858 ( 0.00%)          if !self.in_damage_redirect
      .                       && let Some(src) = source
      .                       && let Some((_, to)) =
    174 ( 0.00%)                  self.spell_damage_to_controller.iter().find(|(s, _)| *s == src).copied()
      .                       && ent != EntityRef::Player(to)
      .                   {
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(EntityRef::Player(to), amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // CR 614.9 — Harsh Judgment: an I/S spell of the chosen colour burns
      .                   // its own controller instead of the enchantment's controller.
    348 ( 0.00%)          if !self.in_damage_redirect
      .                       && let EntityRef::Player(p) = ent
      .                       && let Some(to) = self.chosen_color_spell_damage_redirect(p, source)
      .                   {
      .                       self.in_damage_redirect = true;
      .                       self.deal_damage_to_from(EntityRef::Player(to), amount, source, events);
      .                       self.in_damage_redirect = false;
      .                       return;
      .                   }
      .                   // CR 615 — Iroas-style "prevent all damage to attacking creatures
      .                   // you control".
    360 ( 0.00%)          if let EntityRef::Permanent(tgt) = ent
      .                       && self.damage_to_attacker_prevented(tgt)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Glacial-Chasm-style "prevent all damage that would be dealt
      .                   // to you" (combat and noncombat alike).
    168 ( 0.00%)          if let EntityRef::Player(p) = ent
    840 ( 0.00%)              && self.all_damage_to_player_prevented(p)
139,655 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::all_damage_to_player_prevented (168x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 702.16j — a player with protection from a card type (Serra's
      .                   // Emissary) takes no damage from a source of that type.
    840 ( 0.00%)          if let (EntityRef::Player(p), Some(src)) = (ent, source) {
    336 ( 0.00%)              let types = self.player_protection_card_types(p);
    336 ( 0.00%)              if !types.is_empty() {
      .                           let src_types = self
      .                               .computed_permanent(src)
      .                               .map(|c| c.card_types.to_vec())
      .                               .or_else(|| {
      .                                   self.find_card_anywhere(src)
      .                                       .map(|c| c.definition.card_types.clone())
      .                               })
      .                               .unwrap_or_default();
      .                           if types.iter().any(|t| src_types.contains(t)) {
      .                               return;
      .                           }
      .                       }
    168 ( 0.00%)          }
      .                   // CR 615 — self-static "prevent all damage to this permanent"
      .                   // (Gideon Blackblade during your turn); the combat half is gated in
      .                   // the combat resolver.
      .                   if let EntityRef::Permanent(tgt) = ent
      .                       && self.permanent_prevents_all_damage_to_self(tgt)
      .                   {
      .                       return;
      .                   }
-- line 968 ----------------------------------------
-- line 972 ----------------------------------------
      .                   if let EntityRef::Permanent(tgt) = ent
      .                       && self.noncombat_damage_to_creature_prevented(tgt)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — The Wanderer: "prevent all noncombat damage to you and other
      .                   // permanents you control" (shields the player and any permanent, not
      .                   // just creatures).
    348 ( 0.00%)          if self.noncombat_damage_to_you_and_permanents_prevented(ent) {
      .                       return;
      .                   }
      .                   // CR 615 — Emmara-Tandris-style "prevent all damage to creature tokens
      .                   // you control" (both damage paths; combat is gated in combat.rs).
    180 ( 0.00%)          if let EntityRef::Permanent(tgt) = ent
      .                       && (self.all_damage_to_creature_token_prevented(tgt)
      .                           || self.all_damage_to_your_creature_prevented(tgt))
      .                   {
      .                       return;
      .                   }
      .                   // CR 614.2 / 614.5 — global damage doubling (Furnace of Rath) then
      .                   // halving (Ghosts of the Innocent), applied before prevention so a
      .                   // shield soaks the already-scaled total (CR 616 lets the affected
      .                   // player order the replacements — double-then-halve is the common
      .                   // pick and keeps the event single-pass here).
  1,218 ( 0.00%)          let amount = self.scale_damage_to(source, ent, amount);
279,024 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::scale_damage_to (174x)
      .                   // Desperate Gambit — "the next time that source would deal damage this
      .                   // turn, it deals double that damage instead". One-shot per entry.
      .                   let amount = match source
      .                       .and_then(|s| self.double_next_damage_from.iter().position(|&d| d == s))
      .                   {
      .                       Some(i) => {
      .                           self.double_next_damage_from.remove(i);
      .                           amount.saturating_mul(2)
      .                       }
      .                       None => amount,
      .                   };
      .                   // CR 614.5 — Solphim, Mayhem Dominus: a noncombat-only doubler scoped
      .                   // to "a source you control" hitting an opponent / their permanent.
      .                   // Applied here (not in `scale_damage_to`) so combat damage is exempt.
      .                   let amount = {
    870 ( 0.00%)              let n = self.noncombat_damage_doublers_for(source, ent);
 93,392 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::noncombat_damage_doublers_for (174x)
    174 ( 0.00%)              amount.saturating_mul(1 << n.min(16))
      .                   };
      .                   // Taii Wakeen's {X}, {T} — "it deals that much damage plus X instead",
      .                   // scoped to the activating seat's sources and to this turn.
    870 ( 0.00%)          let amount = if self.noncombat_damage_bonus_this_turn.is_empty() {
      .                       amount
      .                   } else {
      .                       let ctrl = source.and_then(|s| self.computed_permanent(s).map(|cp| cp.controller));
      .                       let bonus: u32 = self
      .                           .noncombat_damage_bonus_this_turn
      .                           .iter()
      .                           .filter(|(seat, _)| Some(*seat) == ctrl)
      .                           .map(|(_, n)| *n)
-- line 1025 ----------------------------------------
-- line 1026 ----------------------------------------
      .                           .sum();
      .                       amount.saturating_add(bonus)
      .                   };
      .                   // CR 614 — Phytohydra: "If damage would be dealt to this creature, put
      .                   // that many +1/+1 counters on it instead." A replacement (not
      .                   // prevention), so it fires even when damage can't be prevented; grows
      .                   // by the full scaled amount. Combat damage is replaced on the combat
      .                   // path (`ironscale_replace`).
    180 ( 0.00%)          if let EntityRef::Permanent(tgt) = ent
     18 ( 0.00%)              && let Some(kind) = self.creature_replaces_damage_with_counters(tgt)
      .                   {
      .                       if let Some(c) = self.battlefield_find_mut(tgt) {
      .                           c.add_counters(kind, amount);
      .                       }
      .                       events.push(GameEvent::CounterAdded { card_id: tgt, counter_type: kind, count: amount });
      .                       return;
      .                   }
      .                   // CR 614.9 — Treacherous Link: damage bound for the host lands on its
      .                   // controller instead. Applied before shields so the host's own
      .                   // protection can't soak damage it never receives.
      .                   if let EntityRef::Permanent(tgt) = ent
     12 ( 0.00%)              && amount > 0
     30 ( 0.00%)              && let Some(owner) = self.creature_redirects_damage_to_controller(tgt)
 12,068 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::creature_redirects_damage_to_controller (6x)
      .                   {
      .                       self.deal_damage_to_from(EntityRef::Player(owner), amount, source, events);
      .                       return;
      .                   }
      .                   // CR 615 — Sekki, Seasons' Guide: prevent the damage, trading that
      .                   // many +1/+1 counters for that many Spirit tokens.
      .                   if let EntityRef::Permanent(tgt) = ent
     42 ( 0.00%)              && self.trade_counters_for_damage(tgt, amount, events)
  1,166 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::trade_counters_for_damage (6x)
      .                   {
      .                       return;
      .                   }
      .                   // CR 615 — Stormwild Capridor: "If noncombat damage would be dealt
      .                   // to this creature, prevent that damage. Put a +1/+1 counter on it
      .                   // for each 1 damage prevented this way." Applied after scaling so
      .                   // the counter count matches the damage that WOULD have been dealt;
      .                   // skipped when prevention is off (CR 615.12).
      .                   if let EntityRef::Permanent(tgt) = ent
     12 ( 0.00%)              && !self.damage_cant_be_prevented_this_turn
      .                       && self
      .                           .battlefield_find(tgt)
      .                           .is_some_and(|c| c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(
      .                                   sa.effect,
      .                                   crate::effect::StaticEffect::PreventNoncombatDamageToSelfAddCounters
      .                               )
      .                           }))
-- line 1074 ----------------------------------------
-- line 1081 ----------------------------------------
      .                           counter_type: crate::card::CounterType::PlusOnePlusOne,
      .                           count: amount,
      .                       });
      .                       return;
      .                   }
      .                   // "…deals that much damage plus N instead" (Aether Revolt) — additive,
      .                   // opponent-scoped, applied after the doublers. Only when damage is
      .                   // actually being dealt (amount > 0), so a 0 stays 0.
    348 ( 0.00%)          let amount = if amount > 0 {
  1,392 ( 0.00%)              amount.saturating_add(self.noncombat_damage_bonus_for(source, ent))
102,796 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::noncombat_damage_bonus_for (174x)
      .                   } else {
      .                       amount
      .                   };
      .                   // CR 615.1 — prevention shields. Before applying the damage, let
      .                   // any shield around the target soak it (unless a "damage can't be
      .                   // prevented this turn" effect is active, CR 615.12). Returns the
      .                   // unprevented remainder; 0 means the whole event is prevented.
  1,044 ( 0.00%)          let amount = self.apply_prevention_shields(ent, amount, source, events);
128,594 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields (174x)
    348 ( 0.00%)          if amount == 0 {
      .                       return;
      .                   }
      .                   // Case of the Burning Masks — tally the distinct sources each seat
      .                   // controlled that have dealt damage this turn.
    696 ( 0.00%)          if let (Some(src), Some(seat)) = (source, from_controller)
    174 ( 0.00%)              && !self.damage_sources_this_turn.contains(&(seat, src))
      .                   {
    344 ( 0.00%)              self.damage_sources_this_turn.push((seat, src));
      .                   }
      .                   // Reverse Polarity — tally artifact damage dealt to each player.
    690 ( 0.00%)          if let (EntityRef::Player(victim), Some(src)) = (ent, source)
    840 ( 0.00%)              && self.source_is_artifact(src)
 30,624 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::source_is_artifact (168x)
      .                   {
      .                       match self.artifact_damage_to_players_this_turn.iter_mut().find(|(s, _)| *s == victim) {
      .                           Some(entry) => entry.1 += amount,
      .                           None => self.artifact_damage_to_players_this_turn.push((victim, amount)),
      .                       }
      .                   }
      .                   // Backdraft — tally the damage each sorcery spell deals as it resolves.
      .                   let sorcery_caster = self.resolving_source.as_ref().and_then(|(rid, caster, _, types)| {
-- line 1119 ----------------------------------------
-- line 1127 ----------------------------------------
      .                       }
      .                   }
      .                   // CR 702.90b — damage dealt to a player by a source with infect
      .                   // doesn't cause life loss; it gives the player poison counters
      .                   // equal to that damage. We check the source's effective
      .                   // keywords via `computed_permanent` so layered grants (e.g.
      .                   // Triumph of the Hordes-style anthems) are honored.
      .                   let src_kws: Vec<crate::card::Keyword> = source
    522 ( 0.00%)              .and_then(|s| self.computed_permanent(s))
 42,184 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (174x)
      .                       .map(|cp| cp.keywords.to_vec())
      .                       .unwrap_or_default();
      .                   let source_has_infect = src_kws.contains(&crate::card::Keyword::Infect);
      .                   // CR 702.80a / 702.90e — wither/infect damage to a creature lands as
      .                   // -1/-1 counters instead of marked damage; CR 702.2c — nonzero
      .                   // deathtouch damage flags the creature for the destroy SBA.
      .                   let source_has_wither =
      .                       source_has_infect || src_kws.contains(&crate::card::Keyword::Wither);
-- line 1143 ----------------------------------------
-- line 1157 ----------------------------------------
      .                   // Kumano / Frostwielder: a creature this source damages is exiled
      .                   // instead of dying for the rest of the turn (source-bound CR 614
      .                   // replacement). Registered after the damage lands below.
      .                   let source_exiles_damaged = source
      .                       .map(|s| {
      .                           self.damage_exiles_victim_eot.contains(&s)
      .                               || self
      .                                   .battlefield_find(s)
     16 ( 0.00%)                          .is_some_and(|c| c.definition.damage_exiles_if_dies)
      .                       })
      .                       .unwrap_or(false);
      .                   // Runesword — a creature this source damages can't be regenerated
      .                   // for the rest of the turn.
      .                   let source_denies_regen = source.is_some_and(|s| self.damage_denies_regen_eot.contains(&s));
    360 ( 0.00%)          match ent {
    168 ( 0.00%)              EntityRef::Player(p) => {
      .                           // Bloodthirst (CR 702.54) window: any damage to a player
      .                           // (combat or not, incl. infect→poison) marks them damaged
      .                           // this turn.
    336 ( 0.00%)                  self.players[p].was_dealt_damage_this_turn = true;
 79,744 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (168x)
      .                           // Eye for an Eye — pay the hit back to the source's
      .                           // controller. Guarded by `in_damage_redirect` so the mirror
      .                           // can't mirror itself.
    672 ( 0.00%)                  if self.players[p].damage_mirrors > 0
      .                               && !self.in_damage_redirect
      .                               && let Some(dealer) = source.and_then(|s| self.damage_source_controller(s))
      .                               && dealer != p
      .                           {
      .                               self.players[p].damage_mirrors -= 1;
      .                               self.in_damage_redirect = true;
      .                               self.deal_damage_to_from(EntityRef::Player(dealer), amount, source, events);
      .                               self.in_damage_redirect = false;
      .                           }
      .                           // The Fallen — the source remembers who it has bled, forever.
    336 ( 0.00%)                  if let Some(src) = source
      .                               && let Some(c) = self.battlefield_find_mut(src)
      4 ( 0.00%)                      && !c.damaged_players_this_game.contains(&p)
      .                           {
      6 ( 0.00%)                      c.damaged_players_this_game.push(p);
      .                           }
    336 ( 0.00%)                  self.players[p].damage_taken_this_turn =
  5,040 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (168x)
    336 ( 0.00%)                      self.players[p].damage_taken_this_turn.saturating_add(amount);
      .                           // Record the damaging creature so "destroy target creature
      .                           // that dealt damage to you this turn" (Spear of Heliod) can
      .                           // filter targets. Only track battlefield creatures.
    336 ( 0.00%)                  if let Some(src) = source {
      .                               let is_creature = self
    168 ( 0.00%)                          .computed_permanent(src)
 25,268 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (168x)
      .                                   .map(|cp| cp.card_types.contains(&crate::card::CardType::Creature))
      .                                   .unwrap_or(false);
      8 ( 0.00%)                      if is_creature && !self.players[p].creatures_that_damaged_me_this_turn.contains(&src) {
      8 ( 0.00%)                          self.players[p].creatures_that_damaged_me_this_turn.push(src);
     60 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2x)
      .                               }
      .                           }
      .                           // The Mindskinner — "if a source you control would deal damage
      .                           // to an opponent, prevent that damage and each opponent mills
      .                           // that many cards."
      .                           if amount > 0
      .                               && let Some(src) = source
      6 ( 0.00%)                      && let Some(dealer) = self.battlefield_find(src).map(|c| c.controller)
     10 ( 0.00%)                      && !self.same_team(dealer, p)
     70 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (2x)
      8 ( 0.00%)                      && self.battlefield.iter().any(|c| {
    604 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (2x)
    120 ( 0.00%)                          c.controller == dealer
      .                                       && c.definition.static_abilities.iter().any(|sa| {
      .                                           matches!(
      .                                               sa.effect,
      .                                               crate::effect::StaticEffect::YourDamageToOpponentsBecomesMill
      .                                           )
      .                                       })
      .                               })
      .                           {
-- line 1227 ----------------------------------------
-- line 1257 ----------------------------------------
      .                                   let cid = card.id;
      .                                   self.exile.push(card);
      .                                   events.push(GameEvent::PermanentExiled { card_id: cid });
      .                               }
      .                               return;
      .                           }
      .                           // CR 614 — Delaying Shield: damage to its controller becomes
      .                           // delay counters on the enchantment instead.
    672 ( 0.00%)                  if let Some(shield) = self.battlefield.iter().find_map(|c| {
 13,016 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (168x)
  1,376 ( 0.00%)                      (c.controller == p).then(|| {
      .                                   c.definition.static_abilities.iter().find_map(|sa| {
      .                                       match &sa.effect {
      .                                           crate::effect::StaticEffect::
      .                                               ReplaceDamageToYouWithCountersOnSource { kind } =>
      .                                               Some((c.id, *kind)),
      .                                           _ => None,
      .                                       }
      .                                   })
-- line 1274 ----------------------------------------
-- line 1282 ----------------------------------------
      .                                   card_id: cid,
      .                                   counter_type: kind,
      .                                   count: amount,
      .                               });
      .                               return;
      .                           }
      .                           // CR 614 — Nefarious Lich: damage to its controller exiles
      .                           // that many graveyard cards instead; failing that, they lose.
  1,008 ( 0.00%)                  if self.battlefield.iter().any(|c| {
 14,738 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (168x)
  2,752 ( 0.00%)                      c.controller == p
      .                                   && c.definition.static_abilities.iter().any(|sa| {
      .                                       matches!(
      .                                           sa.effect,
      .                                           crate::effect::StaticEffect::
      .                                               ReplaceDamageToYouWithGraveyardExile
      .                                       )
      .                                   })
      .                           }) {
-- line 1299 ----------------------------------------
-- line 1306 ----------------------------------------
      .                                       self.exile.push(card);
      .                                       events.push(GameEvent::PermanentExiled { card_id: cid });
      .                                       self.note_left_graveyard(p, cid, events);
      .                                   }
      .                               }
      .                               return;
      .                           }
      .                           // Phyrexian Unlife — at ≤ 0 life all damage lands as poison.
    504 ( 0.00%)                  let unlife_infect = self.players[p].life <= 0 && self.player_unlife_active(p);
    168 ( 0.00%)                  if source_has_infect || unlife_infect {
      .                               self.players[p].poison_counters =
      .                                   self.players[p].poison_counters.saturating_add(amount);
      .                               events.push(GameEvent::PoisonAdded { player: p, amount });
      .                               events.push(GameEvent::DamageDealt {
      .                                   amount,
      .                                   to_player: Some(p),
      .                                   to_card: None,
      .                                   combat: false,
      .                                   from_controller,
      .                                   from_card: source,
      .                               });
      .                           } else {
      .                               // Angel's Grace / Worship — the damage is dealt in full
      .                               // (the event below carries `amount`), but the life
      .                               // reduction is clamped to the floor.
    504 ( 0.00%)                      let life_delta = self.clamp_damage_to_life_floor(p, amount);
 16,880 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::clamp_damage_to_life_floor (168x)
    672 ( 0.00%)                      let applied = self.adjust_life_applied(p, -(life_delta as i32));
106,672 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::adjust_life_applied (168x)
  2,520 ( 0.00%)                      events.push(GameEvent::DamageDealt { amount, to_player: Some(p), to_card: None, combat: false, from_controller, from_card: source });
    168 ( 0.00%)                      let lost = (-applied).max(0) as u32;
    336 ( 0.00%)                      if lost > 0 {
    840 ( 0.00%)                          events.push(GameEvent::LifeLost { player: p, amount: lost });
      .                               }
      .                           }
      .                           // Phase M: direct damage from a commander source also
      .                           // counts toward the 21-commander-damage SBA
      .                           // (CR 704.5v doesn't restrict the damage type — combat
      .                           // and non-combat both apply).
    336 ( 0.00%)                  if let Some(src) = source
      .                               && self.is_commander(src)
      .                           {
      .                               self.record_commander_damage(p, src, amount);
      .                           }
      .                           // "Whenever this deals damage" (Kiyomaro) — the non-combat,
      .                           // player-side half; combat fires the same kind from
      .                           // `resolve_combat`.
      .                           if let Some(src) = source {
    504 ( 0.00%)                      self.fire_noncombat_damage_to_player_triggers(src, p, amount);
 20,578 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_noncombat_damage_to_player_triggers (168x)
      .                           }
      .                       }
      6 ( 0.00%)              EntityRef::Permanent(cid) => {
      .                           // CR 122.1c — Shield counters: if damage would be dealt
      .                           // to this permanent, prevent that damage and remove a
      .                           // shield counter from it.
      .                           let has_shield = self
      .                               .battlefield_find(cid)
     12 ( 0.00%)                      .map(|c| c.counter_count(CounterType::Shield) > 0)
     40 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (4x)
      .                               .unwrap_or(false);
      4 ( 0.00%)                  if has_shield {
      .                               if let Some(c) = self.battlefield_find_mut(cid) {
      .                                   c.remove_counters(CounterType::Shield, 1);
      .                                   // No 0-count residue (CR 700.9 IsModified).
      .                                   if c.counter_count(CounterType::Shield) == 0 {
      .                                       c.counters.remove(&CounterType::Shield);
      .                                   }
      .                               }
      .                               return;
-- line 1371 ----------------------------------------
-- line 1377 ----------------------------------------
      .                           // regardless of card type, so a Lightning Bolt at a 3-loyalty
      .                           // PW correctly removed 3 damage to be applied to toughness
      .                           // (toughness = 0 → die!) but skipped the printed
      .                           // loyalty-loss path. Combat damage already routes through
      .                           // `combat.rs::AttackTarget::Planeswalker` which decrements
      .                           // loyalty — this aligns spell damage with the same rule.
      .                           let is_pw = self
      .                               .battlefield_find(cid)
      4 ( 0.00%)                      .map(|c| c.definition.is_planeswalker())
     64 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (4x)
      .                               .unwrap_or(false);
      .                           // CR 310.10 — damage dealt to a battle removes that many
      .                           // defense counters (the noncombat analogue of the combat path
      .                           // in `combat.rs`; a battle isn't a creature, so without this it
      .                           // would mark useless `c.damage`). The defeat trigger fires from
      .                           // the SBA once the last counter is gone.
      .                           let is_battle = self
      .                               .battlefield_find(cid)
      4 ( 0.00%)                      .map(|c| c.definition.is_battle())
     64 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_battle (4x)
      .                               .unwrap_or(false);
     12 ( 0.00%)                  if is_pw {
      .                               if let Some(c) = self.battlefield_find_mut(cid) {
      .                                   let current = c.counter_count(CounterType::Loyalty);
      .                                   let new_loyalty = current.saturating_sub(amount);
      .                                   c.counters
      .                                       .insert(CounterType::Loyalty, new_loyalty);
      .                                   events.push(GameEvent::DamageDealt {
      .                                       amount,
      .                                       to_player: None,
-- line 1404 ----------------------------------------
-- line 1415 ----------------------------------------
      .                                   if removed > 0 {
      .                                       events.push(GameEvent::CounterRemoved {
      .                                           card_id: cid,
      .                                           counter_type: CounterType::Loyalty,
      .                                           count: removed,
      .                                       });
      .                                   }
      .                               }
      8 ( 0.00%)                  } else if is_battle {
      .                               if let Some(c) = self.battlefield_find_mut(cid) {
      .                                   let current = c.counter_count(CounterType::Defense);
      .                                   let new_defense = current.saturating_sub(amount);
      .                                   c.counters.insert(CounterType::Defense, new_defense);
      .                                   events.push(GameEvent::DamageDealt {
      .                                       amount,
      .                                       to_player: None,
      .                                       to_card: Some(cid),
-- line 1431 ----------------------------------------
-- line 1434 ----------------------------------------
      .                                       from_card: source,
      .                                   });
      .                               }
      .                           } else {
      .                               // CR 120.10 — excess damage: amount beyond what's lethal,
      .                               // accounting for damage already marked. Deathtouch makes any
      .                               // damage past 1 excess (CR 702.2c). Computed before the
      .                               // mutable borrow below applies the new damage.
     12 ( 0.00%)                      if let Some(lethal_needed) =
     24 ( 0.00%)                          self.lethal_damage_needed(cid, source_has_deathtouch)
 12,274 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::lethal_damage_needed (6x)
      .                               {
      .                                   let excess = amount.saturating_sub(lethal_needed);
      4 ( 0.00%)                          self.excess_damage_this_resolution =
      .                                       self.excess_damage_this_resolution.saturating_add(excess);
      .                               }
      .                               // Name of the damaging source, read before the mutable
      .                               // borrow (Blazing Effigy's per-name tally).
      .                               let source_name = source
      .                                   .filter(|s| *s != cid)
     30 ( 0.00%)                          .and_then(|s| self.find_card_anywhere(s).map(|c| c.definition.name));
    896 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (6x)
      .                               if let Some(src) = source
      .                                   && src != cid
      8 ( 0.00%)                          && self
      .                                       .battlefield_find(cid)
      4 ( 0.00%)                              .is_some_and(|c| c.definition.is_planeswalker())
     64 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (4x)
      .                                   && let Some(c) = self.battlefield_find_mut(src)
      .                                   && !c.damaged_permanents_this_game.contains(&cid)
      .                               {
      .                                   c.damaged_permanents_this_game.push(cid);
      .                               }
      .                               // Lichenthrope — the victim itself converts damage to
      .                               // -1/-1 counters, whatever the source is.
     32 ( 0.00%)                      let victim_converts = self.computed_permanent(cid).is_some_and(|cp| {
 10,906 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (6x)
      .                                   cp.keywords.contains(&crate::card::Keyword::DamageBecomesMinusCounters)
     20 ( 0.00%)                      });
      .                               if let Some(c) = self.battlefield_find_mut(cid) {
     12 ( 0.00%)                      if c.definition.is_creature() {
     52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
      4 ( 0.00%)                          c.dealt_damage_this_turn = true;
      4 ( 0.00%)                          c.damage_dealt_to_this_turn += amount;
      8 ( 0.00%)                          if let Some(src) = source {
     12 ( 0.00%)                              c.damaged_by_this_turn.push(src);
     12 ( 0.00%)                              c.record_damage_from(src, amount);
    864 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::record_damage_from (4x)
      .                                   }
      8 ( 0.00%)                          if let Some(name) = source_name {
     20 ( 0.00%)                              c.record_damage_from_named(name, amount);
    880 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::record_damage_from_named (4x)
      .                                   }
      .                               }
      8 ( 0.00%)                      if (source_has_wither || victim_converts) && c.definition.is_creature() {
      .                                   c.add_counters(CounterType::MinusOneMinusOne, amount);
      .                                   events.push(GameEvent::CounterAdded {
      .                                       card_id: cid,
      .                                       counter_type: CounterType::MinusOneMinusOne,
      .                                       count: amount,
      .                                   });
      .                               } else {
      4 ( 0.00%)                          c.damage += amount;
      8 ( 0.00%)                          if source_has_deathtouch && c.definition.is_creature() {
      .                                       c.dealt_deathtouch_damage = true;
      .                                   }
      .                               }
     60 ( 0.00%)                      events.push(GameEvent::DamageDealt {
      .                                   amount,
      .                                   to_player: None,
      .                                   to_card: Some(cid),
      .                                   combat: false,
      .                                   from_controller,
      .                                   from_card: source,
      .                               });
      4 ( 0.00%)                      let is_creature = c.definition.is_creature();
     52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
      8 ( 0.00%)                      if source_exiles_damaged && is_creature {
      .                                   self.dies_to_exile_eot.insert(cid);
      .                               }
      8 ( 0.00%)                      if source_denies_regen
      .                                   && is_creature
      .                                   && let Some(v) = self.battlefield_find_mut(cid)
      .                               {
      .                                   v.cant_regenerate_this_turn = true;
      .                               }
      .                               }
      .                               // "Whenever this deals damage to a creature" (Neko-Te,
      .                               // Kumano's Blessing) — the non-combat half; the combat
      .                               // step fires the same kind from `resolve_combat`.
     12 ( 0.00%)                      if let Some(src) = source
      8 ( 0.00%)                          && self
      .                                       .battlefield_find(cid)
      4 ( 0.00%)                              .is_some_and(|c| c.definition.is_creature())
     52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
      .                               {
     16 ( 0.00%)                          self.fire_noncombat_damage_to_creature_triggers(src, cid, amount);
 13,304 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_noncombat_damage_to_creature_triggers (4x)
      .                               }
      .                           }
      .                       }
      .                       EntityRef::Card(_) => {}
      .                   }
      .                   // Record "damaged this way" for `Selector::DamagedThisResolution`
      .                   // (Aurelia's Fury). Reached only after every prevention / shield /
      .                   // replacement early-return, so it captures creatures and players that
      .                   // truly took damage. Planeswalkers/battles are excluded (only combat-
      .                   // relevant creatures + players are ever queried).
      .                   match ent {
    106 ( 0.00%)              EntityRef::Player(_) => self.damaged_this_resolution.push(ent),
      .                       EntityRef::Permanent(cid)
      8 ( 0.00%)                  if self
      .                               .battlefield_find(cid)
      4 ( 0.00%)                      .is_some_and(|c| c.definition.is_creature()) =>
     52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
      .                       {
      4 ( 0.00%)                  self.damaged_this_resolution.push(ent)
      .                       }
      .                       _ => {}
      .                   }
      .                   // Running total for "equal to the damage dealt this way" riders
      .                   // (Brightflame). Same gate as `damaged_this_resolution`: only damage
      .                   // that actually landed counts.
    348 ( 0.00%)          self.damage_dealt_this_resolution =
      .                       self.damage_dealt_this_resolution.saturating_add(amount);
      .                   // CR 702.15 — lifelink on the non-combat damage path: if the source is
      .                   // a lifelink permanent (a ping ability) or an instant/sorcery spell
      .                   // whose controller has "your spells have lifelink" (Radiant
      .                   // Scrollwielder), that controller gains life equal to the damage dealt.
      .                   // (Combat damage handles its own lifelink in `combat.rs`.)
    696 ( 0.00%)          if let Some(seat) = self.noncombat_lifelink_seat(source) {
 47,198 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::noncombat_lifelink_seat (174x)
      .                       let applied = self.adjust_life_applied(seat, amount as i32);
      .                       if applied > 0 {
      .                           events.push(GameEvent::LifeGained { player: seat, amount: applied as u32 });
      .                       }
      .                   }
      .                   // CR 603.4 — "whenever [this creature] deals damage this turn" delayed
      .                   // triggers watching the noncombat source (Paladin of Prahv's Forecast).
    348 ( 0.00%)          if let Some(src) = source {
    174 ( 0.00%)              self.fire_source_dealt_damage_watchers(src, amount);
 16,530 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_source_dealt_damage_watchers (174x)
      .                   }
      .                   // "Whenever an instant or sorcery spell you control deals damage"
      .                   // (Blaze Commando). Fires once per resolution across a multi-hit spell.
    174 ( 0.00%)          self.fire_your_spell_dealt_damage(source, amount);
 42,368 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::fire_your_spell_dealt_damage (174x)
      .                   // The damage-to-a-player sibling (Satyr Firedancer) — once per damaged
      .                   // player, with that player bound as the trigger subject.
    516 ( 0.00%)          if let EntityRef::Player(victim) = ent {
    168 ( 0.00%)              self.fire_your_spell_damaged_player(source, victim, amount);
 45,680 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::fire_your_spell_damaged_player (168x)
      .                   }
  1,566 ( 0.00%)      }
  4,524 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::card::Keyword>> (174x)
      .           
      .               /// Fire `EventKind::YourInstantOrSorceryDealtDamageToPlayer` for the caster
      .               /// of the instant/sorcery currently resolving — once per damaged player,
      .               /// with that player bound as the trigger subject so
      .               /// `SelectionRequirement::ControlledByTriggerPlayer` narrows "target
      .               /// creature that player controls" (Satyr Firedancer).
    672 ( 0.00%)      fn fire_your_spell_damaged_player(
      .                   &mut self,
      .                   source: Option<crate::card::CardId>,
      .                   victim: usize,
      .                   amount: u32,
      .               ) {
      .                   use crate::effect::{EventKind, EventScope};
      .                   if amount == 0 {
      .                       return;
      .                   }
    668 ( 0.00%)          let Some(seat) = self.resolving_spell_caster else { return };
    996 ( 0.00%)          if let (Some(src), Some((res_id, _, _, _))) = (source, &self.resolving_source)
      .                       && src != *res_id
      .                   {
      .                       return;
      .                   }
    166 ( 0.00%)          let queue: Vec<crate::game::types::PendingTriggerPush> = self
      .                       .battlefield
      .                       .iter()
  3,948 ( 0.00%)              .filter(|c| c.controller == seat)
      .                       .flat_map(|c| {
      .                           c.definition
      .                               .triggered_abilities
      .                               .iter()
      .                               .filter(|ta| {
      .                                   ta.event.kind == EventKind::YourInstantOrSorceryDealtDamageToPlayer
      .                                       && ta.event.scope == EventScope::YourControl
      .                               })
  1,316 ( 0.00%)                      .map(move |ta| crate::game::types::PendingTriggerPush {
      .                                   from_mana_ability: false,
      .                               actor: None,
      .                                   source: c.id,
      .                                   controller: c.controller,
      .                                   effect: ta.effect.clone(),
      .                                   subject: Some(EntityRef::Player(victim)),
      .                                   event_amount: amount,
      .                                   mode: None,
      .                                   intervening_if: None,
      .                               })
      .                       })
      .                       .collect();
    332 ( 0.00%)          if !queue.is_empty() {
      .                       self.drain_trigger_queue(queue);
      .                   }
    504 ( 0.00%)      }
      .           
      .               /// Fire `EventKind::YourInstantOrSorceryDealtDamage` for the caster of the
      .               /// instant/sorcery currently resolving, once per resolution (Blaze Commando).
      .               /// `source` is the damage source (the spell); we only fire when it is the
      .               /// resolving spell so combat/ability damage during the same window is
      .               /// ignored.
  1,392 ( 0.00%)      fn fire_your_spell_dealt_damage(&mut self, source: Option<crate::card::CardId>, amount: u32) {
      .                   use crate::effect::{EventKind, EventScope};
    696 ( 0.00%)          if self.spell_damage_trigger_fired || amount == 0 {
      .                       return;
      .                   }
    332 ( 0.00%)          let Some(seat) = self.resolving_spell_caster else { return };
      .                   // The damage must be dealt by the resolving spell itself.
    996 ( 0.00%)          if let (Some(src), Some((res_id, _, _, _))) = (source, &self.resolving_source)
      .                       && src != *res_id
      .                   {
      .                       return;
      .                   }
    166 ( 0.00%)          let listeners: Vec<(crate::card::CardId, crate::effect::Effect, usize)> = self
      .                       .battlefield
      .                       .iter()
  3,948 ( 0.00%)              .filter(|c| c.controller == seat)
      .                       .flat_map(|c| {
      .                           c.definition
      .                               .triggered_abilities
      .                               .iter()
      .                               .filter(|ta| {
      .                                   ta.event.kind == EventKind::YourInstantOrSorceryDealtDamage
      .                                       && ta.event.scope == EventScope::YourControl
      .                               })
      .                               .map(move |ta| (c.id, ta.effect.clone(), c.controller))
      .                       })
      .                       .collect();
    332 ( 0.00%)          if listeners.is_empty() {
      .                       return;
      .                   }
      .                   self.spell_damage_trigger_fired = true;
      .                   for (listener, effect, controller) in listeners {
      .                       let auto_target = self.auto_target_for_effect_avoiding(&effect, controller, Some(listener));
      .                       self.stack.push(
      .                           TriggerPush::new(listener, controller, effect)
      .                               .target(auto_target)
      .                               .event_amount(amount)
      .                               .build(),
      .                       );
      .                   }
  1,392 ( 0.00%)      }
      .           
      .               /// Seat that gains life from lifelink on a *non-combat* damage event from
      .               /// `source`, if any (CR 702.15). Returns the source's controller when the
      .               /// source is a lifelink permanent, or the caster of an instant/sorcery
      .               /// spell whose controller grants spells lifelink.
  1,044 ( 0.00%)      fn noncombat_lifelink_seat(&self, source: Option<crate::card::CardId>) -> Option<usize> {
      .                   use crate::card::Keyword;
      .                   // A lifelink permanent (e.g. a ping ability from a lifelink creature).
    348 ( 0.00%)          if let Some(src) = source
    878 ( 0.00%)              && let Some(cp) = self.computed_permanent(src)
 42,184 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (174x)
      .                       && cp.keywords.contains(&Keyword::Lifelink)
      .                   {
      .                       return Some(cp.controller);
      .                   }
      .                   // The currently-resolving instant/sorcery whose controller grants
      .                   // spells lifelink (stamped in `resolve_top_of_stack`).
    348 ( 0.00%)          self.resolving_spell_lifelink_seat
  1,044 ( 0.00%)      }
      .           
      .               /// True if a "that spell gains [keyword]" grant is riding the stack item
      .               /// for `spell` (Judith, Carnage Connoisseur).
      .               pub(crate) fn spell_granted_keyword(
      .                   &self,
      .                   spell: CardId,
      .                   kw: &crate::card::Keyword,
      .               ) -> bool {
-- line 1696 ----------------------------------------
-- line 1697 ----------------------------------------
      .                   self.spell_keyword_grants.iter().any(|(id, k)| *id == spell && k == kw)
      .               }
      .           
      .               /// True if `seat` controls a permanent granting
      .               /// `StaticEffect::YourInstantSorcerySpellsHaveLifelink` (Radiant Scrollwielder).
      .               pub(crate) fn controller_grants_spell_lifelink(&self, seat: usize) -> bool {
      .                   use crate::effect::StaticEffect;
      .                   self.battlefield.iter().any(|c| {
 51,176 ( 0.00%)              c.controller == seat
      .                           && c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(sa.effect, StaticEffect::YourInstantSorcerySpellsHaveLifelink)
      .                           })
      .                   })
      .               }
      .           
      .               /// True if `seat` controls a permanent granting
      .               /// `StaticEffect::YourISSpellsHaveDeathtouch` (Pestilent Spirit).
      .               pub(crate) fn controller_grants_spell_deathtouch(&self, seat: usize) -> bool {
      .                   use crate::effect::StaticEffect;
      .                   self.battlefield.iter().any(|c| {
 51,176 ( 0.00%)              c.controller == seat
      .                           && c.definition.static_abilities.iter().any(|sa| {
      .                               matches!(sa.effect, StaticEffect::YourISSpellsHaveDeathtouch)
      .                           })
      .                   })
      .               }
      .           
      .               /// CR 701.34 — manifest the card `cid` (in player `p`'s library): flip it
      .               /// face down in place so it enters as a vanilla 2/2 (no real-card ETB
-- line 1725 ----------------------------------------
-- line 1793 ----------------------------------------
      .                   }
      .                   for cid in from_gy {
      .                       self.note_left_graveyard(owner, cid, events);
      .                   }
      .                   
      .                   self.shuffle_library(owner, events);
      .               }
      .           
  1,536 ( 0.00%)      pub fn move_card_to(
      .                   &mut self,
      .                   cid: CardId,
      .                   dest: &ZoneDest,
      .                   ctx: &EffectContext,
      .                   events: &mut Vec<GameEvent>,
      .               ) {
      .                   // CR 607 — the linked-exile stamp, applied after the hop lands.
  1,152 ( 0.00%)          if matches!(dest, ZoneDest::ExileWithSourceStamp) {
      .                       self.move_card_to(cid, &ZoneDest::Exile, ctx, events);
      .                       if let Some(c) = self.exile.iter_mut().find(|c| c.id == cid) {
      .                           c.exiled_with = ctx.source;
      .                       }
      .                       return;
      .                   }
      .                   // CR 400.4a — a nonpermanent card that would enter the battlefield
      .                   // remains in its previous zone. Face-down cards are exempt: they enter
-- line 1817 ----------------------------------------
-- line 1847 ----------------------------------------
      .                       if blocked {
      .                           return;
      .                       }
      .                   }
      .                   // Resolve any selector-based player refs in the destination *now*,
      .                   // while the card is still findable in its source zone — otherwise
      .                   // `PlayerRef::OwnerOf(Target(0))` can't see the card after we remove
      .                   // it. The resolved dest uses concrete `PlayerRef::You`-anchored refs.
    512 ( 0.00%)          let resolved_dest = self.resolve_zonedest_player(dest, ctx);
 87,553 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::resolve_zonedest_player (128x)
      .           
      .                   // Try battlefield first.
    128 ( 0.00%)          if let Some(pos) = self.battlefield.iter().position(|c| c.id == cid) {
      .                       let mut card = self.battlefield.remove(pos);
      .                       self.remove_effects_from_source(cid);
      .                       self.collect_leaver_counters(&card);
      .                       // CR 708.10 — a face-down permanent is turned face up as it leaves
      .                       // the battlefield (no-op unless it carries a stashed real def).
      .                       card.turn_face_up();
      .                       // CR 709.5c — Room unlocked designations are battlefield-only.
      .                       card.reset_room_doors();
-- line 1866 ----------------------------------------
-- line 1916 ----------------------------------------
      .                           events.push(GameEvent::CreatureLeftWithoutDying { card_id, controller });
      .                       }
      .                       return;
      .                   }
      .                   // Then graveyards. Emit `CardLeftGraveyard` so Strixhaven
      .                   // "cards leave your graveyard" payoffs (Garrison Excavator,
      .                   // Living History, Spirit Mascot, Hardened Academic) trigger.
      .                   for p in 0..self.players.len() {
    396 ( 0.00%)              if let Some(pos) = self.players[p].graveyard.iter().position(|c| c.id == cid) {
    504 ( 0.00%)                  let card = self.players[p].graveyard.remove(pos);
 34,215 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (126x)
    252 ( 0.00%)                  self.players[p].cards_left_graveyard_this_turn =
  3,780 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (126x)
    252 ( 0.00%)                      self.players[p].cards_left_graveyard_this_turn.saturating_add(1);
    378 ( 0.00%)                  events.push(GameEvent::CardLeftGraveyard { player: p, card_id: cid });
      .                           // CR 400.7 — "put into exile from a graveyard" (Ketramose).
    630 ( 0.00%)                  if matches!(resolved_dest, ZoneDest::Exile | ZoneDest::ExilePlotted) {
     32 ( 0.00%)                      events.push(GameEvent::CardExiledFromPlayOrGraveyard { card_id: cid });
      .                           }
      .                           // Prized Amalgam's gate — record gy→battlefield entries.
    220 ( 0.00%)                  if matches!(resolved_dest, ZoneDest::Battlefield { .. }) {
      .                               self.entered_from_graveyard_this_turn.insert(cid);
      .                           }
      .                           // "When this card is put into your hand from your graveyard"
      .                           // (Golgari Brownscale). Emitted for any graveyard→hand return.
    220 ( 0.00%)                  if matches!(resolved_dest, ZoneDest::Hand(_)) {
    330 ( 0.00%)                      events.push(GameEvent::CardPutIntoHandFromGraveyard { player: p, card_id: cid });
      .                           }
    630 ( 0.00%)                  self.place_card_in_dest(card, p, &resolved_dest, events);
149,336 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::place_card_in_dest (126x)
      .                           return;
      .                       }
      .                   }
      .                   // Then exile.
      2 ( 0.00%)          if let Some(pos) = self.exile.iter().position(|c| c.id == cid) {
      4 ( 0.00%)              let mut card = self.exile.remove(pos);
      2 ( 0.00%)              let owner = card.owner;
      .                       // CR 406.7 — re-exiling an exiled object doesn't change zones, but
      .                       // it becomes a *new* object that has just been exiled: the first
      .                       // exiler's linked abilities (CR 607) can no longer find it.
      6 ( 0.00%)              if matches!(
      2 ( 0.00%)                  resolved_dest,
      .                           ZoneDest::Exile | ZoneDest::ExilePlotted | ZoneDest::ExileWithSourceStamp
      .                       ) {
      2 ( 0.00%)                  card.exiled_with = None;
      2 ( 0.00%)                  card.exiled_by = None;
      .                       }
      .                       // Fire Lord Zuko's gate — record exile→battlefield entries so a
      .                       // "whenever a permanent you control enters from exile" trigger can
      .                       // distinguish them from cast / reanimate-from-graveyard entries.
      2 ( 0.00%)              if matches!(resolved_dest, ZoneDest::Battlefield { .. }) {
      .                           self.entered_from_exile_this_turn.insert(cid);
      .                       }
     14 ( 0.00%)              self.place_card_in_dest(card, owner, &resolved_dest, events);
  1,004 ( 0.00%)  => crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::place_card_in_dest (2x)
      .                       return;
      .                   }
      .                   // Hands. Used by start-of-game opening-hand effects
      .                   // (Leyline of Sanctity, Gemstone Caverns) that move a hand card
      .                   // to the battlefield.
      .                   for p in 0..self.players.len() {
      .                       if let Some(pos) = self.players[p].hand.iter().position(|c| c.id == cid) {
      .                           let card = self.players[p].hand.remove(pos);
-- line 1974 ----------------------------------------
-- line 1981 ----------------------------------------
      .                   // Daydream's exile-then-return flicker pattern in passing).
      .                   for p in 0..self.players.len() {
      .                       if let Some(pos) = self.players[p].library.iter().position(|c| c.id == cid) {
      .                           let card = self.players[p].library.remove(pos);
      .                           self.place_card_in_dest(card, p, &resolved_dest, events);
      .                           return;
      .                       }
      .                   }
  1,024 ( 0.00%)      }
      .           
      .               /// Pre-resolve any selector-based player refs in a `ZoneDest` against
      .               /// the active ctx. `place_card_in_dest` constructs its own bare ctx and
      .               /// can't see the caster's targets, so any `PlayerRef::OwnerOf(Selector)`
      .               /// / `ControllerOf(Selector)` need to be flattened to a concrete
      .               /// `PlayerRef::Seat(n)` while the source card is still in its origin
      .               /// zone. Other ref kinds (You / ActivePlayer / etc.) pass through.
    640 ( 0.00%)      pub(super) fn resolve_zonedest_player(&self, dest: &ZoneDest, ctx: &EffectContext) -> ZoneDest {
      .                   let flatten = |who: &PlayerRef| -> PlayerRef {
    550 ( 0.00%)              match who {
      .                           PlayerRef::OwnerOf(_) | PlayerRef::ControllerOf(_) => {
    770 ( 0.00%)                      if let Some(p) = self.resolve_player(who, ctx) {
 81,567 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_player (110x)
    330 ( 0.00%)                          PlayerRef::Seat(p)
      .                               } else {
      .                                   who.clone()
      .                               }
      .                           }
      .                           // Flatten `PlayerRef::You` to the caster's seat now —
      .                           // `place_card_in_dest` builds its own context anchored to
      .                           // the card's *origin owner* (which is the graveyard owner
      .                           // for gy-to-bf moves like Mind Roots, not the caster). If
-- line 2010 ----------------------------------------
-- line 2011 ----------------------------------------
      .                           // we don't flatten here, "controller: PlayerRef::You" on a
      .                           // ZoneDest::Battlefield would end up resolving to the
      .                           // graveyard's owner instead of the caster, putting the
      .                           // stolen land back under the opp's control.
      .                           PlayerRef::You => PlayerRef::Seat(ctx.controller),
      .                           _ => who.clone(),
      .                       }
      .                   };
  1,024 ( 0.00%)          match dest {
  1,430 ( 0.00%)              ZoneDest::Hand(who) => ZoneDest::Hand(flatten(who)),
      .                       ZoneDest::Library { who, pos } => ZoneDest::Library {
      .                           who: flatten(who),
      .                           pos: *pos,
      .                       },
      .                       ZoneDest::Battlefield { controller, tapped } => ZoneDest::Battlefield {
      .                           controller: flatten(controller),
      .                           tapped: *tapped,
      .                       },
      .                       ZoneDest::Graveyard
      .                       | ZoneDest::Exile
      .                       | ZoneDest::ExilePlotted
      .                       | ZoneDest::ExileWithSourceStamp
      .                       | ZoneDest::Ante => dest.clone(),
      .                   }
    768 ( 0.00%)      }
      .           
  1,280 ( 0.00%)      pub(crate) fn place_card_in_dest(
      .                   &mut self,
      .                   mut card: CardInstance,
      .                   default_player: usize,
      .                   dest: &ZoneDest,
      .                   events: &mut Vec<GameEvent>,
      .               ) {
      .                   // Phase H — consult the replacement-effect registry. The
      .                   // resolver only sees the *destination kind* (a `Zone`); the
-- line 2045 ----------------------------------------
-- line 2046 ----------------------------------------
      .                   // origin is left unconstrained here (passed as
      .                   // `Zone::Battlefield` for now, which covers the Commander
      .                   // case since its replacement effect uses `from: None`).
      .                   // If the resolver redirects to a different zone, we hand off
      .                   // to `place_card_at_resolved_zone` which handles the
      .                   // terminal-zone placement uniformly. Same-zone return falls
      .                   // through to the existing rich `ZoneDest` logic so player /
      .                   // tapped / library-position information is preserved.
  1,536 ( 0.00%)          let intended = match dest {
      .                       ZoneDest::Hand(_) => crate::card::Zone::Hand,
      .                       ZoneDest::Library { .. } => crate::card::Zone::Library,
      .                       ZoneDest::Battlefield { .. } => crate::card::Zone::Battlefield,
      .                       ZoneDest::Graveyard => crate::card::Zone::Graveyard,
      .                       ZoneDest::Ante => crate::card::Zone::Ante,
      .                       ZoneDest::Exile | ZoneDest::ExilePlotted | ZoneDest::ExileWithSourceStamp => {
      .                           crate::card::Zone::Exile
      .                       }
-- line 2062 ----------------------------------------
-- line 2063 ----------------------------------------
      .                   };
      .                   // CR 702.47e — a spell loses its splice changes once it leaves the
      .                   // stack for any reason.
      .                   card.spliced_effects.clear();
      .                   card.spliced_names.clear();
      .                   // CR 712.16/712.17 — a melded permanent leaving the battlefield
      .                   // leaves as its two component cards; the melded shell ceases to
      .                   // exist.
    384 ( 0.00%)          if !card.meld_parts.is_empty() && intended != crate::card::Zone::Battlefield {
      .                       for part in std::mem::take(&mut card.meld_parts) {
      .                           self.place_card_in_dest(part, default_player, dest, events);
      .                       }
      .                       return;
      .                   }
      .                   // CR 702.140e — a merged (mutated) permanent leaving the battlefield
      .                   // becomes its individual component cards in that zone.
    256 ( 0.00%)          if !card.mutate_stack.is_empty() && intended != crate::card::Zone::Battlefield {
      .                       for part in std::mem::take(&mut card.mutate_stack) {
      .                           self.place_card_in_dest(part, default_player, dest, events);
      .                       }
      .                       return;
      .                   }
    640 ( 0.00%)          let resolved = self.resolve_zone_change(
 12,928 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::resolve_zone_change (128x)
    256 ( 0.00%)              card.id,
      .                       crate::card::Zone::Battlefield,
      .                       intended,
      .                   );
    128 ( 0.00%)          if resolved != intended {
      .                       self.place_card_at_resolved_zone(card, resolved);
      .                       return;
      .                   }
      .                   // CR 717.6 — a card with an Astrotorium back headed anywhere but the
      .                   // battlefield, exile, or the command zone is put into its owner's
      .                   // face-up "junkyard" pile in the command zone instead.
    256 ( 0.00%)          if !card.definition.attraction_lights.is_empty()
      .                       && matches!(
      .                           intended,
      .                           crate::card::Zone::Hand
      .                               | crate::card::Zone::Library
      .                               | crate::card::Zone::Graveyard
      .                       )
      .                   {
      .                       card.counters.clear();
-- line 2105 ----------------------------------------
-- line 2110 ----------------------------------------
      .                       return;
      .                   }
      .                   // CR 122.2 — counters are not retained when an object changes
      .                   // zones; they cease to exist. Cleared for every destination (the
      .                   // battlefield arm additionally re-seeds planeswalker loyalty).
      .                   // Dies-with-counters triggers read the `died_card_snapshots` /
      .                   // `leaves_bf_lki` LKI caches, not the new zone's object.
      .                   card.counters.clear();
    384 ( 0.00%)          card.keyword_counters.clear();
  2,432 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::KeywordCounters::clear (128x)
      .                   // CR 710.4 — a flip card has only its normal characteristics off the
      .                   // battlefield, and keeps no memory of having flipped. (The
      .                   // replacement-redirect path does this in `place_card_at_resolved_zone`;
      .                   // this is the direct path. The DFC/prototype reverts stay on that path
      .                   // only — a defeated battle transforms *through* exile.)
    256 ( 0.00%)          if intended != crate::card::Zone::Battlefield {
    128 ( 0.00%)              card.revert_flip();
  5,760 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::revert_flip (128x)
      .                   }
    384 ( 0.00%)          match dest {
      .                       ZoneDest::Hand(who) => {
    880 ( 0.00%)                  let ctx = EffectContext::for_spell(default_player, None, 0, 0);
  5,170 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::EffectContext::for_spell (110x)
      .                           // `OwnerOfMoved` routes the card to *its own* owner (per-card
      .                           // board-bounce — Aetherize / Evacuation).
    220 ( 0.00%)                  let p = match who {
      .                               PlayerRef::OwnerOfMoved => card.owner,
    440 ( 0.00%)                      _ => self.resolve_player(who, &ctx).unwrap_or(default_player),
  2,750 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_player (110x)
      .                           };
    110 ( 0.00%)                  card.controller = p;
    440 ( 0.00%)                  self.players[p].hand.push(card);
  3,300 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (110x)
      .                       }
      .                       ZoneDest::Library { who, pos } => {
      .                           let ctx = EffectContext::for_spell(default_player, None, 0, 0);
      .                           let p = match who {
      .                               PlayerRef::OwnerOfMoved => card.owner,
      .                               _ => self.resolve_player(who, &ctx).unwrap_or(default_player),
      .                           };
      .                           // A one-shot top reveal (Aven Windreader) only covers the card
-- line 2145 ----------------------------------------
-- line 2242 ----------------------------------------
      .                           let cid = card.id;
      .                           self.exile.push(card);
      .                           self.plotted_cards.insert(cid);
      .                           self.plotted_this_turn.insert(cid);
      .                           events.push(GameEvent::PermanentExiled { card_id: cid });
      .                           self.fire_becomes_plotted_triggers(cid, default_player);
      .                       }
      .                       ZoneDest::Exile | ZoneDest::ExileWithSourceStamp => {
     18 ( 0.00%)                  let cid = card.id;
     36 ( 0.00%)                  self.exile.push(card);
      .                           // Record for `Selector::ExiledThisResolution` ("if you exiled
      .                           // a [type] card this way" — Bonehoard Dracosaur).
     14 ( 0.00%)                  self.exiled_card_ids_this_resolution.push(cid);
      .                           // Bump the controller-of-the-exile-effect's per-turn
      .                           // exile tally for Strixhaven "if one or more cards were
      .                           // put into exile this turn" payoffs (Ennis the Debate
      .                           // Moderator). Reset on `do_untap`.
     36 ( 0.00%)                  if default_player < self.players.len() {
     36 ( 0.00%)                      self.players[default_player].cards_exiled_this_turn =
    540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (18x)
     36 ( 0.00%)                          self.players[default_player].cards_exiled_this_turn.saturating_add(1);
      .                           }
      .                           events.push(GameEvent::PermanentExiled { card_id: cid });
      .                       }
      .                       ZoneDest::Battlefield { controller, tapped } => {
      .                           // CR 614.x — Containment Priest. A nontoken creature put onto
      .                           // the battlefield without being cast (reanimate / blink /
      .                           // reveal-and-put — anything routed through this path rather
      .                           // than `resolve_spell`) is exiled instead.
-- line 2269 ----------------------------------------
-- line 2463 ----------------------------------------
      .                           // destination seat.
      .                           let etb_ctrl = self
      .                               .battlefield
      .                               .iter()
      .                               .find(|c| c.id == cid)
      .                               .map(|c| c.controller)
      .                               .unwrap_or(p);
      .                           self.fire_self_etb_triggers(cid, etb_ctrl);
    110 ( 0.00%)              }
      .                   }
  1,024 ( 0.00%)      }
      .           
      .               /// Shared leaves-battlefield bookkeeping, called from every
      .               /// battlefield-removal path: CR 603.6e linked-exile returns plus
      .               /// "when [that permanent] leaves the battlefield" delayed triggers
      .               /// (`DelayedKind::WhenCardLeavesBattlefield` — Hofri Ghostforge).
 77,640 ( 0.00%)      pub(crate) fn on_left_battlefield(&mut self, id: CardId, events: &mut Vec<GameEvent>) {
      .                   self.return_linked_exiles(id, events);
      .                   // CR 702.26 — permanents phased out "until [this] leaves the
      .                   // battlefield" (Out of Time) phase in now.
      .                   let mut i = 0;
      .                   let mut phased_in: Vec<CardId> = Vec::new();
 11,646 ( 0.00%)          while i < self.phased_out.len() {
      .                       if self.phased_out[i].phased_out_by == Some(id) {
      .                           let mut c = self.phased_out.remove(i);
      .                           c.phased_out_by = None;
      .                           phased_in.push(c.id);
      .                           self.battlefield.push(c);
      .                       } else {
      .                           i += 1;
      .                       }
-- line 2493 ----------------------------------------
-- line 2500 ----------------------------------------
      .                   let mut kept = Vec::new();
      .                   for tc in std::mem::take(&mut self.temporary_control) {
      .                       if tc.source == Some(id) {
      .                           self.change_control(tc.card, tc.original_controller);
      .                       } else {
      .                           kept.push(tc);
      .                       }
      .                   }
 31,056 ( 0.00%)          self.temporary_control = kept;
      .                   // CR 400.7 — the card is a new object in its next zone: effects
      .                   // that granted abilities to the permanent don't follow it, and
      .                   // per-object activation limits (CR 602.5f "only once each turn",
      .                   // CR 702.177a exhaust's "activate only once") start over.
 15,528 ( 0.00%)          if let Some(c) = self.find_card_anywhere_mut(id) {
1,581,714 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::find_card_anywhere_mut (3,882x)
      .                       c.granted_activated_abilities.clear();
      .                       c.granted_activated_eot.clear();
      .                       c.exhausted_abilities.clear();
      .                       c.once_per_turn_used.clear();
      .                       // …and so does how it got here: a reanimated permanent wasn't cast
      .                       // from anywhere, so "if you didn't cast it from your hand" riders
      .                       // see the new object (Phage the Untouchable).
  3,882 ( 0.00%)              c.cast_from_hand = false;
  3,882 ( 0.00%)              c.cast_from_exile = false;
  3,882 ( 0.00%)              c.cast_from_library = false;
  3,882 ( 0.00%)              c.cast_via_flashback = false;
  3,882 ( 0.00%)              c.cast_from_suspend = false;
  3,882 ( 0.00%)              c.cast_from_escape = false;
      .                   }
      .                   // CR 611.2c — continuous effects aimed at this specific permanent
      .                   // end with it (don't re-attach if the same card re-enters).
 11,646 ( 0.00%)          for e in self.continuous_effects.iter_mut() {
 97,050 ( 0.00%)  => /home/user/Crabomination/crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut (3,882x)
      .                       if let crate::game::layers::AffectedPermanents::Specific(ids) = &mut e.affected {
      .                           ids.retain(|cid| *cid != id);
      .                       }
      .                   }
 15,528 ( 0.00%)          self.continuous_effects.retain(|e| {
 97,050 ( 0.00%)  => /home/user/Crabomination/crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut (3,882x)
 69,876 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (3,882x)
      .                       !matches!(&e.affected,
      .                           crate::game::layers::AffectedPermanents::Specific(ids) if ids.is_empty())
      .                   });
      .                   use crate::game::types::DelayedKind;
      .                   let mut fire: Vec<crate::game::types::DelayedTrigger> = Vec::new();
 15,528 ( 0.00%)          self.delayed_triggers.retain(|dt| {
 77,640 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (3,882x)
      .                       if dt.kind == DelayedKind::WhenCardLeavesBattlefield(id)
      .                           || dt.kind == DelayedKind::WhenCardLeavesBattlefieldThisTurn(id)
      .                       {
      .                           fire.push(dt.clone());
      .                           false
      .                       } else {
      .                           true
      .                       }
      .                   });
 31,056 ( 0.00%)          for dt in fire {
      .                       self.stack.push(
      .                           TriggerPush::new(dt.source, dt.controller, dt.effect)
      .                               .target(dt.target)
      .                               .trigger_source(Some(super::EntityRef::Card(id)))
      .                               .build(),
      .                       );
      .                   }
 31,056 ( 0.00%)      }
      .           
      .               /// CR 603.6e — when a permanent that exiled card(s) via
      .               /// `Effect::ExileUntilSourceLeaves` leaves the battlefield, return the
      .               /// linked card(s) to the zone the linking ability specified
      .               /// (battlefield for Banisher Priest / Oblivion Ring, hand for Brain
      .               /// Maggot / Tidehollow Sculler). Called from every battlefield-removal
      .               /// path. The return is resolved directly rather than as a stack
      .               /// trigger — a deliberate simplification; the observable result (the
      .               /// card comes back) matches the printed linked ability.
      .               pub(crate) fn return_linked_exiles(
      .                   &mut self,
      .                   source: CardId,
      .                   events: &mut Vec<GameEvent>,
      .               ) {
      .                   use crate::card::ExileReturnZone;
  3,882 ( 0.00%)          let linked: Vec<CardId> = self
      .                       .exile
      .                       .iter()
      .                       // Monarch-guarded exiles (Palace Jailer) return when the monarchy
      .                       // moves, not when the source leaves — `set_monarch` handles them.
      .                       .filter(|c| {
  1,048 ( 0.00%)                  c.exiled_by.map(|l| l.source) == Some(source)
      .                               && c.exiled_by.and_then(|l| l.monarch_guard).is_none()
      .                       })
      .                       .map(|c| c.id)
      .                       .collect();
      .                   for cid in linked {
      .                       let Some(pos) = self.exile.iter().position(|c| c.id == cid) else {
      .                           continue;
      .                       };
-- line 2589 ----------------------------------------

240,040 ( 0.01%)  <counts for unidentified lines in crabomination/src/game/effects/movement.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/fxhash.rs
--------------------------------------------------------------------------------
Ir              

-- line 45 ----------------------------------------
     .           #[derive(Debug, Default, Clone, Copy)]
     .           pub struct FxHasher {
     .               hash: u64,
     .           }
     .           
     .           impl FxHasher {
     .               #[inline]
     .               fn add(&mut self, word: u64) {
51,812 ( 0.00%)          self.hash = (self.hash.rotate_left(5) ^ word).wrapping_mul(SEED);
     .               }
     .           }
     .           
     .           impl Hasher for FxHasher {
     .               #[inline]
     .               fn write(&mut self, bytes: &[u8]) {
     .                   let mut rest = bytes;
     .                   while let Some((chunk, tail)) = rest.split_first_chunk::<8>() {
     .                       self.add(u64::from_ne_bytes(*chunk));
     .                       rest = tail;
     .                   }
     .                   if let Some((chunk, tail)) = rest.split_first_chunk::<4>() {
 5,818 ( 0.00%)              self.add(u32::from_ne_bytes(*chunk) as u64);
     .                       rest = tail;
     .                   }
     .                   if let Some((chunk, tail)) = rest.split_first_chunk::<2>() {
 4,218 ( 0.00%)              self.add(u16::from_ne_bytes(*chunk) as u64);
     .                       rest = tail;
     .                   }
 3,922 ( 0.00%)          if let Some(&b) = rest.first() {
     .                       self.add(b as u64);
     .                   }
     .               }
     .           
     .               #[inline]
     .               fn write_u8(&mut self, n: u8) {
     .                   self.add(n as u64);
     .               }
-- line 81 ----------------------------------------
-- line 82 ----------------------------------------
     .           
     .               #[inline]
     .               fn write_u16(&mut self, n: u16) {
     .                   self.add(n as u64);
     .               }
     .           
     .               #[inline]
     .               fn write_u32(&mut self, n: u32) {
73,432 ( 0.00%)          self.add(n as u64);
     .               }
     .           
     .               #[inline]
     .               fn write_u64(&mut self, n: u64) {
     .                   self.add(n);
     .               }
     .           
     .               #[inline]
-- line 98 ----------------------------------------

71,194 ( 0.00%)  <counts for unidentified lines in crabomination/src/fxhash.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/turn_step.rs
--------------------------------------------------------------------------------
Ir               

-- line 9 ----------------------------------------
      .           // ── Turn step sequence ────────────────────────────────────────────────────────
      .           
      .           /// `PartialOrd`/`Ord` follow declaration order, which *is* the turn order,
      .           /// so `step < TurnStep::CombatDamage` reads as "earlier in the turn". Only
      .           /// meaningful within a single turn — the comparison says nothing across a
      .           /// turn boundary, where `Cleanup` precedes the next turn's `Untap` in time
      .           /// but sorts after it.
      .           #[derive(
125,750 ( 0.00%)      Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize,
      .           )]
      .           pub enum TurnStep {
      .               Untap,
      .               Upkeep,
      .               Draw,
      .               PreCombatMain,
      .               BeginCombat,
      .               DeclareAttackers,
-- line 25 ----------------------------------------
-- line 29 ----------------------------------------
      .               EndCombat,
      .               PostCombatMain,
      .               End,
      .               Cleanup,
      .           }
      .           
      .           impl TurnStep {
      .               pub fn next(self) -> Self {
127,720 ( 0.00%)          match self {
      .                       TurnStep::Untap => TurnStep::Upkeep,
      .                       TurnStep::Upkeep => TurnStep::Draw,
      .                       TurnStep::Draw => TurnStep::PreCombatMain,
      .                       TurnStep::PreCombatMain => TurnStep::BeginCombat,
  2,614 ( 0.00%)              TurnStep::BeginCombat => TurnStep::DeclareAttackers,
      .                       TurnStep::DeclareAttackers => TurnStep::DeclareBlockers,
      .                       TurnStep::DeclareBlockers => TurnStep::FirstStrikeDamage,
     62 ( 0.00%)              TurnStep::FirstStrikeDamage => TurnStep::CombatDamage,
      .                       TurnStep::CombatDamage => TurnStep::EndCombat,
      .                       TurnStep::EndCombat => TurnStep::PostCombatMain,
      .                       TurnStep::PostCombatMain => TurnStep::End,
      .                       TurnStep::End => TurnStep::Cleanup,
  1,718 ( 0.00%)              TurnStep::Cleanup => TurnStep::Untap,
      .                   }
      .               }
      .           
      .               pub fn is_main_phase(self) -> bool {
 15,712 ( 0.00%)          matches!(self, TurnStep::PreCombatMain | TurnStep::PostCombatMain)
      .               }
      .           
      .               pub fn is_combat_phase(self) -> bool {
  6,936 ( 0.00%)          matches!(
      .                       self,
      .                       TurnStep::BeginCombat
      .                           | TurnStep::DeclareAttackers
      .                           | TurnStep::DeclareBlockers
      .                           | TurnStep::FirstStrikeDamage
      .                           | TurnStep::CombatDamage
      .                           | TurnStep::EndCombat
      .                   )
-- line 67 ----------------------------------------

 31,140 ( 0.00%)  <counts for unidentified lines in crabomination_base/src/turn_step.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/types.rs
--------------------------------------------------------------------------------
Ir                 

-- line 119 ----------------------------------------
        .           ///
        .           /// Holds a handful of entries — it is cleared at the end of every dispatch
        .           /// batch — so the linear scans are cheaper than hashing.
        .           #[derive(Debug, Clone)]
        .           pub struct IdMap<K, V>(Vec<(K, V)>);
        .           
        .           impl<K, V> Default for IdMap<K, V> {
        .               fn default() -> Self {
       24 ( 0.00%)          Self(Vec::new())
        .               }
        .           }
        .           
        .           impl<K: PartialEq, V> IdMap<K, V> {
        .               pub fn get(&self, k: &K) -> Option<&V> {
        .                   self.0.iter().find(|(x, _)| x == k).map(|(_, v)| v)
        .               }
        .               pub fn get_mut(&mut self, k: &K) -> Option<&mut V> {
-- line 135 ----------------------------------------
-- line 161 ----------------------------------------
        .               }
        .               pub fn len(&self) -> usize {
        .                   self.0.len()
        .               }
        .               pub fn is_empty(&self) -> bool {
        .                   self.0.is_empty()
        .               }
        .               pub fn values(&self) -> impl Iterator<Item = &V> {
    3,806 ( 0.00%)          self.0.iter().map(|(_, v)| v)
        .               }
        .               pub fn iter(&self) -> impl Iterator<Item = (&K, &V)> {
        .                   self.0.iter().map(|(k, v)| (k, v))
        .               }
        .           }
        .           
        .           #[cfg(test)]
        .           mod id_set_tests {
-- line 177 ----------------------------------------
-- line 240 ----------------------------------------
        .           // ── Turn step sequence ────────────────────────────────────────────────────────
        .           
        .           // `TurnStep` now lives in `crabomination_base` (below the card catalog in the
        .           // crate graph) so that cards keying off specific steps don't pull in game
        .           // logic. Re-exported here so existing `game::types::TurnStep` /
        .           // `game::TurnStep` paths keep working unchanged.
        .           pub use crabomination_base::TurnStep;
        .           
  106,704 ( 0.00%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum Target {
        .               Player(usize),
        .               Permanent(CardId),
        .           }
        .           
        .           /// What an attacking creature is attacking. In multiplayer each attacker
        .           /// chooses one of the defending players or a planeswalker controlled by one
        .           /// of them; in 2-player games this is always `Player(opponent)`.
    8,464 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum AttackTarget {
        .               Player(usize),
        .               Planeswalker(CardId),
        .               /// CR 508.4 — a Battle controlled by a player other than its protector.
        .               /// In 2-player games this is the active player's own Siege (defended by the
        .               /// opponent it chose as protector).
        .               Battle(CardId),
        .           }
-- line 265 ----------------------------------------
-- line 266 ----------------------------------------
        .           
        .           /// One attacker's declared assignment.
        .           #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
        .           pub struct Attack {
        .               pub attacker: CardId,
        .               pub target: AttackTarget,
        .           }
        .           
  192,270 ( 0.01%)  #[derive(Debug, Clone, Serialize, Deserialize)]
        .           pub enum GameAction {
        .               PlayLand(CardId),
        .               /// Play a modal-double-faced-card land using its **back face**. The
        .               /// resulting `CardInstance.definition` is swapped to the back face's
        .               /// definition before entering the battlefield, so all subsequent abilities
        .               /// (mana abilities, ETB triggers, land types) come from the back face.
        .               PlayLandBack(CardId),
        .               /// CR 305 — play a land from the controller's graveyard, legal only while
        .               /// a `StaticEffect::MayPlayLandsFromGraveyard` permanent (Crucible of
        .               /// Worlds, Ramunap Excavator) is in play.
        .               PlayLandFromGraveyard(CardId),
        .               /// CR 702.139 — Companion: pay {3} at sorcery speed to move the
        .               /// designated companion from the sideboard ("outside the game") into
        .               /// its owner's hand. Once per game (the card leaves the sideboard).
        .               CompanionToHand(CardId),
   10,040 ( 0.00%)      CastSpell { card_id: CardId, target: Option<Target>, additional_targets: Vec<Target>, mode: Option<usize>, x_value: Option<u32> },
  145,580 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (5,020x)
        .               /// CR 702.32 — cast a spell paying its optional Kicker cost in addition
        .               /// to the mana cost. The resolving spell is stamped `kicked` so
        .               /// `Predicate::SpellWasKicked` riders fire (Tear Asunder).
        .               /// CR 702.32b — cast a spell paying any subset of its `kicker_options`
        .               /// ("Kicker {A} and/or {B}" — the Volver cycle). Each paid index is
        .               /// stamped on the spell for `Predicate::SpellWasKickedWith`.
        .               CastSpellKickers {
        .                   card_id: CardId,
-- line 298 ----------------------------------------
-- line 684 ----------------------------------------
        .                   target: Option<Target>,
        .                   #[serde(default)]
        .                   additional_targets: Vec<Target>,
        .                   mode: Option<usize>,
        .                   x_value: Option<u32>,
        .               },
        .               ActivateAbility {
        .                   card_id: CardId,
       38 ( 0.00%)          ability_index: usize,
        .                   target: Option<Target>,
        .                   /// Extra chosen targets (slots 1+) for two-target activated abilities
        .                   /// (Autumn-Tail, Kitsune Sage). Empty for the single-target majority.
        .                   #[serde(default)]
       76 ( 0.00%)          additional_targets: Vec<Target>,
    1,102 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (38x)
        .                   /// X value paid to an `{X}` symbol in the activation's mana cost.
        .                   /// Threaded through to `EffectContext.x_value` so the body can
        .                   /// read `Value::XFromCost`. Used by Pernicious Deed's
        .                   /// `{X}, Sacrifice this: destroy each permanent with MV ≤ X`,
        .                   /// Walking Ballista's `{X}: this creature deals X damage`, and
        .                   /// other X-cost activated abilities.
        .                   #[serde(default)]
        .                   x_value: Option<u32>,
-- line 705 ----------------------------------------
-- line 1087 ----------------------------------------
        .               /// at cleanup. Pure Intentions.
        .               OpponentCausesYouToDiscardThisTurn,
        .           }
        .           
        .           // ── Pending decisions (suspendable resolution) ───────────────────────────────
        .           
        .           /// A decision the engine is waiting on before it can continue resolving the
        .           /// current spell or ability.
    6,812 ( 0.00%)  #[derive(Debug, Clone, Serialize, Deserialize)]
    5,850 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (90x)
        .           pub struct PendingDecision {
      270 ( 0.00%)      pub decision: Decision,
  125,967 ( 0.00%)  => /home/user/Crabomination/crabomination/src/decision.rs:<crabomination::decision::Decision as core::clone::Clone>::clone (90x)
        .               pub resume: ResumeContext,
        .           }
        .           
        .           impl PendingDecision {
        .               pub fn acting_player(&self) -> usize {
        .                   // A library search is answered by the seat the decision names — the
        .                   // searching player (CR 701.19a), which for an opponent-owned search
        .                   // (Boseiju, Karametra under a wants_ui opponent) or a cross-library
        .                   // pick (Seek) is not the resume's owner.
    1,456 ( 0.00%)          if let crate::decision::Decision::SearchLibrary { player, .. } = &self.decision {
        .                       return *player;
        .                   }
        .                   // A suspended effect usually wants its answer from the spell's caster
        .                   // / ability's controller. The exception is a forced sacrifice (CR
        .                   // 701.16), where the *sacrificing* player chooses — which for an Edict
        .                   // is the targeted opponent, not the caster. `SacrificePending` carries
        .                   // that player, so prefer it over the resume's owner.
    5,824 ( 0.00%)          match &self.resume {
        .                       ResumeContext::Spell { in_progress, caster, .. } => {
        .                           in_progress.answering_player().unwrap_or(*caster)
        .                       }
        .                       ResumeContext::Trigger { in_progress, controller, .. } => {
        .                           in_progress.answering_player().unwrap_or(*controller)
        .                       }
        .                       ResumeContext::Ability { in_progress, controller, .. } => {
        .                           in_progress.answering_player().unwrap_or(*controller)
        .                       }
      100 ( 0.00%)              ResumeContext::Mulligan { player, .. } => *player,
        .                       ResumeContext::TriggerTargetPick { pending, .. } => pending.controller,
        .                       // The ordering run is a single controller's triggers.
        .                       ResumeContext::TriggerOrder { run, .. } => {
        .                           run.first().map(|c| c.controller).unwrap_or(0)
        .                       }
        .                       ResumeContext::CleanupDiscard { player } => *player,
        .                       ResumeContext::CombatDamage { player, .. } => *player,
        .                       ResumeContext::CastAdditionalCost { caster, .. } => *caster,
        .                       ResumeContext::ActionFloatConfirm { actor, .. } => *actor,
        .                       ResumeContext::ActionSearchPick { actor, .. } => *actor,
        .                       ResumeContext::ActivateAbilityChoice { activator, .. } => *activator,
        .                       ResumeContext::CastExtraTargetPick { caster, .. } => *caster,
        .                       ResumeContext::CastXPick { caster, .. } => *caster,
        .                       ResumeContext::CastSlot0TargetPick { caster, .. } => *caster,
        .                   }
      728 ( 0.00%)      }
        .           }
        .           
        .           impl PendingEffectState {
        .               /// The player who must answer this suspended decision, when it isn't the
        .               /// owning spell's caster / ability's controller: a forced sacrifice
        .               /// (CR 701.16 — the Edict's target chooses) and seat-routed yes/no
        .               /// questions (rhystic taxes, Tempting Offer, Clash). Everything else
        .               /// returns `None` (answered by the resume's owner).
-- line 1149 ----------------------------------------
-- line 1292 ----------------------------------------
        .                   )
        .               }
        .           }
        .           
        .           /// Recorded where resolution suspended so it can resume after the decision.
        .           /// `remaining` is whatever effects in the original tree still need to run
        .           /// after the answered decision is applied (e.g. the `Draw` half of `Opt`
        .           /// suspended on its `Scry`).
    1,328 ( 0.00%)  #[derive(Debug, Clone, Serialize, Deserialize)]
        .           pub enum ResumeContext {
        .               /// CR 603.3b — a networked controller is ordering a same-controller
        .               /// run of simultaneous triggers. `ordered` holds the runs already
        .               /// sequenced, `run` the one being asked about, `rest` the candidates
        .               /// after it (which may suspend again on a later run).
        .               TriggerOrder {
        .                   ordered: Vec<TriggerCandidate>,
        .                   run: Vec<TriggerCandidate>,
-- line 1308 ----------------------------------------
-- line 1373 ----------------------------------------
        .                   controller: usize,
        .                   target: Option<Target>,
        .                   in_progress: PendingEffectState,
        .                   remaining: Effect,
        .               },
        .               /// Pre-game mulligan phase for `player`. After this player keeps,
        .               /// mulligan advances to `next_player` (None = all players done, start game).
        .               Mulligan {
      148 ( 0.00%)          player: usize,
       74 ( 0.00%)          mulligans_taken: usize,
        .                   next_player: Option<usize>,
        .               },
        .               /// Suspended *before* pushing a triggered ability onto the stack
        .               /// because its controller has `wants_ui` and the effect requires
        .               /// a target. `pending` is the one trigger awaiting its target;
        .               /// `remaining` is the queue of triggers from the same event
        .               /// batch that haven't been processed yet. On answer, push
        .               /// `pending` with the chosen target, then continue draining
-- line 1390 ----------------------------------------
-- line 1463 ----------------------------------------
        .               /// the step advance run.
        .               CleanupDiscard { player: usize },
        .               /// CR 510.1c-d — the active player (with `wants_ui`) is choosing combat
        .               /// damage ordering / assignment for `attacker` during the current damage
        .               /// step. On answer the choice is cached and the damage step is re-entered
        .               /// (gathering the next choice or applying damage once all are made).
        .               CombatDamage {
        .                   /// The active player making the choice (always the attacking player).
       32 ( 0.00%)          player: usize,
        .                   attacker: CardId,
      240 ( 0.00%)          kind: CombatDecisionKind,
        .               },
        .               /// A `wants_ui` caster's spell references a target slot beyond the ones
        .               /// the client sent (multi-target spells — Chelonian Tackle's fight
        .               /// defender). The cast suspended before any cost was paid; on answer the
        .               /// pick is appended to the action's `additional_targets` and the action
        .               /// is replayed (which may suspend again for a further slot).
        .               CastExtraTargetPick {
        .                   caster: usize,
-- line 1481 ----------------------------------------
-- line 1505 ----------------------------------------
        .               pub(crate) fn is_cast_or_loyalty(&self) -> bool {
        .                   self.is_cast() || matches!(self, GameAction::ActivateLoyaltyAbility { .. })
        .               }
        .           
        .               /// True for every `Cast*` action variant — the CR 601 "cast a spell"
        .               /// surface, gated by one-spell-per-turn locks (Rule of Law).
        .               pub(crate) fn is_cast(&self) -> bool {
        .                   use GameAction as A;
1,579,088 ( 0.05%)          matches!(
        .                       self,
        .                       A::CastSpell { .. }
        .                           | A::CastSpellKicked { .. }
        .                           | A::CastSpellMultikicked { .. }
        .                           | A::CastBestow { .. }
        .                           | A::CastSpellBuyback { .. }
        .                           | A::CastSpellEntwine { .. }
        .                           | A::CastRoomDoor { .. }
-- line 1521 ----------------------------------------
-- line 1558 ----------------------------------------
        .               }
        .           
        .               /// The `card_id` of the spell a `Cast*` action casts, if it carries one in
        .               /// a `card_id` field (every variant except `CastPrepareSpell`, which names a
        .               /// battlefield creature rather than the spell). Used by per-turn lock pieces
        .               /// that must inspect the cast spell's card types before payment.
        .               pub(crate) fn cast_card_id(&self) -> Option<CardId> {
        .                   use GameAction as A;
   89,472 ( 0.00%)          match self {
        .                       A::CastSpell { card_id, .. }
        .                       | A::CastSpellKicked { card_id, .. }
        .                       | A::CastSpellMultikicked { card_id, .. }
        .                       | A::CastBestow { card_id, .. }
        .                       | A::CastSpellBuyback { card_id, .. }
        .                       | A::CastSpellEntwine { card_id, .. }
        .                       | A::CastRoomDoor { card_id, .. }
        .                       | A::CastFaceDown { card_id, .. }
-- line 1574 ----------------------------------------
-- line 2126 ----------------------------------------
        .           /// (`GameState.day_night == None`); once it becomes day or night it has
        .           /// exactly one of these from then on.
        .           #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, serde::Serialize, serde::Deserialize)]
        .           pub enum DayNight {
        .               Day,
        .               Night,
        .           }
        .           
  414,688 ( 0.01%)  #[derive(Debug, Clone)]
        .           pub enum GameEvent {
        .               StepChanged(TurnStep),
        .               TurnStarted { player: usize, turn: u32 },
        .               CardDrawn { player: usize, card_id: CardId },
        .               /// CR 121 — the turn's first card drawn by `player` (fired once per turn,
        .               /// when `cards_drawn_this_turn` reaches 1). Carries the drawn card so
        .               /// "the first time each turn you draw a card" triggers (God-Eternal
        .               /// Kefnet) can inspect and act on it.
-- line 2142 ----------------------------------------
-- line 2188 ----------------------------------------
        .               /// (CR 400.7 — the origin matters to Ketramose, the New Dawn). Emitted
        .               /// alongside `PermanentExiled` by `move_card_to`, which is the only place
        .               /// that still knows which zone the card came from.
        .               CardExiledFromPlayOrGraveyard { card_id: CardId },
        .               /// `combat` is true for combat damage (CR 510), false for damage from
        .               /// spells/abilities (burn, pingers, Fight). Read by "whenever … is dealt
        .               /// *noncombat* damage" triggers (Chandra's Spitfire).
        .               DamageDealt {
    5,474 ( 0.00%)          amount: u32,
        .                   to_player: Option<usize>,
        .                   to_card: Option<CardId>,
    5,474 ( 0.00%)          combat: bool,
        .                   /// Controller of the damage's source (None when unattributable).
        .                   /// Powers "a source you control deals damage to …" scoping.
        .                   from_controller: Option<usize>,
        .                   /// The damage source's own identity (None when unattributable).
        .                   /// Powers the "other than this <permanent>" exclusion on
        .                   /// source-scoped watchers (Talon of Pain).
        .                   from_card: Option<CardId>,
        .               },
-- line 2207 ----------------------------------------
-- line 2492 ----------------------------------------
        .               pub fn new(active_player: usize) -> Self {
        .                   Self { player_with_priority: active_player, consecutive_passes: 0 }
        .               }
        .           }
        .           
        .           // ── Stack ─────────────────────────────────────────────────────────────────────
        .           
        .           /// An item on the stack waiting to resolve.
   12,584 ( 0.00%)  #[derive(Debug, Clone, Serialize, Deserialize)]
    7,140 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (238x)
        .           pub enum StackItem {
        .               /// A non-land spell (instant, sorcery, or permanent) waiting to resolve.
        .               Spell {
    1,242 ( 0.00%)          card: Box<CardInstance>,
        .                   caster: usize,
        .                   target: Option<Target>,
        .                   /// Additional targets for slots 1+ (`Selector::Target(n)` with
        .                   /// `n > 0`). `target` carries slot 0 for back-compat; multi-target
        .                   /// spells like Crackle with Power / Snow Day populate this Vec
        .                   /// with the rest. Defaults to empty for snapshot round-trip.
        .                   #[serde(default)]
        .                   additional_targets: Vec<Target>,
        .                   /// Chosen mode index for `ChooseMode` effects (0 if `None`).
        .                   mode: Option<usize>,
        .                   /// X paid into the spell's cost. Threaded into `EffectContext.x_value`
        .                   /// at resolution time so `Value::XFromCost` reads the actual paid X.
    1,242 ( 0.00%)          x_value: u32,
        .                   /// Number of distinct colors of mana spent paying this spell's
        .                   /// cost. Read by `Value::ConvergedValue` (Prismatic Ending, Pest
        .                   /// Control). Convoke pips contribute generic only, so they don't
        .                   /// raise this value.
    1,242 ( 0.00%)          converged_value: u32,
        .                   /// Total mana spent paying this spell's cost (sum of all
        .                   /// `ManaPool` decreases during payment). Read by
        .                   /// `Value::CastSpellManaSpent` and `Predicate::ManaSpentAtLeast`
        .                   /// for SOS's Increment / Opus payoffs that scale on "amount of
        .                   /// mana spent to cast that spell". Defaults to 0 via
        .                   /// `#[serde(default)]` for snapshot back-compat.
        .                   #[serde(default)]
        .                   mana_spent: u32,
        .                   /// True if this spell can't be countered by spells or abilities
        .                   /// (Cavern of Souls–style protection). `Effect::CounterSpell` skips
        .                   /// these stack items.
    8,694 ( 0.00%)          uncounterable: bool,
        .               },
        .               /// A triggered/loyalty ability waiting to resolve.
        .               Trigger {
        .                   source: CardId,
      238 ( 0.00%)          controller: usize,
      238 ( 0.00%)          effect: Box<Effect>,
        .                   target: Option<Target>,
        .                   mode: Option<usize>,
        .                   /// X paid into the originating spell's cost, threaded so
        .                   /// `Value::XFromCost` reads the right number when the trigger
        .                   /// resolves. ETB triggers fired off a spell on resolution
        .                   /// inherit the spell's X; loyalty/state triggers default to 0.
        .                   /// Defaults to 0 via `#[serde(default)]` for snapshot
        .                   /// backwards-compatibility.
        .                   #[serde(default)]
      476 ( 0.00%)          x_value: u32,
        .                   /// Converge value (number of distinct colors of mana spent on
        .                   /// the originating spell's cost). Threaded the same way as
        .                   /// `x_value` so ETB triggers consulting `Value::ConvergedValue`
        .                   /// (Rancorous Archaic, Snarl Song, Together as One) read the
        .                   /// right number. Defaults to 0 for snapshot
        .                   /// backwards-compatibility.
        .                   #[serde(default)]
        .                   converged_value: u32,
-- line 2558 ----------------------------------------
-- line 2569 ----------------------------------------
        .                   trigger_source: Option<crate::game::effects::EntityRef>,
        .                   /// Total mana spent paying the originating spell's cost.
        .                   /// Threaded so spell-cast triggers consulting
        .                   /// `Value::CastSpellManaSpent` (Increment / Opus payoffs:
        .                   /// Cuboid Colony, Berta, Fractal Tender, Tackle Artist, …)
        .                   /// read the right amount. Defaults to 0 for snapshot
        .                   /// backwards-compatibility.
        .                   #[serde(default)]
      476 ( 0.00%)          mana_spent: u32,
        .                   /// Per-event amount of the fired event — life gained, life lost,
        .                   /// damage dealt, cards drawn, etc. Set by
        .                   /// `dispatch_triggers_for_events` from the event payload (typically
        .                   /// the event's `amount` field) so trigger bodies can read it via
        .                   /// `Value::TriggerEventAmount`. Used by Light of Promise's
        .                   /// "Whenever you gain life, put that many +1/+1 counters on
        .                   /// target creature you control." Defaults to 0 for snapshot
        .                   /// backwards-compatibility.
-- line 2585 ----------------------------------------
-- line 2649 ----------------------------------------
        .               intervening_if: Option<crate::card::Predicate>,
        .               activated: bool,
        .               trigger_player: Option<usize>,
        .               mana_spent_by_color: Vec<(crate::mana::Color, u32)>,
        .           }
        .           
        .           impl TriggerPush {
        .               pub fn new(source: CardId, controller: usize, effect: Effect) -> Self {
    2,812 ( 0.00%)          Self {
    6,776 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (88x)
        .                       source,
        .                       controller,
        .                       effect,
        .                       target: None,
        .                       additional_targets: Vec::new(),
        .                       mode: None,
        .                       x_value: 0,
        .                       converged_value: 0,
-- line 2665 ----------------------------------------
-- line 2679 ----------------------------------------
        .                   self
        .               }
        .               /// Mark this item as an activated ability (CR 602) rather than a trigger.
        .               pub fn activated(mut self, v: bool) -> Self {
        .                   self.activated = v;
        .                   self
        .               }
        .               pub fn target(mut self, t: Option<Target>) -> Self {
      874 ( 0.00%)          self.target = t;
    8,346 ( 0.00%)          self
    6,776 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (88x)
        .               }
        .               pub fn additional_targets(mut self, t: Vec<Target>) -> Self {
    1,214 ( 0.00%)          self.additional_targets = t;
    1,544 ( 0.00%)          self
    1,520 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (16x)
        .               }
        .               pub fn mode(mut self, m: Option<usize>) -> Self {
        .                   self.mode = m;
    1,796 ( 0.00%)          self
    6,006 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (78x)
        .               }
        .               pub fn x_value(mut self, x: u32) -> Self {
        .                   self.x_value = x;
        .                   self
        .               }
        .               pub fn converged_value(mut self, v: u32) -> Self {
        .                   self.converged_value = v;
        .                   self
        .               }
        .               pub fn trigger_source(mut self, ts: Option<crate::game::effects::EntityRef>) -> Self {
        .                   self.trigger_source = ts;
        .                   self
        .               }
        .               pub fn mana_spent(mut self, m: u32) -> Self {
        .                   self.mana_spent = m;
      780 ( 0.00%)          self
        .               }
        .               pub fn event_amount(mut self, a: u32) -> Self {
        .                   self.event_amount = a;
        .                   self
        .               }
        .               pub fn intervening_if(mut self, p: Option<crate::card::Predicate>) -> Self {
        .                   self.intervening_if = p;
        .                   self
        .               }
        .               /// Per-colour mana spent paying an activation's cost (Protective Sphere).
        .               pub fn mana_spent_by_color(mut self, m: Vec<(crate::mana::Color, u32)>) -> Self {
        .                   self.mana_spent_by_color = m;
      496 ( 0.00%)          self
    1,520 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (16x)
        .               }
    3,222 ( 0.00%)      pub fn build(self) -> StackItem {
    5,328 ( 0.00%)          StackItem::Trigger {
      358 ( 0.00%)              source: self.source,
      358 ( 0.00%)              controller: self.controller,
    2,802 ( 0.00%)              effect: Box::new(self.effect),
    6,776 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (88x)
    2,168 ( 0.00%)              target: self.target,
      358 ( 0.00%)              mode: self.mode,
      358 ( 0.00%)              x_value: self.x_value,
        .                       converged_value: self.converged_value,
    1,640 ( 0.00%)              trigger_source: self.trigger_source,
        .                       mana_spent: self.mana_spent,
        .                       event_amount: self.event_amount,
    1,294 ( 0.00%)              intervening_if: self.intervening_if,
   10,740 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (358x)
    2,004 ( 0.00%)              additional_targets: self.additional_targets,
      358 ( 0.00%)              activated: self.activated,
      358 ( 0.00%)              trigger_player: self.trigger_player,
    2,400 ( 0.00%)              mana_spent_by_color: self.mana_spent_by_color,
        .                   }
    3,222 ( 0.00%)      }
        .           }
        .           
        .           // ── Errors ────────────────────────────────────────────────────────────────────
        .           
        .           #[derive(Debug, Clone, PartialEq, Eq, thiserror::Error)]
        .           pub enum GameError {
        .               #[error("It is not your priority to act")]
        .               NotYourPriority,
-- line 2753 ----------------------------------------

1,106,178 ( 0.04%)  <counts for unidentified lines in crabomination/src/game/types.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/effect/abilities.rs
--------------------------------------------------------------------------------
Ir                  

-- line 2751 ----------------------------------------
         .           /// triggers as soon as the condition holds and won't trigger again until the
         .           /// condition has been false in between.
         .           #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
         .           pub struct StateTriggeredAbility {
         .               pub condition: Predicate,
         .               pub effect: Effect,
         .           }
         .           
12,247,878 ( 0.39%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]
 4,099,384 ( 0.13%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (76,624x)
         .           pub struct ActivatedAbility {
    55,182 ( 0.00%)      pub tap_cost: bool,
         .               /// CR 107.17 — the untap symbol `{Q}`: the source must be tapped and
         .               /// untaps as a cost (Pili-Pala). Shares `{T}`'s CR 602.5g/h
         .               /// summoning-sickness gate.
         .               #[serde(default)]
         .               pub untap_self_cost: bool,
     3,064 ( 0.00%)      pub mana_cost: crate::mana::ManaCost,
   114,936 ( 0.00%)      pub effect: Effect,
 7,024,418 ( 0.23%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (38,312x)
    76,624 ( 0.00%)      pub once_per_turn: bool,
         .               /// CR 602.5f — "Activate no more than N times each turn" (Roterothopter).
         .               /// The generalization of [`Self::once_per_turn`]; both share the
         .               /// permanent's `once_per_turn_used` tally, so `Some(1)` is equivalent.
         .               #[serde(default)]
         .               pub max_activations_per_turn: Option<u32>,
    38,312 ( 0.00%)      pub sorcery_speed: bool,
         .               /// True if activating this ability requires sacrificing the source
         .               /// permanent as part of its cost. The sacrifice is applied **after**
         .               /// tap and mana payment succeed but **before** the effect is queued
         .               /// for resolution — so by the time the effect runs (or is pushed onto
         .               /// the stack), the source is already in the graveyard. Used by cards
         .               /// like Mind Stone (`{1}, {T}, Sacrifice this: Draw a card`),
         .               /// Cathar Commando, Greater Good, Zuran Orb, etc.
    38,312 ( 0.00%)      pub sac_cost: bool,
         .               /// "Return N [filter] you control to their owner's hand" as an activation
         .               /// cost (Floodbringer, Flooded Shoreline's two Islands). Paid before the
         .               /// ability goes on the stack; rejected when fewer than N match.
         .               #[serde(default)]
         .               pub return_permanent_cost: Option<(SelectionRequirement, u32)>,
         .               /// "Tap N untapped [filter] you control" as an activation cost (Crookclaw
         .               /// Elder, Keeper of the Nine Gales). Paid before the ability goes on the
         .               /// stack; activation is rejected when fewer than N untapped matches are
         .               /// available. The source itself is eligible only if it matches.
         .               #[serde(default)]
         .               pub tap_others_cost: Option<(SelectionRequirement, u32)>,
         .               /// CR 702.6 — "Unattach this Equipment" as an activation cost: the source
         .               /// must currently be attached, and detaching it is part of the cost
         .               /// (Sunforger). Paid alongside `sac_cost` in `activate_ability`.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub unattach_cost: bool,
         .               /// "Exile [this Equipment]" as an activation cost, for an ability the
         .               /// Equipment *grants to its bearer* — the cost exiles the attachment, not
         .               /// the creature paying it (The Dominion Bracelet).
         .               #[serde(default)]
         .               pub exile_attachment_cost: bool,
         .               /// "This ability costs {X} less to activate, where X is this creature's
         .               /// power" (The Dominion Bracelet). Reduces generic mana only.
         .               #[serde(default)]
-- line 2807 ----------------------------------------
-- line 2831 ----------------------------------------
         .               /// by activated abilities that bake "Pay N life:" into the cost
         .               /// line — Great Hall of the Biblioplex's `{T}, Pay 1 life: Add one
         .               /// mana of any color`, future Phyrexian-mana flavoured activations,
         .               /// City of Brass-style "tap for damage" hybrids, etc.
         .               ///
         .               /// Defaults to 0 via `#[serde(default)]` so existing literal
         .               /// initialisations pick up the new field automatically.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub life_cost: u32,
         .               /// "Pay half your life, rounded up" as the activation cost (CR 118.4 —
         .               /// Lurking Evil). A real cost, so it's paid up front and can't be declined;
         .               /// a player at 0 or less life can't activate.
         .               #[serde(default)]
    76,624 ( 0.00%)      pub half_life_cost: bool,
         .               /// "Pay X life" as a variable additional cost, where X is the activation's
         .               /// chosen `x_value` (CR 107.16). The body reads the same X via
         .               /// `Value::XFromCost`. Mirrors `energy_x_cost` but drains life. Powers
         .               /// Krumar Initiate's `{X}{B}, {T}, Pay X life: This creature endures X.`
         .               /// Mutually independent from the fixed `life_cost`. Defaults to false.
         .               #[serde(default)]
         .               pub x_life_cost: bool,
         .               /// CR 601.2g — "Spend only [colour] mana on X" (Atalya, Samite Master).
-- line 2852 ----------------------------------------
-- line 2880 ----------------------------------------
         .               /// whose printed cost line reads "Exile this card from your
         .               /// graveyard: …" (Stone Docent, Eternal Student). The exile
         .               /// happens after tap (n/a from gy) + mana + life payments succeed
         .               /// but **before** the effect resolves, mirroring `sac_cost`'s
         .               /// timing.
         .               ///
         .               /// Defaults to false via `#[serde(default)]`.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub exile_self_cost: bool,
         .               /// Optional cost: exile a *different* card from the controller's
         .               /// graveyard matching this filter. Used by activated abilities
         .               /// whose printed cost line reads "Exile a [filter] card from your
         .               /// graveyard:" where the exiled card is **not** the source — for
         .               /// example Postmortem Professor's `{1}{B}, Exile an instant or
         .               /// sorcery card from your graveyard: Return this card from your
         .               /// graveyard to the battlefield.` and Lorehold Pledgemage's
         .               /// `{2}{R}{W}, Exile a card from your graveyard: This creature
-- line 2896 ----------------------------------------
-- line 2916 ----------------------------------------
         .               /// — Holistic Wisdom). Auto-pay exiles the cheapest match.
         .               #[serde(default)]
         .               pub exile_from_hand_cost: Option<SelectionRequirement>,
         .               /// When true, `exile_other_filter`'s count is the activation's X value
         .               /// rather than the fixed `u32` (which is then ignored). Mirrors
         .               /// `sac_other_x`. Used by "{X}, {T}, Exile X cards from your graveyard:"
         .               /// costs — Necropolis Fiend's "-X/-X".
         .               #[serde(default)]
    76,624 ( 0.00%)      pub exile_other_x: bool,
         .               /// When true, `exile_other_filter` names the *top* matching card of the
         .               /// graveyard rather than any of them ("Exile the top creature card of your
         .               /// graveyard:" — Necratog, Zombie Scavengers). The graveyard is ordered,
         .               /// so there's no pick to make and no chooser is offered.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub exile_other_top: bool,
         .               /// Optional self-counter cost-reduction kind. When `Some(kind)`, the
         .               /// activation's generic mana cost is reduced by one for each counter
         .               /// of `kind` on the source permanent (clamped at the printed generic
         .               /// total). Mirrors `affinity_filter` on spells, but reads the
         .               /// source's own counter pool instead of a battlefield filter — the
         .               /// shape needed by Strixhaven's Book artifacts whose printed Oracle
         .               /// is "This ability costs {1} less to activate for each [counter]
         .               /// counter on this artifact." Currently powers:
-- line 2938 ----------------------------------------
-- line 2994 ----------------------------------------
         .               /// candidate, which is the shape's only sensible auto-line. Feeds the
         .               /// same `Value::Sacrificed{Count,TotalPower}` batch as `sac_other_filter`.
         .               #[serde(default)]
         .               pub sac_any_number_filter: Option<SelectionRequirement>,
         .               /// When true, `sac_other_filter`'s count is the activation's X value
         .               /// ("Sacrifice X [filter]:" costs — Lonis, Genetics Expert). The X is
         .               /// threaded to the effect as `Value::XFromCost`.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub sac_other_x: bool,
         .               /// Optional cost: tap an *untapped, different* permanent the activator
         .               /// controls matching this filter (CR 602.5b "tap an untapped … you
         .               /// control" costs). Mirrors `sac_other_filter` but taps rather than
         .               /// sacrifices. Used by Opposition (`Tap an untapped creature you
         .               /// control: Tap target …`) and similar. The auto-picker takes the
         .               /// lowest-power matching untapped permanent so higher-value creatures
         .               /// stay open. Rejected with `GameError::SelectionRequirementViolated`
         .               /// when nothing matches. Defaults to None via `#[serde(default)]`.
-- line 3010 ----------------------------------------
-- line 3035 ----------------------------------------
         .               /// hand for the source instead of the battlefield. Pairs with
         .               /// `exile_self_cost: true` for the "Exile this card from your hand:"
         .               /// cost line — the pitch mana abilities of Elvish Spirit Guide
         .               /// (`Exile this from your hand: Add {G}.`) and Simian Spirit Guide
         .               /// (`… Add {R}.`). Tap costs are illegal from hand and rejected.
         .               ///
         .               /// Defaults to false via `#[serde(default)]`.
         .               #[serde(default)]
    76,624 ( 0.00%)      pub from_hand: bool,
         .               /// Optional {E} (energy) cost (CR 107.16). When > 0, the activator must
         .               /// have at least this many energy counters; they're spent up front during
         .               /// activation, mirroring the mana/life pre-pay gate. Powers the
         .               /// energy-gated mana abilities of Aether Hub and Servant of the Conduit
         .               /// (`{T}, Pay {E}: Add one mana of any color`).
         .               ///
         .               /// Defaults to 0 via `#[serde(default)]` so existing literal
         .               /// initialisations pick up the new field automatically.
         .               #[serde(default)]
    76,624 ( 0.00%)      pub energy_cost: u32,
         .               /// Optional cost: pay `X` energy, where `X` is the activation's chosen
         .               /// `x_value` (CR 107.16 + a variable cost). The target filter reads the
         .               /// same `X` via `ManaValueExactlyXFromCost`, so "Pay X {E}: return a
         .               /// creature card with mana value X" (Chthonian Nightmare) is one field
         .               /// plus that filter. Mutually exclusive with a fixed `energy_cost`.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub energy_x_cost: bool,
         .               /// Optional cost: discard `count` cards from the activator's hand
         .               /// matching this filter (CR 602.5b "Discard a [filter] card:" cost
         .               /// lines). Mirrors `sac_other_filter`/`exile_other_filter` but moves
         .               /// from hand → graveyard. Used by Fauna Shaman (`{G}, {T}, Discard a
         .               /// creature card: …`), Survival of the Fittest, etc. Applied after
         .               /// tap/mana/life payments succeed but before the effect resolves. The
         .               /// auto-picker takes the lowest-CMC matching hand card. Rejected with
         .               /// `GameError::SelectionRequirementViolated` when nothing matches.
-- line 3068 ----------------------------------------
-- line 3069 ----------------------------------------
         .               ///
         .               /// Defaults to None via `#[serde(default)]`.
         .               #[serde(default)]
         .               pub discard_cost: Option<(SelectionRequirement, u32)>,
         .               /// When set with `discard_cost`, the discarded cards must all share a
         .               /// name (Sphinx of the Chimes — "Discard two nonland cards with the same
         .               /// name:"). The pre-flight picks a name with enough matching copies.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub discard_cost_same_name: bool,
         .               /// When set with `discard_cost`, the discard is at RANDOM rather than the
         .               /// controller's choice (Pyromancy's "{3}, Discard a card at random:").
         .               #[serde(default)]
         .               pub discard_cost_random: bool,
         .               /// When set with `discard_cost`, the discarded card's mana value must equal
         .               /// the target's — the linked-X shape of "Discard a card with mana value X:
         .               /// Counter target spell with mana value X" (Kozilek, the Great Distortion).
         .               /// The pre-flight narrows the hand to that mana value, so an activation
-- line 3085 ----------------------------------------
-- line 3125 ----------------------------------------
         .               /// lowest-impact ones. Defaults to None.
         .               #[serde(default)]
         .               pub tap_permanents_cost: Option<(SelectionRequirement, u32)>,
         .               /// Optional cost: return the source to its owner's hand (Magosi, the
         .               /// Waterveil's "…, Remove an eon counter from this land and return it to
         .               /// its owner's hand:"). Applied alongside `sac_cost`, after tap/mana/
         .               /// counter payments and before the effect resolves. Defaults to false.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub bounce_self_cost: bool,
         .               /// "Remove X [kind] counters from this creature:" (Arcbound Javelineer).
         .               /// X comes from the activation's `x_value`; the pre-flight gate requires
         .               /// that many counters, and the body reads `Value::XFromCost`.
         .               #[serde(default)]
         .               pub remove_counter_x: Option<crate::card::CounterType>,
         .               /// Optional cost: remove `u32` counters of the named kind (`None` = any
         .               /// mix of kinds — Tekuthal's "remove three counters") from among
         .               /// permanents matching the filter the activator controls (CR 602.5b —
-- line 3141 ----------------------------------------
-- line 3167 ----------------------------------------
         .               /// owner's hand:" cost lines). The bounce happens after tap/mana/life
         .               /// payments succeed but before the effect resolves, mirroring
         .               /// `sac_cost`. Powers Grinning Ignus (`{R}, Return this to its owner's
         .               /// hand: Add {C}{C}{R}.`) and Rootha, Mercurial Artist (`{2}, Return
         .               /// Rootha to its owner's hand: Copy target instant or sorcery spell`).
         .               ///
         .               /// Defaults to false via `#[serde(default)]`.
         .               #[serde(default)]
    76,624 ( 0.00%)      pub return_self_cost: bool,
         .               /// CR 602.5 — "Only your opponents may activate this ability." When true,
         .               /// the source permanent's controller is barred from activating it; only an
         .               /// opponent (a player not on the controller's team) may. Powers Detention
         .               /// Vortex's `{3}: Destroy this Aura` escape clause. Defaults to false.
         .               #[serde(default)]
         .               pub opponents_only: bool,
         .               /// CR 113.3d — "Any player may activate this ability." The permanent's
         .               /// controller has no monopoly on it; costs are paid by (and the effect
-- line 3183 ----------------------------------------
-- line 3193 ----------------------------------------
         .               /// Elemental Masterpiece's `{U/R}{U/R}, Discard this card: Create a
         .               /// Treasure`. Defaults to false.
         .               #[serde(default)]
         .               pub discard_self_cost: bool,
         .               /// CR 602.5b — "Put a card from your hand on top of your library:" as an
         .               /// activation cost (Hidden Retreat). Gated on a non-empty hand; the
         .               /// lowest-mana-value card is auto-picked, like `discard_cost`.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub put_hand_on_library_cost: bool,
         .               /// CR 702.177 — Exhaust: this activated ability can be activated only
         .               /// once (per game, not per turn). Tracked per-permanent-instance in
         .               /// `CardInstance.exhausted_abilities`, which — unlike `once_per_turn_used`
         .               /// — is never cleared at turn start. Defaults to false.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub exhaust: bool,
         .               /// "Activate only once." A plain once-per-game gate (Possessed Goat) that
         .               /// reuses `exhausted_abilities` bookkeeping like `exhaust` but is *not* the
         .               /// Exhaust keyword — it fires no `ExhaustAbilityActivated` event. Defaults
         .               /// to false.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub activate_once: bool,
         .               /// Craft (CR 702.169) — exile `count` *other* objects matching this
         .               /// filter from among permanents you control and/or cards in your
         .               /// graveyard, as an additional cost. Pairs with
         .               /// `Effect::ExileSelfReturnTransformed` (which exiles the source and
         .               /// returns it transformed). Activate only as a sorcery
         .               /// (`sorcery_speed: true`). The auto-picker exiles graveyard cards
         .               /// first, then the lowest-power battlefield permanents, so higher-value
         .               /// board pieces stay put. Defaults to None.
-- line 3221 ----------------------------------------
-- line 3222 ----------------------------------------
         .               #[serde(default)]
         .               pub craft_exile_cost: Option<(SelectionRequirement, u32)>,
         .               /// CR 701.67 — Waterbend N as part of this ability's cost ("Waterbend {N}:
         .               /// …"). The N generic lives in `mana_cost`; this flag marks the ability so
         .               /// activation accepts waterbend helpers (tap an untapped artifact/creature
         .               /// you control to pay {1} of the generic, clamped to the generic total).
         .               /// Defaults to false.
         .               #[serde(default)]
    76,624 ( 0.00%)      pub waterbend: bool,
         .               /// CR 702.51 — convoke on the *ability*: "for each mana in this ability's
         .               /// activation cost, you may tap an untapped creature you control rather
         .               /// than pay that mana" (Heirloom Epic). Shares waterbend's helper slot on
         .               /// `GameAction::ActivateAbilityWaterbend`, but only creatures qualify.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub convoke: bool,
         .               /// CR 701.59 — "Collect evidence N" as part of this ability's cost
         .               /// ("{T}, Collect evidence N: …"). Exiles the cheapest set of graveyard
         .               /// cards whose total mana value is ≥ N and emits `EvidenceCollected`.
         .               /// Pre-flight-gated on `graveyard_can_collect_evidence`; paid after
         .               /// tap/mana/life succeed but before the effect resolves. Powers Forensic
         .               /// Researcher's tap-untap sibling. Defaults to None.
         .               #[serde(default)]
         .               pub collect_evidence_cost: Option<u32>,
-- line 3244 ----------------------------------------
-- line 3256 ----------------------------------------
         .               /// before the effect goes on the stack, so a body reading
         .               /// `Value::CountersOn { This, kind }` sees the new total.
         .               #[serde(default)]
         .               pub add_counter_cost: Option<(crate::card::CounterType, u32)>,
         .               /// CR 602.5b — "Exile the top N cards of your library:" as an activation
         .               /// cost (Arc-Slogger). Gated on the library holding that many; paid after
         .               /// tap/mana but before the effect resolves.
         .               #[serde(default)]
    38,312 ( 0.00%)      pub exile_top_cost: u32,
         .               /// CR 602.5b — "Exile a [filter] you control:" as an activation cost
         .               /// (Food Chain). The battlefield sibling of `exile_other_filter`; the
         .               /// exiled permanent's mana value is stamped for `Value::ExiledForCostManaValue`.
         .               #[serde(default)]
         .               pub exile_permanent_cost: Option<(SelectionRequirement, u32)>,
         .               /// CR 107.3 — a generic cost whose amount is defined by the game state
         .               /// rather than chosen ("{X}, {T}: … X is the number of cards in an
         .               /// opponent's hand" — Bargaining Table). Added to the printed generic at
-- line 3272 ----------------------------------------
-- line 3276 ----------------------------------------
         .           }
         .           
         .           impl ActivatedAbility {
         .               /// True when activating this costs and restricts nothing — every field but
         .               /// the body sits at its default, so it can be re-activated without bound.
         .               /// Read by the CR 104.4b mandatory-loop watchdog. Deliberately conservative:
         .               /// any cost, gate or per-turn cap makes it non-free, which at worst means a
         .               /// loop goes undetected.
   220,728 ( 0.01%)      pub fn is_free(&self) -> bool {
    18,394 ( 0.00%)          let mut probe = self.clone();
13,660,572 ( 0.44%)  => crabomination_base/src/effect/abilities.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone (18,394x)
   147,152 ( 0.00%)          probe.effect = Self::default().effect;
 2,832,676 ( 0.09%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (36,788x)
 2,772,368 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (18,394x)
    73,576 ( 0.00%)          probe == Self::default()
 1,324,368 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::abilities::ActivatedAbility> (18,394x)
   202,334 ( 0.01%)  => crabomination_base/src/effect/abilities.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::cmp::PartialEq>::eq (18,394x)
   183,940 ( 0.01%)      }
 1,324,368 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::abilities::ActivatedAbility> (18,394x)
         .           }
         .           
         .           #[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]
         .           pub struct LoyaltyAbility {
         .               pub loyalty_cost: i32,
         .               pub effect: Effect,
         .               /// Variable `-X` loyalty ability (CR 606.5): the player picks X (0..=current
         .               /// loyalty) on activation, loyalty drops by X, and the body reads X via
-- line 3296 ----------------------------------------

   496,532 ( 0.02%)  <counts for unidentified lines in crabomination_base/src/effect/abilities.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/effect/query.rs
--------------------------------------------------------------------------------
Ir               

-- line 108 ----------------------------------------
      .               /// True if this effect is an *adapt* effect (CR 702.108) — the
      .               /// counter-check shape produced by `shortcut::adapt`: "if this creature
      .               /// has no +1/+1 counters, put N +1/+1 counters on it." Recognizes the
      .               /// bare effect and the same shape as the head of a `Seq` (adapt cards that
      .               /// bundle a rider). Used to flag `AbilityActivated` events as adapt-ability
      .               /// activations without tagging every adapt card by hand.
      .               pub fn is_adapt(&self) -> bool {
      .                   use crate::card::{CounterType, SelectionRequirement};
 71,050 ( 0.00%)          match self {
      .                       Effect::If { cond, then, .. } => {
      .                           let counter_check = matches!(
      .                               cond,
      .                               Predicate::Not(inner) if matches!(
      .                                   &**inner,
      .                                   Predicate::EntityMatches {
      .                                       what: Selector::This,
      .                                       filter: SelectionRequirement::WithCounter(CounterType::PlusOnePlusOne),
-- line 124 ----------------------------------------
-- line 133 ----------------------------------------
      .                                   ..
      .                               }
      .                           );
      .                           counter_check && adds_counter
      .                       }
      .                       Effect::Seq(effects) => effects.first().is_some_and(Effect::is_adapt),
      .                       _ => false,
      .                   }
 14,210 ( 0.00%)      }
      .           
      .               pub fn seq(effects: Vec<Effect>) -> Self {
      .                   if effects.is_empty() {
      .                       Effect::Noop
      .                   } else if effects.len() == 1 {
      .                       effects.into_iter().next().unwrap()
      .                   } else {
      .                       Effect::Seq(effects)
      .                   }
      .               }
      .           
      .               /// True if this effect (transitively) requires a chosen target (i.e.
      .               /// references `Selector::Target(_)` anywhere). Used for cast-time
      .               /// validation.
 41,608 ( 0.00%)      pub fn requires_target(&self) -> bool {
 19,548 ( 0.00%)          fn sel_has_target(s: &Selector) -> bool {
 25,986 ( 0.00%)              match s {
      .                           Selector::Target(_) | Selector::TargetFiltered { .. } => true,
      .                           Selector::AttachedTo(i)
      .                           | Selector::AttachedToMe(i)
      .                           | Selector::RadianceGroup { subject: i }
      .                           | Selector::CreaturesInCombatWith(i)
      .                           | Selector::SharingNameWith(i)
      .                           | Selector::SharingColorWith(i) => sel_has_target(i),
      .                           Selector::Both(a, b) => sel_has_target(a) || sel_has_target(b),
-- line 166 ----------------------------------------
-- line 179 ----------------------------------------
      .                           }
      .                           Selector::TopOfLibrary { who, .. }
      .                           | Selector::BottomOfLibrary { who, .. }
      .                           | Selector::CardsInZone { who, .. }
      .                           | Selector::ControlledBy { who, .. }
      .                           | Selector::Player(who) => player_has_target(who),
      .                           _ => false,
      .                       }
 13,032 ( 0.00%)          }
      .                   fn player_has_target(p: &PlayerRef) -> bool {
    930 ( 0.00%)              match p {
      .                           PlayerRef::Target(_) => true,
      .                           PlayerRef::OwnerOf(s) | PlayerRef::ControllerOf(s) => sel_has_target(s),
      .                           _ => false,
      .                       }
      .                   }
  3,132 ( 0.00%)          fn value_has_target(v: &Value) -> bool {
  3,654 ( 0.00%)              match v {
      .                           Value::CountOf(s)
      .                           | Value::PowerOf(s)
      .                           | Value::ToughnessOf(s)
      .                           | Value::MarkedDamageOn(s) => sel_has_target(s),
      .                           Value::CountersOn { what, .. } => sel_has_target(what),
      .                           Value::LifeOf(p)
      .                           | Value::HandSizeOf(p)
      .                           | Value::GraveyardSizeOf(p)
-- line 204 ----------------------------------------
-- line 211 ----------------------------------------
      .                               value_has_target(a) || value_has_target(b)
      .                           }
      .                           Value::NonNeg(v) => value_has_target(v),
      .                           Value::ManaValueOf(s) => sel_has_target(s),
      .                           Value::ColorCountOf(s) => sel_has_target(s),
      .                           Value::LoyaltyOf(s) => sel_has_target(s),
      .                           _ => false,
      .                       }
  2,610 ( 0.00%)          }
      .                   fn pred_has_target(p: &Predicate) -> bool {
      .                       match p {
      .                           Predicate::Not(q) => pred_has_target(q),
      .                           Predicate::All(v) | Predicate::Any(v) => v.iter().any(pred_has_target),
      .                           Predicate::SelectorExists(s) => sel_has_target(s),
      .                           Predicate::SelectorCountAtLeast { sel, n } => {
      .                               sel_has_target(sel) || value_has_target(n)
      .                           }
-- line 227 ----------------------------------------
-- line 228 ----------------------------------------
      .                           Predicate::ValueAtLeast(a, b)
      .                           | Predicate::ValueAtMost(a, b)
      .                           | Predicate::ValueEquals(a, b) => value_has_target(a) || value_has_target(b),
      .                           Predicate::IsTurnOf(p) => player_has_target(p),
      .                           Predicate::EntityMatches { what, .. } => sel_has_target(what),
      .                           _ => false,
      .                       }
      .                   }
 35,664 ( 0.00%)          match self {
      .                       Effect::DestroyAllNoRegenGainControllerLifePerManaValue { .. }
      .                       | Effect::RevealHandDiscardMatchingUnlessPayLife { .. }
      .                       | Effect::EachPlayerCreatesTokenPerControlled { .. }
      .                       | Effect::AbandonThisScheme | Effect::GameIsADraw
      .                       | Effect::PumpAttackersThisTurn { .. }
      .                       | Effect::TruceThisTurnAndNext
      .                       | Effect::MayRepeat { .. }
      .                       | Effect::DrainDefendersLandsForManaNextMain
-- line 244 ----------------------------------------
-- line 754 ----------------------------------------
      .                       Effect::SpellsCostLessThisTurn { .. } => false,
      .                       Effect::FaceDownSpellsCostLessThisTurn { .. } => false,
      .                       Effect::GrantKeywordsToSpell { what, .. } => sel_has_target(what),
      .                       Effect::CastFromHandWithoutPaying { .. } => false,
      .                       Effect::PreventNextDamageFromChosenSource { .. } => false,
      .                       Effect::RevealTopPayOrTake { .. } => false,
      .                       Effect::DigForLandToBattlefield { .. } => false,
      .                       Effect::Tribute { otherwise, .. } => otherwise.requires_target(),
    744 ( 0.00%)              Effect::Seq(v) => v.iter().any(|e| e.requires_target()),
 11,532 ( 0.00%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::requires_target'2 (186x)
      .                       Effect::If { cond, then, else_ } => {
      .                           pred_has_target(cond) || then.requires_target() || else_.requires_target()
      .                       }
      .                       Effect::ForEach { selector, body } => {
      .                           sel_has_target(selector) || body.requires_target()
      .                       }
      .                       Effect::Repeat { count, body } => value_has_target(count) || body.requires_target(),
      .                       Effect::FlipCoin { count, on_heads, on_tails } => {
-- line 770 ----------------------------------------
-- line 1523 ----------------------------------------
      .                       Effect::RerollStoredResults { what } => sel_has_target(what),
      .                       Effect::RedirectNextDamageTo { what, to } => {
      .                           sel_has_target(what) || sel_has_target(to)
      .                       }
      .                       Effect::GrantProtectionFromColorsOf { what, of, .. } => {
      .                           sel_has_target(what) || sel_has_target(of)
      .                       }
      .                   }
 21,350 ( 0.00%)      }
      .           
      .               /// Extract the target's filter if this effect's top-level "what"/"to" is
      .               /// `Selector::Target(0)`. Used by UI/bot for target selection.
 18,820 ( 0.00%)      pub fn primary_target_filter(&self) -> Option<&SelectionRequirement> {
  3,816 ( 0.00%)          fn sel_filter(s: &Selector) -> Option<&SelectionRequirement> {
  9,738 ( 0.00%)              match s {
      .                           Selector::EachMatching { filter, .. } => Some(filter),
      .                           Selector::EachPermanent(f) => Some(f),
      .                           Selector::CardsInZone { filter, .. } => Some(filter),
      .                           Selector::TargetFiltered { filter, .. } => Some(filter),
      .                           Selector::MatchingAmong { inner, .. }
      .                           | Selector::Take { inner, .. }
      .                           | Selector::TakeRandom { inner, .. } => {
      .                               sel_filter(inner)
      .                           }
      .                           Selector::TakeWithSumCap { inner, .. } => sel_filter(inner),
      .                           Selector::RadianceGroup { subject } => sel_filter(subject),
      .                           Selector::CreaturesInCombatWith(subject) => sel_filter(subject),
      .                           _ => None,
      .                       }
  1,908 ( 0.00%)          }
 12,190 ( 0.00%)          match self {
      .                       Effect::MayDealPowerThenNoCombatDamage { to, .. } => sel_filter(to),
      .                       Effect::TapAndHoldWhileSourceTapped { what }
      .                       | Effect::GrantSacrificedLandTypesLandwalk { what, .. } => sel_filter(what),
      .                       // The Aura slot is the targeted one; the host is usually `This`
      .                       // (Iridescent Drake, Hakim, Loreweaver).
      .                       Effect::AttachAuraFromGraveyardTo { aura, host } => {
      .                           sel_filter(aura).or_else(|| sel_filter(host))
      .                       }
-- line 1561 ----------------------------------------
-- line 1740 ----------------------------------------
      .                       // CreateTokenAttachedTo — the `target` is the creature the minted
      .                       // Aura/Role token attaches to (Splashy Spellcaster's Role).
      .                       Effect::CreateTokenAttachedTo { target, .. }
      .                       | Effect::CreateTokenAttachedToEach { target, .. } => sel_filter(target),
      .                       Effect::PumpPT { what, .. }
      .                       | Effect::SetBasePT { what, .. }
      .                       | Effect::SwitchPT { what, .. }
      .                       | Effect::DoublePower { what, .. } => {
     88 ( 0.00%)                  sel_filter(what).or_else(|| implicit_creature_if_bare_target(what))
    704 ( 0.00%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::primary_target_filter::sel_filter (88x)
      .                       }
      .                       Effect::BecomeCreature { what, .. }
      .                       | Effect::BecomeCreatureLosingTypes { what, .. }
      .                       | Effect::SetCardTypesTo { what, .. } => sel_filter(what),
      .                       Effect::AnimateAsCreature { what, .. } => sel_filter(what),
      .                       // Riptide Mangler: the target hides in the power value ("base
      .                       // power becomes target creature's power"), not in `what`.
      .                       Effect::SetBasePower { what, power, .. } => sel_filter(what).or_else(|| match power {
-- line 1756 ----------------------------------------
-- line 1803 ----------------------------------------
      .                       // Geth's Verdict) keep their explicit-target casting contract.
      .                       Effect::Sacrifice { who, .. } | Effect::SacrificeGreatestMV { who, .. } => {
      .                           sel_filter(who)
      .                       }
      .                       // Compound effects: walk into the children. Spells like Goryo's
      .                       // Vengeance wrap a `Move` (target legendary creature) in a
      .                       // `Seq` alongside a delayed exile trigger; the primary target
      .                       // is still the Move's target.
     72 ( 0.00%)              Effect::Seq(v) => v.iter().find_map(|e| e.primary_target_filter()),
  1,422 ( 0.00%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::primary_target_filter'2 (36x)
      .                       Effect::If { then, else_, .. } => then
      .                           .primary_target_filter()
      .                           .or_else(|| else_.primary_target_filter()),
      .                       Effect::DelayUntilWithCapture { body, .. } | Effect::DelayUntil { body, .. } => {
      .                           body.primary_target_filter()
      .                       }
      .                       Effect::OptionalTargets { body, .. } => body.primary_target_filter(),
      .                       // The copy *source* is the targeted slot ("becomes a copy of
-- line 1819 ----------------------------------------
-- line 1944 ----------------------------------------
      .                       Effect::FlipCoinsUntilLoseOrStop { tiers } => {
      .                           tiers.iter().find_map(|(_, e)| e.primary_target_filter())
      .                       }
      .                       Effect::RollDie { results, .. } => results
      .                           .iter()
      .                           .find_map(|(_, _, e)| e.primary_target_filter()),
      .                       _ => None,
      .                   }
  8,746 ( 0.00%)      }
      .           
      .               /// Heuristic: does this effect's primary target want to be a *friendly*
      .               /// permanent (one the caster controls) rather than an opponent's? Drives
      .               /// `auto_target_for_effect` so the random bot doesn't waste Vines of
      .               /// Vastwood on the opp's bear or Reckless Charge on its own.
      .               ///
      .               /// Returns true for unconditional self-buffs (positive `PumpPT`,
      .               /// `GrantKeyword`, `+1/+1` `AddCounter`). Returns false for hostile
-- line 1960 ----------------------------------------
-- line 1965 ----------------------------------------
      .                   let mut targeting = children.iter().filter(|e| e.requires_target()).peekable();
      .                   if targeting.peek().is_some() {
      .                       targeting.any(|e| e.prefers_friendly_target())
      .                   } else {
      .                       children.iter().any(|e| e.prefers_friendly_target())
      .                   }
      .               }
      .           
  6,660 ( 0.00%)      pub fn prefers_friendly_target(&self) -> bool {
  8,588 ( 0.00%)          match self {
      .                       Effect::PumpPT {
      .                           power, toughness, ..
      .                       } => {
      .                           // Pump is friendly when the bonus is non-negative; debuffs
      .                           // (Tragic Slip, Last Gasp) want opponent targets.
    352 ( 0.00%)                  Self::value_is_non_negative(power) && Self::value_is_non_negative(toughness)
      .                       }
      .                       // SetBasePT to 0/N (Square Up) is hostile when the base
      .                       // power drops below the printed body — used as a removal-
      .                       // adjacent effect to neutralize attackers. The bot prefers
      .                       // an opp creature unless the toughness bump is the bigger
      .                       // tell.
      .                       Effect::SetBasePT { .. } => false,
      .                       // Animating your own land into a creature is a friendly self-buff.
-- line 1988 ----------------------------------------
-- line 2024 ----------------------------------------
      .                       | Effect::CapTargetsAt { body, .. }
      .                       | Effect::MayPayX { body, .. } => body.prefers_friendly_target(),
      .                       // "TARGET player draws a card" is a gift — aim slot 0 at the
      .                       // caster (Shadrix Silverquill's draw mode is the mode you take
      .                       // yourself in the canonical two-pick line). A non-targeted
      .                       // Draw (who: You / EachPlayer) declares no preference, so
      .                       // hostile siblings (Keranos's reveal-bolt branch) keep their
      .                       // opponent-facing default.
    390 ( 0.00%)              Effect::Draw { who, .. } => matches!(
     78 ( 0.00%)                  who,
      .                           Selector::Player(crate::effect::PlayerRef::Target(_))
      .                               | Selector::Target(_)
      .                               | Selector::TargetFiltered { .. }
      .                       ),
      .                       // Modal: friendly when the first slot-owning mode (default
      .                       // picks order — slot 0's owner) is friendly.
      .                       Effect::ChooseN { picks, modes } => picks
      .                           .iter()
      .                           .filter_map(|&i| modes.get(i as usize))
      .                           .find(|m| m.requires_target())
      .                           .is_some_and(|m| m.prefers_friendly_target()),
      .                       Effect::Process { then, .. } => then.prefers_friendly_target(),
      .                       // Reanimate-style spells move target → caster's hand or battlefield.
      .                       // Without this, `auto_target_for_effect` picks an opp's battlefield
      .                       // creature first, and Disentomb / Raise Dead happily steal it.
    528 ( 0.00%)              Effect::Move { to, .. } => matches!(
    528 ( 0.00%)                  to,
      .                           ZoneDest::Hand(PlayerRef::You)
      .                               | ZoneDest::Battlefield {
      .                                   controller: PlayerRef::You,
      .                                   ..
      .                               }
      .                       ),
      .                       _ => false,
      .                   }
  5,920 ( 0.00%)      }
      .           
      .               /// This effect's primary target is meant to be a card in *some*
      .               /// graveyard. Covers two cases:
      .               /// - Reanimate-class (Disentomb, Raise Dead, Reanimate, Goryo's
      .               ///   Vengeance) — `Move target → Hand(You)` / `Battlefield(You)`.
      .               /// - Graveyard hate (Ghost Vacuum's "exile target card from a
      .               ///   graveyard") — `Move target → Exile`.
      .               ///
-- line 2067 ----------------------------------------
-- line 2068 ----------------------------------------
      .               /// The auto-target heuristic walks graveyards (in friendly/hostile
      .               /// order) before the battlefield when this is set, so an `Any`-filtered
      .               /// Move-to-Exile picks a graveyard resident rather than a battlefield
      .               /// permanent that happens to be at the top of the scan.
      .               ///
      .               /// Battlefield Move-to-Exile is rare in the catalog (the canonical
      .               /// permanent-exile effect is `Effect::Exile`), so collapsing both
      .               /// graveyard-walk cases under one classifier is safe.
219,660 ( 0.01%)      pub fn prefers_graveyard_target(&self) -> bool {
266,084 ( 0.01%)          match self {
    528 ( 0.00%)              Effect::Move { to, .. } => matches!(
    528 ( 0.00%)                  to,
      .                           ZoneDest::Hand(PlayerRef::You)
      .                               | ZoneDest::Battlefield {
      .                                   controller: PlayerRef::You,
      .                                   ..
      .                               }
      .                               | ZoneDest::Exile
      .                       ),
  1,488 ( 0.00%)              Effect::Seq(v) => v.iter().any(|e| e.prefers_graveyard_target()),
 10,416 ( 0.00%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_graveyard_target'2 (372x)
      .                       Effect::If { then, else_, .. } => {
      .                           then.prefers_graveyard_target() || else_.prefers_graveyard_target()
      .                       }
      .                       // "Exile target card from a graveyard until this leaves"
      .                       // (Gravegouger) — the pick only ever lives in a graveyard.
      .                       Effect::ExileUntilSourceLeaves { return_to, .. } => {
      .                           matches!(return_to, crate::card::ExileReturnZone::Graveyard)
      .                       }
-- line 2095 ----------------------------------------
-- line 2122 ----------------------------------------
      .                       Effect::GrantFlashbackThisTurn { .. }
      .                       | Effect::GrantEmbalmThisTurn { .. }
      .                       | Effect::GrantHarmonizeThisTurn { .. } => true,
      .                       // "Return target Aura card from your graveyard" — the pick only
      .                       // ever lives in a graveyard (Hakim, Iridescent Drake).
      .                       Effect::AttachAuraFromGraveyardTo { .. } => true,
      .                       _ => false,
      .                   }
153,762 ( 0.00%)      }
      .           
      .               /// The slot-0 target phrase for label text — "target creature",
      .               /// "any target", or a plain "target" when the slot-0 filter has no clean
      .               /// noun (complex `Or`/`Not`/stat gates) or there is no slot-0 target.
      .               /// Lets `effect_short_text` surface a target's restriction so a modal like
      .               /// Abrade reads "destroy target artifact" rather than "destroy target".
      .               fn target_phrase(&self) -> String {
      .                   match self.target_filter_for_slot(0) {
-- line 2138 ----------------------------------------
-- line 2569 ----------------------------------------
      .               /// `Any`-filtered Move (Regrowth) auto-targets the caster as a player and
      .               /// silently fizzles, since `Effect::Move` only consumes
      .               /// `EntityRef::{Permanent,Card}` and ignores Player entries.
      .               ///
      .               /// Returns true for effects that legitimately point at a player face:
      .               /// damage, life-gain/loss, drain, mill/draw/discard against a player ref,
      .               /// surveil/scry/look (no-op for non-player anyway). False for effects that
      .               /// move/tap/destroy/exile cards.
  8,140 ( 0.00%)      pub fn accepts_player_target(&self) -> bool {
  5,920 ( 0.00%)          match self {
      .                       Effect::DealDamage { .. }
      .                       | Effect::GainLife { .. }
      .                       | Effect::LoseLife { .. }
      .                       | Effect::SetLifeTotal { .. }
      .                       | Effect::Drain { .. }
      .                       | Effect::Discard { .. }
      .                       | Effect::DiscardAnyNumber { .. }
      .                       | Effect::SetNoMaxHandSize { .. }
-- line 2586 ----------------------------------------
-- line 2759 ----------------------------------------
      .                           results.iter().any(|(_, _, e)| e.accepts_player_target())
      .                       }
      .                       // Conservative default: anything we don't classify is permitted.
      .                       // The legality gate (filter + check_target_legality) still rejects
      .                       // mismatched types, this just changes the heuristic's preference
      .                       // order.
      .                       _ => true,
      .                   }
  6,660 ( 0.00%)      }
      .           
      .               fn value_is_non_negative(v: &Value) -> bool {
    528 ( 0.00%)          match v {
      .                       Value::Const(n) => *n >= 0,
      .                       // Dynamic values (`SacrificedPower`, `XFromCost`, etc.) are always
      .                       // ≥ 0 in practice.
      .                       _ => true,
      .                   }
      .               }
      .           
      .               fn keyword_is_friendly(kw: &Keyword) -> bool {
-- line 2778 ----------------------------------------
-- line 2812 ----------------------------------------
      .               /// (Tear Asunder's kicked "nonland permanent" vs base "artifact or
      .               /// enchantment"). The non-kicked callers use the default-`false` wrapper.
      .               pub fn target_filter_for_slot_in_mode_kicked(
      .                   &self,
      .                   slot: u8,
      .                   mode: Option<usize>,
      .                   kicked: bool,
      .               ) -> Option<&SelectionRequirement> {
208,890 ( 0.01%)          fn sel_find(s: &Selector, slot: u8) -> Option<&SelectionRequirement> {
      .                       // A `ControlledBy { who: Target(n) }` selector declares slot `n`
      .                       // as a *player* target (How to Start a Riot's "creatures target
      .                       // player controls get +2/+0"). Surface a Player filter so the
      .                       // cast/auto-target walk prompts for that slot.
      .                       const PLAYER: SelectionRequirement = SelectionRequirement::Player;
121,038 ( 0.00%)              match s {
 11,678 ( 0.00%)                  Selector::TargetFiltered { slot: s2, filter } if *s2 == slot => Some(filter),
      .                           Selector::GreatestManaValueControlledMatching {
      .                               who: PlayerRef::Target(s2),
      .                               ..
      .                           } if *s2 == slot => Some(&PLAYER),
      .                           Selector::ControlledBy {
      .                               who: PlayerRef::Target(s2),
      .                               ..
      .                           } if *s2 == slot => Some(&PLAYER),
-- line 2835 ----------------------------------------
-- line 2848 ----------------------------------------
      .                           Selector::MatchingAmong { inner, .. }
      .                           | Selector::Take { inner, .. }
      .                           | Selector::TakeRandom { inner, .. } => {
      .                               sel_find(inner, slot)
      .                           }
      .                           Selector::TakeWithSumCap { inner, .. } => sel_find(inner, slot),
      .                           _ => None,
      .                       }
132,930 ( 0.00%)          }
      .                   // A target slot can hide inside a `Value` sub-tree — Rabid Bite
      .                   // deals damage equal to `Value::PowerOf(TargetFiltered{slot:0})`.
      .                   // Descend so slot 0's filter is discoverable for cast/auto-target.
 35,706 ( 0.00%)          fn val_find(v: &Value, slot: u8) -> Option<&SelectionRequirement> {
 16,332 ( 0.00%)              match v {
      .                           Value::CountOf(s)
      .                           | Value::PowerOf(s)
      .                           | Value::ToughnessOf(s)
      .                           | Value::ManaValueOf(s)
      .                           | Value::LoyaltyOf(s) => sel_find(s, slot),
      .                           Value::CountersOn { what, .. } => sel_find(what, slot),
      .                           // Arithmetic combinators can wrap a target-bearing value
      .                           // (Polliwallop: `Times(PowerOf(slot 0), 2)`). Descend both arms.
-- line 2869 ----------------------------------------
-- line 2879 ----------------------------------------
      .                           Value::HandSizeOf(p)
      .                           | Value::LifeOf(p)
      .                           | Value::GraveyardSizeOf(p)
      .                           | Value::LibrarySizeOf(p)
      .                           | Value::PermanentCountControlledBy(p)
      .                           | Value::CreatureCountControlledBy(p) => implicit_player_for_ref_slot(p, slot),
      .                           _ => None,
      .                       }
 14,910 ( 0.00%)          }
309,316 ( 0.01%)          fn eff_find(
      .                       e: &Effect,
      .                       slot: u8,
      .                       mode: Option<usize>,
      .                       kicked: bool,
      .                   ) -> Option<&SelectionRequirement> {
142,250 ( 0.00%)              match e {
  7,180 ( 0.00%)                  Effect::Seq(v) => v.iter().find_map(|x| eff_find(x, slot, mode, kicked)),
170,820 ( 0.01%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked::eff_find'2 (1,436x)
      .                           // `If(SpellWasKicked, …)` chooses the branch that will
      .                           // actually resolve so cast-time target legality matches it.
      .                           Effect::If {
      .                               cond: Predicate::SpellWasKicked,
      .                               then,
      .                               else_,
      .                           } => {
      .                               if kicked {
-- line 2903 ----------------------------------------
-- line 3333 ----------------------------------------
      .                           | Effect::TapBlockedByAndSkipUntap { what }
      .                           | Effect::Untap { what, .. } => {
      .                               sel_find(what, slot).or_else(|| implicit_player_for_slot(what, slot))
      .                           }
      .                           Effect::PumpPT { what, .. }
      .                           | Effect::SetBasePT { what, .. }
      .                           | Effect::SwitchPT { what, .. }
      .                           | Effect::DoublePower { what, .. } => {
  6,228 ( 0.00%)                      sel_find(what, slot).or_else(|| implicit_creature_for_slot(what, slot))
 45,672 ( 0.00%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked::sel_find (2,076x)
      .                           }
      .                           Effect::BecomeCreature { what, .. } => sel_find(what, slot),
      .                           Effect::AnimateAsCreature { what, .. } => sel_find(what, slot),
      .                           Effect::SetBasePower { what, power, .. } => {
      .                               sel_find(what, slot).or_else(|| match power {
      .                                   Value::CountOf(s) | Value::PowerOf(s) | Value::ToughnessOf(s) => {
      .                                       sel_find(s, slot)
      .                                   }
-- line 3349 ----------------------------------------
-- line 3513 ----------------------------------------
      .                           Effect::NthResolutionThisTurn { branches } => branches
      .                               .iter()
      .                               .find_map(|e| eff_find(e, slot, mode, kicked)),
      .                           Effect::SacrificeAnyNumber { per_each, .. } => {
      .                               eff_find(per_each, slot, mode, kicked)
      .                           }
      .                           _ => None,
      .                       }
 62,848 ( 0.00%)          }
 29,260 ( 0.00%)          eff_find(self, slot, mode, kicked)
447,786 ( 0.01%)  => crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked::eff_find (7,570x)
      .               }
      .           
      .               /// Mode-agnostic shorthand for `target_filter_for_slot_in_mode(slot, None)`.
      .               /// For modal effects, returns the first filter from any mode (legacy
      .               /// behaviour preserved for callers that don't yet thread mode info).
      .               pub fn target_filter_for_slot(&self, slot: u8) -> Option<&SelectionRequirement> {
      .                   self.target_filter_for_slot_in_mode(slot, None)
      .               }
-- line 3530 ----------------------------------------
-- line 3614 ----------------------------------------
      .           
      .               /// CR 115.3 — the count of mutually-distinct targets a *single* multi-target
      .               /// instance consumes (the "up to / any number of / N target …" effects:
      .               /// `DealDamageDivided`, `SupportCounters`). Those N targets occupy slots
      .               /// `0..N` and must all differ. Returns `None` for effects whose `target`
      .               /// clauses are separate instances (a `Seq` of single-target effects), where
      .               /// the same object may legally fill each clause. Walks modal wrappers so a
      .               /// chosen mode's multi-target effect is found.
 29,512 ( 0.00%)      pub fn distinct_target_count(&self, mode: Option<usize>) -> Option<u8> {
 89,234 ( 0.00%)          match self {
      .                       Effect::DealDamageDivided { max_targets, .. }
      .                       | Effect::PreventNextDamageDivided { max_targets, .. }
      .                       | Effect::DealDamageDividedEvenly { max_targets, .. }
      .                       | Effect::SupportCounters { max_targets, .. }
      .                       | Effect::ApplyToTargets { max_targets, .. }
      .                       | Effect::DeliverUntoEvil { max_targets, .. }
      .                       | Effect::DistributeCounters { max_targets, .. } => Some(*max_targets),
      .                       Effect::FinaleOfPromise => Some(2),
-- line 3631 ----------------------------------------
-- line 3643 ----------------------------------------
      .                       | Effect::MaySacrifice { then: body, .. }
      .                       | Effect::MaySacrificeSource { then: body, .. }
      .                       | Effect::MayTap { then: body, .. }
      .                       | Effect::MayDiscard { then: body, .. }
      .                       | Effect::MayDiscardMatching { then: body, .. }
      .                       | Effect::MayPayLife { body, .. } => body.distinct_target_count(mode),
      .                       _ => None,
      .                   }
 29,512 ( 0.00%)      }
      .           }

503,886 ( 0.02%)  <counts for unidentified lines in crabomination_base/src/effect/query.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/combat.rs
--------------------------------------------------------------------------------
Ir                 

-- line 9 ----------------------------------------
        .           /// dealer as the body's `TriggerSource` (the Phase-1.5 listener pattern —
        .           /// Kaito's "return one of them to hand").
        .           type DamageTrigger = (CardId, Effect, usize, Option<crate::card::Predicate>, bool);
        .           
        .           /// The `AttackBlockCostTapAnother` filters carried by a computed keyword list
        .           /// (Hollow Warrior) — one helper must be tapped per entry.
        .           fn tap_another_filters(kws: &[Keyword]) -> Vec<crate::card::SelectionRequirement> {
        .               kws.iter()
   16,548 ( 0.00%)          .filter_map(|k| match k {
        .                       Keyword::AttackBlockCostTapAnother(f) => Some((**f).clone()),
    8,274 ( 0.00%)              _ => None,
        .                   })
        .                   .collect()
        .           }
        .           
        .           /// CR 509.1b — how many attackers this creature may block. One by default;
        .           /// `CanBlockAdditional(n)` adds n (they stack), `CanBlockAnyNumber` lifts the
        .           /// cap entirely. `SelfCanBlockAdditionalPerAttachedEquipment` (Kemba's Legion)
        .           /// adds one more per attached Equipment — see `max_blocks_on`.
        .           fn max_blocks_for(kws: &[Keyword]) -> usize {
        .               if kws.contains(&Keyword::CanBlockAnyNumber) {
        .                   return usize::MAX;
        .               }
    1,786 ( 0.00%)      1 + kws
        .                   .iter()
    3,360 ( 0.00%)          .filter_map(|k| match k {
        .                       Keyword::CanBlockAdditional(n) => Some(*n as usize),
        .                       _ => None,
        .                   })
        .                   .sum::<usize>()
        .           }
        .           
        .           impl GameState {
        .               /// CR 702.22d/j — the qualities named by "bands with other [quality]" on
        .               /// any of `ids`, read from the computed keyword set so granted instances
        .               /// (Adventurers' Guildhouse) count.
    7,126 ( 0.00%)      pub(crate) fn bands_with_other_qualities(
        .                   &self,
        .                   ids: &[CardId],
        .               ) -> Vec<crate::card::SelectionRequirement> {
        .                   // The band is 2-3 cards; computing all ~20 permanents to read their
        .                   // keywords was the whole cost. Walk the battlefield for order, but
        .                   // compute only the members — under one freeze so they share a gather.
        .                   self.with_frozen_layers(|g| {
    1,018 ( 0.00%)              g.battlefield
        .                           .iter()
        .                           .filter(|c| ids.contains(&c.id))
    8,168 ( 0.00%)                  .filter_map(|c| g.computed_permanent(c.id))
4,374,181 ( 0.14%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (2,042x)
        .                           .flat_map(|c| {
        .                               c.keywords
        .                                   .iter()
    8,196 ( 0.00%)                          .filter_map(|k| match k {
        .                                       Keyword::BandsWithOther(q) => Some((**q).clone()),
    3,312 ( 0.00%)                              _ => None,
        .                                   })
        .                                   .collect::<Vec<_>>()
        .                           })
        .                           .collect()
        .                   })
    7,126 ( 0.00%)      }
        .           
        .               /// CR 702.22j — is this set of blockers a "bands with other [quality]"
        .               /// band? True when at least two of them match a quality one of them bands
        .               /// with, which hands the damage division to the defending player.
   11,198 ( 0.00%)      fn quality_band_assigner(&self, ids: &[CardId]) -> Option<usize> {
    4,072 ( 0.00%)          let qualities = self.bands_with_other_qualities(ids);
5,908,048 ( 0.19%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::bands_with_other_qualities (1,018x)
        .                   qualities.iter().find(|q| {
        .                       ids.iter()
        .                           .filter(|id| {
        .                               self.evaluate_requirement_static(q, &Target::Permanent(**id), 0, None)
        .                           })
        .                           .count()
        .                           >= 2
        .                   })?;
        .                   ids.iter().find_map(|id| self.battlefield_find(*id)).map(|c| c.controller)
   10,180 ( 0.00%)      }
        .           
        .               /// `max_blocks_for` plus the board-dependent riders keyed on the blocker
        .               /// itself ("…for each Equipment attached to this creature").
        .               /// CR 506.2 / 509.1b — the tightest "no more than N creatures can
        .               /// attack/block each combat" cap in play (Silent Arbiter), or `None` when
        .               /// nothing caps participation.
   34,080 ( 0.00%)      pub(crate) fn combat_participation_cap(&self, blocking: bool) -> Option<u32> {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield
        .                       .iter()
        .                       .flat_map(|c| c.definition.static_abilities.iter())
        .                       .filter_map(|sa| match (&sa.effect, blocking) {
        .                           (StaticEffect::MaxAttackersPerCombat(n), false) => Some(*n),
        .                           (StaticEffect::MaxBlockersPerCombat(n), true) => Some(*n),
        .                           _ => None,
        .                       })
        .                       .min()
   56,800 ( 0.00%)      }
        .           
        .               pub(crate) fn max_blocks_on(&self, blocker: CardId, kws: &[Keyword]) -> usize {
        .                   use crate::effect::StaticEffect;
        .                   let base = max_blocks_for(kws);
    3,572 ( 0.00%)          if base == usize::MAX {
        .                       return base;
        .                   }
        .                   let per_equipment = self
        .                       .battlefield_find(blocker)
        .                       .filter(|c| {
        .                           c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(
        .                                   sa.effect,
-- line 115 ----------------------------------------
-- line 119 ----------------------------------------
        .                       })
        .                       .map(|_| {
        .                           self.battlefield
        .                               .iter()
        .                               .filter(|c| c.attached_to == Some(blocker) && c.definition.is_equipment())
        .                               .count()
        .                       })
        .                       .unwrap_or(0);
    2,456 ( 0.00%)          base + per_equipment
        .               }
        .           
        .               /// True if `card` carries a `CanAttackIgnoringDefenderWhile` static whose
        .               /// condition currently holds — it may attack despite Defender
        .               /// (Drowsing Tyrannodon).
    4,928 ( 0.00%)      pub(crate) fn ignores_defender_for_attack(&self, card: &CardInstance) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   // CR 508.1a — a turn-scoped grant (Krotiq Nestguard's activated ability).
      616 ( 0.00%)          if self.attack_despite_defender_this_turn.contains(&card.id) {
        .                       return true;
        .                   }
        .                   // CR 508.1a — a team-wide static (High Alert, Assault Formation): any
        .                   // permanent the attacker's controller has granting "creatures you
        .                   // control can attack as though they didn't have defender".
      616 ( 0.00%)          if self.battlefield.iter().any(|c| {
   23,880 ( 0.00%)              c.controller == card.controller
        .                           && c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::YourCreaturesCanAttackAsThoughNoDefender)
        .                           })
        .                   }) {
        .                       return true;
        .                   }
        .                   let mut ctx =
        .                       crate::game::effects::EffectContext::for_ability(card.id, card.controller, None);
        .                   // CR 702.32 — the gate may be "if this creature was kicked" (Prison
        .                   // Barricade), which reads the permanent's own cast-time flag.
    1,232 ( 0.00%)          ctx.kicked = card.kicked;
        .                   card.definition.static_abilities.iter().any(|sa| {
        .                       if let StaticEffect::CanAttackIgnoringDefenderWhile { condition } = &sa.effect {
        .                           self.evaluate_predicate(condition, &ctx)
        .                       } else {
        .                           false
        .                       }
        .                   })
    4,928 ( 0.00%)      }
        .           
        .               /// CR 509.1a — true if `controller` has a permanent granting "tapped
        .               /// creatures you control can block as though they were untapped" (Masako
        .               /// the Humorless).
        .               pub(crate) fn tapped_creatures_can_block(&self, controller: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
        .                       c.controller == controller
-- line 170 ----------------------------------------
-- line 173 ----------------------------------------
        .                               .iter()
        .                               .any(|sa| matches!(sa.effect, StaticEffect::TappedCreaturesCanBlock))
        .                   })
        .               }
        .           
        .               /// The seat that declares attackers this turn — the active player unless a
        .               /// `combat_chooser` (Master Warcraft) is set.
        .               pub fn attack_declarer(&self) -> usize {
    8,338 ( 0.00%)          self.combat_chooser.unwrap_or(self.active_player_idx)
        .               }
        .           
        .               /// The single seat that must submit the block declaration, if any:
        .               /// Master Warcraft's one-shot `combat_chooser`, else the active player
        .               /// while an `AttackingPlayerChoosesBlocks` static is out (Invasion Plans).
        .               pub fn block_chooser(&self) -> Option<usize> {
   14,342 ( 0.00%)          if let Some(chooser) = self.combat_chooser {
        .                       return Some(chooser);
        .                   }
        .                   self.battlefield
        .                       .iter()
        .                       .any(|c| {
        .                           c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(
        .                                   sa.effect,
-- line 196 ----------------------------------------
-- line 202 ----------------------------------------
        .               }
        .           
        .               /// May `seat` submit the block declaration? Normally any non-active seat
        .               /// (a defending player declares its own blocks); with a `block_chooser`
        .               /// set, only that seat.
        .               pub fn may_declare_blocks(&self, seat: usize) -> bool {
        .                   match self.block_chooser() {
        .                       Some(chooser) => chooser == seat,
    5,318 ( 0.00%)              None => seat != self.active_player_idx,
        .                   }
        .               }
        .           
        .               // ── Declare attackers ─────────────────────────────────────────────────────
        .           
        .               pub fn declare_attackers(
        .                   &mut self,
        .                   attacks: Vec<Attack>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
    3,780 ( 0.00%)          self.declare_attackers_banded(attacks, vec![])
93,744,532 ( 3.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_attackers_banded (3,780x)
    3,780 ( 0.00%)      }
        .           
        .               /// [`declare_attackers`] with CR 702.22c attacking bands announced in the
        .               /// same step. Each `bands` entry lists one band's members.
        .               ///
        .               /// [`declare_attackers`]: Self::declare_attackers
   41,580 ( 0.00%)      pub fn declare_attackers_banded(
        .                   &mut self,
        .                   attacks: Vec<Attack>,
        .                   bands: Vec<Vec<CardId>>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
    3,780 ( 0.00%)          if self.step != TurnStep::DeclareAttackers {
        .                       return Err(GameError::WrongStep { actual: self.step });
        .                   }
        .                   // Master Warcraft — an outside chooser declares in the active
        .                   // player's place; the attackers are still the active player's
        .                   // creatures, so only the *submitter* changes.
   11,340 ( 0.00%)          if self.priority.player_with_priority != self.attack_declarer() {
        .                       return Err(GameError::NotYourPriority);
        .                   }
        .                   // Peace Talks — CR 508.1a, nobody attacks for its two turns.
        .                   if self.truce_active() && !attacks.is_empty() {
        .                       return Err(GameError::CannotAttack(attacks[0].attacker));
        .                   }
    7,560 ( 0.00%)          let p = self.active_player_idx;
        .           
        .                   // CR 803.1a/b — under the attack-left / attack-right option, the only
        .                   // legal defending player is the nearest living opponent in that
        .                   // direction (and "more than one seat away" means you can't attack at
        .                   // all, which falls out of the same walk).
        .                   let seat_restriction = self.attack_left_right_defender();
        .           
        .                   // Validate every attack target up-front. The defender must be an
        .                   // *opponent* — not self, not a teammate. `same_team` returns true
        .                   // for `a == b`, so this single check rules out both cases. In
        .                   // 1v1 / FFA it behaves identically to the old `target != active`
        .                   // check; in 2HG / team formats it correctly rejects targeting a
        .                   // teammate's life total or planeswalker.
        .                   for atk in &attacks {
   12,696 ( 0.00%)              match atk.target {
    4,232 ( 0.00%)                  AttackTarget::Player(target_player) => {
    8,464 ( 0.00%)                      if target_player >= self.players.len()
   21,160 ( 0.00%)                          || self.same_team(self.active_player_idx, target_player)
  148,120 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (4,232x)
   12,696 ( 0.00%)                          || !self.players[target_player].is_alive()
        .                                   || seat_restriction.is_some_and(|only| only != Some(target_player))
        .                                   // CR 801.3 — only opponents inside the attacker's range.
   25,392 ( 0.00%)                          || !self.player_in_range_of(p, target_player)
  126,960 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::player_in_range_of (4,232x)
        .                                   // CR 809.3c — Emperor: only the seats either side.
        .                                   || !self.seat_attackable_from(p, target_player)
        .                                   // "Creatures they control can't attack you this turn"
        .                                   // (Web of Inertia).
    4,232 ( 0.00%)                          || self
        .                                       .cant_attack_player_this_turn
    4,232 ( 0.00%)                              .contains(&(self.active_player_idx, target_player))
        .                               {
        .                                   return Err(GameError::InvalidAttackTarget(target_player));
        .                               }
        .                           }
        .                           AttackTarget::Planeswalker(pw_id) => {
        .                               let pw = self
        .                                   .battlefield_find(pw_id)
        .                                   .ok_or(GameError::InvalidPlaneswalkerAttackTarget(pw_id))?;
-- line 282 ----------------------------------------
-- line 317 ----------------------------------------
        .                   // MustAttack and *can* legally attack (untapped, not sick / has
        .                   // Haste, not Defender / CantAttack) must be in the declared batch
        .                   // while at least one opponent is in range. Reject an incomplete
        .                   // declaration so the requirement is honored.
        .                   let has_legal_target = self
        .                       .players
        .                       .iter()
        .                       .enumerate()
   28,390 ( 0.00%)              .any(|(i, pl)| !self.same_team(p, i) && pl.is_alive());
  139,892 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (5,678x)
        .                   // CR 508.1d — Magnetic Web: once one of the group attacks, every
        .                   // able member of the group has to join it. Built before the layer
        .                   // pass because an empty group list is what lets the pass stay small
        .                   // — it reads only the battlefield and the active statics, so the
        .                   // hoist is mechanical.
        .                   let groups: Vec<crate::card::SelectionRequirement> = if has_legal_target {
    3,780 ( 0.00%)              self.battlefield
        .                           .iter()
        .                           .flat_map(|c| c.definition.static_abilities.iter().map(move |sa| (c, sa)))
        .                           .filter_map(|(c, sa)| match self.active_static(&sa.effect, c) {
        .                               Some(crate::effect::StaticEffect::AttackTogether { filter }) => {
        .                                   Some(filter.clone())
        .                               }
        .                               _ => None,
        .                           })
        .                           .collect()
        .                   } else {
        .                       Vec::new()
        .                   };
        .                   // CR 508.1a/d — Oracle en-Vec's mandate: only the chosen creatures may
        .                   // attack, and each of them that can attack must.
   15,120 ( 0.00%)          let mandate = self.armed_attack_mandate_for(p);
  105,840 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::armed_attack_mandate_for (3,780x)
        .           
        .                   // One layer pass for the whole declaration. Everything from here to
        .                   // the trigger collection is validation — nothing mutates a layer
        .                   // input — and the band, attacks-alone, can't-attack-alone and trigger
        .                   // passes were each taking their own, up to four per call.
        .                   //
        .                   // Only two consumers below read the *whole* board: the CR 508.1d
        .                   // requirement loop and the `AttackTogether` loop. Both are no-ops
        .                   // unless a permanent can carry an attack requirement, so when none
        .                   // can, the pass covers just the declared attackers, the band members
        .                   // and the creatures already attacking — a handful instead of ~23.
        .                   // One freeze scope so the gate and the pass share a single gather.
   15,120 ( 0.00%)          let (computed, attack_requirement) = self.with_frozen_layers(|g| {
    7,560 ( 0.00%)              let requirement = has_legal_target
    3,780 ( 0.00%)                  && (g.battlefield.iter().any(|c| !c.goaded_by.is_empty())
   15,120 ( 0.00%)                      || g.board_keyword_in_scope(&[
11,784,865 ( 0.38%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::board_keyword_in_scope (3,780x)
        .                                   Keyword::MustAttack,
        .                                   Keyword::MustAttackOrBlock,
        .                                   Keyword::MustAttackIfAnotherAttacks,
        .                               ]));
   15,120 ( 0.00%)              if requirement || !groups.is_empty() {
        .                           return (g.compute_battlefield(), requirement);
        .                       }
        .                       let mut ids: Vec<CardId> = Vec::new();
        .                       let mut want = |id: CardId| {
        .                           if !ids.contains(&id) {
        .                               ids.push(id);
        .                           }
        .                       };
        .                       attacks.iter().for_each(|a| want(a.attacker));
        .                       bands.iter().flatten().for_each(|&m| want(m));
        .                       g.attacking.iter().for_each(|a| want(a.attacker));
        .                       mandate.iter().flatten().for_each(|&id| want(id));
   22,680 ( 0.00%)              (g.compute_permanents(&ids), requirement)
3,858,519 ( 0.12%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::compute_permanents (3,780x)
        .                   });
        .                   // A whole-board consumer that slipped past the gate above would
        .                   // silently read `&[]` for every permanent outside the subset and drop
        .                   // its restriction. Panic on it across the suite instead.
        .                   #[cfg(debug_assertions)]
        .                   let computed_ids: Vec<CardId> = computed.iter().map(|c| c.id).collect();
        .                   #[cfg(debug_assertions)]
        .                   let battlefield_ids: Vec<CardId> = self.battlefield.iter().map(|c| c.id).collect();
-- line 389 ----------------------------------------
-- line 429 ----------------------------------------
        .                   }
        .           
        .                   // CR 508.1 — Crawlspace: no more than N creatures can attack a player
        .                   // who controls an `AttackerCapAgainstController` permanent.
        .                   for p in 0..self.players.len() {
        .                       let Some(cap) = self
        .                           .battlefield
        .                           .iter()
  145,296 ( 0.00%)                  .filter(|c| c.controller == p)
        .                           .filter_map(|c| {
        .                               c.definition.static_abilities.iter().find_map(|sa| match sa.effect {
        .                                   crate::effect::StaticEffect::AttackerCapAgainstController { n } => Some(n),
        .                                   _ => None,
        .                               })
        .                           })
        .                           .min()
        .                       else {
-- line 445 ----------------------------------------
-- line 453 ----------------------------------------
        .                   }
        .           
        .                   // CR 701.15b — a goaded creature "attacks a player other than the
        .                   // controller of the [goad source] if able." Enforce the player-target
        .                   // half: a goaded attacker may not attack one of its own goaders while
        .                   // an unattacked, alive non-goader opponent it could instead attack
        .                   // exists. (Planeswalker/battle redirection is not modeled here.)
        .                   for atk in &attacks {
   12,696 ( 0.00%)              if let AttackTarget::Player(target_player) = atk.target
    4,232 ( 0.00%)                  && let Some(c) = self.battlefield_find(atk.attacker)
        .                           && c.goaded_by.contains(&target_player)
        .                       {
        .                           let has_nongoader_option = (0..self.players.len()).any(|q| {
        .                               q != self.active_player_idx
        .                                   && !self.same_team(self.active_player_idx, q)
        .                                   && self.players[q].is_alive()
        .                                   && !c.goaded_by.contains(&q)
        .                           });
-- line 470 ----------------------------------------
-- line 473 ----------------------------------------
        .                           }
        .                       }
        .                   }
        .           
        .                   // Angelic Arbiter — an opponent who cast a spell this turn can't
        .                   // attack with creatures at all.
        .                   if !attacks.is_empty() {
        .                       let p = self.active_player_idx;
    7,104 ( 0.00%)              if self.players[p].spells_cast_this_turn > 0
    4,176 ( 0.00%)                  && self.opponent_has_static(p, |e| {
  657,296 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::opponent_has_static (1,044x)
        .                               matches!(e, crate::effect::StaticEffect::OpponentsWhoCastCantAttack)
        .                           })
        .                       {
        .                           return Err(GameError::CannotAttack(attacks[0].attacker));
        .                       }
        .                   }
        .           
        .                   // CR 508.0 — "attacks only alone" (Master of Cruelties). If any
        .                   // declared attacker carries AttacksAlone, the batch must be a
        .                   // single attacker. Read from the computed keyword set so granted
        .                   // variants count.
    4,736 ( 0.00%)          if attacks.len() > 1
   10,080 ( 0.00%)              && attacks.iter().any(|atk| {
  206,620 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (1,120x)
        .                           computed
        .                               .iter()
        .                               .find(|c| c.id == atk.attacker)
        .                               .is_some_and(|c| c.keywords.contains(&Keyword::AttacksAlone))
        .                       })
        .                   {
        .                       return Err(GameError::CannotAttack(attacks[0].attacker));
        .                   }
        .           
        .                   // CR 508.0 — "can't attack alone" (Militia Rallier). A lone attacker
        .                   // carrying CantAttackAlone makes the batch illegal.
    2,240 ( 0.00%)          if attacks.len() == 1
   14,976 ( 0.00%)              && computed.iter().find(|c| c.id == attacks[0].attacker).is_some_and(|c| {
   24,960 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find (1,248x)
        .                           c.keywords.contains(&Keyword::CantAttackAlone)
        .                               || c.keywords.contains(&Keyword::CantAttackOrBlockAlone)
    8,736 ( 0.00%)              })
        .                   {
        .                       return Err(GameError::CannotAttack(attacks[0].attacker));
        .                   }
        .           
        .                   // CR 506.2 — Silent Arbiter: "No more than N creatures can attack each
        .                   // combat." The cap covers the whole combat, so count attackers already
        .                   // declared this combat alongside the incoming batch.
   18,900 ( 0.00%)          if let Some(cap) = self.combat_participation_cap(false)
  660,564 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap (3,780x)
        .                       && let Some(first) = attacks.first()
        .                       && self.attacking.len() + attacks.len() > cap as usize
        .                   {
        .                       return Err(GameError::CannotAttack(first.attacker));
        .                   }
        .           
        .                   let mut events = vec![];
        .                   // Per CR 506.5, the Attacks trigger filter must be evaluated
-- line 527 ----------------------------------------
-- line 542 ----------------------------------------
        .                       );
        .                       computed
        .                           .iter()
        .                           .find(|c| c.id == id)
        .                           .map(|c| c.keywords.as_slice())
        .                           .unwrap_or(&[])
        .                   };
        .           
   26,460 ( 0.00%)          if let Some(chosen) = mandate {
        .                       if let Some(bad) = attacks.iter().find(|a| !chosen.contains(&a.attacker)) {
        .                           return Err(GameError::CannotAttack(bad.attacker));
        .                       }
        .                       for id in chosen {
        .                           let Some(c) = self.battlefield.iter().find(|c| c.id == id && c.controller == p)
        .                           else {
        .                               continue;
        .                           };
-- line 558 ----------------------------------------
-- line 563 ----------------------------------------
        .                               && !kws.contains(&Keyword::CantAttack)
        .                               && (!c.summoning_sick || kws.contains(&Keyword::Haste));
        .                           if able && has_legal_target && !attacks.iter().any(|a| a.attacker == id) {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                       }
        .                   }
        .           
    7,560 ( 0.00%)          if attack_requirement {
        .                       for c in &self.battlefield {
        .                           // A creature must be declared if it carries MustAttack
        .                           // (Juggernaut) or is goaded (CR 701.38 — "attacks each
        .                           // combat if able").
        .                           let must = computed_kw(c.id).contains(&Keyword::MustAttack)
        .                               || computed_kw(c.id).contains(&Keyword::MustAttackOrBlock)
        .                               // CR 508.1d — Ekundu Cyclops only has to join an attack
        .                               // someone else already started.
-- line 579 ----------------------------------------
-- line 590 ----------------------------------------
        .                               && !kws.contains(&Keyword::CantAttack)
        .                               && (!c.summoning_sick || kws.contains(&Keyword::Haste));
        .                           if able && !attacks.iter().any(|atk| atk.attacker == c.id) {
        .                               return Err(GameError::CannotAttack(c.id));
        .                           }
        .                       }
        .                   }
        .                   // CR 508.1d — Magnetic Web (`groups`, built above the layer pass).
   30,240 ( 0.00%)          for filter in groups {
        .                       let matches = |id: CardId| {
        .                           self.evaluate_requirement_static(&filter, &Target::Permanent(id), p, None)
        .                       };
        .                       if !attacks.iter().any(|atk| matches(atk.attacker)) {
        .                           continue;
        .                       }
        .                       for c in &self.battlefield {
        .                           if c.controller != p || !matches(c.id) {
-- line 606 ----------------------------------------
-- line 617 ----------------------------------------
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 601.2h-style atomicity: validate the entire declaration before
        .                   // any state is spent (attack tax) or mutated (tapping attackers) —
        .                   // one illegal attacker must not corrupt the batch.
        .                   {
    3,780 ( 0.00%)              let attack_power_caps: Vec<usize> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::AttackPowerCapByControllerHand
        .                                   )
        .                               })
        .                           })
        .                           .map(|c| self.players[c.controller].hand.len())
        .                           .collect();
        .                       let mut seen = crate::fxhash::HashSet::default();
        .                       for atk in &attacks {
    8,464 ( 0.00%)                  let id = atk.attacker;
    8,464 ( 0.00%)                  if !seen.insert(id) {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // Ensnaring Bridge cap (computed power, CR 613).
    8,464 ( 0.00%)                  if !attack_power_caps.is_empty() {
        .                               let power = self.computed_permanent(id).map(|c| c.power).unwrap_or(0);
        .                               if attack_power_caps.iter().any(|cap| power > *cap as i32) {
        .                                   return Err(GameError::CannotAttack(id));
        .                               }
        .                           }
        .                           // "Can't attack unless defending player controls a [filter]"
        .                           // (Dandân).
   44,612 ( 0.00%)                  if let Some(req) = computed_kw(id).iter().find_map(|kw| match kw {
   89,988 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (4,232x)
        .                               Keyword::CanAttackOnlyIfDefenderControls(r) => Some(r.clone()),
        .                               _ => None,
        .                           }) {
        .                               let defender = self.defender_for(atk.target);
        .                               let satisfied = defender.is_some_and(|d| {
        .                                   self.battlefield.iter().any(|c| {
        .                                       c.controller == d && self.evaluate_requirement_on_card(&req, c, d)
        .                                   })
        .                               });
        .                               if !satisfied {
        .                                   return Err(GameError::CannotAttack(id));
        .                               }
        .                           }
        .                           // CR 725 — Crown-Hunter Hireling: only the monarch can be
        .                           // attacked.
    4,232 ( 0.00%)                  if computed_kw(id).contains(&Keyword::CantAttackUnlessDefenderIsMonarch)
        .                               && self.defender_for(atk.target) != self.monarch
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // CR 508.1a — Merchant Ship: the defending player must control
        .                           // a land of the named type.
   14,224 ( 0.00%)                  if let Some(lt) = computed_kw(id).iter().find_map(|k| match k {
        .                               Keyword::CantAttackUnlessDefenderControlsLandType(lt) => Some(*lt),
        .                               _ => None,
        .                           }) && let Some(d) = self.defender_for(atk.target)
        .                               && !self.battlefield.iter().any(|c| {
        .                                   c.controller == d && c.definition.subtypes.land_types.contains(&lt)
        .                               })
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // CR 508.1a — Branded Brawlers: the defender having any
        .                           // untapped land locks the attack.
    4,232 ( 0.00%)                  if computed_kw(id).contains(&Keyword::CantAttackIfDefenderHasUntappedLand)
        .                               && let Some(d) = self.defender_for(atk.target)
        .                               && self.battlefield.iter().any(|c| {
        .                                   c.controller == d && c.definition.is_land() && !c.tapped
        .                               })
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // CR 508.1a — Mogg Toady: strictly more creatures than the
        .                           // defending player.
    4,232 ( 0.00%)                  if computed_kw(id).contains(&Keyword::CantAttackUnlessMoreCreaturesThanDefender)
        .                               && let Some(d) = self.defender_for(atk.target)
        .                               && self.creature_count(p) <= self.creature_count(d)
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // "Can't attack unless you control a [filter]" (Lovestruck Beast).
   36,148 ( 0.00%)                  if let Some(req) = computed_kw(id).iter().find_map(|kw| match kw {
   89,988 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (4,232x)
        .                               Keyword::CanAttackOnlyIfYouControl(r) => Some(r.clone()),
        .                               _ => None,
        .                           }) {
        .                               let satisfied = self.battlefield.iter().any(|c| {
        .                                   c.controller == p && self.evaluate_requirement_on_card(&req, c, p)
        .                               });
        .                               if !satisfied {
        .                                   return Err(GameError::CannotAttack(id));
        .                               }
        .                           }
        .                           // "Can't attack unless you control N+ [filter]" (Topiary
        .                           // Stomper — seven or more lands).
   39,616 ( 0.00%)                  if let Some((req, min, excl)) = computed_kw(id).iter().find_map(|kw| match kw {
   91,184 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (4,232x)
        .                               Keyword::CantAttackOrBlockUnlessYouControlCount {
        .                                   filter, min, block_only: false, exclude_self, ..
        .                               } => Some((filter.clone(), *min, *exclude_self)),
        .                               _ => None,
        .                           }) {
        .                               let n = self
        .                                   .battlefield
        .                                   .iter()
-- line 726 ----------------------------------------
-- line 730 ----------------------------------------
        .                                           && self.evaluate_requirement_on_card(&req, c, p)
        .                                   })
        .                                   .count();
        .                               if (n as u32) < min {
        .                                   return Err(GameError::CannotAttack(id));
        .                               }
        .                           }
        .                           // CR 508.1a — Monstrous Hound: more lands than the defender.
    4,232 ( 0.00%)                  if computed_kw(id).contains(&Keyword::CantAttackUnlessMoreLandsThanDefender)
        .                               && let Some(d) = self.defender_for(atk.target)
        .                               && self.player_tally(p, crate::card::PlayerTally::LandsControlled)
        .                                   <= self.player_tally(d, crate::card::PlayerTally::LandsControlled)
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // "Even number of counters" gate (Sab-Sunen). Zero is even.
    4,232 ( 0.00%)                  if computed_kw(id).contains(&Keyword::CantAttackOrBlockUnlessEvenCounters)
        .                               && let Some(c) = self.battlefield.iter().find(|c| c.id == id)
        .                               && c.counters.values().sum::<u32>() % 2 != 0
        .                           {
        .                               return Err(GameError::CannotAttack(id));
        .                           }
        .                           // Controller (not owner) must be the active player.
    4,232 ( 0.00%)                  let card = self
        .                               .battlefield
        .                               .iter()
   67,816 ( 0.00%)                      .find(|c| c.id == id && c.controller == p)
        .                               .ok_or(GameError::CardNotOnBattlefield(id))?;
        .                           let is_creature_now = computed
        .                               .iter()
        .                               .find(|c| c.id == id)
        .                               .map(|c| c.card_types.contains(&crate::card::CardType::Creature))
        .                               .unwrap_or_else(|| card.definition.is_creature());
    4,232 ( 0.00%)                  let kws = computed_kw(id);
        .                           // CR 508.1a — Goblin Cohort can't attack unless its controller
        .                           // cast a creature spell this turn.
        .                           let cohort_locked = kws
        .                               .contains(&Keyword::CantAttackUnlessCastCreatureThisTurn)
        .                               && self.players[p].creatures_cast_this_turn == 0;
        .                           // CR 508.1a — Hazoret-class: can't attack unless hand is small.
   29,624 ( 0.00%)                  let hand_locked = kws.iter().any(|k| {
   77,452 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (4,232x)
    9,992 ( 0.00%)                      matches!(k, Keyword::CantAttackOrBlockUnlessHandSizeAtMost(n)
        .                                   if self.players[p].hand.len() as u32 > *n)
        .                           });
        .                           // CR 508.1a — Delirium gate (Patchwork Beastie).
        .                           let delirium_locked = kws.contains(&Keyword::CantAttackOrBlockUnlessDelirium)
        .                               && !self.delirium_active(p);
        .                           // CR 508.1a — "a creature died under your control this turn"
        .                           // gate (Bontu the Glorified).
        .                           let creature_died_locked =
        .                               kws.contains(&Keyword::CantAttackOrBlockUnlessCreatureDiedThisTurn)
        .                                   && self.players[p].creatures_died_this_turn == 0;
        .                           // CR 508.1a — Descend N gate (The Ancient One).
   29,624 ( 0.00%)                  let descend_locked = kws.iter().any(|k| {
  142,912 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (4,232x)
    9,992 ( 0.00%)                      matches!(k, Keyword::CantAttackOrBlockUnlessDescend(n)
        .                                   if self.descend_count(p) < *n as usize)
        .                           });
        .                           // CR 508.1a — city's blessing gate (Wayward Swordtooth).
        .                           let blessing_locked =
        .                               kws.contains(&Keyword::CantAttackOrBlockUnlessCityBlessing)
        .                                   && !self.players[p].city_blessing;
        .                           // CR 508.1a — Glacial Crasher: needs a land of the named
        .                           // type on the battlefield (anyone's).
   21,160 ( 0.00%)                  let land_locked = kws.iter().any(|k| {
   71,712 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (4,232x)
    9,992 ( 0.00%)                      matches!(k, Keyword::CantAttackUnlessLandTypeOnBattlefield(lt)
        .                                   if !self.battlefield.iter().any(|c| c.definition.subtypes.land_types.contains(lt)))
        .                           });
        .                           // CR 508.1a — Harbor Serpent's "five or more Islands on the
        .                           // battlefield" and Bloodcrazed Goblin's "an opponent has been
        .                           // dealt damage this turn".
   53,840 ( 0.00%)                  let predicate_locked = kws.iter().any(|k| match k {
  169,400 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (4,232x)
        .                               Keyword::CantAttackUnlessLandCount(lt, n) => {
        .                                   (self
        .                                       .battlefield
        .                                       .iter()
        .                                       .filter(|c| c.definition.subtypes.land_types.contains(lt))
        .                                       .count() as u32)
        .                                       < *n
        .                               }
-- line 808 ----------------------------------------
-- line 818 ----------------------------------------
        .                               }
        .                               // Ketramose — "unless there are seven or more cards in exile".
        .                               Keyword::CantAttackOrBlockUnlessCardsInExile(n) => {
        .                                   (self.exile.len() as u32) < *n
        .                               }
        .                               _ => false,
        .                           });
        .                           // CR 508.1a — Wall of Dust's ban, live for exactly this turn.
    4,232 ( 0.00%)                  let banned = self
        .                               .battlefield_find(id)
        .                               .is_some_and(|c| c.attack_ban == crate::card::AttackBan::Active);
        .                           // CR 508.1a — Okk: needs a strictly bigger partner in the same
        .                           // declared batch (already-declared attackers count too).
        .                           let okk_locked = kws.contains(&Keyword::CantAttackUnlessGreaterPowerAttacks) && {
        .                               let mine = computed
        .                                   .iter()
        .                                   .find(|c| c.id == id)
-- line 834 ----------------------------------------
-- line 840 ----------------------------------------
        .                                   .chain(self.attacking.iter().map(|a| a.attacker))
        .                                   .filter(|other| *other != id)
        .                                   .any(|other| {
        .                                       computed.iter().find(|c| c.id == other).is_some_and(|c| c.power > mine)
        .                                   })
        .                           };
        .                           let defender_locked =
        .                               kws.contains(&Keyword::Defender) && !self.ignores_defender_for_attack(card);
    8,464 ( 0.00%)                  let can_attack = is_creature_now
   12,696 ( 0.00%)                      && !card.tapped
    8,464 ( 0.00%)                      && card.detained_by.is_none()
        .                               && !defender_locked
    8,464 ( 0.00%)                      && !kws.contains(&Keyword::CantAttack)
        .                               && !predicate_locked
        .                               && !banned
   63,480 ( 0.00%)                      && !cohort_locked
        .                               && !land_locked
        .                               && !hand_locked
        .                               && !delirium_locked
        .                               && !creature_died_locked
        .                               && !descend_locked
        .                               && !blessing_locked
        .                               && !okk_locked
    8,464 ( 0.00%)                      && (!card.summoning_sick || kws.contains(&Keyword::Haste));
        .                           if !can_attack {
        .                               if card.tapped {
        .                                   return Err(GameError::CardIsTapped(id));
        .                               }
        .                               // CR 701.35 — a detained permanent can't attack.
        .                               if card.detained_by.is_some()
        .                                   || defender_locked
        .                                   || kws.contains(&Keyword::CantAttack)
-- line 871 ----------------------------------------
-- line 878 ----------------------------------------
        .                                   || blessing_locked
        .                                   || okk_locked
        .                               {
        .                                   return Err(GameError::CannotAttack(id));
        .                               }
        .                               return Err(GameError::SummoningSickness(id));
        .                           }
        .                       }
    3,780 ( 0.00%)          }
        .           
        .                   // CR 508.1 — absolute "creatures can't attack you" prohibition (Blazing
        .                   // Archon). Reject the whole declaration if any attacker targets a player
        .                   // (or protected planeswalker) whose controller has the static.
        .                   for atk in &attacks {
   16,928 ( 0.00%)              let (defender, at_planeswalker) = match atk.target {
    8,464 ( 0.00%)                  crate::game::types::AttackTarget::Player(d) => (Some(d), false),
        .                           crate::game::types::AttackTarget::Planeswalker(pw) => {
        .                               (self.battlefield_find(pw).map(|c| c.controller), true)
        .                           }
        .                           crate::game::types::AttackTarget::Battle(b) => {
        .                               (self.battlefield_find(b).and_then(|c| c.protected_by), false)
        .                           }
        .                       };
    4,232 ( 0.00%)              let Some(d) = defender else { continue };
        .                       // CR 508.1 — Arboria: a player who did nothing on their own last
        .                       // turn can't be attacked at all.
    8,464 ( 0.00%)              if !self.acted_on_their_last_turn(d)
    1,050 ( 0.00%)                  && self.battlefield.iter().any(|c| {
   66,590 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (210x)
        .                               c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       self.active_static(&sa.effect, c),
        .                                       Some(crate::effect::StaticEffect::
        .                                           PlayersCantBeAttackedUnlessTheyActedLastTurn)
        .                                   )
        .                               })
        .                           })
        .                       {
        .                           return Err(GameError::CannotAttack(atk.attacker));
        .                       }
        .                       // (source id, optional attacker filter) for each live prohibition.
    4,232 ( 0.00%)              let locks: Vec<(CardId, Option<crate::card::SelectionRequirement>)> = self
        .                           .battlefield
        .                           .iter()
  370,992 ( 0.01%)                  .filter(|c| c.controller == d)
        .                           .flat_map(|c| c.definition.static_abilities.iter().map(move |sa| (c.id, sa)))
        .                           .filter_map(|(id, sa)| match &sa.effect {
        .                               crate::effect::StaticEffect::CreaturesCantAttackController {
        .                                   protect_planeswalkers,
        .                                   filter,
        .                               } if !at_planeswalker || *protect_planeswalkers => {
        .                                   Some((id, filter.clone()))
        .                               }
-- line 929 ----------------------------------------
-- line 935 ----------------------------------------
        .                               self.evaluate_requirement_static(
        .                                   &f,
        .                                   &Target::Permanent(atk.attacker),
        .                                   d,
        .                                   Some(src),
        .                               )
        .                           })
        .                       });
    8,464 ( 0.00%)              if barred {
        .                           return Err(GameError::CannotAttack(atk.attacker));
        .                       }
        .                   }
        .           
        .                   // CR 508.1g — attack tax (Ghostly Prison / Propaganda). Sum {amount}
        .                   // for each attacker hitting a player who controls an
        .                   // `AttackTaxToController` static (copies stack), then pay it from the
        .                   // active player's pool, auto-tapping for any shortfall. Reject the
        .                   // whole declaration if it's unpayable.
        .                   let mut total_tax = 0u32;
        .                   for atk in &attacks {
        .                       // The defending player whose statics apply, and whether the attack
        .                       // is aimed at a planeswalker (so `protect_planeswalkers` gates it).
   21,160 ( 0.00%)              let (defender, at_planeswalker) = match atk.target {
    8,464 ( 0.00%)                  crate::game::types::AttackTarget::Player(d) => (Some(d), false),
        .                           crate::game::types::AttackTarget::Planeswalker(pw) => {
        .                               (self.battlefield_find(pw).map(|c| c.controller), true)
        .                           }
        .                           // The protector's attack taxes apply when attacking their
        .                           // battle; not a planeswalker for `protect_planeswalkers`.
        .                           crate::game::types::AttackTarget::Battle(b) => {
        .                               (self.battlefield_find(b).and_then(|c| c.protected_by), false)
        .                           }
        .                       };
        .                       let Some(d) = defender else { continue };
        .                       // Forbidding Spirit — a temporary Propaganda tax on the defender
        .                       // that also protects their planeswalkers.
    8,464 ( 0.00%)              total_tax += self.players[d].attack_tax_until_your_turn;
        .                       // War Tax — a symmetric per-attacker tax for the rest of the turn.
    4,232 ( 0.00%)              total_tax += self.attack_tax_this_turn;
        .                       // Evaluate each tax `amount` with the defender as "you" (and the
        .                       // tax permanent as source) so dynamic taxes — Sphere of Safety's
        .                       // "number of enchantments you control" — count the defender's
        .                       // board. Fixed taxes are `Value::Const(n)`.
        .                       for c in &self.battlefield {
  185,496 ( 0.01%)                  if c.controller != d {
        .                               continue;
        .                           }
        .                           for sa in &c.definition.static_abilities {
        .                               if let crate::effect::StaticEffect::AttackTaxToController {
        .                                   amount,
        .                                   protect_planeswalkers,
        .                                   filter,
        .                               } = &sa.effect
-- line 987 ----------------------------------------
-- line 1003 ----------------------------------------
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // CR 508.1g — per-attacker "can't attack unless its controller pays
        .                   // {N}" (Oppressive Rays). Same pool as the Propaganda tax above.
        .                   for atk in &attacks {
        .                       {
    4,232 ( 0.00%)                  total_tax += self
   16,928 ( 0.00%)                      .attack_block_keyword_tax(atk.attacker, computed_kw(atk.attacker), true);
  176,376 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::attack_block_keyword_tax (4,232x)
        .                       }
        .                   }
    7,104 ( 0.00%)          if total_tax > 0 {
        .                       // Pay from the floating pool, auto-tapping mana sources for any
        .                       // shortfall (rolled back atomically if unpayable).
        .                       let tax_cost = crate::mana::cost(&[crate::mana::generic(total_tax)]);
        .                       if self.try_pay_with_auto_tap(p, &tax_cost).is_err() {
        .                           return Err(GameError::CannotAttack(attacks[0].attacker));
        .                       }
        .                   }
        .           
        .                   // CR 508.1g — Floodtide Serpent's attack cost: each such attacker
        .                   // returns one matching permanent its controller controls to hand. The
        .                   // pool is checked and consumed together so two Serpents need two
        .                   // enchantments.
        .                   {
        .                       let mut spent: Vec<CardId> = Vec::new();
        .                       for atk in &attacks {
    8,464 ( 0.00%)                  let filters: Vec<crate::card::SelectionRequirement> = computed_kw(atk.attacker)
        .                               .iter()
    9,992 ( 0.00%)                      .filter_map(|k| match k {
        .                                   Keyword::AttackCostBounce(f) => Some((**f).clone()),
    4,996 ( 0.00%)                          _ => None,
        .                               })
        .                               .collect();
   16,928 ( 0.00%)                  for f in filters {
        .                               let pick = self.battlefield.iter().find(|c| {
        .                                   c.controller == p
        .                                       && !spent.contains(&c.id)
        .                                       && self.evaluate_requirement_static(
        .                                           &f,
        .                                           &crate::game::types::Target::Permanent(c.id),
        .                                           p,
        .                                           Some(atk.attacker),
-- line 1046 ----------------------------------------
-- line 1048 ----------------------------------------
        .                               });
        .                               match pick {
        .                                   Some(c) => spent.push(c.id),
        .                                   None => return Err(GameError::CannotAttack(atk.attacker)),
        .                               }
        .                           }
        .                       }
        .                       let mut events = Vec::new();
   22,680 ( 0.00%)              let ctx = crate::game::effects::EffectContext::for_ability(CardId(0), p, None);
  162,540 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::EffectContext::for_ability (3,780x)
   18,900 ( 0.00%)              for id in spent {
        .                           self.move_card_to(
        .                               id,
        .                               &crate::effect::ZoneDest::Hand(crate::effect::PlayerRef::OwnerOfMoved),
        .                               &ctx,
        .                               &mut events,
        .                           );
        .                       }
    3,780 ( 0.00%)              self.dispatch_triggers_for_events(&events);
  446,040 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (3,780x)
   11,340 ( 0.00%)          }
   98,280 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>> (3,780x)
   60,480 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::effects::EffectContext> (3,780x)
        .           
        .                   // CR 508.1g — Leviathan's attack cost: sacrifice N matching permanents
        .                   // per such attacker. The pool is shared, so two Leviathans need four
        .                   // Islands.
        .                   {
        .                       let mut spent: Vec<CardId> = Vec::new();
        .                       for atk in &attacks {
        .                           let costs: Vec<(crate::card::SelectionRequirement, u32)> =
    8,464 ( 0.00%)                      computed_kw(atk.attacker)
        .                                   .iter()
    9,992 ( 0.00%)                          .filter_map(|k| match k {
        .                                       Keyword::AttackCostSacrifice(f, n) => Some(((**f).clone(), *n)),
    4,996 ( 0.00%)                              _ => None,
        .                                   })
        .                                   .collect();
   21,160 ( 0.00%)                  for (f, n) in costs {
        .                               for _ in 0..n {
        .                                   let pick = self.battlefield.iter().find(|c| {
        .                                       c.controller == p
        .                                           && !spent.contains(&c.id)
        .                                           && self.evaluate_requirement_static(
        .                                               &f,
        .                                               &crate::game::types::Target::Permanent(c.id),
        .                                               p,
-- line 1090 ----------------------------------------
-- line 1094 ----------------------------------------
        .                                   match pick {
        .                                       Some(c) => spent.push(c.id),
        .                                       None => return Err(GameError::CannotAttack(atk.attacker)),
        .                                   }
        .                               }
        .                           }
        .                       }
        .                       let mut events = Vec::new();
   13,252 ( 0.00%)              for id in spent {
        .                           self.sacrifice_one(id, p, &mut events);
        .                       }
    3,780 ( 0.00%)              self.dispatch_triggers_for_events(&events);
  446,040 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (3,780x)
    3,780 ( 0.00%)          }
   98,280 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>> (3,780x)
        .           
        .                   // CR 508.1g — Hollow Warrior's attack cost: tap an untapped matching
        .                   // permanent that isn't itself attacking. One helper per such attacker.
        .                   {
        .                       let declared: Vec<CardId> = attacks.iter().map(|a| a.attacker).collect();
        .                       let mut tapped: Vec<CardId> = Vec::new();
        .                       for atk in &attacks {
   25,392 ( 0.00%)                  for f in tap_another_filters(computed_kw(atk.attacker)) {
        .                               match self.find_tap_helper(p, &f, atk.attacker, &declared, &tapped) {
        .                                   Some(id) => tapped.push(id),
        .                                   None => return Err(GameError::CannotAttack(atk.attacker)),
        .                               }
        .                           }
        .                       }
    7,104 ( 0.00%)              for id in tapped {
        .                           if let Some(c) = self.battlefield_find_mut(id) {
        .                               c.tapped = true;
        .                           }
        .                       }
    6,148 ( 0.00%)          }
        .           
        .                   let any_attackers = !attacks.is_empty();
        .                   // CR 702.121 — Melee counts the distinct opponents this player attacked
        .                   // this combat (a player targeted directly, or the controller of a
        .                   // planeswalker/battle attacked). Computed over the whole batch up front.
        .                   let melee_opponents: i32 = {
        .                       let mut seats: crate::fxhash::HashSet<usize> = crate::fxhash::HashSet::default();
        .                       for atk in &attacks {
    8,464 ( 0.00%)                  let seat = match atk.target {
    4,232 ( 0.00%)                      AttackTarget::Player(s) => Some(s),
        .                               AttackTarget::Planeswalker(cid) | AttackTarget::Battle(cid) => {
        .                                   self.battlefield_find(cid).map(|c| c.controller)
        .                               }
        .                           };
   21,160 ( 0.00%)                  if let Some(s) = seat.filter(|s| !self.same_team(*s, p)) {
  148,120 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (4,232x)
        .                               seats.insert(s);
        .                           }
        .                       }
        .                       seats.len() as i32
        .                   };
   40,512 ( 0.00%)          for atk in attacks {
    8,464 ( 0.00%)              let id = atk.attacker;
        .                       // Statics-granted triggers ("Slivers you control have
        .                       // '…attacks…'" — Thorncaster) fire as though printed. Gathered
        .                       // before the mutable borrow below.
    4,232 ( 0.00%)              let static_granted = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
   12,696 ( 0.00%)                  .map(|c| self.statics_granted_triggers_for(c))
2,569,812 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_for (4,232x)
        .                           .unwrap_or_default();
        .                       // Equipment-granted Attacks triggers (CR 702.6e) — e.g. "whenever
        .                       // equipped creature attacks, …". These fire off the creature, so
        .                       // gather them here alongside the printed/static SelfSource set
        .                       // (the general dispatch hardcodes-skips SelfSource Attacks to avoid
        .                       // double-firing, so equip grants must be picked up on this path).
    4,232 ( 0.00%)              let equip_granted = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
   62,776 ( 0.00%)                  .map(|c| self.equip_granted_triggers_for(c))
        .                           .unwrap_or_default();
        .                       // Validated above — commit only. Filter by *controller*, not
        .                       // *owner*: a stolen creature (Threaten / Mind Control) attacks
        .                       // for its current controller.
        .                       // Both of these read the cold group, whose `Deref` borrows the
        .                       // whole state — take them before the battlefield `&mut`.
    8,464 ( 0.00%)              let decayed = computed_kw(id).contains(&Keyword::Decayed);
        .                       let granted: Vec<crate::card::TriggeredAbility> =
        .                           self.granted_triggers_eot.get(&id).cloned().unwrap_or_default();
        .                       let card = self
        .                           .battlefield
        .                           .iter_mut()
   67,816 ( 0.00%)                  .find(|c| c.id == id && c.controller == p)
        .                           .ok_or(GameError::CardNotOnBattlefield(id))?;
    4,232 ( 0.00%)              if !computed_kw(id).contains(&Keyword::Vigilance) {
    4,068 ( 0.00%)                  card.tapped = true;
        .                           // CR 508.1f — attacking taps the creature; surface a
        .                           // "becomes tapped" event so Tapped triggers fire (Magda).
   16,272 ( 0.00%)                  events.push(GameEvent::PermanentTapped { card_id: id, actor: None, as_attacker: true });
        .                       }
        .                       // CR 702.83 — Exert. We auto-exert any attacking creature with
        .                       // the keyword (the "you may" choice is collapsed; the AutoDecider
        .                       // would have no policy and a real exert is almost always taken for
        .                       // its bonus). The creature won't untap next untap step. Its exert
        .                       // bonus rides its normal SelfSource Attacks trigger.
    4,232 ( 0.00%)              if computed_kw(id).contains(&Keyword::Exert) {
        .                           card.skip_next_untap = true;
        .                       }
        .                       // CR 702.121 — Melee: +1/+1 until end of turn per opponent attacked.
    8,464 ( 0.00%)              if melee_opponents > 0 && computed_kw(id).contains(&Keyword::Melee) {
        .                           card.power_bonus += melee_opponents;
        .                           card.toughness_bonus += melee_opponents;
        .                       }
        .                       // CR 702.142 — record that this creature attacked (gates Boast).
    4,232 ( 0.00%)              card.attacked_this_turn = true;
    4,232 ( 0.00%)              card.attacked_own_turn = true;
        .                       self.attacking.push(atk);
        .                       // Raid (CR 702.108 ability word): the controller attacked this turn.
    8,464 ( 0.00%)              self.players[p].attacked_this_turn = true;
7,347,529 ( 0.24%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (4,232x)
    8,464 ( 0.00%)              self.players[p].creatures_attacked_this_turn += 1;
  126,960 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (4,232x)
   12,696 ( 0.00%)              events.push(GameEvent::AttackerDeclared(id));
        .                       // Walk printed Attacks triggers + any transient granted
        .                       // Attacks triggers (Root Manipulation's "gain 1 life when
        .                       // this attacks" grant lands in `granted_triggers_eot`).
   46,552 ( 0.00%)              for t in card
        .                           .definition
        .                           .triggered_abilities
        .                           .iter()
        .                           .chain(granted.iter())
        .                           .chain(static_granted.iter())
        .                           .chain(equip_granted.iter())
        .                       {
        .                           // Only SelfSource Attacks triggers are hardcoded here.
        .                           // YourControl-scoped Attacks triggers (Exalted via
        .                           // `Predicate::AttackingAlone`, Battle Banner, …) are
        .                           // routed through the unified `dispatch_triggers_for_events`
        .                           // path off the `AttackerDeclared` event — pushing them
        .                           // here too would double-fire the ability.
      412 ( 0.00%)                  if t.event.kind == EventKind::Attacks
      528 ( 0.00%)                      && t.event.scope == crate::effect::EventScope::SelfSource
        .                           {
        .                               // Capture the trigger's optional filter so we can
        .                               // re-evaluate it AFTER the entire attacker batch is
        .                               // declared (CR 506.5 "attacking alone" semantics
        .                               // require the post-batch view).
    5,016 ( 0.00%)                      triggers.push((id, t.effect.clone(), p, t.event.filter.clone()));
   28,248 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (528x)
   19,272 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (264x)
        .                           }
        .                       }
        .                       // CR 702.147 — Decayed. "When it attacks, sacrifice it at end of
        .                       // combat." Reuse the attacking-token cleanup queue (CR 511.3).
    4,232 ( 0.00%)              if decayed {
        .                           self.attacking_token_cleanup.push((
        .                               id,
        .                               crate::effect::AttackingTokenCleanup::SacrificeAtEndOfCombat,
        .                           ));
        .                       }
        .                       // Annihilator N — CR 702.85a: "Whenever this creature attacks,
        .                       // defending player sacrifices N permanents." Translate the
        .                       // keyword to an Attacks-trigger that fires
        .                       // `Effect::Sacrifice { who: defender, count: N, filter: Any }`.
        .                       // The defender comes from `atk.target`; for a planeswalker
        .                       // attack, that's the planeswalker's controller (CR 506.4a).
    4,232 ( 0.00%)              let annihilator_n = computed_kw(id).iter().find_map(|kw| {
    9,992 ( 0.00%)                  if let Keyword::Annihilator(n) = kw {
        .                               Some(*n)
        .                           } else {
        .                               None
        .                           }
        .                       });
    8,464 ( 0.00%)              if let Some(n) = annihilator_n
        .                           && let Some(defender) = self.defender_for(atk.target)
        .                       {
        .                           let sac_effect = Effect::Sacrifice {
        .                               who: Selector::Player(crate::effect::PlayerRef::Seat(defender)),
        .                               count: Value::Const(n as i32),
        .                               filter: crate::card::SelectionRequirement::Permanent,
        .                           };
        .                           triggers.push((id, sac_effect, p, None));
        .                       }
        .                       // Firebending N — CR 702.189a: a triggered mana ability (resolves
        .                       // without the stack, CR 605.3b). Add N {R} now; the mana survives
        .                       // step/phase emptying until end of combat (`firebending_kept_red`).
   63,068 ( 0.00%)              let firebend_n = computed_kw(id).iter().find_map(|kw| match kw {
  157,288 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (4,232x)
        .                           Keyword::Firebending(n) => Some(*n),
        .                           // Firebending X = this creature's power (clamped at 0).
        .                           Keyword::FirebendingPower => Some(
        .                               self.computed_permanent(id)
        .                                   .map(|c| c.power.max(0) as u32)
        .                                   .unwrap_or(0),
        .                           ),
        .                           Keyword::FirebendingCreaturesYouControl => Some(
        .                               self.battlefield
        .                                   .iter()
        .                                   .filter(|c| c.controller == p && c.definition.is_creature())
        .                                   .count() as u32,
        .                           ),
        .                           _ => None,
        .                       });
    8,464 ( 0.00%)              if let Some(n) = firebend_n
        .                           && n > 0
        .                       {
        .                           self.players[p].mana_pool.add(crate::mana::Color::Red, n);
        .                           self.players[p].firebending_kept_red =
        .                               self.players[p].firebending_kept_red.saturating_add(n);
        .                       }
        .                       // ControllerAttackedByOpponent (CR 508.1g listeners): permanents
        .                       // the defending player controls that fire "when a creature an
        .                       // opponent attacks me" — Coveted Jewel's control-flip. The
        .                       // attacking creature's controller is bound to the trigger's
        .                       // target slot ("that creature's controller").
   55,016 ( 0.00%)              if let Some(defender) = self.defender_for(atk.target)
   25,392 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::defender_for (4,232x)
    8,464 ( 0.00%)                  && defender != p
        .                       {
        .                           // The planeswalker-only sibling scope fires only when the
        .                           // attack is aimed at a planeswalker (Mila) — not the player.
        .                           let is_pw_attack =
   12,696 ( 0.00%)                      matches!(atk.target, crate::game::types::AttackTarget::Planeswalker(_));
    4,232 ( 0.00%)                  let listeners: Vec<(CardId, Effect)> = self
        .                               .battlefield
        .                               .iter()
  278,244 ( 0.01%)                      .filter(|c| c.controller == defender)
        .                               .flat_map(|c| {
        .                                   c.definition
        .                                       .triggered_abilities
        .                                       .iter()
        .                                       .filter(|t| {
        .                                           t.event.kind == EventKind::Attacks
      676 ( 0.00%)                                      && (t.event.scope
        .                                                   == crate::effect::EventScope::ControllerAttackedByOpponent
        .                                                   || (is_pw_attack
        .                                                       && t.event.scope
        .                                                           == crate::effect::EventScope::ControllerPlaneswalkerAttackedByOpponent))
        .                                       })
        .                                       .map(move |t| (c.id, t.effect.clone()))
        .                               })
        .                               .collect();
   16,928 ( 0.00%)                  for (src, effect) in listeners {
        .                               self.stack.push(
        .                                   TriggerPush::new(src, defender, effect)
        .                                       .target(Some(Target::Player(p)))
        .                                       // Bind the attacking creature as the trigger source so
        .                                       // "those creatures get -1/-0" (Sabotage Strategist) can
        .                                       // address it via `Selector::TriggerSource`.
        .                                       .trigger_source(Some(crate::game::effects::EntityRef::Permanent(id)))
        .                                       .build(),
        .                               );
        .                           }
        .                       }
   25,392 ( 0.00%)          }
  330,096 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::effect::abilities::TriggeredAbility>> (12,696x)
        .                   // CR 702.22e — a declared band lasts for the rest of combat regardless
        .                   // of later banding loss; drop members that never made it into combat.
   34,020 ( 0.00%)          self.attack_bands = bands
   71,820 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<alloc::vec::Vec<crabomination_base::card::CardId>>> (3,780x)
        .                       .into_iter()
    3,780 ( 0.00%)              .map(|b| {
        .                           b.into_iter().filter(|m| self.attacking.iter().any(|a| a.attacker == *m)).collect()
        .                       })
        .                       .filter(|b: &Vec<CardId>| b.len() > 1)
        .                       .collect();
        .           
        .                   // YourControl-scoped Attacks triggers (e.g. Battle Banner,
        .                   // Sparring Regimen) are NOT walked here — the unified
        .                   // `dispatch_triggers_for_events` path in `mod.rs` picks them up
        .                   // off the `AttackerDeclared` event(s) and routes them through the
        .                   // same trigger pipeline. Walking them here additionally would
        .                   // double-fire the trigger (one push from combat.rs + one from
        .                   // the dispatcher). The hardcoded `is_event_hardcoded` check only
        .                   // marks SelfSource Attacks as already handled.
        .           
   32,352 ( 0.00%)          for (source, effect, controller, filter) in triggers {
   20,328 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (264x)
        .                       // CR 603.2 + CR 506.5: evaluate the trigger's optional filter
        .                       // predicate at fire-time, which for Attacks is "after the
        .                       // entire declare attackers step batch is resolved".
    1,320 ( 0.00%)              if let Some(predicate) = filter {
        .                           let ctx = crate::game::effects::EffectContext {
        .                               controller,
        .                               source: Some(source),
        .                               targets: vec![],
        .                               // The attacker is a battlefield permanent — bind it as such
        .                               // so `ToughnessOf(TriggerSource)`-style gates (Young Hero
        .                               // Role) resolve its P/T (CR 506.5 post-batch view).
        .                               trigger_source: Some(crate::game::effects::EntityRef::Permanent(source)),
-- line 1368 ----------------------------------------
-- line 1389 ----------------------------------------
        .                           }
        .                       }
        .                       let auto_target =
        .                           self.auto_target_for_effect_avoiding(&effect, controller, Some(source));
        .                       // CR 115.1c — fill any additional "up to N target" slots (Lagorin's
        .                       // "put a +1/+1 counter on each of up to two target Mounts and/or
        .                       // Vehicles"), same as the self-source ETB path.
        .                       let additional =
    1,584 ( 0.00%)                  self.auto_extra_targets_for(&effect, source, controller, auto_target.clone());
   27,720 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::auto_extra_targets_for (264x)
        .                       // Isshin / Windcrag Siege (Mardu): a self-source attack trigger of a
        .                       // permanent you control fires an additional time per doubler.
      264 ( 0.00%)              let fires = 1
    1,584 ( 0.00%)                  + self.attack_trigger_extra_fires(controller)
   24,940 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::attack_trigger_extra_fires (264x)
    1,320 ( 0.00%)                  + crate::game::actions::ally_trigger_extra_fires(self, controller, source);
  677,274 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::ally_trigger_extra_fires (264x)
        .                       for _ in 0..fires {
      264 ( 0.00%)                  self.stack.push(
      792 ( 0.00%)                      TriggerPush::new(source, controller, effect.clone())
   19,272 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (264x)
        .                                   .target(auto_target.clone())
      792 ( 0.00%)                          .additional_targets(additional.clone())
    7,656 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (264x)
      792 ( 0.00%)                          .build(),
   80,524 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::TriggerPush::build (264x)
        .                           );
        .                       }
      792 ( 0.00%)          }
   13,200 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (264x)
        .           
        .                   // CR 508 — "Whenever you attack" fires once for the attacking player
        .                   // when one or more attackers are declared. Active-player permanents fire
        .                   // for the SelfSource/YourControl scopes (the event is player-wide);
        .                   // `AnyPlayer`-scoped triggers are observers ("whenever [a player]
        .                   // attacks", Argent Dais) and fire from every controller, once per
        .                   // combat, with the ability's controller as the fired-for player.
    3,780 ( 0.00%)          if any_attackers {
    4,736 ( 0.00%)              let ap = self.active_player_idx;
        .                       #[allow(clippy::type_complexity)]
    2,368 ( 0.00%)              let you_attack: Vec<(CardId, usize, Effect, Option<crate::effect::Predicate>)> = self
        .                           .battlefield
        .                           .iter()
        .                           .flat_map(|c| {
   48,462 ( 0.00%)                      let ctrl = c.controller;
        .                               c.definition
        .                                   .triggered_abilities
        .                                   .iter()
   48,462 ( 0.00%)                          .filter(move |t| {
        .                                       t.event.kind == EventKind::YouAttack
        .                                           && (ctrl == ap
        .                                               || t.event.scope == crate::effect::EventScope::AnyPlayer)
        .                                   })
        .                                   .map(move |t| (c.id, ctrl, t.effect.clone(), t.event.filter.clone()))
        .                           })
        .                           .collect();
   11,840 ( 0.00%)              for (src, ctrl, effect, filter) in you_attack {
        .                           // CR 603.2 — the "whenever you attack with …" rider is a
        .                           // trigger-time gate read off the finished attack declaration.
        .                           if let Some(predicate) = filter {
        .                               let ctx = crate::game::effects::EffectContext::for_ability(src, ctrl, None);
        .                               if !self.evaluate_predicate(&predicate, &ctx) {
        .                                   continue;
        .                               }
        .                           }
-- line 1446 ----------------------------------------
-- line 1456 ----------------------------------------
        .                                       .target(auto_target.clone())
        .                                       .build(),
        .                               );
        .                           }
        .                       }
        .                   }
        .           
        .                   self.give_priority_to_active();
   37,800 ( 0.00%)          Ok(events)
   37,800 ( 0.00%)      }
  590,208 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::layers::ComputedPermanent>> (3,780x)
        .           
        .               // ── Declare blockers ──────────────────────────────────────────────────────
        .           
   20,784 ( 0.00%)      pub fn declare_blockers(
        .                   &mut self,
        .                   assignments: Vec<(CardId, CardId)>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
    2,598 ( 0.00%)          if self.step != TurnStep::DeclareBlockers {
        .                       return Err(GameError::WrongStep { actual: self.step });
        .                   }
        .                   // Master Warcraft / Invasion Plans — only the chooser may submit.
        .                   // (Without one the engine keeps trusting the caller; blocker
        .                   // ownership is validated per assignment below.)
        .                   if let Some(chooser) = self.block_chooser()
        .                       && self.priority.player_with_priority != chooser
        .                   {
-- line 1481 ----------------------------------------
-- line 1485 ----------------------------------------
        .                   // One layer pass for the whole declaration. Every consumer below is a
        .                   // `find(id)` on the declared blockers and their attackers, the
        .                   // creatures already attacking, or the blockers already declared —
        .                   // except the five CR 509.1 requirement loops, which ask the whole
        .                   // board who *has* to block. Four of them are keyed on a keyword and
        .                   // the fifth on `must_block`, so when no permanent can carry one, the
        .                   // pass covers the participants instead of the ~23-permanent board.
        .                   // See `board_keyword_in_scope` for why `false` is authoritative.
   10,392 ( 0.00%)          let (computed, block_requirement) = self.with_frozen_layers(|g| {
    7,794 ( 0.00%)              let requirement = g.battlefield.iter().any(|c| c.must_block.is_some())
   10,392 ( 0.00%)                  || g.board_keyword_in_scope(&[
9,075,195 ( 0.29%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::board_keyword_in_scope (2,598x)
        .                               Keyword::MustBlock,
        .                               Keyword::MustAttackOrBlock,
        .                               Keyword::MustBeBlocked,
        .                               Keyword::AllMustBlock,
        .                               Keyword::CantBeBlockedUnlessAllBlock,
        .                           ]);
    5,196 ( 0.00%)              if requirement {
        .                           return (g.compute_battlefield(), requirement);
        .                       }
        .                       let mut ids: Vec<CardId> = Vec::new();
        .                       let mut want = |id: CardId| {
        .                           if !ids.contains(&id) {
        .                               ids.push(id);
        .                           }
        .                       };
    4,912 ( 0.00%)              for &(blocker, attacker) in &assignments {
        .                           want(blocker);
        .                           want(attacker);
        .                       }
        .                       g.attacking.iter().for_each(|a| want(a.attacker));
        .                       // `block_map`'s key order is a `HashMap`'s, but it only decides
        .                       // which permanents get computed; every reader looks up by id.
        .                       g.block_map.keys().for_each(|&b| want(b));
   15,588 ( 0.00%)              (g.compute_permanents(&ids), requirement)
6,415,990 ( 0.21%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::compute_permanents (2,598x)
        .                   });
        .                   // A whole-board consumer that slipped past the gate would silently
        .                   // read `None` / `&[]` for every permanent outside the subset and drop
        .                   // its restriction. Panic on it across the suite instead.
        .                   #[cfg(debug_assertions)]
        .                   let computed_ids: Vec<CardId> = computed.iter().map(|c| c.id).collect();
        .                   #[cfg(debug_assertions)]
        .                   let battlefield_ids: Vec<CardId> = self.battlefield.iter().map(|c| c.id).collect();
        .                   #[cfg(debug_assertions)]
        .                   let in_subset = |id: CardId| computed_ids.contains(&id) || !battlefield_ids.contains(&id);
    2,598 ( 0.00%)          let cp_of = |id: CardId| {
        .                       #[cfg(debug_assertions)]
        .                       debug_assert!(in_subset(id), "cp_of({id:?}) read outside the gated subset");
        .                       computed.iter().find(|c| c.id == id)
        .                   };
    2,598 ( 0.00%)          let kws_of = |id: CardId| -> &[Keyword] {
        .                       #[cfg(debug_assertions)]
        .                       debug_assert!(in_subset(id), "kws_of({id:?}) read outside the gated subset");
        .                       computed
        .                           .iter()
        .                           .find(|c| c.id == id)
        .                           .map(|c| c.keywords.as_slice())
        .                           .unwrap_or(&[])
        .                   };
-- line 1543 ----------------------------------------
-- line 1548 ----------------------------------------
        .                   // block more (`CanBlockAdditional` / `CanBlockAnyNumber`); count the
        .                   // merged set (already-declared blocks plus this batch) against the
        .                   // allowance, and reject a repeat of the same pair.
        .                   let mut batch_blocks: crate::fxhash::HashMap<CardId, Vec<CardId>> =
        .                       crate::fxhash::HashMap::default();
        .                   // CR 509.1b — Silent Arbiter: "No more than N creatures can block each
        .                   // combat." Count distinct blockers across already-declared blocks and
        .                   // this batch.
   15,588 ( 0.00%)          if let Some(cap) = self.combat_participation_cap(true)
  476,894 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap (2,598x)
        .                       && let Some(&(first, _)) = assignments.first()
        .                   {
        .                       let mut distinct: crate::fxhash::HashSet<CardId> =
        .                           self.block_map.keys().copied().collect();
        .                       distinct.extend(assignments.iter().map(|(b, _)| *b));
        .                       if distinct.len() > cap as usize {
        .                           return Err(GameError::CannotBlock(first));
        .                       }
        .                   }
    4,912 ( 0.00%)          for &(blocker_id, attacker_id) in &assignments {
        .                       let taken = batch_blocks.entry(blocker_id).or_default();
        .                       if taken.contains(&attacker_id) || self.blocks(blocker_id, attacker_id) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .                       taken.push(attacker_id);
    2,456 ( 0.00%)              let total = taken.len() + self.attackers_blocked_by(blocker_id).len();
    9,824 ( 0.00%)              if total > self.max_blocks_on(blocker_id, kws_of(blocker_id)) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .                       let atk = self
        .                           .attack_for(attacker_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(attacker_id))?;
        .                       let defender_idx = self
    7,368 ( 0.00%)                  .defender_for(atk.target)
        .                           .ok_or(GameError::CardNotOnBattlefield(attacker_id))?;
        .           
    9,824 ( 0.00%)              let blocker = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == blocker_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(blocker_id))?;
        .           
        .                       // CR 509.1a: any creature controlled by the defending player
        .                       // (or, in team formats, a teammate of the defending player)
        .                       // may block. In 1v1 / FFA `same_team(a, b)` collapses to
        .                       // `a == b`, so this preserves the historical behavior.
   14,736 ( 0.00%)              if !self.same_team(blocker.controller, defender_idx) {
    9,824 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (2,456x)
        .                           return Err(GameError::BlockerWrongDefender { blocker: blocker_id });
        .                       }
        .           
        .                       // CR 509.1a — blocker legality reads the computed view, so an
        .                       // animated land or crewed Vehicle (layer-4 Creature) can block
        .                       // while an uncrewed Vehicle can't.
    2,456 ( 0.00%)              let blocker_is_creature = cp_of(blocker_id)
        .                           .map(|c| c.card_types.contains(&crate::card::CardType::Creature))
        .                           .unwrap_or(false);
        .                       if !blocker_is_creature
    4,912 ( 0.00%)                  || (blocker.tapped && !self.tapped_creatures_can_block(blocker.controller))
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 701.35 — a detained permanent can't block.
    4,912 ( 0.00%)              if blocker.detained_by.is_some() {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // `Keyword::CantBlock` is enforced from the *computed* keyword
        .                       // set so transient grants (e.g. SOS Duel Tactics's "this
        .                       // creature can't block this turn", Postmortem Professor's
        .                       // static restriction) take effect immediately.
    2,456 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantBlock)
    2,436 ( 0.00%)                  || kws_of(blocker_id).contains(&Keyword::Decayed)
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 509.1b — "can't block [creature type]s" (Burden of Proof).
        .                       // `cp_of`, not `computed_permanent`: nothing above mutates a layer
        .                       // input, so the pass taken at the top of the function already holds
        .                       // this card's computed view and asking for it again is a whole-game
        .                       // gather per assignment.
    2,436 ( 0.00%)              if let Some(a) = cp_of(attacker_id)
    2,436 ( 0.00%)                  && kws_of(blocker_id).iter().any(|k| {
    6,600 ( 0.00%)                      matches!(k, Keyword::CantBlockCreatureType(t) if a.subtypes.creature_types.contains(t))
        .                           })
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 509.1b — "can't block [filter] creatures" (Gibbering Hyenas).
        .                       let blocker_snapshot = blocker.clone();
        .                       if self.blocker_matching_restriction_bars(
        .                           &blocker_snapshot,
    2,436 ( 0.00%)                  kws_of(blocker_id),
        .                           attacker_id,
        .                       ) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 509.1b — "can't block creatures with power equal to or
        .                       // greater than this creature's toughness" (Ironclaw Curse).
    2,436 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantBlockPowerAtLeastOwnToughness)
        .                           && let (Some(b), Some(a)) = (cp_of(blocker_id), cp_of(attacker_id))
        .                           && a.power >= b.toughness
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 509.1b — Monstrous Hound: more lands than the attacker.
    2,436 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantBlockUnlessMoreLandsThanAttacker)
        .                           && let Some(a) = self.battlefield_find(attacker_id)
        .                           && self.player_tally(blocker.controller, crate::card::PlayerTally::LandsControlled)
        .                               <= self.player_tally(a.controller, crate::card::PlayerTally::LandsControlled)
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 509.1b — a blanket "can't block this turn" (Concussive
        .                       // Bolt's metalcraft rider) bars every block.
    2,436 ( 0.00%)              if self.cant_block_this_turn.contains(&blocker_id) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .                       // Per-pair "can't block this creature this turn" (Kozilek's
        .                       // Pathfinder): the blocker is barred only from this attacker.
        .                       if self.cant_block_pairs.contains(&(blocker_id, attacker_id)) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .           
        .                       // "Can't block unless it has an even number of counters on it"
        .                       // (Sab-Sunen). Zero is even; reject an odd total counter count.
    2,436 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantAttackOrBlockUnlessEvenCounters)
        .                           && blocker.counters.values().sum::<u32>() % 2 != 0
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // Void Winnower — a creature with an even mana value can't block
        .                       // while an opponent has the static (zero is even).
    9,744 ( 0.00%)              if blocker.definition.cost.cmc() % 2 == 0
   75,828 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (2,436x)
      440 ( 0.00%)                  && self.battlefield.iter().any(|c| {
        .                               c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::OpponentsCantBlockWithEvenMv
        .                                   )
        .                               }) && !self.same_team(c.controller, blocker.controller)
        .                           })
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // "Can't block unless you control N+ [filter]" (Topiary Stomper).
        .                       // Attack-only gates (Lambholt Pacifist) don't restrict blocking.
   14,546 ( 0.00%)              if let Some((req, min, excl)) = kws_of(blocker_id).iter().find_map(|kw| match kw {
        .                           Keyword::CantAttackOrBlockUnlessYouControlCount {
        .                               filter, min, attack_only: false, exclude_self, ..
        .                           } => Some((filter.clone(), *min, *exclude_self)),
        .                           _ => None,
        .                       }) {
        .                           let owner = blocker.controller;
        .                           let n = self
        .                               .battlefield
-- line 1710 ----------------------------------------
-- line 1717 ----------------------------------------
        .                               .count();
        .                           if (n as u32) < min {
        .                               return Err(GameError::CannotBlock(blocker_id));
        .                           }
        .                       }
        .           
        .                       let attacker = self
        .                           .battlefield_find(attacker_id)
    2,436 ( 0.00%)                  .ok_or(GameError::CardNotOnBattlefield(attacker_id))?;
        .           
        .                       // CR 509.1b — Branded Brawlers: your own untapped land locks the
        .                       // block.
    2,436 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantBlockIfYouHaveUntappedLand)
        .                           && self.battlefield.iter().any(|c| {
        .                               c.controller == blocker.controller && c.definition.is_land() && !c.tapped
        .                           })
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .                       // CR 509.1b — Mogg Toady: strictly more creatures than the
        .                       // attacker's controller.
    2,436 ( 0.00%)              if kws_of(blocker_id).contains(&Keyword::CantBlockUnlessMoreCreaturesThanAttacker)
        .                           && self.creature_count(blocker.controller)
        .                               <= self.creature_count(attacker.controller)
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
    4,872 ( 0.00%)              let blocker_cp = cp_of(blocker_id).ok_or(GameError::CannotBlock(blocker_id))?;
        .                       let atk_colors = cp_of(attacker_id).map(|c| c.colors).unwrap_or_default();
    2,436 ( 0.00%)              let atk_power = cp_of(attacker_id).map(|c| c.power).unwrap_or_else(|| attacker.power());
   17,052 ( 0.00%)              if !super::can_block_attacker_computed(
1,252,956 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::can_block_attacker_computed (2,436x)
        .                           blocker,
        .                           blocker_cp,
    2,436 ( 0.00%)                  kws_of(attacker_id),
        .                           atk_colors,
        .                           atk_power,
        .                       ) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .                       if self.block_barred_by_protection_filter(
    2,436 ( 0.00%)                  kws_of(attacker_id),
    2,436 ( 0.00%)                  attacker.controller,
        .                           blocker_id,
        .                       ) {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // CR 701.54c (level 1+) — a Ring-bearer can't be blocked by
        .                       // creatures with greater power.
    2,436 ( 0.00%)              if self.effective_ring_bearer(attacker.controller) == Some(attacker_id)
        .                           && self.players[attacker.controller].ring_temptations >= 1
        .                           && blocker_cp.power > atk_power
        .                       {
        .                           return Err(GameError::CannotBlock(blocker_id));
        .                       }
        .           
        .                       // Landwalk (CR 702.15): the attacker can't be blocked while the
        .                       // defending player controls a land of the named type. Needs
        .                       // game state (the defender's lands), so it lives here rather
        .                       // than in the pure two-creature `can_block_attacker_computed`.
    2,436 ( 0.00%)              for kw in kws_of(attacker_id) {
   19,332 ( 0.00%)                  if let Keyword::Landwalk(lt) = kw
        .                               && self.defender_controls_land_type(defender_idx, lt)
        .                               // CR 509.1b — Great Wall / Deadfall / Quagmire / Crevasse
        .                               // / Gosta Dirk / Lord Magnus blank one landwalk flavor.
        .                               && !self.landwalk_ignored(*lt)
        .                           {
        .                               return Err(GameError::CannotBlock(blocker_id));
        .                           }
        .                           // CR 702.14c — filtered landwalk (artifact/nonbasic/snow).
-- line 1787 ----------------------------------------
-- line 1820 ----------------------------------------
        .                               return Err(GameError::CannotBlock(blocker_id));
        .                           }
        .                       }
        .           
        .                       // CR 509.1b — "can't be blocked if you've cast N+ spells this turn"
        .                       // (Illvoi Infiltrator). Reads the attacker's controller's spell
        .                       // count this turn, so it lives here rather than in the pure
        .                       // two-creature check.
    2,436 ( 0.00%)              if let Some(atk_ctl) = self.battlefield_find(attacker_id).map(|c| c.controller) {
    2,436 ( 0.00%)                  for kw in kws_of(attacker_id) {
    8,592 ( 0.00%)                      if let Keyword::CantBeBlockedIfControllerCastSpells(n) = kw
        .                                   && self.players[atk_ctl].spells_cast_this_turn >= *n
        .                               {
        .                                   return Err(GameError::CannotBlock(blocker_id));
        .                               }
        .                               // CR 509.1b — "can't be blocked as long as defending player
        .                               // controls a [filter]" (Neurok Spy).
        .                               if let Keyword::CantBeBlockedIfDefenderControls(f) = kw {
        .                                   let def = self
-- line 1838 ----------------------------------------
-- line 1890 ----------------------------------------
        .           
        .                   // CR 509.1c — "can't block alone". A creature blocks alone if it's the
        .                   // only creature blocking this combat; count the merged block set
        .                   // (this batch plus any earlier-declared blockers).
        .                   {
        .                       let mut all_blockers: crate::fxhash::HashSet<CardId> =
        .                           self.block_map.keys().copied().collect();
        .                       all_blockers.extend(assignments.iter().map(|(b, _)| *b));
    5,156 ( 0.00%)              if all_blockers.len() == 1 {
      884 ( 0.00%)                  for &(blocker_id, _) in &assignments {
      884 ( 0.00%)                      if kws_of(blocker_id).contains(&Keyword::CantAttackOrBlockAlone) {
        .                                   return Err(GameError::CannotBlock(blocker_id));
        .                               }
        .                           }
        .                       }
        .                       // CR 509.1b — Okk's blocking half: needs a strictly bigger partner
        .                       // among the whole combat's blockers. Nothing above mutates the
        .                       // board, so this reads the pass taken at the top.
        .                       let computed_pow = &computed;
        .                       let power_of = |id: CardId| {
        .                           computed_pow.iter().find(|c| c.id == id).map(|c| c.power).unwrap_or(0)
        .                       };
    2,424 ( 0.00%)              for &(blocker_id, _) in &assignments {
    2,424 ( 0.00%)                  if kws_of(blocker_id).contains(&Keyword::CantBlockUnlessGreaterPowerBlocks) {
        .                               let mine = power_of(blocker_id);
        .                               if !all_blockers.iter().any(|&o| o != blocker_id && power_of(o) > mine) {
        .                                   return Err(GameError::CannotBlock(blocker_id));
        .                               }
        .                           }
        .                       }
        .                   }
        .           
-- line 1921 ----------------------------------------
-- line 1923 ----------------------------------------
        .                   // `BlockTaxToController` amount (an `only_while_attacking` source counts
        .                   // only while it's attacking this combat) and pay {tax} per declared
        .                   // blocker from that blocker's controller's pool, auto-tapping for any
        .                   // shortfall. Reject the whole declaration if a player can't cover it.
        .                   // The tax is summed per blocker, since a source may narrow itself to
        .                   // some of them (Heat Wave taxes only nonblue blockers) and may charge
        .                   // life rather than mana.
        .                   let block_tax_for = |st: &Self, blocker: CardId| -> (u32, u32) {
    2,424 ( 0.00%)              let (mut mana, mut life) = (st.block_tax_this_turn, 0u32);
        .                       for c in &st.battlefield {
        .                           for sa in &c.definition.static_abilities {
        .                               if let crate::effect::StaticEffect::BlockTaxToController {
        .                                   amount,
        .                                   only_while_attacking,
        .                                   filter,
        .                                   life: as_life,
        .                               } = &sa.effect
-- line 1939 ----------------------------------------
-- line 1962 ----------------------------------------
        .                           }
        .                       }
        .                       (mana, life)
        .                   };
        .                   // The spend is deferred to after every block-legality check so a
        .                   // rejected declaration never costs mana (CR 601.2h-style atomicity).
        .                   let mut block_tax_by_controller: crate::fxhash::HashMap<usize, (u32, u32)> =
        .                       crate::fxhash::HashMap::default();
    2,424 ( 0.00%)          for &(blocker_id, _) in &assignments {
        .                       let (mana, life) = block_tax_for(self, blocker_id);
    9,696 ( 0.00%)              if mana == 0 && life == 0 {
        .                           continue;
        .                       }
        .                       if let Some(b) = self.battlefield_find(blocker_id) {
        .                           let e = block_tax_by_controller.entry(b.controller).or_insert((0, 0));
        .                           e.0 += mana;
        .                           e.1 += life;
        .                       }
        .                   }
        .           
        .                   // Menace: attackers with Menace must be blocked by 2+ creatures or
        .                   // not at all (CR 702.110b). Counts the merged block set (existing
        .                   // blocks plus this batch) so incremental multi-defender submissions
        .                   // compose, same as the CantBeBlockedExceptByN check below.
        .                   for atk in &self.attacking {
    9,384 ( 0.00%)              let has_menace = kws_of(atk.attacker).contains(&Keyword::Menace);
        .                       if has_menace {
        .                           let blocker_count = assignments
        .                               .iter()
        .                               .filter(|(_, aid)| *aid == atk.attacker)
        .                               .count()
        .                               + self.blocker_count_of(atk.attacker);
        .                           if blocker_count == 1 {
        .                               return Err(GameError::MenaceRequiresTwoBlockers(atk.attacker));
-- line 1995 ----------------------------------------
-- line 1997 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // CR 509.1b — "can't block unless its controller pays {N}"
        .                   // (Oppressive Rays). Charged once per declared blocker, paid from
        .                   // that blocker's controller's pool with auto-tap for the shortfall.
        .                   {
        .                       let mut owed: crate::fxhash::HashMap<usize, u32> = Default::default();
    2,424 ( 0.00%)              for &(blocker_id, _) in &assignments {
    2,424 ( 0.00%)                  let Some(seat) = self.battlefield_find(blocker_id).map(|c| c.controller) else {
        .                               continue;
        .                           };
    2,424 ( 0.00%)                  *owed.entry(seat).or_default() +=
    7,272 ( 0.00%)                      self.attack_block_keyword_tax(blocker_id, kws_of(blocker_id), false);
  108,404 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::attack_block_keyword_tax (2,424x)
        .                       }
    4,566 ( 0.00%)              for (seat, amount) in owed {
    9,132 ( 0.00%)                  let tax = crate::mana::cost(&[crate::mana::generic(amount)]);
  181,261 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::cost (1,522x)
    1,522 ( 0.00%)                  if self.try_pay_with_auto_tap(seat, &tax).is_err() {
        .                               return Err(GameError::CannotBlock(assignments[0].0));
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 509.1b — the blocking half of Hollow Warrior's tap cost. Helpers
        .                   // may be neither an attacker nor one of the declared blockers.
        .                   {
        .                       let declared: Vec<CardId> = assignments
        .                           .iter()
        .                           .map(|(b, _)| *b)
        .                           .chain(self.attacking.iter().map(|a| a.attacker))
        .                           .collect();
        .                       let mut tapped: Vec<CardId> = Vec::new();
    2,424 ( 0.00%)              for &(blocker_id, _) in &assignments {
    4,848 ( 0.00%)                  let Some(seat) = self.battlefield_find(blocker_id).map(|c| c.controller) else {
        .                               continue;
        .                           };
   14,544 ( 0.00%)                  for f in tap_another_filters(kws_of(blocker_id)) {
        .                               match self.find_tap_helper(seat, &f, blocker_id, &declared, &tapped) {
        .                                   Some(id) => tapped.push(id),
        .                                   None => return Err(GameError::CannotBlock(blocker_id)),
        .                               }
        .                           }
        .                       }
    4,566 ( 0.00%)              for id in tapped {
        .                           if let Some(c) = self.battlefield_find_mut(id) {
        .                               c.tapped = true;
        .                           }
        .                       }
    5,156 ( 0.00%)          }
        .           
        .                   // "Can't be blocked except by N or more creatures" (Pathrazer of
        .                   // Ulamog). Generalized Menace: 0 or >= N blockers, never 1..N-1.
        .                   for atk in &self.attacking {
   14,076 ( 0.00%)              for kw in kws_of(atk.attacker) {
   10,932 ( 0.00%)                  if let Keyword::CantBeBlockedExceptByN(n) = kw {
        .                               let blocker_count = assignments
        .                                   .iter()
        .                                   .filter(|(_, aid)| *aid == atk.attacker)
        .                                   .count()
        .                                   + self.blocker_count_of(atk.attacker);
        .                               if blocker_count > 0 && (blocker_count as u32) < *n {
        .                                   return Err(GameError::MenaceRequiresTwoBlockers(atk.attacker));
        .                               }
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 509.1g — "can't be blocked by more than one creature" (Charging
        .                   // Rhino). At most one blocker may be assigned (the inverse of Menace).
        .                   for atk in &self.attacking {
   14,076 ( 0.00%)              if kws_of(atk.attacker).contains(&Keyword::CantBeBlockedByMoreThanOne) {
        .                           let blocker_count = assignments
        .                               .iter()
        .                               .filter(|(_, aid)| *aid == atk.attacker)
        .                               .count()
        .                               + self.blocker_count_of(atk.attacker);
        .                           if blocker_count > 1 {
        .                               return Err(GameError::CannotBeBlockedByMoreThanOne(atk.attacker));
        .                           }
-- line 2075 ----------------------------------------
-- line 2079 ----------------------------------------
        .                   // CR 509.1c — "must be blocked if able" (Lure / Academic Dispute).
        .                   // If such an attacker is left unblocked while the defender controls
        .                   // an idle creature that could legally block it, reject the
        .                   // declaration. Considers the merged block set (already-declared
        .                   // blocks plus this batch) so independent multiplayer submissions
        .                   // compose. Single-requirement model; full CR maximization across
        .                   // multiple simultaneous requirements is approximated.
        .                   for atk in &self.attacking {
   14,076 ( 0.00%)              if !kws_of(atk.attacker).contains(&Keyword::MustBeBlocked) {
        .                           continue;
        .                       }
        .                       let already = self.blocker_count_of(atk.attacker) > 0;
        .                       let in_batch = assignments.iter().any(|(_, aid)| *aid == atk.attacker);
        .                       if already || in_batch {
        .                           continue;
        .                       }
        .                       let Some(defender_idx) = self.defender_for(atk.target) else { continue };
-- line 2095 ----------------------------------------
-- line 2116 ----------------------------------------
        .                           return Err(GameError::MustBeBlockedIfAble(atk.attacker));
        .                       }
        .                   }
        .           
        .                   // CR 509.1c — true Lure ("all creatures able to block this do so").
        .                   // Every idle defender creature that *can* legally block such an
        .                   // attacker must be assigned to it in the merged block set.
        .                   for atk in &self.attacking {
   14,076 ( 0.00%)              if !kws_of(atk.attacker).contains(&Keyword::AllMustBlock) {
        .                           continue;
        .                       }
        .                       let Some(defender_idx) = self.defender_for(atk.target) else { continue };
        .                       let attacker = match self.battlefield_find(atk.attacker) {
        .                           Some(a) => a,
        .                           None => continue,
        .                       };
        .                       let atk_colors = cp_of(atk.attacker).map(|c| c.colors).unwrap_or_default();
-- line 2132 ----------------------------------------
-- line 2150 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // CR 702.39 — Provoke: a creature provoked this combat (`must_block`
        .                   // set to an attacker still in this combat) must be assigned to block
        .                   // that attacker if it's able to. It was untapped by the provoke
        .                   // resolution, so "able" reduces to the normal can-block checks.
        .                   for b in &self.battlefield {
  104,508 ( 0.00%)              let Some(required) = b.must_block else { continue };
        .                       // The provoker must still be attacking for the requirement to bind.
        .                       if !self.attacking.iter().any(|a| a.attacker == required) { continue; }
        .                       let b_is_creature = cp_of(b.id)
        .                           .is_some_and(|c| c.card_types.contains(&crate::card::CardType::Creature));
        .                       if !b_is_creature
        .                           || (b.tapped && !self.tapped_creatures_can_block(b.controller))
        .                           || kws_of(b.id).contains(&Keyword::CantBlock)
        .                       {
-- line 2166 ----------------------------------------
-- line 2183 ----------------------------------------
        .                           return Err(GameError::MustBeBlockedIfAble(required));
        .                       }
        .                   }
        .           
        .                   // CR 509.1b — Tromokratis: once such an attacker is blocked at all,
        .                   // *every* untapped defending creature able to block it must also be
        .                   // assigned to it. Checked against the merged block set.
        .                   for atk in &self.attacking {
    9,384 ( 0.00%)              if !kws_of(atk.attacker).contains(&Keyword::CantBeBlockedUnlessAllBlock) {
        .                           continue;
        .                       }
        .                       let blocked = self.blocker_count_of(atk.attacker) > 0
        .                           || assignments.iter().any(|(_, aid)| *aid == atk.attacker);
        .                       if !blocked {
        .                           continue;
        .                       }
        .                       let Some(defender_idx) = self.defender_for(atk.target) else { continue };
-- line 2199 ----------------------------------------
-- line 2221 ----------------------------------------
        .                   }
        .           
        .                   // CR 509.1c — "blocks each combat if able" (`MustBlock`). A creature
        .                   // carrying the keyword that can legally block at least one declared
        .                   // attacker must be assigned to block one of them. Unlike the four
        .                   // loops above, this one asks the computed view *before* the keyword
        .                   // that decides it, so it takes the gate directly.
        .                   let must_block_scan: &[crate::card::CardInstance] =
    5,156 ( 0.00%)              if block_requirement { &self.battlefield } else { &[] };
        .                   for b in must_block_scan {
        .                       let b_is_creature = cp_of(b.id)
        .                           .is_some_and(|c| c.card_types.contains(&crate::card::CardType::Creature));
        .                       if !(kws_of(b.id).contains(&Keyword::MustBlock)
        .                           || kws_of(b.id).contains(&Keyword::MustAttackOrBlock))
        .                           || !b_is_creature
        .                           || b.tapped
        .                           || kws_of(b.id).contains(&Keyword::CantBlock)
-- line 2237 ----------------------------------------
-- line 2264 ----------------------------------------
        .                       if could_block {
        .                           return Err(GameError::MustBeBlockedIfAble(b.id));
        .                       }
        .                   }
        .           
        .                   // All validation passed — pay the block tax (CR 509.1d) from each
        .                   // blocking player's pool, auto-tapping for any shortfall. Restore
        .                   // every payment if any player can't cover theirs.
    7,734 ( 0.00%)          if !block_tax_by_controller.is_empty() {
        .                       let mut snapshots = Vec::new();
        .                       let mut payers: Vec<(usize, (u32, u32))> =
        .                           block_tax_by_controller.into_iter().collect();
        .                       payers.sort_by_key(|(p, _)| *p);
        .                       let mut life_paid: Vec<(usize, u32)> = Vec::new();
        .                       for (player, (mana, life)) in payers {
        .                           let snap = self.snapshot_payment_state(player);
        .                           // CR 119.4 — a life cost is unpayable below the amount owed.
-- line 2280 ----------------------------------------
-- line 2304 ----------------------------------------
        .                   // Flanking (CR 702.25), Bushido (CR 702.45), Rampage (CR 702.23).
        .                   // Snapshot the +/-N deltas (same value on power and toughness)
        .                   // before mutating so the borrow of `assignments` stays clean.
        .                   // Only the assignments' own blockers and attackers are read below, so
        .                   // compute those instead of the whole board: the freeze scope pays one
        .                   // gather and one layer pass per participant, against ~23 passes for a
        .                   // bench board.
        .                   let mut ids: Vec<CardId> = Vec::new();
    4,848 ( 0.00%)          for &(b, a) in &assignments {
        .                       if !ids.contains(&b) {
        .                           ids.push(b);
        .                       }
        .                       if !ids.contains(&a) {
        .                           ids.push(a);
        .                       }
        .                   }
    2,578 ( 0.00%)          let computed: Vec<(CardId, Vec<Keyword>)> = self.with_frozen_layers(|g| {
10,446,149 ( 0.34%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (2,366x)
  462,471 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers (212x)
        .                       ids.iter()
        .                           .map(|&id| {
        .                               let kws =
   11,902 ( 0.00%)                          g.computed_permanent(id).map(|c| c.keywords.to_vec()).unwrap_or_default();
8,447,481 ( 0.27%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,462x)
        .                               (id, kws)
        .                           })
        .                           .collect()
        .                   });
        .                   let kws_for = |id: CardId| -> Vec<Keyword> {
   13,772 ( 0.00%)              computed.iter().find(|(i, _)| *i == id).map(|(_, k)| k.clone()).unwrap_or_default()
  828,586 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (6,886x)
        .                   };
        .                   let sum_n = |kws: &[Keyword], pick: fn(&Keyword) -> Option<i32>| -> i32 {
        .                       kws.iter().filter_map(pick).sum()
        .                   };
        .                   let mut pt_deltas: Vec<(CardId, i32)> = vec![];
        .                   let mut blocked: crate::fxhash::HashMap<CardId, usize> = crate::fxhash::HashMap::default();
    7,272 ( 0.00%)          for &(b, a) in &assignments {
    2,424 ( 0.00%)              *blocked.entry(a).or_insert(0) += 1;
        .                       let bk = kws_for(b);
        .                       let ak = kws_for(a);
        .                       // Flanking: nonflanking blocker shrinks once per flanking instance.
    6,402 ( 0.00%)              let flank = ak.iter().filter(|k| **k == Keyword::Flanking).count() as i32;
   23,474 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::Keyword as core::cmp::PartialEq>::eq (2,134x)
    2,968 ( 0.00%)              if flank > 0 && !bk.contains(&Keyword::Flanking) {
        .                           pt_deltas.push((b, -flank));
        .                       }
        .                       // Bushido on the blocker (it blocks).
    5,178 ( 0.00%)              let bn = sum_n(&bk, |k| if let Keyword::Bushido(x) = k { Some(*x as i32) } else { None });
    3,508 ( 0.00%)              if bn > 0 { pt_deltas.push((b, bn)); }
        .                   }
    4,566 ( 0.00%)          for (a, count) in blocked {
        .                       let ak = kws_for(a);
        .                       // Bushido on the attacker (it becomes blocked — once).
    2,704 ( 0.00%)              let bn = sum_n(&ak, |k| if let Keyword::Bushido(x) = k { Some(*x as i32) } else { None });
    2,448 ( 0.00%)              if bn > 0 { pt_deltas.push((a, bn)); }
        .                       // Rampage: +N for each blocker beyond the first.
    2,704 ( 0.00%)              let rn = sum_n(&ak, |k| if let Keyword::Rampage(x) = k { Some(*x as i32) } else { None });
        .                       let extra = count.saturating_sub(1) as i32;
    1,740 ( 0.00%)              if rn > 0 && extra > 0 { pt_deltas.push((a, rn * extra)); }
        .                   }
        .           
        .                   // All valid — apply (merge into existing block_map so multiple
        .                   // defenders can submit independently in multiplayer).
    5,156 ( 0.00%)          self.blockers_declared = true;
        .                   // CR 702.22h — blocking one member of a band blocks every other member
        .                   // by that same blocker. Expanded before the apply loop so the added
        .                   // pairs go through the same bookkeeping and events.
        .                   let assignments: Vec<(CardId, CardId)> = {
   10,312 ( 0.00%)              let mut out = assignments;
        .                       for i in 0..out.len() {
    9,696 ( 0.00%)                  let (blocker, attacker) = out[i];
    2,424 ( 0.00%)                  let banded: Vec<CardId> = self
        .                               .attack_bands
        .                               .iter()
        .                               .find(|b| b.contains(&attacker))
        .                               .map(|b| b.iter().copied().filter(|m| *m != attacker).collect())
        .                               .unwrap_or_default();
        .                           for m in banded {
        .                               if !out.contains(&(blocker, m)) && !self.blocks(blocker, m) {
        .                                   out.push((blocker, m));
        .                               }
        .                           }
        .                       }
   10,778 ( 0.00%)              out
        .                   };
        .                   let mut events = vec![];
        .                   for (blocker_id, attacker_id) in assignments {
    7,272 ( 0.00%)              self.add_block(blocker_id, attacker_id);
  862,193 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::add_block (2,424x)
        .                       if let Some(b) = self.battlefield_find_mut(blocker_id) {
    2,424 ( 0.00%)                  b.blocked_this_turn = true;
        .                           if !b.blocked_attackers_this_turn.contains(&attacker_id) {
    7,272 ( 0.00%)                      b.blocked_attackers_this_turn.push(attacker_id);
        .                           }
        .                       }
    2,424 ( 0.00%)              if !self.blocks_declared_this_turn.contains(&(blocker_id, attacker_id)) {
    5,468 ( 0.00%)                  self.blocks_declared_this_turn.push((blocker_id, attacker_id));
        .                       }
        .                       // CR 510.1c — once blocked, the attacker stays blocked for this
        .                       // combat even if every blocker later leaves combat.
    2,424 ( 0.00%)              if !self.blocked_attackers.contains(&attacker_id) {
    5,082 ( 0.00%)                  self.blocked_attackers.push(attacker_id);
        .                       }
    7,272 ( 0.00%)              events.push(GameEvent::BlockerDeclared {
        .                           blocker: blocker_id,
        .                           attacker: attacker_id,
        .                       });
        .                       // Noxious Assault's turn rider: each declared block poisons the
        .                       // blocker's controller.
    7,272 ( 0.00%)              if self.block_poison_this_turn > 0 {
        .                           let ctrl = self
        .                               .battlefield_find(blocker_id)
        .                               .map(|c| c.controller);
        .                           if let Some(ctrl) = ctrl {
        .                               let n = self.block_poison_this_turn;
        .                               self.add_poison(ctrl, n, &mut events);
        .                           }
        .                       }
        .                   }
   10,312 ( 0.00%)          for (id, d) in pt_deltas {
        .                       if let Some(c) = self.battlefield_find_mut(id) {
        .                           c.power_bonus += d;
        .                           c.toughness_bonus += d;
        .                       }
        .                   }
        .                   // CR 509.3g — emit `AttackerWentUnblocked` for each attacker
        .                   // with no blockers assigned. Trigger source is the unblocked
        .                   // attacker; consumers can read it via `Selector::TriggerSource`.
        .                   let mut frenzy_deltas: Vec<(CardId, i32)> = Vec::new();
        .                   // One gather for the whole sweep. `computed_permanent` is `&self` and
        .                   // this loop runs at depth 0, so unfrozen it rebuilds the full effect
        .                   // set once per unblocked attacker to read one keyword.
    2,578 ( 0.00%)          self.with_frozen_layers(|g| {
7,752,756 ( 0.25%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (2,366x)
  785,088 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers (212x)
        .                       for atk in &g.attacking {
    9,384 ( 0.00%)                  let blocked = g.blocker_count_of(atk.attacker) > 0;
    4,692 ( 0.00%)                  if !blocked {
    5,308 ( 0.00%)                      events.push(GameEvent::AttackerWentUnblocked { attacker: atk.attacker });
        .                               // CR 702.35 — Frenzy N: an unblocked attacker gets +N/+0.
        .                               // Read computed keywords so statically-granted Frenzy
        .                               // (Frenzy Sliver) counts too.
   15,924 ( 0.00%)                      if let Some(cp) = g.computed_permanent(atk.attacker) {
  621,069 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (274x)
        .                                   let fn_: i32 = cp
        .                                       .keywords
        .                                       .iter()
        .                                       .filter_map(
    5,934 ( 0.00%)                                  |k| if let Keyword::Frenzy(x) = k { Some(*x as i32) } else { None },
        .                                       )
        .                                       .sum();
    3,496 ( 0.00%)                          if fn_ > 0 {
        .                                       frenzy_deltas.push((atk.attacker, fn_));
        .                                   }
        .                               }
        .                           }
        .                       }
        .                   });
    7,734 ( 0.00%)          for (id, d) in frenzy_deltas {
        .                       if let Some(c) = self.battlefield_find_mut(id) {
        .                           c.power_bonus += d;
        .                       }
        .                   }
        .                   self.give_priority_to_active();
   20,624 ( 0.00%)          Ok(events)
   35,196 ( 0.00%)      }
  816,517 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::layers::ComputedPermanent>> (2,578x)
  681,014 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<(crabomination_base::card::CardId,alloc::vec::Vec<crabomination_base::card::Keyword>)>> (2,578x)
        .           
        .               // ── Combat resolution ─────────────────────────────────────────────────────
        .           
        .               pub(crate) fn has_first_strikers(&self) -> bool {
        .                   // Asked once per combat-damage step about the 2-6 combat
        .                   // participants; computing the whole board for it was the cost.
    5,184 ( 0.00%)          if self.attacking.is_empty() && self.block_map.is_empty() {
        .                       return false;
        .                   }
        .                   self.with_frozen_layers(|g| {
   35,300 ( 0.00%)              let strikes_first = |id: CardId| {
    7,060 ( 0.00%)                  g.computed_permanent(id).is_some_and(|c| {
13,236,093 ( 0.42%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (7,060x)
        .                               c.keywords.contains(&Keyword::FirstStrike)
        .                                   || c.keywords.contains(&Keyword::DoubleStrike)
        .                           })
   49,420 ( 0.00%)              };
    9,392 ( 0.00%)              g.attacking.iter().any(|atk| strikes_first(atk.attacker))
11,311,819 ( 0.36%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::has_first_strikers::{{closure}}::{{closure}} (4,696x)
    2,364 ( 0.00%)                  || g.block_map.keys().any(|&id| strikes_first(id))
2,658,416 ( 0.09%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::has_first_strikers::{{closure}}::{{closure}} (2,364x)
        .                   })
        .               }
        .           
        .               /// The computed views [`resolve_combat_damage_with_filter`] reads: every
        .               /// attacker and every declared blocker, from one gather.
        .               ///
        .               /// Everything under that resolver looks its permanents up by id — the
        .               /// attacker infos, the banding assigner, the lethal table, the blocker
-- line 2487 ----------------------------------------
-- line 2491 ----------------------------------------
        .               /// carries, so the whole board is computed only when such an attacker is
        .               /// actually in combat. **The gate is checked, not assumed**:
        .               /// `butcher_orgg_divides_damage_among_defenders` assigns damage to a
        .               /// creature that never blocked, so it fails if this falls back to the
        .               /// subset.
        .               ///
        .               /// [`free_division_targets`]: Self::free_division_targets
        .               /// [`resolve_combat_damage_with_filter`]: Self::resolve_combat_damage_with_filter
   26,192 ( 0.00%)      fn combat_damage_computed(&self) -> Vec<ComputedPermanent> {
        .                   let mut ids: Vec<CardId> = Vec::new();
        .                   for atk in &self.attacking {
        .                       if !ids.contains(&atk.attacker) {
        .                           ids.push(atk.attacker);
        .                       }
        .                   }
        .                   // `block_map`'s key order is a `HashMap`'s, but it only decides which
        .                   // permanents get computed; every reader looks up by id, and the one
        .                   // that collects (`free_division_targets`) sorts.
    3,974 ( 0.00%)          for &bid in self.block_map.keys() {
        .                       if !ids.contains(&bid) {
        .                           ids.push(bid);
        .                       }
        .                   }
    6,548 ( 0.00%)          let subset = self.compute_permanents(&ids);
16,785,300 ( 0.54%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::compute_permanents (3,274x)
        .                   if subset.iter().any(|c| c.keywords.contains(&Keyword::DividesCombatDamageAmongDefenders)) {
        .                       return self.compute_battlefield();
        .                   }
   16,370 ( 0.00%)          subset
   26,192 ( 0.00%)      }
        .           
      780 ( 0.00%)      pub fn resolve_first_strike_damage(&mut self) -> Result<Vec<GameEvent>, GameError> {
       78 ( 0.00%)          let computed = self.combat_damage_computed();
  715,233 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_damage_computed (78x)
        .                   // CR 510.4: in the first-strike combat damage step, only creatures
        .                   // with first strike or double strike deal combat damage. The same
        .                   // gate applies to attackers (who deals?) and blockers (who strikes
        .                   // back at the attacker?).
        .                   let fs_or_ds = |kws: &[Keyword]| {
        .                       kws.contains(&Keyword::FirstStrike) || kws.contains(&Keyword::DoubleStrike)
        .                   };
      234 ( 0.00%)          let mut events = self.resolve_combat_damage_with_filter(&computed, fs_or_ds, fs_or_ds)?;
        .                   // Suspended on a `wants_ui` player's combat-damage choice — no damage
        .                   // has been dealt yet; `submit_decision` re-enters this step.
       78 ( 0.00%)          if self.pending_decision.is_some() {
        .                       return Ok(events);
        .                   }
       78 ( 0.00%)          let mut sba = self.check_state_based_actions();
6,149,596 ( 0.20%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (78x)
        .                   events.append(&mut sba);
       78 ( 0.00%)          events.push(GameEvent::FirstStrikeDamageResolved);
      312 ( 0.00%)          Ok(events)
      702 ( 0.00%)      }
        .           
   31,960 ( 0.00%)      pub fn resolve_combat(&mut self) -> Result<Vec<GameEvent>, GameError> {
    3,196 ( 0.00%)          let computed = self.combat_damage_computed();
18,219,750 ( 0.58%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_damage_computed (3,196x)
        .                   // CR 510.5: in the regular combat damage step, every attacking and
        .                   // blocking creature that didn't deal damage in the first-strike step
        .                   // deals damage now — i.e. anyone without first strike, plus double
        .                   // strikers (who strike in both steps).
        .                   let regular_or_ds = |kws: &[Keyword]| {
        .                       !kws.contains(&Keyword::FirstStrike) || kws.contains(&Keyword::DoubleStrike)
        .                   };
        .                   let mut events =
    9,588 ( 0.00%)              self.resolve_combat_damage_with_filter(&computed, regular_or_ds, regular_or_ds)?;
        .           
        .                   // Suspended on a `wants_ui` player's combat-damage choice — no damage
        .                   // dealt yet; combat is not torn down. `submit_decision` re-enters.
    3,196 ( 0.00%)          if self.pending_decision.is_some() {
    5,536 ( 0.00%)              return Ok(events);
        .                   }
        .           
    5,008 ( 0.00%)          let mut sba = self.check_state_based_actions();
114,815,008 ( 3.68%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (2,504x)
        .                   events.append(&mut sba);
        .           
        .                   self.attacking.clear();
        .                   self.block_map.clear();
        .                   self.blocked_attackers.clear();
        .                   self.attack_bands.clear();
        .                   self.clear_combat_damage_plan();
    2,504 ( 0.00%)          self.blockers_declared = false;
        .                   // CR 702.39 — provoke's "block this combat" requirement ends here.
        .                   for c in &mut self.battlefield {
   45,044 ( 0.00%)              c.must_block = None;
        .                   }
        .           
    2,504 ( 0.00%)          events.push(GameEvent::CombatResolved);
   10,016 ( 0.00%)          Ok(events)
   25,568 ( 0.00%)      }
        .           
        .               /// CR 702.15 — does `defender` control a land with the given land type?
        .               /// Reads printed land subtypes (Forest/Island/…), so dual lands and
        .               /// nonbasics with the type count.
        .               /// CR 802 / 803 / 801.3 — the seats `seat` may legally declare attacks
        .               /// against right now. Empty when it isn't `seat`'s combat, and narrowed to
        .               /// one entry (or none) by the attack-left / attack-right option; under a
        .               /// limited range of influence, only opponents inside `seat`'s range.
-- line 2584 ----------------------------------------
-- line 2623 ----------------------------------------
        .                       })
        .                   })
        .               }
        .           
        .               /// CR 809.3c — under the Emperor variant's attack option, `defender` must
        .               /// be seated immediately next to `seat` (measured over living seats).
        .               /// Always true when the option is off.
        .               pub(crate) fn seat_attackable_from(&self, seat: usize, defender: usize) -> bool {
    8,464 ( 0.00%)          if !self.attack_adjacent_only {
        .                       return true;
        .                   }
        .                   let living: Vec<usize> =
        .                       (0..self.players.len()).filter(|p| self.players[*p].is_alive()).collect();
        .                   let (Some(here), Some(there)) = (
        .                       living.iter().position(|p| *p == seat),
        .                       living.iter().position(|p| *p == defender),
        .                   ) else {
-- line 2639 ----------------------------------------
-- line 2644 ----------------------------------------
        .                   d == 1 || d == n - 1
        .               }
        .           
        .               /// CR 803.1a/b — the single seat the active player may attack under the
        .               /// attack-left / attack-right option. `None` means no restriction (CR 802);
        .               /// `Some(None)` means "no legal defender" (the nearest living opponent is
        .               /// more than one seat away, so this player can't attack at all).
        .               fn attack_left_right_defender(&self) -> Option<Option<usize>> {
   11,340 ( 0.00%)          let step: isize = match self.attack_option {
        .                       crate::game::AttackOption::MultiplePlayers => return None,
        .                       crate::game::AttackOption::AttackLeft => 1,
        .                       crate::game::AttackOption::AttackRight => -1,
        .                   };
        .                   let n = self.players.len() as isize;
    3,780 ( 0.00%)          if n < 2 {
        .                       return Some(None);
        .                   }
        .                   let me = self.active_player_idx as isize;
        .                   let neighbor = ((me + step).rem_euclid(n)) as usize;
        .                   // "More than one seat away" — the adjacent seat has to be a living
        .                   // opponent, otherwise this player has no legal defender.
        .                   (self.players[neighbor].is_alive() && !self.same_team(self.active_player_idx, neighbor))
        .                       .then_some(Some(neighbor))
-- line 2666 ----------------------------------------
-- line 2695 ----------------------------------------
        .                   attacker: CardId,
        .                   default_order: &[CardId],
        .               ) -> crate::decision::Decision {
        .                   let blockers: Vec<(CardId, String)> = default_order
        .                       .iter()
        .                       .map(|id| {
        .                           let name = self
        .                               .battlefield_find(*id)
    1,532 ( 0.00%)                      .map(|c| c.definition.name.to_string())
        .                               .unwrap_or_default();
        .                           (*id, name)
        .                       })
        .                       .collect();
      764 ( 0.00%)          crate::decision::Decision::CombatDamageOrder { attacker, blockers }
        .               }
        .           
        .               /// Validate a `DamageOrder` answer into a concrete blocker order. Ids the
        .               /// answer omits are appended in their default position, and unknown ids
        .               /// are ignored — so a partial or empty answer is always legal and keeps
        .               /// `default_order`.
    3,410 ( 0.00%)      fn resolve_damage_order(
        .                   &self,
        .                   default_order: &[CardId],
        .                   answer: &crate::decision::DecisionAnswer,
        .               ) -> Vec<CardId> {
        .                   use crate::decision::DecisionAnswer;
      930 ( 0.00%)          let DecisionAnswer::DamageOrder(chosen) = answer else {
        .                       return default_order.to_vec();
        .                   };
        .                   let mut ordered: Vec<CardId> = Vec::with_capacity(default_order.len());
        .                   for id in chosen {
        .                       if default_order.contains(id) && !ordered.contains(id) {
        .                           ordered.push(*id);
        .                       }
        .                   }
        .                   for id in default_order {
        .                       if !ordered.contains(id) {
        .                           ordered.push(*id);
        .                       }
        .                   }
    1,550 ( 0.00%)          ordered
    2,480 ( 0.00%)      }
        .           
        .               /// CR 510.1c-d — divide an attacker's `total_power` combat damage among
        .               /// its blockers (given in assignment order with their `lethal` amounts).
        .               /// Returns `(blocker_id, amount)` pairs in the same order; any power not
        .               /// assigned to a blocker is the trample-over leftover.
        .               ///
        .               /// The default (and `AutoDecider`) split assigns lethal to each blocker
        .               /// in order until the power runs out. A `wants_ui` / scripted decider may
-- line 2744 ----------------------------------------
-- line 2752 ----------------------------------------
        .                   lethals: &[(CardId, u32)],
        .                   has_trample: bool,
        .               ) -> Vec<(CardId, u32)> {
        .                   let mut remaining = total_power;
        .                   let mut split: Vec<(CardId, u32)> = lethals
        .                       .iter()
        .                       .map(|&(id, lethal)| {
        .                           let a = lethal.min(remaining);
    2,244 ( 0.00%)                  remaining -= a;
        .                           (id, a)
        .                       })
        .                       .collect();
        .                   // CR 510.1d — ALL the attacker's damage must be assigned; without
        .                   // trample the excess goes to the blockers (default: the last one)
        .                   // rather than vanishing. With trample the remainder is left for the
        .                   // caller's trample-over-to-player path (CR 702.19g).
    4,476 ( 0.00%)          if !has_trample && remaining > 0 && let Some(last) = split.last_mut() {
      548 ( 0.00%)              last.1 += remaining;
        .                   }
        .                   split
        .               }
        .           
        .               /// Build the `Decision::AssignCombatDamage` for dividing `total_power`
        .               /// among `lethals` (in assignment order).
        .               fn assign_combat_damage_decision(
        .                   &self,
-- line 2777 ----------------------------------------
-- line 2779 ----------------------------------------
        .                   total_power: u32,
        .                   lethals: &[(CardId, u32)],
        .               ) -> crate::decision::Decision {
        .                   let blockers: Vec<(CardId, String, u32)> = lethals
        .                       .iter()
        .                       .map(|&(id, lethal)| {
        .                           let name = self
        .                               .battlefield_find(id)
    1,244 ( 0.00%)                      .map(|c| c.definition.name.to_string())
        .                               .unwrap_or_default();
        .                           (id, name, lethal)
        .                       })
        .                       .collect();
      930 ( 0.00%)          crate::decision::Decision::AssignCombatDamage {
        .                       attacker,
        .                       attacker_power: total_power,
        .                       blockers,
        .                   }
        .               }
        .           
        .               /// Validate a `CombatDamageAssignment` answer into a concrete split. An
        .               /// empty or rule-violating answer falls back to `default_damage_split`.
    3,720 ( 0.00%)      fn resolve_damage_assignment(
        .                   &self,
        .                   total_power: u32,
        .                   lethals: &[(CardId, u32)],
        .                   has_trample: bool,
        .                   answer: &crate::decision::DecisionAnswer,
        .               ) -> Vec<(CardId, u32)> {
        .                   use crate::decision::DecisionAnswer;
      930 ( 0.00%)          let DecisionAnswer::CombatDamageAssignment(pairs) = answer else {
        .                       return self.default_damage_split(total_power, lethals, has_trample);
        .                   };
      620 ( 0.00%)          if pairs.is_empty() {
        .                       return self.default_damage_split(total_power, lethals, has_trample);
        .                   }
        .                   // Re-key the answer into blocker order (missing entries = 0).
        .                   let amounts: Vec<u32> = lethals
        .                       .iter()
        .                       .map(|&(id, _)| {
        .                           pairs
        .                               .iter()
-- line 2820 ----------------------------------------
-- line 2844 ----------------------------------------
        .                   }
        .                   // CR 510.1d — all of the attacker's power must be assigned; without
        .                   // trample there is no player to soak the leftover, so an
        .                   // under-assignment (even with every blocker at lethal) is illegal.
        .                   if !has_trample && assigned < total_power {
        .                       return self.default_damage_split(total_power, lethals, has_trample);
        .                   }
        .                   lethals.iter().map(|&(id, _)| id).zip(amounts).collect()
    2,480 ( 0.00%)      }
        .           
        .               /// CR 510.1c-d — gather (and cache) the active player's combat-damage
        .               /// ordering and assignment choices for every multi-blocker attacker,
        .               /// before any damage is applied. Returns `true` if it suspended on a
        .               /// `wants_ui` player's pending decision (the caller must return early and
        .               /// re-enter the damage step after the answer); `false` once every choice
        .               /// is settled. Pure w.r.t. the battlefield — only the decision caches and
        .               /// `pending_decision` are written.
-- line 2860 ----------------------------------------
-- line 2865 ----------------------------------------
        .                   blocker_filter: &impl Fn(&[Keyword]) -> bool,
        .               ) -> bool {
        .                   use crate::game::types::{CombatDecisionKind, PendingDecision, ResumeContext};
        .                   // Reset the caches once when entering a new damage step (first-strike
        .                   // vs regular), but never on a mid-step decision resume.
        .                   if self.combat_damage_plan_step != Some(self.step) {
        .                       self.combat_damage_order.clear();
        .                       self.combat_damage_assignment.clear();
   13,114 ( 0.00%)              self.combat_damage_plan_step = Some(self.step);
        .                   }
    9,822 ( 0.00%)          let active = self.active_player_idx;
        .                   for atk in attacker_infos.iter().filter(|a| a.should_deal) {
   27,716 ( 0.00%)              let blocker_ids = self.blockers_of(atk.id);
  854,081 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::blockers_of (5,452x)
        .                       // A free divider (Butcher Orgg) always gets an assignment choice —
        .                       // it divides over the defending player's creatures, not its
        .                       // blockers, so the multi-blocker gate doesn't apply.
   38,620 ( 0.00%)              let free_divider = !self.free_division_targets(atk.id, computed).is_empty();
  476,242 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets (5,452x)
   22,112 ( 0.00%)              if blocker_ids.len() <= 1 && !free_divider {
        .                           continue;
        .                       }
        .           
        .                       // CR 509.2 / 510.1c — Banding: if any blocking creature has
        .                       // banding, the *defending* player (the blockers' controller), not
        .                       // the attacking player, announces this attacker's damage order and
        .                       // assignment. Otherwise the active (attacking) player decides.
        .                       let banding_assigner = blocker_ids
        .                           .iter()
        .                           .find_map(|bid| {
        .                               computed
        .                                   .iter()
    9,732 ( 0.00%)                          .find(|c| c.id == *bid && c.keywords.contains(&Keyword::Banding))
        .                                   .map(|c| c.controller)
        .                           })
        .                           // CR 702.22j — the "bands with other [quality]" arm.
    4,072 ( 0.00%)                  .or_else(|| self.quality_band_assigner(&blocker_ids));
5,958,948 ( 0.19%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::quality_band_assigner (1,018x)
        .                       // CR 510.1a — Defensive Formation: the defending player assigns
        .                       // the damage of everything attacking them.
        .                       let defending_seat = self.attacking.iter().find(|a| a.attacker == atk.id).and_then(
    2,036 ( 0.00%)                  |a| match a.target {
        .                               crate::game::types::AttackTarget::Player(d) => Some(d),
        .                               crate::game::types::AttackTarget::Planeswalker(pw)
        .                               | crate::game::types::AttackTarget::Battle(pw) => {
        .                                   self.battlefield_find(pw).map(|c| c.controller)
        .                               }
        .                           },
        .                       );
        .                       let defender_assigner = match defending_seat {
        .                           Some(d) if self.battlefield.iter().any(|c| {
   55,068 ( 0.00%)                      c.controller == d
        .                                   && c.definition.static_abilities.iter().any(|sa| {
        .                                       matches!(
        .                                           sa.effect,
        .                                           crate::effect::StaticEffect::ControllerAssignsAttackersCombatDamage
        .                                       )
        .                                   })
        .                           }) => Some(d),
        .                           _ => None,
        .                       };
        .                       let assigner = banding_assigner.or(defender_assigner).unwrap_or(active);
    2,036 ( 0.00%)              let assigner_ui = self.players[assigner].wants_ui;
        .           
        .                       // 1) Blocker order (CR 510.1c) — a free divider has no order to
        .                       // announce, so it goes straight to the assignment.
      382 ( 0.00%)              if !self.combat_damage_order.contains_key(&atk.id) && !free_divider {
        .                           let decision = self.combat_damage_order_decision(atk.id, &blocker_ids);
      764 ( 0.00%)                  if assigner_ui {
    8,786 ( 0.00%)                      self.pending_decision = Some(PendingDecision {
    6,876 ( 0.00%)                          decision,
        .                                   resume: ResumeContext::CombatDamage {
        .                                       player: assigner,
        .                                       attacker: atk.id,
        .                                       kind: CombatDecisionKind::Order,
        .                                   },
        .                               });
        .                               return true;
        .                           }
-- line 2940 ----------------------------------------
-- line 2944 ----------------------------------------
        .                       }
        .                       let order = self.combat_damage_order.get(&atk.id).cloned().unwrap_or_default();
        .           
        .                       // 2) Damage assignment across the ordered blockers (CR 510.1d).
        .                       if !self.combat_damage_assignment.contains_key(&atk.id) {
        .                           let total_power = if self.combat_damage_prevented_for_dealer(atk.id) {
        .                               0
        .                           } else {
      310 ( 0.00%)                      atk.power.max(0) as u32
        .                           };
    4,030 ( 0.00%)                  let (lethals, trample) = self.combat_assignment_plan(
  177,295 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_assignment_plan (310x)
        .                               atk.id,
      310 ( 0.00%)                      atk.has_deathtouch,
      310 ( 0.00%)                      atk.has_trample,
        .                               &order,
        .                               computed,
        .                           );
        .                           // No meaningful choice with zero power — store the default.
      930 ( 0.00%)                  if total_power == 0 {
        .                               let split = self.default_damage_split(total_power, &lethals, trample);
        .                               self.combat_damage_assignment.insert(atk.id, split);
        .                               continue;
        .                           }
        .                           let decision =
        .                               self.assign_combat_damage_decision(atk.id, total_power, &lethals);
      620 ( 0.00%)                  if assigner_ui {
    7,750 ( 0.00%)                      self.pending_decision = Some(PendingDecision {
    6,200 ( 0.00%)                          decision,
        .                                   resume: ResumeContext::CombatDamage {
        .                                       player: assigner,
        .                                       attacker: atk.id,
        .                                       kind: CombatDecisionKind::Assign,
        .                                   },
        .                               });
        .                               return true;
        .                           }
-- line 2979 ----------------------------------------
-- line 2988 ----------------------------------------
        .                   // attackers: its controller (the defending player) orders those
        .                   // attackers and divides the blocker's damage among them. Cached in the
        .                   // same maps, keyed by the blocker's id — a creature is never both an
        .                   // attacker and a blocker in one combat, so the keyspaces don't collide.
        .                   let multi_blockers: Vec<CardId> = {
        .                       let mut v: Vec<CardId> = self
        .                           .block_map
        .                           .iter()
    2,286 ( 0.00%)                  .filter(|(_, atks)| atks.len() > 1)
        .                           .map(|(&b, _)| b)
        .                           .collect();
        .                       v.sort_by_key(|id| id.0);
    2,582 ( 0.00%)              v
        .                   };
        .                   for bid in multi_blockers {
        .                       let Some(bcp) = computed.iter().find(|c| c.id == bid) else { continue };
        .                       if !blocker_filter(&bcp.keywords)
        .                           || bcp.keywords.contains(&Keyword::DealsNoCombatDamage)
        .                           || self.combat_damage_prevented_creatures.contains(&bid)
        .                           || self.assigns_no_combat_damage_this_turn.contains(&bid)
        .                           || self.combat_damage_prevented_for_dealer(bid)
-- line 3008 ----------------------------------------
-- line 3072 ----------------------------------------
        .                   }
        .                   false
        .               }
        .           
        .               /// CR 510.1e — the combat damage `blocker` assigns to `attacker` this
        .               /// step. A creature blocking one attacker deals its whole power; a
        .               /// multi-block divides it per the cached split (defaulting to lethal in
        .               /// declaration order).
    2,222 ( 0.00%)      fn blocker_damage_to(&self, blocker: CardId, attacker: CardId, power: u32) -> u32 {
    4,444 ( 0.00%)          if self.attackers_blocked_by(blocker).len() <= 1 {
        .                       return power;
        .                   }
        .                   self.combat_damage_assignment
        .                       .get(&blocker)
        .                       .and_then(|split| split.iter().find(|(a, _)| *a == attacker).map(|(_, n)| *n))
        .                       .unwrap_or(0)
    2,222 ( 0.00%)      }
        .           
        .               /// Lethal damage required for each blocker in `order` (its toughness, or 1
        .               /// under deathtouch per CR 702.2e). Blockers no longer on the battlefield
        .               /// resolve to 0.
        .               /// CR 702.22 — the attacking bands declared this combat, for the server
        .               /// view. Empty when nothing banded.
        .               pub fn attack_bands_view(&self) -> Vec<Vec<CardId>> {
        .                   self.attack_bands.clone()
        .               }
        .           
        .               /// Butcher Orgg — "you may assign this creature's combat damage divided as
        .               /// you choose among defending player and/or any number of creatures they
        .               /// control". Returns the divisible creature set (every creature the
        .               /// defending player controls, id-ordered) or empty when the attacker has
        .               /// no such ability.
  106,980 ( 0.00%)      fn free_division_targets(
        .                   &self,
        .                   attacker: CardId,
        .                   computed: &[ComputedPermanent],
        .               ) -> Vec<CardId> {
        .                   let free = computed.iter().any(|c| {
   47,518 ( 0.00%)              c.id == attacker
        .                           && c.keywords.contains(&Keyword::DividesCombatDamageAmongDefenders)
        .                   });
        .                   if !free {
        .                       return vec![];
        .                   }
        .                   let Some(defender) = self
        .                       .attacking
        .                       .iter()
-- line 3118 ----------------------------------------
-- line 3125 ----------------------------------------
        .                       .iter()
        .                       .filter(|c| {
        .                           c.controller == defender && c.card_types.contains(&crate::card::CardType::Creature)
        .                       })
        .                       .map(|c| c.id)
        .                       .collect();
        .                   ids.sort_by_key(|id| id.0);
        .                   ids
   85,584 ( 0.00%)      }
        .           
        .               /// The `(id, lethal)` set an attacker divides its combat damage among, and
        .               /// whether unassigned damage flows to the defending player. Normally the
        .               /// ordered blockers with trample as the outlet (CR 510.1c-d); a free
        .               /// divider (Butcher Orgg) uses the defending player's creatures with no
        .               /// lethal requirement and the player always as the outlet.
    9,300 ( 0.00%)      fn combat_assignment_plan(
        .                   &self,
        .                   attacker: CardId,
        .                   attacker_deathtouch: bool,
        .                   has_trample: bool,
        .                   order: &[CardId],
        .                   computed: &[ComputedPermanent],
        .               ) -> (Vec<(CardId, u32)>, bool) {
    1,240 ( 0.00%)          let free = self.free_division_targets(attacker, computed);
   88,240 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets (620x)
    1,240 ( 0.00%)          if free.is_empty() {
    3,100 ( 0.00%)              (self.combat_lethals(attacker_deathtouch, order, computed), has_trample)
        .                   } else {
        .                       (free.into_iter().map(|id| (id, 0)).collect(), true)
        .                   }
    4,960 ( 0.00%)      }
        .           
        .               fn combat_lethals(
        .                   &self,
        .                   attacker_deathtouch: bool,
        .                   order: &[CardId],
        .                   computed: &[ComputedPermanent],
        .               ) -> Vec<(CardId, u32)> {
        .                   order
        .                       .iter()
        .                       .map(|&bid| {
        .                           let tough = computed
        .                               .iter()
        .                               .find(|c| c.id == bid)
    1,244 ( 0.00%)                      .map(|c| c.toughness.max(0) as u32)
        .                               .unwrap_or(0);
        .                           // CR 510.1c — lethal accounts for damage already marked
        .                           // (a double-strike trampler only needs the remainder in the
        .                           // regular step). Deathtouch: any nonzero amount (702.2e).
        .                           let marked = self
        .                               .battlefield_find(bid)
        .                               .map(|c| c.damage)
        .                               .unwrap_or(0);
    2,488 ( 0.00%)                  (bid, if attacker_deathtouch { 1 } else { tough.saturating_sub(marked) })
        .                       })
        .                       .collect()
        .               }
        .           
        .               /// Validate and cache one combat-damage decision answered via
        .               /// `submit_decision`, so the re-entered damage step finds it settled.
        .               pub(crate) fn apply_combat_decision_answer(
        .                   &mut self,
        .                   attacker: CardId,
        .                   kind: crate::game::types::CombatDecisionKind,
        .                   answer: &crate::decision::DecisionAnswer,
        .               ) {
        .                   use crate::game::types::CombatDecisionKind;
    1,240 ( 0.00%)          match kind {
        .                       CombatDecisionKind::Order => {
        .                           // `attacker` is the damage source: an attacker ordering its
        .                           // blockers, or (CR 509.2) a multi-block blocker ordering the
        .                           // attackers it blocks.
        .                           let default_order = match self.attackers_blocked_by(attacker) {
        .                               atks if atks.len() > 1 => atks.to_vec(),
      930 ( 0.00%)                      _ => self.blockers_of(attacker),
   72,686 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::blockers_of (310x)
        .                           };
    1,240 ( 0.00%)                  let order = self.resolve_damage_order(&default_order, answer);
   44,076 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_damage_order (310x)
        .                           self.combat_damage_order.insert(attacker, order);
        .                       }
        .                       CombatDecisionKind::Assign => {
        .                           let order = self
        .                               .combat_damage_order
        .                               .get(&attacker)
        .                               .cloned()
        .                               .unwrap_or_default();
      620 ( 0.00%)                  let computed = self.compute_battlefield();
5,360,051 ( 0.17%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::compute_battlefield (310x)
        .                           let atk_cp = computed.iter().find(|c| c.id == attacker);
        .                           let deathtouch = atk_cp
        .                               .is_some_and(|c| c.keywords.contains(&Keyword::Deathtouch));
        .                           let power = atk_cp.map(combat_damage_value).unwrap_or(0);
        .                           let total_power = if self.combat_damage_prevented_for_dealer(attacker) {
        .                               0
        .                           } else {
        .                               power.max(0) as u32
        .                           };
        .                           // A multi-block blocker (CR 510.1e) has no trample outlet.
      310 ( 0.00%)                  let trample = self.attackers_blocked_by(attacker).len() <= 1
        .                               && atk_cp.is_some_and(|c| c.keywords.contains(&Keyword::Trample));
    1,240 ( 0.00%)                  let (lethals, trample) =
    4,030 ( 0.00%)                      self.combat_assignment_plan(attacker, deathtouch, trample, &order, &computed);
  253,142 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_assignment_plan (310x)
      930 ( 0.00%)                  let split = self.resolve_damage_assignment(total_power, &lethals, trample, answer);
   38,510 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_damage_assignment (310x)
        .                           self.combat_damage_assignment.insert(attacker, split);
        .                       }
        .                   }
        .               }
        .           
        .               /// Clear the cached combat-damage choices at the end of a combat phase.
        .               pub(crate) fn clear_combat_damage_plan(&mut self) {
        .                   self.combat_damage_order.clear();
        .                   self.combat_damage_assignment.clear();
    2,504 ( 0.00%)          self.combat_damage_plan_step = None;
        .               }
        .           
        .               /// Core combat damage resolver. Each attacker has its own defending
        .               /// player or planeswalker (`Attack::target`); damage routing is
        .               /// per-attacker.
        .               fn resolve_combat_damage_with_filter(
        .                   &mut self,
        .                   computed: &[ComputedPermanent],
-- line 3242 ----------------------------------------
-- line 3245 ----------------------------------------
        .               ) -> Result<Vec<GameEvent>, GameError> {
        .                   let mut events = vec![];
        .                   // Each call is one combat-damage batch (first-strike and regular
        .                   // damage are separate sub-steps): reset the "one or more creatures
        .                   // you control deal combat damage" graveyard-trigger dedupe.
        .                   self.gy_combat_trigger_fired_this_step.clear();
        .           
        .                   let computed_of =
   17,312 ( 0.00%)              |id: CardId| -> Option<&ComputedPermanent> { computed.iter().find(|c| c.id == id) };
        .           
        .                   let attacker_infos: Vec<AttackerInfo> = self
        .                       .attacking
        .                       .iter()
        .                       .filter_map(|atk| {
    6,276 ( 0.00%)                  let cp = computed_of(atk.attacker)?;
        .                           let defender_player = self.defender_for(atk.target)?;
        .                           let kws = &cp.keywords;
  106,692 ( 0.00%)                  Some(AttackerInfo {
        .                               id: cp.id,
    6,276 ( 0.00%)                      controller: cp.controller,
        .                               target: atk.target,
        .                               defender_player,
        .                               power: combat_damage_value(cp),
        .                               has_trample: kws.contains(&Keyword::Trample),
        .                               has_trample_over_pw: kws.contains(&Keyword::TrampleOverPlaneswalkers),
        .                               has_lifelink: kws.contains(&Keyword::Lifelink),
        .                               has_deathtouch: kws.contains(&Keyword::Deathtouch),
        .                               has_infect: kws.contains(&Keyword::Infect),
        .                               has_wither: kws.contains(&Keyword::Wither),
   23,046 ( 0.00%)                      toxic: kws.iter().filter_map(|k| match k {
        .                                   // Poisonous N (CR 702.70) folds into the same
        .                                   // combat-damage poison rider as Toxic (CR 702.180).
        .                                   Keyword::Toxic(n) | Keyword::Poisonous(n) => Some(*n),
        .                                   _ => None,
        .                               }).sum(),
        .                               // CR 510.1 — a creature with "deals no combat damage this
        .                               // turn" (Master of Cruelties) is skipped in both damage
        .                               // steps even though it's a legal attacker/blocker. CR 614.9
-- line 3282 ----------------------------------------
-- line 3324 ----------------------------------------
        .           
        .           
        .                   // Creature-vs-creature combat damage recorded here and dispatched after
        .                   // all damage in this step is dealt, so `DealsCombatDamageToCreature`
        .                   // triggers (CR 510.2) go on the stack simultaneously (CR 603.3b).
        .                   let mut creature_damage: Vec<(CardId, CardId, u32)> = vec![];
        .           
        .                   for atk in &attacker_infos {
    9,384 ( 0.00%)              if !atk.should_deal {
        .                           continue;
        .                       }
        .                       // Fog (per-dealer) OR "prevent all combat damage it would deal"
        .                       // (Azorius Ploy) both zero this attacker's outgoing damage while
        .                       // still letting its blockers strike it back.
    4,550 ( 0.00%)              let prevent_combat_damage = self.combat_damage_prevented_for_dealer(atk.id)
    4,550 ( 0.00%)                  || self.combat_damage_prevented_from(atk.id);
2,893,404 ( 0.09%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_from (4,474x)
        .           
        .                       // CR 510.1c: the attacking player chose the order in which an
        .                       // attacker assigns combat damage to its multiple blockers; that
        .                       // choice was gathered in PHASE 1 and cached. Start from the
        .                       // deterministic default (CardId = declaration-order proxy) and use
        .                       // the cached order when present.
   27,300 ( 0.00%)              let mut blocker_ids = self.blockers_of(atk.id);
  577,529 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::blockers_of (4,474x)
        .                       // CR 510.1a — "assigns its combat damage as though it weren't
        .                       // blocked" (Predatory Focus): drop the blocker list so the whole
        .                       // hit lands on the defending player. The blockers still deal
        .                       // theirs, which the blocker loop below handles.
    4,550 ( 0.00%)              if self
   13,650 ( 0.00%)                  .computed_permanent(atk.id)
14,890,117 ( 0.48%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,474x)
        .                           .is_some_and(|cp| cp.keywords.contains(
        .                               &crate::card::Keyword::AssignsDamageAsThoughUnblocked))
        .                       {
        .                           blocker_ids.clear();
        .                           self.blocked_attackers.retain(|id| *id != atk.id);
        .                       }
    9,720 ( 0.00%)              if blocker_ids.len() > 1
        .                           && let Some(order) = self.combat_damage_order.get(&atk.id)
        .                       {
    1,240 ( 0.00%)                  blocker_ids = order.clone();
        .                       }
        .                       // Butcher Orgg divides over the defending player's creatures
        .                       // instead of its blockers, blocked or not.
   27,224 ( 0.00%)              let free_targets = self.free_division_targets(atk.id, computed);
  384,744 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::free_division_targets (4,474x)
        .           
        .                       // Goblin Psychopath — the charge armed by a lost coin flip sends
        .                       // this attacker's whole combat-damage assignment at its
        .                       // controller instead (CR 614.9).
        .                       // Soltari Guerrillas — the armed charge sends this attacker's
        .                       // damage at a chosen creature instead of the defending player.
   13,650 ( 0.00%)              if blocker_ids.is_empty()
    5,236 ( 0.00%)                  && free_targets.is_empty()
    5,236 ( 0.00%)                  && matches!(atk.target, AttackTarget::Player(_))
        .                           && let Some(i) =
    2,618 ( 0.00%)                      self.next_combat_damage_redirect.iter().position(|(a, _)| *a == atk.id)
        .                       {
        .                           let (_, victim) = self.next_combat_damage_redirect.remove(i);
        .                           if self.battlefield_find(victim).is_some() {
        .                               let raw = if prevent_combat_damage { 0 } else { atk.power.max(0) as u32 };
        .                               self.deal_damage_to_from(
        .                                   crate::game::effects::EntityRef::Permanent(victim),
        .                                   raw,
        .                                   Some(atk.id),
-- line 3385 ----------------------------------------
-- line 3395 ----------------------------------------
        .                               crate::game::effects::EntityRef::Player(seat),
        .                               raw,
        .                               Some(atk.id),
        .                               &mut events,
        .                           );
        .                           continue;
        .                       }
        .           
    9,786 ( 0.00%)              if blocker_ids.is_empty() && free_targets.is_empty() {
        .                           // CR 510.1c — an attacker that became blocked stays blocked
        .                           // even if all its blockers left combat (died to first-strike
        .                           // damage or removal). Without trample it assigns no combat
        .                           // damage; with trample everything goes to the defending
        .                           // player (CR 702.19g).
    2,618 ( 0.00%)                  if self.blocked_attackers.contains(&atk.id) && !atk.has_trample {
        .                               continue;
        .                           }
    5,236 ( 0.00%)                  let raw = if prevent_combat_damage {
        .                               0
        .                           } else {
    2,618 ( 0.00%)                      atk.power.max(0) as u32
        .                           };
        .                           // CR 615 — per-target prevention shields on the defending
        .                           // player/planeswalker also reduce unblocked combat damage.
        .                           // Lifelink scales off the post-prevention amount (702.15a).
   13,090 ( 0.00%)                  let amount = self.prevent_combat_to_target(
10,215,725 ( 0.33%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::prevent_combat_to_target (2,562x)
    5,236 ( 0.00%)                      atk.target,
    5,236 ( 0.00%)                      self.scale_combat_damage(Some(atk.id), atk.target, raw),
        .                               Some(atk.id),
        .                               &mut events,
        .                           );
   10,360 ( 0.00%)                  if amount > 0 {
   12,978 ( 0.00%)                      self.deal_combat_damage_to_target(atk, amount, &mut events);
20,129,029 ( 0.65%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target (2,562x)
   10,360 ( 0.00%)                      if atk.has_lifelink {
      664 ( 0.00%)                          let a = self.active_player_idx;
        .                                   let applied = self.adjust_life_applied(a, amount as i32);
    2,544 ( 0.00%)                          if applied > 0 {
    1,992 ( 0.00%)                              events.push(GameEvent::LifeGained { player: a, amount: applied as u32 });
        .                                   }
        .                               }
        .                           }
        .                       } else {
    3,864 ( 0.00%)                  let total_power = if prevent_combat_damage {
        .                               0
        .                           } else {
    1,932 ( 0.00%)                      atk.power.max(0) as u32
        .                           };
        .                           // CR 510.1c-d — the attacking player divides combat damage
        .                           // among the blockers in the chosen order; that choice was
        .                           // gathered in PHASE 1 and cached. Fall back to the default
        .                           // lethal-to-each split when there's no cached choice (single
        .                           // blocker, prevented, or a non-UI path that stored the
        .                           // default). The lethal for each is 1 under deathtouch (CR
        .                           // 702.2e).
        .                           // CR 510.1c — lethal accounts for marked damage; deathtouch
        .                           // needs only 1 (CR 702.2e). A free divider has no lethal
        .                           // requirement and always spills the remainder to the player.
   15,436 ( 0.00%)                  let (lethals, leftover_to_player) = if free_targets.is_empty() {
        .                               let lethals = blocker_ids
        .                                   .iter()
        .                                   .map(|&bid| {
        .                                       let tough = computed_of(bid)
    2,244 ( 0.00%)                                  .map(|c| c.toughness.max(0) as u32)
        .                                           .unwrap_or(0);
        .                                       let marked = self
        .                                           .battlefield_find(bid)
        .                                           .map(|c| c.damage)
        .                                           .unwrap_or(0);
    4,488 ( 0.00%)                              (bid, if atk.has_deathtouch { 1 } else { tough.saturating_sub(marked) })
        .                                   })
        .                                   .collect::<Vec<_>>();
    3,844 ( 0.00%)                      (lethals, atk.has_trample)
        .                           } else {
        .                               (free_targets.iter().map(|&id| (id, 0)).collect(), true)
        .                           };
        .                           let assignment = self
        .                               .combat_damage_assignment
        .                               .get(&atk.id)
        .                               .cloned()
        .                               .unwrap_or_else(|| {
        .                                   self.default_damage_split(total_power, &lethals, leftover_to_player)
        .                               });
        .                           let mut lifelink_dealt = 0i32;
        .                           let mut assigned_to_blockers = 0u32;
        .           
    2,244 ( 0.00%)                  for &(blocker_id, assign) in &assignment {
    2,244 ( 0.00%)                      assigned_to_blockers += assign;
    4,488 ( 0.00%)                      if assign == 0 {
        .                                   continue;
        .                               }
        .                               // One freeze scope over this pair's read-only prefix. Every
        .                               // check here is `&self` and the first `&mut self` call is
        .                               // `apply_prevention_shields` below, so no layer input can
        .                               // move between them — the reads already happened at one
        .                               // game state, this only stops each rebuilding the gather.
        .                               // `damage_prevented_by_protection` and `scale_damage_to`
        .                               // both take one, and both already freeze internally, so
        .                               // the scope is what merges them.
    6,684 ( 0.00%)                      let Some(scaled) = self.with_frozen_layers(|g| {
        .                                   // CR 614.9 — a Maze-of-Ith'd blocker takes no combat
        .                                   // damage. CR 615 — Emmara shields your creature tokens.
    2,228 ( 0.00%)                          if g.combat_damage_prevented_creatures.contains(&blocker_id)
        .                                       || g.all_damage_to_creature_token_prevented(blocker_id)
        .                                       || g.all_damage_to_your_creature_prevented(blocker_id)
        .                                       // CR 702.16e — protection from the attacker's color
        .                                       // prevents its combat damage to the blocker.
   13,388 ( 0.00%)                              || g.damage_prevented_by_protection(atk.id, blocker_id)
8,162,188 ( 0.26%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (2,208x)
        .                                       // CR 615 — Light of Sanction: your source → your
        .                                       // creature.
        .                                       || g.damage_from_your_source_to_your_creature_prevented(
        .                                           atk.id,
        .                                           blocker_id,
        .                                       )
        .                                       // CR 615 — Indentured Oaf: this source prevents its
        .                                       // own damage to creatures of a chosen color.
   11,140 ( 0.00%)                              || g.source_damage_to_color_prevented(atk.id, blocker_id)
  324,108 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::source_damage_to_color_prevented (2,208x)
        .                                   {
        .                                       return None;
        .                                   }
   11,140 ( 0.00%)                          Some(g.double_creature_combat_damage(g.scale_damage_to(
8,223,770 ( 0.26%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::scale_damage_to (2,208x)
        .                                       Some(atk.id),
    6,684 ( 0.00%)                              crate::game::effects::EntityRef::Permanent(blocker_id),
        .                                       assign,
        .                                   )))
        .                               }) else {
        .                                   continue;
        .                               };
        .                               // CR 615 — route attacker→blocker combat damage through
        .                               // the blocker's prevention shields. Lifelink and the
        .                               // wither/infect -1/-1 counters scale off the actual
        .                               // (post-prevention) amount dealt (CR 702.15a).
   15,576 ( 0.00%)                      let dealt = self.apply_prevention_shields(
12,603,626 ( 0.40%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields (2,208x)
    4,476 ( 0.00%)                          crate::game::effects::EntityRef::Permanent(blocker_id),
        .                                   scaled,
        .                                   Some(atk.id),
        .                                   &mut events,
        .                               ) as i32;
        .                               // Ironscale Hydra replaces the damage with a +1/+1 counter
        .                               // (and so the attacker's lifelink scales off 0).
   11,120 ( 0.00%)                      let dealt = self.ironscale_replace(blocker_id, dealt, &mut events);
9,372,156 ( 0.30%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace (2,208x)
        .                               // CR 615 — a blocker that prevents all damage to itself
        .                               // (Wall of Denial) takes none, and grants no lifelink.
   11,120 ( 0.00%)                      let dealt = if self.combat_damage_prevented_to_self(blocker_id)
2,028,460 ( 0.07%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_to_self (2,208x)
   13,348 ( 0.00%)                          || self.damage_from_source_prevented_by_keyword(blocker_id, atk.id)
9,160,549 ( 0.29%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword (2,208x)
        .                               {
        .                                   0
        .                               } else {
        .                                   dealt
        .                               };
    2,228 ( 0.00%)                      lifelink_dealt += dealt;
        .           
        .                               // Karona's Zealot — a standing turn-scoped redirect moves
        .                               // the whole combat-damage event onto another creature.
        .                               let blocker_id =
        .                                   self.turn_damage_redirect_for(blocker_id).unwrap_or(blocker_id);
    6,684 ( 0.00%)                      if dealt > 0 && let Some(b) = self.battlefield_find_mut(blocker_id) {
    2,228 ( 0.00%)                          b.dealt_damage_this_turn = true;
    2,228 ( 0.00%)                          b.damage_dealt_to_this_turn += dealt.max(0) as u32;
    6,684 ( 0.00%)                          b.damaged_by_this_turn.push(atk.id);
        .                               }
    8,912 ( 0.00%)                      if atk.has_infect || atk.has_wither {
        .                                   if dealt > 0
        .                                       && let Some(blocker) = self.battlefield_find_mut(blocker_id)
        .                                   {
        .                                       blocker.add_counters(
        .                                           crate::card::CounterType::MinusOneMinusOne,
        .                                           dealt as u32,
        .                                       );
        .                                       events.push(GameEvent::CounterAdded {
        .                                           card_id: blocker_id,
        .                                           counter_type: crate::card::CounterType::MinusOneMinusOne,
        .                                           count: dealt as u32,
        .                                       });
        .                                   }
    2,228 ( 0.00%)                      } else if dealt > 0
        .                                   && let Some(blocker) = self.battlefield_find_mut(blocker_id)
        .                               {
    2,228 ( 0.00%)                          blocker.damage += dealt as u32;
    8,912 ( 0.00%)                          blocker.record_damage_from(atk.id, dealt as u32);
  515,695 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::record_damage_from (2,208x)
    4,456 ( 0.00%)                          if atk.has_deathtouch {
      338 ( 0.00%)                              blocker.dealt_deathtouch_damage = true;
        .                                   }
   22,280 ( 0.00%)                          events.push(GameEvent::DamageDealt {
        .                                       amount: dealt as u32,
        .                                       to_player: None,
        .                                       to_card: Some(blocker_id),
        .                                       combat: true,
    2,228 ( 0.00%)                              from_controller: Some(atk.controller),
        .                                       from_card: Some(atk.id),
        .                                   });
        .                                   creature_damage.push((atk.id, blocker_id, dealt as u32));
        .                               }
        .                           }
        .           
        .                           let trample_leftover = total_power.saturating_sub(assigned_to_blockers);
    3,884 ( 0.00%)                  if leftover_to_player && trample_leftover > 0 {
        .                               // Trample-over damage to the defending player/PW is also
        .                               // subject to prevention shields; lifelink follows the
        .                               // post-prevention amount.
      350 ( 0.00%)                      let amount = self.prevent_combat_to_target(
  288,325 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::prevent_combat_to_target (70x)
      140 ( 0.00%)                          atk.target,
      140 ( 0.00%)                          self.scale_combat_damage(Some(atk.id), atk.target, trample_leftover),
        .                                   Some(atk.id),
        .                                   &mut events,
        .                               );
       70 ( 0.00%)                      lifelink_dealt += amount as i32;
      140 ( 0.00%)                      if amount > 0 {
      210 ( 0.00%)                          self.deal_combat_damage_to_target(atk, amount, &mut events);
  554,922 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::deal_combat_damage_to_target (70x)
        .                               }
        .                           }
        .           
    4,092 ( 0.00%)                  if atk.has_lifelink && lifelink_dealt > 0 {
      114 ( 0.00%)                      let a = self.active_player_idx;
        .                               let applied = self.adjust_life_applied(a, lifelink_dealt);
      228 ( 0.00%)                      if applied > 0 {
      342 ( 0.00%)                          events.push(GameEvent::LifeGained { player: a, amount: applied as u32 });
        .                               }
        .                           }
        .           
        .                           // Only blockers whose own keywords say they deal damage in
        .                           // this step strike back at the attacker. Per CR 510.4/510.5
        .                           // the attacker's keywords don't gate the blocker's strike
        .                           // step — a regular blocker must wait for the regular step
        .                           // even if the attacker has first strike.
-- line 3618 ----------------------------------------
-- line 3625 ----------------------------------------
        .                                   let ids: Vec<CardId> = blocker_ids
        .                                       .iter()
        .                                       .copied()
        .                                       .filter(|&bid| computed_of(bid)
        .                                           .is_some_and(|bc| blocker_filter(&bc.keywords)))
        .                                       // CR 614.9 — a Maze-of-Ith'd blocker deals no combat damage.
        .                                       .filter(|bid| !g.combat_damage_prevented_creatures.contains(bid))
        .                                       // CR 615.1 — "prevent all combat damage it would deal" (Azorius Ploy).
    4,444 ( 0.00%)                              .filter(|bid| !g.combat_damage_prevented_from(*bid))
1,529,714 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_from (2,222x)
        .                                       // CR 615.1 — fog (with Inspire Awe's per-dealer exception).
        .                                       .filter(|&bid| !g.combat_damage_prevented_for_dealer(bid))
        .                                       // CR 702.16e — a blocker whose color the attacker has
        .                                       // protection from deals no combat damage to it.
    6,666 ( 0.00%)                              .filter(|&bid| !g.damage_prevented_by_protection(bid, atk.id))
7,107,558 ( 0.23%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (2,222x)
        .                                       // CR 615 — Light of Sanction: your source → your creature.
        .                                       .filter(|&bid| {
    2,222 ( 0.00%)                                  !g.damage_from_your_source_to_your_creature_prevented(bid, atk.id)
        .                                       })
        .                                       // CR 615 — Indentured Oaf: a blocker's own damage to a
        .                                       // chosen color is prevented.
    6,666 ( 0.00%)                              .filter(|&bid| !g.source_damage_to_color_prevented(bid, atk.id))
  333,356 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::source_damage_to_color_prevented (2,222x)
        .                                       .collect();
        .                                   let takes =
        .                                       // CR 614.9 — a Maze-of-Ith'd attacker takes no combat damage.
        .                                       !g.combat_damage_prevented_creatures.contains(&atk.id)
        .                                       // CR 615 — Iroas shields attacking creatures you control.
        .                                       && !g.damage_to_attacker_prevented(atk.id)
        .                                       // CR 615 — Emmara shields your creature tokens.
        .                                       && !g.all_damage_to_creature_token_prevented(atk.id)
        .                                       // CR 615 — Rune-Tail's Essence shields all your creatures.
        .                                       && !g.all_damage_to_your_creature_prevented(atk.id);
    9,640 ( 0.00%)                          (ids, takes)
        .                               });
        .           
    7,688 ( 0.00%)                  if attacker_takes_strike_back {
        .                               // CR 702.90 / 615.6 — each blocker's strike-back is its
        .                               // own damage event: scaling (CR 614.2), prevention
        .                               // shields, infect/wither, deathtouch, and lifelink all
        .                               // apply per source, not to the summed total.
        .                               let mut lifelink_by_controller: crate::fxhash::HashMap<usize, i32> =
        .                                   crate::fxhash::HashMap::default();
    2,222 ( 0.00%)                      for &bid in &dealing_blocker_ids {
    2,222 ( 0.00%)                          let Some(bc) = computed_of(bid) else { continue };
        .                                   // CR 510.1e — a multi-block blocker only assigns this
        .                                   // attacker its share of the divided damage.
        .                                   // Same read-only prefix as the attacker side: both
        .                                   // calls are `&self` and `apply_prevention_shields` is
        .                                   // the first write, so one scope holds the pair.
    2,222 ( 0.00%)                          let Some(scaled) = self.with_frozen_layers(|g| {
    8,888 ( 0.00%)                              let power = g.blocker_damage_to(
   95,546 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::blocker_damage_to (2,222x)
        .                                           bid,
    2,222 ( 0.00%)                                  atk.id,
        .                                           combat_damage_value(bc).max(0) as u32,
        .                                       );
        .                                       (power != 0).then(|| {
   11,110 ( 0.00%)                                  g.double_creature_combat_damage(g.scale_damage_to(
14,242,727 ( 0.46%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::scale_damage_to (2,222x)
        .                                               Some(bid),
    8,888 ( 0.00%)                                      crate::game::effects::EntityRef::Permanent(atk.id),
        .                                               power,
        .                                           ))
        .                                       })
        .                                   }) else {
        .                                       continue;
        .                                   };
   13,332 ( 0.00%)                          let dmg = self.apply_prevention_shields(
12,291,676 ( 0.39%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::apply_prevention_shields (2,222x)
    6,666 ( 0.00%)                              crate::game::effects::EntityRef::Permanent(atk.id),
        .                                       scaled,
        .                                       Some(bid),
        .                                       &mut events,
        .                                   );
        .                                   // Ironscale Hydra replaces the blocker's strike-back
        .                                   // with a +1/+1 counter (blocker's lifelink sees 0).
    8,888 ( 0.00%)                          let dmg = self.ironscale_replace(atk.id, dmg as i32, &mut events) as u32;
9,247,721 ( 0.30%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ironscale_replace (2,222x)
        .                                   // CR 615 — an attacker that prevents all damage to itself,
        .                                   // or specifically damage from its blockers (Armored
        .                                   // Transport), takes none from this blocker (no lifelink).
    6,666 ( 0.00%)                          let dmg = if self.combat_damage_prevented_to_self(atk.id)
2,022,850 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::combat_damage_prevented_to_self (2,222x)
        .                                       || self.combat_damage_from_blockers_prevented(atk.id)
    8,888 ( 0.00%)                              || self.damage_from_source_prevented_by_keyword(atk.id, bid)
8,972,167 ( 0.29%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_from_source_prevented_by_keyword (2,222x)
        .                                   {
        .                                       0
        .                                   } else {
        .                                       dmg
        .                                   };
        .                                   if dmg == 0 {
        .                                       continue;
        .                                   }
        .                                   let infect = bc.keywords.contains(&Keyword::Infect)
        .                                       || bc.keywords.contains(&Keyword::Wither);
        .                                   let hit = self.turn_damage_redirect_for(atk.id).unwrap_or(atk.id);
        .                                   if let Some(attacker) = self.battlefield_find_mut(hit) {
    2,222 ( 0.00%)                              attacker.dealt_damage_this_turn = true;
    2,222 ( 0.00%)                              attacker.damage_dealt_to_this_turn += dmg;
    6,042 ( 0.00%)                              attacker.damaged_by_this_turn.push(bid);
    4,444 ( 0.00%)                              if infect {
        .                                           attacker
        .                                               .add_counters(crate::card::CounterType::MinusOneMinusOne, dmg);
        .                                           events.push(GameEvent::CounterAdded {
        .                                               card_id: hit,
        .                                               counter_type: crate::card::CounterType::MinusOneMinusOne,
        .                                               count: dmg,
        .                                           });
        .                                       } else {
    2,222 ( 0.00%)                                  attacker.damage += dmg;
    6,666 ( 0.00%)                                  attacker.record_damage_from(bid, dmg);
  447,178 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::record_damage_from (2,222x)
        .                                           if bc.keywords.contains(&Keyword::Deathtouch) {
      680 ( 0.00%)                                      attacker.dealt_deathtouch_damage = true;
        .                                           }
   19,998 ( 0.00%)                                  events.push(GameEvent::DamageDealt {
        .                                               amount: dmg,
        .                                               to_player: None,
        .                                               to_card: Some(hit),
        .                                               combat: true,
    4,444 ( 0.00%)                                      from_controller: Some(bc.controller),
        .                                               from_card: Some(bid),
        .                                           });
        .                                       }
        .                                   }
        .                                   // CR 510.2 — this blocker dealt combat damage to a
        .                                   // creature (post-prevention amount).
        .                                   creature_damage.push((bid, atk.id, dmg));
        .                                   // CR 702.15a — lifelink scales off damage actually
        .                                   // dealt; credited to the blocker's controller.
        .                                   if bc.keywords.contains(&Keyword::Lifelink) {
      508 ( 0.00%)                              let controller = self
        .                                           .battlefield
        .                                           .iter()
        .                                           .find(|c| c.id == bid)
    1,016 ( 0.00%)                                  .map(|c| c.controller)
        .                                           .unwrap_or(atk.defender_player);
      508 ( 0.00%)                              *lifelink_by_controller.entry(controller).or_insert(0) += dmg as i32;
        .                                   }
        .                               }
        .                               // Sort by seat for deterministic event ordering;
        .                               // life-gain math is commutative but the event log
        .                               // shouldn't shuffle across replays.
        .                               let mut lifelink_entries: Vec<(usize, i32)> =
    5,730 ( 0.00%)                          lifelink_by_controller.into_iter().collect();
        .                               lifelink_entries.sort_by_key(|(p, _)| *p);
    1,932 ( 0.00%)                      for (player, gained) in lifelink_entries {
      988 ( 0.00%)                          if gained > 0 {
        .                                       let applied = self.adjust_life_applied(player, gained);
      988 ( 0.00%)                              if applied > 0 {
    1,482 ( 0.00%)                                  events.push(GameEvent::LifeGained {
        .                                               player,
        .                                               amount: applied as u32,
        .                                           });
        .                                       }
        .                                   }
        .                               }
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 614 — a source with the exile-on-death damage rider (Kumano's
        .                   // Pupils, Kumano) exiles instead of buries any creature it damaged
        .                   // this turn that would die. Mirrors the non-combat path in
        .                   // `deal_damage_to_from`.
    8,900 ( 0.00%)          for &(source, damaged, _) in &creature_damage {
    4,450 ( 0.00%)              if self.damage_exiles_victim_eot.contains(&source)
    8,900 ( 0.00%)                  || self
        .                               .battlefield_find(source)
        .                               .is_some_and(|c| c.definition.damage_exiles_if_dies)
        .                       {
        .                           self.dies_to_exile_eot.insert(damaged);
        .                       }
        .                       // Runesword — its damage also denies regeneration this turn.
    4,450 ( 0.00%)              if self.damage_denies_regen_eot.contains(&source)
        .                           && let Some(v) = self.battlefield_find_mut(damaged)
        .                       {
        .                           v.cant_regenerate_this_turn = true;
        .                       }
        .                   }
        .                   // Stamp the damaging source's controller on each recipient so a
        .                   // "whenever this is dealt combat damage" trigger can still name the
        .                   // attacking player once `block_map` is torn down (Souls of the Faultless).
    8,900 ( 0.00%)          for &(source, damaged, _) in &creature_damage {
    4,450 ( 0.00%)              if let Some(ctrl) = self.battlefield_find(source).map(|c| c.controller)
        .                           && let Some(c) = self.battlefield_find_mut(damaged)
        .                       {
   13,330 ( 0.00%)                  c.combat_damager_controller = Some(ctrl);
        .                       }
        .                   }
        .                   // CR 510.2 — now that all combat damage in this step has been dealt,
        .                   // put `DealsCombatDamageToCreature` triggers on the stack.
   16,088 ( 0.00%)          for (source, damaged, amount) in creature_damage {
   17,800 ( 0.00%)              self.fire_source_dealt_damage_watchers(source, amount);
  420,850 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_source_dealt_damage_watchers (4,430x)
   31,150 ( 0.00%)              self.fire_combat_damage_to_creature_triggers(source, damaged, amount);
18,024,999 ( 0.58%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_creature_triggers (4,430x)
        .                   }
        .           
    7,746 ( 0.00%)          Ok(events)
        .               }
        .           
        .               /// CR 506.2 — whether `id` carries a computed `Keyword::CantBeAttacked`,
        .               /// so it can't be declared as an attack target (The Aetherspark).
        .               pub(crate) fn permanent_cant_be_attacked(&self, id: CardId) -> bool {
        .                   self.computed_permanent(id)
        .                       .is_some_and(|cp| cp.keywords.contains(&Keyword::CantBeAttacked))
        .               }
-- line 3823 ----------------------------------------
-- line 3833 ----------------------------------------
        .               /// Target-aware scaling for a combat-damage event aimed at a player or
        .               /// planeswalker attack target (CR 614.2 / 614.5). `source` is the
        .               /// attacking creature (Torbran's source-scoped bonus).
        .               /// CR 615.1 — is `dealer`'s combat damage prevented this turn? True when
        .               /// the global fog flag is set, unless an exception filter (Inspire Awe)
        .               /// is in play and `dealer` matches it (enchanted / enchantment creatures
        .               /// still deal damage).
        .               pub(crate) fn combat_damage_prevented_for_dealer(&self, dealer: CardId) -> bool {
   18,764 ( 0.00%)          if !self.prevent_combat_damage_this_turn {
        .                       return false;
        .                   }
        .                   // CR 615.12 (scoped) — Questing Beast: combat damage dealt by creatures
        .                   // its controller controls can't be prevented, so fog doesn't stop it.
        .                   if let Some(ctrl) = self.battlefield_find(dealer).map(|c| c.controller)
        .                       && self.battlefield.iter().any(|c| {
        .                           c.controller == ctrl
        .                               && c.definition.static_abilities.iter().any(|sa| {
-- line 3849 ----------------------------------------
-- line 3875 ----------------------------------------
        .                       })
        .                   })
        .               }
        .           
        .               /// Apply the Ironscale replacement to `recipient` taking `dealt` combat
        .               /// damage: if it has the static and `dealt > 0`, add one +1/+1 counter,
        .               /// emit the event, and return 0 (the damage is prevented); otherwise
        .               /// return `dealt` unchanged.
   31,150 ( 0.00%)      fn ironscale_replace(&mut self, recipient: CardId, dealt: i32, events: &mut Vec<GameEvent>) -> i32 {
    8,900 ( 0.00%)          if dealt <= 0 {
        .                       return dealt;
        .                   }
        .                   // CR 614.9 — Treacherous Link redirects combat damage too.
   13,350 ( 0.00%)          if let Some(owner) = self.creature_redirects_damage_to_controller(recipient) {
17,052,977 ( 0.55%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::creature_redirects_damage_to_controller (4,450x)
        .                       self.deal_damage_to_from(
        .                           crate::game::effects::EntityRef::Player(owner),
        .                           dealt as u32,
        .                           None,
        .                           events,
        .                       );
        .                       return 0;
        .                   }
        .                   // Sekki trades counters for tokens instead of growing (CR 615).
   40,050 ( 0.00%)          if self.trade_counters_for_damage(recipient, dealt as u32, events) {
  630,198 ( 0.02%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::trade_counters_for_damage (4,450x)
        .                       return 0;
        .                   }
        .                   // Ironscale Hydra grows by exactly one; Phytohydra grows by the full
        .                   // amount. Both are replacements (CR 614), so they apply even when
        .                   // damage can't be prevented.
    8,900 ( 0.00%)          let (kind, grow) = if self.creature_prevents_combat_damage_grows(recipient) {
        .                       (crate::card::CounterType::PlusOnePlusOne, 1)
        .                   } else if let Some(kind) = self.creature_replaces_damage_with_counters(recipient) {
        .                       (kind, dealt as u32)
        .                   } else {
        .                       return dealt;
        .                   };
        .                   if let Some(c) = self.battlefield_find_mut(recipient) {
        .                       c.add_counters(kind, grow);
        .                   }
        .                   events.push(GameEvent::CounterAdded {
        .                       card_id: recipient,
        .                       counter_type: kind,
        .                       count: grow,
        .                   });
        .                   0
   31,150 ( 0.00%)      }
        .           
        .               /// Goblin Psychopath — "the next time this would deal combat damage this
        .               /// turn, it deals that damage to you instead". Consumes the charge and
        .               /// returns the creature's controller.
        .               pub(crate) fn take_combat_damage_diversion(&mut self, id: CardId) -> Option<usize> {
    4,550 ( 0.00%)          let idx = self.next_combat_damage_to_controller.iter().position(|c| *c == id)?;
        .                   self.next_combat_damage_to_controller.remove(idx);
        .                   self.battlefield_find(id).map(|c| c.controller)
        .               }
        .           
        .               /// CR 614.9 — Treacherous Link: does `id` redirect damage aimed at it onto
        .               /// its controller? Returns that controller when it does.
   31,192 ( 0.00%)      pub(crate) fn creature_redirects_damage_to_controller(&self, id: CardId) -> Option<usize> {
        .                   let c = self.battlefield_find(id)?;
    8,908 ( 0.00%)          self.computed_permanent(id)?
15,612,371 ( 0.50%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,454x)
        .                       .keywords
        .                       .contains(&crate::card::Keyword::DamageToThisGoesToItsController)
        .                       .then_some(c.controller)
   44,560 ( 0.00%)      }
        .           
        .               /// Sekki, Seasons' Guide (CR 615) — prevent `dealt` damage to `recipient`,
        .               /// remove that many counters, and mint that many tokens. Returns false
        .               /// (damage stands) when `recipient` has no such static.
   71,296 ( 0.00%)      pub(crate) fn trade_counters_for_damage(
        .                   &mut self,
        .                   recipient: CardId,
        .                   dealt: u32,
        .                   events: &mut Vec<GameEvent>,
        .               ) -> bool {
   17,824 ( 0.00%)          if dealt == 0 || self.damage_cant_be_prevented_this_turn {
        .                       return false;
        .                   }
        .                   let Some((counter, token)) = self.battlefield_find(recipient).and_then(|c| {
        .                       c.definition.static_abilities.iter().find_map(|s| match &s.effect {
        .                           crate::effect::StaticEffect::PreventDamageToSelfTradingCounters {
        .                               counter,
        .                               token,
        .                           } => Some((*counter, (**token).clone())),
-- line 3958 ----------------------------------------
-- line 3976 ----------------------------------------
        .                           count: removed,
        .                       });
        .                   }
        .                   let def = crabomination_base::tokens::token_to_card_definition(&token);
        .                   for _ in 0..dealt {
        .                       self.mint_token_onto_battlefield(def.clone(), controller, false, events);
        .                   }
        .                   true
   35,648 ( 0.00%)      }
        .           
        .               /// Phytohydra (CR 614) — does `id` replace incoming damage with that many
        .               /// +1/+1 counters? Reads `StaticEffect::ReplaceDamageToSelfWithCounters`.
        .               pub(crate) fn creature_replaces_damage_with_counters(
        .                   &self,
        .                   id: CardId,
        .               ) -> Option<crate::card::CounterType> {
        .                   self.battlefield_find(id).and_then(|c| {
-- line 3992 ----------------------------------------
-- line 3999 ----------------------------------------
        .           
        .               fn scale_combat_damage(
        .                   &self,
        .                   source: Option<crate::card::CardId>,
        .                   target: AttackTarget,
        .                   amount: u32,
        .               ) -> u32 {
        .                   use crate::game::effects::EntityRef;
    8,008 ( 0.00%)          let ent = match target {
    5,376 ( 0.00%)              AttackTarget::Player(p) => EntityRef::Player(p),
        .                       AttackTarget::Planeswalker(pw) => EntityRef::Permanent(pw),
        .                       AttackTarget::Battle(b) => EntityRef::Permanent(b),
        .                   };
   10,822 ( 0.00%)          self.scale_damage_to(source, ent, amount)
23,984,426 ( 0.77%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::scale_damage_to (2,632x)
        .               }
        .           
   26,880 ( 0.00%)      fn prevent_combat_to_target(
        .                   &mut self,
        .                   target: AttackTarget,
        .                   amount: u32,
        .                   source: Option<crate::card::CardId>,
        .                   events: &mut Vec<GameEvent>,
        .               ) -> u32 {
        .                   use crate::game::effects::EntityRef;
    8,064 ( 0.00%)          match target {
    2,688 ( 0.00%)              AttackTarget::Player(p) => {
        .                           // CR 615 — Glacial-Chasm-style blanket prevention soaks the whole
        .                           // combat hit before any shield is consumed.
   16,128 ( 0.00%)                  if self.all_damage_to_player_prevented(p) {
6,011,948 ( 0.19%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::all_damage_to_player_prevented (2,688x)
        .                               return 0;
        .                           }
        .                           // CR 615 — turn-scoped "prevent all combat damage that would be
        .                           // dealt to you this turn" (Druid's Deliverance).
    5,376 ( 0.00%)                  if !self.damage_cant_be_prevented_this_turn
    2,688 ( 0.00%)                      && self.combat_damage_prevented_to_players_this_turn.contains(&p)
        .                           {
        .                               return 0;
        .                           }
        .                           // CR 702.16j — protection from a card type (Serra's Emissary):
        .                           // no combat damage from an attacker of that type.
        .                           if let Some(src) = source {
    5,376 ( 0.00%)                      let types = self.player_protection_card_types(p);
    5,376 ( 0.00%)                      if !types.is_empty()
        .                                   && self
        .                                       .computed_permanent(src)
        .                                       .is_some_and(|c| types.iter().any(|t| c.card_types.contains(t)))
        .                               {
        .                                   return 0;
        .                               }
        .                           }
   13,440 ( 0.00%)                  self.apply_prevention_shields(EntityRef::Player(p), amount, source, events)
        .                       }
        .                       AttackTarget::Planeswalker(pw) => {
        .                           self.apply_prevention_shields(EntityRef::Permanent(pw), amount, source, events)
        .                       }
        .                       AttackTarget::Battle(b) => {
        .                           self.apply_prevention_shields(EntityRef::Permanent(b), amount, source, events)
        .                       }
        .                   }
   21,504 ( 0.00%)      }
        .           
        .               /// CR 615 — total per-creature combat-damage reduction `p` gets from
        .               /// their untapped `ReduceCombatDamageToControllerWhileUntapped` permanents
        .               /// (Thunderstaff). Applies once per damaging creature, not once per point.
        .               fn combat_damage_shaved_for(&self, p: usize) -> u32 {
        .                   self.battlefield
        .                       .iter()
  110,836 ( 0.00%)              .filter(|c| c.controller == p && !c.tapped)
        .                       .flat_map(|c| c.definition.static_abilities.iter())
        .                       .filter_map(|s| match s.effect {
        .                           crate::effect::StaticEffect::ReduceCombatDamageToControllerWhileUntapped(n) => {
        .                               Some(n)
        .                           }
        .                           _ => None,
        .                       })
        .                       .sum()
        .               }
        .           
   29,568 ( 0.00%)      fn deal_combat_damage_to_target(
        .                   &mut self,
        .                   atk: &AttackerInfo,
        .                   amount: u32,
        .                   events: &mut Vec<GameEvent>,
        .               ) {
   10,752 ( 0.00%)          match atk.target {
    2,688 ( 0.00%)              AttackTarget::Player(p) => {
        .                           // CR 615 — per-creature combat-damage shaving from an untapped
        .                           // permanent the defender controls (Thunderstaff).
    2,688 ( 0.00%)                  let shave = self.combat_damage_shaved_for(p);
        .                           let amount = amount.saturating_sub(shave);
    2,688 ( 0.00%)                  if amount == 0 && shave > 0 {
        .                               return;
        .                           }
        .                           // CR 614 — Szadek: this attacker's combat damage to a player
        .                           // becomes that many +1/+1 counters on it, and the player mills
        .                           // that many instead of losing life.
        .                           if amount > 0
    5,376 ( 0.00%)                      && self.battlefield_find(atk.id).is_some_and(|c| {
        .                                   c.definition.static_abilities.iter().any(|s| {
        .                                       matches!(
        .                                           s.effect,
        .                                           crate::effect::StaticEffect::CombatDamageToPlayerBecomesCountersAndMill
        .                                       )
        .                                   })
        .                               })
        .                           {
-- line 4104 ----------------------------------------
-- line 4121 ----------------------------------------
        .                                   }
        .                               }
        .                               return;
        .                           }
        .                           // CR 614.9 — Palisade-Giant-style redirect: combat damage
        .                           // aimed at the player lands on the redirector instead. Turn
        .                           // the Tables' combat-only redirect is checked first; both
        .                           // require the destination to still be on the battlefield.
    8,064 ( 0.00%)                  if let Some(redirect) = self
        .                               .combat_damage_redirect_this_turn
        .                               .iter()
        .                               .find(|(seat, to)| *seat == p && self.battlefield_find(*to).is_some())
        .                               .map(|(_, to)| *to)
        .                               .or_else(|| {
   16,128 ( 0.00%)                          self.damage_redirect_target(crate::game::effects::EntityRef::Player(p))
1,156,892 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::damage_redirect_target (2,688x)
        .                               })
        .                           {
        .                               if let Some(c) = self.battlefield_find_mut(redirect) {
        .                                   c.damage += amount;
        .                                   c.dealt_damage_this_turn = true;
        .                                   c.damage_dealt_to_this_turn += amount;
        .                                   c.damaged_by_this_turn.push(atk.id);
        .                                   c.record_damage_from(atk.id, amount);
-- line 4143 ----------------------------------------
-- line 4148 ----------------------------------------
        .                                   to_card: Some(redirect),
        .                                   combat: true,
        .                                   from_controller: Some(atk.controller),
        .                                   from_card: Some(atk.id),
        .                               });
        .                               return;
        .                           }
        .                           // Phyrexian Unlife — at ≤ 0 life all damage lands as poison.
   16,638 ( 0.00%)                  if atk.has_infect || (self.players[p].life <= 0 && self.player_unlife_active(p)) {
   20,858 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::player_unlife_active (102x)
        .                               self.add_poison(p, amount, events);
        .                           } else {
        .                               // Angel's Grace / Worship — damage lands in full, but the
        .                               // life reduction is clamped to the floor.
   13,440 ( 0.00%)                      let life_delta = self.clamp_damage_to_life_floor(p, amount);
  532,564 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::clamp_damage_to_life_floor (2,688x)
    2,688 ( 0.00%)                      let applied = self.adjust_life_applied(p, -(life_delta as i32));
   24,192 ( 0.00%)                      events.push(GameEvent::DamageDealt {
        .                                   amount,
        .                                   to_player: Some(p),
        .                                   to_card: None,
        .                                   combat: true,
    5,376 ( 0.00%)                          from_controller: Some(atk.controller),
        .                                   from_card: Some(atk.id),
        .                               });
        .                               let amount = (-applied).max(0) as u32;
    8,064 ( 0.00%)                      events.push(GameEvent::LifeLost {
        .                                   player: p,
        .                                   amount,
        .                               });
        .                           }
        .                           // Mark the player damaged this turn (Bloodthirst window, CR
        .                           // 702.54) and record the attacker so "destroy target creature
        .                           // that dealt damage to you this turn" (Spear of Heliod) can
        .                           // filter targets.
        .                           if amount > 0 {
    5,376 ( 0.00%)                      self.players[p].was_dealt_damage_this_turn = true;
   80,640 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,688x)
    5,376 ( 0.00%)                      self.players[p].damage_taken_this_turn =
   80,640 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,688x)
    2,688 ( 0.00%)                          self.players[p].damage_taken_this_turn.saturating_add(amount);
    5,376 ( 0.00%)                      self.players[p].combat_damage_taken_this_turn =
   80,640 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,688x)
    2,688 ( 0.00%)                          self.players[p].combat_damage_taken_this_turn.saturating_add(amount);
    2,688 ( 0.00%)                      if !self.players[p].creatures_that_damaged_me_this_turn.contains(&atk.id) {
    9,096 ( 0.00%)                          self.players[p].creatures_that_damaged_me_this_turn.push(atk.id);
   80,640 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,688x)
        .                               }
        .                               // CR 702.76 — Prowl window: record the damaging creature's
        .                               // types for its controller (Changeling counts as every
        .                               // type, recorded via the controller-side any flag).
    2,688 ( 0.00%)                      if let Some(c) = self.battlefield.iter().find(|c| c.id == atk.id) {
    5,376 ( 0.00%)                          let ctrl = c.controller;
        .                                   if c.definition.keywords.contains(&Keyword::Changeling) {
        .                                       self.players[ctrl].prowl_any_type_this_turn = true;
        .                                   }
        .                                   let types = c.definition.subtypes.creature_types.clone();
        .                                   for t in types {
    3,922 ( 0.00%)                              if !self.players[ctrl].prowl_types_this_turn.contains(&t) {
   10,548 ( 0.00%)                                  self.players[ctrl].prowl_types_this_turn.push(t);
  121,114 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,364x)
        .                                       }
        .                                   }
        .                               }
        .                           }
        .                           // CR 702.180c — Toxic N adds N poison on combat damage to a
        .                           // player, on top of any life loss (and stacks with Infect's
        .                           // poison). Only when damage was actually dealt.
    8,064 ( 0.00%)                  if atk.toxic > 0 && amount > 0 {
        .                               self.add_poison(p, atk.toxic, events);
        .                           }
        .                           // Phase M: bump the 21-commander-damage tally when the
        .                           // attacker is a Commander. Both Infect and regular
        .                           // damage paths credit here — CR 704.5v doesn't restrict
        .                           // by damage type. The SBA in `check_state_based_actions`
        .                           // reads this table and eliminates the player when any
        .                           // single (victim, commander) entry crosses 21.
-- line 4217 ----------------------------------------
-- line 4232 ----------------------------------------
        .                           if amount > 0 && self.initiative == Some(p) {
        .                               let ctrl = self.battlefield.iter()
        .                                   .find(|c| c.id == atk.id).map(|c| c.controller);
        .                               if let Some(ctrl) = ctrl
        .                                   && ctrl != p {
        .                                       self.take_initiative(ctrl, events);
        .                                   }
        .                           }
   32,256 ( 0.00%)                  self.fire_combat_damage_to_player_triggers(atk.id, p, amount);
12,460,545 ( 0.40%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_to_player_triggers (2,688x)
        .                       }
        .                       AttackTarget::Planeswalker(pw_id) => {
        .                           // CR 702.19c — "trample over planeswalkers" (Thrasta): the
        .                           // attacker assigns lethal (= remaining loyalty) to the
        .                           // planeswalker and the excess to its controller. Plain
        .                           // trample never spills past a planeswalker (CR 702.19f).
        .                           let mut spill = 0u32;
        .                           let mut spill_to: Option<usize> = None;
-- line 4248 ----------------------------------------
-- line 4350 ----------------------------------------
        .                               )
        .                       })
        .                       .map(|c| c.id)
        .               }
        .           
        .               /// CR 508.1a / 509.1a — the "can't attack or block unless its controller
        .               /// pays {N}" tax carried by `id`'s own computed keywords. Shared by the
        .               /// declare-attackers and declare-blockers payment loops.
  127,728 ( 0.00%)      pub fn attack_block_keyword_tax(
        .                   &self,
        .                   id: CardId,
        .                   keywords: &[Keyword],
        .                   for_attack: bool,
        .               ) -> u32 {
        .                   keywords
        .                       .iter()
   87,240 ( 0.00%)              .map(|k| match k {
        .                           Keyword::CantAttackOrBlockUnlessPay(n) => *n,
        .                           // Brainwash — attack-only, so it's excluded at the block site.
        .                           Keyword::CantAttackUnlessPay(n) if for_attack => *n,
        .                           // Myr Prototype — the tax is its own counter count.
        .                           Keyword::CantAttackOrBlockUnlessPayPerCounter(kind) => {
        .                               self.battlefield_find(id).map(|c| c.counter_count(*kind)).unwrap_or(0)
        .                           }
        .                           // Cowed by Wisdom — one per card in each granting Aura's
-- line 4374 ----------------------------------------
-- line 4390 ----------------------------------------
        .                           Keyword::CantAttackOrBlockUnlessPayPerPermanent(filter) => self
        .                               .battlefield
        .                               .iter()
        .                               .filter(|c| self.evaluate_requirement_on_card(filter, c, c.controller))
        .                               .count() as u32,
        .                           _ => 0,
        .                       })
        .                       .sum()
  113,536 ( 0.00%)      }
        .           
        .               /// Push triggered abilities of `source` whose event spec is
        .               /// `DealsCombatDamageToPlayer` onto the stack, with `damaged_player`
        .               /// stored as the trigger's target so the effect can refer to "that
        .               /// player" via `PlayerRef::Target(0)`.
        .               ///
        .               /// Phase 1 walks the battlefield for the attacker's own
        .               /// `SelfSource` / `AnyPlayer` triggers (the printed
-- line 4406 ----------------------------------------
-- line 4408 ----------------------------------------
        .               ///
        .               /// Phase 2 walks every player's graveyard for `FromYourGraveyard`
        .               /// triggers whose controller (the gy owner) matches the source's
        .               /// controller — the "whenever your creatures deal combat damage,
        .               /// return this card from your graveyard" pattern used by Killian's
        .               /// Confidence and friends. The trigger source is bound to the
        .               /// graveyard card itself so a `Move(SelfSource → Hand)` body
        .               /// returns the right card.
   32,256 ( 0.00%)      pub fn fire_combat_damage_to_player_triggers(
        .                   &mut self,
        .                   source: CardId,
        .                   damaged_player: usize,
        .                   damage_amount: u32,
        .               ) {
        .                   // CR 702.179 — the dealing creature's controller has now dealt combat
        .                   // damage to a player this turn (Freerunning's alt-cost gate).
        .                   if let Some(c) = self.battlefield_find(source) {
    5,376 ( 0.00%)              let controller = c.controller;
    5,376 ( 0.00%)              self.players[controller].dealt_combat_damage_to_player_this_turn = true;
   80,640 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,688x)
        .                       // CR 603.4 — turn-scoped "whenever a creature you control deals
        .                       // combat damage to a player" delayed triggers (Mistway Spy).
        .                       let watchers: Vec<crate::game::types::DelayedTrigger> = self
        .                           .delayed_triggers
        .                           .iter()
        .                           .filter(|dt| {
        .                               dt.controller == controller
        .                                   && matches!(
        .                                       dt.kind,
        .                                       crate::game::types::DelayedKind::CreatureYouControlDealsCombatDamageThisTurn
        .                                   )
        .                           })
        .                           .cloned()
        .                           .collect();
   13,440 ( 0.00%)              for dt in watchers {
        .                           self.stack.push(
        .                               TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                                   .trigger_source(Some(crate::game::effects::EntityRef::Permanent(source)))
        .                                   .event_amount(damage_amount)
        .                                   .build(),
        .                           );
        .                       }
        .                   }
   13,440 ( 0.00%)          self.fire_source_dealt_damage_watchers(source, damage_amount);
  255,360 ( 0.01%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_source_dealt_damage_watchers (2,688x)
        .                   self.fire_source_combat_damage_to_player_watchers(source, damage_amount);
        .                   let granted = self.static_granted_triggers_of(source);
        .                   // Combat damage is damage: the combat-agnostic wordings fire too, in
        .                   // this order (CR 603.2 — one batch, grouped per kind).
   26,880 ( 0.00%)          self.fire_combat_damage_triggers(
8,165,511 ( 0.26%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers (2,688x)
        .                       source,
        .                       &[
        .                           EventKind::DealsCombatDamageToPlayer,
        .                           EventKind::DealsDamageToPlayer,
        .                           EventKind::DealsCombatDamage,
        .                           EventKind::DealsDamage,
        .                       ],
    5,376 ( 0.00%)              Target::Player(damaged_player),
        .                       damage_amount,
        .                       &granted,
        .                   );
        .                   // CR 510 — "whenever combat damage is dealt to you" listeners fire off
        .                   // the *recipient's* own permanents (SelfSource on a permanent the
        .                   // damaged player controls). Risona sheds an indestructible counter.
    2,688 ( 0.00%)          let listeners: Vec<(CardId, Effect, usize)> = self
        .                       .battlefield
        .                       .iter()
  166,254 ( 0.01%)              .filter(|c| c.controller == damaged_player)
        .                       .flat_map(|c| {
        .                           c.definition
        .                               .triggered_abilities
        .                               .iter()
        .                               .filter(|ta| {
        .                                   ta.event.kind == EventKind::ControllerDealtCombatDamage
        .                                       && ta.event.scope == crate::effect::EventScope::SelfSource
        .                               })
        .                               .map(move |ta| (c.id, ta.effect.clone(), c.controller))
        .                       })
        .                       .collect();
   13,440 ( 0.00%)          for (listener, effect, controller) in listeners {
        .                       let auto_target = self.auto_target_for_effect_avoiding(&effect, controller, Some(listener));
        .                       // Bind the creature that dealt the damage as `Selector::TriggerSource`
        .                       // so "whenever a creature deals combat damage to you, destroy it"
        .                       // clauses can reference the dealer (Teysa, Envoy of Ghosts).
        .                       self.stack.push(
        .                           TriggerPush::new(listener, controller, effect)
        .                               .target(auto_target)
        .                               .trigger_source(Some(crate::game::effects::EntityRef::Permanent(source)))
        .                               .event_amount(damage_amount)
        .                               .build(),
        .                       );
        .                   }
   21,504 ( 0.00%)      }
        .           
        .               /// CR 603.4 — fire any `SourceDealsDamageThisTurn` delayed triggers
        .               /// watching `source` (Paladin of Prahv's Forecast rider). Called from every
        .               /// damage-delivery path — combat to a player/creature and noncombat — so the
        .               /// watcher sees *any* damage the creature deals. The amount rides in via
        .               /// `Value::TriggerEventAmount`.
   65,808 ( 0.00%)      pub(crate) fn fire_source_dealt_damage_watchers(&mut self, source: CardId, amount: u32) {
   14,624 ( 0.00%)          if amount == 0 {
        .                       return;
        .                   }
        .                   let watchers: Vec<crate::game::types::DelayedTrigger> = self
        .                       .delayed_triggers
        .                       .iter()
        .                       .filter(|dt| {
        .                           matches!(dt.kind, crate::game::types::DelayedKind::SourceDealsDamageThisTurn(id) if id == source)
        .                       })
        .                       .cloned()
        .                       .collect();
   36,560 ( 0.00%)          for dt in watchers {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .trigger_source(Some(crate::game::effects::EntityRef::Permanent(source)))
        .                               .event_amount(amount)
        .                               .build(),
        .                       );
        .                   }
   58,496 ( 0.00%)      }
        .           
        .               /// CR 603.4 — fire any `SourceDealsCombatDamageToPlayerThisTurn` delayed
        .               /// triggers watching `source` (Captain Howler's pumped creature).
        .               pub(crate) fn fire_source_combat_damage_to_player_watchers(
        .                   &mut self,
        .                   source: CardId,
        .                   amount: u32,
        .               ) {
    5,376 ( 0.00%)          if amount == 0 {
        .                       return;
        .                   }
        .                   let watchers: Vec<crate::game::types::DelayedTrigger> = self
        .                       .delayed_triggers
        .                       .iter()
        .                       .filter(|dt| {
        .                           matches!(
        .                               dt.kind,
        .                               crate::game::types::DelayedKind::SourceDealsCombatDamageToPlayerThisTurn(id)
        .                                   if id == source
        .                           )
        .                       })
        .                       .cloned()
        .                       .collect();
   13,440 ( 0.00%)          for dt in watchers {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .trigger_source(Some(crate::game::effects::EntityRef::Permanent(source)))
        .                               .event_amount(amount)
        .                               .build(),
        .                       );
        .                   }
        .               }
        .           
        .               /// CR 603.4 — fire any `YouGainLifeThisTurn` delayed triggers whose
        .               /// controller is `player` (Vizkopa Guildmage's "whenever you gain life,
        .               /// each opponent loses that much"). The gained amount rides in via
        .               /// `Value::TriggerEventAmount`.
   11,862 ( 0.00%)      pub(crate) fn fire_life_gained_watchers(&mut self, player: usize, amount: u32) {
    2,636 ( 0.00%)          if amount == 0 {
        .                       return;
        .                   }
        .                   let watchers: Vec<crate::game::types::DelayedTrigger> = self
        .                       .delayed_triggers
        .                       .iter()
        .                       .filter(|dt| {
        .                           dt.controller == player
        .                               && matches!(dt.kind, crate::game::types::DelayedKind::YouGainLifeThisTurn)
        .                       })
        .                       .cloned()
        .                       .collect();
    6,590 ( 0.00%)          for dt in watchers {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .event_amount(amount)
        .                               .build(),
        .                       );
        .                   }
   10,544 ( 0.00%)      }
        .           
        .               /// CR 603.4 — fire `CardEntersOpponentGraveyardThisTurn` delayed triggers
        .               /// (Duskmantle Guildmage) when a card is put into `owner`'s graveyard, for
        .               /// each watcher that treats `owner` as an opponent. The owner is bound as
        .               /// the body's `Target(0)` so "that player loses 1 life" is exact.
   48,690 ( 0.00%)      pub(crate) fn fire_opponent_graveyard_watchers(&mut self, owner: usize) {
        .                   let watchers: Vec<crate::game::types::DelayedTrigger> = self
        .                       .delayed_triggers
        .                       .iter()
        .                       .filter(|dt| {
        .                           matches!(
        .                               dt.kind,
        .                               crate::game::types::DelayedKind::CardEntersOpponentGraveyardThisTurn
        .                           ) && self.opponents_of(dt.controller).contains(&owner)
        .                       })
        .                       .cloned()
        .                       .collect();
   27,050 ( 0.00%)          for dt in watchers {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .target(Some(Target::Player(owner)))
        .                               .build(),
        .                       );
        .                   }
   43,280 ( 0.00%)      }
        .           
        .               /// Push triggered abilities of `source` whose event spec is
        .               /// `DealsCombatDamageToCreature` onto the stack, binding the damaged
        .               /// creature to the trigger's target so "destroy / exile / -1/-1 that
        .               /// creature" payoffs and equipment charge-triggers (Umezawa's Jitte)
        .               /// resolve correctly. CR 510.2. Fires once per (source, damaged-creature)
        .               /// pair; an equipped creature blocked by several creatures therefore
        .               /// charges Jitte once per blocker (a minor over-count for the rare
        .               /// multi-block case).
   44,500 ( 0.00%)      pub(crate) fn fire_combat_damage_to_creature_triggers(
        .                   &mut self,
        .                   source: CardId,
        .                   damaged_creature: CardId,
        .                   damage_amount: u32,
        .               ) {
        .                   let granted = self.static_granted_triggers_of(source);
        .                   // Combat damage is damage: the combat-agnostic wordings fire too.
   40,050 ( 0.00%)          self.fire_combat_damage_triggers(
14,583,277 ( 0.47%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers (4,450x)
        .                       source,
        .                       &[
        .                           EventKind::DealsCombatDamageToCreature,
        .                           EventKind::DealsDamageToCreature,
        .                           EventKind::DealsCombatDamage,
        .                           EventKind::DealsDamage,
        .                       ],
    8,900 ( 0.00%)              Target::Permanent(damaged_creature),
        .                       damage_amount,
        .                       &granted,
        .                   );
   35,600 ( 0.00%)      }
        .           
        .               /// The non-combat half of `EventKind::DealsDamageToCreature`: a permanent
        .               /// source that just dealt non-combat damage to a creature fires the same
        .               /// source-scoped triggers, with the damaged creature bound to slot 0.
       32 ( 0.00%)      pub(crate) fn fire_noncombat_damage_to_creature_triggers(
        .                   &mut self,
        .                   source: CardId,
        .                   damaged_creature: CardId,
        .                   damage_amount: u32,
        .               ) {
        .                   if self.battlefield_find(source).is_none() {
        .                       return;
        .                   }
        .                   let granted = self.static_granted_triggers_of(source);
       32 ( 0.00%)          self.fire_combat_damage_triggers(
    9,780 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers (4x)
        .                       source,
        .                       &[EventKind::DealsDamageToCreature, EventKind::DealsDamage],
        8 ( 0.00%)              Target::Permanent(damaged_creature),
        .                       damage_amount,
        .                       &granted,
        .                   );
       32 ( 0.00%)      }
        .           
        .               /// The non-combat, player-side half of `EventKind::DealsDamage` /
        .               /// `DealsDamageToPlayer`: a permanent that just burned a player fires its
        .               /// own damage triggers with that player bound to slot 0.
    1,344 ( 0.00%)      pub(crate) fn fire_noncombat_damage_to_player_triggers(
        .                   &mut self,
        .                   source: CardId,
        .                   damaged_player: usize,
        .                   damage_amount: u32,
        .               ) {
        .                   if self.battlefield_find(source).is_none() {
        .                       return;
        .                   }
        .                   let granted = self.static_granted_triggers_of(source);
       16 ( 0.00%)          self.fire_combat_damage_triggers(
    6,480 ( 0.00%)  => crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers (2x)
        .                       source,
        .                       &[EventKind::DealsDamageToPlayer, EventKind::DealsDamage],
        4 ( 0.00%)              Target::Player(damaged_player),
        .                       damage_amount,
        .                       &granted,
        .                   );
    1,344 ( 0.00%)      }
        .           
        .               /// Shared body for the combat-damage trigger dispatch (to a player or to a
        .               /// creature). Walks the attacker's printed `SelfSource`/`AnyPlayer`
        .               /// triggers, equipment- and soulbond-granted triggers (CR 702.6e / 702.95),
        .               /// `YourControl`-scope listeners, and `FromYourGraveyard` triggers, pushing
        .               /// each onto the stack with `default_target` bound to slot 0.
        .               /// The `GrantTriggeredAbility` set a battlefield permanent currently
        .               /// carries — empty when it has left. Board-level, so the damage-trigger
        .               /// callers build it once per damage event.
        .               fn static_granted_triggers_of(&self, source: CardId) -> Vec<crate::card::TriggeredAbility> {
    7,138 ( 0.00%)          self.battlefield
        .                       .iter()
        .                       .find(|c| c.id == source)
   18,780 ( 0.00%)              .map(|c| self.statics_granted_triggers_for(c))
    2,352 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_for (4x)
        .                       .unwrap_or_default()
        .               }
        .           
        .               /// `static_granted` is the source's `GrantTriggeredAbility` set, which is
        .               /// board-level and *kind-independent* — the callers below fire four event
        .               /// kinds per damage event, so they build it once and pass it in rather
        .               /// than making each call rebuild `trigger_grant_sources` (a whole-board
        .               /// scan) for the same permanent.
        .               ///
        .               /// `kinds` is the caller's whole kind list for one damage event, for the
        .               /// same reason: every walk below except the graveyard one is
        .               /// kind-independent, so a per-kind call re-walked the battlefield four
        .               /// times for equipment, auras, soulbond and the `YourControl` /
        .               /// `AnyPlayer` dealer listeners. The pushes stay grouped per kind, in
        .               /// `kinds` order, so the stack sees exactly the per-kind batches it did.
  107,160 ( 0.00%)      fn fire_combat_damage_triggers(
        .                   &mut self,
        .                   source: CardId,
        .                   kinds: &[EventKind],
        .                   default_target: Target,
        .                   damage_amount: u32,
        .                   static_granted: &[crate::card::TriggeredAbility],
        .               ) {
    7,144 ( 0.00%)          let attacker_controller = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == source)
   14,288 ( 0.00%)              .map(|c| c.controller);
        .           
        .                   // One [`DamageTrigger`] bucket per requested kind; drained in order at
        .                   // the bottom.
   21,810 ( 0.00%)          let slot = |k: &EventKind| kinds.iter().position(|want| want == k);
   14,288 ( 0.00%)          let mut by_kind: Vec<Vec<DamageTrigger>> = kinds.iter().map(|_| Vec::new()).collect();
        .           
    7,144 ( 0.00%)          if let Some(c) = self.battlefield.iter().find(|c| c.id == source) {
        .                       // Printed + statics-granted ("Slivers you control have
        .                       // '…combat damage…'" — Tempered/Virulent) + instance-granted
        .                       // (`GrantTriggeredAbility` on `granted_triggers_eot` — Summon:
        .                       // Primal Odin's Zantetsuken) fire alike.
        .                       let instance_granted: &[crate::card::TriggeredAbility] =
    7,144 ( 0.00%)                  self.granted_triggers_eot.get(&c.id).map(Vec::as_slice).unwrap_or(&[]);
        .                       for t in c
        .                           .definition
        .                           .triggered_abilities
        .                           .iter()
        .                           .chain(static_granted.iter())
        .                           .chain(instance_granted.iter())
        .                       {
    1,338 ( 0.00%)                  if !matches!(
    1,144 ( 0.00%)                      t.event.scope,
        .                               crate::effect::EventScope::SelfSource | crate::effect::EventScope::AnyPlayer
        .                           ) {
        .                               continue;
        .                           }
    4,914 ( 0.00%)                  if let Some(i) = slot(&t.event.kind) {
   11,718 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers::{{closure}} (378x)
        .                               by_kind[i].push((
        .                                   c.id,
        .                                   t.effect.clone(),
        .                                   c.controller,
        .                                   t.event.filter.clone(),
        .                                   false,
        .                               ));
        .                           }
-- line 4759 ----------------------------------------
-- line 4760 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // Phase 1b: equipment-granted combat-damage triggers (CR 702.6e). Each
        .                   // Equipment attached to the attacker grants its `equipped_bonus.
        .                   // triggered_abilities` to the creature; a `DealsCombatDamageToPlayer`
        .                   // one fires here (the Sword cycle's "create a token / mill / draw"
        .                   // riders), bound to the attacker's controller.
    7,144 ( 0.00%)          if let Some(atk_ctrl) = attacker_controller {
        .                       for eq in &self.battlefield {
    3,198 ( 0.00%)                  if eq.attached_to != Some(source) {
        .                               continue;
        .                           }
    1,392 ( 0.00%)                  let Some(bonus) = &eq.definition.equipped_bonus else { continue };
        .                           // CR 702.6e — the granted ability fires off the creature, unless
        .                           // the Equipment opts to fire off itself (Umezawa's Jitte puts the
        .                           // counters on the Equipment, so `Selector::This` must read it).
      928 ( 0.00%)                  let trig_source = if bonus.triggers_on_equipment { eq.id } else { source };
        .                           for t in &bonus.triggered_abilities {
        .                               if matches!(
        .                                   t.event.scope,
        .                                   crate::effect::EventScope::SelfSource
        .                                       | crate::effect::EventScope::AnyPlayer
        .                               ) && let Some(i) = slot(&t.event.kind)
        .                               {
        .                                   by_kind[i].push((
-- line 4785 ----------------------------------------
-- line 4792 ----------------------------------------
        .                               }
        .                           }
        .                       }
        .                       // Auras on the attacker with an `EnchantedBySource` combat-damage
        .                       // trigger ("whenever enchanted creature deals combat damage to a
        .                       // player" — Pollenbright Wings). The trigger fires off the *Aura*,
        .                       // so `Selector::AttachedTo(This)` reaches the host.
        .                       for aura in &self.battlefield {
    4,590 ( 0.00%)                  if aura.attached_to != Some(source) || !aura.definition.is_enchantment() {
    6,032 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_enchantment (464x)
        .                               continue;
        .                           }
      928 ( 0.00%)                  for t in &aura.definition.triggered_abilities {
      928 ( 0.00%)                      if t.event.scope == crate::effect::EventScope::EnchantedBySource
        .                                   && let Some(i) = slot(&t.event.kind)
        .                               {
        .                                   by_kind[i].push((
        .                                       aura.id,
        .                                       t.effect.clone(),
        .                                       aura.controller,
        .                                       t.event.filter.clone(),
        .                                       false,
-- line 4812 ----------------------------------------
-- line 4814 ----------------------------------------
        .                               }
        .                           }
        .                       }
        .                       // CR 702.95 — Soulbond-granted combat-damage triggers. A paired
        .                       // creature carrying `soulbond_bonus.triggered_abilities` grants
        .                       // them to BOTH members; a `DealsCombatDamageToPlayer` one fires off
        .                       // the attacker (Tandem Lookout's "deals combat damage → draw").
        .                       for src in &self.battlefield {
  475,500 ( 0.02%)                  let Some(bonus) = &src.definition.soulbond_bonus else { continue };
        .                           let Some(partner) = src.soulbond_partner else { continue };
        .                           if src.id != source && partner != source {
        .                               continue;
        .                           }
        .                           if !self.battlefield.iter().any(|c| c.id == partner) {
        .                               continue;
        .                           }
        .                           for t in &bonus.triggered_abilities {
-- line 4830 ----------------------------------------
-- line 4846 ----------------------------------------
        .                   // control deals combat damage to a player" listeners (e.g.,
        .                   // Quandrix Echocrasher b171, Enduring Curiosity). The listener's
        .                   // controller must match the attacker's controller. The source itself
        .                   // counts ("a creature you control" includes the dealer) — its
        .                   // `YourControl` trigger isn't gathered in Phase 1 (SelfSource-only),
        .                   // so it would otherwise never fire.
        .                   if let Some(atk_ctrl) = attacker_controller {
        .                       for c in &self.battlefield {
  317,000 ( 0.01%)                  if c.controller != atk_ctrl {
        .                               continue;
        .                           }
        .                           for t in &c.definition.triggered_abilities {
    5,844 ( 0.00%)                      if matches!(t.event.scope, crate::effect::EventScope::YourControl)
      724 ( 0.00%)                          && let Some(i) = slot(&t.event.kind)
        .                               {
        .                                   by_kind[i].push((
        .                                       c.id,
        .                                       t.effect.clone(),
        .                                       c.controller,
        .                                       t.event.filter.clone(),
        .                                       true,
        .                                   ));
-- line 4867 ----------------------------------------
-- line 4872 ----------------------------------------
        .           
        .                   // Phase 1.6: `AnyPlayer`-scope listeners on *other* permanents —
        .                   // "whenever a Goblin deals combat damage to a player" (Cabal Slaver),
        .                   // which cares about the dealer's characteristics rather than its
        .                   // controller. `EventSpec.dealer_filter` gates on the dealing creature;
        .                   // the dealer's own `AnyPlayer` trigger already fired in Phase 1.
        .                   {
        .                       let dealer_ok: Vec<(usize, CardId, Effect, usize, Option<crate::card::Predicate>)> =
   28,576 ( 0.00%)                  self.battlefield
        .                               .iter()
        .                               .filter(|c| c.id != source)
        .                               .flat_map(|c| {
  302,712 ( 0.01%)                          c.definition.triggered_abilities.iter().filter_map(move |t| {
    5,044 ( 0.00%)                              matches!(t.event.scope, crate::effect::EventScope::AnyPlayer)
        .                                           .then(|| slot(&t.event.kind))
        .                                           .flatten()
        .                                           .map(|i| (i, c.id, c.controller, t))
        .                                   })
        .                               })
        .                               .filter(|(_, _, ctrl, t)| {
        .                                   t.event.dealer_filter.as_ref().is_none_or(|f| {
        .                                       self.evaluate_requirement_static(
-- line 4893 ----------------------------------------
-- line 4895 ----------------------------------------
        .                                           &Target::Permanent(source),
        .                                           *ctrl,
        .                                           None,
        .                                       )
        .                                   })
        .                               })
        .                               .map(|(i, id, ctrl, t)| (i, id, t.effect.clone(), ctrl, t.event.filter.clone()))
        .                               .collect();
   35,720 ( 0.00%)              for (i, id, effect, ctrl, filter) in dealer_ok {
        .                           by_kind[i].push((id, effect, ctrl, filter, true));
        .                       }
        .                   }
        .           
        .                   // Phase 2: walk every player's graveyard for `FromYourGraveyard`
        .                   // triggers. Only fire if the attacker is controlled by the gy
        .                   // owner (the printed "creatures you control" filter on the
        .                   // attacker side). These cards read "whenever ONE OR MORE creatures
-- line 4911 ----------------------------------------
-- line 4913 ----------------------------------------
        .                   // damage batch (CR 603.2), so each graveyard card fires at most
        .                   // once per sub-step even when several attackers connect
        .                   // (`gy_combat_trigger_fired_this_step` dedupes the per-attacker
        .                   // walks; it's cleared at the top of each damage sub-step).
        .                   //
        .                   // This is the one walk that stays per-kind: the dedupe set is read and
        .                   // written between kinds, so a merged walk would let one graveyard card
        .                   // fire for two kinds of the same damage event.
    7,144 ( 0.00%)          if let Some(atk_controller) = attacker_controller {
        .                       for (i, kind) in kinds.iter().enumerate() {
        .                           let mut fired: Vec<CardId> = Vec::new();
        .                           for player in &self.players {
  171,384 ( 0.01%)                      if player.id.0 != atk_controller {
        .                                   continue;
        .                               }
        .                               for gy_card in &player.graveyard {
  194,660 ( 0.01%)                          if self.gy_combat_trigger_fired_this_step.contains(&gy_card.id) {
        .                                       continue;
        .                                   }
        .                                   for t in &gy_card.definition.triggered_abilities {
        .                                       if t.event.kind == *kind
        .                                           && matches!(
        .                                               t.event.scope,
        .                                               crate::effect::EventScope::FromYourGraveyard
        .                                           )
-- line 4937 ----------------------------------------
-- line 4943 ----------------------------------------
        .                                               t.event.filter.clone(),
        .                                               false,
        .                                           ));
        .                                           fired.push(gy_card.id);
        .                                       }
        .                                   }
        .                               }
        .                           }
  142,820 ( 0.00%)                  self.gy_combat_trigger_fired_this_step.extend(fired);
        .                       }
        .                   }
        .           
        .                   // CR 702.46 — Cipher. A card exiled encoded on this creature offers its
        .                   // controller a free copy whenever the creature deals combat damage to a
        .                   // player. Reuses the Paradigm free-copy effect (mint a token copy of the
        .                   // exiled card and free-cast it; the encoded original stays in exile).
   42,864 ( 0.00%)          if let Some(i) = slot(&EventKind::DealsCombatDamageToPlayer)
  218,572 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_combat_damage_triggers::{{closure}} (7,144x)
        .                       && let Some(atk_ctrl) = attacker_controller
        .                   {
        .                       for enc in &self.exile {
        .                           if enc.encoded_on == Some(source) {
        .                               by_kind[i].push((enc.id, Effect::CastFreeParadigmCopy, atk_ctrl, None, false));
        .                           }
        .                       }
        .                       // CR 701.54c (level 4+) — "Whenever your Ring-bearer deals combat
        .                       // damage to a player, each opponent loses 3 life."
    8,064 ( 0.00%)              if self.players[atk_ctrl].ring_temptations >= 4
        .                           && self.effective_ring_bearer(atk_ctrl) == Some(source)
        .                       {
        .                           by_kind[i].push((
        .                               source,
        .                               Effect::LoseLife {
        .                                   who: crate::effect::Selector::Player(crate::effect::PlayerRef::EachOpponent),
        .                                   amount: crate::effect::Value::Const(3),
        .                               },
-- line 4977 ----------------------------------------
-- line 4981 ----------------------------------------
        .                           ));
        .                       }
        .                   }
        .           
        .                   // "…deals combat damage to a player" bodies that act on something
        .                   // *that player* controls (Hammer of Ruin, Mordant Dragon) read the
        .                   // damaged seat through `ControlledByTriggerPlayer`, both here (target
        .                   // enumeration) and again at resolution.
   31,266 ( 0.00%)          if let Target::Player(p) = default_target {
    8,070 ( 0.00%)              self.trigger_event_player_scratch = Some(p);
        .                   }
        .                   for (trig_source, effect, controller, filter, bind_dealer) in
   57,152 ( 0.00%)              by_kind.into_iter().flatten()
        .                   {
        .                       // CR 603.4 — intervening-'if' on combat-damage triggers ("whenever
        .                       // a creature you control *with toxic* deals combat damage…" —
        .                       // Necrogen Rotpriest). `TriggerSource` in the filter reads the
        .                       // dealing creature, not the listener.
        .                       if let Some(pred) = &filter {
        .                           let mut ctx = crate::game::effects::EffectContext::for_trigger(
        .                               trig_source,
-- line 5001 ----------------------------------------
-- line 5052 ----------------------------------------
        .                               // The damage dealt doubles as the trigger's X so
        .                               // `…XFromCost` filters read the hit at resolution too.
        .                               .x_value(damage_amount)
        .                               // CR 119.3 — the damage dealt, so Value::TriggerEventAmount
        .                               // riders scale by the hit (Visions of Brutality).
        .                               .event_amount(damage_amount)
        .                               .build(),
        .                       );
    7,144 ( 0.00%)          }
  985,900 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::iter::adapters::flatten::Flatten<alloc::vec::into_iter::IntoIter<alloc::vec::Vec<(crabomination_base::card::CardId,crabomination_base::effect::Effect,usize,core::option::Option<crabomination_base::effect::Predicate>,bool)>>>> (7,144x)
   57,152 ( 0.00%)      }
        .           }
        .           
        .           /// The combat-damage value a creature assigns: its toughness when it has
        .           /// `Keyword::AssignsCombatDamageByToughness` (Doran, the Siege Tower; Bill the
        .           /// Pony), otherwise its power (CR 510.1c). The substitution is unconditional —
        .           /// a 5/1 Doran-creature assigns 1.
    1,860 ( 0.00%)  fn combat_damage_value(cp: &ComputedPermanent) -> i32 {
        .               if cp.keywords.contains(&Keyword::AssignsCombatDamageByToughness) {
        .                   cp.toughness
        .               } else {
        .                   cp.power
        .               }
    1,860 ( 0.00%)  }
        .           
        .           /// Resolution-time snapshot of one attacker's combat-relevant data. Captures
        .           /// the attacker's target so damage routes correctly even if the target moves
        .           /// during the loop.
        .           #[derive(Clone)]
        .           struct AttackerInfo {
        .               id: CardId,
        .               controller: usize,
-- line 5082 ----------------------------------------

1,387,908 ( 0.04%)  <counts for unidentified lines in crabomination/src/game/combat.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/recommend.rs
--------------------------------------------------------------------------------
Ir               

-- line 820 ----------------------------------------
      .               Mcts(MctsConfig),
      .           }
      .           
      .           impl Pilot {
      .               /// The evaluation profile this pilot plays, when it has one. Used to
      .               /// push profile settings that the *engine* reads (rather than the
      .               /// bot) onto the seat before the game starts.
      .               fn weights(self) -> Option<EvalWeights> {
    240 ( 0.00%)          match self {
      .                       Pilot::Scored(w) => Some(w),
      .                       Pilot::Uniform | Pilot::Mcts(_) => None,
      .                   }
      .               }
      .           
     48 ( 0.00%)      fn build(self) -> Box<dyn Bot> {
    336 ( 0.00%)          match self {
      .                       Pilot::Scored(w) => Box::new(HeuristicBot::with_weights(w)),
      .                       Pilot::Uniform => Box::new(HeuristicBot::uniform_baseline()),
      .                       Pilot::Mcts(cfg) => Box::new(MctsBot::new(cfg)),
      .                   }
     96 ( 0.00%)      }
      .           }
      .           
      .           impl Default for Pilot {
      .               fn default() -> Self {
      .                   Pilot::Scored(EvalWeights::default())
      .               }
      .           }
      .           
-- line 848 ----------------------------------------
-- line 872 ----------------------------------------
      .               /// consecutive rounds — the loop was stuck, not slow.
      .               pub no_legal_move: u64,
      .               /// Games that ended by the rules with no winner (CR 104.4 draw).
      .               pub draws: u64,
      .           }
      .           
      .           impl SimCost {
      .               fn record(&mut self, o: &GameOutcome) {
    144 ( 0.00%)          self.games += 1;
      .                   self.decisions += o.actions as u64;
     48 ( 0.00%)          self.turns += o.turns as u64;
     72 ( 0.00%)          match o.stop {
      .                       StopReason::ActionCap => self.action_capped += 1,
      .                       StopReason::NoLegalMove => self.no_legal_move += 1,
     36 ( 0.00%)              StopReason::GameOver if o.winner.is_none() => self.draws += 1,
      .                       StopReason::GameOver => {}
      .                   }
      .               }
      .           }
      .           
      .           impl std::ops::AddAssign for SimCost {
      .               fn add_assign(&mut self, r: Self) {
     12 ( 0.00%)          self.games += r.games;
      .                   self.decisions += r.decisions;
     12 ( 0.00%)          self.turns += r.turns;
      .                   self.action_capped += r.action_capped;
     12 ( 0.00%)          self.no_legal_move += r.no_legal_move;
      .                   self.draws += r.draws;
      .               }
      .           }
      .           
      .           /// Decided/undecided tally of `games` bot-vs-bot games between two decks,
      .           /// alternating which deck sits in seat 0 (turn order). `uniform_*` selects
      .           /// the legacy uniform-pick bot for that side (A/B ladders).
      .           pub struct MatchTally {
-- line 906 ----------------------------------------
-- line 1016 ----------------------------------------
      .           
      .           /// Wilson score interval at `z` standard normal deviates.
      .           ///
      .           /// Not the normal approximation: at p̂ = 0 or 1 that has zero width, which
      .           /// once let a lucky 5-0 candidate claim a lower bound of 1.0 and eliminate
      .           /// the entire rest of the field (see
      .           /// `wilson_interval_is_not_degenerate_at_extremes`).
      .           pub fn wilson(wins: u32, n: u32, z: f64) -> (f64, f64) {
     18 ( 0.00%)      if n == 0 {
      .                   return (0.0, 1.0);
      .               }
     22 ( 0.00%)      let (n, p) = (n as f64, wins as f64 / n as f64);
      .               let z2 = z * z;
      .               let denom = 1.0 + z2 / n;
     41 ( 0.00%)      let center = (p + z2 / (2.0 * n)) / denom;
     74 ( 0.00%)      let half = (z / denom) * (p * (1.0 - p) / n + z2 / (4.0 * n * n)).sqrt();
     15 ( 0.00%)      ((center - half).max(0.0), (center + half).min(1.0))
      .           }
      .           
      .           /// Paired win-rate estimate from per-pair scores in {−1, 0, +1}.
      .           ///
      .           /// `p̂ = (E[S] + 1) / 2` and `SE(p̂) = sd(S) / (2√n)`, both read off the
      .           /// empirical pair-score distribution — no independence assumption between
      .           /// the two games of a pair, which is exactly the assumption pairing
      .           /// breaks.
-- line 1040 ----------------------------------------
-- line 1043 ----------------------------------------
      .               pub p: f64,
      .               pub se: f64,
      .               /// Within-pair correlation, backed out of Var(S) = 2p(1−p)(1 + ρ).
      .               /// Negative is the useful direction: the deal decided both games the
      .               /// same way, so it cancelled.
      .               pub rho: f64,
      .           }
      .           
      1 ( 0.00%)  pub fn paired_stat(pairs: &[i8]) -> Option<PairedStat> {
      .               let n = pairs.len();
      2 ( 0.00%)      if n < 2 {
      .                   return None;
      .               }
     26 ( 0.00%)      let (nf, sum) = (n as f64, pairs.iter().map(|&s| s as f64).sum::<f64>());
      .               let mean = sum / nf;
      .               // S ∈ {−1,0,1} so E[S²] is just the sweep fraction.
     45 ( 0.00%)      let var = pairs.iter().map(|&s| (s as f64) * (s as f64)).sum::<f64>() / nf - mean * mean;
      4 ( 0.00%)      let p = (mean + 1.0) / 2.0;
      1 ( 0.00%)      let se = (var.max(0.0) / nf).sqrt() / 2.0;
      .               // Independent games would give Var(S) = 2p(1−p); the shortfall is ρ.
      3 ( 0.00%)      let indep = 2.0 * p * (1.0 - p);
      5 ( 0.00%)      let rho = if indep > 1e-9 { var / indep - 1.0 } else { 0.0 };
      4 ( 0.00%)      Some(PairedStat { n, p, se, rho })
      1 ( 0.00%)  }
      .           
      .           /// [`simulate_match_games_piloted`] with **antithetic seat pairing**: each
      .           /// pair plays the same shuffle twice with the pilots swapped between the
      .           /// seats.
      .           ///
      .           /// The ordinary ladder draws a fresh shuffle for every game and swaps
      .           /// seats on alternate games, so seat luck and deal luck are averaged away
      .           /// rather than cancelled. In a 40-card mirror most of the game-to-game
-- line 1074 ----------------------------------------
-- line 1087 ----------------------------------------
      .           /// efficiency rather than asserting one.
      .           ///
      .           /// This is only a variance reduction, not a change of estimand: the mean
      .           /// is the same win rate the unpaired ladder estimates, which is why the
      .           /// unpaired total is still reported alongside it as the control.
      .           ///
      .           /// Both games of a pair take their shuffle from one `seed_base + k` stream,
      .           /// so a mirror deals seat 0 the identical library in both directions.
     72 ( 0.00%)  pub fn simulate_match_pairs_piloted(
      .               deck_a: &[CardFactory],
      .               deck_b: &[CardFactory],
      .               pairs: usize,
      .               pilots: [Pilot; 2],
      .               max_actions: usize,
      .               seed_base: u64,
      .           ) -> PairedTally {
      4 ( 0.00%)      let template_a0 = build_match_template(deck_a, deck_b);
12,453,338 ( 0.40%)  => crabomination/src/recommend.rs:crabomination::recommend::build_match_template (4x)
     20 ( 0.00%)      let template_b0 = build_match_template(deck_b, deck_a);
12,438,899 ( 0.40%)  => crabomination/src/recommend.rs:crabomination::recommend::build_match_template (4x)
     48 ( 0.00%)      let mut tally = PairedTally {
      .                   wins_a: 0,
      .                   wins_b: 0,
      .                   undecided: 0,
      .                   cost: SimCost::default(),
      .                   pairs: Vec::with_capacity(pairs),
      .               };
      .               for k in 0..pairs {
      .                   let seed = seed_base.wrapping_add((k as u64).wrapping_mul(0x9E37_79B9_7F4A_7C15));
      .                   let mut score = 0i8;
      .                   let mut decided_both = true;
      .                   for a_seat0 in [true, false] {
      .                       // Re-seeded per game, *not* carried across the pair: the second
      .                       // game has to draw the same shuffle as the first, which a shared
      .                       // generator would not do.
     72 ( 0.00%)              let mut shuffle_rng = StdRng::seed_from_u64(seed);
  5,448 ( 0.00%)  => /root/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand_core-0.10.0/src/seedable_rng.rs:rand_core::seedable_rng::SeedableRng::seed_from_u64 (24x)
      .                       let template = if a_seat0 { &template_a0 } else { &template_b0 };
    672 ( 0.00%)              let seated = if a_seat0 { [pilots[0], pilots[1]] } else { [pilots[1], pilots[0]] };
      .                       // Both games of the pair get the *same* jitter stream, so the
      .                       // bots' tie-breaks replay identically and the only thing left
      .                       // that can separate the two games is the profiles themselves.
      .                       let outcome =
      .                           play_one_game(template, seated, max_actions, Some(&mut shuffle_rng), Some(seed));
      .                       tally.cost.record(&outcome);
      .                       match outcome.winner {
      .                           Some(seat) => {
     48 ( 0.00%)                      if (seat == 0) == a_seat0 {
     24 ( 0.00%)                          tally.wins_a += 1;
      .                                   score += 1;
      .                               } else {
     34 ( 0.00%)                          tally.wins_b += 1;
      .                                   score -= 1;
      .                               }
      .                           }
      .                           None => {
      .                               tally.undecided += 1;
      .                               decided_both = false;
      .                           }
      .                       }
      .                   }
     29 ( 0.00%)          if decided_both {
     12 ( 0.00%)              tally.pairs.push(score.signum());
      .                   }
      .               }
     72 ( 0.00%)      tally
     44 ( 0.00%)  }
1,451,503 ( 0.05%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (8x)
      .           
      .           /// Sample a variant's spell/land split that always sums to the legal
      .           /// 40-card sealed deck: spells from `spell_count_range`, lands = 40 −
      .           /// spells (clamped into `land_count_range`, re-deriving spells from the
      .           /// clamp if the config ranges don't line up). Independent sampling used
      .           /// to produce 38-39-card decks, which are illegal AND thinner-deck
      .           /// advantaged — they won tournaments on the bug.
      .           fn sample_deck_split<R: Rng>(cfg: &SimConfig, rng: &mut R) -> (usize, u32) {
-- line 1158 ----------------------------------------
-- line 1161 ----------------------------------------
      .               let lands = (DECK_SIZE.saturating_sub(spells) as u32)
      .                   .clamp(cfg.land_count_range.0, cfg.land_count_range.1);
      .               let spells = DECK_SIZE.saturating_sub(lands as usize);
      .               (spells, lands)
      .           }
      .           
      .           /// Unshuffled two-seat state with both libraries loaded — the clone-me
      .           /// template for [`play_one_game`].
    176 ( 0.00%)  pub(crate) fn build_match_template(seat0: &[CardFactory], seat1: &[CardFactory]) -> GameState {
    112 ( 0.00%)      let mut g = GameState::new(vec![Player::new(0, "A"), Player::new(1, "B")]);
143,574 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:crabomination::player::Player::new (16x)
 31,940 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::new (8x)
      .               for (seat, deck) in [seat0, seat1].into_iter().enumerate() {
      .                   for &f in deck {
  1,980 ( 0.00%)              g.add_card_to_library(seat, f());
252,481 ( 0.01%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/lands.rs:crabomination_catalog::sets::lea::lands::swamp (68x)
248,529 ( 0.01%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/lands.rs:crabomination_catalog::sets::lea::lands::island (68x)
220,412 ( 0.01%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/lands.rs:crabomination_catalog::sets::lea::lands::mountain (68x)
130,855 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/lands.rs:crabomination_catalog::sets::lea::lands::plains (36x)
130,166 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/lands.rs:crabomination_catalog::sets::lea::lands::forest (36x)
 79,320 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/mod_set/instants.rs:crabomination_catalog::sets::mod_set::instants::doom_blade (28x)
 60,329 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::llanowar_elves (16x)
 53,596 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::air_elemental (20x)
 51,375 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::vampire_nighthawk (20x)
 50,168 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/instants.rs:crabomination_catalog::sets::lea::instants::counterspell (24x)
 49,750 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::divination (24x)
 43,280 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/zen/creatures.rs:crabomination_catalog::sets::zen::creatures::goblin_guide (16x)
 42,410 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::elvish_mystic (12x)
 42,126 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::wind_drake (16x)
 38,184 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::monastery_swiftspear (12x)
 37,605 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::prodigal_sorcerer (12x)
 35,089 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::wall_of_omens (12x)
 33,560 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/instants.rs:crabomination_catalog::sets::lea::instants::swords_to_plowshares (12x)
 33,074 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::pacifism (12x)
 31,276 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/recent.rs:crabomination_catalog::sets::decks::recent::ambush_viper (12x)
 31,109 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::sengir_vampire (12x)
 30,544 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/instants.rs:crabomination_catalog::sets::lea::instants::lightning_bolt (16x)
 28,918 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/mod_set/creatures.rs:crabomination_catalog::sets::mod_set::creatures::centaur_courser (12x)
 28,552 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::grizzly_bears (12x)
 26,644 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::hill_giant (12x)
 26,544 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::gray_ogre (12x)
 25,496 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::rancor (8x)
 23,008 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/por/instants.rs:crabomination_catalog::sets::por::instants::shock (12x)
 21,012 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::serra_angel (8x)
 19,265 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::craw_wurm (8x)
 17,704 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/recent71.rs:crabomination_catalog::sets::decks::recent71::fire_elemental (8x)
 16,442 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/instants.rs:crabomination_catalog::sets::lea::instants::giant_growth (8x)
 10,163 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/decks/modern.rs:crabomination_catalog::sets::decks::modern::baneslayer_angel (4x)
  9,936 ( 0.00%)  => /home/user/Crabomination/crabomination_catalog/src/sets/lea/creatures.rs:crabomination_catalog::sets::lea::creatures::shivan_dragon (4x)
      .                   }
     40 ( 0.00%)          g.players[seat].wants_ui = true;
    480 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (16x)
      .               }
     32 ( 0.00%)      g
  3,304 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (8x)
     72 ( 0.00%)  }
      .           
      .           /// Consecutive no-move rounds before a bot play loop gives the game up.
      .           /// Two bots that both pass priority forever produce one such round per
      .           /// pass, so this is deliberately small. Shared with
      .           /// [`crate::selfplay`]'s recording loop, which is otherwise a separate
      .           /// hand-written copy of the same fixed point — the two stall counts are
      .           /// only comparable if this number is.
      .           pub const STALE_ROUNDS: usize = 8;
-- line 1186 ----------------------------------------
-- line 1217 ----------------------------------------
      .           /// actor's fixed-point bot polling.
      .           pub(crate) fn play_one_game(
      .               template: &GameState,
      .               pilots: [Pilot; 2],
      .               max_actions: usize,
      .               shuffle_rng: Option<&mut StdRng>,
      .               jitter_seed: Option<u64>,
      .           ) -> GameOutcome {
    240 ( 0.00%)      play_one_game_traced(template, pilots, max_actions, shuffle_rng, jitter_seed, None)
3,089,035,282 (99.12%)  => crabomination/src/recommend.rs:crabomination::recommend::play_one_game_traced (24x)
      .           }
      .           
      .           /// A full game's observable history: one line per accepted action, each
      .           /// pairing the action with the state it produced. Two engine builds that
      .           /// agree on this agree on everything a game can be judged by.
      .           ///
      .           /// Deliberately textual. A struct would be cheaper to compare and useless
      .           /// to read: when a trace moves, the question is always *which* action
-- line 1233 ----------------------------------------
-- line 1312 ----------------------------------------
      .                   g.players[0].graveyard.len(),
      .                   g.players[1].graveyard.len(),
      .                   g.players[0].library.len(),
      .                   g.players[1].library.len(),
      .                   g.stack.len(),
      .               )
      .           }
      .           
    384 ( 0.00%)  fn play_one_game_traced(
      .               template: &GameState,
      .               pilots: [Pilot; 2],
      .               max_actions: usize,
      .               shuffle_rng: Option<&mut StdRng>,
      .               jitter_seed: Option<u64>,
      .               mut trace: Option<&mut Vec<String>>,
      .           ) -> GameOutcome {
      .               // Installed for the duration of this game and cleared after, so a
      .               // seeded game can't leak its stream into whatever the worker plays
      .               // next. See `bot::set_jitter_seed`.
      .               crate::server::bot::set_jitter_seed(jitter_seed);
     48 ( 0.00%)      let mut g = template.clone();
 45,518 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (24x)
      .               // Profile settings the engine reads off the seat, not the bot.
     96 ( 0.00%)      for (seat, pilot) in pilots.into_iter().enumerate() {
      .                   if let Some(w) = pilot.weights() {
    144 ( 0.00%)              g.players[seat].smart_tap = w.smart_tap;
247,962 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (48x)
      .                   }
      .               }
      .               let mut seeded = shuffle_rng;
      .               for seat in 0..2 {
      .                   match &mut seeded {
     96 ( 0.00%)              Some(r) => g.players[seat].library.shuffle(*r),
  1,440 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (48x)
      .                       None => g.players[seat].library.shuffle(&mut rand::rng()),
      .                   }
      .               }
      .               // A seeded deal implies a seeded *game*: mulligan reshuffles, random
      .               // discards and every other in-game roll come off the state's own
      .               // stream, drawn from the shuffle stream so it stays a function of the
      .               // caller's seed. Without this a mulligan re-randomized the deal and
      .               // the pair's two games diverged — which is also what broke the
      .               // antithetic pairing's variance reduction on exactly those games.
      .               if let Some(r) = &mut seeded {
      .                   g.rng.reseed(r.random());
      .               }
     24 ( 0.00%)      g.start_mulligan_phase();
160,575 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::start_mulligan_phase (24x)
      .               let mut bots: Vec<Box<dyn Bot>> = pilots.into_iter().map(Pilot::build).collect();
      .               let (mut actions, mut stale) = (0usize, 0usize);
 49,440 ( 0.00%)      while !g.is_game_over() && actions < max_actions && stale < STALE_ROUNDS {
      .                   let mut any = false;
 16,484 ( 0.00%)          for (s, bot) in bots.iter_mut().enumerate() {
331,260 ( 0.01%)              let Some(a) = bot.next_action(&g, s) else { continue };
2,675,229,077 (85.84%)  => /home/user/Crabomination/crabomination/src/server/bot.rs:<crabomination::server::bot::HeuristicBot as crabomination::server::bot::Bot>::next_action (16,484x)
      .                       // Formatted before the move, because `perform_action` takes it
      .                       // by value; only kept when the move is accepted.
      .                       let pending = trace.as_ref().map(|_| format!("p{s} {a:?}"));
256,356 ( 0.01%)              if g.perform_action(a).is_ok() {
410,261,160 (13.16%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action (14,242x)
 14,242 ( 0.00%)                  if let (Some(t), Some(p)) = (trace.as_mut(), pending) {
      .                               t.push(format!("{:>5}. {p}  ~~  {}", t.len() + 1, trace_state(&g)));
 56,968 ( 0.00%)                  }
      .                           any = true;
 14,242 ( 0.00%)                  actions += 1;
      .                           if g.is_game_over() {
      .                               break;
      .                           }
      .                       }
      .                   }
 24,672 ( 0.00%)          if any { stale = 0 } else { stale += 1 }
      .               }
      .               crate::server::bot::set_jitter_seed(None);
      .               // Tested in the loop's own order: `is_game_over` wins over both caps,
      .               // and a game that hits the action cap on the same iteration it goes
      .               // stale is reported as capped.
     96 ( 0.00%)      let stop = if g.is_game_over() {
      .                   StopReason::GameOver
      .               } else if actions >= max_actions {
      .                   StopReason::ActionCap
      .               } else {
      .                   StopReason::NoLegalMove
      .               };
    144 ( 0.00%)      GameOutcome { winner: g.game_over.flatten(), actions, turns: g.turn_number, stop }
    216 ( 0.00%)  }
559,217 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (24x)
      .           
      .           // ─────────────────────────────── evaluation ──────────────────────────────
      .           
      .           /// Per-game outcomes keyed by (gauntlet opponent, game slot): +1 win,
      .           /// −1 loss, 0 undecided. Slot indices are shared across candidates (CRN
      .           /// seeds every candidate's slot identically), which is what makes two
      .           /// candidates' outcomes pairable game-for-game.
      .           pub type SlotOutcomes = HashMap<(u32, u32), i8>;
-- line 1398 ----------------------------------------

 30,911 ( 0.00%)  <counts for unidentified lines in crabomination/src/recommend.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/effects/events.rs
--------------------------------------------------------------------------------
Ir               

-- line 4 ----------------------------------------
      .           use super::EntityRef;
      .           use crate::card::{CardId, CardInstance};
      .           use crate::effect::{EventKind, EventScope, EventSpec};
      .           use crate::game::{GameEvent, GameState};
      .           
      .           /// Returns true if `event` matches the `EventSpec` on `source` (a permanent
      .           /// on the battlefield). Used by `fire_triggers_for_event` to decide whether a
      .           /// triggered ability should be pushed onto the stack.
446,922 ( 0.01%)  pub(crate) fn event_matches_spec(
      .               state: &GameState,
      .               event: &GameEvent,
      .               spec: &EventSpec,
      .               source: &CardInstance,
      .           ) -> bool {
638,460 ( 0.02%)      let kind_ok = match (&spec.kind, event) {
      .                   (EventKind::EntersBattlefield, GameEvent::PermanentEntered { .. }) => true,
      .                   (EventKind::CreatureDied, GameEvent::CreatureDied { .. }) => true,
      .                   (
      .                       EventKind::CreatureOrArtifactDied,
      .                       GameEvent::PermanentDied { is_creature, is_artifact, .. },
      .                   ) => *is_creature || *is_artifact,
      .                   (EventKind::PermanentDied, GameEvent::PermanentDied { .. }) => true,
      .                   (EventKind::CreatureSacrificed, GameEvent::CreatureSacrificed { .. }) => true,
-- line 26 ----------------------------------------
-- line 181 ----------------------------------------
      .                       EventKind::BecameAttached,
      .                       GameEvent::AttachmentMoved { attached_to: Some(_), .. },
      .                   ) => true,
      .                   (EventKind::DayNightChanged, GameEvent::DayNightChanged { was_transition, .. }) => {
      .                       *was_transition
      .                   }
      .                   _ => false,
      .               };
191,538 ( 0.01%)      if !kind_ok {
      .                   return false;
      .               }
      .           
      .               // "Whenever a [filter] deals damage …" — the damage events bind the
      .               // damaged object to `TriggerSource`, so the dealer is gated here.
      .               if let Some(dealer) = &spec.dealer_filter {
      .                   let GameEvent::DamageDealt { from_card: Some(from), .. } = event else { return false };
      .                   if !state.evaluate_requirement_static(
-- line 197 ----------------------------------------
-- line 653 ----------------------------------------
      .                   && event_actor(state, event).is_none_or(|p| state.same_team(p, source.controller))
      .               {
      .                   return false;
      .               }
      .           
      .               // Filter predicate evaluation is deferred to when the trigger actually
      .               // resolves; at this stage we just ensure the shape matches.
      .               true
510,768 ( 0.02%)  }
      .           
      .           /// The "actor" of an event for `EventScope::YourControl` /
      .           /// `OpponentControl` checks: the player whose action / permanent the
      .           /// event hangs off. For player-keyed events (CardDrawn, LifeGained, etc.)
      .           /// this is the event's `player` field; for permanent-keyed events
      .           /// (PermanentEntered, AttackerDeclared, CreatureDied) this is the
      .           /// permanent's controller, looked up on the battlefield. CreatureDied
      .           /// fires after the card has left the battlefield, so we fall back to the
-- line 669 ----------------------------------------

127,692 ( 0.00%)  <counts for unidentified lines in crabomination/src/game/effects/events.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/server/bot.rs
--------------------------------------------------------------------------------
Ir                 

-- line 36 ----------------------------------------
        .           /// every one of the 368 non-split pairs and held the within-pair
        .           /// correlation at −0.69 instead of −1.
        .           ///
        .           /// Seeding it identically for both games of an antithetic pair makes the
        .           /// two replays differ only where the *profiles* differ, which is the
        .           /// whole point of common random numbers. Real matches leave it `None`.
        .           pub fn set_jitter_seed(seed: Option<u64>) {
        .               JITTER.with(|j| {
      120 ( 0.00%)          *j.borrow_mut() = seed.map(StdRng::seed_from_u64);
    1,560 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (24x)
        .               });
        .           }
        .           
        .           /// A jitter draw in `0..n`, from the seeded stream when one is installed.
        .           fn jitter_below(n: usize) -> usize {
        .               if n <= 1 {
        .                   return 0;
        .               }
    8,170 ( 0.00%)      JITTER.with(|j| match &mut *j.borrow_mut() {
  145,502 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/thread/local.rs:std::thread::local::LocalKey<T>::with (1,634x)
    9,804 ( 0.00%)          Some(r) => r.random_range(0..n),
   83,410 ( 0.00%)  => /root/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand-0.10.2/src/rng.rs:rand::rng::RngExt::random_range (1,634x)
        .                   None => rng().random_range(0..n),
        .               })
        .           }
        .           
        .           /// A uniform draw in [0, 1) from the jitter stream.
        .           fn jitter_f64() -> f64 {
        .               JITTER.with(|j| match &mut *j.borrow_mut() {
        .                   Some(r) => r.random::<f64>(),
-- line 62 ----------------------------------------
-- line 111 ----------------------------------------
        .                   }
        .               }
        .               scores.len().saturating_sub(1)
        .           }
        .           
        .           /// Choose among `(candidate index, score)` pairs: softmax-sampled when
        .           /// this thread has sampling installed and the turn qualifies, otherwise
        .           /// the first-wins-ties argmax every ladder number was measured under.
    2,580 ( 0.00%)  fn choose_scored(turn: u32, scored: &[(usize, i32)]) -> Option<usize> {
    1,716 ( 0.00%)      if scored.is_empty() {
        .                   return None;
        .               }
        .               if scored.len() > 1
        .                   && let Some(t) = sampling_temp(turn)
        .               {
        .                   let ws: Vec<i32> = scored.iter().map(|&(_, s)| s).collect();
        .                   return Some(scored[sample_scored_index(&ws, t)].0);
        .               }
      858 ( 0.00%)      let mut best = scored[0];
    2,218 ( 0.00%)      for &c in &scored[1..] {
    7,752 ( 0.00%)          if c.1 > best.1 {
        .                       best = c;
        .                   }
        .               }
        .               Some(best.0)
    3,010 ( 0.00%)  }
        .           
        .           /// Drives one seat without a human client. Implementations see the full
        .           /// `GameState` and return the single next action they'd like to submit.
        .           pub trait Bot: Send {
        .               /// Return `Some(action)` to submit, or `None` if it's not this bot's turn
        .               /// to act right now (no priority, waiting on an opponent decision, game
        .               /// already over, etc.).
        .               fn next_action(&mut self, state: &GameState, seat: usize) -> Option<GameAction>;
-- line 144 ----------------------------------------
-- line 1236 ----------------------------------------
        .                       scored: true,
        .                       reveal_commit: None,
        .                       weights: EvalWeights::default(),
        .                   }
        .               }
        .           
        .               /// The scored bot piloted with a specific evaluation profile.
        .               pub fn with_weights(weights: EvalWeights) -> Self {
      384 ( 0.00%)          Self { weights, ..Self::new() }
        .               }
        .           
        .               /// The pre-scoring reference bot: identical candidate enumeration and
        .               /// combat, but the castable pick is uniform-random. Kept as the ladder
        .               /// baseline for measuring bot improvements.
        .               pub fn uniform_baseline() -> Self {
        .                   Self { scored: false, ..Self::new() }
        .               }
        .           
        .               fn sync_step(&mut self, state: &GameState) {
   49,452 ( 0.00%)          let key = (state.turn_number, state.step, state.active_player_idx);
        .                   if self.last_step_key != Some(key) {
   32,544 ( 0.00%)              self.last_step_key = Some(key);
   21,696 ( 0.00%)              self.attackers_declared = false;
        .                       self.blocks_declared = false;
        .                   }
        .               }
        .           
        .               /// Is this combat step's declaration still to be made? For the Monte
        .               /// Carlo layer, which searches the declaration itself but leaves
        .               /// every other tick of the phase (tricks, removal, passes) to this
        .               /// bot — the two share one latch or they double-declare.
-- line 1266 ----------------------------------------
-- line 1291 ----------------------------------------
        .           impl Bot for HeuristicBot {
        .               /// The whole tick runs inside one `with_frozen_layers` scope. Sound by
        .               /// construction — a bot only ever receives `&GameState`, so nothing it
        .               /// does here can invalidate the gathered continuous-effect set — and it
        .               /// turns a bot tick's many `computed_permanent` reads into one gather
        .               /// instead of one gather each. The scope does NOT reach the dry-run
        .               /// probes and combat sims: those clone the state, and `LayerFreeze`
        .               /// clones as unfrozen precisely because the clone gets mutated.
   98,904 ( 0.00%)      fn next_action(&mut self, state: &GameState, seat: usize) -> Option<GameAction> {
  115,388 ( 0.00%)          state.with_frozen_layers(|state| self.next_action_inner(state, seat))
2,675,014,785 (85.83%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers (16,484x)
   65,936 ( 0.00%)      }
        .           }
        .           
        .           impl HeuristicBot {
  131,872 ( 0.00%)      fn next_action_inner(&mut self, state: &GameState, seat: usize) -> Option<GameAction> {
   16,484 ( 0.00%)          if state.is_game_over() {
        .                       return None;
        .                   }
        .                   self.sync_step(state);
        .           
        .                   // Any pending decision addressed to us: auto-answer it.
   32,968 ( 0.00%)          if let Some(pending) = &state.pending_decision {
      372 ( 0.00%)              if pending.acting_player() == seat {
    1,488 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::PendingDecision::acting_player (124x)
        .                           // Ad Nauseam's per-reveal prompt is the one STATEFUL
        .                           // policy (it tracks committed reveals on the bot struct
        .                           // across the series — see `RevealTopToHandLoseLifeRepeat`),
        .                           // so it answers here; every other decision goes through
        .                           // [`decide_pending_policy`], the same table simulations
        .                           // use.
      180 ( 0.00%)                  if let crate::decision::Decision::OptionalTrigger { source, description } =
        .                               &pending.decision
        .                               && description.starts_with("Reveal the top card (")
        .                           {
        .                               let (cards, life_committed) = match &self.reveal_commit {
        .                                   Some((s, c, l)) if *s == *source => (*c, *l),
        .                                   _ => (0, 0),
        .                               };
        .                               let mv = state.players[seat]
-- line 1328 ----------------------------------------
-- line 1333 ----------------------------------------
        .                               let yes = state.effective_life(seat) - life_committed - mv > 10;
        .                               self.reveal_commit =
        .                                   if yes { Some((*source, cards + 1, life_committed + mv)) } else { None };
        .                               return Some(GameAction::SubmitDecision(
        .                                   crate::decision::DecisionAnswer::Bool(yes),
        .                               ));
        .                           }
        .                           let answer =
       90 ( 0.00%)                      decide_pending_policy(state, seat, &self.weights, &pending.decision, true);
    1,350 ( 0.00%)                  return Some(GameAction::SubmitDecision(answer));
        .                       }
        .                       return None;
        .                   }
        .           
   32,720 ( 0.00%)          if state.player_with_priority() != seat {
        .                       return None;
        .                   }
        .           
    8,892 ( 0.00%)          let is_active = state.active_player_idx == seat;
        .           
   77,184 ( 0.00%)          match state.step {
    1,352 ( 0.00%)              TurnStep::DeclareBlockers if state.may_declare_blocks(seat) => {
    1,316 ( 0.00%)                  if !self.blocks_declared && !state.attacking().is_empty() {
        .                               // Kill the biggest attacker BEFORE committing blocks —
        .                               // removal cast here shrinks the combat the blocks then
        .                               // answer. Validated actions only, so a resolved kill
        .                               // falls through to the block declaration next tick.
      212 ( 0.00%)                      if !is_active
    1,908 ( 0.00%)                          && let Some(a) = pick_defensive_removal(state, seat, &self.weights)
  913,501 ( 0.03%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_defensive_removal (212x)
        .                               {
        .                                   return Some(a);
        .                               }
      424 ( 0.00%)                      self.blocks_declared = true;
        .                               // On our own turn we're choosing the *defender's* blocks
        .                               // (Master Warcraft, Invasion Plans), so submit only what
        .                               // CR 509.1c forces — and aim each forced blocker at the
        .                               // attacker most likely to kill it.
        .                               let blocks = if is_active {
        .                                   forced_blocks(state)
        .                               } else {
      848 ( 0.00%)                          pick_blocks_scored(state, seat, &self.weights)
86,628,003 ( 2.78%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks_scored (212x)
        .                               };
    1,060 ( 0.00%)                      Some(GameAction::DeclareBlockers(blocks))
    1,846 ( 0.00%)                  } else if state.blockers_declared() && state.stack.is_empty() {
        .                               // Post-block priority: a held pump trick that flips a
        .                               // fight one of our blockers is losing.
        .                               Some(pick_combat_trick(state, seat, &self.weights).unwrap_or(GameAction::PassPriority))
        .                           } else {
        .                               Some(GameAction::PassPriority)
        .                           }
        .                       }
        .                       // Active side of the same window: blocks are in, stack is
        .                       // empty — the classic trick timing for a blocked attacker.
        .                       TurnStep::DeclareBlockers
    1,664 ( 0.00%)                  if is_active && state.blockers_declared() && state.stack.is_empty() =>
        .                       {
    9,252 ( 0.00%)                  Some(pick_combat_trick(state, seat, &self.weights).unwrap_or(GameAction::PassPriority))
        .                       }
        .                       // Master Warcraft on an opponent's turn: we choose *their*
        .                       // attackers, so declare only the creatures that must attack.
    2,604 ( 0.00%)              TurnStep::DeclareAttackers if !is_active && state.attack_declarer() == seat => {
      842 ( 0.00%)                  if !self.attackers_declared {
        .                               self.attackers_declared = true;
        .                               Some(GameAction::DeclareAttackers(forced_attacks(state)))
        .                           } else {
        .                               Some(GameAction::PassPriority)
        .                           }
        .                       }
    1,288 ( 0.00%)              TurnStep::DeclareAttackers if is_active && state.attack_declarer() == seat => {
    2,576 ( 0.00%)                  if !self.attackers_declared {
      630 ( 0.00%)                      self.attackers_declared = true;
    2,520 ( 0.00%)                      Some(GameAction::DeclareAttackers(pick_attacks_scored(
1,553,500,104 (49.85%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_attacks_scored (630x)
        .                                   state,
        .                                   seat,
      630 ( 0.00%)                          &self.weights,
        .                               )))
        .                           } else {
        .                               Some(GameAction::PassPriority)
        .                           }
        .                       }
    4,588 ( 0.00%)              TurnStep::PreCombatMain | TurnStep::PostCombatMain if is_active => {
        .                           // A non-empty stack in our own main is response timing,
        .                           // not sorcery timing: an opponent's answer is resolving,
        .                           // and the response layer sees threats the main-phase
        .                           // enumerator can't (counter it, fire a prepared inset
        .                           // spell before its body dies). Both pickers ignore the
        .                           // bot's own spells-in-flight, so a stack we put there
        .                           // falls through to the enumerator as before.
    8,304 ( 0.00%)                  if !state.stack.is_empty()
    4,832 ( 0.00%)                      && let Some(a) = pick_stack_response(state, seat, &self.weights)
   29,996 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_stack_response (604x)
    2,416 ( 0.00%)                          .or_else(|| pick_ability_counter_response(state, seat))
   17,396 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_ability_counter_response (604x)
    3,624 ( 0.00%)                          .or_else(|| pick_prepare_response(state, seat, &self.weights))
   39,796 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_prepare_response (604x)
        .                           {
        .                               return Some(a);
        .                           }
        .                           // CR 116.2j — the agenda is already named, so turning it face
        .                           // up is pure upside; do it at the first opportunity.
   11,072 ( 0.00%)                  if let Some(c) = state.players[seat]
        .                               .command
        .                               .iter()
    2,768 ( 0.00%)                      .find(|c| c.face_down && c.definition.is_conspiracy())
   47,056 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find (2,768x)
        .                           {
        .                               return Some(GameAction::RevealConspiracy { card_id: c.id });
        .                           }
        .                           // CR 901.9 — take the turn's one free planar-die roll before
        .                           // spending mana. Later rolls cost {N} and compete with real
        .                           // plays, so the bot stops after the free one. Inert outside a
        .                           // Planechase game (no planar deck, nothing face up).
   11,072 ( 0.00%)                  if state.stack.is_empty()
    6,492 ( 0.00%)                      && state.players[seat].planar_die_rolls_this_turn == 0
    6,492 ( 0.00%)                      && !state.players[seat].planar_deck.is_empty()
        .                               && !state.face_up_planes().is_empty()
        .                           {
        .                               return Some(GameAction::RollPlanarDie);
        .                           }
   11,686 ( 0.00%)                  Some(main_phase_action_with(state, seat, self.scored, &self.weights))
        .                       }
        .                       // Opponent's end step with an empty stack — the bot's canonical
        .                       // off-turn window. Reuse the whole scored main-phase enumeration:
        .                       // `would_accept` filters it down to instant-legal lines (removal,
        .                       // tricks, flash creatures, EOT draw, mana-sink abilities), while
        .                       // land drops, sorcery-speed casts, and loyalty/crew lines simply
        .                       // drop out of the candidate set. Without this, every non-counter
        .                       // instant was dead in hand until the bot's own main phase.
    3,100 ( 0.00%)              TurnStep::End if !is_active && state.stack.is_empty() => {
    3,070 ( 0.00%)                  Some(main_phase_action_with(state, seat, self.scored, &self.weights))
        .                       }
        .                       _ => Some(
   44,030 ( 0.00%)                  pick_stack_response(state, seat, &self.weights)
8,997,480 ( 0.29%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_stack_response (8,806x)
   26,298 ( 0.00%)                      .or_else(|| pick_ability_counter_response(state, seat))
  190,484 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_ability_counter_response (8,766x)
   35,064 ( 0.00%)                      .or_else(|| pick_prepare_response(state, seat, &self.weights))
  273,120 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_prepare_response (8,766x)
        .                               // Defender windows in the attack steps (the picker
        .                               // no-ops unless declared attackers are coming at us).
        .                               .or_else(|| {
   26,298 ( 0.00%)                          if state.stack.is_empty() {
   32,616 ( 0.00%)                              pick_defensive_removal(state, seat, &self.weights)
1,466,294 ( 0.05%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_defensive_removal (8,154x)
        .                                   } else {
        .                                       None
        .                                   }
        .                               })
    9,418 ( 0.00%)                      .unwrap_or(GameAction::PassPriority),
        .                       ),
        .                   }
  148,356 ( 0.00%)      }
        .           }
        .           
        .           /// The bot's answer to a pending `decision` for `seat` — the policy table
        .           /// behind `next_action`, extracted so SIMULATIONS answer with it too.
        .           /// Every lookahead used to answer internal decisions with `AutoDecider`,
        .           /// which meant a line was scored as if the bot's future self would scry
        .           /// badly, decline its tutors, dump the head of its hand to a discard, and
        .           /// take mode 0 — and the opponent would as well. Now both seats inside a
-- line 1484 ----------------------------------------
-- line 1496 ----------------------------------------
        .               state: &GameState,
        .               seat: usize,
        .               w: &EvalWeights,
        .               decision: &crate::decision::Decision,
        .               eval_modes: bool,
        .           ) -> crate::decision::DecisionAnswer {
        .               // Read-only, and reached once per pending decision inside the attack /
        .               // block sims on a cloned (unfrozen) state — share one gather.
   13,096 ( 0.00%)      state.with_frozen_layers(|s| decide_pending_policy_inner(s, seat, w, decision, eval_modes))
  110,255 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (90x)
        .           }
        .           
    6,246 ( 0.00%)  fn decide_pending_policy_inner(
        .               state: &GameState,
        .               seat: usize,
        .               w: &EvalWeights,
        .               decision: &crate::decision::Decision,
        .               eval_modes: bool,
        .           ) -> crate::decision::DecisionAnswer {
    4,858 ( 0.00%)      match decision {
        .                   // Smarter mulligan than AutoDecider's blanket Keep:
        .                   // ship hands that are flooded or screwed on lands.
        .                   crate::decision::Decision::Mulligan { mulligans_taken, .. } => {
        .                       decide_mulligan(state, seat, *mulligans_taken, w)
       62 ( 0.00%)          }
        .                   // Unlike AutoDecider (which declines every tutor), the
        .                   // bot actually fetches — preferring a basic land toward
        .                   // its weakest color so singleplayer tutors fix mana.
        .                   crate::decision::Decision::SearchLibrary { candidates, eligible, .. } => {
        .                       // Only consider picks the engine will accept.
        .                       let pickable: Vec<(crate::card::CardId, String)> = match eligible {
        .                           Some(ok) => {
        .                               candidates.iter().filter(|(id, _)| ok.contains(id)).cloned().collect()
-- line 1527 ----------------------------------------
-- line 1611 ----------------------------------------
        .                   }
        .                   // London mulligan bottoming (CR 103.5) and "put N cards
        .                   // from your hand on top/bottom" effects. `AutoDecider`
        .                   // takes the first N cards of the hand, so a bot that
        .                   // mulliganed bottomed whichever cards happened to sit at
        .                   // the front — routinely its business spells. Rank them
        .                   // the same way a discard is ranked: surplus lands first,
        .                   // then the priciest spells.
       24 ( 0.00%)          crate::decision::Decision::PutOnLibrary { player, count, hand } if *player == seat => {
       60 ( 0.00%)              let order = hand_worst_first(state, seat, hand);
   20,728 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::hand_worst_first (12x)
       36 ( 0.00%)              crate::decision::DecisionAnswer::PutOnLibrary(
       36 ( 0.00%)                  order.into_iter().take(*count).collect(),
        .                       )
       12 ( 0.00%)          }
        .                   // A self-discard (cleanup over max hand size, rummaging, a
        .                   // discard cost): every offered card is in our own hand and
        .                   // we're the one choosing. Unlike AutoDecider (which dumps
        .                   // the head of the hand — possibly our best spell), shed the
        .                   // least useful cards. Inquisition-style "choose from an
        .                   // opponent's hand" Discards fail the own-hand guard and
        .                   // fall through to AutoDecider unchanged.
        .                   crate::decision::Decision::Discard { player, count, hand }
-- line 1632 ----------------------------------------
-- line 1693 ----------------------------------------
        .                   }
        .                   // AutoDecider answers every mid-resolution modal with
        .                   // mode 0. Evaluate each mode's settled outcome instead.
        .                   crate::decision::Decision::ChooseMode { num_modes, .. } if eval_modes => {
        .                       crate::decision::DecisionAnswer::Mode(decide_mode_by_outcome(
        .                           state, seat, *num_modes, w,
        .                       ))
        .                   }
    1,860 ( 0.00%)          other => AutoDecider.decide(other),
   16,120 ( 0.00%)  => /home/user/Crabomination/crabomination/src/decision.rs:<crabomination::decision::AutoDecider as crabomination::decision::Decider>::decide (620x)
        .               }
    6,172 ( 0.00%)  }
        .           
        .           /// The minimum legal attack declaration for the active player: only the
        .           /// creatures that "attack each combat if able" (CR 508.1d — `MustAttack` or
        .           /// goaded). Master Warcraft's outside chooser declares this and nothing else.
        .           fn forced_attacks(state: &GameState) -> Vec<Attack> {
        .               use crate::card::Keyword;
        .               let active = state.active_player_idx;
        .               let computed = state.compute_battlefield();
-- line 1711 ----------------------------------------
-- line 1828 ----------------------------------------
        .           }
        .           
        .           /// The combat-damage value an attacker on the battlefield actually assigns:
        .           /// its computed toughness when it has `AssignsCombatDamageByToughness` (Doran,
        .           /// the Siege Tower; CR 510.1c), otherwise its computed power. Falls back to the
        .           /// raw `CardInstance` value when no computed view is available. Used by the
        .           /// block planner so a Doran board's high-toughness attackers are scored at
        .           /// their real threat.
   96,080 ( 0.00%)  fn attacker_damage_value(state: &GameState, id: CardId) -> i32 {
        .               use crate::card::Keyword;
   60,050 ( 0.00%)      if let Some(cp) = state.computed_permanent(id) {
9,952,074 ( 0.32%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (12,010x)
        .                   let mut base = if cp.keywords.contains(&Keyword::AssignsCombatDamageByToughness) {
        .                       cp.toughness
        .                   } else {
        .                       cp.power
        .                   };
        .                   // CR 702.121 — Melee grows the attacker +1/+1 per opponent it attacks
        .                   // this combat. In a duel that's a guaranteed +1 the moment it's
        .                   // declared, so the planner should weigh it in.
   24,020 ( 0.00%)          if cp.keywords.contains(&Keyword::Melee) {
        .                       base += 1;
        .                   }
        .                   base
        .               } else {
        .                   state.battlefield_find(id).map(|c| c.power()).unwrap_or(0)
        .               }
   96,080 ( 0.00%)  }
        .           
        .           /// Instant-speed response layer: when an opponent's spell sits on top of
        .           /// the stack and it's worth answering (it targets the bot's stuff / the
        .           /// bot itself, or it's expensive), cast a counterspell from hand at it.
        .           /// The `would_accept` dry-run is the final gate (timing, mana via
        .           /// auto-tap, per-counter target filters like Spell Snare's MV gate).
  110,672 ( 0.00%)  fn pick_stack_response(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               use crate::game::types::StackItem;
   19,514 ( 0.00%)      let (spell_id, threat) = state.stack.iter().rev().find_map(|si| {
   12,716 ( 0.00%)          let StackItem::Spell { card, caster, target, uncounterable, .. } = si else {
        .                       return None;
        .                   };
   16,176 ( 0.00%)          if *caster == seat || *uncounterable {
        .                       return None;
        .                   }
        .                   // Score the spell like a candidate play of the caster's: mana
        .                   // investment + body + what it's aimed at. Replaces the old
        .                   // "anything ≥ 3 cmc or pointed at us" gate, which burned
        .                   // Counterspell on 3-mana value creatures and face burn at 20 life.
    2,728 ( 0.00%)          let def = &card.definition;
        .                   // Raw card stats lifted onto the profile's scale so the
        .                   // `permanent_value` term below and the bar at the bottom agree.
   19,096 ( 0.00%)          let mut threat = def.cost.cmc() as i32 * w.unit;
   76,524 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (2,728x)
        .                   if def.card_types.contains(&crate::card::CardType::Creature) {
    4,518 ( 0.00%)              threat += (def.power.max(0) + def.toughness.max(0)) * w.unit;
    4,518 ( 0.00%)              threat += (def.keywords.len() as i32).min(3) * w.unit;
        .                   }
   16,128 ( 0.00%)          match target {
        .                       // Aimed at one of our permanents: the spell is worth what
        .                       // we'd lose.
        .                       Some(crate::game::Target::Permanent(id))
    2,150 ( 0.00%)                  if state.battlefield_find(*id).is_some_and(|c| c.controller == seat) =>
        .                       {
      450 ( 0.00%)                  threat += permanent_value(state, *id, w);
  275,300 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (90x)
       90 ( 0.00%)              }
        .                       // Aimed at our face: mildly threatening, urgent when low.
      240 ( 0.00%)              Some(crate::game::Target::Player(p)) if *p == seat => {
      240 ( 0.00%)                  threat += 6 * w.unit;
      480 ( 0.00%)                  if state.effective_life(seat) <= 10 {
    5,264 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (120x)
       38 ( 0.00%)                      threat += 8 * w.unit;
        .                           }
        .                       }
        .                       _ => {}
        .                   }
        .                   Some((card.id, threat))
        .               })?;
        .               // Hold the counter below this bar — a vanilla two-drop or an early
        .               // cantrip isn't worth the bot's only interaction. The bar drops as
        .               // the hand clogs: the Prismari probe measured reactive spells
        .               // rotting to cleanup (42 discards in 60 games) while a full-height
        .               // bar held them for a threat that never came — a counter pitched at
        .               // end of turn answered nothing at all.
   19,096 ( 0.00%)      let bar = if state.players[seat].hand.len() >= 6 { 5 } else { 10 };
    8,184 ( 0.00%)      if threat < bar * w.unit {
        .                   return None;
        .               }
        .               let mut counters: Vec<&crate::card::CardInstance> = state.players[seat]
        .                   .hand
        .                   .iter()
        .                   .filter(|c| {
        .                       c.definition.card_types.contains(&crate::card::CardType::Instant)
      688 ( 0.00%)                  && effect_counters_spells(&c.definition.effect)
        .                   })
        .                   .collect();
        .               // Cheapest answer first — hold the expensive counter for later.
        .               counters.sort_by_key(|c| c.definition.cost.cmc());
      986 ( 0.00%)      for c in counters {
    3,420 ( 0.00%)          let action = GameAction::CastSpell {
      342 ( 0.00%)              card_id: c.id,
        .                       target: Some(crate::game::Target::Permanent(spell_id)),
        .                       additional_targets: vec![],
        .                       mode: None,
        .                       x_value: None,
        .                   };
    2,736 ( 0.00%)          if state.would_accept(action.clone()) {
42,641,526 ( 1.37%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (342x)
   26,676 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (342x)
    2,860 ( 0.00%)              return Some(action);
        .                   }
      244 ( 0.00%)      }
    2,806 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (122x)
        .               None
  110,672 ( 0.00%)  }
        .           
        .           /// React to a threatening opponent ability on the stack with a dedicated
        .           /// ability-counter card (Stifle / Disallow). The ability's source is the
        .           /// target slot. Held separate from `pick_stack_response`'s spell logic so a
        .           /// counter that can only hit abilities still gets used.
   95,298 ( 0.00%)  fn pick_ability_counter_response(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::game::types::StackItem;
        .               // Topmost opponent ability on the stack — counter the most recent one.
   31,466 ( 0.00%)      let source = state.stack.iter().rev().find_map(|si| match si {
    1,688 ( 0.00%)          StackItem::Trigger { source, controller, .. } if *controller != seat => Some(*source),
        .                   _ => None,
        .               })?;
      812 ( 0.00%)      let mut counters: Vec<&crate::card::CardInstance> = state.players[seat]
        .                   .hand
        .                   .iter()
        .                   .filter(|c| {
        .                       c.definition.card_types.contains(&crate::card::CardType::Instant)
      220 ( 0.00%)                  && effect_counters_abilities(&c.definition.effect)
    2,078 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::effect_counters_abilities (110x)
        .                   })
        .                   .collect();
    1,060 ( 0.00%)      counters.sort_by_key(|c| c.definition.cost.cmc());
   11,024 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (424x)
      406 ( 0.00%)      for c in counters {
        .                   let action = GameAction::CastSpell {
        .                       card_id: c.id,
        .                       target: Some(crate::game::Target::Permanent(source)),
        .                       additional_targets: vec![],
        .                       mode: None,
        .                       x_value: None,
        .                   };
        .                   if state.would_accept(action.clone()) {
        .                       return Some(action);
        .                   }
        .               }
      812 ( 0.00%)      None
  108,912 ( 0.00%)  }
        .           
        .           /// Instant removal at a declared attacker, from the DEFENDER's side of
        .           /// combat. The response chain only ever countered spells, so a hand full
        .           /// of kill spells watched every alpha strike connect — the SOS college
        .           /// probes measured 68-78 % of attackers unblocked while removal rotted
        .           /// to cleanup discards. Aim at the most valuable attacker the spell
        .           /// actually answers, before blocks commit; `would_accept` gates instant
        .           /// timing and the ward gate keeps taxes payable.
   92,026 ( 0.00%)  fn pick_defensive_removal(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               use crate::card::CardType;
        .               use crate::effect::{Selector, Value};
        .               let mut attackers: Vec<CardId> = state
   16,732 ( 0.00%)          .attacking()
        .                   .iter()
    3,348 ( 0.00%)          .filter(|a| state.defender_for(a.target) == Some(seat))
        .                   .map(|a| a.attacker)
        .                   .collect();
   16,732 ( 0.00%)      if attackers.is_empty() {
    7,936 ( 0.00%)          return None;
        .               }
    1,440 ( 0.00%)      attackers.sort_by_key(|id| std::cmp::Reverse(permanent_value(state, *id, w)));
1,138,932 ( 0.04%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (720x)
        .               // First-leaf removal shapes, the same convention the counter scan
        .               // uses: a dedicated kill spell, not a buried rider.
        .               fn removal_leaf(e: &Effect) -> Option<&Effect> {
    1,512 ( 0.00%)          match e {
        .                       Effect::Destroy { .. } | Effect::DestroyNoRegen { .. } | Effect::DealDamage { .. } => {
        .                           Some(e)
        .                       }
        .                       Effect::Seq(v) => v.first().and_then(removal_leaf),
        .                       _ => None,
        .                   }
        .               }
      860 ( 0.00%)      for c in state.players[seat]
        .                   .hand
        .                   .iter()
        .                   .filter(|c| c.definition.card_types.contains(&CardType::Instant))
        .               {
      188 ( 0.00%)          let Some(leaf) = removal_leaf(&c.definition.effect) else { continue };
       56 ( 0.00%)          for &atk in &attackers {
        .                       // Worth a card: skip chaff attackers.
      336 ( 0.00%)              if permanent_value(state, atk, w) < 6 * w.unit {
   72,812 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (56x)
        .                           continue;
        .                       }
       80 ( 0.00%)              let answers = match leaf {
        .                           Effect::Destroy { what } | Effect::DestroyNoRegen { what } => {
       32 ( 0.00%)                      matches!(what, Selector::Target(_) | Selector::TargetFiltered { .. })
        .                           }
        .                           Effect::DealDamage { to, amount } => {
       32 ( 0.00%)                      matches!(to, Selector::Target(_) | Selector::TargetFiltered { .. })
       16 ( 0.00%)                          && match amount {
        .                                       Value::Const(n) => state
       24 ( 0.00%)                                  .computed_permanent(atk)
      496 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (8x)
        .                                           .is_some_and(|cp| {
        .                                               let marked = state
        .                                                   .battlefield_find(atk)
        8 ( 0.00%)                                          .map(|c| c.damage as i32)
        .                                                   .unwrap_or(0);
       32 ( 0.00%)                                      *n >= cp.toughness - marked
        .                                           }),
        .                                       _ => false,
        .                                   }
        .                           }
        .                           _ => false,
        .                       };
       12 ( 0.00%)              if !answers {
        .                           continue;
        .                       }
      120 ( 0.00%)              let action = GameAction::CastSpell {
       12 ( 0.00%)                  card_id: c.id,
        .                           target: Some(Target::Permanent(atk)),
        .                           additional_targets: vec![],
        .                           mode: None,
        .                           x_value: None,
        .                       };
       72 ( 0.00%)              if !ward_gate_ok(state, seat, &action) {
   12,544 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::ward_gate_ok (12x)
        .                           continue;
        .                       }
       96 ( 0.00%)              if state.would_accept(action.clone()) {
  487,883 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (12x)
      936 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (12x)
        .                           return Some(action);
        .                       }
       24 ( 0.00%)          }
      276 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (12x)
        .               }
      430 ( 0.00%)      None
   66,928 ( 0.00%)  }
        .           
        .           /// SOS Prepare — the inset spell is a one-shot resource carried by a
        .           /// fragile body. When an opponent's spell on the stack targets one of the
        .           /// bot's prepared creatures, cast the inset spell in response, so the
        .           /// resource is spent before the body (and the Prepared counter with it)
        .           /// is answered. `would_accept` gates timing: only an instant-speed inset
        .           /// spell actually fires here, a sorcery copy is simply rejected.
  136,140 ( 0.00%)  fn pick_prepare_response(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               use crate::game::types::StackItem;
   19,074 ( 0.00%)      let threatened: Vec<CardId> = state.stack.iter().rev().find_map(|si| {
   12,788 ( 0.00%)          let StackItem::Spell { caster, target, additional_targets, .. } = si else {
        .                       return None;
        .                   };
   10,784 ( 0.00%)          if *caster == seat {
        .                       return None;
        .                   }
        .                   let hits: Vec<CardId> = target
        .                       .iter()
        .                       .chain(additional_targets.iter())
    2,272 ( 0.00%)              .filter_map(|t| match t {
        .                           Target::Permanent(id) => state
        .                               .battlefield_find(*id)
        .                               .filter(|c| {
    1,824 ( 0.00%)                          c.controller == seat
       90 ( 0.00%)                              && c.definition.prepare_spell.is_some()
        .                                       && c.counter_count(crate::card::CounterType::Prepared) > 0
        .                               })
        .                               .map(|c| c.id),
        .                           _ => None,
        .                       })
        .                       .collect();
    5,204 ( 0.00%)          if hits.is_empty() { None } else { Some(hits) }
        .               })?;
        .               for creature_id in threatened {
        .                   let Some(c) = state.battlefield_find(creature_id) else { continue };
        .                   let Some(spell) = c.definition.prepare_spell.as_deref() else { continue };
        .                   // Same construction as the main-phase candidate block.
        .                   let (target, additional_targets) = if spell.effect.requires_target() {
        .                       let (t, extras) = state.auto_targets_for_effect_all_slots(&spell.effect, seat, None);
        .                       if t.is_none() {
-- line 2093 ----------------------------------------
-- line 2112 ----------------------------------------
        .                   if !ward_gate_ok(state, seat, &action) {
        .                       continue;
        .                   }
        .                   if state.would_accept(action.clone()) {
        .                       return Some(action);
        .                   }
        .               }
        .               None
  108,912 ( 0.00%)  }
        .           
        .           /// True when the effect tree's primary action counters a spell (the shapes
        .           /// a dedicated counterspell card uses — not buried `MayDo` riders).
        .           fn effect_counters_spells(eff: &Effect) -> bool {
    2,112 ( 0.00%)      match eff {
        .                   Effect::CounterSpell { .. }
        .                   | Effect::CounterSpellExileSameNamed { .. }
        .                   | Effect::CounterSpellToZone { .. }
        .                   | Effect::CounterUnlessPaid { .. }
        .                   | Effect::CounterUnless { .. } => true,
        .                   Effect::Seq(v) => v.first().is_some_and(effect_counters_spells),
        .                   _ => false,
        .               }
        .           }
        .           
        .           /// True when the effect can counter an activated/triggered ability (Stifle's
        .           /// `CounterAbility`, or a modal counter like Disallow whose `ChooseN`/
        .           /// `ChooseMode` carries a `CounterAbility` arm).
      330 ( 0.00%)  fn effect_counters_abilities(eff: &Effect) -> bool {
    1,032 ( 0.00%)      match eff {
        .                   Effect::CounterAbility { .. } => true,
        .                   Effect::Seq(v) => v.first().is_some_and(effect_counters_abilities),
        .                   Effect::ChooseMode(modes) => modes.iter().any(effect_counters_abilities),
        .                   Effect::ChooseN { modes, .. } => modes.iter().any(effect_counters_abilities),
        .                   _ => false,
        .               }
      550 ( 0.00%)  }
        .           
        .           /// Land-count mulligan heuristic. A keepable opening hand wants roughly
        .           /// 2–5 lands out of seven; 0–1 (screw) or 6–7 (flood) are shipped. We stop
        .           /// digging after two mulligans (a London mulligan past that bottoms too
        .           /// many cards to be worth chasing a perfect curve) and always keep a hand
        .           /// of three or fewer cards. Reads land counts off the live hand zone since
        .           /// the `Decision::Mulligan` payload only carries names.
        .           /// Colors a land card could tap for, for mulligan color-screw checks.
        .           /// Reads basic land types (Plains→W, …) plus `AddMana` effects on its
        .           /// activated abilities; "any color" payloads yield the full WUBRG set.
        .           fn land_color_output(card: &CardDefinition) -> crate::mana::ColorSet {
        .               use crate::card::LandType;
        .               use crate::mana::{Color, ColorSet};
        .               let mut set = ColorSet::empty();
        .               for lt in &card.subtypes.land_types {
  158,258 ( 0.01%)          match lt {
      912 ( 0.00%)              LandType::Plains => set.insert(Color::White),
    3,978 ( 0.00%)              LandType::Island => set.insert(Color::Blue),
    3,898 ( 0.00%)              LandType::Swamp => set.insert(Color::Black),
      764 ( 0.00%)              LandType::Mountain => set.insert(Color::Red),
        .                       LandType::Forest => set.insert(Color::Green),
        .                       _ => {}
        .                   }
        .               }
   21,548 ( 0.00%)      for ab in &card.activated_abilities {
   32,322 ( 0.00%)          accumulate_mana_colors(&ab.effect, &mut set);
    8,700 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::accumulate_mana_colors (174x)
        .               }
   24,236 ( 0.00%)      set
        .           }
        .           
        .           /// Choose which land to play this turn. Among the lands the engine would
        .           /// accept, prefer the one that covers the most colors the bot's hand wants
        .           /// but can't yet produce from the lands it already controls — basic
        .           /// mana-fixing so a green hand doesn't strand its spells behind a Mountain.
        .           /// Falls back to the first playable land when nothing improves color
        .           /// coverage (or no land needs fixing).
-- line 2183 ----------------------------------------
-- line 2186 ----------------------------------------
        .           /// gates. Statics granted by *other* permanents aren't the land's
        .           /// property and aren't consulted.)
        .           fn land_enters_tapped(def: &crate::card::CardDefinition) -> bool {
        .               def.static_abilities
        .                   .iter()
        .                   .any(|s| matches!(s.effect, crate::card::StaticEffect::EntersTapped { .. }))
        .           }
        .           
   14,100 ( 0.00%)  fn pick_land_to_play(state: &GameState, seat: usize, w: &EvalWeights) -> Option<CardId> {
        .               use crate::mana::{Color, ColorSet};
        .               const WUBRG: [Color; 5] =
        .                   [Color::White, Color::Blue, Color::Black, Color::Red, Color::Green];
        .           
        .               // Colors already producible from battlefield lands the bot controls.
    1,410 ( 0.00%)      let have = state
        .                   .battlefield
        .                   .iter()
   63,048 ( 0.00%)          .filter(|c| c.controller == seat && c.definition.is_land())
  168,538 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (12,364x)
        .                   .fold(ColorSet::empty(), |acc, c| acc.union(land_color_output(&c.definition)));
        .               // Colors the bot's nonland hand cards want to be cast.
        .               let mut want = ColorSet::empty();
   10,792 ( 0.00%)      for c in state.players[seat].hand.iter().filter(|c| !c.definition.is_land()) {
   57,124 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (3,742x)
    8,478 ( 0.00%)          for col in c.definition.cost.colors() {
  574,466 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (2,826x)
        .                       want.insert(col);
        .                   }
        .               }
        .               // The colors still missing from the bot's mana base.
        .               let needed: Vec<Color> =
    7,050 ( 0.00%)          WUBRG.into_iter().filter(|&col| want.contains(col) && !have.contains(col)).collect();
        .           
    2,820 ( 0.00%)      if !w.land_urgency {
        .                   let mut best: Option<(CardId, usize)> = None;
    6,562 ( 0.00%)          for c in state.players[seat].hand.iter().filter(|c| c.definition.is_land()) {
   57,124 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (3,742x)
    6,412 ( 0.00%)              if !state.would_accept(GameAction::PlayLand(c.id)) {
23,505,038 ( 0.75%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (916x)
        .                           continue;
        .                       }
        .                       let out = land_color_output(&c.definition);
        .                       let coverage = needed.iter().filter(|&&col| out.contains(col)).count();
        .                       // Higher coverage wins; the first playable land is the fallback (so a
        .                       // colorless/utility land still gets played when nothing needs fixing).
      920 ( 0.00%)              if best.is_none_or(|(_, s)| coverage > s) {
      460 ( 0.00%)                  best = Some((c.id, coverage));
        .                       }
        .                   }
        .                   return best.map(|(id, _)| id);
        .               }
        .           
        .               // Per-color urgency: the cheapest hand card wanting that color sets
        .               // how soon a source is needed. A {B} two-drop scores 6, a {B}
        .               // six-drop 2 — both "missing", not equally missing.
-- line 2235 ----------------------------------------
-- line 2285 ----------------------------------------
        .                   if tapped {
        .                       score -= 1;
        .                   }
        .                   if best.is_none_or(|(_, s)| score > s) {
        .                       best = Some((c.id, score));
        .                   }
        .               }
        .               best.map(|(id, _)| id)
   12,690 ( 0.00%)  }
        .           
        .           /// Bot policy for `Decision::OptionalTrigger`: take the trigger unless its
        .           /// matching `MayDo` body imposes a clear self-cost (lose life / sacrifice /
        .           /// discard on the bot). `AutoDecider` declines *every* optional trigger,
        .           /// which means a bot would never take a beneficial "you may" (Provoke's
        .           /// "you may", Boast token riders, etc.); this makes those fire.
        .           pub fn optional_trigger_beneficial(state: &GameState, source: CardId, description: &str) -> bool {
        .               // Locate the source card's definition in any zone the bot can see.
-- line 2301 ----------------------------------------
-- line 2514 ----------------------------------------
        .           /// adds the bot's least-covered color, else (no basic land among the
        .           /// candidates) grab the highest-mana-value candidate — a creature/spell
        .           /// tutor (Fauna Shaman, Imperial Recruiter, Spellseeker) should fetch its
        .           /// most impactful hit, not the first one, and certainly not fizzle like the
        .           /// stock `AutoDecider`.
        .           /// Rough board value of a permanent for target selection: mana value + size,
        .           /// plus a loyalty term for planeswalkers and a small legendary premium. When
        .           /// the profile enables it, a keyword term (see [`keyword_value`]) too.
  197,420 ( 0.01%)  fn permanent_value(state: &GameState, id: crate::card::CardId, w: &EvalWeights) -> i32 {
        .               use crate::card::{CardType, CounterType, Supertype};
   98,710 ( 0.00%)      let Some(c) = state.computed_permanent(id) else { return 0 };
20,474,709 ( 0.66%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (19,742x)
        .               let inst = state.battlefield_find(id);
   59,226 ( 0.00%)      let mut v = inst.map(|c| c.definition.cost.cmc() as i32).unwrap_or(0) * w.cmc;
  573,534 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (19,742x)
        .               if c.card_types.contains(&CardType::Creature) {
  181,620 ( 0.01%)          v += w.creature_base + c.power.max(0) * w.power + c.toughness.max(0) * w.toughness;
   54,486 ( 0.00%)          if w.keyword_pct != 0 {
        .                       v += keyword_value(&c.keywords, c.power, w) * w.keyword_pct / 100;
        .                   }
        .               }
        .               if c.card_types.contains(&CardType::Planeswalker) {
        .                   v += inst.map(|c| c.counter_count(CounterType::Loyalty) as i32).unwrap_or(0) * w.unit;
        .               }
   98,710 ( 0.00%)      if c.supertypes.contains(&Supertype::Legendary) {
        .                   v += 2 * w.unit;
        .               }
        .               // A Prepared counter on a prepare-spell body is a castable spell in
        .               // waiting (SOS): worth about the inset spell's mana value. Gives the
        .               // eval, the mid-resolution mode picker (Biblioplex Tomekeeper's
        .               // prepare-vs-unprepare), the attack simulation (attack-trigger
        .               // preparers gain the counter mid-sim), and removal targeting a live
        .               // read on the resource — an opponent's prepared creature IS the
        .               // better kill at equal stats.
        .               if let Some(inst) = inst
   98,710 ( 0.00%)          && inst.counter_count(CounterType::Prepared) > 0
  197,420 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (19,742x)
        .                   && let Some(spell) = inst.definition.prepare_spell.as_deref()
        .               {
        .                   v += (1 + spell.cost.cmc() as i32) * w.unit;
        .               }
        .               v
  177,678 ( 0.01%)  }
        .           
        .           /// Keyword contribution to a creature's board value, in `w.unit`-scaled
        .           /// points. Ported from Forge's `CreatureEvaluator`, whose central idea is
        .           /// that keywords split into two families:
        .           ///
        .           /// * **Offensive** -- evasion and damage riders are worth what they let the
        .           ///   body actually deal, so they scale with power. Flying on a 5/5 is a
        .           ///   five-point-per-turn clock; flying on a 1/1 is a chump-blocker that
-- line 2561 ----------------------------------------
-- line 2654 ----------------------------------------
        .           /// 20 points it was under the linear term -- only the shape changes, which
        .           /// keeps this comparable against the baseline on the ladder without a
        .           /// wholesale re-tune of every other weight.
        .           ///
        .           /// Expressed in tenths of a point (then scaled by `unit`) so the curve stays
        .           /// strictly increasing under integer arithmetic -- a flat spot would make
        .           /// "gain 1 life" evaluate to exactly zero.
        .           fn life_value(life: i32, w: &EvalWeights) -> i32 {
   21,688 ( 0.00%)      if !w.concave_life {
   10,844 ( 0.00%)          return life * w.unit;
        .               }
        .               /// Tenths of a point per life total, index = life, 0..=20.
        .               const LIFE_TENTHS: [i32; 21] = [
        .                   0, 20, 40, 60, 80, 90, 100, 110, 120, 130, 140, 148, 156, 164, 172, 180, 184, 188, 192,
        .                   196, 200,
        .               ];
        .               const MAX: i32 = LIFE_TENTHS.len() as i32 - 1;
        .               let tenths = if life <= 0 {
-- line 2671 ----------------------------------------
-- line 2674 ----------------------------------------
        .                   LIFE_TENTHS[life as usize]
        .               } else {
        .                   // Past the starting total each extra point is worth the same as the
        .                   // shallowest part of the curve (0.4), not nothing -- Ad Nauseam and
        .                   // friends do care about a big buffer.
        .                   LIFE_TENTHS[MAX as usize] + (life - MAX) * 4
        .               };
        .               tenths * w.unit / 10
   10,844 ( 0.00%)  }
        .           
        .           /// Keep-value for deciding which of the bot's *own* permanents to give up (to an
        .           /// edict, a "sacrifice a creature" cost, or a self-vote). Distinct from
        .           /// `permanent_value`, which ranks removal targets: here a token is the ideal
        .           /// thing to lose (it can't be recast and vanishes on leaving), so it sorts
        .           /// strictly below every real card, even a bare land of `permanent_value` 0.
        .           fn sacrifice_keep_value(state: &GameState, id: crate::card::CardId, w: &EvalWeights) -> i32 {
        .               if state.battlefield_find(id).is_some_and(|c| c.is_token) {
-- line 2690 ----------------------------------------
-- line 2996 ----------------------------------------
        .               )
        .           }
        .           
        .           /// Ascending-usefulness ranking of `offered` hand cards (worst first) —
        .           /// the shed order shared by self-discards and sacrifice/discard-cost
        .           /// `ChooseCards` prompts. Surplus lands go first once the bot is no
        .           /// longer mana-light; otherwise the most expensive spells (least likely
        .           /// to be cast soon) are pitched. Ties keep hand order.
      144 ( 0.00%)  fn hand_worst_first(
        .               state: &GameState,
        .               seat: usize,
        .               offered: &[(crate::card::CardId, String)],
        .           ) -> Vec<crate::card::CardId> {
        .               // Lands already in play: once we have plenty, extra lands in hand are the
        .               // first thing to pitch; while still mana-light, keep them.
        .               let lands_in_play = state
        .                   .battlefield
-- line 3012 ----------------------------------------
-- line 3018 ----------------------------------------
        .               // holding a fistful of duplicate lands shouldn't cost us our spells, and a
        .               // flooded bot (≥5 in play) pitches every spare land first.
        .               let mut lands_still_wanted = 5usize.saturating_sub(lands_in_play);
        .               // Score each offered card — LOWER is pitched sooner.
        .               let mut scored: Vec<(i64, crate::card::CardId)> = offered
        .                   .iter()
        .                   .filter_map(|(id, _)| {
        .                       let card = state.players[seat].hand.iter().find(|c| c.id == *id)?;
      272 ( 0.00%)              let score = if card.definition.is_land() {
    1,242 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (84x)
        .                           // Keep lands up to the buffer; surplus lands are worth the
        .                           // least so they're pitched first.
      106 ( 0.00%)                  if lands_still_wanted > 0 {
      106 ( 0.00%)                      lands_still_wanted -= 1;
        .                               1_000
        .                           } else {
        .                               -100
        .                           }
        .                       } else {
        .                           // Among spells, keep the cheap (castable) ones; pitch the
        .                           // most expensive first.
      200 ( 0.00%)                  -(card.definition.cost.cmc() as i64)
    1,354 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (50x)
        .                       };
        .                       Some((score, *id))
        .                   })
        .                   .collect();
        .               scored.sort_by_key(|(s, _)| *s);
       12 ( 0.00%)      scored.into_iter().map(|(_, id)| id).collect()
       96 ( 0.00%)  }
        .           
        .           /// Order a Scry / Surveil / Rearrange window. `AutoDecider` keeps every
        .           /// card on top — a no-op that wastes every scry in the catalog (the SOS
        .           /// school lands alone surveil in every college deck). The land logic is
        .           /// the discard ranker's (see [`hand_worst_first`]): a land is wanted
        .           /// while total sources — in play plus in hand — run below the
        .           /// five-source buffer, surplus past it; a spell is kept unless its cost
        .           /// sits more than two land drops beyond what the bot can see. Kept cards
-- line 3053 ----------------------------------------
-- line 3185 ----------------------------------------
        .           /// keeps the conservative decline.
        .           fn decide_optional_by_outcome(state: &GameState, seat: usize, w: &EvalWeights) -> Option<bool> {
        .               use crate::decision::DecisionAnswer;
        .               let yes = settle_answer(state, seat, w, DecisionAnswer::Bool(true))?;
        .               let no = settle_answer(state, seat, w, DecisionAnswer::Bool(false))?;
        .               Some(yes > no)
        .           }
        .           
  248,512 ( 0.01%)  fn accumulate_mana_colors(eff: &Effect, set: &mut crate::mana::ColorSet) {
  310,640 ( 0.01%)      match eff {
   62,128 ( 0.00%)          Effect::AddMana { pool, .. } => accumulate_payload_colors(pool, set),
        .                   Effect::Seq(v) => v.iter().for_each(|e| accumulate_mana_colors(e, set)),
        .                   _ => {}
        .               }
  248,512 ( 0.01%)  }
        .           
        .           fn accumulate_payload_colors(pool: &ManaPayload, set: &mut crate::mana::ColorSet) {
  683,408 ( 0.02%)      match pool {
   62,128 ( 0.00%)          ManaPayload::Colors(cs) | ManaPayload::OfColors(cs, _) => {
        .                       cs.iter().for_each(|c| set.insert(*c))
        .                   }
        .                   ManaPayload::OfColor(c, _) => set.insert(*c),
        .                   ManaPayload::AnyOneColor(_)
        .                   | ManaPayload::AnyColors(_)
        .                   | ManaPayload::AnyColorOpponentCouldProduce
        .                   | ManaPayload::AnyColorYouCouldProduce
        .                   | ManaPayload::AnyTypeTriggerSourceProduces
-- line 3211 ----------------------------------------
-- line 3229 ----------------------------------------
        .           fn decide_mulligan(
        .               state: &GameState,
        .               seat: usize,
        .               mulligans_taken: usize,
        .               w: &EvalWeights,
        .           ) -> crate::decision::DecisionAnswer {
        .               use crate::decision::DecisionAnswer;
        .               let hand = &state.players[seat].hand;
      434 ( 0.00%)      let lands = hand.iter().filter(|c| c.definition.is_land()).count();
    6,422 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (434x)
        .               // Curve check: a 2–5-land hand is only worth keeping if it has at least
        .               // one nonland spell cheap enough to cast in the first few turns — three
        .               // lands plus four 7-drops is a screwed keep. "Castable early" means a
        .               // spell whose mana value is within `lands + 1` (a generous early-curve
        .               // window that still trusts a couple of draws).
        .               // Color-screw awareness: an early play only counts if the hand's lands
        .               // can actually produce its colored pips. Three Forests + a hand of blue
        .               // spells is a screwed keep even though the curve looks fine.
        .               let producible = hand
        .                   .iter()
      434 ( 0.00%)          .filter(|c| c.definition.is_land())
    6,422 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (434x)
        .                   .fold(crate::mana::ColorSet::empty(), |acc, c| {
        .                       acc.union(land_color_output(&c.definition))
        .                   });
        .               let has_early_play = hand.iter().any(|c| {
    1,082 ( 0.00%)          if c.definition.is_land() || c.definition.cost.cmc() as usize > lands + 1 {
    2,598 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (174x)
    3,108 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (112x)
        .                       return false;
        .                   }
        .                   let mut need = crate::mana::ColorSet::empty();
      234 ( 0.00%)          for col in c.definition.cost.colors() {
   16,211 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (78x)
        .                       need.insert(col);
        .                   }
        .                   need.is_subset_of(producible)
        .               });
      124 ( 0.00%)      if !w.mull_quality {
      138 ( 0.00%)          let keepable = ((2..=5).contains(&lands) && has_early_play) || hand.len() <= 3;
       56 ( 0.00%)          return if keepable || mulligans_taken >= 2 {
        .                       DecisionAnswer::Keep
        .                   } else {
        .                       DecisionAnswer::TakeMulligan
        .                   };
        .               }
        .           
        .               // How many spells this hand can actually deploy in the early turns,
        .               // not merely whether one exists: a two-lander living off a single
-- line 3272 ----------------------------------------
-- line 3381 ----------------------------------------
        .           }
        .           
        .           /// Run one gated specialty block. Release skips it when its bit is clear;
        .           /// debug runs it anyway and asserts the gate against what it actually
        .           /// emitted, so the whole suite audits the mask on real boards rather than
        .           /// against a re-derived list.
        .           macro_rules! gated_block {
        .               ($mask:expr, $bit:expr, $out:expr, $body:block) => {{
    7,024 ( 0.00%)          let gate = $mask & $bit != 0;
  259,888 ( 0.01%)          if gate || cfg!(debug_assertions) {
        .                       let before = $out.len();
        .                       $body
        .                       debug_assert!(
        .                           gate || $out.len() == before,
        .                           concat!("cast_candidates gate ", stringify!($bit), " skipped a real candidate"),
        .                       );
        .                   }
        .               }};
-- line 3398 ----------------------------------------
-- line 3409 ----------------------------------------
        .               /// Some permanent carries a prepared inset spell.
        .               prepared: bool,
        .           }
        .           
        .           impl BoardFacts {
        .               fn gather(state: &GameState, seat: usize) -> Self {
        .                   let mut f = BoardFacts { repartee: false, grants_convoke: false, prepared: false };
        .                   for c in state.battlefield.iter() {
  251,976 ( 0.01%)              if c.controller != seat {
        .                           continue;
        .                       }
  188,244 ( 0.01%)              f.repartee = f.repartee
        .                           || c.definition.triggered_abilities.iter().any(is_repartee_trigger);
  125,496 ( 0.00%)              f.grants_convoke = f.grants_convoke
        .                           || c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, crate::effect::StaticEffect::GrantConvokeToSpells { .. })
        .                           });
  250,992 ( 0.01%)              f.prepared = f.prepared
   62,748 ( 0.00%)                  || (c.definition.prepare_spell.is_some()
        .                               && c.counter_count(crate::card::CounterType::Prepared) > 0);
        .                   }
        .                   f
        .               }
        .           }
        .           
        .           /// One walk of `seat`'s hand for every specialty block's entry predicate.
        .           fn hand_specialties(state: &GameState, seat: usize, facts: &BoardFacts) -> u32 {
        .               use crate::card::Keyword;
        .               let mut m = 0;
        .               for c in state.players[seat].hand.iter() {
        .                   let def = &c.definition;
        .                   for kw in &def.keywords {
   93,944 ( 0.00%)              m |= match kw {
        .                           Keyword::Delve => spec::DELVE,
        .                           Keyword::Convoke | Keyword::Improvise => spec::CONVOKE,
        .                           Keyword::Conspire => spec::CONSPIRE,
        .                           Keyword::Kicker(_) | Keyword::Offspring(_) => spec::KICKER,
        .                           Keyword::Multikicker(_) => spec::MULTIKICKER,
        .                           _ => 0,
        .                       };
        .                   }
   17,540 ( 0.00%)          if def.gift.is_some() {
        .                       m |= spec::GIFT;
        .                   }
   87,700 ( 0.00%)          if matches!(
   17,540 ( 0.00%)              def.effect,
        .                       Effect::Spree { .. }
        .                           | Effect::Tiered { .. }
        .                           | Effect::ChooseModesCast { .. }
        .                           | Effect::ChooseModesByPoints { .. }
        .                   ) {
        .                       m |= spec::SPREE;
        .                   }
   35,080 ( 0.00%)          if !def.kicker_options.is_empty() {
        .                       m |= spec::KICKERS;
        .                   }
   70,160 ( 0.00%)          if def.bestow.is_some() {
        .                       m |= spec::BESTOW;
        .                   }
   52,620 ( 0.00%)          if def.adventure.is_some() {
        .                       m |= spec::ADVENTURE;
        .                   }
   52,620 ( 0.00%)          if def.omen.is_some() {
        .                       m |= spec::OMEN;
        .                   }
   52,620 ( 0.00%)          if def.prototype.is_some() {
        .                       m |= spec::PROTOTYPE;
        .                   }
        .                   if def.split.as_deref().is_some_and(|s| !s.aftermath) {
        .                       m |= spec::SPLIT;
        .                   }
   17,540 ( 0.00%)          if def.back_face.is_some() {
        .                       m |= spec::BACK;
        .                   }
   52,620 ( 0.00%)          if def.alternative_cost.is_some() {
        .                       m |= spec::ALT_COST;
        .                   }
        .               }
        .               // A convoke *grant* is a board property; the block tested it per hand
        .               // card, which ran a whole-battlefield walk once per card per tick.
   21,072 ( 0.00%)      if facts.grants_convoke {
        .                   m |= spec::CONVOKE;
        .               }
        .               m
        .           }
        .           
        .           /// One walk of `seat`'s graveyard for the graveyard blocks' predicates.
        .           fn graveyard_specialties(state: &GameState, seat: usize) -> u32 {
        .               use crate::card::Keyword;
        .               let mut m = 0;
        .               for c in state.players[seat].graveyard.iter() {
        .                   let def = &c.definition;
        .                   for kw in &def.keywords {
  337,148 ( 0.01%)              m |= match kw {
        .                           Keyword::Flashback(_)
        .                           | Keyword::JumpStart
        .                           | Keyword::GraveyardCast
        .                           | Keyword::Disturb(_)
        .                           | Keyword::Mayhem(_)
        .                           | Keyword::Harmonize(_) => spec::GY_RECAST,
        .                           _ => 0,
        .                       };
        .                   }
   85,028 ( 0.00%)          if c.granted_flashback_eot.is_some() || c.granted_harmonize_eot.is_some() {
        .                       m |= spec::GY_RECAST;
        .                   }
        .                   if def.activated_abilities.iter().any(|ab| ab.from_graveyard) {
        .                       m |= spec::GY_RECAST;
        .                   }
        .                   if def.split.as_deref().is_some_and(|s| s.aftermath) {
        .                       m |= spec::GY_AFTERMATH;
        .                   }
   85,028 ( 0.00%)          if c.may_cast_back_from_graveyard && def.back_face.is_some() {
        .                       m |= spec::GY_BACK;
        .                   }
        .               }
        .               m
        .           }
        .           
  126,432 ( 0.00%)  fn cast_candidates(
        .               state: &GameState,
        .               seat: usize,
        .               probe: &GameState,
        .               w: &EvalWeights,
        .           ) -> Vec<(GameAction, bool)> {
        .               // Build list of castable non-land spells. Affordability + target
        .               // pre-filters reduce the candidate set; the FINAL gate is still the
        .               // engine dry-run, which discards anything the engine would reject
-- line 3536 ----------------------------------------
-- line 3537 ----------------------------------------
        .               // (sorcery timing under Teferi, Damping Sphere mana tax, hexproof
        .               // targets, stolen permanents, etc.) — but for this main block it runs
        .               // *lazily* at the pick site below, in descending score order, so a
        .               // typical tick probes one or two candidates instead of the whole hand.
        .               //
        .               // One board walk and one hand walk for every block below — see
        .               // `BoardFacts` / `spec`. SOS Repartee (`facts.repartee`) steers the
        .               // plain-cast block toward offering creature-aimed sibling candidates.
    7,024 ( 0.00%)      let facts = BoardFacts::gather(state, seat);
    7,024 ( 0.00%)      let mask = hand_specialties(state, seat, &facts)
        .                   | graveyard_specialties(state, seat)
        .                   | if facts.prepared { spec::PREPARED } else { 0 };
    7,024 ( 0.00%)      let has_repartee = facts.repartee;
        .               let mut unvalidated: Vec<GameAction> = state.players[seat]
        .                   .hand
        .                   .iter()
   17,540 ( 0.00%)          .filter(|c| !c.definition.is_land())
  266,384 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (17,540x)
        .                   // Pure temp-pump instants are combat tricks: held for the fight
        .                   // window (`pick_combat_trick`), not main-phased where the buff
        .                   // telegraphs and fizzles at cleanup.
        .                   .filter(|c| !is_combat_trick(&c.definition))
        .                   // Spree spells need `CastSpellSpree` with chosen modes — a plain
        .                   // `CastSpell` resolves them as a no-op. They get their own candidate
        .                   // block below.
   20,158 ( 0.00%)          .filter(|c| !matches!(c.definition.effect, Effect::Spree { .. }))
        .                   // A gift card whose base effect is empty (a permanent gift — the payoff
        .                   // is a `SourceGiftPromised`-gated ETB) is wasted by a plain cast; it
        .                   // gets a `CastGift` candidate in the gift block below instead.
   31,188 ( 0.00%)          .filter(|c| !(c.definition.gift.is_some() && matches!(c.definition.effect, Effect::Noop)))
   60,570 ( 0.00%)          .filter(|c| can_afford_in_state(state, seat, c, w))
51,627,340 ( 1.66%)  => crabomination/src/server/bot.rs:crabomination::server::bot::can_afford_in_state (12,114x)
        .                   .flat_map(|c| {
        .                       // For modal effects (ChooseMode), enumerate each mode so the
        .                       // bot can pick (e.g.) Drown in the Loch's mode 1 (destroy
        .                       // creature) when no opp spell is on the stack to counter.
        .                       // Falls back to `mode: None` (engine defaults to mode 0) for
        .                       // non-modal spells.
        .                       let modes: Vec<Option<usize>> = match modal_mode_count(&c.definition.effect) {
        .                           Some(n) => (0..n).map(Some).collect(),
   31,430 ( 0.00%)                  None => vec![None],
        .                       };
   12,572 ( 0.00%)              let x_value = if x_relevant(&c.definition) {
        .                           Some(max_affordable_x(state, seat, c, w))
        .                       } else {
        .                           None
        .                       };
   62,860 ( 0.00%)              modes.into_iter().flat_map(move |mode| {
        .                           // Pick a target appropriate to the chosen mode (ChooseMode
        .                           // mode-aware filter check happens in the cast paths).
        .                           // Multi-target shapes (Snow Day, Homesickness, Cost of
        .                           // Brilliance, Render Speechless, Vibrant Outburst, …) ask
        .                           // the picker for every slot index used by the effect tree;
        .                           // slots that find no legal target are skipped, matching
        .                           // "up to N target" semantics.
   18,858 ( 0.00%)                  let mode_effect = mode_branch(&c.definition.effect, mode);
        .                           // Beneficial Auras pick their host explicitly: `Effect::Attach`
        .                           // isn't classified friendly by the generic auto-targeter, so
        .                           // without this a Rancor walks the OPPONENT's creatures first.
        .                           // No friendly host at all → skip the candidate rather than
        .                           // let the fallback pump an opposing creature.
      458 ( 0.00%)                  let (target, additional_targets) = if is_beneficial_aura(&c.definition) {
    8,850 ( 0.00%)                      match beneficial_aura_host(state, seat, c, w) {
3,383,648 ( 0.11%)  => crabomination/src/server/bot.rs:crabomination::server::bot::beneficial_aura_host (606x)
        .                                   Some(t) => (Some(t), vec![]),
        .                                   None => return vec![],
        .                               }
   22,720 ( 0.00%)                  } else if mode_effect.requires_target() {
  204,172 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::requires_target (5,680x)
   10,944 ( 0.00%)                      let (t, extras) =
    5,472 ( 0.00%)                          state.auto_targets_for_effect_all_slots(mode_effect, seat, mode);
    2,736 ( 0.00%)                      if t.is_none() {
        .                                   return vec![];
        .                               }
        .                               (t, extras)
        .                           } else {
        .                               (None, vec![])
        .                           };
   73,692 ( 0.00%)                  let mut out = vec![GameAction::CastSpell {
        .                               card_id: c.id,
        .                               target: target.clone(),
        .                               additional_targets: additional_targets.clone(),
        .                               mode,
        .                               // For X-cost spells (Banefire, Earthquake, Wrath of the
        .                               // Skies, Mind Twist, Repeal, …), pump as much generic
        .                               // mana as the pool can spare into X. Casting at X=0
        .                               // was a known dead end — Banefire dealt 0 damage, Mind
        .                               // Twist discarded nothing, Earthquake was a no-op.
    4,094 ( 0.00%)                      x_value,
        .                           }];
        .                           // SOS Repartee: with a controlled payoff that wants an
        .                           // instant/sorcery to target a CREATURE, an "any target"
        .                           // spell the auto-targeter aimed at a player also gets a
        .                           // creature-aimed sibling candidate. The outcome eval sees
        .                           // the extra triggers fire when it resolves the sibling, so
        .                           // the swap is judged, not assumed.
    4,628 ( 0.00%)                  if has_repartee
        .                               && matches!(target, Some(Target::Player(_)))
        .                               && {
        .                                   use crate::card::CardType;
        .                                   c.definition.card_types.contains(&CardType::Instant)
        .                                       || c.definition.card_types.contains(&CardType::Sorcery)
        .                               }
        .                               && let Some(swap) = best_hostile_creature_target(state, seat, mode_effect, w)
        .                           {
-- line 3637 ----------------------------------------
-- line 3911 ----------------------------------------
        .               // one-splicer case; `would_accept` re-checks the combined cost, and the
        .               // spliced clauses' targets are auto-aimed inside `cast_spell_spliced`.
        .               // Ask for the one category we read, against the probe template this call
        .               // already built. `compute_hand_affordances` would run ~40 categories of
        .               // per-card dry-runs (each a full state clone) and throw 39 of them away —
        .               // and `cast_candidates` runs inside the attack simulations, so that waste
        .               // was multiplied per simulated priority pass. Same result: the sweep's
        .               // `spliceable` field *is* this call, and it is empty off-priority.
   42,144 ( 0.00%)      let spliceable = if state.player_with_priority() == seat {
   14,048 ( 0.00%)          state.spliceable_hand_cards_on(probe, seat)
  638,192 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::spliceable_hand_cards_on (7,024x)
        .               } else {
        .                   Vec::new()
        .               };
   42,144 ( 0.00%)      for (host, splicers) in spliceable {
        .                   let (target, additional_targets) = {
        .                       let eff = state.players[seat]
        .                           .hand
        .                           .iter()
        .                           .find(|c| c.id == host)
        .                           .map(|c| c.definition.effect.clone());
        .                       match eff {
        .                           Some(e) if e.requires_target() => {
-- line 3932 ----------------------------------------
-- line 4453 ----------------------------------------
        .                       castable.push(action);
        .                   }
        .               }
        .               });
        .           
        .               // Adventure creature (CR 715) and plotted cards (CR 702.170d): cast the
        .               // creature half / a plotted card from exile. `would_accept` enforces the
        .               // later-turn + sorcery-speed timing, so this is only offered when legal.
   16,376 ( 0.00%)      for c in state.exile.iter().filter(|c| c.owner == seat) {
    2,344 ( 0.00%)          let action = if c.on_adventure && c.definition.is_land() {
        .                       // CR 715.3d — a land half is played, not cast (FIN's Town cycle).
        .                       GameAction::PlayLand(c.id)
        .                   } else if c.on_adventure {
        .                       let (target, additional_targets) = if c.definition.effect.requires_target() {
        .                           state.auto_targets_for_effect_all_slots(&c.definition.effect, seat, None)
        .                       } else {
        .                           (None, vec![])
        .                       };
        .                       GameAction::CastAdventureCreature {
        .                           card_id: c.id, target, additional_targets, mode: None, x_value: None,
        .                       }
    2,344 ( 0.00%)          } else if state.plotted_cards.contains(&c.id) {
        .                       let (target, additional_targets) = if c.definition.effect.requires_target() {
        .                           state.auto_targets_for_effect_all_slots(&c.definition.effect, seat, None)
        .                       } else {
        .                           (None, vec![])
        .                       };
        .                       GameAction::CastPlotted {
        .                           card_id: c.id, target, additional_targets, mode: None, x_value: None,
        .                       }
-- line 4482 ----------------------------------------
-- line 4531 ----------------------------------------
        .                       x_value: None,
        .                   };
        .                   if GameState::would_accept_on(probe, action.clone()) {
        .                       castable.push(action);
        .                   }
        .               }
        .               });
        .           
    7,024 ( 0.00%)      let mut out: Vec<(GameAction, bool)> = Vec::with_capacity(castable.len() + unvalidated.len());
   19,132 ( 0.00%)      out.extend(castable.into_iter().map(|a| (a, true)));
   14,048 ( 0.00%)      out.extend(unvalidated.into_iter().map(|a| (a, false)));
        .               // Ward gate, applied once for every candidate block above: a cast
        .               // aimed at a warded permanent whose tax the bot can't pay after the
        .               // spell's own cost resolves as a counter, not a cast (the engine
        .               // auto-pays ward and `would_accept` can't see the trigger fail).
   44,472 ( 0.00%)      out.retain(|(a, _)| ward_gate_ok(state, seat, a));
2,927,776 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (7,024x)
   35,120 ( 0.00%)      out
   56,192 ( 0.00%)  }
        .           
   60,876 ( 0.00%)  fn main_phase_action_with(
        .               state: &GameState,
        .               seat: usize,
        .               scored: bool,
        .               w: &EvalWeights,
        .           ) -> GameAction {
        .               // One library-stripped probe template per tick: every candidate dry-run
        .               // below re-clones this light template instead of the full state. The
        .               // library is the largest part of a `GameState` clone and cast/activate/
        .               // play-land legality never reads it (see `affordance_probe_template`),
        .               // so this turns N full-deck clones into one + N light ones.
    3,382 ( 0.00%)      let probe = state.affordance_probe_template();
30,489,930 ( 0.98%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::affordance_probe_template (3,382x)
        .           
        .               // NOTE: the bot deliberately does *not* pre-tap its mana sources here.
        .               //
        .               // It used to: one untapped land per tick until the board was empty,
        .               // which is what made `can_afford_in_state` work off the floating pool.
        .               // The cost was severe and invisible to the unit tests (which all
        .               // pre-fill `mana_pool` by hand). Pools empty at every step and phase
        .               // boundary (CR 500.4), so tapping out in the precombat main left the
-- line 4569 ----------------------------------------
-- line 4573 ----------------------------------------
        .               // of spells were cast in the precombat main. `pick_stack_response`,
        .               // `pick_ability_counter_response`, `pick_combat_trick` and the
        .               // end-of-turn instant window below were all dead code in real play.
        .               //
        .               // Now the engine's auto-tap pays each cast from only the sources it
        .               // needs (`try_pay_with_auto_tap`, which `would_accept_on` already runs
        .               // for every candidate), so leftover mana simply stays untapped and is
        .               // still there at instant speed.
    6,764 ( 0.00%)      if w.legacy_pretap
        .                   && let Some(id) = state
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.controller == seat && c.definition.is_land() && !c.tapped)
        .                       .map(|c| c.id)
        .               {
        .                   let action = GameAction::ActivateAbility {
        .                       card_id: id,
-- line 4589 ----------------------------------------
-- line 4594 ----------------------------------------
        .                       mode: None,
        .                   };
        .                   if GameState::would_accept_on(&probe, action.clone()) {
        .                       return action;
        .                   }
        .               }
        .           
        .               // Everything castable this tick — see `cast_candidates`.
   23,674 ( 0.00%)      let pool = cast_candidates(state, seat, &probe, w);
81,928,299 ( 2.63%)  => crabomination/src/server/bot.rs:crabomination::server::bot::cast_candidates (3,382x)
        .           
        .               // Play a land if possible — gated through `would_accept` for
        .               // the same reason (the engine enforces sorcery timing, lands-
        .               // played-this-turn, etc.). Use the game-level helper so an
        .               // Exploration / Azusa-style ExtraLandPerTurn static lets the bot
        .               // play a second land in the same turn (CR 305.2).
   16,910 ( 0.00%)      if state.can_player_play_land(seat)
2,237,318 ( 0.07%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land (3,382x)
    9,870 ( 0.00%)          && let Some(land_id) = pick_land_to_play(state, seat, w)
26,425,060 ( 0.85%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_land_to_play (1,410x)
        .               {
    1,784 ( 0.00%)          let action = GameAction::PlayLand(land_id);
    1,784 ( 0.00%)          if GameState::would_accept_on(&probe, action.clone()) {
13,341,873 ( 0.43%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (446x)
   15,164 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (446x)
        .                       return action;
        .                   }
        .               }
        .           
        .               // Crucible of Worlds / Ramunap Excavator: replay a land from the
        .               // graveyard if no hand land was played (CR 305 land-from-gy permission).
   14,680 ( 0.00%)      if state.can_player_play_land(seat)
1,987,432 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land (2,936x)
      964 ( 0.00%)          && state.player_may_play_lands_from_graveyard(seat)
        .                   && let Some(land) =
        .                       state.players[seat].graveyard.iter().find(|c| c.definition.is_land())
        .               {
        .                   let action = GameAction::PlayLandFromGraveyard(land.id);
        .                   if GameState::would_accept_on(&probe, action.clone()) {
        .                       return action;
        .                   }
        .               }
        .           
        .               // Impulse exile (Light Up the Stage, Gonti Night Minister): a land the
        .               // seat has a may-play grant on is played from exile before it expires.
   14,680 ( 0.00%)      if state.can_player_play_land(seat)
1,987,432 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land (2,936x)
      964 ( 0.00%)          && let Some(land) = state.exile.iter().find(|c| {
    1,338 ( 0.00%)              c.definition.is_land() && c.may_play_until.is_some_and(|perm| perm.player == seat)
    7,136 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (446x)
        .                   })
        .               {
        .                   let action = GameAction::PlayLand(land.id);
        .                   if GameState::would_accept_on(&probe, action.clone()) {
        .                       return action;
        .                   }
        .               }
        .           
    5,872 ( 0.00%)      if !pool.is_empty() {
        .                   // Magecraft-aware bias: if the bot controls a permanent with a
        .                   // magecraft trigger, prefer instants/sorceries so the trigger
        .                   // fires — IS candidates sort first, and finalist collection stops
        .                   // at the IS/non-IS boundary once an IS line has validated (the
        .                   // lazy-probe equivalent of the old only-IS pool restriction).
        .                   // Push (claude/modern_decks batch 202).
      908 ( 0.00%)          let has_magecraft = state.battlefield.iter().any(|c| {
   26,864 ( 0.00%)              c.controller == seat
        .                           && c.definition.triggered_abilities.iter().any(is_magecraft_trigger)
        .                   });
    2,724 ( 0.00%)          let is_is_spell = |a: &GameAction| {
      116 ( 0.00%)              matches!(a, GameAction::CastSpell { card_id, .. } if is_instant_or_sorcery_in_hand(state, seat, *card_id))
        .                   };
    1,816 ( 0.00%)          if !scored {
        .                       // Uniform baseline: validate everything (the historical
        .                       // behavior) and sample.
        .                       let valid: Vec<GameAction> = pool
        .                           .into_iter()
        .                           .filter(|(a, ok)| *ok || GameState::would_accept_on(&probe, a.clone()))
        .                           .map(|(a, _)| a)
        .                           .collect();
        .                       if !valid.is_empty() {
-- line 4666 ----------------------------------------
-- line 4687 ----------------------------------------
        .                       // * Increment: a controlled Increment body grows when the
        .                       //   cast's mana spent clears its smaller stat.
        .                       // * Infusion: an Infusion-gated card in hand unlocks on any
        .                       //   lifegain, so lifegain casts go first while none has been
        .                       //   gained this turn.
        .                       // Each nudge is 8 = two score points ≈ one mana of cast
        .                       // value on `score_candidate`'s ×4 scale — a tiebreaker, not
        .                       // an override.
    1,816 ( 0.00%)              let has_opus = state.battlefield.iter().any(|c| {
   27,352 ( 0.00%)                  c.controller == seat
        .                               && c.definition.triggered_abilities.iter().any(is_opus_trigger)
        .                       });
    1,816 ( 0.00%)              let increment_bar = increment_threshold(state, seat);
    3,632 ( 0.00%)              let wants_lifegain = state.players[seat].life_gained_this_turn == 0
      882 ( 0.00%)                  && state.players[seat]
        .                               .hand
        .                               .iter()
        .                               .any(|c| card_infusion_gated(&c.definition));
    1,816 ( 0.00%)              let mut ranked: Vec<(i32, GameAction, bool)> = pool
        .                           .into_iter()
        .                           .map(|(a, ok)| {
        .                               let mut s =
   11,438 ( 0.00%)                          score_candidate(state, seat, &a, w) * 4 + jitter_below(4) as i32;
  811,926 ( 0.03%)  => crabomination/src/server/bot.rs:crabomination::server::bot::score_candidate (1,634x)
    8,170 ( 0.00%)                      let spent = if has_opus || increment_bar.is_some() {
        .                                   cast_mana_spent(state, seat, &a)
        .                               } else {
        .                                   0
        .                               };
        .                               if has_opus && spent >= 5 {
        .                                   s += 8 * w.unit;
        .                               }
        .                               if increment_bar.is_some_and(|bar| spent >= bar) {
        .                                   s += 8 * w.unit;
        .                               }
    3,268 ( 0.00%)                      if wants_lifegain && cast_gains_life(state, seat, &a) {
        .                                   s += 8 * w.unit;
        .                               }
   19,608 ( 0.00%)                      (s, a, ok)
        .                           })
        .                           .collect();
    1,816 ( 0.00%)              if has_magecraft {
       12 ( 0.00%)                  ranked.sort_by_key(|&(s, ref a, _)| (!is_is_spell(a), std::cmp::Reverse(s)));
        .                       } else {
        .                           ranked.sort_by_key(|&(s, _, _)| std::cmp::Reverse(s));
        .                       }
        .                       const EVAL_TOP: usize = 3;
        .                       let mut finalists: Vec<(i32, GameAction)> = Vec::new();
   26,032 ( 0.00%)              for (s, a, ok) in ranked {
    3,200 ( 0.00%)                  if finalists.len() >= EVAL_TOP {
        .                               break;
        .                           }
    3,094 ( 0.00%)                  if has_magecraft && !finalists.is_empty() && !is_is_spell(&a) {
        .                               break;
        .                           }
   15,340 ( 0.00%)                  if ok || GameState::would_accept_on(&probe, a.clone()) {
165,748,982 ( 5.32%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (1,534x)
  115,076 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (1,534x)
   18,984 ( 0.00%)                      finalists.push((s, a));
        .                           }
      422 ( 0.00%)              }
    5,612 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (244x)
   18,348 ( 0.00%)              if let Some(best) = pick_by_outcome(state, seat, finalists, w) {
205,755,890 ( 6.60%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_by_outcome (908x)
        .                           // Forge's summon-sick gate (`SpellAbilityPicker`): if the
        .                           // winning line's only gain this turn is a body that can't
        .                           // attack, it is worth exactly as much after combat — and
        .                           // played then it costs the opponent a turn of information
        .                           // and leaves the mana up in between. Hold it.
        .                           //
        .                           // Applied to the *winner* only, deliberately. Screening
        .                           // every candidate this way would have the bot pick some
        .                           // lesser non-creature line now and then have no mana left
        .                           // for the creature it actually wanted in the second main.
    3,392 ( 0.00%)                  let own_main = state.active_player_idx == seat
    1,774 ( 0.00%)                      && matches!(
    1,684 ( 0.00%)                          state.step,
        .                                   TurnStep::PreCombatMain | TurnStep::PostCombatMain
        .                               );
        .                           // Hold a creature that can't attack yet until the second
        .                           // main; hold an instant-speed line until the opponent's
        .                           // turn. Both ask the same question -- "is this worth the
        .                           // same later?" -- and both only fire on our own main phase,
        .                           // where there is a later to wait for.
        .                           let gate = own_main
    1,774 ( 0.00%)                      && ((w.hold_sick && state.step == TurnStep::PreCombatMain)
      752 ( 0.00%)                          || (w.hold_instants && castable_at_instant_speed(state, seat, &best)));
      932 ( 0.00%)                  if gate && !improves_this_turn(state, seat, &best, w) {
      348 ( 0.00%)                      return GameAction::PassPriority;
        .                           }
        .                           // SOS Converge: float a missing color first so the cast
        .                           // counts it — see `pick_converge_prefloat`.
    1,618 ( 0.00%)                  if let Some(tap) = pick_converge_prefloat(state, seat, &best) {
        .                               return tap;
        .                           }
    4,500 ( 0.00%)                  return best;
      348 ( 0.00%)              }
    8,004 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (348x)
        .                   }
        .               }
        .           
        .               // Morph / Disguise (CR 702.36 / 702.166): cast a hand card face down for
        .               // {3} as a 2/2 (with ward {2} for Disguise). Reached only when no normal
        .               // spell candidate validated, so the bot still prefers casting cards face
        .               // up; `would_accept` enforces sorcery timing and the {3} payment.
    4,176 ( 0.00%)      for c in state.players[seat].hand.iter().filter(|c| {
        .                   c.definition.keywords.iter().any(|k| {
    8,224 ( 0.00%)              matches!(
        .                           k,
        .                           crate::card::Keyword::Morph(_)
        .                               | crate::card::Keyword::MorphCost(_)
        .                               | crate::card::Keyword::Megamorph(_)
        .                               | crate::card::Keyword::Disguise(_)
        .                       )
        .                   })
        .               }) {
-- line 4796 ----------------------------------------
-- line 4798 ----------------------------------------
        .                   if GameState::would_accept_on(&probe, action.clone()) {
        .                       return action;
        .                   }
        .               }
        .           
        .               // Discard-activated hand abilities (Magma Opus's {U/R}{U/R}, Discard:
        .               // create a Treasure) — a fallback value play, reached only when the bot
        .               // has no spell/face-down line so it never pitches a castable card.
    6,264 ( 0.00%)      for c in state.players[seat]
        .                   .hand
        .                   .iter()
        .                   .filter(|c| c.definition.discard_activated.is_some())
        .               {
        .                   let action = GameAction::ActivateDiscardAbility { card_id: c.id };
        .                   if GameState::would_accept_on(&probe, action.clone()) {
        .                       return action;
        .                   }
        .               }
        .           
        .               // Activate planeswalker loyalty abilities the bot controls. Pick the
        .               // first usable ability per walker (engine enforces sorcery timing and
        .               // once-per-turn). The candidate set is dry-run-gated so failed targets
        .               // / over-spent loyalty / opp-controlled-walker rejections drop out.
    2,088 ( 0.00%)      if let Some(action) = pick_loyalty_ability(state, seat, w) {
        .                   return action;
        .               }
        .           
        .               // Crew (CR 702.122): turn an uncrewed Vehicle into an attacker by tapping
        .               // the bot's least-valuable untapped creatures. Dry-run-gated.
    2,088 ( 0.00%)      if let Some(action) = pick_crew(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Saddle (CR 702.171): tap the bot's least-valuable untapped creatures to
        .               // saddle a Mount that's about to attack, so its "attacks while saddled"
        .               // riders fire. Dry-run-gated.
    4,176 ( 0.00%)      if let Some(action) = pick_saddle(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Equip (CR 702.6): if the bot controls an Equipment that isn't yet
        .               // attached to one of its creatures, and it controls a creature to wear
        .               // it, move the Equipment onto the biggest such creature. Dry-run-gated
        .               // so the equip cost / sorcery timing / target legality all bottom out
        .               // in `would_accept`.
    4,176 ( 0.00%)      if let Some(action) = pick_equip(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Activated two-slot attach (Brass Squire's "{T}: attach target Equipment
        .               // you control to target creature you control"). The native-equip pass
        .               // above only covers `Keyword::Equip`; this drives the Equipment-mover
        .               // creatures so the AI plays them.
    2,088 ( 0.00%)      if let Some(action) = pick_attach_ability(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Spend surplus energy on beneficial energy-payoff abilities (Bristling
        .               // Hydra's grow, Longtusk Cub's +1/+1, Aetherstream Leopard's
        .               // unblockable, …). Only pure "Pay {E}: do X" abilities with no other
        .               // cost are considered, so the bot can't bankrupt mana or sacrifice
        .               // anything. Dry-run-gated like everything else.
    2,088 ( 0.00%)      if let Some(action) = pick_energy_payoff(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Recur value from the graveyard (Embalm CR 702.88 / Eternalize CR 702.91
        .               // and any "Exile this from your graveyard: …" ability) when there's spare
        .               // mana and nothing better to do. Dry-run-gated so cost / sorcery timing
        .               // bottom out in `would_accept`.
    7,132 ( 0.00%)      if let Some(action) = pick_graveyard_recursion(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Reanimate a creature from the graveyard via a battlefield permanent's
        .               // activated ability (Seedship Broodtender's sac-to-return) when there's a
        .               // worthwhile target. Dry-run-gated so cost / sorcery-speed timing bottom
        .               // out in `would_accept`.
    2,088 ( 0.00%)      if let Some(action) = pick_battlefield_reanimate(state, seat) {
        .                   return action;
        .               }
        .           
        .               // Crack a Lander token (CR — `{2}, {T}, Sacrifice: fetch a basic land
        .               // tapped`) for ramp when there's a basic still in the library and spare
        .               // mana. Sequenced after spell-casting so the bot only ramps when it has
        .               // nothing better to spend mana on. Dry-run-gated.
   12,528 ( 0.00%)      if let Some(action) = pick_crack_lander(state, seat) {
  779,500 ( 0.03%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_crack_lander (2,088x)
        .                   return action;
        .               }
        .           
        .               // Fire a "{cost}: deal damage to any target" value ability (Frostwielder's
        .               // {T} ping, Kiku's tap-and-burn, Pain Kami-style sac burn) when it kills an
        .               // opposing creature outright. Dry-run-gated so cost / timing / target
        .               // legality bottom out in `would_accept`.
   12,528 ( 0.00%)      if let Some(action) = pick_removal_ping(state, seat) {
51,418,318 ( 1.65%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_ping (2,088x)
        .                   return action;
        .               }
        .           
        .               // Close the game: fire a "deal N to each opponent" / "drain N" / "each
        .               // opponent loses N" ability when it's lethal to a living opponent
        .               // (Hazoret's discard-burn, drain pingers). Lethal-only, so the bot never
        .               // wastes the resource. Dry-run-gated via `would_accept`.
   12,480 ( 0.00%)      if let Some(action) = pick_reach_burn(state, seat) {
1,068,728 ( 0.03%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_reach_burn (2,080x)
        .                   return action;
        .               }
        .           
        .               // Fire a "Sacrifice this: destroy target creature" ability (Pus Kami,
        .               // Nezumi Bone-Reader-style sac-removal) on a favorable trade — only when
        .               // the destroyed foe is at least as big as the creature being sacrificed.
   12,480 ( 0.00%)      if let Some(action) = pick_removal_sacrifice(state, seat) {
35,410,238 ( 1.14%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_sacrifice (2,080x)
        .                   return action;
        .               }
        .           
        .               // Fire a repeatable "{cost}: Destroy target creature" (the Torment
        .               // Possessed cycle's Threshold ability, Royal Assassin-style tappers) on
        .               // the biggest legal foe. No trade math — the source survives.
   12,480 ( 0.00%)      if let Some(action) = pick_removal_destroy(state, seat) {
24,714,445 ( 0.79%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_removal_destroy (2,080x)
        .                   return action;
        .               }
        .           
        .               // Unmask a face-down threat (Morph / Megamorph / Disguise / a cloaked or
        .               // manifested creature card) when the turn-up cost is affordable. Dry-run-
        .               // gated, so the cost / timing / "manifested noncreature can't turn up"
        .               // rules all bottom out in `would_accept`.
   12,480 ( 0.00%)      if let Some(action) = pick_turn_face_up(state, seat) {
  299,364 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_turn_face_up (2,080x)
        .                   return action;
        .               }
        .           
        .               // Pump the whole team before combat damage (Bearer of Glory's
        .               // "{4}{W}: creatures you control get +1/+1") when the bot has two or more
        .               // attacking creatures — the pump pays off on the swing. Dry-run-gated.
   12,480 ( 0.00%)      if let Some(action) = pick_team_pump(state, seat) {
   43,680 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_team_pump (2,080x)
        .                   return action;
        .               }
        .           
        .               // As a last resort before passing, sink spare mana into a "{cost}: draw a
        .               // card" ability when card-starved (Bonders' Enclave, Arch of Orazca-style
        .               // engines). Dry-run-gated, so cost / activation conditions bottom out in
        .               // `would_accept`.
   12,480 ( 0.00%)      if let Some(action) = pick_card_draw_ability(state, seat) {
  419,334 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_card_draw_ability (2,080x)
        .                   return action;
        .               }
        .           
        .               // Re-arm an unprepared prepare-spell creature via an off-card "target
        .               // creature becomes prepared" ability (SOS: Skycoach Waypoint). The
        .               // counter is worth about the inset spell — see the `permanent_value`
        .               // term — so this banks value on par with the draw sink above.
   12,480 ( 0.00%)      if let Some(action) = pick_reprepare(state, seat) {
  486,486 ( 0.02%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_reprepare (2,080x)
        .                   return action;
        .               }
        .           
        .               // Sacrifice-for-value engines (sac a Pest: payoff), judged by the
        .               // resolved outcome rather than skipped for carrying a sac cost.
   14,560 ( 0.00%)      if let Some(action) = pick_sacrifice_value(state, seat, w) {
27,937,596 ( 0.90%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_sacrifice_value (2,080x)
        .                   return action;
        .               }
        .           
        .               // Crew an uncrewed Vehicle so it can attack this turn (Vehicles are dead
        .               // cards to the bot otherwise). Dry-run-gated.
   12,480 ( 0.00%)      if let Some(action) = pick_crew_vehicle(state, seat) {
3,259,399 ( 0.10%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_crew_vehicle (2,080x)
        .                   return action;
        .               }
        .           
        .               // Sink leftover mana into a repeatable "{cost}: +1/+1 counter on this"
        .               // ability to grow the board (Fire Sages, Water Tribe Captain). Last resort,
        .               // so it never pre-empts a spell or land. Dry-run-gated.
   12,480 ( 0.00%)      if let Some(action) = pick_self_pump_counter(state, seat) {
  813,946 ( 0.03%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_self_pump_counter (2,080x)
        .                   return action;
        .               }
        .           
        .               // Sink leftover mana into a "{cost}: create a token" ability to grow the
        .               // board (Sun Warriors' {5}: 1/1 Ally, Realm of Koh's Spirit, Jasmine Dragon).
        .               // Last resort, dry-run-gated.
   12,480 ( 0.00%)      if let Some(action) = pick_token_maker(state, seat) {
  665,076 ( 0.02%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_token_maker (2,080x)
        .                   return action;
        .               }
        .           
    2,080 ( 0.00%)      GameAction::PassPriority
   39,002 ( 0.00%)  }
10,236,294 ( 0.33%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (3,382x)
   52,520 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<(crabomination::game::types::GameAction,bool)>> (2,020x)
        .           
        .           /// Activate a sacrifice-cost ability when the RESOLVED outcome beats
        .           /// doing nothing. The generic ability pickers skip sac costs outright and
        .           /// `pick_removal_sacrifice` only knows the destroy-for-trade shape — the
        .           /// value shapes (sacrifice a token: draw / drain / counters) had no
        .           /// judge at all. The clone-and-resolve eval prices both sides of the
        .           /// exchange: the permanent given up AND what its death buys, triggers
        .           /// included. Strictly-better-than-passing or nothing.
   31,200 ( 0.00%)  fn pick_sacrifice_value(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               let baseline = eval_material(state, seat, w);
    6,240 ( 0.00%)      let scan = state.grant_scan();
1,565,716 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (2,080x)
   52,006 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
  253,768 ( 0.01%)          for (idx, ab) in usable_abilities(state, card, &scan) {
3,136,874 ( 0.10%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (14,194x)
   99,358 ( 0.00%)              if !ab.sac_cost && ab.sac_other_filter.is_none() {
        .                           continue;
        .                       }
        .                       // Destroy-shaped sac removal keeps its dedicated trade math
        .                       // (`pick_removal_sacrifice`, earlier in the chain).
        .                       if matches!(&ab.effect, Effect::Destroy { .. } | Effect::DestroyNoRegen { .. }) {
        .                           continue;
        .                       }
        .                       let target = if ab.effect.requires_target() {
-- line 4998 ----------------------------------------
-- line 5019 ----------------------------------------
        .                       }
        .                       if let Some(ev) = evaluate_action_outcome(state, seat, &action, w)
        .                           && ev > baseline
        .                       {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
    4,160 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// SOS Prepare mana sink: aim an off-card "target creature becomes
        .           /// prepared" ability (Skycoach Waypoint's `{3},{T}`) at the bot's best
        .           /// unprepared prepare-spell creature — biggest inset spell first, since
        .           /// that's what the counter is worth. Dry-run-gated through `would_accept`.
   24,960 ( 0.00%)  fn pick_reprepare(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::card::CounterType;
        .               use crate::effect::Selector;
        .               fn prepares_target(e: &Effect) -> bool {
        .                   match e {
        .                       Effect::AddCounter { what, kind: CounterType::Prepared, .. } => {
        .                           matches!(what, Selector::Target(_) | Selector::TargetFiltered { .. })
        .                       }
        .                       Effect::Seq(v) => v.iter().any(prepares_target),
        .                       _ => false,
        .                   }
        .               }
    2,080 ( 0.00%)      let mut targets: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
   99,714 ( 0.00%)              c.controller == seat
   16,688 ( 0.00%)                  && c.definition.prepare_spell.is_some()
        .                           && c.counter_count(CounterType::Prepared) == 0
        .                   })
        .                   .collect();
    4,160 ( 0.00%)      if targets.is_empty() {
    4,160 ( 0.00%)          return None;
        .               }
        .               targets.sort_by_key(|c| {
        .                   std::cmp::Reverse(c.definition.prepare_spell.as_deref().map(|s| s.cost.cmc()).unwrap_or(0))
        .               });
        .               let scan = state.grant_scan();
        .               for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in usable_abilities(state, card, &scan) {
        .                       if !prepares_target(&ab.effect) {
-- line 5064 ----------------------------------------
-- line 5075 ----------------------------------------
        .                           };
        .                           if state.would_accept(action.clone()) {
        .                               return Some(action);
        .                           }
        .                       }
        .                   }
        .               }
        .               None
   16,640 ( 0.00%)  }
        .           
        .           /// Activate a non-sacrifice "{cost}: create a token" ability as a last-resort
        .           /// mana sink — grows the board when the bot has nothing better to do. Skips
        .           /// sacrifice-cost and once-per-game (Exhaust) abilities. Dry-run-gated through
        .           /// `would_accept`, so cost/timing legality bottoms out there.
   18,720 ( 0.00%)  fn pick_token_maker(state: &GameState, seat: usize) -> Option<GameAction> {
        .               fn makes_token(e: &Effect) -> bool {
   99,358 ( 0.00%)          match e {
        .                       Effect::CreateToken { .. } | Effect::CreateTokenAttacking { .. } => true,
        .                       Effect::Seq(steps) => steps.iter().any(makes_token),
        .                       _ => false,
        .                   }
        .               }
   35,318 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
   56,776 ( 0.00%)              if ab.sac_cost || ab.exhaust || !makes_token(&ab.effect) {
        .                           continue;
        .                       }
        .                       let action = GameAction::ActivateAbility {
        .                           card_id: card.id,
        .                           ability_index: idx,
        .                           target: None,
        .                           additional_targets: Vec::new(),
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
    2,080 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// Activate a repeatable "{cost}: put a +1/+1 counter on this creature" ability
        .           /// as a last-resort mana sink — grows the board when the bot has nothing better
        .           /// to do. Skips sacrifice-cost and once-per-game (Exhaust) abilities so it never
        .           /// throws away a permanent or a one-shot. Dry-run-gated through `would_accept`.
   18,720 ( 0.00%)  fn pick_self_pump_counter(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::card::CounterType;
        .               use crate::effect::Selector;
   35,318 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
   56,776 ( 0.00%)              if ab.sac_cost || ab.exhaust {
        .                           continue;
        .                       }
        .                       // Adapt abilities (CR 702.108) put +1/+1 counters on a creature with
        .                       // none — recognize the `If`-wrapped counter shape and fire only when
        .                       // the creature isn't already adapted (else it's a mana-wasting no-op).
   28,388 ( 0.00%)              let useful = if let Effect::AddCounter { what: Selector::This, kind, .. } = &ab.effect {
        .                           // Always sink into +1/+1 self-pumps; otherwise only into a counter
        .                           // that still progresses an unmet "becomes a creature at N counters"
        .                           // static (War Balloon's fire counters), so the bot animates it
        .                           // instead of stalling and doesn't dump mana past the threshold.
        .                           *kind == CounterType::PlusOnePlusOne
        .                               || card.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(&sa.effect,
        .                                       crate::effect::StaticEffect::SelfIsCreatureWhileCountersAtLeast { kind: k, n }
        .                                       if k == kind && card.counter_count(*kind) < *n)
        .                               })
   42,582 ( 0.00%)              } else if ab.effect.is_adapt() {
   99,358 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::is_adapt (14,194x)
        .                           card.counter_count(CounterType::PlusOnePlusOne) == 0
        .                       } else {
        .                           false
        .                       };
        .                       if !useful {
        .                           continue;
        .                       }
        .                       let action = GameAction::ActivateAbility {
-- line 5151 ----------------------------------------
-- line 5155 ----------------------------------------
        .                           additional_targets: Vec::new(),
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
    4,160 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// Crew an uncrewed Vehicle the bot controls, paying with the smallest
        .           /// untapped creatures whose total power covers the crew cost — but only when
        .           /// the Vehicle is at least as big as the power tapped to crew it (a net combat
        .           /// gain). Dry-run-gated, so crew legality (CR 702.122) bottoms out in
        .           /// `would_accept`.
   20,800 ( 0.00%)  fn pick_crew_vehicle(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::card::CardType;
    2,080 ( 0.00%)      let mut crewers: Vec<(CardId, i32)> = state
        .                   .battlefield
        .                   .iter()
   99,714 ( 0.00%)          .filter(|c| c.controller == seat && !c.tapped)
        .                   .filter_map(|c| {
   34,092 ( 0.00%)              let cp = state.computed_permanent(c.id)?;
1,329,324 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (11,364x)
    4,640 ( 0.00%)              cp.card_types.contains(&CardType::Creature).then_some((c.id, cp.power.max(0)))
        .                   })
        .                   .collect();
        .               crewers.sort_by_key(|&(_, p)| p);
        .           
   35,318 ( 0.00%)      for v in state.battlefield.iter().filter(|c| c.controller == seat) {
   66,752 ( 0.00%)          let Some(cost) = v.definition.crew_cost() else { continue };
  184,760 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::crew_cost (16,688x)
        .                   let Some(cp) = state.computed_permanent(v.id) else { continue };
        .                   // Already a creature (crewed/animated this turn) → nothing to do.
        .                   if cp.card_types.contains(&CardType::Creature) {
        .                       continue;
        .                   }
        .                   let mut chosen = Vec::new();
        .                   let mut total = 0i32;
        .                   for &(id, p) in crewers.iter().filter(|&&(id, _)| id != v.id) {
-- line 5193 ----------------------------------------
-- line 5202 ----------------------------------------
        .                   if chosen.is_empty() || total < cost as i32 || cp.power < total {
        .                       continue;
        .                   }
        .                   let action = GameAction::Crew { vehicle: v.id, crew_creatures: chosen };
        .                   if state.would_accept(action.clone()) {
        .                       return Some(action);
        .                   }
        .               }
    2,080 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// Fire a "deal N damage to each opponent" / "drain N" / "each opponent loses
        .           /// N" activated ability when it's lethal to a living opponent. Only fixed
        .           /// (`Value::Const`) amounts are considered, and only when some opponent's life
        .           /// is at or below the amount, so the bot spends the resource (mana / a discard
        .           /// / a tap) exclusively to win — never to chip. Dry-run-gated via
        .           /// `would_accept`.
   18,720 ( 0.00%)  fn pick_reach_burn(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::{PlayerRef, Selector, Value};
        .               // Amount an ability's effect would subtract from each opponent's life, if
        .               // it's an each-opponent reach effect with a fixed amount.
   70,970 ( 0.00%)      fn reach_amount(effect: &Effect) -> Option<i32> {
  141,460 ( 0.00%)          match effect {
        .                       Effect::DealDamage { to: Selector::Player(PlayerRef::EachOpponent), amount: Value::Const(n) }
        .                       | Effect::LoseLife { who: Selector::Player(PlayerRef::EachOpponent), amount: Value::Const(n) }
        .                       | Effect::Drain { from: Selector::Player(PlayerRef::EachOpponent), amount: Value::Const(n), .. } => {
        .                           Some(*n)
        .                       }
        .                       // Compound abilities (e.g. "do X; each opponent loses N") still
        .                       // count their each-opponent reach: sum a Seq's components, take the
        .                       // best mode of a modal. `would_accept` still gates legality, so a
-- line 5232 ----------------------------------------
-- line 5234 ----------------------------------------
        .                       // keeps the whole activation from firing.
        .                       Effect::Seq(parts) => {
        .                           let total: i32 = parts.iter().filter_map(reach_amount).sum();
        .                           (total > 0).then_some(total)
        .                       }
        .                       Effect::ChooseMode(modes) => modes.iter().filter_map(reach_amount).max(),
        .                       _ => None,
        .                   }
   99,358 ( 0.00%)      }
        .               let lethal_threshold = state
        .                   .players
        .                   .iter()
        .                   .enumerate()
   30,184 ( 0.00%)          .filter(|(p, pl)| !state.same_team(*p, seat) && pl.is_alive())
   81,120 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (4,160x)
    2,080 ( 0.00%)          .map(|(_, pl)| pl.life)
        .                   .min()?;
   35,318 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
   28,388 ( 0.00%)          for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
   56,776 ( 0.00%)              let Some(amount) = reach_amount(&ab.effect) else { continue };
  325,982 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_reach_burn::reach_amount (14,194x)
        .                       if amount < lethal_threshold {
        .                           continue;
        .                       }
        .                       let action = GameAction::ActivateAbility {
        .                           card_id: card.id,
        .                           ability_index: idx,
        .                           target: None,
        .                           additional_targets: Vec::new(),
-- line 5260 ----------------------------------------
-- line 5261 ----------------------------------------
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
        .               None
   16,640 ( 0.00%)  }
        .           
        .           /// Activate a team-wide "creatures you control get +N/+N until end of turn"
        .           /// ability while the bot has two or more attacking creatures, so the pump
        .           /// connects on the swing. Only positive, no-target, non-sacrifice pumps are
        .           /// considered; dry-run-gated so cost / timing bottom out in `would_accept`.
        .           /// True when `req` constrains its subjects to creatures the controller owns
        .           /// (a `ControlledByYou` clause anywhere in its And/Or tree).
        .           fn requirement_restricts_to_your_creatures(req: &crate::card::SelectionRequirement) -> bool {
-- line 5277 ----------------------------------------
-- line 5280 ----------------------------------------
        .                   R::ControlledByYou => true,
        .                   R::And(a, b) | R::Or(a, b) => {
        .                       requirement_restricts_to_your_creatures(a) || requirement_restricts_to_your_creatures(b)
        .                   }
        .                   _ => false,
        .               }
        .           }
        .           
   16,640 ( 0.00%)  fn pick_team_pump(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::{Selector, Value};
        .               let attackers = state
        .                   .attacking
        .                   .iter()
        .                   .filter(|a| state.battlefield_find(a.attacker).is_some_and(|c| c.controller == seat))
        .                   .count();
        .               if attackers < 2 {
    2,080 ( 0.00%)          return None;
        .               }
        .               for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       if ab.sac_cost {
        .                           continue;
        .                       }
        .                       let Effect::PumpPT { what: Selector::EachPermanent(req), power: Value::Const(p), .. } =
        .                           &ab.effect
-- line 5304 ----------------------------------------
-- line 5318 ----------------------------------------
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
        .               None
   16,640 ( 0.00%)  }
        .           
        .           /// Activate a bare "{cost}: draw a card" ability (no target, doesn't sacrifice
        .           /// the source) when the bot is card-starved (≤2 cards in hand) and can afford
        .           /// it. Fired last, as a mana sink, so it never pre-empts casting spells or
        .           /// playing lands. Dry-run-gated through `would_accept`.
   16,640 ( 0.00%)  fn pick_card_draw_ability(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::Selector;
    8,320 ( 0.00%)      if state.players[seat].hand.len() > 2 {
      952 ( 0.00%)          return None;
        .               }
   25,450 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
   20,908 ( 0.00%)              let Effect::Draw { who: Selector::You, .. } = &ab.effect else { continue };
        .                       if ab.sac_cost {
        .                           continue; // don't sacrifice the source just to draw
        .                       }
        .                       let action = GameAction::ActivateAbility {
        .                           card_id: card.id,
        .                           ability_index: idx,
        .                           target: None,
        .                           additional_targets: Vec::new(),
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
    1,128 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// Offer a `TurnFaceUp` for the first affordable face-down permanent the bot
        .           /// controls. The cost is the real card's Morph/Megamorph/Disguise cost, or its
        .           /// mana cost for a manifested/cloaked creature card; `would_accept` enforces it.
   16,640 ( 0.00%)  fn pick_turn_face_up(state: &GameState, seat: usize) -> Option<GameAction> {
    2,080 ( 0.00%)      state
        .                   .battlefield
        .                   .iter()
   99,852 ( 0.00%)          .filter(|c| c.controller == seat && c.face_down && c.face_up_def.is_some())
        .                   .map(|c| GameAction::TurnFaceUp { card_id: c.id })
        .                   .find(|a| state.would_accept(a.clone()))
   16,640 ( 0.00%)  }
        .           
        .           /// Fire a single-target "deal damage to any target" activated ability that
        .           /// kills an opposing creature outright. Handles a constant damage amount
        .           /// (Frostwielder, Pain Kami at fixed X) and the "damage equal to its own power"
        .           /// shape (Kiku, Night's Flower). Targets the highest-power killable opponent
        .           /// creature; dry-run-gated so cost / sorcery timing / target legality all
        .           /// bottom out in `would_accept`. Points the ability at an opponent's face only
        .           /// when the hit is exactly lethal (reach for the win); otherwise never chips a
-- line 5376 ----------------------------------------
-- line 5397 ----------------------------------------
        .               let printed = &card.definition.activated_abilities;
        .               let n = printed.len();
        .               printed
        .                   .iter()
        .                   .map(std::borrow::Cow::Borrowed)
        .                   .enumerate()
        .                   .chain(
        .                       state
  334,534 ( 0.01%)                  .granted_abilities_of(card, scan)
4,180,164 ( 0.13%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of (33,544x)
        .                           .into_iter()
        .                           .enumerate()
        .                           .map(|(i, ab)| (n + i, std::borrow::Cow::Owned(ab))),
        .                   )
        .                   .collect()
        .           }
        .           
        .           /// "{cost}: Destroy target creature" on a permanent that survives the
        .           /// activation — the untargeted-at-self sibling of `pick_removal_sacrifice`.
        .           /// Fires on the biggest legal opposing creature.
   24,960 ( 0.00%)  fn pick_removal_destroy(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::Selector;
    2,080 ( 0.00%)      let mut foes: Vec<(crate::card::CardId, i32)> = state
        .                   .battlefield
        .                   .iter()
  653,036 ( 0.02%)          .filter(|c| !state.same_team(c.controller, seat) && c.definition.is_creature())
1,940,838 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (99,864x)
  767,472 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (49,722x)
   46,800 ( 0.00%)          .filter_map(|c| state.computed_permanent(c.id).map(|cp| (c.id, cp.power)))
3,717,320 ( 0.12%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (9,360x)
        .                   .collect();
        .               foes.sort_by_key(|(_, pow)| std::cmp::Reverse(*pow));
    4,160 ( 0.00%)      let scan = state.grant_scan();
1,566,713 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (2,080x)
   52,006 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
  182,798 ( 0.01%)          for (idx, ab) in usable_abilities(state, card, &scan) {
3,136,874 ( 0.10%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (14,194x)
   56,776 ( 0.00%)              if ab.sac_cost {
        .                           continue; // `pick_removal_sacrifice` owns the trade math.
        .                       }
   99,358 ( 0.00%)              let (Effect::Destroy { what } | Effect::DestroyNoRegen { what }) = &ab.effect else {
        .                           continue;
        .                       };
        .                       if !matches!(what, Selector::Target(_) | Selector::TargetFiltered { .. }) {
        .                           continue;
        .                       }
        .                       for (foe, _) in &foes {
        .                           let action = GameAction::ActivateAbility {
        .                               card_id: card.id,
-- line 5439 ----------------------------------------
-- line 5449 ----------------------------------------
        .                               continue;
        .                           }
        .                           if state.would_accept(action.clone()) {
        .                               return Some(action);
        .                           }
        .                       }
        .                   }
        .               }
    2,080 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
   31,320 ( 0.00%)  fn pick_removal_ping(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::{Selector, Value};
        .               // Reach for the win first: if a constant-damage "any target" ability is
        .               // lethal to an opponent, point it at their face. Only fires when the hit
        .               // is actually lethal (life ≤ amount), so it's never a wasted chip ping.
    2,088 ( 0.00%)      let scan = state.grant_scan();
1,574,966 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (2,088x)
   52,306 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
  183,994 ( 0.01%)          for (idx, ab) in usable_abilities(state, card, &scan) {
3,158,532 ( 0.10%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (14,292x)
   58,176 ( 0.00%)              let Effect::DealDamage { to, amount: Value::Const(n) } = &ab.effect else { continue };
        .                       // Must be an untyped "any target" slot (a creature-only filter
        .                       // can't be pointed at a player).
    1,008 ( 0.00%)              if !matches!(to, Selector::Target(_)) {
        .                           continue;
        .                       }
        .                       for opp in 0..state.players.len() {
    4,032 ( 0.00%)                  if state.same_team(opp, seat) || state.players[opp].life > *n {
    9,828 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (504x)
        .                               continue;
        .                           }
      144 ( 0.00%)                  let action = GameAction::ActivateAbility {
       12 ( 0.00%)                      card_id: card.id,
        .                               ability_index: idx,
        .                               target: Some(crate::game::Target::Player(opp)),
        .                               additional_targets: Vec::new(),
        .                               x_value: None, mode: None,
        .                           };
       86 ( 0.00%)                  if state.would_accept(action.clone()) {
  308,003 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (12x)
      960 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (12x)
       26 ( 0.00%)                      return Some(action);
        .                           }
       30 ( 0.00%)              }
      230 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (10x)
        .                   }
        .               }
        .               // Opposing creatures, highest computed power first (best removal value).
    2,086 ( 0.00%)      let mut foes: Vec<(crate::card::CardId, i32)> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| !state.same_team(c.controller, seat) && c.definition.is_creature())
        .                   .filter_map(|c| state.computed_permanent(c.id).map(|cp| (c.id, cp.power)))
        .                   .collect();
        .               foes.sort_by_key(|(_, pow)| std::cmp::Reverse(*pow));
        .               // Reuses the scan built above: `state` is `&GameState` and nothing
        .               // between the two loops mutates it, so a second `grant_scan` was a
        .               // second walk of every static ability in play for the same answer.
   52,194 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
  183,590 ( 0.01%)          for (idx, ab) in usable_abilities(state, card, &scan) {
3,151,460 ( 0.10%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (14,260x)
        .                       // The effect must be a bare single-target DealDamage whose target
        .                       // can be a creature (not a self/own-board selector).
   57,040 ( 0.00%)              let Effect::DealDamage { to, amount } = &ab.effect else { continue };
    1,488 ( 0.00%)              if !matches!(to, Selector::Target(_) | Selector::TargetFiltered { .. }) {
        .                           continue;
        .                       }
        .                       for (foe, foe_pow) in &foes {
      832 ( 0.00%)                  let Some(cp) = state.computed_permanent(*foe) else { continue };
   11,992 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (104x)
        .                           // Remaining toughness after damage already marked this turn
        .                           // (CR 120.6) — a ping that wouldn't kill a fresh creature can
        .                           // still finish one that's been chipped in combat.
      208 ( 0.00%)                  let marked = state.battlefield_find(*foe).map(|c| c.damage as i32).unwrap_or(0);
      208 ( 0.00%)                  let remaining = cp.toughness - marked;
        .                           // Lethal check: a constant amount, or "equal to its own power"
        .                           // (Kiku) where the creature dies if power ≥ remaining toughness.
      520 ( 0.00%)                  let lethal = match amount {
      104 ( 0.00%)                      Value::Const(n) => *n >= remaining,
        .                               Value::PowerOf(s) if matches!(**s, Selector::Target(_)) => {
        .                                   *foe_pow >= remaining
        .                               }
        .                               // "Deals damage equal to its own power" pingers (firebreather-
        .                               // style {T} abilities) read the source's computed power.
        .                               Value::PowerOf(s) if matches!(**s, Selector::This) => state
        .                                   .computed_permanent(card.id)
        .                                   .is_some_and(|p| p.power >= remaining),
        .                               _ => false,
        .                           };
      182 ( 0.00%)                  if !lethal {
        .                               continue;
        .                           }
      312 ( 0.00%)                  let action = GameAction::ActivateAbility {
       26 ( 0.00%)                      card_id: card.id,
        .                               ability_index: idx,
        .                               target: Some(crate::game::Target::Permanent(*foe)),
        .                               additional_targets: Vec::new(),
        .                               x_value: None, mode: None,
        .                           };
      208 ( 0.00%)                  if !ward_gate_ok(state, seat, &action) {
   19,284 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::ward_gate_ok (26x)
        .                               continue;
        .                           }
      208 ( 0.00%)                  if state.would_accept(action.clone()) {
  817,664 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (26x)
    2,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (26x)
       78 ( 0.00%)                      return Some(action);
        .                           }
       40 ( 0.00%)              }
      460 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (20x)
        .                   }
        .               }
        .               // Opposing planeswalkers, highest loyalty first — a constant-damage "any
        .               // target" ability that's lethal to the loyalty removes the threat.
    2,080 ( 0.00%)      let mut walkers: Vec<(crate::card::CardId, i32)> = state
        .                   .battlefield
        .                   .iter()
  249,216 ( 0.01%)          .filter(|c| !state.same_team(c.controller, seat) && c.definition.is_planeswalker())
  646,002 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (33,238x)
  264,800 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (16,550x)
        .                   .map(|c| (c.id, c.counter_count(crate::card::CounterType::Loyalty) as i32))
        .                   .collect();
        .               walkers.sort_by_key(|(_, loy)| std::cmp::Reverse(*loy));
    2,080 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       let Effect::DealDamage { to, amount: Value::Const(n) } = &ab.effect else { continue };
        .                       // Any target slot that could point at a planeswalker (would_accept
        .                       // re-checks the filter).
        .                       if !matches!(to, Selector::Target(_) | Selector::TargetFiltered { .. }) {
        .                           continue;
        .                       }
        .                       for (walker, loy) in &walkers {
-- line 5567 ----------------------------------------
-- line 5579 ----------------------------------------
        .                               continue;
        .                           }
        .                           if state.would_accept(action.clone()) {
        .                               return Some(action);
        .                           }
        .                       }
        .                   }
        .               }
    2,080 ( 0.00%)      None
   22,366 ( 0.00%)  }
        .           
        .           /// Activate a "Sacrifice this creature: Destroy target creature" ability
        .           /// (Pus Kami, Scuttling Death-style sac removal) on a *favorable* trade: the
        .           /// destroyed opposing creature must be at least as powerful as the creature
        .           /// being sacrificed, so the bot won't pitch a 3/3 to kill a 1/1. Targets the
        .           /// biggest qualifying foe. Dry-run-gated through `would_accept`.
   24,960 ( 0.00%)  fn pick_removal_sacrifice(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::effect::Selector;
    2,080 ( 0.00%)      let mut foes: Vec<(crate::card::CardId, i32)> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| !state.same_team(c.controller, seat) && c.definition.is_creature())
        .                   .filter_map(|c| state.computed_permanent(c.id).map(|cp| (c.id, cp.power)))
        .                   .collect();
        .               foes.sort_by_key(|(_, pow)| std::cmp::Reverse(*pow));
    4,160 ( 0.00%)      let scan = state.grant_scan();
1,568,188 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (2,080x)
   52,006 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
   66,752 ( 0.00%)          let src_power = state.computed_permanent(card.id).map(|cp| cp.power).unwrap_or(0);
10,632,510 ( 0.34%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (16,688x)
  182,798 ( 0.01%)          for (idx, ab) in usable_abilities(state, card, &scan) {
3,136,874 ( 0.10%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (14,194x)
   56,776 ( 0.00%)              if !ab.sac_cost {
        .                           continue;
        .                       }
        .                       let target_is_creature = match &ab.effect {
        .                           Effect::Destroy { what } | Effect::DestroyNoRegen { what } => {
        .                               matches!(what, Selector::Target(_) | Selector::TargetFiltered { .. })
        .                           }
        .                           _ => false,
        .                       };
-- line 5616 ----------------------------------------
-- line 5633 ----------------------------------------
        .                               continue;
        .                           }
        .                           if state.would_accept(action.clone()) {
        .                               return Some(action);
        .                           }
        .                       }
        .                   }
        .               }
    2,080 ( 0.00%)      None
   16,640 ( 0.00%)  }
        .           
        .           /// Find an affordable graveyard-activated ability whose cost exiles the source
        .           /// (Embalm / Eternalize, Stone Docent-style recursion). Returns the activation
        .           /// for the first such card the bot can pay for.
        .           fn pick_graveyard_recursion(state: &GameState, seat: usize) -> Option<GameAction> {
        .               // The bot's own creatures, highest-power first — candidate targets for
        .               // abilities that need one (Scavenge's +1/+1 counters, Daring Fiendbonder's
        .               // indestructible counter). For no-target recursion (Embalm / Eternalize /
        .               // Stone Docent) we pass `None`.
    2,088 ( 0.00%)      let mut own: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
  152,256 ( 0.00%)          .filter(|c| c.controller == seat && c.definition.is_creature())
  259,780 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (16,798x)
        .                   .collect();
    8,432 ( 0.00%)      own.sort_by_key(|c| std::cmp::Reverse(c.power()));
  242,780 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (3,980x)
    4,176 ( 0.00%)      for card in state.players[seat].graveyard.iter() {
        .                   // Printed graveyard abilities plus static-granted ones (Varolz's
        .                   // scavenge) at indices ≥ the printed count.
   20,816 ( 0.00%)          let printed = card.definition.activated_abilities.clone();
2,418,103 ( 0.08%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (10,408x)
   52,040 ( 0.00%)          let granted = state.graveyard_granted_abilities(seat, card);
1,802,530 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::graveyard_granted_abilities (10,408x)
        .                   for (idx, ab) in printed.iter().chain(granted.iter()).enumerate() {
        .                       // Graveyard-activated abilities worth firing: an exile-self payoff
        .                       // (Embalm-style value) or a self-return that replays the creature
        .                       // (Llanowar Greenwidow's "{7}{G}: return this from your graveyard").
    3,064 ( 0.00%)              if !(ab.from_graveyard
        .                           && (ab.exile_self_cost || effect_returns_self_to_battlefield(&ab.effect)))
        .                       {
        .                           continue;
        .                       }
        .                       // Only try a no-target activation when the effect needs none —
        .                       // otherwise `would_accept` (which doesn't re-derive targets) would
        .                       // wave through a wasted target-less activation.
        .                       let candidates: Vec<Option<crate::game::Target>> = if ab.effect.requires_target() {
-- line 5675 ----------------------------------------
-- line 5719 ----------------------------------------
        .                   _ => false,
        .               }
        .           }
        .           
        .           /// True if `eff` moves a graveyard-targeted card onto the battlefield (a
        .           /// reanimation effect — Seedship Broodtender's sac-to-return). Recurses `Seq`.
        .           fn effect_reanimates_from_graveyard(eff: &Effect) -> bool {
        .               use crate::effect::ZoneDest;
   71,500 ( 0.00%)      match eff {
        .                   Effect::Move {
        .                       what: crate::card::Selector::TargetFiltered { filter, .. },
        .                       to: ZoneDest::Battlefield { .. },
        .                   } => filter_targets_graveyard(filter),
        .                   Effect::Seq(v) => v.iter().any(effect_reanimates_from_graveyard),
        .                   _ => false,
        .               }
        .           }
        .           
        .           /// Activate a battlefield permanent's ability that reanimates a card from the
        .           /// graveyard (Seedship Broodtender's "{cost}, Sacrifice this: return target
        .           /// creature/Spacecraft from your graveyard to the battlefield"), aimed at the
        .           /// engine's auto-picked best graveyard target. Skips when nothing legal exists.
        .           /// Dry-run-gated so cost / sorcery-speed timing bottom out in `would_accept`.
        .           fn pick_battlefield_reanimate(state: &GameState, seat: usize) -> Option<GameAction> {
   35,536 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       if !effect_reanimates_from_graveyard(&ab.effect) {
        .                           continue;
        .                       }
        .                       let target = state.auto_target_for_effect(&ab.effect, seat);
        .                       if target.is_none() {
        .                           continue; // no graveyard creature worth returning
        .                       }
-- line 5751 ----------------------------------------
-- line 5764 ----------------------------------------
        .               None
        .           }
        .           
        .           /// Crack a Lander token for ramp: a `{2}, {T}, Sacrifice: search a basic land
        .           /// onto the battlefield tapped` ability. Only fires when the controller still
        .           /// has a basic land in their library (so the fetch isn't wasted) and the
        .           /// engine accepts the activation (mana/timing). Targets nothing — the fetch
        .           /// resolves via the library-search decider.
   18,792 ( 0.00%)  fn pick_crack_lander(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::card::{ArtifactSubtype, SelectionRequirement};
    4,176 ( 0.00%)      let has_basic = state.players[seat]
        .                   .library
        .                   .iter()
   36,540 ( 0.00%)          .any(|c| state.evaluate_requirement_on_card(&SelectionRequirement::IsBasicLand, c, seat));
  253,512 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_on_card (5,220x)
        .               if !has_basic {
       40 ( 0.00%)          return None;
        .               }
   67,396 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat && !c.tapped) {
        .                   let is_lander = card.definition.subtypes.artifact_subtypes.contains(&ArtifactSubtype::Lander);
        .                   if !is_lander {
        .                       continue;
        .                   }
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       if !ab.sac_cost || !matches!(ab.effect, Effect::Search { .. }) {
        .                           continue;
        .                       }
-- line 5789 ----------------------------------------
-- line 5794 ----------------------------------------
        .                           additional_targets: Vec::new(),
        .                           x_value: None, mode: None,
        .                       };
        .                       if state.would_accept(action.clone()) {
        .                           return Some(action);
        .                       }
        .                   }
        .               }
    2,068 ( 0.00%)      None
   16,704 ( 0.00%)  }
        .           
        .           /// Find a beneficial energy-only activated ability the bot can pay for: an
        .           /// `Effect::PayEnergy { amount, .. }` ability with no mana/tap/sac cost,
        .           /// where the bot controls the source and has at least `amount` energy.
        .           fn pick_energy_payoff(state: &GameState, seat: usize) -> Option<GameAction> {
    6,264 ( 0.00%)      if state.players[seat].energy == 0 {
        .                   return None;
        .               }
        .               for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       // The energy can be modeled either as a real activation cost
        .                       // (`ActivatedAbility.energy_cost`, the up-front-gated form) or as a
        .                       // resolve-time `Effect::PayEnergy` rider. Match either so the bot
        .                       // fires Longtusk Cub-style `{E}{E}{E}: +1/+1` payoffs regardless of
-- line 5817 ----------------------------------------
-- line 5852 ----------------------------------------
        .           /// `would_accept` is bypassed here (we gate inline) so the bot doesn't
        .           /// thrash re-equipping the same creature.
        .           /// Crew an uncrewed Vehicle (CR 702.122) the bot controls, tapping the
        .           /// smallest untapped creatures that together meet the crew cost. Skipped
        .           /// unless the Vehicle's power is worth more than the creatures spent on it
        .           /// (so the bot never taps a bigger attacker to animate a smaller Vehicle).
        .           fn pick_crew(state: &GameState, seat: usize) -> Option<GameAction> {
        .               for vehicle in &state.battlefield {
  100,344 ( 0.00%)          if vehicle.controller != seat {
        .                       continue;
        .                   }
   67,192 ( 0.00%)          let Some(crew_n) = vehicle.definition.crew_cost() else { continue };
  185,932 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::crew_cost (16,798x)
        .                   // Already a creature this turn (crewed / animated)? Don't re-crew.
        .                   if state
        .                       .computed_permanent(vehicle.id)
        .                       .is_some_and(|cp| cp.card_types.contains(&crate::card::CardType::Creature))
        .                   {
        .                       continue;
        .                   }
        .                   // Candidate crew members: the bot's untapped creatures, smallest first.
-- line 5871 ----------------------------------------
-- line 5910 ----------------------------------------
        .           /// CR 702.171 — saddle a Mount the bot is about to attack with by tapping its
        .           /// least-valuable other untapped creatures (smallest power first). Only saddles
        .           /// a Mount that can attack this turn and isn't already saddled, and never spends
        .           /// more board power than the Mount itself is worth.
        .           fn pick_saddle(state: &GameState, seat: usize) -> Option<GameAction> {
        .               // Saddled is "until end of turn" (CR 702.171e), so only pay the tap cost
        .               // when a combat phase still follows — i.e. precombat main. Saddling in
        .               // postcombat main just wastes the saddlers before the buff can matter.
    2,088 ( 0.00%)      if state.step != TurnStep::PreCombatMain {
        .                   return None;
        .               }
        .               for mount in &state.battlefield {
   33,710 ( 0.00%)          if mount.controller != seat || mount.saddled || mount.tapped {
        .                       continue;
        .                   }
   11,680 ( 0.00%)          let Some(saddle_n) = mount.definition.saddle_cost() else { continue };
   32,920 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::saddle_cost (2,920x)
        .                   if !mount.can_attack() {
        .                       continue;
        .                   }
        .                   // Candidate saddlers: the bot's other untapped creatures. Tap the ones
        .                   // that can't attack this turn (summoning-sick / Defender) *first* — they
        .                   // are "free" since they'd sit idle anyway — then fall back to would-be
        .                   // attackers, smallest power first (the crew-power rider counts, CR
        .                   // 702.171). Track how much *attacker* power we spend so the overspend
-- line 5933 ----------------------------------------
-- line 5978 ----------------------------------------
        .           
        .           fn pick_equip(state: &GameState, seat: usize) -> Option<GameAction> {
        .               // Best creature to wear an Equipment: highest current power, but skip
        .               // attack-locked bodies (Defender / CantAttack) — an Equipment's combat
        .               // bonus is wasted on them. Fall back to any creature only if every
        .               // candidate is attack-locked (a board of Walls still wants the
        .               // deathtouch/keyword grant for blocking).
        .               use crate::card::Keyword;
    2,088 ( 0.00%)      let can_attack = |c: &crate::card::CardInstance| {
        .                   state
    8,988 ( 0.00%)              .computed_permanent(c.id)
4,103,986 ( 0.13%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (1,660x)
        .                       .map(|cp| {
        .                           (!cp.keywords.contains(&Keyword::Defender)
    1,290 ( 0.00%)                      || state.ignores_defender_for_attack(c))
   49,246 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ignores_defender_for_attack (216x)
        .                               && !cp.keywords.contains(&Keyword::CantAttack)
        .                       })
        .                       .unwrap_or(true)
        .               };
        .               let mine = || {
        .                   state
        .                       .battlefield
        .                       .iter()
  145,254 ( 0.00%)              .filter(|c| c.controller == seat && c.definition.is_creature())
  244,526 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (15,614x)
        .               };
        .               // Rank by *computed* power so anthems / lords / conditional pumps count
        .               // (a small body under a big anthem is a better Voltron target than a
        .               // vanilla bigger base body).
    2,088 ( 0.00%)      let cpow = |c: &crate::card::CardInstance| {
    8,358 ( 0.00%)          state.computed_permanent(c.id).map(|cp| cp.power).unwrap_or_else(|| c.power())
  110,764 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (1,478x)
        .               };
        .               let target = mine()
    2,996 ( 0.00%)          .filter(|c| can_attack(c))
    2,664 ( 0.00%)          .max_by_key(|c| cpow(c))
      122 ( 0.00%)          .or_else(|| mine().max_by_key(|c| cpow(c)))
        .                   .map(|c| c.id)?;
        .               for eq in &state.battlefield {
  134,860 ( 0.00%)          if eq.controller != seat || !eq.definition.is_equipment() {
  122,940 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_equipment (13,660x)
        .                       continue;
        .                   }
        .                   if eq.definition.has_equip().is_none() {
        .                       continue;
        .                   }
        .                   // Skip if already on the chosen target (no point re-equipping).
        .                   if eq.attached_to == Some(target) {
        .                       continue;
-- line 6022 ----------------------------------------
-- line 6031 ----------------------------------------
        .           
        .           /// Drive a "{cost}: attach target Equipment you control to target creature you
        .           /// control" activated ability (Brass Squire). Picks an Equipment not already on
        .           /// the chosen wearer for slot 0 and the highest-power creature for slot 1. The
        .           /// dry-run gate enforces the activation cost / target legality.
        .           fn pick_attach_ability(state: &GameState, seat: usize) -> Option<GameAction> {
        .               use crate::card::Selector;
        .               use crate::effect::Effect;
    2,088 ( 0.00%)      let wearer = state
        .                   .battlefield
        .                   .iter()
   85,782 ( 0.00%)          .filter(|c| c.controller == seat && c.definition.is_creature())
  259,780 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (16,798x)
    5,992 ( 0.00%)          .max_by_key(|c| c.power())
  182,756 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (2,996x)
    2,956 ( 0.00%)          .map(|c| c.id)?;
   28,218 ( 0.00%)      for card in state.battlefield.iter().filter(|c| c.controller == seat) {
        .                   for (idx, ab) in card.definition.activated_abilities.iter().enumerate() {
        .                       // Two distinct target slots: `what` (slot 0) and `to` (slot 1).
   22,324 ( 0.00%)              let Effect::Attach {
        .                           what: Selector::TargetFiltered { slot: 0, .. },
        .                           to: Selector::TargetFiltered { slot: 1, .. },
        .                       } = &ab.effect
        .                       else {
        .                           continue;
        .                       };
        .                       let Some(equip) = state.battlefield.iter().find(|e| {
        .                           e.controller == seat
-- line 6056 ----------------------------------------
-- line 6076 ----------------------------------------
        .           
        .           /// Walk every planeswalker the bot controls and pick the first activatable
        .           /// loyalty ability. Auto-target via `auto_target_for_effect` for abilities
        .           /// that require a target. Prefers a +loyalty ability when available
        .           /// (preserves the walker for next turn), falling back to the ability with
        .           /// the smallest absolute loyalty cost so we don't suicide-ult immediately.
        .           fn pick_loyalty_ability(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               for card in &state.battlefield {
   66,896 ( 0.00%)          if card.controller != seat {
        .                       continue;
        .                   }
   50,394 ( 0.00%)          if !card.definition.is_planeswalker() {
  268,768 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (16,798x)
        .                       continue;
        .                   }
        .                   let allowed = if card.definition.loyalty_twice_each_turn { 2 } else { 1 };
        .                   if card.loyalty_uses_this_turn >= allowed {
        .                       continue;
        .                   }
        .                   // Gather every affordable ability and pick by OUTCOME, not by
        .                   // loyalty-cost order. The old plus-first walk meant a walker with
-- line 6095 ----------------------------------------
-- line 6182 ----------------------------------------
        .           /// what. Extracted from `next_action` so the combat-aware evaluation can
        .           /// replay the same choice inside a simulation (see
        .           /// [`simulate_through_combat`]) rather than re-deriving it.
        .           pub fn pick_attacks(state: &GameState, seat: usize) -> Vec<Attack> {
        .               // Layer-aware per-creature checks (Defender/Flying grants, Propaganda,
        .               // computed P/T) run once per candidate attacker — share one gather, the
        .               // same way `pick_blocks` does. Matters most inside the attack/block sims,
        .               // which call this on a freshly cloned (and therefore unfrozen) state.
   13,794 ( 0.00%)      state.with_frozen_layers(|state| pick_attacks_inner(state, seat))
6,319,743 ( 0.20%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (630x)
        .           }
        .           
   28,754 ( 0.00%)  fn pick_attacks_inner(state: &GameState, seat: usize) -> Vec<Attack> {
        .               use crate::card::Keyword;
        .               // Pick the attack target: prefer an opposing monarch (CR
        .               // 724 — stealing the crown denies their end-step card and
        .               // hands it to us); otherwise the next alive opponent.
    5,228 ( 0.00%)      let target_player = match state.monarch {
        .                   Some(m)
        .                       if m != seat
        .                           && state.players.get(m).map(|p| p.is_alive()).unwrap_or(false) =>
        .                   {
        .                       m
        .                   }
        .                   _ => state.next_alive_seat(seat),
        .               };
-- line 6206 ----------------------------------------
-- line 6217 ----------------------------------------
        .               // When NOT lethal:
        .               //   * skip attackers whose toughness is <= the maximum
        .               //     opponent blocker power AND there's at least one
        .               //     opponent blocker with deathtouch + reach/flying
        .               //     parity (i.e. a blocker can be assigned).
        .               // This keeps small attackers from auto-dying to
        .               // Witherbloom Crawler / Sapworm / Toxicultivator and
        .               // similar deathtouch defenders.
    2,614 ( 0.00%)      let opp_seat = target_player;
    5,228 ( 0.00%)      let opp_life = state.players[opp_seat].life;
    2,614 ( 0.00%)      let raw_attackers: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
  190,720 ( 0.01%)              c.controller == seat
        .                           // `can_attack()`'s components minus its printed-
        .                           // Defender gate, which is re-checked below against
        .                           // the computed keyword set so a team "attack as
        .                           // though no defender" grant (High Alert) applies.
   71,430 ( 0.00%)                  && c.definition.is_creature()
  366,626 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (23,810x)
    9,556 ( 0.00%)                  && !c.tapped
   13,534 ( 0.00%)                  && (!c.summoning_sick || c.has_keyword(&Keyword::Haste))
   59,074 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (802x)
   20,420 ( 0.00%)                  && !c.has_keyword(&Keyword::CantAttack)
  311,300 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,084x)
        .                           // Honor layer-granted Defender / can't-attack
        .                           // (Pacifism, crewed-Vehicle states) — can_attack
        .                           // only sees printed keywords.
    4,084 ( 0.00%)                  && state
   12,252 ( 0.00%)                      .computed_permanent(c.id)
9,359,431 ( 0.30%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,084x)
    4,084 ( 0.00%)                      .map(|cp| {
        .                                   (!cp.keywords.contains(&Keyword::Defender)
    1,432 ( 0.00%)                              || state.ignores_defender_for_attack(c))
   94,032 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::ignores_defender_for_attack (358x)
        .                                       && !cp.keywords.contains(&Keyword::CantAttack)
        .                                       // CR 508.1a — "can attack only if
        .                                       // defending player controls [X]"
        .                                       // (Dandân). Don't declare it into a
        .                                       // defender whose board fails the
        .                                       // filter, or the whole batch is
        .                                       // rejected.
   15,640 ( 0.00%)                              && cp.keywords.iter().all(|kw| match kw {
        .                                           Keyword::CanAttackOnlyIfDefenderControls(req) => {
        .                                               state.battlefield.iter().any(|d| {
        .                                                   d.controller == target_player
        .                                                       && state.evaluate_requirement_on_card(
        .                                                           req, d, target_player,
        .                                                       )
        .                                               })
        .                                           }
-- line 6263 ----------------------------------------
-- line 6305 ----------------------------------------
        .                                       })
        .                               })
        .                               .unwrap_or(true)
        .                   })
        .                   .collect();
        .               // Use the damage-aware value so toughness-attackers (Doran,
        .               // High Alert) are weighed by what they actually deal.
        .               let total_raw_power: i32 =
   10,638 ( 0.00%)          raw_attackers.iter().map(|c| attacker_damage_value(state, c.id)).sum();
  615,290 ( 0.02%)  => crabomination/src/server/bot.rs:crabomination::server::bot::attacker_damage_value (3,546x)
    2,614 ( 0.00%)      let lethal_swing = total_raw_power >= opp_life;
        .               // Race math: compare full-out clocks. We strike first
        .               // (it's our combat), so strictly fewer turns-to-lethal
        .               // than the opponent's counter-clock — inside a short
        .               // horizon — means holding back only concedes the race;
        .               // attack like it's lethal-in-N. Defenders and can't-
        .               // attack bodies add nothing to their clock.
    2,614 ( 0.00%)      let opp_clock: i32 = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
   95,360 ( 0.00%)              c.controller == opp_seat
   71,610 ( 0.00%)                  && c.definition.is_creature()
  366,848 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (23,870x)
   25,120 ( 0.00%)                  && !c.has_keyword(&Keyword::Defender)
  383,264 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (5,024x)
   14,040 ( 0.00%)                  && !c.has_keyword(&Keyword::CantAttack)
  360,560 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,680x)
        .                   })
   14,040 ( 0.00%)          .map(|c| c.power().max(0))
  285,480 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (4,680x)
        .                   .sum();
   14,428 ( 0.00%)      let racing = total_raw_power > 0 && opp_clock > 0 && {
    9,010 ( 0.00%)          let our_turns = (opp_life.max(1) + total_raw_power - 1) / total_raw_power;
        .                   let their_turns =
   12,614 ( 0.00%)              (state.effective_life(seat).max(1) + opp_clock - 1) / opp_clock;
   80,246 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (1,802x)
    5,782 ( 0.00%)          our_turns < their_turns && our_turns <= 5
        .               };
   10,456 ( 0.00%)      let lethal_swing = lethal_swing || racing;
    2,614 ( 0.00%)      let opp_blockers: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
        .                       // A creature that's tapped, not a creature, or has a
        .                       // computed `CantBlock` (Sandstorm Verge, pacifism-
        .                       // style effects) can't block — don't let the bot hold
        .                       // attackers back for a blocker that can't legally block.
  143,040 ( 0.00%)              c.controller == opp_seat
   95,480 ( 0.00%)                  && c.can_block()
  353,026 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::can_block (23,870x)
        .                           && !state
   11,040 ( 0.00%)                      .computed_permanent(c.id)
4,673,719 ( 0.15%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (3,680x)
        .                               .is_some_and(|cp| cp.keywords.contains(&Keyword::CantBlock))
        .                   })
        .                   .collect();
    2,614 ( 0.00%)      let has_ground_deathtouch = opp_blockers
        .                   .iter()
   18,760 ( 0.00%)          .any(|b| b.has_keyword(&Keyword::Deathtouch) && !b.has_keyword(&Keyword::Flying));
  286,710 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,008x)
        .               let max_ground_blocker_power: i32 = opp_blockers
        .                   .iter()
   11,446 ( 0.00%)          .filter(|b| !b.has_keyword(&Keyword::Flying))
  218,798 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (3,450x)
    2,852 ( 0.00%)          .map(|b| b.power())
  123,586 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (2,026x)
        .                   .max()
        .                   .unwrap_or(0);
    2,614 ( 0.00%)      let mut attackers: Vec<crate::card::CardId> = raw_attackers
        .                   .into_iter()
        .                   .filter(|c| {
        .                       // CR 508.1d — must-attack creatures (Juggernaut,
        .                       // goaded) have no choice; always include them so
        .                       // the engine's requirement check accepts the batch.
   24,822 ( 0.00%)              if c.has_keyword(&Keyword::MustAttack) || !c.goaded_by.is_empty() {
  269,750 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (3,546x)
        .                           return true;
        .                       }
        .                       // Always attack on lethal swings — the bot
        .                       // would rather suicide than miss a kill.
   10,638 ( 0.00%)              if lethal_swing {
        .                           return true;
        .                       }
        .                       // CR 615.1 — don't swing with a creature whose
        .                       // combat damage is prevented this turn (Fog /
        .                       // Inspire Awe's exception); attacking only risks it
        .                       // for no damage.
    1,990 ( 0.00%)              if state.combat_damage_prevented_for_dealer(c.id) {
        .                           return false;
        .                       }
        .                       // Unblockable by the current board: if the
        .                       // opponent has creatures but none can legally
        .                       // block this attacker (Unblockable, "can't be
        .                       // blocked by/except by" restrictions the board
        .                       // can't satisfy), it's a free swing. Generalizes
        .                       // the Flying/Menace evasion checks below.
    7,960 ( 0.00%)              if !opp_blockers.is_empty()
        .                           && opp_blockers
        .                               .iter()
    6,344 ( 0.00%)                      .all(|b| !state.blocker_can_block_attacker(b.id, c.id))
2,209,308 ( 0.07%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::blocker_can_block_attacker (1,586x)
        .                       {
        .                           return true;
        .                       }
    7,712 ( 0.00%)              let flying = c.has_keyword(&Keyword::Flying);
  123,820 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,928x)
        .                       // Evasive attackers (flying) — only block-
        .                       // worried if there's a flying opp blocker.
        .                       // Skip the deathtouch / ground-power filter
        .                       // for them; assume they're safe.
    3,856 ( 0.00%)              if flying {
        .                           let opp_has_flying_blocker = opp_blockers.iter()
    3,920 ( 0.00%)                      .any(|b| b.has_keyword(&Keyword::Flying)
   67,676 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (740x)
      198 ( 0.00%)                            || b.has_keyword(&Keyword::Reach));
    4,110 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (66x)
    1,480 ( 0.00%)                  if !opp_has_flying_blocker {
        .                               return true; // free swing
        .                           }
        .                       }
        .                       // Trample: tougher creatures still come in
        .                       // (we'll get some damage through chumps).
    8,790 ( 0.00%)              if c.has_keyword(&Keyword::Trample) {
  127,970 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,758x)
        .                           return true;
        .                       }
        .                       // Indestructible: safe to swing (won't die).
    8,790 ( 0.00%)              if c.has_keyword(&Keyword::Indestructible) {
  127,970 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,758x)
        .                           return true;
        .                       }
        .                       // Shield counter on the attacker — the first
        .                       // damage is prevented, so a basic ground-trade
        .                       // is safe (push XXVI bot improvement).
    8,790 ( 0.00%)              if c.counter_count(crate::card::CounterType::Shield) > 0 {
   17,580 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (1,758x)
        .                           return true;
        .                       }
        .                       // Lifelink: even if we trade, we gain life —
        .                       // worth swinging when we can race.
    8,790 ( 0.00%)              if c.has_keyword(&Keyword::Lifelink) {
  126,404 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,758x)
        .                           return true;
        .                       }
        .                       // Deathtouch attacker: any blocker that deals
        .                       // with it dies (CR 702.2), so blocking is at
        .                       // best an even trade for the opponent — swinging
        .                       // is always at least fine.
    8,256 ( 0.00%)              if c.has_keyword(&Keyword::Deathtouch) && c.power() >= 1 {
  107,204 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,584x)
    5,124 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (84x)
        .                           return true;
        .                       }
        .                       // Menace (CR 702.111): needs two+ blockers. If
        .                       // the opponent has fewer than two creatures that
        .                       // can legally block this attacker, it gets
        .                       // through unblocked — safe to swing.
    7,500 ( 0.00%)              if c.has_keyword(&Keyword::Menace) {
   99,980 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,500x)
        .                           let able = opp_blockers
        .                               .iter()
        .                               .filter(|b| {
        .                                   !flying
        .                                       || b.has_keyword(&Keyword::Flying)
        .                                       || b.has_keyword(&Keyword::Reach)
        .                               })
        .                               .count();
        .                           if able < 2 {
        .                               return true;
        .                           }
        .                       }
        .                       // First strike + bigger power than blockers'
        .                       // toughness — we kill the blocker before it
        .                       // strikes back. Safe attack (push XXVI).
    7,500 ( 0.00%)              if c.has_keyword(&Keyword::FirstStrike)
   99,980 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,500x)
    7,500 ( 0.00%)                  || c.has_keyword(&Keyword::DoubleStrike)
   99,980 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (1,500x)
        .                       {
        .                           let max_blocker_toughness: i32 = opp_blockers
        .                               .iter()
        .                               .filter(|b| !b.has_keyword(&Keyword::Flying) || flying)
        .                               .map(|b| b.toughness())
        .                               .max()
        .                               .unwrap_or(0);
        .                           if c.power() > max_blocker_toughness {
        .                               return true;
        .                           }
        .                       }
        .                       // Hold back if a deathtouch blocker exists
        .                       // and we don't outsize the biggest blocker.
    4,876 ( 0.00%)              if has_ground_deathtouch && !flying {
        .                           return false;
        .                       }
        .                       // Finality counter on the attacker — if it
        .                       // dies it'll exile instead of returning to
        .                       // the graveyard (CR 122.1h). Don't suicide
        .                       // a finality-counter creature into ground
        .                       // blockers that can kill it.
        .                       // Push (claude/modern_decks, batches 192-197).
   11,898 ( 0.00%)              if c.counter_count(crate::card::CounterType::Finality) > 0
   13,220 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (1,322x)
        .                           && !flying
      178 ( 0.00%)                  && max_ground_blocker_power >= c.toughness()
        .                       {
        .                           return false;
        .                       }
        .                       // Hold back if our toughness is <= biggest
        .                       // blocker power and we wouldn't kill them
        .                       // (basic suicide filter).
    2,644 ( 0.00%)              if !flying
    7,408 ( 0.00%)                  && max_ground_blocker_power >= c.toughness()
   62,968 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (926x)
    2,360 ( 0.00%)                  && c.power() <= max_ground_blocker_power
   28,792 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (472x)
        .                       {
        .                           return false;
        .                       }
        .                       true
        .                   })
    2,896 ( 0.00%)          .map(|c| c.id)
        .                   .collect();
        .               // CR 506.2 — Silent Arbiter caps the whole combat. An
        .               // over-sized batch is rejected outright, so trim to the
        .               // cap keeping the biggest attackers.
   13,070 ( 0.00%)      if let Some(cap) = state.combat_participation_cap(false)
  436,334 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap (2,614x)
        .                   && attackers.len() > cap as usize
        .               {
        .                   attackers.sort_by_key(|id| {
        .                       -state.computed_permanent(*id).map(|cp| cp.power).unwrap_or(0)
        .                   });
        .                   attackers.truncate(cap as usize);
        .               }
        .               // CR 508.0 — drop a lone attacker that can't attack alone
        .               // (Militia Rallier): a single-attacker batch with
        .               // CantAttackAlone would be rejected, costing the bot its
        .               // whole combat. Only matters when it's the sole attacker.
    5,228 ( 0.00%)      if attackers.len() == 1
      958 ( 0.00%)          && state
    2,874 ( 0.00%)              .computed_permanent(attackers[0])
   61,064 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (958x)
        .                       .is_some_and(|cp| cp.keywords.contains(&Keyword::CantAttackAlone))
        .               {
        .                   attackers.clear();
        .               }
        .               // Find opponent planeswalkers in loyalty-ascending
        .               // order. The bot will redirect attacks at PWs whose
        .               // current loyalty is at-or-below our total attacking
        .               // power — finishing off the walker. Each PW consumes
        .               // up to its loyalty worth of attackers; the rest
        .               // attack the player.
    2,614 ( 0.00%)      let mut walker_targets: Vec<(crate::card::CardId, u32)> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
  143,040 ( 0.00%)              c.definition.is_planeswalker()
  762,880 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (47,680x)
        .                           && c.controller != seat
        .                           && state.players[c.controller].is_alive()
        .                           // CR 506.2 — The Aetherspark while attached.
        .                           && !state.permanent_cant_be_attacked(c.id)
        .                   })
        .                   .map(|c| {
        .                       let loyalty = c
        .                           .counters
-- line 6540 ----------------------------------------
-- line 6546 ----------------------------------------
        .                           .unwrap_or(0);
        .                       (c.id, loyalty)
        .                   })
        .                   .collect();
        .               walker_targets.sort_by_key(|(_, l)| *l);
        .               let total_power: i32 = attackers
        .                   .iter()
        .                   .filter_map(|id| {
    5,792 ( 0.00%)              state.battlefield.iter().find(|c| c.id == *id).map(|c| c.power())
  176,656 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (2,896x)
        .                   })
        .                   .sum();
        .               let mut attacks: Vec<Attack> = Vec::new();
    2,614 ( 0.00%)      for (pw_id, loyalty) in walker_targets {
        .                   // Only redirect when we can plausibly finish it
        .                   // off (total attacking power >= loyalty). Avoids
        .                   // throwing 1-power chumps at a 5-loyalty walker.
        .                   if (total_power as u32) < loyalty || loyalty == 0 {
        .                       continue;
        .                   }
        .                   // Pull as many attackers as the walker's loyalty
        .                   // for this redirect, picking smallest-power
-- line 6566 ----------------------------------------
-- line 6592 ----------------------------------------
        .                           budget -= pow;
        .                       } else {
        .                           remaining.push(id);
        .                       }
        .                   }
        .                   attackers = remaining;
        .               }
        .               // Remaining attackers go at the player.
   13,070 ( 0.00%)      for id in attackers {
        .                   attacks.push(Attack {
        .                       attacker: id,
    2,896 ( 0.00%)              target: AttackTarget::Player(target_player),
        .                   });
        .               }
   13,070 ( 0.00%)      attacks
   23,526 ( 0.00%)  }
        .           
        .           /// The attack declaration, chosen by search rather than by rule.
        .           ///
        .           /// [`pick_attacks`] is a greedy accretion: a pile of individually sensible
        .           /// exclusions (don't suicide into deathtouch, respect Propaganda, honor
        .           /// layer-granted Defender) applied to "swing with everything". Each of
        .           /// those rules is right about the case it names. What none of them can see
        .           /// is the *cost of tapping the team* — that a creature which attacks is
-- line 6615 ----------------------------------------
-- line 6647 ----------------------------------------
        .           /// Ties go to the greedy set, so the search only ever departs from the
        .           /// current behavior for a strict improvement.
        .           /// The attack declarations worth scoring: greedy first (index 0 wins
        .           /// every tie), all-home, then greedy-minus-one holdbacks ordered by
        .           /// toughness ascending — the cheapest body to keep home is also the one
        .           /// most likely to die attacking, so the front of the order is where
        .           /// both halves of the trade are largest. Shared by the sim search below
        .           /// and the Monte Carlo bot, so the two search identical menus.
    8,190 ( 0.00%)  pub(crate) fn attack_candidates_for_mcts(
        .               state: &GameState,
        .               seat: usize,
        .               w: &EvalWeights,
        .           ) -> Vec<Vec<Attack>> {
        .               let greedy = pick_attacks(state, seat);
    3,150 ( 0.00%)      if w.attack_search == 0 || greedy.is_empty() {
    1,136 ( 0.00%)          return vec![greedy];
        .               }
    2,422 ( 0.00%)      let mut candidates: Vec<Vec<Attack>> = vec![greedy.clone(), Vec::new()];
      692 ( 0.00%)      if greedy.len() > 1 {
        .                   let mut order: Vec<usize> = (0..greedy.len()).collect();
        .                   order.sort_by_key(|&i| {
    2,062 ( 0.00%)              state.battlefield_find(greedy[i].attacker).map(|c| c.toughness()).unwrap_or(0)
   53,584 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (788x)
        .                   });
        .                   for &i in order.iter().take(w.attack_search as usize) {
        .                       let mut alt = greedy.clone();
        .                       alt.remove(i);
        .                       candidates.push(alt);
        .                   }
        .               }
    2,422 ( 0.00%)      candidates
    5,670 ( 0.00%)  }
        .           
    7,560 ( 0.00%)  fn pick_attacks_scored(state: &GameState, seat: usize, w: &EvalWeights) -> Vec<Attack> {
      630 ( 0.00%)      let mut candidates = attack_candidates_for_mcts(state, seat, w);
6,864,504 ( 0.22%)  => crabomination/src/server/bot.rs:crabomination::server::bot::attack_candidates_for_mcts (630x)
    1,260 ( 0.00%)      if candidates.len() == 1 {
        .                   return candidates.swap_remove(0);
        .               }
        .           
        .               // First-wins-ties in `choose_scored`: index 0 is greedy, so equal
        .               // scores keep it (unless this thread is sampling — actors only).
        .               let mut scored: Vec<(usize, i32)> = Vec::new();
        .               for (i, cand) in candidates.iter().enumerate() {
    2,332 ( 0.00%)          let Some(score) = simulate_attack_outcome(state, seat, cand, w) else { continue };
        .                   scored.push((i, score));
        .               }
    2,422 ( 0.00%)      match choose_scored(state.turn_number, &scored) {
   19,330 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::choose_scored (346x)
        .                   Some(i) => candidates.swap_remove(i),
        .                   None => candidates.swap_remove(0),
        .               }
        .           }
        .           
        .           /// Declare `attacks`, play the turn out through the opponent's counter-
        .           /// attack, and score the board for `seat`.
        .           ///
-- line 6700 ----------------------------------------
-- line 6731 ----------------------------------------
        .                   for k in 0..w.determinize {
        .                       if let Some(v) = simulate_attack_outcome_once(state, seat, attacks, w, k) {
        .                           total += v as i64;
        .                           n += 1;
        .                       }
        .                   }
        .                   return (n > 0).then(|| (total / n) as i32);
        .               }
    2,332 ( 0.00%)      simulate_attack_outcome_once(state, seat, attacks, w, 0)
1,546,187,117 (49.61%)  => crabomination/src/server/bot.rs:crabomination::server::bot::simulate_attack_outcome_once (1,166x)
        .           }
        .           
        .           /// One action on a throwaway dry-run clone.
        .           ///
        .           /// `perform_action`'s transaction checkpoint clones the whole state so a
        .           /// *rejected* action can be restored — and every caller of this helper throws
        .           /// the clone away on `Err`, so the restore is never read. The checkpoint is
        .           /// not free twice over: it also shares every CoW zone, so the simulation's
        .           /// next write deep-copies one. Use this wherever an `Err` ends the dry run.
        .           #[inline]
        .           fn dry_run(
        .               g: &mut GameState,
        .               action: GameAction,
        .           ) -> Result<Vec<crate::game::GameEvent>, crate::game::GameError> {
   17,174 ( 0.00%)      g.perform_action_inner(action)
73,624,076 ( 2.36%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner (1,482x)
        .           }
        .           
        .           /// The simulation loops' action step, with the historical fallback: a rejected
        .           /// action is rolled back and retried as a priority pass.
        .           ///
        .           /// When the action *is* a pass, the fallback is a no-op — the engine is
        .           /// deterministic, so retrying the same call on the restored state fails the
        .           /// same way — which is why a pass takes the checkpoint-free path and abandons
        .           /// on `Err`. The one error `perform_action` deliberately does not roll back is
        .           /// `ManualTapRequired`, which leaves exactly the state the retry would have
        .           /// seen, so that retry is kept and reads the same. Returns false when the
        .           /// simulation must be abandoned.
  212,688 ( 0.01%)  fn sim_step(g: &mut GameState, action: GameAction) -> bool {
   70,896 ( 0.00%)      if matches!(action, GameAction::PassPriority) {
  836,730 ( 0.03%)          return match g.perform_action_inner(GameAction::PassPriority) {
593,642,626 (19.05%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner (30,990x)
        .                       Ok(_) => true,
        .                       Err(crate::game::GameError::ManualTapRequired { .. }) => {
        .                           g.perform_action_inner(GameAction::PassPriority).is_ok()
        .                       }
        .                       Err(_) => false,
   30,990 ( 0.00%)          };
2,988,000 ( 0.10%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (30,990x)
        .               }
   98,100 ( 0.00%)      g.perform_action(action).is_ok() || dry_run(g, GameAction::PassPriority).is_ok()
352,617,756 (11.31%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action (4,458x)
1,036,222 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (4,466x)
  274,668 ( 0.01%)  }
  619,800 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (30,990x)
        .           
   19,822 ( 0.00%)  fn simulate_attack_outcome_once(
        .               state: &GameState,
        .               seat: usize,
        .               attacks: &[Attack],
        .               w: &EvalWeights,
        .               k: u8,
        .           ) -> Option<i32> {
        .               let mut g = sim_start_state(state, seat, w, k);
    6,996 ( 0.00%)      dry_run(&mut g, GameAction::DeclareAttackers(attacks.to_vec())).ok()?;
    1,166 ( 0.00%)      let start_turn = g.turn_number;
        .               // One turn cycle of pure priority passes is on the order of fifty
        .               // actions; the rest is headroom for triggers and decisions.
        .               let mut fuel = 400u32;
        .               // Break when this turn's opponent combat has resolved; the race
        .               // horizon can push the stop out one more cycle (see below).
        .               let mut stop_turn = start_turn;
        .               let mut extended = false;
        .               let mut declared: crate::fxhash::HashSet<(u32, TurnStep)> = Default::default();
        .               // *This* turn's attack declaration is the candidate, already submitted.
        .               // Without this the loop's own DeclareAttackers arm fires on the same
        .               // turn and re-declares the greedy set over the top of it — which the
        .               // engine happily accepts, so every candidate silently collapses back to
        .               // the alpha strike and the whole search scores one line N times.
        .               declared.insert((g.turn_number, TurnStep::DeclareAttackers));
   36,196 ( 0.00%)      while !g.is_game_over() {
        .                   // Stop once the opponent's combat is resolved — the first board on
        .                   // which a creature held back has actually done anything. Under
        .                   // `attack_race_horizon`, a sim ending with either life total in
        .                   // burn range runs one more full cycle instead, so the race this
        .                   // attack started is scored at its result, not mid-sprint.
  288,000 ( 0.01%)          if g.turn_number > stop_turn && g.step >= TurnStep::EndCombat {
    3,204 ( 0.00%)              if w.attack_race_horizon
        .                           && !extended
        .                           && g.players.iter().any(|p| p.is_alive() && p.life <= 10)
        .                       {
        .                           extended = true;
        .                           stop_turn = g.turn_number;
        .                           fuel = fuel.saturating_add(400);
        .                       } else {
        .                           break;
        .                       }
        .                   }
        .                   fuel = fuel.checked_sub(1)?;
   34,932 ( 0.00%)          if g.pending_decision.is_some() {
        .                       let answer = {
        .                           let pending = g.pending_decision.as_ref().unwrap();
    2,192 ( 0.00%)                  decide_pending_policy(&g, pending.acting_player(), w, &pending.decision, false)
    6,576 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::PendingDecision::acting_player (548x)
        .                       };
    5,480 ( 0.00%)              dry_run(&mut g, GameAction::SubmitDecision(answer)).ok()?;
        .                       continue;
        .                   }
        .                   // Declarations are one-shot per step per turn; the marker keeps a
        .                   // rejected declaration from being retried forever.
        .                   let key = (g.turn_number, g.step);
  154,118 ( 0.00%)          let action = match g.step {
        .                       TurnStep::DeclareAttackers if !declared.contains(&key) => {
        .                           declared.insert(key);
        .                           let declarer = g.attack_declarer();
        .                           // Greedy, deliberately: calling the search here would
        .                           // recurse a turn deeper on every candidate.
    7,714 ( 0.00%)                  GameAction::DeclareAttackers(pick_attacks(&g, declarer))
        .                       }
    1,478 ( 0.00%)              TurnStep::DeclareBlockers if !declared.contains(&key) && !g.attacking().is_empty() => {
        .                           match (0..g.players.len()).find(|&s| g.may_declare_blocks(s)) {
        .                               Some(defender) => {
        .                                   declared.insert(key);
   16,258 ( 0.00%)                          GameAction::DeclareBlockers(pick_blocks(&g, defender))
34,446,778 ( 1.11%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks (1,478x)
        .                               }
        .                               None => GameAction::PassPriority,
        .                           }
        .                       }
   63,608 ( 0.00%)              _ if w.attack_sim_spells => {
   63,608 ( 0.00%)                  sim_spell_action(&g, w).unwrap_or(GameAction::PassPriority)
        .                       }
        .                       _ => GameAction::PassPriority,
        .                   };
  171,920 ( 0.01%)          if !sim_step(&mut g, action) {
900,795,490 (28.90%)  => crabomination/src/server/bot.rs:crabomination::server::bot::sim_step (34,384x)
        .                       return None;
        .                   }
        .               }
        .               let v = eval_material(&g, seat, w);
        .               super::leaf_capture::maybe(&g, seat, v);
        .               Some(v)
   12,826 ( 0.00%)  }
33,381,776 ( 1.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (1,166x)
        .           
        .           /// The spell a combat simulation lets the current priority holder cast —
        .           /// see [`EvalWeights::attack_sim_spells`]. The response layer and the
        .           /// post-block trick window mirror the real dispatch; a main phase inside
        .           /// the sim's horizon takes the best STATIC-ranked candidate. No outcome
        .           /// eval, no hold gates, no jitter: nesting the full pick inside a sim
        .           /// would multiply clone-and-resolve work per candidate, and a
        .           /// deterministic greedy stand-in carries exactly the information the sim
        .           /// is missing — "that mana will be spent on something".
        .           fn sim_spell_action(g: &GameState, w: &EvalWeights) -> Option<GameAction> {
        .               // Called once per sim-loop iteration on a cloned (unfrozen) state; every
        .               // candidate it ranks runs layer-aware checks.
  197,208 ( 0.01%)      g.with_frozen_layers(|g| sim_spell_action_inner(g, w))
7,286,109 ( 0.23%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (1,064x)
        .           }
        .           
  394,416 ( 0.01%)  fn sim_spell_action_inner(g: &GameState, w: &EvalWeights) -> Option<GameAction> {
   65,736 ( 0.00%)      let p = g.player_with_priority();
   98,604 ( 0.00%)      if !g.stack.is_empty() {
   13,272 ( 0.00%)          return pick_stack_response(g, p, w)
34,775,086 ( 1.12%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_stack_response (4,424x)
   12,732 ( 0.00%)              .or_else(|| pick_ability_counter_response(g, p))
  156,130 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_ability_counter_response (4,244x)
   25,464 ( 0.00%)              .or_else(|| pick_prepare_response(g, p, w));
  445,480 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_prepare_response (4,244x)
        .               }
  130,348 ( 0.00%)      if g.step == TurnStep::DeclareBlockers && g.blockers_declared() {
   18,590 ( 0.00%)          return pick_combat_trick(g, p, w);
34,525,306 ( 1.11%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_combat_trick (3,718x)
        .               }
        .               if matches!(g.step, TurnStep::PreCombatMain | TurnStep::PostCombatMain)
   11,708 ( 0.00%)          && g.active_player_idx == p
        .               {
   10,926 ( 0.00%)          let probe = g.affordance_probe_template();
32,869,340 ( 1.05%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::affordance_probe_template (3,642x)
   21,852 ( 0.00%)          let mut ranked: Vec<(i32, GameAction, bool)> = cast_candidates(g, p, &probe, w)
93,076,097 ( 2.99%)  => crabomination/src/server/bot.rs:crabomination::server::bot::cast_candidates (3,642x)
        .                       .into_iter()
   35,632 ( 0.00%)              .map(|(a, ok)| (score_candidate(g, p, &a, w), a, ok))
1,160,194 ( 0.04%)  => crabomination/src/server/bot.rs:crabomination::server::bot::score_candidate (2,096x)
        .                       .collect();
        .                   ranked.sort_by_key(|(s, _, _)| std::cmp::Reverse(*s));
   43,160 ( 0.00%)          for (_, a, ok) in ranked {
   15,220 ( 0.00%)              if ok || GameState::would_accept_on(&probe, a.clone()) {
202,500,072 ( 6.50%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (1,522x)
  114,044 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (1,522x)
   17,160 ( 0.00%)                  return Some(a);
        .                       }
      184 ( 0.00%)          }
    2,116 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (92x)
    3,642 ( 0.00%)      }
11,719,575 ( 0.38%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (3,642x)
   23,296 ( 0.00%)      None
  295,812 ( 0.01%)  }
        .           
        .           /// The block assignment, chosen by search rather than by rule.
        .           ///
        .           /// [`pick_blocks`] assigns blockers greedily, one at a time, in ascending
        .           /// power order, each taking the best attacker it can find *given the
        .           /// assignments already made*. That ordering is a heuristic standing in for
        .           /// the thing it can't do: score the whole assignment. A first-fit choice
        .           /// that looks locally best can spend the one blocker a later, bigger
-- line 6913 ----------------------------------------
-- line 6927 ----------------------------------------
        .           /// Ties go to the greedy assignment, so the search only ever departs from
        .           /// current behavior for a strict improvement.
        .           /// The block assignments worth scoring: greedy first (index 0 wins every
        .           /// tie), no-blocks, greedy-minus-one releases (cheapest bodies first —
        .           /// those are the chump-blocks the greedy pass throws in to save life,
        .           /// and the ones most likely to be worth more alive than the damage they
        .           /// absorb), then the gang candidates. Shared by the sim search below and
        .           /// the Monte Carlo bot, so the two search identical menus.
    2,544 ( 0.00%)  pub(crate) fn block_candidates_for_mcts(
        .               state: &GameState,
        .               seat: usize,
        .               w: &EvalWeights,
        .           ) -> Vec<Vec<(CardId, CardId)>> {
      212 ( 0.00%)      let greedy = pick_blocks(state, seat);
2,794,285 ( 0.09%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks (212x)
    1,060 ( 0.00%)      if w.block_search == 0 || greedy.is_empty() {
      512 ( 0.00%)          return vec![greedy];
        .               }
      588 ( 0.00%)      let mut candidates: Vec<Vec<(CardId, CardId)>> = vec![greedy.clone(), Vec::new()];
      168 ( 0.00%)      if greedy.len() > 1 {
        .                   let mut order: Vec<usize> = (0..greedy.len()).collect();
        .                   order.sort_by_key(|&i| {
      270 ( 0.00%)              state.battlefield_find(greedy[i].0).map(|c| c.toughness()).unwrap_or(0)
    7,344 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (108x)
        .                   });
        .                   for &i in order.iter().take(w.block_search as usize) {
        .                       let mut alt = greedy.clone();
        .                       alt.remove(i);
        .                       candidates.push(alt);
        .                   }
        .               }
        .           
      168 ( 0.00%)      if w.block_gang {
        .                   candidates.extend(gang_block_candidates(state, seat, &greedy, w));
        .               }
      336 ( 0.00%)      candidates
    1,908 ( 0.00%)  }
        .           
    2,544 ( 0.00%)  fn pick_blocks_scored(state: &GameState, seat: usize, w: &EvalWeights) -> Vec<(CardId, CardId)> {
      212 ( 0.00%)      let mut candidates = block_candidates_for_mcts(state, seat, w);
3,075,809 ( 0.10%)  => crabomination/src/server/bot.rs:crabomination::server::bot::block_candidates_for_mcts (212x)
      424 ( 0.00%)      if candidates.len() == 1 {
        .                   return candidates.swap_remove(0);
        .               }
        .           
        .               let mut scored: Vec<(usize, i32)> = Vec::new();
        .               for (i, cand) in candidates.iter().enumerate() {
      460 ( 0.00%)          let Some(score) = simulate_block_outcome(state, seat, cand, w) else { continue };
        .                   scored.push((i, score));
        .               }
      588 ( 0.00%)      match choose_scored(state.turn_number, &scored) {
    4,438 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::choose_scored (84x)
        .                   Some(i) => candidates.swap_remove(i),
        .                   None => candidates.swap_remove(0),
        .               }
        .           }
        .           
        .           /// Block assignments that add a gang onto an attacker the greedy pass
        .           /// left alone, one candidate per attacker worth ganging.
        .           ///
-- line 6982 ----------------------------------------
-- line 7003 ----------------------------------------
        .               const MAX_CANDIDATES: usize = 3;
        .           
        .               let blocked: crate::fxhash::HashSet<CardId> =
        .                   greedy.iter().map(|(_, a)| *a).collect();
        .               let used: crate::fxhash::HashSet<CardId> = greedy.iter().map(|(b, _)| *b).collect();
        .           
        .               // Idle bodies, cheapest first: the gang should cost as little as it
        .               // can and still kill.
       84 ( 0.00%)      let mut idle: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
   29,994 ( 0.00%)          .filter(|c| c.controller == seat && bot_can_block(c) && !used.contains(&c.id))
        .                   .collect();
       96 ( 0.00%)      idle.sort_by_key(|c| permanent_value(state, c.id, w));
   14,906 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (40x)
        .               if idle.len() < 2 {
        .                   return Vec::new();
        .               }
        .           
        .               // Unblocked attackers, most valuable first — the gang is only worth
        .               // its losses against a real threat.
        .               let mut targets: Vec<&crate::card::CardInstance> = state
        .                   .attacking
        .                   .iter()
        .                   .filter(|a| !blocked.contains(&a.attacker))
        .                   .filter_map(|a| state.battlefield_find(a.attacker))
        8 ( 0.00%)          .filter(|c| c.controller != seat)
        .                   .collect();
        .               targets.sort_by_key(|c| -permanent_value(state, c.id, w));
        .           
        .               let mut out = Vec::new();
       12 ( 0.00%)      for atk in targets.into_iter().take(MAX_CANDIDATES) {
       16 ( 0.00%)          let a_flying = atk.has_keyword(&Keyword::Flying);
      314 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4x)
       16 ( 0.00%)          let a_tough = atk.toughness() - atk.damage as i32;
      272 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (4x)
        .                   let mut gang: Vec<CardId> = Vec::new();
        .                   let mut dmg = 0i32;
        .                   for b in &idle {
       40 ( 0.00%)              if a_flying && !b.has_keyword(&Keyword::Flying) && !b.has_keyword(&Keyword::Reach) {
      600 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (8x)
        .                           continue;
        .                       }
        8 ( 0.00%)              gang.push(b.id);
       16 ( 0.00%)              dmg += b.power().max(0);
      244 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (4x)
       32 ( 0.00%)              if b.has_keyword(&Keyword::Deathtouch) || dmg >= a_tough {
      260 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4x)
        .                           break;
        .                       }
        .                   }
        .                   // A single blocker is the greedy pass's own decision, already
        .                   // scored; two or more that kill is the new option.
       12 ( 0.00%)          if gang.len() < 2 || dmg < a_tough {
        .                       continue;
        .                   }
        .                   let mut cand = greedy.to_vec();
        6 ( 0.00%)          for b in gang {
        4 ( 0.00%)              cand.push((b, atk.id));
        .                   }
        4 ( 0.00%)          out.push(cand);
        .               }
       12 ( 0.00%)      out
        .           }
        .           
        .           /// Declare `blocks`, run combat damage, and score the board for `seat`.
        .           ///
        .           /// `None` on a rejected declaration (a must-block creature we tried to hold
        .           /// back, an over-cap batch) or a combat that won't settle — an unfinished
        .           /// combat is scored not at all rather than scored wrong.
        .           /// Redeal everything `seat` cannot legitimately see: each opponent's hand
-- line 7067 ----------------------------------------
-- line 7120 ----------------------------------------
        .                   g.players[p].hand.extend(redrawn);
        .               }
        .           }
        .           
        .           /// The state a simulation should start from: the real one, or a redeal of
        .           /// its hidden zones. `k` indexes the redeal so an averaging caller gets
        .           /// different hands each time.
        .           fn sim_start_state(state: &GameState, seat: usize, w: &EvalWeights, k: u8) -> GameState {
    1,396 ( 0.00%)      let mut g = state.clone();
  554,677 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (230x)
    4,418 ( 0.00%)      if w.determinize > 0 {
        .                   determinize_hidden(&mut g, seat, 0x5EED_0000 ^ k as u64);
        .               }
    2,792 ( 0.00%)      g
   94,990 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (230x)
        .           }
        .           
        .           fn simulate_block_outcome(
        .               state: &GameState,
        .               seat: usize,
        .               blocks: &[(CardId, CardId)],
        .               w: &EvalWeights,
        .           ) -> Option<i32> {
-- line 7140 ----------------------------------------
-- line 7148 ----------------------------------------
        .                           total += v as i64;
        .                           n += 1;
        .                       }
        .                   }
        .                   // Every redeal failing means the assignment is illegal, not
        .                   // merely unlucky — propagate that as before.
        .                   return (n > 0).then(|| (total / n) as i32);
        .               }
      460 ( 0.00%)      simulate_block_outcome_once(state, seat, blocks, w, 0)
83,476,382 ( 2.68%)  => crabomination/src/server/bot.rs:crabomination::server::bot::simulate_block_outcome_once (230x)
        .           }
        .           
    3,910 ( 0.00%)  fn simulate_block_outcome_once(
        .               state: &GameState,
        .               seat: usize,
        .               blocks: &[(CardId, CardId)],
        .               w: &EvalWeights,
        .               k: u8,
        .           ) -> Option<i32> {
        .               let mut g = sim_start_state(state, seat, w, k);
    1,368 ( 0.00%)      dry_run(&mut g, GameAction::DeclareBlockers(blocks.to_vec())).ok()?;
        .               let turn = g.turn_number;
        .               let mut fuel = 200u32;
    6,120 ( 0.00%)      while !g.is_game_over() && g.turn_number == turn && g.step < TurnStep::EndCombat {
        .                   fuel = fuel.checked_sub(1)?;
    1,120 ( 0.00%)          if g.pending_decision.is_some() {
        .                       let answer = {
        .                           let pending = g.pending_decision.as_ref().unwrap();
      224 ( 0.00%)                  decide_pending_policy(&g, pending.acting_player(), w, &pending.decision, false)
      672 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::PendingDecision::acting_player (56x)
        .                       };
      560 ( 0.00%)              dry_run(&mut g, GameAction::SubmitDecision(answer)).ok()?;
        .                       continue;
        .                   }
        .                   // Under `attack_sim_spells` the combat window is live: tricks and
        .                   // responses fire for whichever seat holds priority, so a block
        .                   // that only works until the attacker pumps is scored as such.
    3,136 ( 0.00%)          let action = if w.attack_sim_spells {
    1,064 ( 0.00%)              sim_spell_action(&g, w).unwrap_or(GameAction::PassPriority)
        .                   } else {
        .                       GameAction::PassPriority
        .                   };
    5,320 ( 0.00%)          if !sim_step(&mut g, action) {
51,707,602 ( 1.66%)  => crabomination/src/server/bot.rs:crabomination::server::bot::sim_step (1,064x)
        .                       return None;
        .                   }
        .               }
        .               let v = eval_material(&g, seat, w);
        .               super::leaf_capture::maybe(&g, seat, v);
        .               Some(v)
    2,530 ( 0.00%)  }
3,798,202 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (230x)
        .           
   18,944 ( 0.00%)  fn pick_blocks(state: &GameState, seat: usize) -> Vec<(CardId, CardId)> {
        .               // The heuristic probes block legality per blocker×attacker pair, each a
        .               // layer-aware check — share one gather across the whole scan.
    7,104 ( 0.00%)      let mut blocks = state.with_frozen_layers(|state| pick_blocks_inner(state, seat));
49,656,684 ( 1.59%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (2,368x)
        .               // CR 509.1b — Silent Arbiter caps the distinct blockers for the whole
        .               // combat; an over-sized batch is rejected outright, so keep only the
        .               // first `cap` blockers (the heuristic already ordered them best-first).
   11,840 ( 0.00%)      if let Some(cap) = state.combat_participation_cap(true) {
  437,424 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::combat_participation_cap (2,368x)
        .                   let mut kept: Vec<CardId> = state.block_map.keys().copied().collect();
        .                   blocks.retain(|(blocker, _)| {
        .                       if kept.contains(blocker) {
        .                           return true;
        .                       }
        .                       if kept.len() >= cap as usize {
        .                           return false;
        .                       }
        .                       kept.push(*blocker);
        .                       true
        .                   });
        .               }
    9,472 ( 0.00%)      blocks
    9,472 ( 0.00%)  }
        .           
        .           /// A creature the bot may legally declare as a blocker: `can_block()` only
        .           /// checks creature-ness + untapped, so also exclude Decayed (CR 702.147 — a
        .           /// Decayed creature can't block) and a granted/printed `CantBlock`. Used by
        .           /// every blocker-candidate pass so the gang-block / must-be-blocked / menace
        .           /// top-up passes never assemble an illegal declaration the engine rejects.
        .           fn bot_can_block(c: &crate::card::CardInstance) -> bool {
        .               use crate::card::Keyword;
  271,264 ( 0.01%)      c.can_block() && !c.has_keyword(&Keyword::Decayed) && !c.has_keyword(&Keyword::CantBlock)
  546,020 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (7,052x)
  350,304 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::can_block (23,660x)
        .           }
        .           
   23,680 ( 0.00%)  fn pick_blocks_inner(state: &GameState, seat: usize) -> Vec<(CardId, CardId)> {
        .               // Improved blocker heuristic (push claude/modern_decks):
        .               //   1. Build the candidate set of (attacker, attacker_power,
        .               //      attacker_toughness, has_flying) attacking us.
        .               //   2. Sort blockers by ascending power so cheap chumps get
        .               //      assigned first; bigger blockers stay free for must-block
        .               //      situations.
        .               //   3. For each blocker, pick the **best** attacker it can block:
        .               //      - Prefer attackers it can kill outright (blocker_power >=
-- line 7238 ----------------------------------------
-- line 7248 ----------------------------------------
        .               //      blocker can't kill it — the loop falls through to try the
        .               //      next blocker.
        .               use crate::card::Keyword;
        .               // (id, power, toughness, flying, deathtouch). Deathtouch makes the
        .               // attacker lethal to any blocker it damages regardless of power, so
        .               // the bot must treat a block against it as a likely loss of the
        .               // blocker when scoring trades.
        .               let attacker_info: Vec<(CardId, i32, i32, bool, bool)> = state
    4,736 ( 0.00%)          .attacking()
        .                   .iter()
        .                   .filter(|atk| state.defender_for(atk.target) == Some(seat))
        .                   .filter_map(|atk| {
    4,232 ( 0.00%)              state
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == atk.attacker)
        .                           .map(|a| {
        .                               (
        .                                   atk.attacker,
   12,696 ( 0.00%)                          attacker_damage_value(state, atk.attacker),
9,901,234 ( 0.32%)  => crabomination/src/server/bot.rs:crabomination::server::bot::attacker_damage_value (4,232x)
   10,832 ( 0.00%)                          a.toughness(),
  287,776 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (4,232x)
   17,432 ( 0.00%)                          a.has_keyword(&Keyword::Flying),
  267,544 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,232x)
   15,064 ( 0.00%)                          a.has_keyword(&Keyword::Deathtouch),
  307,546 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,232x)
        .                               )
        .                           })
        .                   })
        .                   .collect();
        .               // Only attackers aimed at the *player* threaten our life total — damage
        .               // to a planeswalker we control hits its loyalty, not our face. Summing
        .               // every attacker here would over-state the life threat and trigger
        .               // needless chump-blocks.
        .               let total_incoming: i32 = state
    2,368 ( 0.00%)          .attacking()
        .                   .iter()
        .                   .filter(|atk| atk.target == AttackTarget::Player(seat))
    4,232 ( 0.00%)          .map(|atk| attacker_damage_value(state, atk.attacker))
  745,452 ( 0.02%)  => crabomination/src/server/bot.rs:crabomination::server::bot::attacker_damage_value (4,232x)
        .                   .sum();
        .               // Planeswalker defense (CR 306.7): for each planeswalker we control that
        .               // is being attacked, if the attackers aimed at it would deal lethal
        .               // (total power ≥ its loyalty), mark those attackers so the chump-block
        .               // pass will trade idle blockers to save the walker.
        .               let defend_attackers: crate::fxhash::HashSet<CardId> = {
        .                   use crate::card::CounterType;
        .                   let mut pw_attackers: crate::fxhash::HashMap<CardId, (u32, Vec<CardId>)> =
        .                       crate::fxhash::HashMap::default();
    4,736 ( 0.00%)          for atk in state.attacking() {
    8,464 ( 0.00%)              if let AttackTarget::Planeswalker(pw) = atk.target
        .                           && state.battlefield_find(pw).map(|c| c.controller) == Some(seat)
        .                           && let Some(a) = state.battlefield.iter().find(|c| c.id == atk.attacker)
        .                       {
        .                           let e = pw_attackers.entry(pw).or_default();
        .                           e.0 += a.power().max(0) as u32;
        .                           e.1.push(atk.attacker);
        .                       }
        .                   }
        .                   let mut set = crate::fxhash::HashSet::default();
    7,104 ( 0.00%)          for (pw, (incoming, atkrs)) in pw_attackers {
        .                       let loyalty = state
        .                           .battlefield_find(pw)
        .                           .and_then(|c| c.counters.iter().find_map(|(k, v)| {
        .                               matches!(k, CounterType::Loyalty).then_some(*v)
        .                           }))
        .                           .unwrap_or(0);
        .                       if loyalty > 0 && incoming >= loyalty {
        .                           set.extend(atkrs);
        .                       }
        .                   }
   11,840 ( 0.00%)          set
        .               };
        .               // Infect (CR 702.90) / Toxic (CR 702.180) make poison the lethal clock,
        .               // not life: a player with 10+ poison counters loses (CR 104.3d). The bot
        .               // must chump an infect/toxic attacker to avoid a poison-out even at a
        .               // healthy life total. Infect deals its power as poison; Toxic N adds N on
        .               // top of normal combat damage.
        .               let incoming_poison: u32 = state
    2,368 ( 0.00%)          .attacking()
        .                   .iter()
        .                   .filter(|atk| state.defender_for(atk.target) == Some(seat))
        .                   .filter_map(|atk| state.battlefield.iter().find(|c| c.id == atk.attacker))
        .                   .map(|a| {
        .                       let mut p = 0u32;
   21,160 ( 0.00%)              if a.has_keyword(&Keyword::Infect) {
  329,720 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,232x)
        .                           p += a.power().max(0) as u32;
        .                       }
    4,232 ( 0.00%)              p += a
        .                           .definition
        .                           .keywords
        .                           .iter()
   14,544 ( 0.00%)                  .filter_map(|k| match k {
        .                               Keyword::Toxic(n) | Keyword::Poisonous(n) => Some(*n),
        .                               _ => None,
        .                           })
        .                           .sum::<u32>();
        .                       p
        .                   })
        .                   .sum();
        .               let poison_threatened =
    4,736 ( 0.00%)          incoming_poison > 0 && state.players[seat].poison_counters + incoming_poison >= 10;
   11,840 ( 0.00%)      let life_threatened = state.players[seat].life <= total_incoming || poison_threatened;
        .           
    2,368 ( 0.00%)      let mut blockers: Vec<(CardId, i32, i32, bool, bool, bool)> = state
        .                   .battlefield
        .                   .iter()
        .                   // `can_block()` only checks creature-ness + untapped; also exclude
        .                   // creatures that genuinely can't block (Decayed CR 702.147, or a
        .                   // granted "can't block") so the bot never submits an illegal block.
  145,386 ( 0.00%)          .filter(|c| c.controller == seat && bot_can_block(c))
        .                   .map(|c| {
        .                       (
    7,052 ( 0.00%)                  c.id,
   10,578 ( 0.00%)                  c.power(),
  215,086 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (3,526x)
   10,578 ( 0.00%)                  c.toughness(),
  239,768 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (3,526x)
   15,842 ( 0.00%)                  c.has_keyword(&Keyword::Flying),
  223,262 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (3,526x)
   14,104 ( 0.00%)                  c.has_keyword(&Keyword::Reach),
  273,010 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (3,526x)
   14,104 ( 0.00%)                  c.has_keyword(&Keyword::Deathtouch),
  255,524 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (3,526x)
        .                       )
        .                   })
        .                   .collect();
        .               blockers.sort_by_key(|(_, p, _, _, _, _)| *p);
        .           
        .               // Track which attackers have already been damage-saturated by
        .               // assigned blockers — if blocker total toughness >= attacker
        .               // power, additional blockers on the same attacker are wasteful
-- line 7370 ----------------------------------------
-- line 7372 ----------------------------------------
        .               let mut attacker_damage_taken: crate::fxhash::HashMap<CardId, i32> =
        .                   crate::fxhash::HashMap::default();
        .               // Blockers already committed to each attacker — folds Rampage (CR 702.23)
        .               // into the trade math for the second-and-later blocker.
        .               let mut attacker_block_count: crate::fxhash::HashMap<CardId, i32> =
        .                   crate::fxhash::HashMap::default();
        .               let mut assignments: Vec<(CardId, CardId)> = Vec::new();
        .           
    9,420 ( 0.00%)      for (b_id, b_pow, b_tough, b_flying, b_reach, b_dt) in blockers {
        .                   // Pick the best attacker for this blocker.
        .                   let mut best: Option<(CardId, i32, bool)> = None; // (attacker, score, was_kill)
        .                   for (a_id, a_pow, a_tough, a_flying, a_dt) in &attacker_info {
   42,060 ( 0.00%)              if *a_flying && !b_flying && !b_reach {
        .                           continue;
        .                       }
        .                       // Authoritative legality gate (CR 509.1b): also honors
        .                       // "can't be blocked except by …" / "… by …" restrictions,
        .                       // protection, shadow, etc. Skip attackers this blocker can't
        .                       // legally be assigned to, so the bot never submits a block batch
        .                       // the engine will reject.
   43,722 ( 0.00%)              if !state.blocker_can_block_attacker(b_id, *a_id) {
12,083,843 ( 0.39%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::blocker_can_block_attacker (6,246x)
        .                           continue;
        .                       }
        .                       // Skip attackers that already have at least their toughness
        .                       // worth of damage queued unless we have deathtouch.
    1,388 ( 0.00%)              let queued = attacker_damage_taken.get(a_id).copied().unwrap_or(0);
        .                       // Rampage N (CR 702.23): every blocker beyond the first pumps the
        .                       // attacker +N/+N. When this would be an additional blocker, fold
        .                       // that bonus into the effective P/T so the bot doesn't gang-block
        .                       // into a pump that saves the attacker and kills the extra blocker.
    1,388 ( 0.00%)              let bcount = attacker_block_count.get(a_id).copied().unwrap_or(0);
    5,950 ( 0.00%)              let rampage = state
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == *a_id)
        .                           .map(|a| {
        .                               a.definition
        .                                   .keywords
        .                                   .iter()
        .                                   .chain(a.granted_keywords_eot.iter())
    5,744 ( 0.00%)                          .filter_map(|k| match k {
        .                                       Keyword::Rampage(n) => Some(*n as i32),
        .                                       _ => None,
        .                                   })
        .                                   .max()
        .                                   .unwrap_or(0)
        .                           })
        .                           .unwrap_or(0);
        .                       let ramp_bonus = rampage * bcount;
   11,900 ( 0.00%)              let eff_a_tough = *a_tough + ramp_bonus;
    5,950 ( 0.00%)              let eff_a_pow = *a_pow + ramp_bonus;
    8,058 ( 0.00%)              if !b_dt && queued >= eff_a_tough {
        .                           continue;
        .                       }
        .                       // First-strike awareness (CR 702.7): if the attacker strikes
        .                       // first (and the blocker doesn't strike back first) and its
        .                       // first-strike damage is already lethal to the blocker, the
        .                       // blocker dies *before* dealing any damage — so it never trades
        .                       // up. Such a "kill" is illusory; downgrade it to a chump.
        .                       let atk_first_strike = state
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == *a_id)
        .                           .is_some_and(|a| {
   41,020 ( 0.00%)                      a.has_keyword(&Keyword::FirstStrike) || a.has_keyword(&Keyword::DoubleStrike)
  754,340 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (10,240x)
        .                           });
        .                       let blk_first_strike = {
    5,150 ( 0.00%)                  let blk = state.battlefield.iter().find(|c| c.id == b_id);
        .                           blk.is_some_and(|c| {
   51,490 ( 0.00%)                      c.has_keyword(&Keyword::FirstStrike) || c.has_keyword(&Keyword::DoubleStrike)
  816,772 ( 0.03%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (10,298x)
        .                           })
        .                       };
        .                       // CR 702.16e — protection prevents combat damage either way:
        .                       // a blocker protected from the attacker's color takes none (won't
        .                       // die), and an attacker protected from the blocker's color takes
        .                       // none (won't be killed). Factor both into the trade math.
   20,600 ( 0.00%)              let blocker_takes_no_dmg = state.damage_prevented_by_protection(*a_id, b_id);
1,058,710 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (5,150x)
   20,600 ( 0.00%)              let attacker_takes_no_dmg = state.damage_prevented_by_protection(b_id, *a_id);
1,044,014 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (5,150x)
        .                       // CR 702.12 — an indestructible permanent isn't destroyed by lethal
        .                       // damage (or deathtouch), so it never dies in a trade and can't be
        .                       // killed by a blocker. Block freely behind an indestructible body.
        .                       let blocker_indestructible =
   15,450 ( 0.00%)                  state.battlefield_find(b_id).is_some_and(|c| c.is_indestructible());
  501,310 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::is_indestructible (5,150x)
        .                       let attacker_indestructible =
    5,150 ( 0.00%)                  state.battlefield_find(*a_id).is_some_and(|c| c.is_indestructible());
  476,590 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::is_indestructible (5,150x)
   20,600 ( 0.00%)              let dies_before_striking = atk_first_strike
        .                           && !blk_first_strike
      120 ( 0.00%)                  && !blocker_takes_no_dmg
      180 ( 0.00%)                  && !blocker_indestructible
      180 ( 0.00%)                  && (eff_a_pow >= b_tough || (*a_dt && eff_a_pow >= 1));
   26,996 ( 0.00%)              let kills_attacker = !attacker_takes_no_dmg
        .                           && !attacker_indestructible
        .                           && !dies_before_striking
    7,088 ( 0.00%)                  && (b_dt || b_pow >= (eff_a_tough - queued));
        .                       // A deathtouch attacker kills the blocker on any damage.
   10,180 ( 0.00%)              let dies_to_attacker = !blocker_takes_no_dmg
        .                           && !blocker_indestructible
   11,750 ( 0.00%)                  && (eff_a_pow >= b_tough || (*a_dt && eff_a_pow >= 1));
        .                       // Scoring: clean trade (kill, don't die) > kill-and-die >
        .                       // chump (don't kill, die). Higher attacker power adds value.
    5,082 ( 0.00%)              let score = if kills_attacker && !dies_to_attacker {
    2,172 ( 0.00%)                  1000 + *a_pow
   15,592 ( 0.00%)              } else if kills_attacker && dies_to_attacker {
        .                           // Even trade (both die). Prefer trading up: score by the
        .                           // stat delta (proxy = power + toughness). Don't sacrifice a
        .                           // much bigger creature for a small attacker unless we're
        .                           // under pressure — keep the body and take the hit.
    6,740 ( 0.00%)                  let delta = (*a_pow + *a_tough) - (b_pow + b_tough);
    4,182 ( 0.00%)                  if !life_threatened && delta < -2 {
        .                               continue;
        .                           }
    2,970 ( 0.00%)                  500 + delta
    6,336 ( 0.00%)              } else if blocker_indestructible && !dies_to_attacker && *a_pow >= 1 {
        .                           // An indestructible wall absorbs the attacker's damage at no
        .                           // cost (it survives and isn't tapped). Free value even with no
        .                           // life pressure — block the biggest attacker it can.
        .                           200 + *a_pow
    2,112 ( 0.00%)              } else if life_threatened || defend_attackers.contains(a_id) {
        .                           // Chump-block to stop lethal damage (or to save a doomed
        .                           // planeswalker). A trampler tramples over a chump
        .                           // (CR 702.19e), so a lone chump only stops `blocker_toughness`
        .                           // of its damage — score by the actual damage saved so the bot
        .                           // prefers fully blocking a non-trampler over partially
        .                           // blocking a trampler.
      534 ( 0.00%)                  let a_trample = state
        .                               .battlefield
        .                               .iter()
        .                               .find(|c| c.id == *a_id)
    1,068 ( 0.00%)                      .is_some_and(|a| a.has_keyword(&Keyword::Trample));
   35,570 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (534x)
    1,068 ( 0.00%)                  let saved = if a_trample { b_tough.min(*a_pow) } else { *a_pow };
      534 ( 0.00%)                  100 + saved
        .                       } else {
        .                           continue;
        .                       };
        .                       if best.map(|(_, s, _)| s < score).unwrap_or(true) {
        .                           best = Some((*a_id, score, kills_attacker));
        .                       }
        .                   }
    8,890 ( 0.00%)          if let Some((a_id, _score, _kill)) = best {
        .                       assignments.push((b_id, a_id));
        .                       // Mark the damage queued so subsequent blockers can pile on
        .                       // attackers that aren't fully covered yet.
    4,476 ( 0.00%)              *attacker_damage_taken.entry(a_id).or_insert(0) += b_tough;
    2,238 ( 0.00%)              *attacker_block_count.entry(a_id).or_insert(0) += 1;
        .                   }
        .               }
        .               // Gang-block-to-kill when our life is threatened. The greedy single-
        .               // blocker pass above only starts blocking an attacker when one blocker
        .               // alone can kill it (or we chump). When we're facing lethal, trading
        .               // several spare creatures to *remove* a big attacker permanently beats
        .               // scattering chumps that die for nothing. For each still-unblocked
        .               // attacker (largest power first), pile idle blockers on until their
        .               // combined power reaches the attacker's toughness, then commit only if
        .               // the gang actually kills it.
    7,104 ( 0.00%)      if life_threatened {
        .                   let mut used: crate::fxhash::HashSet<CardId> =
        .                       assignments.iter().map(|(b, _)| *b).collect();
      360 ( 0.00%)          let mut idle: Vec<(CardId, i32, i32, bool, bool, bool)> = state
        .                       .battlefield
        .                       .iter()
        .                       .filter(|c| c.controller == seat && bot_can_block(c) && !used.contains(&c.id))
        .                       .map(|c| {
        .                           (
      236 ( 0.00%)                      c.id,
      354 ( 0.00%)                      c.power(),
    7,198 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::power (118x)
      354 ( 0.00%)                      c.toughness(),
    8,024 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (118x)
      562 ( 0.00%)                      c.has_keyword(&Keyword::Flying),
    7,540 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (118x)
      444 ( 0.00%)                      c.has_keyword(&Keyword::Reach),
    8,450 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (118x)
      444 ( 0.00%)                      c.has_keyword(&Keyword::Deathtouch),
    8,450 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (118x)
        .                           )
        .                       })
        .                       .collect();
        .                   let mut uncovered: Vec<(CardId, i32, i32, bool, bool)> = attacker_info
        .                       .iter()
        .                       .filter(|(a_id, _, _, _, _)| !assignments.iter().any(|(_, aid)| aid == a_id))
        .                       .copied()
        .                       .collect();
      748 ( 0.00%)          uncovered.sort_by_key(|(_, p, _, _, _)| -*p);
    1,276 ( 0.00%)          for (a_id, _a_pow, a_tough, a_flying, _a_dt) in uncovered {
        .                       // Rampage N (CR 702.23): each blocker beyond the first raises the
        .                       // attacker's toughness by N, so a gang must out-damage the pumped
        .                       // total — otherwise the chumps die and the attacker survives.
      458 ( 0.00%)              let rampage = state
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == a_id)
        .                           .map(|a| {
        .                               a.definition
        .                                   .keywords
        .                                   .iter()
        .                                   .chain(a.granted_keywords_eot.iter())
      478 ( 0.00%)                          .filter_map(|k| match k {
        .                                       Keyword::Rampage(n) => Some(*n as i32),
        .                                       _ => None,
        .                                   })
        .                                   .max()
        .                                   .unwrap_or(0)
        .                           })
        .                           .unwrap_or(0);
        .                       // Collect a gang of legal idle blockers that together kill it.
        .                       let mut gang: Vec<CardId> = Vec::new();
        .                       let mut dmg = 0i32;
        .                       let mut kills = false;
        .                       for (b_id, b_pow, _bt, b_fly, b_reach, b_dt) in &idle {
      388 ( 0.00%)                  if a_flying && !b_fly && !b_reach {
        .                               continue;
        .                           }
       84 ( 0.00%)                  gang.push(*b_id);
        .                           dmg += *b_pow;
        .                           let eff_tough = a_tough + rampage * (gang.len() as i32 - 1);
      588 ( 0.00%)                  if *b_dt || dmg >= eff_tough {
        .                               kills = true;
        .                               break;
        .                           }
        .                       }
        .                       if kills {
        .                           for b_id in &gang {
       18 ( 0.00%)                      assignments.push((*b_id, a_id));
        .                               used.insert(*b_id);
        .                           }
        .                           idle.retain(|(id, ..)| !gang.contains(id));
        .                       }
        .                   }
        .               }
        .           
        .               // CR 509.1c — satisfy "must be blocked if able" (Academic Dispute /
        .               // Lure). The engine rejects a declaration that leaves such an attacker
        .               // unblocked while an idle able blocker exists, so the bot must assign
        .               // one or it would deadlock the combat step. Pull any unused creature
        .               // that can legally block (respecting flying/reach) onto each
        .               // must-be-blocked attacker still missing a blocker.
    8,464 ( 0.00%)      for (a_id, _a_pow, _a_tough, a_flying, _a_dt) in &attacker_info {
    4,232 ( 0.00%)          let must_block = state
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == *a_id)
    8,464 ( 0.00%)              .is_some_and(|a| a.has_keyword(&Keyword::MustBeBlocked));
  329,720 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (4,232x)
   16,928 ( 0.00%)          if !must_block || assignments.iter().any(|(_, aid)| aid == a_id) {
        .                       continue;
        .                   }
        .                   // Pick the cheapest (lowest-power) legal idle blocker so a forced block
        .                   // doesn't throw away the bot's best body.
        .                   if let Some(idle) = state
        .                       .battlefield
        .                       .iter()
        .                       .filter(|c| {
-- line 7617 ----------------------------------------
-- line 7632 ----------------------------------------
        .               // CR 509.1b / 702.110b — Menace (≥2 blockers) and "can't be blocked
        .               // except by N or more creatures" (CantBeBlockedExceptByN — Pathrazer of
        .               // Ulamog) impose a minimum block count: an attacker so keyworded must be
        .               // blocked by 0 or ≥N, never 1..N-1. The greedy passes assign one at a
        .               // time, so an under-filled multi-block is illegal and the engine rejects
        .               // the whole declaration. For each such attacker, top the block up to the
        .               // minimum with legal idle blockers; if the minimum can't be reached,
        .               // drop every block on it (better unblocked than an illegal batch).
    8,464 ( 0.00%)      for (a_id, _a_pow, _a_tough, a_flying, _a_dt) in &attacker_info {
    4,232 ( 0.00%)          let min_blockers = state
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == *a_id)
        .                       .map(min_blockers_required)
        .                       .unwrap_or(1);
    8,464 ( 0.00%)          if min_blockers <= 1 {
        .                       continue;
        .                   }
        .                   let mut count = assignments.iter().filter(|(_, aid)| aid == a_id).count();
        .                   if count == 0 || count >= min_blockers {
        .                       continue;
        .                   }
        .                   while count < min_blockers {
        .                       // Cheapest legal idle blocker first — minimise value lost to the
-- line 7655 ----------------------------------------
-- line 7680 ----------------------------------------
        .                       }
        .                   }
        .               }
        .           
        .               // CR 509.1b — spend spare block capacity (Guardian of the Gateless and
        .               // friends). A blocker that can block extra attackers soaks additional
        .               // ones for free as long as the total damage it would take stays under its
        .               // toughness and no extra attacker has deathtouch.
   46,256 ( 0.00%)      let extra_capacity = |id: CardId| -> usize {
        .                   let Some(c) = state.battlefield_find(id) else { return 0 };
   34,692 ( 0.00%)          if c.has_keyword(&Keyword::CanBlockAnyNumber) {
  456,210 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (5,782x)
        .                       return usize::MAX;
        .                   }
        .                   state
   17,346 ( 0.00%)              .computed_permanent(id)
  959,740 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (5,782x)
    4,164 ( 0.00%)              .map(|cp| cp.keywords.to_vec())
        .                       .unwrap_or_default()
        .                       .iter()
    7,704 ( 0.00%)              .filter_map(|k| match k {
        .                           Keyword::CanBlockAdditional(n) => Some(*n as usize),
        .                           _ => None,
        .                       })
        .                       .sum()
   46,256 ( 0.00%)      };
        .               // Seed from every legal blocker with spare capacity, not just the ones the
        .               // scoring loop already assigned: a 0/N `CanBlockAnyNumber` wall kills
        .               // nothing and isn't needed against lethal, so it never gets picked up
        .               // there — but it can still soak the whole swing for free.
        .               let mut multi: Vec<CardId> = Vec::new();
        .               let seeds = assignments.iter().map(|(b, _)| *b).chain(
    2,368 ( 0.00%)          state
        .                       .battlefield
        .                       .iter()
  145,386 ( 0.00%)              .filter(|c| c.controller == seat && bot_can_block(c))
    7,052 ( 0.00%)              .map(|c| c.id),
        .               );
        .               for id in seeds {
   23,128 ( 0.00%)          if !multi.contains(&id) && extra_capacity(id) > 0 {
3,615,283 ( 0.12%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks_inner::{{closure}} (5,782x)
        .                       multi.push(id);
        .                   }
        .               }
    7,104 ( 0.00%)      for b_id in multi {
        .                   let Some(b) = state.battlefield_find(b_id) else { continue };
        .                   let (b_tough, b_flying, b_reach) = (
        .                       b.toughness(),
        .                       b.has_keyword(&Keyword::Flying),
        .                       b.has_keyword(&Keyword::Reach),
        .                   );
        .                   let mut taken: i32 = assignments
        .                       .iter()
-- line 7729 ----------------------------------------
-- line 7746 ----------------------------------------
        .                       {
        .                           continue;
        .                       }
        .                       assignments.push((b_id, *a_id));
        .                       taken += *a_pow;
        .                       spare = spare.saturating_sub(1);
        .                   }
        .               }
   11,840 ( 0.00%)      assignments
   21,312 ( 0.00%)  }
        .           
        .           /// [`min_blockers_required`] for a battlefield id (1 when it isn't there).
        .           fn min_blockers_required_by_id(state: &GameState, id: CardId) -> usize {
        .               state.battlefield_find(id).map(min_blockers_required).unwrap_or(1)
        .           }
        .           
        .           /// Minimum number of creatures legally required to block `attacker` (CR
        .           /// 509.1b): 2 for Menace, N for `CantBeBlockedExceptByN(N)`, the max of any
-- line 7763 ----------------------------------------
-- line 7767 ----------------------------------------
        .               use crate::card::Keyword;
        .               let mut min = 1usize;
        .               for kw in attacker
        .                   .definition
        .                   .keywords
        .                   .iter()
        .                   .chain(attacker.granted_keywords_eot.iter())
        .               {
   24,240 ( 0.00%)          match kw {
        .                       Keyword::Menace => min = min.max(2),
        .                       Keyword::CantBeBlockedExceptByN(n) => min = min.max(*n as usize),
        .                       _ => {}
        .                   }
        .               }
        .               min
        .           }
        .           
-- line 7783 ----------------------------------------
-- line 7820 ----------------------------------------
        .           /// Deliberately **optimistic**: it ignores the assignment problem (which
        .           /// source pays which pip), counts every color a choice-source could make,
        .           /// and rounds dynamic amounts down to one rather than giving up. That is
        .           /// the right bias, because this is only a pre-filter -- the authoritative
        .           /// gate on every candidate is still `would_accept_on`, which runs the
        .           /// engine's real auto-tap. An over-permissive estimate costs a few extra
        .           /// dry-run probes; an under-permissive one silently makes castable spells
        .           /// invisible to the bot, which is exactly the failure being fixed here.
  101,616 ( 0.00%)  fn available_mana(state: &GameState, seat: usize) -> AvailableMana {
        .               use crate::mana::{Color, ColorSet};
   12,702 ( 0.00%)      let pool = &state.players[seat].mana_pool;
        .               let mut out = AvailableMana {
        .                   total: pool.total(),
        .                   colors: ColorSet::empty(),
   25,404 ( 0.00%)          colorless: pool.colorless_amount() > 0,
        .               };
        .               for c in Color::ALL {
  127,020 ( 0.00%)          if pool.amount(c) > 0 {
        .                       out.colors.insert(c);
        .                   }
        .               }
        .               // One board-level grant scan for the whole sweep instead of one per
        .               // untapped permanent (see `GameState::grant_scan`).
   25,404 ( 0.00%)      let scan = state.grant_scan();
8,155,753 ( 0.26%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (12,702x)
  353,882 ( 0.01%)      for p in state.battlefield.iter().filter(|p| p.controller == seat && !p.tapped) {
        .                   // Printed abilities plus anything granted to it (Cryptolith Rite
        .                   // turning creatures into mana sources, Urza's Saga chapters), so a
        .                   // granted mana ability doesn't read as "no mana here".
  190,968 ( 0.01%)          let granted = state.granted_abilities_of(p, &scan);
7,951,708 ( 0.26%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of (63,656x)
        .                   let mut best = 0u32;
   51,354 ( 0.00%)          for a in p.definition.activated_abilities.iter().chain(granted.iter()) {
   52,092 ( 0.00%)              if !is_countable_mana_ability(a) {
        .                           continue;
        .                       }
  256,770 ( 0.01%)              let (amount, colors, colorless) = mana_ability_output(&a.effect);
        .                       best = best.max(amount);
        .                       out.colors = out.colors.union(colors);
  102,708 ( 0.00%)              out.colorless |= colorless;
        .                   }
   63,656 ( 0.00%)          out.total += best;
        .               }
        .               out
  190,530 ( 0.01%)  }
        .           
        .           /// A mana ability the bot is willing to count toward affordability: it
        .           /// costs a tap and nothing the bot would regret.
        .           ///
        .           /// We only need to know the mana *could* be paid, so color-choice sources
        .           /// (dual lands, Birds of Paradise) and painland-style life costs count --
        .           /// the engine's auto-tap will happily use them. Sources that consume a
        .           /// real resource to fire (sacrifice, discard, exile, energy) are excluded:
        .           /// counting them would have the bot commit to lines it can only pay for by
        .           /// spending something it would rather keep.
        .           fn is_countable_mana_ability(a: &ActivatedAbility) -> bool {
  208,368 ( 0.01%)      a.tap_cost
1,458,576 ( 0.05%)          && a.mana_cost.symbols.is_empty()
        .                   && !a.sac_cost
        .                   && a.sac_other_filter.is_none()
        .                   && a.bounce_other_filter.is_none()
        .                   && a.tap_other_filter.is_none()
        .                   && a.tap_n_filter.is_none()
        .                   && a.exile_other_filter.is_none()
  833,472 ( 0.03%)          && a.discard_cost.is_none()
        .                   && !a.exile_self_cost
        .                   && a.energy_cost == 0
        .                   && a.collect_evidence_cost.is_none()
  468,828 ( 0.02%)          && a.condition.is_none()
        .                   && !a.from_graveyard
        .                   && !a.from_hand
  156,276 ( 0.01%)          && matches!(a.effect, Effect::AddMana { .. })
        .           }
        .           
        .           /// `(most mana produced, colors it could be, produces true colorless)` for
        .           /// a mana ability's effect. Dynamic amounts (`{T}: add {G} equal to this
        .           /// creature's power`) count as one -- enough to keep the source visible
        .           /// without inventing a board state to measure it against.
        .           fn mana_ability_output(eff: &Effect) -> (u32, crate::mana::ColorSet, bool) {
        .               use crate::effect::Value;
        .               use crate::mana::{Color, ColorSet};
        .               let mut colors = ColorSet::empty();
  102,708 ( 0.00%)      accumulate_mana_colors(eff, &mut colors);
2,567,700 ( 0.08%)  => crabomination/src/server/bot.rs:crabomination::server::bot::accumulate_mana_colors (51,354x)
        .               let amount_of = |v: &Value| match v {
   51,354 ( 0.00%)          Value::Const(n) => (*n).max(0) as u32,
        .                   _ => 1,
        .               };
  102,708 ( 0.00%)      let Effect::AddMana { pool, .. } = eff else { return (0, colors, false) };
  770,310 ( 0.02%)      let (amount, colorless) = match pool {
        .                   ManaPayload::Colors(cs) => (cs.len() as u32, false),
        .                   ManaPayload::Colorless(v) => (amount_of(v), true),
        .                   ManaPayload::OfColor(_, v) | ManaPayload::OfColors(_, v) => (amount_of(v), false),
        .                   ManaPayload::AnyOneColor(v) | ManaPayload::AnyColors(v) => {
        .                       for c in Color::ALL {
        .                           colors.insert(c);
        .                       }
        .                       (amount_of(v), false)
-- line 7914 ----------------------------------------
-- line 7918 ----------------------------------------
        .                   // so assume the source is live for any color.
        .                   _ => {
        .                       for c in Color::ALL {
        .                           colors.insert(c);
        .                       }
        .                       (1, true)
        .                   }
        .               };
   51,354 ( 0.00%)      (amount, colors, colorless)
  102,708 ( 0.00%)  }
        .           
        .           /// State-aware affordability check: queries the engine for any
        .           /// per-spell tax that would apply (Damping Sphere etc.) and folds it
        .           /// into the cost before testing what `seat` can produce. Used by the bot to
        .           /// avoid submitting `CastSpell` actions that the engine will reject
        .           /// with a mana shortfall — repeated rejections are what deadlocked
        .           /// `debug/deadlock-t8-1777411577-473115700.json` (Damping Sphere on
        .           /// the board, bot casting its second spell of the turn).
  101,616 ( 0.00%)  pub fn can_afford_in_state(
        .               state: &GameState,
        .               seat: usize,
        .               card: &crate::card::CardInstance,
        .               w: &EvalWeights,
        .           ) -> bool {
   25,404 ( 0.00%)      let extra = state.extra_cost_for_card_in_hand(seat, card.id);
        .               // Fold in generic cost *reductions* (Affinity, CostReduction statics,
        .               // graveyard-affinity) the same way the real cast path does — otherwise the
        .               // bot overestimates the cost of e.g. Tolarian Terror with a full graveyard
        .               // and never casts it. Target-dependent reductions are skipped (no target
        .               // chosen yet), so this stays conservative.
        .               let reduction = crate::game::actions::cost_reduction_for_spell(state, seat, card, None);
        .               // Coloured surcharges (the Leech cycle) can't ride the generic `extra`
        .               // channel, so they join the printed cost before relaxation.
        .               let mut printed = card.definition.cost.clone();
        .               printed
        .                   .symbols
  101,616 ( 0.00%)          .extend(crate::game::actions::colored_spell_tax_for_spell(state, seat, card).symbols);
2,363,156 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::colored_spell_tax_for_spell (12,702x)
        .               // Mirror the payment funnel's Lattice relaxation so the bot doesn't
        .               // pass on a spell whose coloured pips any mana can now cover.
   38,106 ( 0.00%)      let cost = state.relax_cost_colors(&printed);
4,241,947 ( 0.14%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::relax_cost_colors (12,702x)
   25,404 ( 0.00%)      if w.legacy_pretap {
        .                   return can_afford_with_extra(&cost, &state.players[seat].mana_pool, extra, reduction);
        .               }
  114,318 ( 0.00%)      can_afford_from(&cost, &available_mana(state, seat), extra, reduction)
30,302,927 ( 0.97%)  => crabomination/src/server/bot.rs:crabomination::server::bot::available_mana (12,702x)
4,237,549 ( 0.14%)  => crabomination/src/server/bot.rs:crabomination::server::bot::can_afford_from (12,702x)
  114,318 ( 0.00%)  }
        .           
        .           /// Could `printed` be paid from `have`? Two independent tests: enough
        .           /// total mana for the (taxed, reduced) mana value, and a producible
        .           /// source for every coloured pip.
        .           ///
        .           /// Hybrid pips pass if *either* half is producible and Phyrexian pips
        .           /// always pass (life is a legal payment), matching what the real payment
        .           /// funnel will accept.
  139,722 ( 0.00%)  fn can_afford_from(
        .               printed: &ManaCost,
        .               have: &AvailableMana,
        .               extra_generic: u32,
        .               reduction: u32,
        .           ) -> bool {
        .               use crate::mana::ManaSymbol;
   38,106 ( 0.00%)      let mut cost = if printed.has_x() { printed.with_x_value(0) } else { printed.clone() };
  298,902 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (12,702x)
   25,404 ( 0.00%)      if reduction > 0 {
        .                   cost.reduce_generic(reduction);
        .               }
   50,808 ( 0.00%)      if cost.cmc() + extra_generic > have.total {
  363,098 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (12,702x)
        .                   return false;
        .               }
   54,912 ( 0.00%)      cost.symbols.iter().all(|s| match s {
        .                   ManaSymbol::Colored(c) => have.colors.contains(*c),
        .                   ManaSymbol::Hybrid(a, b) => have.colors.contains(*a) || have.colors.contains(*b),
        .                   // Phyrexian pips are payable with 2 life, so they never gate.
        .                   ManaSymbol::Phyrexian(_) | ManaSymbol::PhyrexianHybrid(_, _) => true,
        .                   ManaSymbol::Colorless(_) => have.colorless,
        .                   _ => true,
        .               })
  114,318 ( 0.00%)  }
        .           
        .           /// CR 702.21 — the tax `actor` would owe for aiming a spell or ability at
        .           /// `id`: the permanent's computed Ward cost when it is hostile and
        .           /// non-trivial, `None` when targeting it is tax-free. The engine's
        .           /// auto-targeter already *prefers* un-warded candidates
        .           /// (`auto_target_for_effect_avoiding_set_xc`); this helper exists for the
        .           /// fallback case where every candidate is warded and the bot has to judge
        .           /// whether the tax is survivable at all.
    9,184 ( 0.00%)  fn ward_tax(state: &GameState, id: CardId, actor: usize) -> Option<crate::card::WardCost> {
        .               use crate::card::Keyword;
        .               let c = state.battlefield_find(id)?;
    8,064 ( 0.00%)      if state.same_team(c.controller, actor) {
   14,324 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (1,008x)
        .                   return None;
        .               }
        .               state
      996 ( 0.00%)          .computed_permanent(id)
   41,776 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (332x)
      118 ( 0.00%)          .map(|cp| cp.keywords.to_vec())
        .                   .unwrap_or_else(|| c.definition.keywords.clone())
        .                   .iter()
      678 ( 0.00%)          .find_map(|k| match k {
        .                       Keyword::Ward(w) if !crate::game::actions::ward_cost_is_trivial(w) => Some(w.clone()),
      162 ( 0.00%)              _ => None,
        .                   })
   10,332 ( 0.00%)  }
        .           
        .           /// Whether the bot could actually pay `tax` on top of `besides` — the
        .           /// mana the cast or activation itself is about to consume. The engine
        .           /// auto-pays ward taxes when the trigger resolves (`try_pay_ward_cost`);
        .           /// a payment that fails there gets the bot's spell countered, which is
        .           /// strictly worse than never casting it, and a life payment the engine
        .           /// *can* make is still refused here when it would spend the bot's whole
        .           /// life total into the state-based loss. Variants with no cheap
-- line 8025 ----------------------------------------
-- line 8089 ----------------------------------------
        .           /// bot could not pay after the action's own mana cost. Such a candidate
        .           /// is a dead card, not an expensive one — the resolution path auto-pays
        .           /// or counters (see [`ward_tax_payable`]) — so it is dropped from the
        .           /// pool entirely rather than merely down-ranked. The printed cost stands
        .           /// in for alternative-cost casts (flashback, delve, …); that
        .           /// over-estimates what some casts consume, which errs toward holding a
        .           /// spell, never toward blanking one. Actions with no recognized target
        .           /// shape pass.
   33,056 ( 0.00%)  fn ward_gate_ok(state: &GameState, seat: usize, action: &GameAction) -> bool {
        .               let empty = ManaCost::new(vec![]);
   45,452 ( 0.00%)      let (mut cost, target, additional): (ManaCost, &Option<Target>, &[Target]) = match action {
        .                   GameAction::CastSpell { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellDelve { card_id, target, additional_targets, .. }
        .                   | GameAction::CastGift { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellSpree { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellConspire { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellKicked { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellKickers { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellMultikicked { card_id, target, additional_targets, .. }
-- line 8107 ----------------------------------------
-- line 8113 ----------------------------------------
        .                   | GameAction::CastAftermath { card_id, target, additional_targets, .. }
        .                   | GameAction::CastFlashback { card_id, target, additional_targets, .. }
        .                   | GameAction::CastMayhem { card_id, target, additional_targets, .. }
        .                   | GameAction::CastHarmonize { card_id, target, additional_targets, .. }
        .                   | GameAction::CastSpellAlternative { card_id, target, additional_targets, .. }
        .                   | GameAction::CastAdventureCreature { card_id, target, additional_targets, .. }
        .                   | GameAction::CastPlotted { card_id, target, additional_targets, .. } => {
        .                       let cost = state
   12,318 ( 0.00%)                  .find_card_anywhere(*card_id)
1,290,958 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (4,106x)
        .                           .map(|c| c.definition.cost.clone())
        .                           .unwrap_or(empty);
        .                       (cost, target, additional_targets.as_slice())
        .                   }
        .                   // Back-face casts pay the back's cost.
        .                   GameAction::CastSpellBack { card_id, target, additional_targets, .. }
        .                   | GameAction::CastDisturb { card_id, target, additional_targets, .. } => {
        .                       let cost = state
-- line 8129 ----------------------------------------
-- line 8135 ----------------------------------------
        .                   // Prepare-casts pay the inset spell's cost.
        .                   GameAction::CastPrepareSpell { creature_id, target, additional_targets, .. } => {
        .                       let cost = state
        .                           .battlefield_find(*creature_id)
        .                           .and_then(|c| c.definition.prepare_spell.as_deref().map(|s| s.cost.clone()))
        .                           .unwrap_or(empty);
        .                       (cost, target, additional_targets.as_slice())
        .                   }
       26 ( 0.00%)          GameAction::ActivateAbility { card_id, ability_index, target, additional_targets, .. } => {
        .                       // Granted abilities index past the printed list; missing costs
        .                       // fall back to free, which errs permissive — the gate still
        .                       // sees the tax itself.
        .                       let cost = state
       26 ( 0.00%)                  .battlefield_find(*card_id)
        .                           .and_then(|c| c.definition.activated_abilities.get(*ability_index))
        .                           .map(|a| a.mana_cost.clone())
        .                           .unwrap_or(empty);
        .                       (cost, target, additional_targets.as_slice())
       26 ( 0.00%)          }
        .                   GameAction::ActivateLoyaltyAbility { target, .. } => (empty, target, &[]),
        .                   _ => return true,
        .               };
        .               // The one candidate shape that sinks *extra* mana into the cast: a
        .               // plain CastSpell with a chosen X (`max_affordable_x` dumps the whole
        .               // spare pool into it). Price the X into the gate, or a max-X spell
        .               // aimed at a warded target taps the bot out of the tax it then owes.
   20,530 ( 0.00%)      if let GameAction::CastSpell { x_value: Some(x), .. } = action {
        .                   cost.symbols.push(crate::mana::ManaSymbol::Generic(*x));
        .               }
    2,376 ( 0.00%)      target.iter().chain(additional.iter()).all(|t| match t {
    6,060 ( 0.00%)          Target::Permanent(id) => match ward_tax(state, *id, seat) {
  175,984 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::ward_tax (1,010x)
        .                       Some(tax) => ward_tax_payable(state, seat, &tax, &cost),
        .                       None => true,
        .                   },
        .                   _ => true,
        .               })
   33,056 ( 0.00%)  }
        .           
        .           /// For an X-cost spell (or a spell whose effect reads
        .           /// `Value::XFromCost`), return the largest non-negative X the caster can
        .           /// pay given their current mana pool — leftover generic mana after the
        .           /// fixed (non-X) portion of the cost is what fuels X. Static cost taxes
        .           /// (Damping Sphere etc.) are folded in via
        .           /// `extra_cost_for_card_in_hand`. Returns 0 when the fixed cost itself
        .           /// is more than the available pool — the caller relies on `would_accept`
-- line 8179 ----------------------------------------
-- line 8266 ----------------------------------------
        .                   .max()
        .           }
        .           
        .           /// True if X matters for this spell — either the cost has an `{X}` pip
        .           /// or the effect tree mentions `Value::XFromCost`. The latter catches
        .           /// catalog cards (Banefire, Mind Twist, …) whose costs predate the
        .           /// engine's proper X-pip wiring.
        .           pub fn x_relevant(def: &CardDefinition) -> bool {
   37,716 ( 0.00%)      def.cost.has_x() || effect_uses_x(&def.effect)
  207,894 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::effect_uses_x (6,286x)
  144,278 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (6,286x)
        .           }
        .           
   39,948 ( 0.00%)  fn effect_uses_x(eff: &Effect) -> bool {
        .               use crate::effect::Value;
    4,040 ( 0.00%)      fn value_uses_x(v: &Value) -> bool {
    8,888 ( 0.00%)          match v {
        .                       Value::XFromCost => true,
        .                       Value::Sum(parts) => parts.iter().any(value_uses_x),
        .                       Value::Diff(a, b)
        .                       | Value::Times(a, b)
        .                       | Value::Min(a, b)
        .                       | Value::Max(a, b) => value_uses_x(a) || value_uses_x(b),
        .                       Value::NonNeg(inner) => value_uses_x(inner),
        .                       Value::CountOf(_) | Value::PowerOf(_) | Value::ToughnessOf(_)
        .                       | Value::CountersOn { .. } | Value::ManaValueOf(_)
        .                       | Value::DistinctTypesInTopOfLibrary { .. }
        .                       | Value::DistinctTypesInGraveyard { .. } => false,
        .                       _ => false,
        .                   }
    4,040 ( 0.00%)      }
        .               fn predicate_uses_x(p: &crate::effect::Predicate) -> bool {
        .                   use crate::effect::Predicate as P;
        .                   match p {
        .                       P::ValueAtLeast(a, b) | P::ValueAtMost(a, b) | P::ValueEquals(a, b) => {
        .                           value_uses_x(a) || value_uses_x(b)
        .                       }
        .                       P::Not(inner) => predicate_uses_x(inner),
        .                       P::All(parts) | P::Any(parts) => parts.iter().any(predicate_uses_x),
        .                       P::SelectorCountAtLeast { n, .. } => value_uses_x(n),
        .                       _ => false,
        .                   }
        .               }
   84,566 ( 0.00%)      match eff {
        .                   Effect::Seq(steps) => steps.iter().any(effect_uses_x),
        .                   Effect::If { cond, then, else_ } => {
        .                       predicate_uses_x(cond) || effect_uses_x(then) || effect_uses_x(else_)
        .                   }
        .                   Effect::ChooseMode(modes) => modes.iter().any(effect_uses_x),
        .                   Effect::ForEach { body, .. }
        .                   | Effect::Repeat { body, .. }
        .                   | Effect::DelayUntil { body, .. } => effect_uses_x(body),
-- line 8315 ----------------------------------------
-- line 8337 ----------------------------------------
        .                   | Effect::CreateTokenCopiesHasteSac { count, .. }
        .                   | Effect::CopySpell { count, .. }
        .                   | Effect::CopySpellWithRiders { count, .. }
        .                   | Effect::CopySpellMayChooseTargets { count, .. } => value_uses_x(count),
        .                   Effect::RevealUntilFind { cap, .. } => value_uses_x(cap),
        .                   Effect::AddFirstSpellTax { count, .. } => value_uses_x(count),
        .                   _ => false,
        .               }
   29,250 ( 0.00%)  }
        .           
        .           /// If `eff` is (or wraps via `Seq`) a top-level `ChooseMode`, return the
        .           /// number of modes. Otherwise `None`. The bot uses this to enumerate each
        .           /// mode separately when generating castable actions, so a card whose
        .           /// default mode (mode 0) is dead in the current board state (e.g. Drown
        .           /// in the Loch's "counter target spell" with no opp spell on the stack)
        .           /// still surfaces a viable alternate (mode 1: destroy creature).
        .           fn modal_mode_count(eff: &Effect) -> Option<usize> {
   53,264 ( 0.00%)      match eff {
        .                   Effect::ChooseMode(modes) => Some(modes.len()),
        .                   // Cast-time multi-mode spells (Choreographed Sparks, Moment of
        .                   // Reckoning): the bot casts them single-mode via the plain
        .                   // `CastSpell { mode }` back-compat path.
        .                   Effect::ChooseModesCast { modes, .. } | Effect::ChooseModesByPoints { modes, .. } => {
        .                       Some(modes.len())
        .                   }
        .                   Effect::Seq(steps) => steps.iter().find_map(modal_mode_count),
-- line 8362 ----------------------------------------
-- line 8364 ----------------------------------------
        .               }
        .           }
        .           
        .           /// Resolve the effect branch for a chosen mode. For non-modal effects
        .           /// (or `mode == None`), returns the original effect. For modal effects,
        .           /// returns the chosen mode's body so the auto-target heuristic uses the
        .           /// correct filter for that mode.
        .           fn mode_branch(eff: &Effect, mode: Option<usize>) -> &Effect {
   36,380 ( 0.00%)      match (eff, mode) {
        .                   (Effect::ChooseMode(modes), Some(m)) if m < modes.len() => &modes[m],
        .                   (Effect::ChooseModesCast { modes, .. } | Effect::ChooseModesByPoints { modes, .. }, Some(m))
        .                       if m < modes.len() =>
        .                   {
        .                       &modes[m]
        .                   }
        .                   (Effect::Seq(steps), Some(_)) => steps
        .                       .iter()
-- line 8380 ----------------------------------------
-- line 8408 ----------------------------------------
        .           }
        .           
        .           /// True when `ta` is the canonical Strixhaven magecraft trigger:
        .           /// SpellCast scope=YourControl with the IS-only predicate. Used by
        .           /// the bot's spell-bias heuristic so a controlled magecraft permanent
        .           /// nudges the bot toward casting an IS spell to fire the trigger.
        .           fn is_magecraft_trigger(ta: &crate::card::TriggeredAbility) -> bool {
        .               use crate::card::{EventKind, EventScope};
      552 ( 0.00%)      matches!(ta.event.kind, EventKind::SpellCast)
        .                   && matches!(ta.event.scope, EventScope::YourControl)
        .                   && ta.event.filter.is_some()
        .           }
        .           
        .           /// True when `ta` is an Opus-style rider (SOS): an on-cast trigger whose
        .           /// body branches on `Predicate::CastSpellManaSpentAtLeast` — "if five or
        .           /// more mana was spent to cast that spell, [big] instead". See
        .           /// `shortcut::opus_trigger`.
        .           fn is_opus_trigger(ta: &crate::card::TriggeredAbility) -> bool {
        .               use crate::card::EventKind;
      324 ( 0.00%)      fn branches(e: &Effect) -> bool {
      180 ( 0.00%)          match e {
        .                       Effect::If { cond, then, else_ } => {
        .                           matches!(cond, crate::effect::Predicate::CastSpellManaSpentAtLeast(_))
        .                               || branches(then)
        .                               || branches(else_)
        .                       }
        .                       Effect::Seq(v) => v.iter().any(branches),
        .                       Effect::MayDo { body, .. } | Effect::ForEach { body, .. } => branches(body),
        .                       _ => false,
        .                   }
      252 ( 0.00%)      }
      552 ( 0.00%)      matches!(ta.event.kind, EventKind::SpellCast) && branches(&ta.effect)
      756 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::is_opus_trigger::branches (36x)
        .           }
        .           
        .           /// True when `ta` is an Increment rider (SOS): an on-cast trigger gated
        .           /// on `Predicate::IncrementSatisfied` — "if the amount of mana spent is
        .           /// greater than this creature's power or toughness, put a +1/+1 counter
        .           /// on it". See `shortcut::increment_trigger`.
        .           fn is_increment_trigger(ta: &crate::card::TriggeredAbility) -> bool {
        .               use crate::card::EventKind;
      216 ( 0.00%)      fn branches(e: &Effect) -> bool {
      180 ( 0.00%)          match e {
        .                       Effect::If { cond, then, else_ } => {
        .                           matches!(cond, crate::effect::Predicate::IncrementSatisfied)
        .                               || branches(then)
        .                               || branches(else_)
        .                       }
        .                       Effect::Seq(v) => v.iter().any(branches),
        .                       Effect::MayDo { body, .. } | Effect::ForEach { body, .. } => branches(body),
        .                       _ => false,
        .                   }
      180 ( 0.00%)      }
      552 ( 0.00%)      matches!(ta.event.kind, EventKind::SpellCast) && branches(&ta.effect)
      576 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::is_increment_trigger::branches (36x)
        .           }
        .           
        .           /// The smallest mana-spent total that grows at least one of the bot's
        .           /// Increment bodies: `min(power, toughness) + 1` over them (the gate is
        .           /// "spent > power OR toughness", so clearing the smaller stat suffices).
        .           /// `None` with no Increment body out. Computed stats, so the threshold
        .           /// climbs as counters land — exactly the printed escalation.
        .           fn increment_threshold(state: &GameState, seat: usize) -> Option<u32> {
        .               state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| {
   41,028 ( 0.00%)              c.controller == seat
        .                           && c.definition.triggered_abilities.iter().any(is_increment_trigger)
        .                   })
        .                   .filter_map(|c| state.computed_permanent(c.id))
        .                   .map(|cp| (cp.power.min(cp.toughness).max(0) + 1) as u32)
        .                   .min()
        .           }
        .           
        .           /// True when `ta` is a Repartee trigger (SOS): an on-cast event filter
        .           /// that requires the spell to target a creature. See `shortcut::repartee`.
        .           fn is_repartee_trigger(ta: &crate::card::TriggeredAbility) -> bool {
        .               use crate::card::EventKind;
        .               use crate::effect::Predicate;
        .               fn wants_creature_target(p: &Predicate) -> bool {
    1,392 ( 0.00%)          match p {
        .                       Predicate::CastSpellTargetsMatch(_) => true,
        .                       Predicate::All(v) => v.iter().any(wants_creature_target),
        .                       _ => false,
        .                   }
        .               }
    4,372 ( 0.00%)      matches!(ta.event.kind, EventKind::SpellCast)
        .                   && ta.event.filter.as_ref().is_some_and(wants_creature_target)
        .           }
        .           
        .           /// True when `eff` carries a this-turn-lifegain gate (SOS Infusion) —
        .           /// the shape whose payoff a pre-gain cast wastes.
   35,420 ( 0.00%)  fn effect_infusion_gated(eff: &Effect) -> bool {
        .               use crate::effect::Predicate;
        .               fn gated(p: &Predicate) -> bool {
        .                   match p {
        .                       Predicate::LifeGainedThisTurnAtLeast { .. }
        .                       | Predicate::FirstLifeGainThisTurn { .. } => true,
        .                       Predicate::All(v) => v.iter().any(gated),
        .                       _ => false,
        .                   }
        .               }
   16,298 ( 0.00%)      match eff {
        .                   Effect::If { cond, then, else_ } => {
        .                       gated(cond) || effect_infusion_gated(then) || effect_infusion_gated(else_)
        .                   }
        .                   Effect::Seq(v) => v.iter().any(effect_infusion_gated),
        .                   Effect::MayDo { body, .. } | Effect::ForEach { body, .. } => effect_infusion_gated(body),
        .                   _ => false,
        .               }
   28,980 ( 0.00%)  }
        .           
        .           /// Whether any face of `def` is Infusion-gated — spell body or a
        .           /// triggered rider (the ETB Infusion shape).
        .           fn card_infusion_gated(def: &CardDefinition) -> bool {
   11,888 ( 0.00%)      effect_infusion_gated(&def.effect)
   79,514 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::effect_infusion_gated (2,972x)
      464 ( 0.00%)          || def.triggered_abilities.iter().any(|t| effect_infusion_gated(&t.effect))
    2,900 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::effect_infusion_gated (116x)
        .           }
        .           
        .           /// Mana the bot would spend casting `a`: printed cost plus the chosen X.
        .           /// Only the plain-cast shape is priced — it is the one that carries a
        .           /// live `x_value` — which is all the Opus nudge needs.
        .           fn cast_mana_spent(state: &GameState, seat: usize, a: &GameAction) -> u32 {
        .               match a {
        .                   GameAction::CastSpell { card_id, x_value, .. } => state.players[seat]
-- line 8531 ----------------------------------------
-- line 8576 ----------------------------------------
        .               w: &EvalWeights,
        .           ) -> Option<Target> {
        .               let filter = eff.primary_target_filter();
        .               let mut foes: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| !state.same_team(c.controller, seat) && c.definition.is_creature())
        .                   .collect();
    4,808 ( 0.00%)      foes.sort_by_key(|c| std::cmp::Reverse(permanent_value(state, c.id, w)));
2,403,034 ( 0.08%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (2,084x)
        .               foes.into_iter().map(|c| Target::Permanent(c.id)).find(|t| match &filter {
        .                   Some(f) => state.evaluate_requirement_static(f, t, seat, None),
        .                   None => true,
        .               })
        .           }
        .           
        .           /// True when casting `def` reads the converge count — the distinct colors
        .           /// of mana spent — anywhere the bot can see it: an effect `Value`
        .           /// (`ConvergedValue`), a `ManaValueAtMostConverged` target filter
        .           /// (Sundering Archaic), or an enters-with-counters amount. The `Value`
        .           /// walk mirrors [`effect_uses_x`]'s variant coverage.
        .           fn card_reads_converge(def: &CardDefinition) -> bool {
        .               use crate::effect::Value;
      470 ( 0.00%)      fn value_is_converge(v: &Value) -> bool {
    1,034 ( 0.00%)          match v {
        .                       Value::ConvergedValue => true,
        .                       Value::Sum(parts) => parts.iter().any(value_is_converge),
        .                       Value::Diff(a, b) | Value::Times(a, b) | Value::Min(a, b) | Value::Max(a, b) => {
        .                           value_is_converge(a) || value_is_converge(b)
        .                       }
        .                       Value::NonNeg(inner) => value_is_converge(inner),
        .                       _ => false,
        .                   }
      470 ( 0.00%)      }
      504 ( 0.00%)      fn req_converge(r: &crate::card::SelectionRequirement) -> bool {
        .                   use crate::card::SelectionRequirement as R;
    1,122 ( 0.00%)          match r {
        .                       R::ManaValueAtMostConverged => true,
      288 ( 0.00%)              R::And(a, b) | R::Or(a, b) => req_converge(a) || req_converge(b),
      864 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::card_reads_converge::req_converge'2 (48x)
        .                       _ => false,
        .                   }
      630 ( 0.00%)      }
    3,752 ( 0.00%)      fn walk(eff: &Effect) -> bool {
    5,026 ( 0.00%)          match eff {
        .                       Effect::Seq(steps) => steps.iter().any(walk),
        .                       Effect::If { then, else_, .. } => walk(then) || walk(else_),
        .                       Effect::ChooseMode(modes) => modes.iter().any(walk),
        .                       Effect::ForEach { body, .. }
        .                       | Effect::Repeat { body, .. }
        .                       | Effect::DelayUntil { body, .. }
        .                       | Effect::MayDo { body, .. } => walk(body),
        .                       Effect::DealDamage { amount, .. }
-- line 8626 ----------------------------------------
-- line 8637 ----------------------------------------
        .                       | Effect::AddPoison { amount, .. }
        .                       | Effect::Discard { amount, .. } => value_is_converge(amount),
        .                       Effect::PumpPT { power, toughness, .. } => {
        .                           value_is_converge(power) || value_is_converge(toughness)
        .                       }
        .                       Effect::CreateToken { count, .. } => value_is_converge(count),
        .                       _ => false,
        .                   }
    2,210 ( 0.00%)      }
    2,500 ( 0.00%)      walk(&def.effect)
   14,146 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::card_reads_converge::walk (500x)
    1,156 ( 0.00%)          || def.effect.primary_target_filter().is_some_and(req_converge)
   17,276 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::primary_target_filter (500x)
        .                   || def.enters_with_counters.as_ref().is_some_and(|(_, v)| value_is_converge(v))
        .           }
        .           
        .           /// SOS Converge pre-float: when the bot's chosen play scales with the
        .           /// distinct colors of mana spent, tap one plain source of a color the
        .           /// pool doesn't hold yet and cast NEXT tick — the payment funnel spends
        .           /// pool mana first, so every floated color is a drained (counted) color
        .           /// when the cast goes off. Bounded on every side: only fires while the
-- line 8655 ----------------------------------------
-- line 8659 ----------------------------------------
        .           /// firing adds a color the pool lacked, so at most four taps precede the
        .           /// cast.
        .           fn pick_converge_prefloat(
        .               state: &GameState,
        .               seat: usize,
        .               action: &GameAction,
        .           ) -> Option<GameAction> {
        .               use crate::mana::Color;
    1,500 ( 0.00%)      let def: &CardDefinition = match action {
        .                   GameAction::CastSpell { card_id, .. } => {
    1,000 ( 0.00%)              &state.players[seat].hand.iter().find(|c| c.id == *card_id)?.definition
        .                   }
        .                   GameAction::CastPrepareSpell { creature_id, .. } => {
        .                       state.battlefield_find(*creature_id)?.definition.prepare_spell.as_deref()?
        .                   }
        .                   _ => return None,
        .               };
        .               if !card_reads_converge(def) {
        .                   return None;
-- line 8677 ----------------------------------------
-- line 8731 ----------------------------------------
        .           /// granted keyword), pick the bot's most valuable creature that satisfies
        .           /// the enchant filter as the host. Returns `None` for non-Auras and for
        .           /// debuff Auras (negative stats — Pacifism-style restrictions live in
        .           /// other def fields and keep the hostile auto-target walk). Without this,
        .           /// `Effect::Attach` falls into the auto-targeter's hostile branch and a
        .           /// Rancor prefers the opponent's creatures.
        .           fn is_beneficial_aura(def: &CardDefinition) -> bool {
        .               use crate::card::EnchantmentSubtype;
    1,212 ( 0.00%)      if !def.subtypes.enchantment_subtypes.contains(&EnchantmentSubtype::Aura) {
        .                   return false;
        .               }
        .               def.equipped_bonus.as_ref().is_some_and(|bonus| {
    4,848 ( 0.00%)          bonus.power + bonus.toughness > 0
      916 ( 0.00%)              || (bonus.power + bonus.toughness == 0 && !bonus.keywords.is_empty())
        .               })
        .           }
        .           
    6,060 ( 0.00%)  fn beneficial_aura_host(
        .               state: &GameState,
        .               seat: usize,
        .               aura: &crate::card::CardInstance,
        .               w: &EvalWeights,
        .           ) -> Option<crate::game::Target> {
        .               let def = &aura.definition;
      458 ( 0.00%)      if !is_beneficial_aura(def) {
        .                   return None;
        .               }
    2,424 ( 0.00%)      let filter = def.effect.primary_target_filter();
   32,118 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::primary_target_filter (606x)
      606 ( 0.00%)      let mut hosts: Vec<&crate::card::CardInstance> = state
        .                   .battlefield
        .                   .iter()
        .                   .filter(|c| c.controller == seat && c.definition.is_creature())
        .                   .collect();
        .               hosts.sort_by_key(|c| std::cmp::Reverse(permanent_value(state, c.id, w)));
      606 ( 0.00%)      hosts
        .                   .into_iter()
      452 ( 0.00%)          .map(|c| crate::game::Target::Permanent(c.id))
        .                   .find(|t| match &filter {
    3,164 ( 0.00%)              Some(f) => state.evaluate_requirement_static(f, t, seat, Some(aura.id)),
  549,590 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (452x)
        .                       None => true,
        .                   })
    5,454 ( 0.00%)  }
        .           
        .           /// Best cutoff for "choose a number; destroy all creatures with power ≥
        .           /// it": maximize destroyed enemy value minus destroyed own value,
        .           /// breaking ties upward (spare more of everyone's board when equal).
        .           fn best_destroy_power_cutoff(state: &GameState, seat: usize, max: u32, w: &EvalWeights) -> u32 {
        .               let mut best = (i32::MIN, 0u32);
        .               for n in 0..=max {
        .                   let mut score = 0i32;
-- line 8780 ----------------------------------------
-- line 8817 ----------------------------------------
        .           }
        .           
        .           /// First damage amount a spell's effect tree deals (walking `Seq`), with
        .           /// `{X}` resolved to the candidate's chosen X. `None` when the effect deals
        .           /// no (statically knowable) damage — non-Const amounts are treated as
        .           /// unknown rather than guessed.
        .           fn first_damage_amount(effect: &Effect, x: u32) -> Option<i32> {
        .               use crate::effect::Value;
   23,564 ( 0.00%)      match effect {
      760 ( 0.00%)          Effect::DealDamage { amount, .. } => match amount {
      304 ( 0.00%)              Value::Const(n) => Some(*n),
        .                       Value::XFromCost => Some(x as i32),
        .                       _ => None,
        .                   },
    2,976 ( 0.00%)          Effect::Seq(steps) => steps.iter().find_map(|e| first_damage_amount(e, x)),
        .                   _ => None,
        .               }
        .           }
        .           
        .           /// Heuristic rank for one candidate play. Rough scale:
        .           ///
        .           /// * mana investment counts double (printed cmc + chosen X + kick count) —
        .           ///   the bot leads with its biggest affordable play and spends its pool;
-- line 8839 ----------------------------------------
-- line 8861 ----------------------------------------
        .               const FULL_UNTIL: f32 = 5.0;
        .               const ZERO_AT: f32 = 12.0;
        .               ((ZERO_AT - turn as f32) / (ZERO_AT - FULL_UNTIL)).clamp(0.0, 1.0)
        .           }
        .           
        .           fn eval_material(state: &GameState, seat: usize, w: &EvalWeights) -> i32 {
        .               // Scores a whole board, so it reads every permanent's computed state —
        .               // and the sims call it on a cloned (unfrozen) state once per candidate.
   22,824 ( 0.00%)      state.with_frozen_layers(|state| eval_material_frozen(state, seat, w))
1,775,357 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (224x)
        .           }
        .           
   23,330 ( 0.00%)  fn eval_material_frozen(state: &GameState, seat: usize, w: &EvalWeights) -> i32 {
        .               // The learned value net, when a profile asks for it and a net is
        .               // loaded. Undecided positions only: the heuristic's ±100 000·unit for
        .               // a decided game must keep dominating the net's 0..10 000 range so
        .               // "actually winning" always outranks "the net likes it".
   13,998 ( 0.00%)      if w.net_slot != 0
        .                   && state.game_over.is_none()
        .                   && let Some(p) = super::net_eval::win_prob(state, seat, w.net_slot)
        .               {
        .                   // Snap onto the grid *before* anything downstream sees it, so
        .                   // both the replacement and blend paths inherit the tie.
        .                   let p = if w.net_quantize > 0 {
        .                       let q = w.net_quantize as f32;
        .                       (p * q).round() / q
-- line 8885 ----------------------------------------
-- line 8890 ----------------------------------------
        .                       let mut bias = ((p - 0.5) * (w.net_blend_scale * w.unit) as f32) as i32;
        .                       if w.net_blend_ply {
        .                           bias = (bias as f32 * ply_blend_factor(state.turn_number)) as i32;
        .                       }
        .                       return eval_material_inner(state, seat, w, false) + bias;
        .                   }
        .                   return (p * 10_000.0) as i32;
        .               }
   32,662 ( 0.00%)      eval_material_inner(state, seat, w, false)
22,202,581 ( 0.71%)  => crabomination/src/server/bot.rs:crabomination::server::bot::eval_material_inner (4,666x)
        .           }
        .           
        .           /// [`eval_material`] with `seat`'s own summoning-sick creatures counted as
        .           /// worth nothing.
        .           ///
        .           /// Forge's `GameStateEvaluator` carries this alongside the real score as
        .           /// `summonSickValue`, and uses it to answer one question: does this line
        .           /// achieve anything *this turn*, or does it only add a body that can't
        .           /// attack yet? A creature deployed in the precombat main and a creature
        .           /// deployed after combat are worth the same at end of turn, but the second
        .           /// one was played with a turn's more information and left the mana up in
        .           /// between. Only the first reads as progress to a greedy evaluator, which
        .           /// is why this bot puts 95 % of its plays in the precombat main.
        .           fn eval_material_summon_sick_blind(state: &GameState, seat: usize, w: &EvalWeights) -> i32 {
    3,262 ( 0.00%)      state.with_frozen_layers(|state| eval_material_inner(state, seat, w, true))
6,789,485 ( 0.22%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::with_frozen_layers'2 (932x)
        .           }
        .           
   50,382 ( 0.00%)  fn eval_material_inner(
        .               state: &GameState,
        .               seat: usize,
        .               w: &EvalWeights,
        .               blind_to_sick: bool,
        .           ) -> i32 {
   27,990 ( 0.00%)      if let Some(over) = state.game_over {
        .                   return match over {
      548 ( 0.00%)              Some(winner) if winner == seat => 100_000 * w.unit,
       78 ( 0.00%)              Some(_) => -100_000 * w.unit,
        .                       None => 0,
        .                   };
        .               }
        .               let mut v = 0i32;
        .               for c in &state.battlefield {
        .                   // Lands are worth a small flat amount — enough that ramp/fetch
        .                   // registers and land destruction isn't free, without a flooded
        .                   // board dominating the material count.
  335,336 ( 0.01%)          let pv = if c.definition.is_land() {
1,139,684 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (83,834x)
        .                       2 * w.unit
        .                   } else {
   83,070 ( 0.00%)              let mut pv = permanent_value(state, c.id, w);
21,839,917 ( 0.70%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (16,614x)
        .                       // Loyalty is a spendable RESOURCE, not material: counting it
        .                       // here made every plus ability self-rewarding (+2 loyalty
        .                       // read as +6 material for free) and every ultimate
        .                       // self-punishing (−6 loyalty read as −18), so walkers ticked
        .                       // up forever. `permanent_value` keeps the loyalty term for
        .                       // removal targeting — a fat walker is still the best target.
   49,842 ( 0.00%)              if c.definition.is_planeswalker() {
  265,824 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (16,614x)
        .                           pv -= c.counter_count(crate::card::CounterType::Loyalty) as i32 * w.unit;
        .                       }
   16,614 ( 0.00%)              3 * pv
        .                   };
        .                   // A body that can't attack yet isn't this turn's progress -- see
        .                   // `eval_material_summon_sick_blind`.
        .                   let sick = blind_to_sick
   26,856 ( 0.00%)              && c.controller == seat
   20,394 ( 0.00%)              && c.definition.is_creature()
  105,228 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (6,798x)
    2,360 ( 0.00%)              && c.summoning_sick
      960 ( 0.00%)              && !c.has_keyword(&crate::card::Keyword::Haste);
   23,814 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (320x)
      960 ( 0.00%)          let pv = if sick { 0 } else { pv };
  328,538 ( 0.01%)          if c.controller == seat {
   42,492 ( 0.00%)              v += pv;
  206,710 ( 0.01%)          } else if !state.same_team(c.controller, seat) {
1,446,970 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (41,342x)
        .                       v -= pv;
        .                   }
        .               }
   16,266 ( 0.00%)      for (i, p) in state.players.iter().enumerate() {
   54,220 ( 0.00%)          if !p.is_alive() {
        .                       continue;
        .                   }
        .                   // A hand card at 4 ≈ half an average permanent — enough that
        .                   // "draw a card" beats "gain 3 life" (a card is a future play;
        .                   // three life at a healthy total is nearly nothing).
        .                   let emblems: i32 = p.emblems.iter().map(|e| emblem_value(state, i, e)).sum();
        .                   // CR 725 / 726 — the crown and the initiative are recurring resources,
        .                   // not one-shots: the monarch draws at each of their end steps and the
        .                   // initiative-holder ventures on top of that. Priced above a single
        .                   // hand card (4) because they keep paying until someone takes them.
        .                   let crown = i32::from(state.monarch == Some(i)) * 7
        .                       + i32::from(state.initiative == Some(i)) * 9;
   54,220 ( 0.00%)          let material = (4 * p.hand.len() as i32 + emblems + crown) * w.unit
   32,532 ( 0.00%)              + life_value(state.effective_life(i), w);
  482,558 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (10,844x)
   32,532 ( 0.00%)          if i == seat {
    5,422 ( 0.00%)              v += material;
   48,798 ( 0.00%)          } else if !state.same_team(i, seat) {
  189,770 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (5,422x)
        .                       v -= material;
        .                   }
        .               }
        .               v
   50,382 ( 0.00%)  }
        .           
        .           /// Material value of one emblem for `seat`. Emblems are ultimates and
        .           /// usually game-bending — but a CONDITIONAL emblem is only worth what
        .           /// the deck can feed it. A lifegain-triggered emblem (Professor Dellian
        .           /// Fel's "whenever you gain life, target opponent loses that much") is
        .           /// priced by the seat's visible lifegain sources: with none it's nearly
        .           /// dead (2 — below a +2 ability's gain-3, so the walker holds the fort
        .           /// instead of ulting into nothing), and each source adds 6, capped at
-- line 8994 ----------------------------------------
-- line 9094 ----------------------------------------
        .           ///
        .           /// Previously a `bool`, which conflated "there was no combat to look at"
        .           /// with "the simulation ran out of fuel partway". Callers treat those
        .           /// oppositely — the first means score the state as-is, the second means
        .           /// refuse to score a board where attackers are tapped but damage was never
        .           /// dealt — and collapsing them made every evaluation on a board with no
        .           /// possible attackers unscoreable, silently dropping the whole position
        .           /// back to the static rank.
      466 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq)]
        .           enum CombatSim {
        .               /// Nothing to simulate; `g` is untouched.
        .               Skipped,
        .               /// Ran through combat damage.
        .               Completed,
        .               /// Started and could not finish; `g` is now a torn state.
        .               Incomplete,
        .           }
        .           
        .           /// Bails cheaply — without touching `g` — when there is no combat to look
        .           /// at: the game is over, the turn is already past combat damage, or the
        .           /// active player has no creature that could attack. Forge guards the same
        .           /// way, because the state copy is the expensive part.
        .           fn simulate_through_combat(g: &mut GameState, fuel: &mut u32, w: &EvalWeights) -> CombatSim {
    3,566 ( 0.00%)      if g.is_game_over() || g.step >= TurnStep::CombatDamage {
        .                   return CombatSim::Skipped;
        .               }
        .               let attacker_seat = g.active_player_idx;
    1,408 ( 0.00%)      let could_attack = g.battlefield.iter().any(|c| {
   26,456 ( 0.00%)          c.controller == attacker_seat
   20,700 ( 0.00%)              && c.definition.is_creature()
  107,046 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (6,900x)
    2,236 ( 0.00%)              && !c.tapped
    2,966 ( 0.00%)              && (!c.summoning_sick || c.has_keyword(&crate::card::Keyword::Haste))
   18,664 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (254x)
        .               });
        .               if !could_attack {
        .                   return CombatSim::Skipped;
        .               }
    2,646 ( 0.00%)      let turn = g.turn_number;
        .               let mut attacks_submitted = false;
        .               let mut blocks_submitted = false;
   38,672 ( 0.00%)      while g.step < TurnStep::CombatDamage && g.turn_number == turn && !g.is_game_over() {
    8,434 ( 0.00%)          *fuel = match fuel.checked_sub(1) {
        .                       Some(f) => f,
        .                       None => return CombatSim::Incomplete,
        .                   };
    8,434 ( 0.00%)          if g.pending_decision.is_some() {
        .                       let answer = {
        .                           let pending = g.pending_decision.as_ref().unwrap();
        .                           decide_pending_policy(g, pending.acting_player(), w, &pending.decision, false)
        .                       };
        .                       // Not `dry_run`: `combat_aware`'s `before` probe scores this
        .                       // state even when the walk comes back `Incomplete`, so a
        .                       // rejected action here must still be rolled back.
        .                       if g.perform_action(GameAction::SubmitDecision(answer)).is_err() {
        .                           return CombatSim::Incomplete;
        .                       }
        .                       continue;
        .                   }
   33,704 ( 0.00%)          let action = match g.step {
    5,644 ( 0.00%)              TurnStep::DeclareAttackers if !attacks_submitted => {
        .                           attacks_submitted = true;
        .                           let declarer = g.attack_declarer();
        .                           GameAction::DeclareAttackers(pick_attacks(g, declarer))
        .                       }
    8,112 ( 0.00%)              TurnStep::DeclareBlockers if !blocks_submitted && !g.attacking().is_empty() => {
        .                           // The defender is not the priority holder at this point, so
        .                           // ask the engine which seat is actually owed the
        .                           // declaration. Getting this wrong silently leaves every
        .                           // attacker unblocked, which flatters the attack.
        .                           match (0..g.players.len()).find(|&s| g.may_declare_blocks(s)) {
        .                               Some(defender) => {
        .                                   blocks_submitted = true;
    3,390 ( 0.00%)                          GameAction::DeclareBlockers(pick_blocks(g, defender))
12,905,141 ( 0.41%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_blocks (678x)
        .                               }
        .                               None => GameAction::PassPriority,
        .                           }
        .                       }
        .                       _ => GameAction::PassPriority,
        .                   };
        .                   // Checkpointed for the same reason as the decision above: an
        .                   // abandoned walk's state is read, so it has to be the rolled-back
        .                   // one. A rejected declaration would spin forever, hence the pass.
   59,038 ( 0.00%)          if g.perform_action(action).is_err()
248,482,292 ( 7.97%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action (8,434x)
  943,813 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (8,434x)
       78 ( 0.00%)              && g.perform_action(GameAction::PassPriority).is_err()
    3,204 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action (6x)
      168 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (6x)
        .                   {
        .                       return CombatSim::Incomplete;
        .                   }
        .               }
        .               CombatSim::Completed
    1,784 ( 0.00%)  }
        .           
        .           /// Dry-run `action` to quiescence on a full-state clone (libraries kept —
        .           /// resolution may draw) and score the result for `seat`: the cast is
        .           /// applied, then priority passes with [`AutoDecider`] answers for any
        .           /// decision that surfaces until the stack empties. `None` on rejection or
        .           /// a resolution that won't settle — callers fall back to the static rank.
    8,520 ( 0.00%)  fn evaluate_action_outcome(
        .               state: &GameState,
        .               seat: usize,
        .               action: &GameAction,
        .               w: &EvalWeights,
        .           ) -> Option<i32> {
        .               // Under determinization the planner's dry-runs must not read the true
        .               // hidden zones either: a resolution that draws would otherwise plan
        .               // around the exact card coming. One redeal (k = 0), not an average —
        .               // the planner evaluates many candidates per decision and the salt is
        .               // turn/step-keyed, so every finalist is judged against the *same*
        .               // redeal, which keeps the comparison among candidates consistent.
        .               // The recursion inside `evaluate_action_sequence` must NOT re-redeal:
        .               // it continues a line through cards the sim already drew.
    1,704 ( 0.00%)      if w.determinize > 0 {
        .                   let g = sim_start_state(state, seat, w, 0);
        .                   return evaluate_action_sequence(&g, seat, action, w, w.lookahead);
        .               }
    8,520 ( 0.00%)      evaluate_action_sequence(state, seat, action, w, w.lookahead)
203,966,921 ( 6.54%)  => crabomination/src/server/bot.rs:crabomination::server::bot::evaluate_action_sequence (852x)
        .           }
        .           
        .           /// Score of the best *sequence* of up to `depth + 1` plays that starts with
        .           /// `action`, rather than the score the moment `action` resolves.
        .           ///
        .           /// This is the gap a one-action-at-a-time evaluator cannot close: with four
        .           /// mana available, "cast the four-drop" and "cast a two-drop" are compared
        .           /// as single plays, so the bot never sees that the second line continues
        .           /// into *another* two-drop and ends the turn ahead. Forge searches
        .           /// sequences to three plies for exactly this reason
        .           /// (`SpellAbilityPicker` recursing through `SimulationController`).
        .           ///
        .           /// Stopping is always one of the options considered, so a sequence is never
        .           /// forced to spend everything — dumping the hand is a line, not an
        .           /// obligation.
   12,780 ( 0.00%)  fn evaluate_action_sequence(
        .               state: &GameState,
        .               seat: usize,
        .               action: &GameAction,
        .               w: &EvalWeights,
        .               depth: u8,
        .           ) -> Option<i32> {
      852 ( 0.00%)      let mut g = state.clone();
1,874,122 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (852x)
    3,408 ( 0.00%)      dry_run(&mut g, action.clone()).ok()?;
   64,032 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (852x)
        .               let mut fuel = 64u32;
        .               loop {
    2,628 ( 0.00%)          if g.is_game_over() {
        .                       break;
        .                   }
    2,628 ( 0.00%)          if g.pending_decision.is_some() {
        .                       let answer = {
        .                           let pending = g.pending_decision.as_ref().unwrap();
        .                           decide_pending_policy(&g, pending.acting_player(), w, &pending.decision, false)
        .                       };
        .                       dry_run(&mut g, GameAction::SubmitDecision(answer)).ok()?;
    7,884 ( 0.00%)          } else if g.stack.is_empty() {
        .                       break;
        .                   } else {
    5,328 ( 0.00%)              dry_run(&mut g, GameAction::PassPriority).ok()?;
        .                   }
        .                   fuel = fuel.checked_sub(1)?;
        .               }
        .               // The value of stopping here.
    2,556 ( 0.00%)      let mut best = score_settled_state(&g, seat, w)?;
79,411,901 ( 2.55%)  => crabomination/src/server/bot.rs:crabomination::server::bot::score_settled_state (852x)
    1,704 ( 0.00%)      if depth > 0 {
        .                   for follow in follow_up_candidates(&g, seat, w) {
        .                       if let Some(v) = evaluate_action_sequence(&g, seat, &follow, w, depth - 1) {
        .                           best = best.max(v);
        .                       }
        .                   }
        .               }
        .               Some(best)
    9,372 ( 0.00%)  }
6,235,731 ( 0.20%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (852x)
        .           
        .           /// Score a state that has resolved to quiescence, running it through this
        .           /// turn's combat first when the profile asks for it. `None` when the combat
        .           /// simulation can't complete — see `simulate_through_combat`.
    5,964 ( 0.00%)  fn score_settled_state(g: &GameState, seat: usize, w: &EvalWeights) -> Option<i32> {
    1,704 ( 0.00%)      if !w.combat_aware {
        .                   return Some(eval_material(g, seat, w));
        .               }
        .               // Score the board this line actually leads to, not the one that exists
        .               // the moment it resolves -- see `simulate_through_combat`. Its own fuel
        .               // budget: combat is a long way through the step machine (two
        .               // declarations plus a priority round per step, before triggers).
    1,704 ( 0.00%)      let mut sim = g.clone();
1,893,575 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (852x)
    1,704 ( 0.00%)      let mut combat_fuel = 256u32;
    4,260 ( 0.00%)      match simulate_through_combat(&mut sim, &mut combat_fuel, w) {
67,710,057 ( 2.17%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::simulate_through_combat (852x)
        .                   // A half-simulated combat is worse than none: attackers are
        .                   // declared and tapped but damage was never dealt, so the line reads
        .                   // as pure downside. Refuse to score a torn state -- the caller
        .                   // falls back to the static rank.
        .                   CombatSim::Incomplete => None,
        .                   // Skipped leaves `sim` untouched, so scoring it is just scoring `g`.
        .                   CombatSim::Skipped | CombatSim::Completed => {
        .                       let v = eval_material(&sim, seat, w);
        .                       super::leaf_capture::maybe(&sim, seat, v);
        .                       Some(v)
        .                   }
        .               }
    7,668 ( 0.00%)  }
3,529,042 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (852x)
        .           
        .           /// The few plays worth considering as a continuation of a sequence:
        .           /// the best-scoring validated candidates from `g`, capped hard.
        .           ///
        .           /// The cap is the whole reason this is affordable. Enumerating candidates
        .           /// runs an engine dry-run per specialty card shape, so a wide branching
        .           /// factor at every ply would cost far more than the extra ply is worth;
        .           /// two continuations is enough to catch the case this exists for (a second
-- line 9296 ----------------------------------------
-- line 9367 ----------------------------------------
        .           ) -> bool {
        .               // The baseline has to be measured the same way the outcome is. With
        .               // `combat_aware` the outcome runs through combat, so a raw pre-combat
        .               // baseline would be compared against a post-combat score and every
        .               // connecting attack would read as "this action improved things" —
        .               // making the gate fire almost never. Forge avoids this by routing both
        .               // sides through the same `getScoreForGameState`, which fast-forwards
        .               // combat itself.
    1,398 ( 0.00%)      let before = if w.combat_aware {
      466 ( 0.00%)          let mut idle = state.clone();
1,037,862 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (466x)
    1,864 ( 0.00%)          let mut idle_fuel = 256u32;
      932 ( 0.00%)          let _ = simulate_through_combat(&mut idle, &mut idle_fuel, w);
100,069,588 ( 3.21%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::simulate_through_combat (466x)
        .                   eval_material_summon_sick_blind(&idle, seat, w)
      932 ( 0.00%)      } else {
4,346,464 ( 0.14%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (466x)
        .                   eval_material_summon_sick_blind(state, seat, w)
        .               };
      466 ( 0.00%)      let mut g = state.clone();
1,037,862 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (466x)
    1,864 ( 0.00%)      if dry_run(&mut g, action.clone()).is_err() {
  164,981 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (466x)
   34,900 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (466x)
        .                   return true;
        .               }
        .               let mut fuel = 64u32;
    1,482 ( 0.00%)      while !g.is_game_over() {
    1,480 ( 0.00%)          if g.pending_decision.is_some() {
        .                       let answer = {
        .                           let pending = g.pending_decision.as_ref().unwrap();
        .                           decide_pending_policy(&g, pending.acting_player(), w, &pending.decision, false)
        .                       };
        .                       if dry_run(&mut g, GameAction::SubmitDecision(answer)).is_err() {
        .                           return true;
        .                       }
    4,440 ( 0.00%)          } else if g.stack.is_empty() {
        .                       break;
    6,096 ( 0.00%)          } else if dry_run(&mut g, GameAction::PassPriority).is_err() {
   89,971 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (1,016x)
        .                       return true;
        .                   }
        .                   fuel = match fuel.checked_sub(1) {
        .                       Some(f) => f,
        .                       None => return true,
        .                   };
        .               }
    1,398 ( 0.00%)      if w.combat_aware {
    1,864 ( 0.00%)          let mut combat_fuel = 256u32;
        .                   // A torn simulation can't answer the question; don't hold on it.
      932 ( 0.00%)          if simulate_through_combat(&mut g, &mut combat_fuel, w) == CombatSim::Incomplete {
107,611,461 ( 3.45%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::server::bot::simulate_through_combat (466x)
        .                       return true;
        .                   }
        .               }
      932 ( 0.00%)      eval_material_summon_sick_blind(&g, seat, w) > before
      466 ( 0.00%)  }
6,083,183 ( 0.20%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (466x)
        .           
        .           /// Final pick among the validated finalists `(jittered static score,
        .           /// action)`: resolve each candidate's outcome and take the best resulting
        .           /// state, static score breaking ties and ordering candidates whose outcome
        .           /// probe bailed. A lone finalist skips the outcome clones entirely.
    9,080 ( 0.00%)  fn pick_by_outcome(
        .               state: &GameState,
        .               seat: usize,
        .               finalists: Vec<(i32, GameAction)>,
        .               w: &EvalWeights,
        .           ) -> Option<GameAction> {
    1,816 ( 0.00%)      if finalists.len() <= 1 {
    1,128 ( 0.00%)          return finalists.into_iter().next().map(|(_, a)| a);
        .               }
      344 ( 0.00%)      let baseline = eval_material(state, seat, w);
      688 ( 0.00%)      let evd: Vec<(i32, i32, GameAction)> = finalists
        .                   .into_iter()
   11,076 ( 0.00%)          .map(|(s, a)| {
        .                       // Known-temporary casts (bounce, until-EOT stat changes) are
        .                       // pinned to the baseline: the post-resolution snapshot can't
        .                       // see the effect reversing, so evaluating it would sell a
        .                       // bounce as removal. They win only on static score against
        .                       // other no-eval-gain lines.
    3,408 ( 0.00%)              let ev = if action_outcome_is_temporary(state, &a) {
  271,168 ( 0.01%)  => crabomination/src/server/bot.rs:crabomination::server::bot::action_outcome_is_temporary (852x)
        .                           baseline
        .                       } else {
    4,260 ( 0.00%)                  evaluate_action_outcome(state, seat, &a, w).unwrap_or(baseline)
203,985,665 ( 6.55%)  => crabomination/src/server/bot.rs:crabomination::server::bot::evaluate_action_outcome (852x)
        .                       };
   10,224 ( 0.00%)              (ev, s, a)
        .                   })
        .                   .collect();
        .               // Actor-side exploration: sample finalists by outcome score. The
        .               // static score stays out of the softmax — it's a tiebreak, not a
        .               // second opinion at temperature.
        .               if let Some(t) = sampling_temp(state.turn_number) {
        .                   let ws: Vec<i32> = evd.iter().map(|e| e.0).collect();
        .                   let i = sample_scored_index(&ws, t);
        .                   capture_decision(state, seat, &evd, i);
        .                   return evd.into_iter().nth(i).map(|(_, _, a)| a);
        .               }
        .               let best = evd
        .                   .iter()
        .                   .enumerate()
      852 ( 0.00%)          .max_by_key(|(_, (ev, s, _))| (*ev, *s))
        .                   .map(|(i, _)| i)?;
    2,064 ( 0.00%)      capture_decision(state, seat, &evd, best);
    5,504 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::capture_decision (344x)
        .               evd.into_iter().nth(best).map(|(_, _, a)| a)
    7,264 ( 0.00%)  }
        .           
        .           /// Feed the finalist set and the pick to [`decision_capture`].
        .           ///
        .           /// Hooked here rather than at the enumerator because this is the point
        .           /// where a *choice* is actually made: everything upstream is filtering,
        .           /// and the finalists are the set the bot genuinely weighed. That does
        .           /// mean the recorded candidate set is the shortlist (`EVAL_TOP`) rather
        .           /// than every legal action — which is the same convention AlphaZero uses
        .           /// when it records the search's action set rather than the rules'.
    2,064 ( 0.00%)  fn capture_decision(
        .               state: &GameState,
        .               seat: usize,
        .               evd: &[(i32, i32, GameAction)],
        .               chosen: usize,
        .           ) {
      344 ( 0.00%)      if !super::decision_capture::enabled() {
        .                   return;
        .               }
        .               let actions: Vec<GameAction> = evd.iter().map(|(_, _, a)| a.clone()).collect();
        .               super::decision_capture::maybe(state, seat, &actions, chosen);
    2,408 ( 0.00%)  }
        .           
        .           /// True when `e`'s tree contains a leaf whose apparent value REVERSES
        .           /// after the turn: an until-end-of-turn stat change, or a bounce of a
        .           /// battlefield permanent to hand (the permanent comes back next turn).
        .           /// The outcome evaluation snapshots the state right after resolution, so
        .           /// these leaves read as permanent gains — a bounced 4-drop looked like
        .           /// Doom Blade (+3×value) and a "base P/T 5/5 until end of turn" like a
        .           /// real +18 material swing, and the bot burned Proctor's Gaze / Quandrix
        .           /// Charm on them at sorcery speed for nothing. Graveyard/exile-to-hand
        .           /// moves (Regrowth) are real card advantage and are NOT temporary.
    6,876 ( 0.00%)  fn contains_temporary_leaf(e: &Effect) -> bool {
        .               use crate::effect::{Duration, ZoneDest};
   12,866 ( 0.00%)      match e {
        .                   Effect::PumpPT { duration: Duration::EndOfTurn | Duration::EndOfCombat, .. }
        .                   | Effect::SetBasePT { duration: Duration::EndOfTurn | Duration::EndOfCombat, .. }
        .                   | Effect::SwitchPT { duration: Duration::EndOfTurn | Duration::EndOfCombat, .. } => true,
        .                   Effect::Move { what, to: ZoneDest::Hand(_) } => {
        .                       // A bounce of a battlefield object; an off-board (graveyard /
        .                       // exile) retrieval keeps the card — permanent value.
        .                       match what {
        .                           crate::effect::Selector::TargetFiltered { filter, .. } => {
-- line 9505 ----------------------------------------
-- line 9511 ----------------------------------------
        .                   Effect::Seq(v) => v.iter().any(contains_temporary_leaf),
        .                   Effect::If { then, else_, .. } => {
        .                       contains_temporary_leaf(then) || contains_temporary_leaf(else_)
        .                   }
        .                   Effect::MayDo { body, .. } => contains_temporary_leaf(body),
        .                   Effect::ApplyToTargets { effect, .. } => contains_temporary_leaf(effect),
        .                   _ => false,
        .               }
    5,730 ( 0.00%)  }
        .           
        .           /// True when `action` is a cast whose (mode-resolved) effect contains a
        .           /// temporary leaf — such candidates skip the outcome evaluation (see
        .           /// [`contains_temporary_leaf`]) and compete on static score alone.
    2,556 ( 0.00%)  fn action_outcome_is_temporary(state: &GameState, action: &GameAction) -> bool {
    1,704 ( 0.00%)      let (card_id, mode) = match action {
    2,556 ( 0.00%)          GameAction::CastSpell { card_id, mode, .. } => (*card_id, *mode),
        .                   _ => return false,
        .               };
    3,408 ( 0.00%)      let Some(card) = state.find_card_anywhere(card_id) else { return false };
  227,804 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (852x)
    4,260 ( 0.00%)      contains_temporary_leaf(mode_branch(&card.definition.effect, mode))
   21,212 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::contains_temporary_leaf (852x)
        .           }
        .           
        .           /// A pure temporary-pump instant aimed at a target creature (Giant
        .           /// Growth, Infuriate): the whole effect tree is target pumps with an
        .           /// end-of-turn/combat duration. Anything with riders (draw, damage,
        .           /// counters, keyword grants) stays castable on the normal schedule.
        .           fn is_combat_trick(def: &CardDefinition) -> bool {
        .               use crate::card::CardType;
        .               use crate::effect::{Duration, Selector};
        .               if !def.card_types.contains(&CardType::Instant) {
        .                   return false;
        .               }
        .               fn all_temp_pumps(e: &Effect) -> bool {
   34,792 ( 0.00%)          match e {
        .                       Effect::PumpPT {
        .                           what: Selector::Target(_) | Selector::TargetFiltered { .. },
        .                           duration: Duration::EndOfTurn | Duration::EndOfCombat,
        .                           ..
        .                       } => true,
      776 ( 0.00%)              Effect::Seq(v) => !v.is_empty() && v.iter().all(all_temp_pumps),
        .                       _ => false,
        .                   }
        .               }
        .               all_temp_pumps(&def.effect)
        .           }
        .           
        .           /// After blocks are in: cast a held pump trick when it flips a fight our
        .           /// creature is currently losing — it dies to its opposite number and the
        .           /// pump saves it, or it fails to kill and the pump finishes the job.
        .           /// Covers both sides of combat (our blocked attacker on our turn, our
        .           /// blocker on theirs). Constant pumps only; dynamic amounts are skipped
        .           /// rather than mis-valued.
   37,476 ( 0.00%)  fn pick_combat_trick(state: &GameState, seat: usize, w: &EvalWeights) -> Option<GameAction> {
        .               use crate::effect::{Duration, Selector, Value};
    3,216 ( 0.00%)      fn pump_amounts(e: &Effect) -> Option<(i32, i32)> {
    7,504 ( 0.00%)          match e {
        .                       Effect::PumpPT {
        .                           what: Selector::Target(_) | Selector::TargetFiltered { .. },
        .                           power: Value::Const(p),
        .                           toughness: Value::Const(t),
        .                           duration: Duration::EndOfTurn | Duration::EndOfCombat,
    3,216 ( 0.00%)              } => Some((*p, *t)),
        .                       Effect::Seq(v) => {
        .                           let mut acc: Option<(i32, i32)> = None;
        .                           for e in v {
        .                               let (p, t) = pump_amounts(e)?;
        .                               let (ap, at) = acc.unwrap_or((0, 0));
        .                               acc = Some((ap + p, at + t));
        .                           }
        .                           acc
        .                       }
        .                       _ => None,
        .                   }
    3,752 ( 0.00%)      }
    8,328 ( 0.00%)      let tricks: Vec<(CardId, i32, i32)> = state.players[seat]
        .                   .hand
        .                   .iter()
        .                   .filter(|c| is_combat_trick(&c.definition))
    2,940 ( 0.00%)          .filter(|c| can_afford_in_state(state, seat, c, w))
2,778,610 ( 0.09%)  => crabomination/src/server/bot.rs:crabomination::server::bot::can_afford_in_state (588x)
    2,680 ( 0.00%)          .filter_map(|c| pump_amounts(&c.definition.effect).map(|(p, t)| (c.id, p, t)))
   18,224 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::pick_combat_trick::pump_amounts (536x)
        .                   .collect();
    8,328 ( 0.00%)      if tricks.is_empty() {
    3,702 ( 0.00%)          return None;
        .               }
        .               let computed_pt = |id: CardId| -> Option<(i32, i32)> {
    2,976 ( 0.00%)          let cp = state.computed_permanent(id);
1,199,122 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (744x)
        .                   let raw = state.battlefield_find(id)?;
    1,488 ( 0.00%)          Some(match cp {
    2,604 ( 0.00%)              Some(cp) => (cp.power, cp.toughness),
        .                       None => (raw.power(), raw.toughness()),
        .                   })
        .               };
      924 ( 0.00%)      for (blocker, attacker) in state.block_map_snapshot() {
  112,354 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::block_map_snapshot (462x)
      372 ( 0.00%)          let (Some(b), Some(a)) =
      372 ( 0.00%)              (state.battlefield_find(blocker), state.battlefield_find(attacker))
        .                   else {
        .                       continue;
        .                   };
    3,252 ( 0.00%)          let (our_id, their_id) = if a.controller == seat && !state.same_team(b.controller, seat) {
   10,290 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (294x)
        .                       (attacker, blocker)
      702 ( 0.00%)          } else if b.controller == seat && !state.same_team(a.controller, seat) {
    2,730 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (78x)
        .                       (blocker, attacker)
        .                   } else {
        .                       continue;
        .                   };
      372 ( 0.00%)          let (Some((op, ot)), Some((tp, tt))) = (computed_pt(our_id), computed_pt(their_id))
        .                   else {
        .                       continue;
        .                   };
      956 ( 0.00%)          let dies = tp >= ot;
      314 ( 0.00%)          let kills = op >= tt;
      576 ( 0.00%)          if !dies && kills {
        .                       continue; // already winning this fight
        .                   }
      292 ( 0.00%)          for &(cid, p, t) in &tricks {
      540 ( 0.00%)              let saves = dies && ot + t > tp;
      832 ( 0.00%)              let now_kills = !kills && op + p >= tt;
      468 ( 0.00%)              if !(saves || now_kills) {
        .                           continue;
        .                       }
    3,212 ( 0.00%)              let action = GameAction::CastSpell {
        .                           card_id: cid,
        .                           target: Some(Target::Permanent(our_id)),
        .                           additional_targets: vec![],
        .                           mode: None,
        .                           x_value: None,
        .                       };
    2,602 ( 0.00%)              if state.would_accept(action.clone()) {
32,016,794 ( 1.03%)  => /home/user/Crabomination/crabomination/src/game/affordances.rs:crabomination::game::affordances::<impl crabomination::game::GameState>::would_accept (292x)
   22,776 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:<crabomination::game::types::GameAction as core::clone::Clone>::clone (292x)
    3,718 ( 0.00%)                  return Some(action);
        .                       }
       24 ( 0.00%)          }
      138 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameAction> (6x)
        .               }
      176 ( 0.00%)      None
   33,312 ( 0.00%)  }
        .           
   26,110 ( 0.00%)  fn score_candidate(state: &GameState, seat: usize, action: &GameAction, w: &EvalWeights) -> i32 {
        .               use crate::card::CardType;
        .               // (source card, slot-0 target, variant bonus, extra mana sunk in).
   33,570 ( 0.00%)      let (card_id, target, variant_bonus, extra_mana) = match action {
        .                   GameAction::CastSpell { card_id, target, x_value, .. } => {
        .                       (*card_id, target.clone(), 0, x_value.unwrap_or(0))
        .                   }
        .                   GameAction::CastSpellBack { card_id, target, .. } => (*card_id, target.clone(), 0, 0),
        .                   GameAction::CastSpellDelve { card_id, target, x_value, .. } => {
        .                       (*card_id, target.clone(), 3, x_value.unwrap_or(0))
        .                   }
        .                   GameAction::CastGift { card_id, target, .. } => (*card_id, target.clone(), 3, 0),
-- line 9657 ----------------------------------------
-- line 9689 ----------------------------------------
        .                   }
        .                   // Fallback lines (face-down morphs, discard-activated) only appear
        .                   // when nothing else is castable, so their exact rank is moot.
        .                   _ => return 0,
        .               };
        .           
        .               let mut score = 0i32;
        .               let mut damage: Option<i32> = None;
   22,380 ( 0.00%)      if let Some(card) = state.find_card_anywhere(card_id) {
1,184,558 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (3,730x)
        .                   // Score the face actually being cast when it isn't the front:
        .                   // MDFC backs for back-face casts, and the inset spell for
        .                   // prepare-casts — scoring the latter by the CREATURE valued
        .                   // "cast draw-3 for {U}" like deploying a 5/5 body, so the bot
        .                   // fired every inset spell at the first opportunity.
        .                   let def = match (action, card.definition.back_face.as_deref()) {
        .                       (GameAction::CastSpellBack { .. } | GameAction::CastDisturb { .. }, Some(back)) => back,
        .                       _ => &card.definition,
-- line 9705 ----------------------------------------
-- line 9706 ----------------------------------------
        .                   };
        .                   let def = match (action, card.definition.prepare_spell.as_deref()) {
        .                       (GameAction::CastPrepareSpell { .. }, Some(spell)) => spell,
        .                       _ => def,
        .                   };
        .                   // These terms are raw card stats; `permanent_value` below is on the
        .                   // profile's scale, so lift them into the same units or a scaled
        .                   // profile would drown the cast's own merits in the target's value.
   26,110 ( 0.00%)          score += 2 * (def.cost.cmc() as i32 + extra_mana as i32) * w.unit;
  105,580 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (3,730x)
        .                   if def.card_types.contains(&CardType::Creature) {
    9,168 ( 0.00%)              score += (def.power.max(0) + def.toughness.max(0)) * w.unit;
    2,292 ( 0.00%)              score += (def.keywords.len() as i32).min(3) * w.unit;
        .                   }
        .                   damage = first_damage_amount(&def.effect, extra_mana);
        .               }
        .           
        .               // Unpreparing forfeits any "prepared creatures you control …" static
        .               // the bot has out (SOS Top of the Class); charge the cast for the
        .               // rider it strips.
    7,460 ( 0.00%)      if matches!(action, GameAction::CastPrepareSpell { .. })
        .                   && state.battlefield.iter().any(|c| {
        .                       c.controller == seat && static_rewards_prepared(&c.definition)
        .                   })
        .               {
        .                   score -= 4 * w.unit;
        .               }
        .           
   14,616 ( 0.00%)      match target {
        .                   // Aimed at an opponent's permanent: removal / theft / lockdown —
        .                   // worth what the target is worth. Aimed at our own: pump / aura /
        .                   // equip, a small flat gain.
        .                   Some(Target::Permanent(id)) => {
        .                       match state.battlefield_find(id).map(|c| c.controller) {
    1,552 ( 0.00%)                  Some(ctrl) if ctrl != seat => {
      690 ( 0.00%)                      let mut v = permanent_value(state, id, w);
   51,460 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::permanent_value (138x)
        .                               // CR 702.21 — a ward tax is mana/life the cast sinks
        .                               // with no effect of its own; price it at the same
        .                               // 2-per-mana rate the cast's own cost earns above so
        .                               // an un-warded target of equal value, or a different
        .                               // spell entirely, wins the tie. Payability is the
        .                               // candidate gate's job (`ward_gate_ok`); this term
        .                               // only ranks survivors.
      966 ( 0.00%)                      if let Some(tax) = ward_tax(state, id, seat) {
   53,684 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::ward_tax (138x)
        .                                   v -= 2 * ward_tax_burden(&tax) * w.unit;
        .                               }
        .                               // Damage spells only count as removal when they kill:
        .                               // chip damage at a too-big creature keeps a quarter of
        .                               // the value, and overkill (a huge X at a small body)
        .                               // pays back the wasted points so Shock-the-2/2 beats
        .                               // Fireball-for-8-the-2/2.
    1,656 ( 0.00%)                      if let (Some(dmg), Some(cp)) = (damage, state.computed_permanent(id))
   17,124 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (138x)
        .                                   && cp.card_types.contains(&CardType::Creature)
        .                               {
        .                                   if dmg < cp.toughness {
        .                                       v /= 4;
        .                                   } else {
        .                                       // Overkill is charged in scaled points -- `dmg` and
        .                                       // `toughness` are raw, `v` is not.
        .                                       v -= (dmg - cp.toughness).max(0) * w.unit;
        .                                   }
        .                               }
        .                               // A bounce is tempo, not removal — the permanent comes
        .                               // back next turn. A third of the value keeps target
        .                               // selection sane without treating it as a kill.
      276 ( 0.00%)                      if let GameAction::CastSpell { mode, .. } = action
      690 ( 0.00%)                          && let Some(card) = state.find_card_anywhere(card_id)
   46,206 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (138x)
      828 ( 0.00%)                          && contains_temporary_leaf(mode_branch(&card.definition.effect, *mode))
    6,210 ( 0.00%)  => crabomination/src/server/bot.rs:crabomination::server::bot::contains_temporary_leaf (138x)
        .                               {
        .                                   v /= 3;
        .                               }
      138 ( 0.00%)                      score += v;
      138 ( 0.00%)                  }
    1,914 ( 0.00%)                  Some(_) => score += 2 * w.unit,
        .                           None => {}
        .                       }
        .                   }
        .                   // Face damage / discard at an opponent beats a self-aimed cantrip.
      760 ( 0.00%)          Some(Target::Player(p)) => score += if p != seat { 4 * w.unit } else { w.unit },
        .                   _ => {}
        .               }
        .           
    7,460 ( 0.00%)      score + variant_bonus * w.unit
   33,570 ( 0.00%)  }
        .           
        .           
        .           // ── Accessors for the Monte Carlo bot ────────────────────────────────────
        .           //
        .           // `mcts` needs the same candidate enumeration and leaf evaluation the
        .           // heuristic bot uses, so the two are compared on identical inputs and any
        .           // ladder difference is the *search*, not a second opinion about what is
        .           // castable or what a board is worth.
-- line 9796 ----------------------------------------

3,579,352 ( 0.11%)  <counts for unidentified lines in crabomination/src/server/bot.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/effects/targeting.rs
--------------------------------------------------------------------------------
Ir                 

-- line 59 ----------------------------------------
        .               /// no X pass 0 via the non-`_x` wrappers.
        .               pub fn auto_target_for_effect_avoiding_set_x(
        .                   &self,
        .                   eff: &Effect,
        .                   controller: usize,
        .                   avoid: &[CardId],
        .                   x: u32,
        .               ) -> Option<Target> {
    6,618 ( 0.00%)          self.auto_target_for_effect_avoiding_set_xc(eff, controller, avoid, x, 0)
1,149,294 ( 0.04%)  => crabomination/src/game/effects/targeting.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_target_for_effect_avoiding_set_xc (88x)
        .               }
        .           
        .               /// Like [`auto_target_for_effect_avoiding_set_x`] but also concretizes
        .               /// `ManaValueAtMostConverged` against the cast's converge count before
        .               /// picking (Sundering Archaic's "exile target nonland permanent … with
        .               /// mana value ≤ the number of colors of mana spent"). Without this the
        .               /// converge atom evaluates false-for-everything at selection time, and
        .               /// a resolve-time-only gate lets the picker aim at over-cap permanents
        .               /// whose exile then fizzles.
    9,620 ( 0.00%)      pub fn auto_target_for_effect_avoiding_set_xc(
        .                   &self,
        .                   eff: &Effect,
        .                   controller: usize,
        .                   avoid: &[CardId],
        .                   x: u32,
        .                   converge: u32,
        .               ) -> Option<Target> {
        .                   let avoid_source = avoid.first().copied();
        .                   // Effects with a bare `Selector::Target(0)` (e.g. Lightning Bolt's
        .                   // "deal 3 damage to any target") have no surfaced primary filter —
        .                   // they accept any legal entity. Fall back to `Any` so the picker
        .                   // walks players + permanents instead of short-circuiting to None.
      740 ( 0.00%)          let any_filter = crate::card::SelectionRequirement::Any;
        .                   let req_owned =
    1,480 ( 0.00%)              eff.primary_target_filter().map(|f| f.resolve_x(x).resolve_converge(converge));
   27,652 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::primary_target_filter (740x)
        .                   let req = req_owned.as_ref().unwrap_or(&any_filter);
        .                   // First opponent on a different team. Falls back to the next
        .                   // seat in singleton-team / unknown-team cases so the legacy 1v1
        .                   // pick (`(controller + 1) % n`) is preserved.
    2,220 ( 0.00%)          let opp = self
      740 ( 0.00%)              .opponents_of(controller)
  185,131 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::opponents_of (740x)
        .                       .first()
        .                       .copied()
    7,400 ( 0.00%)              .unwrap_or((controller + 1) % self.players.len());
    2,220 ( 0.00%)          let prefer_friendly = eff.prefers_friendly_target();
   24,998 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_friendly_target (740x)
        .                   // `prefers_graveyard_target` is the broader classifier — it covers
        .                   // both reanimate (friendly graveyard) and graveyard hate (Ghost
        .                   // Vacuum exiling target card from a graveyard). We walk graveyards
        .                   // BEFORE the battlefield when this is set, so an `Any`-filtered
        .                   // Move-to-Exile doesn't grab a battlefield permanent.
    2,220 ( 0.00%)          let prefer_graveyard = eff.prefers_graveyard_target();
   22,048 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_graveyard_target (740x)
        .                   // Skip Player candidates entirely when the effect operates on
        .                   // permanents/stack — without this, an `Any`-filtered Move (Regrowth)
        .                   // auto-targets the caster as a player and silently fizzles since
        .                   // `Effect::Move` only consumes Permanent / Card entity refs.
    2,220 ( 0.00%)          let accepts_player = eff.accepts_player_target();
   22,420 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::accepts_player_target (740x)
        .                   let primary_player = if prefer_friendly { controller } else { opp };
    2,960 ( 0.00%)          let secondary_player = if prefer_friendly { opp } else { controller };
        .           
        .                   // Combined check: requirement match + targetable by `controller`.
    5,180 ( 0.00%)          let is_legal = |t: &Target| -> bool {
   23,738 ( 0.00%)              self.evaluate_requirement_static(req, t, controller, avoid_source)
   33,956 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (1,306x)
        .                           && self.check_target_legality(t, controller).is_ok()
        .                   };
        .                   // CR 702.21 — a hostile permanent with a non-trivial Ward gets the
        .                   // targeting spell/ability countered unless the actor pays, and the
        .                   // engine pays wards from a (typically empty) floating pool. Prefer
        .                   // un-warded candidates; warded ones stay as a fallback so effects
        .                   // never fizzle outright.
    2,220 ( 0.00%)          let hostile_ward = |cid: CardId| -> bool {
        .                       use crate::card::Keyword;
    1,916 ( 0.00%)              let Some(c) = self.battlefield.iter().find(|c| c.id == cid) else { return false };
    5,748 ( 0.00%)              if c.controller == controller {
        .                           return false;
        .                       }
    1,916 ( 0.00%)              self.computed_permanent(cid)
4,922,746 ( 0.16%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (1,916x)
       30 ( 0.00%)                  .map(|cp| cp.keywords.to_vec())
        .                           .unwrap_or_else(|| c.definition.keywords.clone())
        .                           .iter()
      202 ( 0.00%)                  .any(|k| matches!(k, Keyword::Ward(w)
        .                               if !crate::game::actions::ward_cost_is_trivial(w)))
        .                   };
        .           
        .                   // CR 601.2c — Flagbearer: an opponent's Flagbearer must be chosen when
        .                   // the slot can take it, so it outranks every other candidate here.
        .                   for fb in self.flagbearer_candidates(controller) {
        .                       let t = Target::Permanent(fb);
        .                       if is_legal(&t) {
        .                           return Some(t);
        .                       }
        .                   }
        .           
    3,700 ( 0.00%)          if accepts_player {
    1,040 ( 0.00%)              let player_primary = Target::Player(primary_player);
    1,040 ( 0.00%)              if is_legal(&player_primary) { return Some(player_primary); }
        .                       let player_secondary = Target::Player(secondary_player);
        .                       if is_legal(&player_secondary) { return Some(player_secondary); }
        .                   }
        .           
        .                   // Stack walk for counter-class effects (Spell Queller, Mystic
        .                   // Snake, Venser-style ETB / triggered counters whose target is a
        .                   // spell on the stack). The battlefield/graveyard walks below
        .                   // never consider stack objects, so without this pass an
-- line 160 ----------------------------------------
-- line 166 ----------------------------------------
        .                   // counter-class targets — effects with a looser filter (e.g. a
        .                   // magecraft "exile a card from your graveyard" with an `Any` /
        .                   // `Nonland` filter) must not grab the just-cast spell sitting on
        .                   // the stack.
        .                   // The gate fires for a bare `IsSpellOnStack` *and* for compound
        .                   // filters that narrow it (`And(IsSpellOnStack, Creature)` — "counter
        .                   // target creature spell"); `is_legal` still applies the full filter,
        .                   // so widening the gate only decides whether to walk the stack.
      440 ( 0.00%)          fn mentions_spell_on_stack(r: &crate::card::SelectionRequirement) -> bool {
        .                       use crate::card::SelectionRequirement as R;
    1,540 ( 0.00%)              match r {
        .                           R::IsSpellOnStack
        .                           | R::SpellTargetsControllerOrControlled
        .                           | R::SpellTargetsCreature => true,
        .                           R::And(a, b) | R::Or(a, b) => {
        .                               mentions_spell_on_stack(a) || mentions_spell_on_stack(b)
        .                           }
        .                           _ => false,
        .                       }
      440 ( 0.00%)          }
      880 ( 0.00%)          if mentions_spell_on_stack(req) {
    2,640 ( 0.00%)  => crabomination/src/game/effects/targeting.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_target_for_effect_avoiding_set_xc::mentions_spell_on_stack (220x)
        .                       use crate::game::types::StackItem;
        .                       // Topmost first: iterate the stack in reverse.
        .                       let mut hostile: Option<Target> = None;
        .                       let mut friendly: Option<Target> = None;
        .                       for si in self.stack.iter().rev() {
        .                           if let StackItem::Spell { card, caster, .. } = si {
        .                               let t = Target::Permanent(card.id);
        .                               if is_legal(&t) {
-- line 194 ----------------------------------------
-- line 205 ----------------------------------------
        .                           return Some(t);
        .                       }
        .                   }
        .           
        .                   // Graveyard-target effects: walk primary player's graveyard first,
        .                   // then secondary's. Reanimate/Disentomb (friendly) hits the caster's
        .                   // graveyard; Ghost Vacuum (hostile) hits the opp's. Falls through
        .                   // to the battlefield walk below if no graveyard match.
      440 ( 0.00%)          if prefer_graveyard {
        .                       // The `avoid` set matters here too: a multi-slot trigger fanning
        .                       // out over a graveyard (Celestial Gatekeeper's "up to two target
        .                       // Bird and/or Cleric cards") re-picked the card it already claimed
        .                       // and stalled after one slot.
        .                       for &p in &[primary_player, secondary_player] {
        .                           if let Some(c) = self.players[p]
        .                               .graveyard
        .                               .iter()
-- line 221 ----------------------------------------
-- line 231 ----------------------------------------
        .                   // Battlefield: walk preferred-controller permanents first, then
        .                   // any matching permanent. Without the preference, the bot would
        .                   // happily Vines its opponent's bear instead of its own.
        .                   //
        .                   // Source-avoidance pass (see `auto_target_for_effect_avoiding`'s
        .                   // doc comment): when caller asked us to avoid the trigger source,
        .                   // skip the source on the first pass and only fall back to it if
        .                   // no other legal candidate exists.
    3,448 ( 0.00%)          let is_avoided = |cid: CardId| -> bool { avoid.contains(&cid) };
        .                   // For friendly pumps (Magecraft / Repartee +1/+1 fan-out, transient
        .                   // PumpPT spells), prefer the highest-power friendly creature so the
        .                   // buff lands on the bot's biggest threat — improves expected value
        .                   // versus the prior "first-in-Vec" pick (which was deterministic but
        .                   // typically picked a 1-drop utility creature). For hostile picks the
        .                   // current first-match heuristic still applies.
        .                   let collect_legal_on_player = |p: usize| -> Vec<(CardId, i32)> {
      220 ( 0.00%)              self.battlefield
        .                           .iter()
    7,984 ( 0.00%)                  .filter(|c| c.controller == p)
    1,394 ( 0.00%)                  .filter(|c| !is_avoided(c.id))
    2,612 ( 0.00%)                  .filter(|c| is_legal(&Target::Permanent(c.id)))
        .                           .map(|c| {
        .                               let power = self
    4,784 ( 0.00%)                          .computed_permanent(c.id)
3,195,010 ( 0.10%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (1,306x)
    1,306 ( 0.00%)                          .map(|cp| cp.power)
        .                                   .unwrap_or(c.definition.power);
    1,306 ( 0.00%)                      (c.id, power)
        .                           })
        .                           .collect()
        .                   };
        .                   let mut primary_candidates = collect_legal_on_player(primary_player);
      704 ( 0.00%)          if prefer_friendly && !primary_candidates.is_empty() {
        .                       // Sort by descending power so the strongest creature wins.
        .                       primary_candidates.sort_by_key(|c| std::cmp::Reverse(c.1));
        .                   } else {
        .                       // Hostile pick: un-warded candidates first (stable within groups).
   34,488 ( 0.00%)              primary_candidates.sort_by_key(|c| hostile_ward(c.0));
        .                   }
      220 ( 0.00%)          if let Some(&(cid, _)) = primary_candidates.first() {
      660 ( 0.00%)              return Some(Target::Permanent(cid));
        .                   }
        .                   for pass_warded in [false, true] {
        .                       if let Some(t) = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| !is_avoided(c.id))
        .                           .filter(|c| pass_warded || !hostile_ward(c.id))
        .                           .map(|c| Target::Permanent(c.id))
-- line 278 ----------------------------------------
-- line 321 ----------------------------------------
        .                       .exile
        .                       .iter()
        .                       .map(|c| Target::Permanent(c.id))
        .                       .find(|t| is_legal(t))
        .                   {
        .                       return Some(c);
        .                   }
        .                   None
    7,840 ( 0.00%)      }
   10,360 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement> (740x)
        .           
        .               /// Enumerate every legal slot-0 target for `eff` from the
        .               /// perspective of `controller`. Used by the UI trigger-target
        .               /// picker — when a wants_ui controller is about to push a
        .               /// targeted trigger, the engine surfaces a `Decision::ChooseTarget`
        .               /// listing all of these so the player can pick.
        .               ///
        .               /// Order: players (controller first, then opponents), then
-- line 337 ----------------------------------------
-- line 440 ----------------------------------------
        .               /// Used by the bot harness to drive multi-target casts without
        .               /// surfacing a UI prompt.
        .               pub fn auto_targets_for_effect_all_slots(
        .                   &self,
        .                   eff: &Effect,
        .                   controller: usize,
        .                   mode: Option<usize>,
        .               ) -> (Option<Target>, Vec<Target>) {
   24,624 ( 0.00%)          self.auto_targets_for_effect_all_slots_kicked(eff, controller, mode, false, None)
101,206,188 ( 3.25%)  => crabomination/src/game/effects/targeting.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_targets_for_effect_all_slots_kicked (2,736x)
        .               }
        .           
        .               /// Source-aware variant: slot filters that read the ability's source
        .               /// (`ManaValueEqualsCountersOnSource` — Wishing Well's coin count) need it
        .               /// to resolve. Reflexive "when you do" bodies pass their `ctx.source`.
        .               pub fn auto_targets_for_effect_all_slots_sourced(
        .                   &self,
        .                   eff: &Effect,
-- line 456 ----------------------------------------
-- line 460 ----------------------------------------
        .               ) -> (Option<Target>, Vec<Target>) {
        .                   self.auto_targets_for_effect_all_slots_kicked(eff, controller, mode, false, source)
        .               }
        .           
        .               /// Kicker-aware variant (CR 702.32): slot filters resolve to the
        .               /// `If(SpellWasKicked, …)` branch that matches `kicked`, so a bot
        .               /// preparing a `CastSpellKicked` aims at the kicked target set (Tear
        .               /// Asunder's nonland permanent rather than the base artifact/enchant).
   51,984 ( 0.00%)      pub fn auto_targets_for_effect_all_slots_kicked(
        .                   &self,
        .                   eff: &Effect,
        .                   controller: usize,
        .                   mode: Option<usize>,
        .                   kicked: bool,
        .                   source: Option<CardId>,
        .               ) -> (Option<Target>, Vec<Target>) {
        .                   // Slot 0 — if it carries its own numbered `TargetFiltered` filter
        .                   // (Rabid Bite's friendly-creature power source lives in slot 0,
        .                   // inside `Value::PowerOf`), pick it by that filter in the loop
        .                   // below. Otherwise (bare `Target(0)` effects like Lightning Bolt)
        .                   // use the source-aware heuristic picker.
   19,152 ( 0.00%)          let slot0_has_filter = eff.target_filter_for_slot_in_mode_kicked(0, mode, kicked).is_some();
  222,516 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (2,736x)
    2,736 ( 0.00%)          let mut slot_0 = if slot0_has_filter {
    5,116 ( 0.00%)              None
        .                   } else {
        .                       self.auto_target_for_effect_avoiding(eff, controller, None)
        .                   };
        .                   let mut additional = Vec::new();
    2,558 ( 0.00%)          let mut slot: u8 = if slot0_has_filter { 0 } else { 1 };
        .                   // Cap at 16 slots — no real card uses more than 4, but cap defensively.
    1,040 ( 0.00%)          while slot < 16 {
   26,048 ( 0.00%)              let req = match eff.target_filter_for_slot_in_mode_kicked(slot, mode, kicked) {
  289,010 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (3,256x)
    7,674 ( 0.00%)                  Some(r) => r.clone(),
  182,415 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone (2,558x)
        .                           None => break,
        .                       };
        .                       // Use the same hostile/friendly preference heuristics by
        .                       // constructing a small Effect::PumpPT-style probe and calling
        .                       // the picker against that filter. Simpler approach: walk
        .                       // battlefield + players, return first legal.
    2,558 ( 0.00%)              let opp = self
    7,674 ( 0.00%)                  .opponents_of(controller)
  670,578 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::opponents_of (2,558x)
        .                           .first()
        .                           .copied()
   25,580 ( 0.00%)                  .unwrap_or((controller + 1) % self.players.len());
   17,906 ( 0.00%)              let is_legal = |t: &Target| -> bool {
1,462,922 ( 0.05%)                  self.evaluate_requirement_static(&req, t, controller, source)
22,005,762 ( 0.71%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (28,484x)
        .                               && self.check_target_legality(t, controller).is_ok()
        .                       };
        .                       let pick = {
        .                           // Player slots: try controller first (caster-friendly),
        .                           // then opponent.
        .                           let mut found: Option<Target> = None;
    7,674 ( 0.00%)                  let player_caster = Target::Player(controller);
    5,116 ( 0.00%)                  let player_opp = Target::Player(opp);
        .                           if is_legal(&player_caster) {
        .                               found = Some(player_caster);
        .                           } else if is_legal(&player_opp) {
        .                               found = Some(player_opp);
        .                           }
        .                           // Graveyard-preferring effects (reanimate / regrow — Young
        .                           // Necromancer's reflexive return) must not grab a battlefield
        .                           // permanent that happens to match the filter; sweep
        .                           // graveyards first for those.
    7,674 ( 0.00%)                  if found.is_none() && eff.prefers_graveyard_target() {
   92,712 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_graveyard_target (2,558x)
        .                               found = self
        .                                   .players
        .                                   .iter()
        .                                   .flat_map(|p| p.graveyard.iter())
        .                                   .map(|c| Target::Permanent(c.id))
        .                                   .find(|t| is_legal(t));
        .                           }
        .                           // Battlefield: walk all permanents, prefer one not already
-- line 531 ----------------------------------------
-- line 534 ----------------------------------------
        .                           if found.is_none() {
        .                               let already_picked: Vec<CardId> = std::iter::once(slot_0.clone())
        .                                   .chain(additional.iter().cloned().map(Some))
        .                                   .filter_map(|t| match t {
        .                                       Some(Target::Permanent(id)) => Some(id),
        .                                       _ => None,
        .                                   })
        .                                   .collect();
    2,558 ( 0.00%)                      if let Some(c) = self
        .                                   .battlefield
        .                                   .iter()
        .                                   .filter(|c| !already_picked.contains(&c.id))
        .                                   .map(|c| Target::Permanent(c.id))
        .                                   .find(|t| is_legal(t))
        .                               {
        .                                   found = Some(c);
    2,178 ( 0.00%)                      } else if let Some(c) = self
        .                                   .battlefield
        .                                   .iter()
   45,532 ( 0.00%)                          .map(|c| Target::Permanent(c.id))
        .                                   .find(|t| is_legal(t))
        .                               {
        .                                   // Fall back to allowing reuse if nothing else
        .                                   // matches — better to have a target than skip
        .                                   // when the spell semantics allow it.
        .                                   found = Some(c);
        .                               }
        .                           }
-- line 561 ----------------------------------------
-- line 563 ----------------------------------------
        .                           // Curious Forager's "return target permanent card from your
        .                           // graveyard"). The battlefield walk above can't see them, so
        .                           // sweep every graveyard as a last resort.
        .                           if found.is_none() {
        .                               found = self
        .                                   .players
        .                                   .iter()
        .                                   .flat_map(|p| p.graveyard.iter())
   28,484 ( 0.00%)                          .map(|c| Target::Permanent(c.id))
        .                                   .find(|t| is_legal(t));
        .                           }
        .                           found
        .                       };
        .                       match pick {
    4,160 ( 0.00%)                  Some(t) if slot == 0 => slot_0 = Some(t),
        .                           Some(t) => additional.push(t),
        .                           // Slot has a filter but no legal target — stop here.
        .                           // "Up to N target" effects resolve cleanly with whatever
        .                           // targets were filled in.
        .                           None => break,
        .                       }
      520 ( 0.00%)              slot += 1;
    3,078 ( 0.00%)          }
  203,534 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement> (2,558x)
   19,152 ( 0.00%)          (slot_0, additional)
   21,888 ( 0.00%)      }
        .           }

   26,612 ( 0.00%)  <counts for unidentified lines in crabomination/src/game/effects/targeting.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/mod.rs
--------------------------------------------------------------------------------
Ir                  

-- line 176 ----------------------------------------
         .               pub equip_grants: Vec<(CardId, &'a [crate::card::TriggeredAbility])>,
         .           }
         .           
         .           /// Interior-mutable memo for [`GameState::with_frozen_layers`]: the gathered
         .           /// continuous-effect set, shared via `Arc` so per-permanent layer passes
         .           /// don't re-clone it. `Mutex` (not `RefCell`) keeps `GameState: Sync` for the
         .           /// server's `Arc<GameState>` snapshot sink. Clones reset to unfrozen — a bot
         .           /// dry-run clone taken inside a freeze scope mutates, so it must re-gather.
   235,294 ( 0.01%)  #[derive(Default)]
         .           pub(crate) struct LayerFreeze(std::sync::Mutex<LayerFreezeState>);
         .           
         .           #[derive(Default)]
         .           struct LayerFreezeState {
         .               /// Nesting depth of active `with_frozen_layers` scopes; 0 = unfrozen.
         .               depth: u32,
         .               /// Lazily-gathered effect set, populated on the first computed read
         .               /// inside a scope and cleared when the outermost scope exits.
-- line 192 ----------------------------------------
-- line 344 ----------------------------------------
         .           /// single [`CowBox`]: the clone is one reference bump, and the first write
         .           /// in an action unshares the group once.
         .           ///
         .           /// Membership is a pure performance question — `Deref`/`DerefMut` on
         .           /// `GameState` mean moving a field in or out changes no call site. Move a
         .           /// field *out* if it turns out to be written on most actions (it would
         .           /// unshare the group every time); move one *in* if it is a collection only
         .           /// rare cards touch.
 2,204,488 ( 0.07%)  #[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
         .           pub struct ColdState {
         .               /// CR 801.2c — the range matrix, recomputed as each turn begins so a
         .               /// player leaving the game only shifts ranges on the next turn.
         .               /// `range_matrix[a][b]` is "b is within a's range". Empty means unlimited.
         .               #[serde(default)]
    50,068 ( 0.00%)      pub range_matrix: Vec<Vec<bool>>,
   801,088 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// Partition of seats into teams. Every seat appears in exactly one
         .               /// entry; free-for-all formats have one singleton team per seat,
         .               /// team formats (Two-Headed Giant) have multiple seats per team.
         .               /// Populated by `GameState::new`; reshape with `assign_teams`.
         .               /// Defaults to empty for snapshots predating the field — helpers
         .               /// (`team_of`, `teammates`, `opponents_of`) treat empty as "each
         .               /// seat is its own singleton team".
         .               #[serde(default)]
    75,102 ( 0.00%)      pub teams: Vec<crate::team::Team>,
11,107,543 ( 0.36%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 702.29 — per-game tally of how many times a card with each name has
         .               /// been cycled (Yidaro, Wandering Monster's "four or more times this game"
         .               /// recursion). Keyed by the printed `&'static str` name (an owned key
         .               /// would allocate per entry on every state clone); never reset.
         .               #[serde(with = "crate::static_str_serde::map_u32", default)]
         .               pub cycled_count_by_name:
         .                   crate::fxhash::HashMap<crate::static_str_serde::StaticStr, u32>,
         .               /// Cards put into a graveyard **from the battlefield** this turn (CR —
-- line 375 ----------------------------------------
-- line 467 ----------------------------------------
         .               /// that target this" shield (Silhouette). Cleared at cleanup.
         .               #[serde(default)]
         .               pub(crate) targeting_damage_prevented_this_turn: Vec<CardId>,
         .               /// CR 611.2 — floating "this turn, whenever a [filter] …" watchers. Merged
         .               /// into every matching permanent's trigger set for the rest of the turn, so
         .               /// permanents entering later carry them too (Mage Hunters' Onslaught).
         .               /// Cleared at cleanup.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) turn_granted_triggers:
   801,088 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .                   Vec<(crate::card::SelectionRequirement, crate::card::TriggeredAbility)>,
         .               /// CR 615.1 fog with an exception (Inspire Awe). When `Some(filter)` and
         .               /// `prevent_combat_damage_this_turn` is set, a creature's combat damage is
         .               /// prevented unless the *dealer* matches `filter`. `None` = prevent all.
         .               #[serde(default)]
         .               pub(crate) prevent_combat_damage_except: Option<crate::card::SelectionRequirement>,
         .               /// CR 614.9 / 615 — creatures whose combat damage is prevented in both
         .               /// directions for the rest of the turn (Maze of Ith: "prevent all combat
-- line 483 ----------------------------------------
-- line 520 ----------------------------------------
         .               /// combat damage. Cleared when combat ends.
         .               #[serde(default)]
         .               pub(crate) blocked_attackers: Vec<CardId>,
         .               /// CR 702.22c-f — the attacking bands declared this combat, each a list of
         .               /// still-attacking members. A band lasts for the rest of combat even if a
         .               /// member loses banding (CR 702.22e); a creature removed from combat drops
         .               /// out of its band (CR 702.22f). Cleared when combat ends.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) attack_bands: Vec<Vec<CardId>>,
   801,088 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 614.13-style ETB-control replacement (Gather Specimens): seats
         .               /// whose opponents' creatures enter under their control instead this
         .               /// turn. Cleared at cleanup.
         .               #[serde(default)]
         .               pub creature_etb_steal_this_turn: Vec<usize>,
         .               /// Players who have paid the Leonin Arbiter search tax this turn
         .               /// (covers further searches until end of turn). Cleared at cleanup.
         .               #[serde(default)]
         .               pub(crate) search_tax_paid_this_turn: Vec<usize>,
         .               /// "Spells your opponents cast cost {N} more until your next turn"
         .               /// (Elspeth Conquers Death II). Each entry taxes matching spells cast by
         .               /// opponents of `controller`; cleared at `controller`'s untap.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub turn_scoped_spell_taxes: Vec<TurnScopedSpellTax>,
   951,292 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 615.7 — sources whose damage is prevented this turn (Burrenton
         .               /// Forge-Tender's chosen source, Hallow's life refund, Awe Strike's single
         .               /// instance per CR 615.8). Cleared at cleanup.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) damage_prevented_sources: Vec<crate::game::types::PreventedSource>,
   826,122 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 614 — turn-scoped replacements of what a land tap produces
         .               /// (Pale Moon, Harvest Mage). Applied at the mana-ability chokepoint;
         .               /// cleared at cleanup.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) land_mana_replacements_this_turn: Vec<crate::game::types::LandManaReplacement>,
   725,986 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 614 — seats whose spells and abilities add `Color` instead of any
         .               /// other colour for the rest of the turn, and who may spend that colour as
         .               /// any colour (False Dawn). Cleared at cleanup.
         .               pub(crate) colored_mana_becomes_this_turn: Vec<(usize, crate::mana::Color)>,
         .               /// `(blocker, attacker)` pairs declared this turn, kept off the permanents
         .               /// so it survives the blocker's death — "destroy it and all creatures it
         .               /// blocked this turn" (Defiant Vanguard). Cleared at cleanup.
         .               #[serde(default)]
-- line 560 ----------------------------------------
-- line 698 ----------------------------------------
         .               /// Registered replacement effects (Phase H — Commander prerequisite).
         .               /// Walked by zone-change paths (`place_card_in_dest`,
         .               /// `remove_from_battlefield_to_*`) at placement time; a matching
         .               /// entry rewrites the destination zone.
         .               ///
         .               /// `#[serde(default)]` so snapshots written before this field
         .               /// existed deserialize cleanly as empty (no replacements active).
         .               #[serde(default)]
    75,102 ( 0.00%)      pub replacement_effects: Vec<crate::replacement::ReplacementEffect>,
   801,088 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// Per-commander cast-from-command-zone counter (Phase L).
         .               /// Keyed by the commander's `CardId`; each entry tracks how many
         .               /// times that commander has been cast from the command zone this
         .               /// game. The commander tax is `{2}` × this value, added as
         .               /// generic mana on top of the printed cost (CR 903.8).
         .               ///
         .               /// `#[serde(default)]` for snapshot back-compat.
         .               #[serde(default)]
-- line 714 ----------------------------------------
-- line 780 ----------------------------------------
         .               /// CR 701.15g — creatures whose damage this turn denies its victim
         .               /// regeneration (Runesword). Cleared at cleanup.
         .               #[serde(default)]
         .               pub(crate) damage_denies_regen_eot: crate::game::types::IdSet<CardId>,
         .               /// "That spell gains [keywords]" grants onto spells still on the stack
         .               /// (Judith, Carnage Connoisseur). Consulted when a spell starts resolving
         .               /// and dropped once it leaves the stack.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) spell_keyword_grants: Vec<(CardId, crate::card::Keyword)>,
   851,156 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// Temporary control changes awaiting reversion (Act of Treason /
         .               /// Threaten / Tempted by the Oriq). `Effect::GainControl` with a
         .               /// non-`Permanent` duration records the controller the permanent had
         .               /// immediately before the steal so control snaps back when the
         .               /// duration ends (CR 800.4 control-changing effects). `#[serde(default)]`
         .               /// for snapshot back-compat.
         .               #[serde(default)]
         .               pub(crate) temporary_control: Vec<TempControl>,
         .               /// CR 508.1a/d — a per-seat next-turn attack mandate (Oracle en-Vec): on
         .               /// `seat`'s next turn the listed creatures attack if able and no other
         .               /// creature they control may attack; at that turn's end step each listed
         .               /// creature that didn't attack is destroyed. Armed when `seat` becomes the
         .               /// active player and consumed at that turn's end step.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub(crate) attack_mandates: Vec<AttackMandate>,
   801,088 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// Temporary "becomes a copy" definition swaps awaiting reversion
         .               /// (`Effect::BecomeCopyOfFor`, CR 707.2). Records the pre-copy
         .               /// definition; the swap snaps back when the duration ends, mirroring
         .               /// `temporary_control`. Entries whose card left the battlefield are
         .               /// dropped (a new object keeps nothing). `#[serde(default)]` for
         .               /// snapshot back-compat.
         .               #[serde(default)]
    75,102 ( 0.00%)      pub temporary_copies: Vec<TempCopy>,
   851,156 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (25,034x)
         .               /// CR 702.143b — cards foretold this turn can't be cast from exile until
         .               /// a later turn. Tracks the cards a player foretold during the current
         .               /// turn; cleared at cleanup. `#[serde(default)]` for snapshot back-compat.
         .               #[serde(default)]
         .               pub foretold_this_turn: crate::game::types::IdSet<CardId>,
         .               /// CR 702.170 — cards currently plotted (exiled face-up, castable from
         .               /// exile without paying their mana cost on a later turn).
         .               /// `#[serde(default)]` for snapshot back-compat.
-- line 819 ----------------------------------------
-- line 1920 ----------------------------------------
         .               fn deref(&self) -> &ColdState {
         .                   &self.cold
         .               }
         .           }
         .           
         .           /// Writing any cold field unshares the whole group once — see [`ColdState`].
         .           impl std::ops::DerefMut for GameState {
         .               fn deref_mut(&mut self) -> &mut ColdState {
   575,520 ( 0.02%)          &mut self.cold
27,363,460 ( 0.88%)  => /home/user/Crabomination/crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut (7,456x)
         .               }
         .           }
         .           
         .           /// One seat's pending next-turn attack mandate — see
         .           /// `GameState.attack_mandates`.
         .           #[derive(Debug, Clone, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
         .           pub struct AttackMandate {
         .               pub seat: usize,
-- line 1936 ----------------------------------------
-- line 2003 ----------------------------------------
         .           }
         .           
         .           /// Manual `Clone` impl so the bot can dry-run an action against a copy
         .           /// of the state without committing it. `Box<dyn Decider>` blocks the
         .           /// derive — we round-trip through `DeciderKind`. Custom deciders not
         .           /// modeled by the kind enum collapse to `AutoDecider` on clone, which
         .           /// is fine for the dry-run use case (we discard the clone immediately).
         .           impl Clone for GameState {
   336,020 ( 0.01%)      fn clone(&self) -> Self {
14,684,074 ( 0.47%)          Self {
 9,744,114 ( 0.31%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (100,806x)
         .                       players: self.players.clone(),
    33,602 ( 0.00%)              attack_option: self.attack_option,
    33,602 ( 0.00%)              range_of_influence: self.range_of_influence,
    33,602 ( 0.00%)              attack_adjacent_only: self.attack_adjacent_only,
    33,602 ( 0.00%)              deploy_creatures: self.deploy_creatures,
    67,204 ( 0.00%)              battlefield: self.battlefield.clone(),
    67,204 ( 0.00%)              phased_out: self.phased_out.clone(),
    67,204 ( 0.00%)              exile: self.exile.clone(),
    67,204 ( 0.00%)              stack: self.stack.clone(),
    33,602 ( 0.00%)              step: self.step,
    33,602 ( 0.00%)              active_player_idx: self.active_player_idx,
    33,602 ( 0.00%)              turn_number: self.turn_number,
         .                       rng: self.rng.clone(),
    33,602 ( 0.00%)              truce_until_turn: self.truce_until_turn,
    33,602 ( 0.00%)              game_over: self.game_over,
    67,204 ( 0.00%)              mandatory_loop_watch: self.mandatory_loop_watch,
   201,612 ( 0.01%)              free_activation_watch: self.free_activation_watch,
    33,602 ( 0.00%)              priority: self.priority.clone(),
    67,204 ( 0.00%)              continuous_effects: self.continuous_effects.clone(),
         .                       next_effect_timestamp: self.next_effect_timestamp,
         .                       next_id: self.next_id,
         .                       attacking: self.attacking.clone(),
         .                       block_map: self.block_map.clone(),
         .                       combat_damage_order: self.combat_damage_order.clone(),
         .                       combat_damage_assignment: self.combat_damage_assignment.clone(),
    67,204 ( 0.00%)              combat_damage_plan_step: self.combat_damage_plan_step,
    33,602 ( 0.00%)              blockers_declared: self.blockers_declared,
    33,602 ( 0.00%)              skip_first_draw: self.skip_first_draw,
    67,204 ( 0.00%)              spells_cast_this_turn: self.spells_cast_this_turn,
         .                       noncreature_spells_cast_this_turn: self.noncreature_spells_cast_this_turn,
         .                       mana_spent_on_spells_this_turn: self.mana_spent_on_spells_this_turn,
         .                       expend_prev_total: self.expend_prev_total,
    67,204 ( 0.00%)              casting_from_library_top: self.casting_from_library_top,
    67,204 ( 0.00%)              spells_cast_last_turn: self.spells_cast_last_turn,
         .                       permanents_to_graveyard_this_turn: self.permanents_to_graveyard_this_turn,
   100,806 ( 0.00%)              delayed_triggers: self.delayed_triggers.clone(),
 1,075,264 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (33,602x)
    33,602 ( 0.00%)              sacrificed_power: self.sacrificed_power,
         .                       revealed_for_cost_power: self.revealed_for_cost_power,
         .                       sacrificed_total_power: self.sacrificed_total_power,
         .                       sacrificed_count: self.sacrificed_count,
    33,602 ( 0.00%)              sacrificed_was_artifact: self.sacrificed_was_artifact,
         .                       sacrificed_was_outlaw: self.sacrificed_was_outlaw,
         .                       sacrificed_was_vehicle: self.sacrificed_was_vehicle,
         .                       sacrificed_card: self.sacrificed_card,
         .                       last_discarded_was_multicolored: self.last_discarded_was_multicolored,
         .                       last_discarded_card_types: self.last_discarded_card_types,
    33,602 ( 0.00%)              sacrificed_toughness: self.sacrificed_toughness,
         .                       sacrificed_mana_value: self.sacrificed_mana_value,
    33,602 ( 0.00%)              exiled_for_cost_mana_value: self.exiled_for_cost_mana_value,
    33,602 ( 0.00%)              prevention_source_color_scratch: self.prevention_source_color_scratch,
         .                       last_discarded_mana_value: self.last_discarded_mana_value,
   134,408 ( 0.00%)              last_revealed_from_hand: self.last_revealed_from_hand,
         .                       cost_discarded_mana_value: self.cost_discarded_mana_value,
         .                       block_poison_this_turn: self.block_poison_this_turn,
         .                       tapped_for_cost_power: self.tapped_for_cost_power,
         .                       life_gain_punish_this_turn: self.life_gain_punish_this_turn,
         .                       trigger_event_amount_scratch: self.trigger_event_amount_scratch,
         .                       trigger_event_player_scratch: self.trigger_event_player_scratch,
         .                       activation_mana_colors_scratch: self.activation_mana_colors_scratch.clone(),
         .                       target_slots_scratch: self.target_slots_scratch.clone(),
         .                       last_created_token: self.last_created_token,
         .                       last_die_roll: self.last_die_roll,
         .                       extra_cast_reduction: self.extra_cast_reduction,
         .                       cast_paid_uncounterable: self.cast_paid_uncounterable,
         .                       cast_kick_count: self.cast_kick_count,
         .                       last_created_tokens: self.last_created_tokens.clone(),
         .                       last_moved_cards: self.last_moved_cards.clone(),
    67,204 ( 0.00%)              cards_discarded_this_resolution: self.cards_discarded_this_resolution,
         .                       cards_drawn_this_resolution: self.cards_drawn_this_resolution,
         .                       energy_paid_this_resolution: self.energy_paid_this_resolution,
         .                       permanents_returned_this_resolution: self.permanents_returned_this_resolution,
    33,602 ( 0.00%)              permanents_tapped_this_resolution: self.permanents_tapped_this_resolution,
         .                       cards_revealed_this_resolution: self.cards_revealed_this_resolution,
         .                       creature_cards_discarded_this_resolution: self.creature_cards_discarded_this_resolution,
         .                       greatest_discarded_mv_this_resolution: self.greatest_discarded_mv_this_resolution,
         .                       cards_discarded_per_player_this_resolution: self.cards_discarded_per_player_this_resolution.clone(),
         .                       nonland_cards_discarded_per_player_this_resolution: self.nonland_cards_discarded_per_player_this_resolution.clone(),
    67,204 ( 0.00%)              shuffle_resolving_spell_into_library: self.shuffle_resolving_spell_into_library,
         .                       return_resolving_spell_to_hand: self.return_resolving_spell_to_hand,
         .                       exile_resolving_spell: self.exile_resolving_spell,
    33,602 ( 0.00%)              resolving_spell_library_from_top: self.resolving_spell_library_from_top,
    33,602 ( 0.00%)              resolving_spell_to_battlefield_transformed: self
         .                           .resolving_spell_to_battlefield_transformed,
         .                       end_turn_requested: self.end_turn_requested,
         .                       cipher_encode_pending: self.cipher_encode_pending,
         .                       haunt_pending: self.haunt_pending.clone(),
         .                       discarded_card_ids_this_resolution: self.discarded_card_ids_this_resolution.clone(),
         .                       suppress_extra_target_prompts: self.suppress_extra_target_prompts,
         .                       exiled_card_ids_this_resolution: self.exiled_card_ids_this_resolution.clone(),
         .                       permanents_destroyed_this_resolution: self.permanents_destroyed_this_resolution,
-- line 2102 ----------------------------------------
-- line 2103 ----------------------------------------
         .                       destroyed_this_resolution: self.destroyed_this_resolution.clone(),
         .                       excess_damage_this_resolution: self.excess_damage_this_resolution,
         .                       creatures_died_this_resolution: self.creatures_died_this_resolution,
         .                       resolution_depth: self.resolution_depth,
         .                       resolution_targets: self.resolution_targets.clone(),
         .                       cost_sacrificed_batch: self.cost_sacrificed_batch.clone(),
         .                       damage_dealt_this_resolution: self.damage_dealt_this_resolution,
         .                       damaged_this_resolution: self.damaged_this_resolution.clone(),
    67,204 ( 0.00%)              countered_spell_mana_spent: self.countered_spell_mana_spent,
         .                       countered_spell_mana_value: self.countered_spell_mana_value,
         .                       chosen_number_this_resolution: self.chosen_number_this_resolution,
    67,204 ( 0.00%)              resolution_causer: self.resolution_causer,
         .                       nonland_cards_exiled_this_effect: self.nonland_cards_exiled_this_effect,
    67,204 ( 0.00%)              countered_spell_controller: self.countered_spell_controller,
    67,204 ( 0.00%)              accepting_player: self.accepting_player,
    33,602 ( 0.00%)              shared_team_turns: self.shared_team_turns,
    67,204 ( 0.00%)              playing_for_ante: self.playing_for_ante,
    67,204 ( 0.00%)              archenemy: self.archenemy,
    33,602 ( 0.00%)              permanents_enter_tapped_this_turn: self.permanents_enter_tapped_this_turn,
    33,602 ( 0.00%)              counters_removed_this_effect: self.counters_removed_this_effect,
         .                       counters_removed_as_cost: self.counters_removed_as_cost,
         .                       players_sacrificed_this_resolution: self.players_sacrificed_this_resolution.clone(),
         .                       cards_sacrificed_this_resolution: self.cards_sacrificed_this_resolution.clone(),
         .                       chosen_creature_type_scratch: self.chosen_creature_type_scratch,
         .                       chosen_creature_types_scratch: self.chosen_creature_types_scratch.clone(),
         .                       named_card_this_resolution: self.named_card_this_resolution.clone(),
         .                       names_this_resolution: self.names_this_resolution.clone(),
    33,602 ( 0.00%)              pending_cast_face: self.pending_cast_face,
         .                       pending_cast_sacrifices: self.pending_cast_sacrifices.clone(),
         .                       pending_cast_discards: self.pending_cast_discards.clone(),
         .                       pending_spree_modes: self.pending_spree_modes.clone(),
         .                       pending_cast_spend_float: self.pending_cast_spend_float,
         .                       pending_prepare_copies: self.pending_prepare_copies.clone(),
    67,204 ( 0.00%)              pending_landcycle_pick: self.pending_landcycle_pick,
    33,602 ( 0.00%)              pending_ability_sac_other: self.pending_ability_sac_other,
         .                       pending_ability_tap_other: self.pending_ability_tap_other,
         .                       pending_ability_exile_other: self.pending_ability_exile_other.clone(),
         .                       pending_ability_sac_any: self.pending_ability_sac_any.clone(),
   134,408 ( 0.00%)              decider: self.decider.kind().into_boxed(),
   134,408 ( 0.00%)  => /home/user/Crabomination/crabomination/src/decision.rs:<crabomination::decision::AutoDecider as crabomination::decision::Decider>::kind (33,602x)
         .                       pending_decision: self.pending_decision.clone(),
         .                       suspend_signal: self.suspend_signal.clone(),
         .                       stashed_resolution_answer: self.stashed_resolution_answer.clone(),
   100,806 ( 0.00%)              resolution_answer_log: self.resolution_answer_log.clone(),
 1,108,866 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (33,602x)
   100,806 ( 0.00%)              pending_cost_events: self.pending_cost_events.clone(),
 1,377,682 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (33,602x)
         .                       pending_permanent_deaths: self.pending_permanent_deaths.clone(),
         .                       pending_control_changes: self.pending_control_changes.clone(),
    67,204 ( 0.00%)              prevent_combat_damage_this_turn: self.prevent_combat_damage_this_turn,
    67,204 ( 0.00%)              nonland_permanent_left_bf_this_turn: self.nonland_permanent_left_bf_this_turn,
    33,602 ( 0.00%)              mana_production_multiplier: self.mana_production_multiplier,
         .                       resolving_source: self.resolving_source.clone(),
         .                       in_layer_gather: std::sync::atomic::AtomicBool::new(false),
         .                       layer_freeze: LayerFreeze::default(),
    67,204 ( 0.00%)              additional_combat_phases: self.additional_combat_phases,
    67,204 ( 0.00%)              combat_chooser: self.combat_chooser,
         .                       additional_post_main_combats: self.additional_post_main_combats,
         .                       combat_phases_this_turn: self.combat_phases_this_turn,
         .                       additional_end_steps: self.additional_end_steps,
         .                       end_steps_this_turn: self.end_steps_this_turn,
         .                       additional_upkeep_steps: self.additional_upkeep_steps,
         .                       upkeep_steps_this_turn: self.upkeep_steps_this_turn,
   134,408 ( 0.00%)              damage_becomes_this_turn: self.damage_becomes_this_turn,
         .                       vote_controller_this_turn: self.vote_controller_this_turn,
         .                       source_name_scratch: self.source_name_scratch,
         .                       monarch_at_turn_start: self.monarch_at_turn_start,
         .                       creature_combat_damage_doublers: self.creature_combat_damage_doublers,
         .                       token_minting_source: self.token_minting_source,
         .                       damage_cant_be_prevented_this_turn: self.damage_cant_be_prevented_this_turn,
         .                       attack_tax_this_turn: self.attack_tax_this_turn,
         .                       block_tax_this_turn: self.block_tax_this_turn,
-- line 2171 ----------------------------------------
-- line 2174 ----------------------------------------
         .                       creature_deaths_drain_toughness_this_turn: self.creature_deaths_drain_toughness_this_turn,
         .                       no_search_this_turn: self.no_search_this_turn,
         .                       current_voter: self.current_voter,
         .                       graveyard_play_pooled_for: self.graveyard_play_pooled_for,
         .                       controlled_by: self.controlled_by.clone(),
         .                       next_replacement_id: self.next_replacement_id,
         .                       died_card_snapshots: self.died_card_snapshots.clone(),
         .                       leaves_bf_lki: self.leaves_bf_lki.clone(),
    67,204 ( 0.00%)              resolving_lki_source: self.resolving_lki_source,
         .                       resolving_lki_subject: self.resolving_lki_subject,
    67,204 ( 0.00%)              subgame_depth: self.subgame_depth,
    67,204 ( 0.00%)              resolving_spell_lifelink_seat: self.resolving_spell_lifelink_seat,
    33,602 ( 0.00%)              resolving_spell_caster: self.resolving_spell_caster,
         .                       resolving_spell_snapshot: self.resolving_spell_snapshot.clone(),
    67,204 ( 0.00%)              resolving_spell_deathtouch_seat: self.resolving_spell_deathtouch_seat,
    67,204 ( 0.00%)              spell_damage_trigger_fired: self.spell_damage_trigger_fired,
         .                       in_draw_double: self.in_draw_double,
         .                       in_turn_based_draw: self.in_turn_based_draw,
         .                       in_draw_redirect: self.in_draw_redirect,
         .                       in_damage_redirect: self.in_damage_redirect,
         .                       in_chains_replacement: self.in_chains_replacement,
         .                       exile_resolving_spell_with_countdown: self.exile_resolving_spell_with_countdown,
         .                       in_token_replacement: self.in_token_replacement,
    67,204 ( 0.00%)              monarch: self.monarch,
    67,204 ( 0.00%)              initiative: self.initiative,
    33,602 ( 0.00%)              day_night: self.day_night,
    67,204 ( 0.00%)              previous_turn_active: self.previous_turn_active,
    67,204 ( 0.00%)              current_turn_is_extra: self.current_turn_is_extra,
    67,204 ( 0.00%)              sector_block_lock_turn: self.sector_block_lock_turn,
    67,204 ( 0.00%)              chosen_sector: self.chosen_sector,
    33,602 ( 0.00%)              cold: self.cold.clone(),
         .                   }
   302,418 ( 0.01%)      }
         .           }
         .           
         .           
         .           /// CR 706 — the copy-relevant snapshot of a spell taken as it starts resolving,
         .           /// so a mid-resolution "copy this spell" rider still works after the stack
         .           /// entry has been popped (`Effect::MayCopyThisSpell`).
   128,368 ( 0.00%)  #[derive(Debug, Clone)]
         .           pub(crate) struct ResolvingSpell {
         .               pub definition: std::sync::Arc<crate::card::CardDefinition>,
         .               pub target: Option<crate::game::types::Target>,
         .               pub additional_targets: Vec<crate::game::types::Target>,
         .               pub mode: Option<usize>,
    32,092 ( 0.00%)      pub x_value: u32,
         .               pub converged_value: u32,
         .           }
         .           
         .           impl GameState {
         .               /// Spend `amount` {E} from player `p`, clamped to what they have, and add
         .               /// it to `energy_spent_this_turn` (the tally behind "paid or lost N+ {E}
         .               /// this turn" gates). All energy-cost chokepoints route through here.
         .               pub fn spend_energy(&mut self, p: usize, amount: u32) {
-- line 2227 ----------------------------------------
-- line 2230 ----------------------------------------
         .                   self.players[p].energy_spent_this_turn =
         .                       self.players[p].energy_spent_this_turn.saturating_add(amount);
         .               }
         .           
         .               /// Create a fresh game.  `players` must have at least 2 entries. Defaults
         .               /// to 20-life, 2-player rules; call [`apply_format`] (or set
         .               /// `skip_first_draw` / per-player `life` directly) to configure the game
         .               /// for a specific format or player count.
       104 ( 0.00%)      pub fn new(players: Vec<Player>) -> Self {
         .                   let n = players.len();
         .                   // Default: one singleton team per seat (free-for-all semantics).
         .                   // Team formats reshape this via `assign_teams`.
         .                   let teams = (0..n)
         .                       .map(|i| crate::team::Team {
         .                           id: crate::team::TeamId(i),
        16 ( 0.00%)                  members: vec![i],
         .                           shared_life: None,
         .                       })
         .                       .collect();
     1,800 ( 0.00%)          Self {
     2,328 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (24x)
         .                       players,
         .                       attack_option: AttackOption::default(),
         .                       range_of_influence: None,
         .                       attack_adjacent_only: false,
         .                       deploy_creatures: false,
         8 ( 0.00%)              battlefield: CowBox::default(),
         8 ( 0.00%)              phased_out: CowBox::default(),
         8 ( 0.00%)              exile: CowBox::default(),
         8 ( 0.00%)              stack: CowBox::default(),
         .                       step: TurnStep::Untap,
         .                       active_player_idx: 0,
         .                       turn_number: 1,
         .                       rng: rng::GameRng::default(),
         .                       truce_until_turn: None,
         .                       game_over: None,
         .                       mandatory_loop_watch: (0, 0),
         .                       free_activation_watch: (0, None, 0),
         .                       priority: PriorityState::new(0),
         8 ( 0.00%)              continuous_effects: CowBox::default(),
         .                       next_effect_timestamp: 1,
         .                       next_id: 1,
         .                       attacking: Vec::new(),
         .                       block_map: HashMap::default(),
         .                       combat_damage_order: HashMap::default(),
         .                       combat_damage_assignment: HashMap::default(),
         .                       combat_damage_plan_step: None,
         .                       blockers_declared: false,
         .                       // Multiplayer (3+) doesn't skip the first draw — only the 2-player
         .                       // starting player does.
         8 ( 0.00%)              skip_first_draw: n <= 2,
         .                       spells_cast_this_turn: 0,
         .                       noncreature_spells_cast_this_turn: 0,
         .                       mana_spent_on_spells_this_turn: 0,
         .                       expend_prev_total: 0,
         .                       casting_from_library_top: None,
         .                       spells_cast_last_turn: 0,
         .                       permanents_to_graveyard_this_turn: 0,
         .                       delayed_triggers: Vec::new(),
-- line 2287 ----------------------------------------
-- line 2328 ----------------------------------------
         .                       nonland_cards_discarded_per_player_this_resolution: HashMap::default(),
         .                       shuffle_resolving_spell_into_library: false,
         .                       return_resolving_spell_to_hand: false,
         .                       exile_resolving_spell: false,
         .                       resolving_spell_library_from_top: None,
         .                       resolving_spell_to_battlefield_transformed: None,
         .                       end_turn_requested: false,
         .                       cipher_encode_pending: None,
         8 ( 0.00%)              haunt_pending: None,
         .                       discarded_card_ids_this_resolution: Vec::new(),
         .                       suppress_extra_target_prompts: None,
         .                       exiled_card_ids_this_resolution: Vec::new(),
         .                       permanents_destroyed_this_resolution: 0,
         .                       excess_damage_this_resolution: 0,
         .                       creatures_died_this_resolution: 0,
         .                       resolution_depth: 0,
         .                       resolution_targets: Vec::new(),
-- line 2344 ----------------------------------------
-- line 2372 ----------------------------------------
         .                       pending_cast_spend_float: None,
         .                       pending_prepare_copies: Vec::new(),
         .                       pending_landcycle_pick: None,
         .                       pending_ability_sac_other: None,
         .                       pending_ability_tap_other: None,
         .                       pending_ability_exile_other: None,
         .                       pending_ability_sac_any: None,
         .                       decider: Box::new(AutoDecider),
         8 ( 0.00%)              pending_decision: None,
        16 ( 0.00%)              suspend_signal: None,
        16 ( 0.00%)              stashed_resolution_answer: None,
         .                       resolution_answer_log: Vec::new(),
         .                       pending_cost_events: Vec::new(),
         .                       pending_permanent_deaths: Vec::new(),
         .                       pending_control_changes: Vec::new(),
         .                       prevent_combat_damage_this_turn: false,
         .                       nonland_permanent_left_bf_this_turn: false,
         .                       mana_production_multiplier: 1,
         8 ( 0.00%)              resolving_source: None,
         .                       in_layer_gather: std::sync::atomic::AtomicBool::new(false),
         .                       layer_freeze: LayerFreeze::default(),
         .                       additional_combat_phases: 0,
         .                       combat_chooser: None,
         .                       additional_post_main_combats: 0,
         .                       combat_phases_this_turn: 0,
         .                       additional_end_steps: 0,
         .                       end_steps_this_turn: 0,
-- line 2398 ----------------------------------------
-- line 2434 ----------------------------------------
         .                       in_token_replacement: false,
         .                       monarch: None,
         .                       initiative: None,
         .                       day_night: None,
         .                       previous_turn_active: None,
         .                       current_turn_is_extra: false,
         .                       sector_block_lock_turn: None,
         .                       chosen_sector: None,
     2,928 ( 0.00%)              cold: CowBox::new(ColdState { teams, ..Default::default() }),
         .                   }
        72 ( 0.00%)      }
         .           
         .               /// CR 725 — make `player` the monarch. No-op if they already are; emits
         .               /// `MonarchChanged` on a real change.
         .               pub fn set_monarch(&mut self, player: usize, events: &mut Vec<GameEvent>) {
         .                   if self.monarch == Some(player) {
         .                       return;
         .                   }
         .                   self.monarch = Some(player);
-- line 2452 ----------------------------------------
-- line 2508 ----------------------------------------
         .                       level: self.players[player].ring_temptations,
         .                       bearer: self.players[player].ring_bearer,
         .                   });
         .               }
         .           
         .               /// CR 701.54a/b — `player`'s current Ring-bearer, validated: the stored
         .               /// designation only counts while that creature is on the battlefield and
         .               /// still controlled by `player` (a control change clears the designation).
    60,288 ( 0.00%)      pub fn effective_ring_bearer(&self, player: usize) -> Option<CardId> {
         .                   let id = self.players[player].ring_bearer?;
         .                   self.battlefield
         .                       .iter()
         .                       .find(|c| c.id == id && c.controller == player && c.definition.is_creature())
         .                       .map(|c| c.id)
    60,288 ( 0.00%)      }
         .           
         .               /// CR 731 — set the game's day/night designation, emitting
         .               /// `DayNightChanged` on a real change.
         .               /// CR 712 — flip one DFC permanent to its other face in place. The object
         .               /// is unchanged (counters/tapped/attachments persist); fires `Transformed`.
         .               /// CR 712.9/712.10 — nothing happens if the permanent isn't a DFC, or if
         .               /// the face it would turn to is an instant or sorcery face.
         .               /// The permanents a layer-static's `applies_to` selector covers, resolved
-- line 2530 ----------------------------------------
-- line 2670 ----------------------------------------
         .               }
         .           
         .               /// CR 502.2 — the day/night turn-based check run as each turn begins.
         .               /// If it's day and the previous turn's active player cast no spells, it
         .               /// becomes night; if it's night and they cast two or more, it becomes
         .               /// day. No effect while the game is neither day nor night.
         .               pub fn check_day_night_transition(&mut self, events: &mut Vec<GameEvent>) {
         .                   use crate::game::types::DayNight;
     5,154 ( 0.00%)          let Some(current) = self.day_night else { return };
         .                   let Some(prev) = self.previous_turn_active else { return };
         .                   let cast = self.players.get(prev).map(|p| p.spells_cast_this_turn).unwrap_or(0);
         .                   match current {
         .                       DayNight::Day if cast == 0 => self.set_day_night(DayNight::Night, events),
         .                       DayNight::Night if cast >= 2 => self.set_day_night(DayNight::Day, events),
         .                       _ => {}
         .                   }
         .               }
-- line 2686 ----------------------------------------
-- line 2699 ----------------------------------------
         .                       .map(Vec::as_slice)
         .                       .unwrap_or(&[])
         .               }
         .           
         .               /// Triggered abilities granted to `card` by battlefield
         .               /// `StaticEffect::GrantTriggeredAbility` statics ("All artifacts have
         .               /// '…'" — Kataki, War's Wage). Each fires as though printed on the
         .               /// matching permanent (CR 702.6e-style source binding).
   152,800 ( 0.00%)      pub fn statics_granted_triggers_for(
         .                   &self,
         .                   card: &CardInstance,
         .               ) -> Vec<crate::card::TriggeredAbility> {
   106,960 ( 0.00%)          self.statics_granted_triggers_with(card, &self.trigger_grant_sources())
 7,652,044 ( 0.25%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::trigger_grant_sources (15,280x)
   550,080 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_with (15,280x)
   122,240 ( 0.00%)      }
         .           
         .               /// The live `GrantTriggeredAbility` statics on the board, each with its
         .               /// gating wrappers already peeled (Threshold, "while your turn", … —
         .               /// Decaying Soil, Cephalid Sage) and its filter already resolved against
         .               /// the source's named card.
         .               ///
         .               /// Hoisted out of [`statics_granted_triggers_for`] so a caller asking
         .               /// about every battlefield permanent — the trigger dispatcher does, once
         .               /// per event batch — peels and resolves the grants once instead of once
         .               /// per permanent. On the overwhelmingly common board that grants nothing
         .               /// this is a single cheap pass and every per-card call becomes a no-op.
         .               ///
         .               /// [`statics_granted_triggers_for`]: Self::statics_granted_triggers_for
   297,420 ( 0.01%)      pub(crate) fn trigger_grant_sources(&self) -> Vec<TriggerGrant<'_>> {
         .                   let mut out = Vec::new();
         .                   // CR 315.5 — a face-up conspiracy grants from the command zone too.
         .                   for src in self.all_static_sources() {
         .                       for sa in &src.definition.static_abilities {
         .                           if let Some(crate::effect::StaticEffect::GrantTriggeredAbility {
         .                               filter,
         .                               ability,
         .                           }) = self.active_static(&sa.effect, src)
-- line 2734 ----------------------------------------
-- line 2737 ----------------------------------------
         .                                   filter: filter.resolve_named_by_source(src.named_card.as_deref()),
         .                                   ability,
         .                                   controller: src.controller,
         .                                   source: src.id,
         .                               });
         .                           }
         .                       }
         .                   }
   148,710 ( 0.00%)          out
   237,936 ( 0.01%)      }
         .           
         .               /// [`statics_granted_triggers_for`] against a prebuilt grant list.
         .               ///
         .               /// [`statics_granted_triggers_for`]: Self::statics_granted_triggers_for
   137,520 ( 0.00%)      pub(crate) fn statics_granted_triggers_with(
         .                   &self,
         .                   card: &CardInstance,
         .                   grants: &[TriggerGrant<'_>],
         .               ) -> Vec<crate::card::TriggeredAbility> {
         .                   let mut out = Vec::new();
         .                   for g in grants {
         .                       if self.evaluate_requirement_static(
         .                           &g.filter,
-- line 2759 ----------------------------------------
-- line 2762 ----------------------------------------
         .                           Some(g.source),
         .                       ) {
         .                           out.push((*g.ability).clone());
         .                       }
         .                   }
         .                   // CR 611.2 — turn-scoped floating watchers ("whenever a creature blocks
         .                   // this turn, …"). Unlike an EOT trigger grant, these reach permanents
         .                   // that enter after the granting spell resolved.
    15,280 ( 0.00%)          for (filter, ability) in &self.turn_granted_triggers {
         .                       if self.evaluate_requirement_static(
         .                           filter,
         .                           &Target::Permanent(card.id),
         .                           card.controller,
         .                           None,
         .                       ) {
         .                           out.push(ability.clone());
         .                       }
         .                   }
         .                   // CR 721.2a — a station card's own `{N+}` triggered-ability striations,
         .                   // active only while it has at least `min` charge counters.
    30,560 ( 0.00%)          if !card.definition.station.is_empty() {
         .                       let charges = card.counter_count(crate::card::CounterType::Charge);
         .                       for band in card.definition.station.iter().filter(|b| charges >= b.min) {
         .                           out.extend(band.triggers.iter().cloned());
         .                       }
         .                   }
    76,400 ( 0.00%)          out
   122,240 ( 0.00%)      }
         .           
         .               /// The static-granted triggered abilities a permanent carried at the
         .               /// moment it left the battlefield. Same walk as
         .               /// `statics_granted_triggers_for`, but the grant filter is evaluated
         .               /// against the death LKI snapshot (the card is no longer on the
         .               /// battlefield, so a `Target::Permanent` lookup would miss).
         .               pub(crate) fn statics_granted_dying_triggers(
         .                   &self,
         .                   snap: &CardInstance,
         .               ) -> Vec<crate::card::TriggeredAbility> {
         .                   let mut out = Vec::new();
         .                   // CR 603.10a — the dying permanent's *own* self-granting static
         .                   // ("Threshold — this creature has 'when this dies, …'") is read off
         .                   // the snapshot; it is no longer on the battlefield to be walked.
     3,806 ( 0.00%)          let sources = self
         .                       .battlefield
         .                       .iter()
     3,806 ( 0.00%)              .chain(std::iter::once(snap).filter(|s| self.battlefield_find(s.id).is_none()));
         .                   for src in sources {
         .                       for sa in &src.definition.static_abilities {
         .                           if let Some(crate::effect::StaticEffect::GrantTriggeredAbility {
         .                               filter,
         .                               ability,
         .                           }) = self.active_static(&sa.effect, src)
         .                               && self.evaluate_requirement_on_card(
         .                                   &filter.resolve_is_source(src.id == snap.id),
-- line 2815 ----------------------------------------
-- line 2816 ----------------------------------------
         .                                   snap,
         .                                   src.controller,
         .                               )
         .                           {
         .                               out.push((**ability).clone());
         .                           }
         .                       }
         .                   }
    15,224 ( 0.00%)          out
         .               }
         .           
         .               /// Triggered abilities granted to `card` by Equipment attached to it
         .               /// (CR 702.6e). Only the `triggers_on_equipment == false` abilities are
         .               /// surfaced here — they fire as though printed on the equipped creature
         .               /// (Tarrian's Soulcleaver's "whenever another artifact/creature dies, put
         .               /// a +1/+1 counter on equipped creature"). The `triggers_on_equipment`
         .               /// (Jitte-style) abilities fire off the Equipment via the dedicated
-- line 2832 ----------------------------------------
-- line 2865 ----------------------------------------
         .               /// [`equip_granted_triggers_for`]: Self::equip_granted_triggers_for
         .               pub(crate) fn equip_granted_triggers_with(
         .                   &self,
         .                   card: &CardInstance,
         .                   sources: &[(CardId, &[crate::card::TriggeredAbility])],
         .               ) -> Vec<crate::card::TriggeredAbility> {
         .                   let mut out = Vec::new();
         .                   for (host, abilities) in sources {
   416,814 ( 0.01%)              if *host == card.id {
    38,920 ( 0.00%)                  out.extend(abilities.iter().cloned());
         .                       }
         .                   }
 1,390,490 ( 0.04%)          out
         .               }
         .           
         .               /// The four board-level facts [`dispatch_triggers_for_events`] asks for on
         .               /// every dispatch, gathered in one pass over the battlefield instead of
         .               /// four. Same device as `stack.rs`'s `SbaBoardScan`; the debug cross-check
         .               /// below keeps it from drifting from the functions it fuses.
         .               ///
         .               /// [`dispatch_triggers_for_events`]: Self::dispatch_triggers_for_events
   475,024 ( 0.02%)      pub(crate) fn dispatch_board_scan(&self) -> DispatchScan<'_> {
         .                   use crate::effect::StaticEffect;
   475,024 ( 0.02%)          let mut scan = DispatchScan {
         .                       dies_suppressed: false,
         .                       strip_on_battlefield: false,
         .                       trigger_grants: Vec::new(),
         .                       equip_grants: Vec::new(),
         .                   };
    59,378 ( 0.00%)          for card in self.battlefield.iter() {
         .                       let def = &card.definition;
 3,201,054 ( 0.10%)              if let Some(host) = card.attached_to
         .                           && let Some(bonus) = def.equipped_bonus.as_ref()
         .                       {
    38,604 ( 0.00%)                  if !bonus.triggers_on_equipment {
         .                               scan.equip_grants.push((host, bonus.triggered_abilities.as_slice()));
         .                           }
    57,906 ( 0.00%)                  scan.strip_on_battlefield |= bonus.remove_abilities;
         .                       }
         .                       for sa in &def.static_abilities {
         .                           scan.dies_suppressed |= matches!(
         .                               sa.effect,
         .                               StaticEffect::SuppressCreatureEtbTriggers { also_dies: true, .. }
         .                           );
         .                           scan.strip_on_battlefield |= static_effect_strips_abilities(&sa.effect);
         .                           if let Some(StaticEffect::GrantTriggeredAbility { filter, ability }) =
-- line 2910 ----------------------------------------
-- line 2913 ----------------------------------------
         .                               scan.trigger_grants.push(TriggerGrant {
         .                                   filter: filter.resolve_named_by_source(card.named_card.as_deref()),
         .                                   ability,
         .                                   controller: card.controller,
         .                                   source: card.id,
         .                               });
         .                           }
         .                       }
 2,134,036 ( 0.07%)              scan.strip_on_battlefield |= def
         .                           .station
         .                           .iter()
         .                           .any(|band| band.statics.iter().any(static_effect_strips_abilities));
         .                   }
         .                   // CR 315.5 — a face-up conspiracy grants from the command zone too.
         .                   for p in &self.players {
   237,512 ( 0.01%)              for src in p.command.iter().filter(|c| c.command_zone_abilities_active()) {
         .                           for sa in &src.definition.static_abilities {
         .                               if let Some(StaticEffect::GrantTriggeredAbility { filter, ability }) =
         .                                   self.active_static(&sa.effect, src)
         .                               {
         .                                   scan.trigger_grants.push(TriggerGrant {
         .                                       filter: filter.resolve_named_by_source(src.named_card.as_deref()),
         .                                       ability,
         .                                       controller: src.controller,
-- line 2936 ----------------------------------------
-- line 2950 ----------------------------------------
         .                               .iter()
         .                               .map(|g| (g.source, g.controller, std::ptr::from_ref(g.ability)))
         .                               .eq(self
         .                                   .trigger_grant_sources()
         .                                   .iter()
         .                                   .map(|g| (g.source, g.controller, std::ptr::from_ref(g.ability)))),
         .                       "dispatch_board_scan drifted from the four walks it fuses",
         .                   );
   534,402 ( 0.02%)          scan
   475,024 ( 0.02%)      }
         .           
         .               /// The largest number of `seat`'s creatures sharing one creature type
         .               /// (CR 702.73a — changelings count toward every type). Powers
         .               /// `Value::GreatestSharedCreatureTypeCount` and Graxiplon's block
         .               /// restriction.
         .               pub fn greatest_shared_type_count(&self, seat: usize) -> usize {
         .                   use crate::card::{CardType, CreatureType, Keyword};
         .                   let mut tally: crate::fxhash::HashMap<CreatureType, usize> =
-- line 2967 ----------------------------------------
-- line 3033 ----------------------------------------
         .                               shared_life: Some(life),
         .                           })
         .                           .collect();
         .                   }
         .               }
         .           
         .               /// Number of players that have not been eliminated.
         .               pub fn alive_count(&self) -> usize {
    75,582 ( 0.00%)          self.players.iter().filter(|p| p.is_alive()).count()
         .               }
         .           
         .               /// Next non-eliminated seat strictly after `from` (wrapping). Returns
         .               /// `from` if no other alive players remain.
         .               pub fn next_alive_seat(&self, from: usize) -> usize {
         .                   let n = self.players.len();
         .                   for step in 1..=n {
   220,090 ( 0.01%)              let i = (from + step) % n;
    93,204 ( 0.00%)              if self.players[i].is_alive() {
         .                           return i;
         .                       }
         .                   }
         .                   from
         .               }
         .           
         .               /// Sort `seats` into APNAP order — active player first, then each other
         .               /// seat in turn order (CR 101.4). Used when a single effect affects
-- line 3058 ----------------------------------------
-- line 3081 ----------------------------------------
         .                   seats
         .               }
         .           
         .               // ── Team partitioning ─────────────────────────────────────────────────────
         .           
         .               /// Team that contains `seat`. Falls back to a virtual singleton
         .               /// `TeamId(seat)` when `teams` is empty (e.g. snapshots from before
         .               /// the field was added).
    21,328 ( 0.00%)      pub fn team_of(&self, seat: usize) -> crate::team::TeamId {
         .                   // Fast path for the layout `GameState::new` builds and every
         .                   // non-team format keeps: one singleton team per seat, in seat
         .                   // order. One index and one compare instead of a scan of every
         .                   // team's member list — `same_team` is called ~1.2 M times per six
         .                   // games and was 1.45 % of the program walking those `Vec`s.
         .                   // `assign_teams` rejects a seat appearing twice, so a hit here is
         .                   // the same team the scan below would find; the `debug_assert` is
         .                   // the audit.
         .                   if let Some(t) = self.teams.get(seat)
 1,241,160 ( 0.04%)              && t.members.len() == 1
 1,241,160 ( 0.04%)              && t.members[0] == seat
         .                   {
         .                       debug_assert_eq!(t.id, self.team_of_scan(seat), "team_of fast path drifted");
         .                       return t.id;
         .                   }
         .                   self.team_of_scan(seat)
    21,328 ( 0.00%)      }
         .           
         .               fn team_of_scan(&self, seat: usize) -> crate::team::TeamId {
         .                   for t in &self.teams {
         .                       if t.members.contains(&seat) {
         .                           return t.id;
         .                       }
         .                   }
         .                   crate::team::TeamId(seat)
-- line 3114 ----------------------------------------
-- line 3124 ----------------------------------------
         .                       }
         .                   }
         .                   Vec::new()
         .               }
         .           
         .               /// Seats on every team other than `seat`'s. Includes eliminated
         .               /// players; callers that need a live-only list should filter on
         .               /// `players[s].is_alive()` themselves.
    40,200 ( 0.00%)      pub fn opponents_of(&self, seat: usize) -> Vec<usize> {
     8,040 ( 0.00%)          if self.teams.is_empty() {
         .                       // No teams declared — treat every other seat as an opponent.
         .                       return (0..self.players.len()).filter(|&s| s != seat).collect();
         .                   }
         .                   let my_team = self.team_of(seat);
         .                   let mut out = Vec::new();
         .                   for t in &self.teams {
     8,040 ( 0.00%)              if t.id != my_team {
         .                           out.extend(t.members.iter().copied());
         .                       }
         .                   }
    20,100 ( 0.00%)          out
    36,180 ( 0.00%)      }
         .           
         .               /// True when `a` and `b` are on the same team. A seat is always its
         .               /// own teammate (returns true for `a == b`).
   510,156 ( 0.02%)      pub fn same_team(&self, a: usize, b: usize) -> bool {
 1,020,312 ( 0.03%)          a == b || self.team_of(a) == self.team_of(b)
   510,156 ( 0.02%)      }
         .           
         .               /// CR 809 — set the game up as an Emperor game of `teams` teams of
         .               /// `per_team` players each (three by default). Seats are dealt out team by
         .               /// team so each team sits together with its emperor in the middle
         .               /// (CR 809.2); ranges are 2 for emperors and 1 for generals (CR 809.3a),
         .               /// the deploy-creatures option is on (CR 809.3b), and attacks are limited
         .               /// to the seats immediately either side (CR 809.3c).
         .               pub fn set_emperor_variant(&mut self, teams: usize, per_team: usize) {
-- line 3159 ----------------------------------------
-- line 3251 ----------------------------------------
         .                       self.players[seat].loss_cause.get_or_insert(crate::player::LossCause::Other);
         .                   }
         .                   doomed
         .               }
         .           
         .               /// CR 801 — is the limited range of influence option on for this game?
         .               /// True when the table sets a default range or any seat carries its own.
         .               pub fn limited_range(&self) -> bool {
    83,858 ( 0.00%)          self.range_of_influence.is_some()
         .                       || self.players.iter().any(|p| p.range_of_influence.is_some())
         .               }
         .           
         .               /// CR 801.2 — is `other` within `observer`'s range of influence? Always
         .               /// true with the unlimited default (`range_of_influence == None`), and
         .               /// always true for `observer` itself (CR 801.2b).
     5,062 ( 0.00%)      pub fn player_in_range_of(&self, observer: usize, other: usize) -> bool {
    10,124 ( 0.00%)          if observer == other || !self.limited_range() {
         .                       return true;
         .                   }
         .                   match self.range_matrix.get(observer).and_then(|row| row.get(other)) {
         .                       Some(v) => *v,
         .                       // Not yet snapshotted (a state built mid-turn) — fall back to the
         .                       // live seating.
         .                       None => self.seats_within_range(observer).contains(&other),
         .                   }
    10,124 ( 0.00%)      }
         .           
         .               /// CR 801.2d — is the object `card` within `observer`'s range? An object is
         .               /// in range when its controller is.
         .               pub fn object_in_range_of(&self, observer: usize, card: CardId) -> bool {
         .                   if !self.limited_range() {
         .                       return true;
         .                   }
         .                   match self.find_card_anywhere(card) {
-- line 3284 ----------------------------------------
-- line 3301 ----------------------------------------
         .                   let len = living.len() as isize;
         .                   let n = (n as isize).min(len / 2);
         .                   (-n..=n)
         .                       .map(|d| living[((here as isize + d).rem_euclid(len)) as usize])
         .                       .collect()
         .               }
         .           
         .               /// CR 801.2c — snapshot every player's range as the turn begins.
     8,590 ( 0.00%)      pub fn refresh_range_matrix(&mut self) {
         .                   if !self.limited_range() {
         .                       self.range_matrix.clear();
         .                       return;
         .                   }
         .                   let len = self.players.len();
         .                   self.range_matrix = (0..len)
         .                       .map(|observer| {
         .                           let inside = self.seats_within_range(observer);
         .                           (0..len).map(|other| observer == other || inside.contains(&other)).collect()
         .                       })
         .                       .collect();
    10,308 ( 0.00%)      }
         .           
         .               /// CR 805.4a — the seats on the active team, in seat order. Just the
         .               /// active player outside the shared-team-turns option.
    15,462 ( 0.00%)      pub fn active_team_members(&self) -> Vec<usize> {
     3,436 ( 0.00%)          if !self.shared_team_turns {
     3,436 ( 0.00%)              return vec![self.active_player_idx];
         .                   }
         .                   let mut seats = vec![self.active_player_idx];
         .                   seats.extend(self.teammates(self.active_player_idx));
         .                   seats.sort_unstable();
         .                   seats
    15,462 ( 0.00%)      }
         .           
         .               /// CR 805.4a / 805.5a — is `seat` on the active team? Under the shared
         .               /// team turns option a teammate acts on the team's turn, so this (rather
         .               /// than `active_player_idx == seat`) gates sorcery-speed timing.
         .               pub fn on_active_team(&self, seat: usize) -> bool {
    14,926 ( 0.00%)          if self.shared_team_turns {
         .                       self.same_team(self.active_player_idx, seat)
         .                   } else {
     6,598 ( 0.00%)              self.active_player_idx == seat
         .                   }
         .               }
         .           
         .               // ── Life total helpers (Phase F) ──────────────────────────────────────
         .           
         .               /// Effective life total visible to `seat`. In 2HG (`Team.shared_life
         .               /// == Some(n)`) every member of the team sees the same number; in
         .               /// solo-team formats (1v1 / FFA / Commander) this is just the
         .               /// player's own `life` field. Callers checking lethal damage,
         .               /// "if you have ≤ X life" predicates, "the most life total" etc.
         .               /// should consult this rather than `players[seat].life`.
    45,812 ( 0.00%)      pub fn effective_life(&self, seat: usize) -> i32 {
         .                   if let Some(t) = self.teams.iter().find(|t| t.members.contains(&seat))
    91,624 ( 0.00%)              && let Some(shared) = t.shared_life
         .                   {
         .                       return shared;
         .                   }
    45,812 ( 0.00%)          self.players[seat].life
    91,624 ( 0.00%)      }
         .           
         .               /// CR 810.5 — poison counters are a shared team resource in Two-Headed
         .               /// Giant (identified here by the shared life pool): a team's poison total
         .               /// is the sum over its members. Solo teams just report the seat's own.
    21,134 ( 0.00%)      pub fn effective_poison(&self, seat: usize) -> u32 {
         .                   match self.teams.iter().find(|t| t.members.contains(&seat)) {
    42,268 ( 0.00%)              Some(t) if t.shared_life.is_some() => {
         .                           t.members.iter().map(|&m| self.players[m].poison_counters).sum()
         .                       }
    21,134 ( 0.00%)              _ => self.players[seat].poison_counters,
         .                   }
    42,268 ( 0.00%)      }
         .           
         .               /// CR 704.5c / 810.8d — the poison total that loses the game: ten for a
         .               /// solo player, fifteen for a shared-pool (2HG) team.
         .               pub fn poison_loss_threshold(&self, seat: usize) -> u32 {
    63,402 ( 0.00%)          match self.teams.iter().find(|t| t.members.contains(&seat)) {
    42,268 ( 0.00%)              Some(t) if t.shared_life.is_some() => 15,
         .                       _ => 10,
         .                   }
         .               }
         .           
         .               /// Number of Equipment currently attached to permanent `id` (CR 301.5).
         .               /// The single source of truth for equipped-state checks — `IsEquipped`,
         .               /// `EquippedByAtLeast`, `SourceIsEquipped`, and the per-Equipment CDA all
         .               /// route through here.
-- line 3387 ----------------------------------------
-- line 3400 ----------------------------------------
         .               /// Per-turn counters (`life_gained_this_turn`) are bumped on the
         .               /// *seat* receiving the change — they're a "you" payoff and the
         .               /// triggering side is still a specific player. For 2HG, CR 810.8
         .               /// also propagates the gain to teammates' "you gain life"
         .               /// triggers; that broader fan-out is handled at trigger-scope
         .               /// resolution time (`EventScope::YourControl` would need a
         .               /// team-aware variant), not here. This helper only owns the
         .               /// state-mutation half.
    33,392 ( 0.00%)      pub fn adjust_life(&mut self, seat: usize, delta: i32) -> i32 {
    12,522 ( 0.00%)          if delta == 0 {
         .                       return self.effective_life(seat);
         .                   }
         .                   // CR 119.7: if `seat` can't gain life and the delta would
         .                   // increase their life total, drop the gain on the floor. The
         .                   // 119.10 rider — "If a player gains 0 life, no life gain event
         .                   // would occur, and these effects won't apply" — is honored
         .                   // implicitly: the gain never happens, no LifeGained event is
         .                   // emitted, the `life_gained_this_turn` counter isn't bumped.
-- line 3417 ----------------------------------------
-- line 3420 ----------------------------------------
         .                   // flag (set by emblems / once-per-game effects) with the
         .                   // dynamic battlefield scan via `player_cannot_gain_life_now`
         .                   // (consults `StaticEffect::PlayerCannotGainLife` statics on
         .                   // the live battlefield).
         .                   // CR 614 — Tainted Remedy-style replacement: a would-be life *gain*
         .                   // becomes an equal life *loss* instead. Applied before the cannot-
         .                   // gain drop (the gain is replaced, not prevented) and re-routed as a
         .                   // negative delta so the loss honors cannot-lose-life / shared pools.
    12,522 ( 0.00%)          if delta > 0 && self.life_gain_becomes_loss_now(seat) {
         .                       return self.adjust_life(seat, -delta);
         .                   }
         .                   // CR 614 — Nefarious Lich: "if you would gain life, draw that many
         .                   // cards instead." The gain never happens.
     6,590 ( 0.00%)          if delta > 0 && self.life_gain_becomes_draw_now(seat) {
   331,530 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::life_gain_becomes_draw_now (1,318x)
         .                       let mut events = Vec::new();
         .                       for _ in 0..delta {
         .                           self.draw_one(seat, &mut events);
         .                       }
         .                       return self.effective_life(seat);
         .                   }
     6,590 ( 0.00%)          if delta > 0 && self.player_cannot_gain_life_now(seat) {
   321,356 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::player_cannot_gain_life_now (1,318x)
         .                       return self.effective_life(seat);
         .                   }
         .                   // CR 119.10 — a genuine life *gain* is increased by any active
         .                   // "you gain that much plus N" replacement (Honor Troll). Folded in
         .                   // before the gain applies so the bonus counts toward
         .                   // `life_gained_this_turn` and any downstream lifegain triggers.
         .                   let delta = if delta > 0 {
         .                       delta.saturating_mul(self.life_gain_multiplier_now(seat))
         .                   } else {
         .                       delta
         .                   };
     2,636 ( 0.00%)          let delta = if delta > 0 {
         .                       delta.saturating_add(self.life_gain_bonus_now(seat))
         .                   } else {
         .                       delta
         .                   };
         .                   // CR 119.8: symmetric drop for negative deltas (lose-life).
     5,272 ( 0.00%)          if delta < 0 && self.player_cannot_lose_life_now(seat) {
         .                       return self.effective_life(seat);
         .                   }
         .                   // CR 614 — Bloodletter of Aclazotz: an opponent losing life during the
         .                   // Bloodletter controller's turn loses twice that much instead. Applied
         .                   // after the cannot-lose drop so a locked player still loses nothing.
         .                   let delta = if delta < 0 && self.life_loss_doubled_now(seat) {
         .                       delta.saturating_mul(2)
         .                   } else {
         .                       delta
         .                   };
     4,174 ( 0.00%)          let team_idx = self
         .                       .teams
         .                       .iter()
         .                       .position(|t| t.members.contains(&seat));
         .                   let writes_to_shared = team_idx
         .                       .and_then(|i| self.teams[i].shared_life)
         .                       .is_some();
         .           
     8,348 ( 0.00%)          let new_total = if writes_to_shared {
         .                       let t = team_idx.unwrap();
         .                       let current = self.teams[t].shared_life.unwrap();
         .                       let next = current.saturating_add(delta);
         .                       self.teams[t].shared_life = Some(next);
         .                       next
         .                   } else {
         .                       let p = &mut self.players[seat];
    16,696 ( 0.00%)              p.life = p.life.saturating_add(delta);
   152,114 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (4,174x)
    12,522 ( 0.00%)              p.life
         .                   };
         .           
     8,348 ( 0.00%)          if delta > 0 {
     2,636 ( 0.00%)              self.players[seat].life_gained_this_turn =
    39,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,318x)
     1,318 ( 0.00%)                  self.players[seat].life_gained_this_turn.saturating_add(delta as u32);
         .                       // False Cure — "whenever a player gains life, that player loses N
         .                       // life for each 1 life they gained." Re-entrant on the loss half,
         .                       // which the `delta < 0` branch below never re-punishes.
     3,954 ( 0.00%)              if self.life_gain_punish_this_turn > 0 {
         .                           let punish = delta.saturating_mul(self.life_gain_punish_this_turn as i32);
         .                           self.adjust_life(seat, -punish);
         .                       }
         .                   } else {
         .                       // delta < 0 — this player lost life (CR 119.3). Powers Spectacle.
     5,712 ( 0.00%)              self.players[seat].lost_life_this_turn = true;
    85,680 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,856x)
     5,712 ( 0.00%)              self.players[seat].life_lost_this_turn =
    85,680 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,856x)
     5,712 ( 0.00%)                  self.players[seat].life_lost_this_turn.saturating_add((-delta) as u32);
         .                       // CR 702.179 — the active player's speed increases by 1 (capped at
         .                       // 4), once on their own turn, the first time an opponent loses life.
     2,856 ( 0.00%)              let active = self.active_player_idx;
     5,712 ( 0.00%)              if active < self.players.len()
    18,454 ( 0.00%)                  && self.players[active].speed >= 1
         .                           && self.players[active].speed < 4
         .                           && !self.players[active].speed_increased_this_turn
         .                           && !self.same_team(seat, active)
         .                       {
         .                           self.players[active].speed += 1;
         .                           self.players[active].speed_increased_this_turn = true;
         .                       }
         .                   }
         .                   new_total
    37,566 ( 0.00%)      }
         .           
         .               /// Like [`adjust_life`] but returns the *applied* delta — after the
         .               /// cannot-gain/lose drops, gain→loss replacement, and gain bonuses
         .               /// (CR 119.7/119.10/614). Callers that emit `LifeGained`/`LifeLost`
         .               /// must use this so triggers don't fire on gains that never happened.
     1,712 ( 0.00%)      pub fn adjust_life_applied(&mut self, seat: usize, delta: i32) -> i32 {
    14,826 ( 0.00%)          let before = self.effective_life(seat);
     9,124 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (214x)
    18,606 ( 0.00%)          let after = self.adjust_life(seat, delta);
   141,703 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::adjust_life (214x)
     1,486 ( 0.00%)          after - before
     1,284 ( 0.00%)      }
         .           
         .               /// Overwrite the effective life total for `seat` (Effect::SetLife
         .               /// path). Routes through the shared pool when set, else writes
         .               /// `players[seat].life` directly. Does not bump
         .               /// `life_gained_this_turn` (set-to-N isn't a "gain").
         .               pub fn set_life(&mut self, seat: usize, new_total: i32) {
         .                   if let Some(t) = self.teams.iter_mut().find(|t| t.members.contains(&seat))
         .                       && t.shared_life.is_some()
-- line 3536 ----------------------------------------
-- line 3750 ----------------------------------------
         .               /// `seat`: the permanents they control plus their face-up command-zone
         .               /// conspiracies (CR 315.5). Vanguard avatars ride along too (CR 902.5).
         .               pub(crate) fn seat_static_sources(
         .                   &self,
         .                   seat: usize,
         .               ) -> impl Iterator<Item = &CardInstance> {
         .                   self.battlefield
         .                       .iter()
   111,552 ( 0.00%)              .filter(move |c| c.controller == seat)
         .                       .chain(self.players[seat].command.iter().filter(|c| c.command_zone_abilities_active()))
         .               }
         .           
         .               /// Every object contributing controller-scoped statics to *any* seat —
         .               /// the battlefield plus every face-up command-zone conspiracy. A
         .               /// command-zone card's `controller` is its owner (CR 315.6).
         .               pub(crate) fn all_static_sources(&self) -> impl Iterator<Item = &CardInstance> {
         .                   self.battlefield.iter().chain(
-- line 3766 ----------------------------------------
-- line 3926 ----------------------------------------
         .               /// commander" play), tests can script the opposite via
         .               /// `ScriptedDecider`. A declined optional replacement still
         .               /// counts as "applied" for CR 614.5 purposes so the same prompt
         .               /// isn't surfaced twice in one resolution walk.
         .               ///
         .               /// `&mut self` because the decider call is mutable. CR 616
         .               /// ordering ("affected card's controller chooses") is
         .               /// approximated by registration order.
    40,100 ( 0.00%)      pub fn resolve_zone_change(
         .                   &mut self,
         .                   card_id: crate::card::CardId,
         .                   from: crate::card::Zone,
         .                   mut to: crate::card::Zone,
         .               ) -> crate::card::Zone {
         .                   use crate::replacement::{ReplacementSource, MAX_REPLACEMENT_ITERATIONS};
         .                   // Note: CR 122.1h finality counter redirect is applied at the
         .                   // call site (`remove_from_battlefield_to_graveyard_raw`) because by
-- line 3942 ----------------------------------------
-- line 3959 ----------------------------------------
         .                                   r.source.clone(),
         .                                   r.from,
         .                                   r.to_zones.clone(),
         .                                   r.redirect_to,
         .                                   r.optional,
         .                               )
         .                           })
         .                           .collect();
    20,050 ( 0.00%)              for (rid, source, r_from, to_zones, redirect_to, optional) in candidates {
         .                           if applied.contains(&rid) {
         .                               // CR 614.5 — a replacement effect can apply at most
         .                               // once to a given event. Skip ones we've already
         .                               // used in this resolution.
         .                               continue;
         .                           }
         .                           match source {
         .                               ReplacementSource::Card(target) if target != card_id => continue,
-- line 3975 ----------------------------------------
-- line 4010 ----------------------------------------
         .                           break;
         .                       }
         .                       if !fired {
         .                           return to;
         .                       }
         .                   }
         .                   debug_assert!(false, "replacement-effect resolution hit iteration cap");
         .                   to
    36,090 ( 0.00%)      }
         .           
         .               /// Number of `StaticEffect::DoubleTokens` permanents `seat` controls
         .               /// on the battlefield. Used by `Effect::CreateToken` to scale the
         .               /// token count by `2^n` — one Adrix and Nev, Twincasters in play
         .               /// means twice as many tokens are minted; two doublers means four
         .               /// times as many; etc. (CR 614.13: multiple replacement effects
         .               /// apply in any order chosen by the controller, but all functionally
         .               /// multiply rather than just add.)
-- line 4026 ----------------------------------------
-- line 4082 ----------------------------------------
         .                       })
         .                       .sum()
         .               }
         .           
         .               /// CR 613.2 layer 4 — is this battlefield permanent a creature *right now*?
         .               /// An animated land (a Zendikon, a manland, an Awaken land) is, even though
         .               /// its printed type line isn't, so its death is a creature dying (CR 700.4).
         .               /// Falls back to the printed types mid-layer-recompute.
     1,210 ( 0.00%)      pub(crate) fn permanent_is_creature(&self, cid: crate::card::CardId) -> bool {
       968 ( 0.00%)          if let Some(cp) = self.computed_permanent(cid) {
   619,657 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (242x)
         .                       return cp.card_types.contains(&crate::card::CardType::Creature);
         .                   }
         .                   self.battlefield_find(cid).is_some_and(|c| c.definition.is_creature())
     1,210 ( 0.00%)      }
         .           
         .               /// How many creatures `seat` controls right now (layer-aware, so animated
         .               /// lands count). Backs the CR 508.1a/509.1b "more creatures than the
         .               /// defending/attacking player" gates (Mogg Toady).
         .               pub(crate) fn creature_count(&self, seat: usize) -> usize {
         .                   // Not worth a `with_frozen_layers` here: measured as an exact null
         .                   // (0 gathers removed) — its callers already hold a scope.
         .                   self.battlefield
-- line 4103 ----------------------------------------
-- line 4423 ----------------------------------------
         .                       .sum()
         .               }
         .           
         .               /// Extra ETB counters granted by `StaticEffect::ChosenTypeEntersWithCounter`
         .               /// (Metallic Mimic). For a creature `entering` under `controller`, returns
         .               /// one counter spec per matching source: a *different* permanent the same
         .               /// player controls whose chosen creature type is among the entering
         .               /// creature's types. The entering card must already be on the battlefield.
    17,496 ( 0.00%)      pub fn chosen_type_etb_counter_specs(
         .                   &self,
         .                   entering: CardId,
         .                   controller: usize,
         .               ) -> Vec<(crate::card::CounterType, u32)> {
         .                   use crate::effect::StaticEffect;
     1,944 ( 0.00%)          let Some(ec) = self.battlefield.iter().find(|c| c.id == entering) else {
         .                       return vec![];
         .                   };
         .                   // Oath of Gideon's loyalty rider is the one spec that applies to a
         .                   // non-creature entrant, so it's collected before the creature gate.
         .                   let mut pw_specs = vec![];
     7,776 ( 0.00%)          if ec.definition.is_planeswalker() {
    31,104 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (1,944x)
         .                       for src in &self.battlefield {
         .                           if src.controller != controller || src.id == entering {
         .                               continue;
         .                           }
         .                           for sa in &src.definition.static_abilities {
         .                               if let StaticEffect::PlaneswalkersEnterWithExtraLoyalty { amount } = sa.effect {
         .                                   pw_specs.push((crate::card::CounterType::Loyalty, amount));
         .                               }
         .                           }
         .                       }
         .                   }
     7,776 ( 0.00%)          if !ec.definition.is_creature() {
    25,272 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (1,944x)
         .                       return pw_specs;
         .                   }
         .                   let entering_types = ec.definition.subtypes.creature_types.clone();
     7,776 ( 0.00%)          let mut specs = pw_specs;
         .                   for src in self.all_static_sources() {
    94,324 ( 0.00%)              if src.controller != controller || src.id == entering {
         .                           continue;
         .                       }
         .                       let changeling = ec
         .                           .definition
         .                           .keywords
         .                           .contains(&crate::card::Keyword::Changeling);
         .                       for sa in &src.definition.static_abilities {
         .                           match &sa.effect {
-- line 4469 ----------------------------------------
-- line 4535 ----------------------------------------
         .                                   }
         .                               }
         .                               _ => {}
         .                           }
         .                       }
         .                   }
         .                   // Combine Guildmage — a turn-scoped "each creature you control enters
         .                   // with an additional +1/+1 counter" grant on the controller.
     3,888 ( 0.00%)          let extra = self.players[controller].extra_etb_p1p1_counters_this_turn;
     3,888 ( 0.00%)          if extra > 0 {
         .                       specs.push((crate::card::CounterType::PlusOnePlusOne, extra));
         .                   }
     9,720 ( 0.00%)          specs
    17,496 ( 0.00%)      }
         .           
         .               /// CR 702.32 / 702.62 — a permanent with Fading N / Vanishing N enters
         .               /// with N fade / time counters. Called from both ETB paths after the
         .               /// permanent is on the battlefield.
         .               /// CR 614.1c — apply a permanent's printed `enters_with_counters` to a
         .               /// permanent that has just entered, honoring the CR 614.16 counter
         .               /// replacements and Solemnity's counter lock. The land-drop path
         .               /// (`place_land_card`) uses this; the spell-resolution and move paths
-- line 4556 ----------------------------------------
-- line 4577 ----------------------------------------
         .                   }
         .                   events.push(crate::game::GameEvent::CounterAdded {
         .                       card_id: cid,
         .                       counter_type: kind,
         .                       count: n,
         .                   });
         .               }
         .           
    13,044 ( 0.00%)      pub(crate) fn apply_fading_vanishing_etb(
         .                   &mut self,
         .                   cid: CardId,
         .                   events: &mut Vec<crate::game::GameEvent>,
         .               ) {
         .                   use crate::card::{CounterType, Keyword};
         .                   let Some(card) = self.battlefield_find(cid) else { return };
     7,800 ( 0.00%)          let spec = card.definition.keywords.iter().find_map(|k| match k {
         .                       Keyword::Fading(n) => Some((CounterType::Fade, *n)),
         .                       Keyword::Vanishing(n) => Some((CounterType::Time, *n)),
         .                       _ => None,
         .                   });
     6,522 ( 0.00%)          let Some((kind, n)) = spec else { return };
         .                   if n == 0 {
         .                       return;
         .                   }
         .                   if let Some(card_mut) = self.battlefield_find_mut(cid) {
         .                       card_mut.add_counters(kind, n);
         .                   }
         .                   events.push(crate::game::GameEvent::CounterAdded {
         .                       card_id: cid,
         .                       counter_type: kind,
         .                       count: n,
         .                   });
    13,044 ( 0.00%)      }
         .           
         .               /// CR 702.183 — a permanent cast for its Impending cost enters with N
         .               /// time counters (stamped on `CardInstance.impending_counters` at cast).
         .               /// CR 701.28 — a permanent spell cast *converted* (More Than Meets the
         .               /// Eye) enters with its back face up. Applied at ETB, before the first SBA
         .               /// sweep, so the back face's characteristics are what the game ever sees.
         .               pub(crate) fn apply_cast_converted_etb(
         .                   &mut self,
         .                   cid: CardId,
         .                   events: &mut Vec<crate::game::GameEvent>,
         .               ) {
         .                   let Some(card) = self.battlefield_find_mut(cid) else { return };
     4,348 ( 0.00%)          if !std::mem::take(&mut card.cast_converted) || card.definition.back_face.is_none() {
         .                       return;
         .                   }
         .                   let back = card.definition.back_face.as_ref().map(|b| (**b).clone()).unwrap();
         .                   card.front_face = Some(card.definition.clone());
         .                   card.definition = std::sync::Arc::new(back);
         .                   card.transformed = true;
         .                   events.push(crate::game::GameEvent::Transformed { card_id: cid });
         .               }
-- line 4630 ----------------------------------------
-- line 4631 ----------------------------------------
         .           
         .               pub(crate) fn apply_impending_etb(
         .                   &mut self,
         .                   cid: CardId,
         .                   events: &mut Vec<crate::game::GameEvent>,
         .               ) {
         .                   use crate::card::CounterType;
         .                   let Some(card) = self.battlefield_find_mut(cid) else { return };
     2,174 ( 0.00%)          let n = card.impending_counters;
     4,348 ( 0.00%)          if n == 0 {
         .                       return;
         .                   }
         .                   card.impending_counters = 0;
         .                   card.add_counters(CounterType::Time, n);
         .                   events.push(crate::game::GameEvent::CounterAdded {
         .                       card_id: cid,
         .                       counter_type: CounterType::Time,
         .                       count: n,
-- line 4648 ----------------------------------------
-- line 4649 ----------------------------------------
         .                   });
         .               }
         .           
         .               /// CR 702.183 — at the beginning of the active player's end step, remove
         .               /// one time counter from each Impending permanent they control. Unlike
         .               /// Vanishing there's no sacrifice: when the last counter comes off the
         .               /// permanent simply stops being a non-creature (the layer effect reads
         .               /// the live counter count) and turns into a creature.
    15,462 ( 0.00%)      pub fn process_impending(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, Keyword};
     3,436 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
         .                   let affected: Vec<CardId> = self
         .                       .battlefield
         .                       .iter()
         .                       .filter(|c| {
    97,452 ( 0.00%)                  c.controller == active
    85,060 ( 0.00%)                      && c.counter_count(CounterType::Time) > 0
   170,120 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (17,012x)
         .                               && c.definition
         .                                   .keywords
         .                                   .iter()
         .                                   .any(|k| matches!(k, Keyword::Impending(_)))
         .                       })
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in affected {
-- line 4674 ----------------------------------------
-- line 4676 ----------------------------------------
         .                           c.remove_counters(CounterType::Time, 1);
         .                       }
         .                       events.push(crate::game::GameEvent::CounterRemoved {
         .                           card_id: id,
         .                           counter_type: CounterType::Time,
         .                           count: 1,
         .                       });
         .                   }
     8,590 ( 0.00%)          events
    13,744 ( 0.00%)      }
         .           
         .               /// CR 702.32 / 702.62 — at the beginning of the active player's upkeep,
         .               /// each Fading / Vanishing permanent they control removes a counter (and
         .               /// is sacrificed when it runs out). Processed as a turn-based action at
         .               /// upkeep before priority.
    13,936 ( 0.00%)      pub fn process_fading_vanishing(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, Keyword};
     3,484 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
         .                   // Snapshot the affected (id, fading?) pairs first to avoid borrow churn.
     1,742 ( 0.00%)          let affected: Vec<(CardId, bool)> = self
         .                       .battlefield
         .                       .iter()
    32,478 ( 0.00%)              .filter(|c| c.controller == active)
         .                       .filter_map(|c| {
     9,928 ( 0.00%)                  c.definition.keywords.iter().find_map(|k| match k {
         .                               Keyword::Fading(_) => Some((c.id, true)),
         .                               Keyword::Vanishing(_) => Some((c.id, false)),
         .                               _ => None,
         .                           })
         .                       })
         .                       .collect();
         .                   for (id, is_fading) in affected {
         .                       let kind = if is_fading { CounterType::Fade } else { CounterType::Time };
-- line 4709 ----------------------------------------
-- line 4738 ----------------------------------------
         .                           had <= 1
         .                       };
         .                       if sacrifice {
         .                           // CR 700.4 — the shared sacrifice helper emits the full
         .                           // event set (CreatureDied included) + die snapshot.
         .                           self.sacrifice_one(id, active, &mut events);
         .                       }
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// CR 702.24 — Cumulative upkeep. As a turn-based action at the active
         .               /// player's upkeep, each permanent they control with
         .               /// `Keyword::CumulativeUpkeep(cost)` gets one age counter; its controller
         .               /// then pays `cost` once per age counter on it (mana from the pool, life,
         .               /// or sacrificing matching permanents), or sacrifices the permanent.
         .               /// (Following `PayManaOrElse`, mana is auto-paid from the pool when
         .               /// affordable — an interactive pay prompt is a follow-up.)
    13,936 ( 0.00%)      pub fn process_cumulative_upkeep(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, CumulativeUpkeepCost, Keyword};
     5,226 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
         .                   // Computed keywords so a granted cumulative upkeep (Mana Chains' Aura)
         .                   // ticks too, not just the printed keyword.
         .                   // Gated: the upkeep step took a whole-board layer pass every turn to
         .                   // find a keyword almost no board carries.
         .                   let affected: Vec<(CardId, CumulativeUpkeepCost)> = self.with_frozen_layers(|g| {
     8,096 ( 0.00%)              if !g.board_keyword_matching(|k| matches!(k, Keyword::CumulativeUpkeep(_))) {
         .                           return Vec::new();
         .                       }
         .                       g.compute_battlefield()
         .                           .iter()
         .                           .filter(|c| c.controller == active)
         .                           // CR 702.24b — multiple instances each trigger separately, and
         .                           // each counts every age counter on the permanent.
         .                           .flat_map(|c| {
         .                               c.keywords.iter().filter_map(move |k| match k {
         .                                   Keyword::CumulativeUpkeep(cost) => Some((c.id, cost.clone())),
         .                                   _ => None,
         .                               })
         .                           })
         .                           .collect()
         .                   });
     8,710 ( 0.00%)          for (id, cost) in affected {
         .                       if let Some(c) = self.battlefield_find_mut(id) {
         .                           c.add_counters(CounterType::Age, 1);
         .                       }
         .                       events.push(crate::game::GameEvent::CounterAdded {
         .                           card_id: id,
         .                           counter_type: CounterType::Age,
         .                           count: 1,
         .                       });
-- line 4789 ----------------------------------------
-- line 4898 ----------------------------------------
         .                           };
         .                           // Dispatch before the sacrifice so the trigger's source is
         .                           // still on the battlefield to be found.
         .                           self.dispatch_triggers_for_events(std::slice::from_ref(&unpaid));
         .                           events.push(unpaid);
         .                           self.sacrifice_one(id, active, &mut events);
         .                       }
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// CR 702.62d/e — remove one time counter from each suspended card the
         .               /// active player owns in exile; when the last counter comes off, cast
         .               /// the card without paying its mana cost (a creature so cast clears its
         .               /// summoning sickness — Suspend grants haste). Targets are auto-chosen,
         .               /// matching AutoDecider behavior for other free casts.
    15,678 ( 0.00%)      pub fn process_suspend(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, Keyword};
     3,484 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
         .                   // Snapshot suspended exiled cards (Suspend keyword + ≥1 time counter)
         .                   // owned by the active player, so the borrow is released before casting.
     1,742 ( 0.00%)          let suspended: Vec<CardId> = self
         .                       .exile
         .                       .iter()
         .                       .filter(|c| {
     1,542 ( 0.00%)                  c.owner == active
     1,340 ( 0.00%)                      && c.counter_count(CounterType::Time) > 0
     2,680 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (268x)
         .                               && c.definition
         .                                   .keywords
         .                                   .iter()
         .                                   .any(|k| matches!(k, Keyword::Suspend(..)))
         .                       })
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in suspended {
         .                       events.append(&mut self.remove_suspend_time_counter(id));
         .                   }
         .                   // CR 702.62e — cards that *gained* suspend (the card "Suspend")
         .                   // tick on the same schedule even without the printed keyword.
     1,742 ( 0.00%)          let granted: Vec<CardId> = self
         .                       .exile
         .                       .iter()
         .                       .filter(|c| {
     2,078 ( 0.00%)                  c.owner == active && c.granted_suspend && c.counter_count(CounterType::Time) > 0
         .                       })
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in granted {
         .                       events.append(&mut self.remove_suspend_time_counter(id));
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// The creatures pending attack mandates name for `seat` (Oracle en-Vec),
         .               /// unioned across every mandate on that seat and filtered to creatures
         .               /// `seat` still controls — a chosen creature that changed hands is no
         .               /// longer compelled (CR 508.1a reads the current controller). `None` when
         .               /// the seat is under no mandate at all. Read by the server view so the
         .               /// client can badge both the named attackers and the benched rest.
         .               pub fn attack_mandate_for(&self, seat: usize) -> Option<Vec<CardId>> {
-- line 4958 ----------------------------------------
-- line 4968 ----------------------------------------
         .                           }
         .                       }
         .                   }
         .                   any.then_some(out)
         .               }
         .           
         .               /// The armed half of [`attack_mandate_for`] — what `declare_attackers` and
         .               /// the end-step sweep enforce.
    49,482 ( 0.00%)      pub(crate) fn armed_attack_mandate_for(&self, seat: usize) -> Option<Vec<CardId>> {
         .                   let mut out: Vec<CardId> = Vec::new();
         .                   let mut any = false;
         .                   for m in self.attack_mandates.iter().filter(|m| m.seat == seat && m.armed) {
         .                       any = true;
         .                       for id in &m.chosen {
         .                           if !out.contains(id)
         .                               && self.battlefield_find(*id).is_some_and(|c| c.controller == seat)
         .                           {
         .                               out.push(*id);
         .                           }
         .                       }
         .                   }
         .                   any.then_some(out)
    43,984 ( 0.00%)      }
         .           
         .               /// Ertai's Meddling — at the beginning of each of the delaying player's
         .               /// upkeeps, remove a delay counter from each spell card they own exiled
         .               /// this way; the last removal puts it back on the stack.
    15,678 ( 0.00%)      pub fn process_delayed_spells(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::CounterType;
     3,484 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
     1,742 ( 0.00%)          let delayed: Vec<CardId> = self
         .                       .exile
         .                       .iter()
     2,614 ( 0.00%)              .filter(|c| c.owner == active && c.counter_count(CounterType::Delay) > 0)
     2,680 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (268x)
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in delayed {
         .                       let Some(card) = self.exile.iter_mut().find(|c| c.id == id) else { continue };
         .                       card.remove_counters(CounterType::Delay, 1);
         .                       events.push(crate::game::GameEvent::CounterRemoved {
         .                           card_id: id,
         .                           counter_type: CounterType::Delay,
-- line 5010 ----------------------------------------
-- line 5028 ----------------------------------------
         .                           None,
         .                           false,
         .                       );
         .                       self.priority.player_with_priority = saved_priority;
         .                       if let Ok(mut evs) = cast {
         .                           events.append(&mut evs);
         .                       }
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// CR 702.29 — Echo. At the beginning of the controller's upkeep, each
         .               /// permanent they control with an unpaid echo (it came under their
         .               /// control since their last upkeep) is sacrificed unless its echo cost
         .               /// is paid: mana echoes auto-pay from the pool when affordable
         .               /// (matching `process_cumulative_upkeep`), `EchoDiscard` discards a
         .               /// card picked by the controller's decider. A `wants_ui` controller
         .               /// instead gets a real echo trigger on the stack whose resolution asks
         .               /// pay-or-sacrifice (`Effect::EchoPayOrSacrifice`).
    15,678 ( 0.00%)      pub fn process_echo(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::Keyword;
     3,484 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
     1,742 ( 0.00%)          let affected: Vec<(CardId, Option<crate::mana::ManaCost>)> = self
         .                       .battlefield
         .                       .iter()
    97,434 ( 0.00%)              .filter(|c| c.controller == active && !c.echo_paid)
         .                       .filter_map(|c| {
    17,892 ( 0.00%)                  c.definition.keywords.iter().find_map(|k| match k {
         .                               Keyword::Echo(cost) => Some((c.id, Some(cost.clone()))),
         .                               Keyword::EchoDiscard => Some((c.id, None)),
         .                               _ => None,
         .                           })
         .                       })
         .                       .collect();
    10,452 ( 0.00%)          for (id, cost) in affected {
         .                       if self.players.get(active).is_some_and(|p| p.wants_ui) {
         .                           self.push_pending_trigger(
         .                               PendingTriggerPush {
         .                                   from_mana_ability: false,
         .                                   actor: None,
         .                                   source: id,
         .                                   controller: active,
         .                                   effect: Effect::EchoPayOrSacrifice { mana_cost: cost },
-- line 5071 ----------------------------------------
-- line 5105 ----------------------------------------
         .                       if paid {
         .                           if let Some(c) = self.battlefield_find_mut(id) {
         .                               c.echo_paid = true;
         .                           }
         .                       } else {
         .                           self.sacrifice_one(id, active, &mut events);
         .                       }
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// Uvilda, Dean of Perfection — at the active player's upkeep, remove one
         .               /// hone counter from each instant/sorcery they own in exile with hone
         .               /// counters. When the last comes off, grant them permission to cast it
         .               /// from exile for {4} less (the printed "you may cast it" window).
    15,678 ( 0.00%)      pub fn process_hone(&mut self) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, MayPlayDuration, MayPlayPermission};
     3,484 ( 0.00%)          let active = self.active_player_idx;
     3,484 ( 0.00%)          let turn = self.turn_number;
         .                   let mut events = Vec::new();
     1,742 ( 0.00%)          let honed: Vec<CardId> = self
         .                       .exile
         .                       .iter()
     2,614 ( 0.00%)              .filter(|c| c.owner == active && c.counter_count(CounterType::Hone) > 0)
     2,680 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (268x)
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in honed {
         .                       let Some(card) = self.exile.iter_mut().find(|c| c.id == id) else { continue };
         .                       card.remove_counters(CounterType::Hone, 1);
         .                       events.push(crate::game::GameEvent::CounterRemoved {
         .                           card_id: id,
         .                           counter_type: CounterType::Hone,
-- line 5136 ----------------------------------------
-- line 5146 ----------------------------------------
         .                           player: active,
         .                           granted_turn: turn,
         .                           duration: MayPlayDuration::EndOfControllersNextTurn,
         .                           exile_after: false,
         .                           miracle: false,
         .                       });
         .                       card.granted_alt_cast_cost_eot = Some(cost);
         .                   }
     6,968 ( 0.00%)          events
    15,678 ( 0.00%)      }
         .           
         .               /// `CardDefinition.exile_countdown` — tick one counter off each exiled
         .               /// card the active player owns; when the last comes off, put the card into
         .               /// its owner's graveyard and resolve the fuse's effect (All Hallow's Eve).
    13,936 ( 0.00%)      pub fn process_exile_countdowns(&mut self) -> Vec<crate::game::GameEvent> {
     3,484 ( 0.00%)          let active = self.active_player_idx;
         .                   let mut events = Vec::new();
     1,742 ( 0.00%)          let fused: Vec<CardId> = self
         .                       .exile
         .                       .iter()
         .                       .filter(|c| {
     1,542 ( 0.00%)                  c.owner == active
         .                               && c.definition
         .                                   .exile_countdown
         .                                   .as_ref()
         .                                   .is_some_and(|f| c.counter_count(f.counter) > 0)
         .                       })
         .                       .map(|c| c.id)
         .                       .collect();
         .                   for id in fused {
-- line 5175 ----------------------------------------
-- line 5189 ----------------------------------------
         .                       let card = self.exile.remove(pos);
         .                       let owner = card.owner;
         .                       self.players[owner].graveyard.push(card);
         .                       let ctx = crate::game::effects::EffectContext::for_ability(id, owner, None);
         .                       if let Ok(mut evs) = self.resolve_effect(&fuse.effect, &ctx) {
         .                           events.append(&mut evs);
         .                       }
         .                   }
     8,710 ( 0.00%)          events
    13,936 ( 0.00%)      }
         .           
         .               /// True when `src` is an artifact — a battlefield permanent, a resolving
         .               /// spell, or a card in any other zone. Reverse Polarity / Martyrs of Korlis.
     1,680 ( 0.00%)      pub(crate) fn source_is_artifact(&self, src: CardId) -> bool {
         .                   use crate::card::CardType::Artifact;
     1,012 ( 0.00%)          if let Some(cp) = self.computed_permanent(src) {
    50,536 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (336x)
         .                       return cp.card_types.contains(&Artifact);
         .                   }
         .                   if let Some((id, _, _, types)) = self.resolving_source.as_ref()
       332 ( 0.00%)              && *id == src
         .                   {
         .                       return types.contains(&Artifact);
         .                   }
         .                   self.find_card_anywhere(src)
         .                       .is_some_and(|c| c.definition.card_types.contains(&Artifact))
     1,344 ( 0.00%)      }
         .           
         .               /// Arboria — note that `p` cast a spell or put a nontoken permanent onto
         .               /// the battlefield during their own turn. Actions taken on someone else's
         .               /// turn don't count ("during their last turn").
         .               pub(crate) fn note_acted_on_own_turn(&mut self, p: usize) {
    14,092 ( 0.00%)          if p != self.active_player_idx {
         .                       return;
         .                   }
    20,720 ( 0.00%)          if self.acted_on_own_turn.len() < self.players.len() {
         .                       self.acted_on_own_turn.resize(self.players.len(), false);
         .                   }
    10,360 ( 0.00%)          self.acted_on_own_turn[p] = true;
         .               }
         .           
         .               /// Land Equilibrium — a land just entered under `p`. For each opponent's
         .               /// `OpponentLandsEnterThenSacrifice` static whose controller now has fewer
         .               /// lands than `p` (i.e. `p` had at least as many before the land entered),
         .               /// `p` sacrifices a land of their choice.
         .               pub(crate) fn apply_land_equilibrium(&mut self, p: usize) {
         .                   use crate::effect::StaticEffect;
         .                   let lands = |state: &Self, seat: usize| {
   600,220 ( 0.02%)              state.battlefield.iter().filter(|c| c.controller == seat && c.definition.is_land()).count()
 1,472,768 ( 0.05%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (108,428x)
         .                   };
     3,460 ( 0.00%)          let mine = lands(self, p);
     1,730 ( 0.00%)          let watchers: Vec<CardId> = self
         .                       .battlefield
         .                       .iter()
         .                       .filter(|c| {
    89,392 ( 0.00%)                  c.controller != p
    92,966 ( 0.00%)                      && mine > lands(self, c.controller)
         .                               && c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(
         .                                       self.active_static(&sa.effect, c),
         .                                       Some(StaticEffect::OpponentLandsEnterThenSacrifice)
         .                                   )
         .                               })
         .                       })
         .                       .map(|c| c.id)
-- line 5252 ----------------------------------------
-- line 5349 ----------------------------------------
         .                       events.append(&mut evs);
         .                   }
         .                   events
         .               }
         .           
         .               /// CR 702.62 accelerants — when `caster` casts a spell, tick a time
         .               /// counter off every opponent-owned suspended card that has
         .               /// `Keyword::SuspendAccelerant` (Deep-Sea Kraken).
    70,460 ( 0.00%)      pub fn process_suspend_accelerants(
         .                   &mut self,
         .                   caster: usize,
         .               ) -> Vec<crate::game::GameEvent> {
         .                   use crate::card::{CounterType, Keyword};
     7,046 ( 0.00%)          let targets: Vec<CardId> = self
         .                       .exile
         .                       .iter()
         .                       .filter(|c| {
    11,216 ( 0.00%)                  c.owner != caster
     6,860 ( 0.00%)                      && c.counter_count(CounterType::Time) > 0
    13,720 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (1,372x)
         .                               && c.definition.keywords.contains(&Keyword::SuspendAccelerant)
         .                       })
         .                       .map(|c| c.id)
         .                       .collect();
         .                   let mut events = Vec::new();
         .                   for id in targets {
         .                       events.append(&mut self.remove_suspend_time_counter(id));
         .                   }
    35,230 ( 0.00%)          events
    56,368 ( 0.00%)      }
         .           
         .               /// CR 614.x — true if any active `StaticEffect::ExileNontokenCreaturesNotCast`
         .               /// (Containment Priest) is on the battlefield. Consulted by
         .               /// `place_card_in_dest` to reroute non-cast nontoken creatures to exile.
         .               pub fn nontoken_creature_etb_exile_active(&self) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|c| {
         .                       c.definition
-- line 5385 ----------------------------------------
-- line 5406 ----------------------------------------
         .                       .sum()
         .               }
         .           
         .               /// CR 614.5 — Solphim-style noncombat doublers that apply to this exact
         .               /// (source, target) pair: a `DoubleNoncombatDamageToOpponents` static
         .               /// whose controller also controls `source`, where `ent` is an opponent of
         .               /// that controller (a player or a permanent they control). Each match
         .               /// doubles the dealt amount. Only consulted on the noncombat funnel.
       174 ( 0.00%)      pub fn noncombat_damage_doublers_for(
         .                   &self,
         .                   source: Option<CardId>,
         .                   ent: crate::game::effects::EntityRef,
         .               ) -> u32 {
         .                   use crate::effect::StaticEffect;
         .                   use crate::game::effects::EntityRef;
         .                   // Controller of the damage source (a battlefield permanent or the
         .                   // resolving spell stamped by `resolve_spell`).
       174 ( 0.00%)          let Some(src_ctrl) = source.and_then(|s| {
       356 ( 0.00%)              self.computed_permanent(s).map(|cp| cp.controller).or_else(|| {
    42,184 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (174x)
       332 ( 0.00%)                  match &self.resolving_source {
       332 ( 0.00%)                      Some((id, caster, _, _)) if *id == s => Some(*caster),
         .                               _ => None,
         .                           }
         .                       })
         .                   }) else {
         .                       return 0;
         .                   };
         .                   // Affected player (a damaged player, or the controller of a damaged
         .                   // permanent).
       534 ( 0.00%)          let affected = match ent {
       168 ( 0.00%)              EntityRef::Player(p) => Some(p),
        14 ( 0.00%)              EntityRef::Permanent(c) => self.battlefield_find(c).map(|c| c.controller),
         .                       EntityRef::Card(_) => None,
         .                   };
         .                   let Some(target_player) = affected else { return 0 };
       860 ( 0.00%)          let hits_opponent = !self.same_team(src_ctrl, target_player);
     6,020 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (172x)
       172 ( 0.00%)          self.battlefield
         .                       .iter()
     1,460 ( 0.00%)              .filter(|c| c.controller == src_ctrl)
         .                       .flat_map(|c| c.definition.static_abilities.iter().map(move |sa| (c.id, sa)))
         .                       .filter(|(cid, sa)| match &sa.effect {
         .                           StaticEffect::DoubleNoncombatDamageToOpponents => hits_opponent,
         .                           // The Rollercrusher Ride — any permanent or player, gated on a
         .                           // live predicate read with the static's own source as context.
         .                           StaticEffect::DoubleYourNoncombatDamageWhile { condition } => self
         .                               .evaluate_predicate(
         .                                   condition,
         .                                   &crate::game::effects::EffectContext::for_ability(*cid, src_ctrl, None),
         .                               ),
         .                           _ => false,
         .                       })
         .                       .count() as u32
       174 ( 0.00%)      }
         .           
         .               /// Additive noncombat-damage bonus (Aether Revolt): sum of
         .               /// `NoncombatDamageToOpponentsBonus.amount` over battlefield permanents
         .               /// whose controller controls `source` and where `ent` is an opponent /
         .               /// their permanent. `while_revolt` statics only count while that
         .               /// controller has revolt (CR 702.139). Mirrors the doubler's scoping.
     1,218 ( 0.00%)      pub fn noncombat_damage_bonus_for(
         .                   &self,
         .                   source: Option<CardId>,
         .                   ent: crate::game::effects::EntityRef,
         .               ) -> u32 {
         .                   use crate::effect::StaticEffect;
         .                   use crate::game::effects::EntityRef;
         .                   let Some(src_ctrl) = source.and_then(|s| {
       356 ( 0.00%)              self.computed_permanent(s).map(|cp| cp.controller).or_else(|| {
    42,184 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (174x)
       332 ( 0.00%)                  match &self.resolving_source {
       332 ( 0.00%)                      Some((id, caster, _, _)) if *id == s => Some(*caster),
         .                               _ => None,
         .                           }
         .                       })
         .                   }) else {
         .                       return 0;
         .                   };
       534 ( 0.00%)          let affected = match ent {
       168 ( 0.00%)              EntityRef::Player(p) => Some(p),
        10 ( 0.00%)              EntityRef::Permanent(c) => self.battlefield_find(c).map(|c| c.controller),
         .                       EntityRef::Card(_) => None,
         .                   };
         .                   let Some(target_player) = affected else { return 0 };
       168 ( 0.00%)          self.battlefield
         .                       .iter()
         .                       .map(|c| {
    12,462 ( 0.00%)                  if c.controller != src_ctrl || self.same_team(src_ctrl, target_player) {
    25,690 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (734x)
         .                               return 0;
         .                           }
         .                           c.definition
         .                               .static_abilities
         .                               .iter()
         .                               .filter_map(|sa| match sa.effect {
         .                                   StaticEffect::NoncombatDamageToOpponentsBonus { amount, while_revolt } => {
         .                                       if while_revolt
-- line 5499 ----------------------------------------
-- line 5504 ----------------------------------------
         .                                           Some(amount)
         .                                       }
         .                                   }
         .                                   _ => None,
         .                               })
         .                               .sum::<u32>()
         .                       })
         .                       .sum()
     1,566 ( 0.00%)      }
         .           
         .               /// CR 614.5 — number of `StaticEffect::HalveDamageDealt` permanents on
         .               /// the battlefield (Ghosts of the Innocent). Each halves the dealt
         .               /// amount, rounded down; applied after any doublers.
         .               pub fn damage_halvers(&self) -> u32 {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield
         .                       .iter()
-- line 5520 ----------------------------------------
-- line 5550 ----------------------------------------
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield_find(tgt).is_some_and(|c| {
         .                       c.definition.static_abilities.iter().any(|sa| {
         .                           matches!(sa.effect, StaticEffect::PreventAllCombatDamageToThis)
         .                               || self.self_static_prevents_all_damage_active(&sa.effect, c.controller)
         .                       })
         .                   }) || self.battlefield.iter().any(|eq| {
         .                       // General's Kabuto — the Equipment carries the prevention for its host.
     2,076 ( 0.00%)              eq.attached_to == Some(tgt)
         .                           && eq.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, StaticEffect::PreventAllCombatDamageToAttached)
         .                           })
         .                   })
         .               }
         .           
         .               /// True when a self-static (possibly `WhileYourTurn`-wrapped and thus
         .               /// gated on the active player) prevents *all* damage to its source
-- line 5566 ----------------------------------------
-- line 5585 ----------------------------------------
         .           
         .               /// True when `tgt` prevents all damage to itself (combat or noncombat) via
         .               /// an active `PreventAllDamageToThis` self-static and prevention isn't off.
         .               pub(crate) fn permanent_prevents_all_damage_to_self(&self, tgt: crate::card::CardId) -> bool {
         .                   if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   self.battlefield_find(tgt).is_some_and(|c| {
         8 ( 0.00%)              !c.damage_prevention_off_eot
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               self.self_static_prevents_all_damage_active(&sa.effect, c.controller)
         .                           })
         .                   }) || self.damage_sealed_by_aura(tgt, true)
         .               }
         .           
         .               /// CR 615 — `who` is enchanted by an Aura that blanks damage in the
         .               /// `incoming` direction: Heart of Light seals both, Inviolability only
         .               /// damage dealt *to* the host, Muzzle only damage dealt *by* it.
         .               /// CR 615 — the combat-only sibling of [`damage_sealed_by_aura`]: an Aura
         .               /// on `who` that seals only its *combat* damage (Sandskin).
         .               pub(crate) fn combat_damage_sealed_by_aura(&self, who: crate::card::CardId) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   !self.damage_cant_be_prevented_this_turn
         .                       && self.battlefield.iter().any(|src| {
     5,120 ( 0.00%)                  src.attached_to == Some(who)
         .                               && src.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(sa.effect, StaticEffect::PreventAllCombatDamageToAndFromEnchanted)
         .                               })
         .                       })
         .               }
         .           
         .               pub(crate) fn damage_sealed_by_aura(&self, who: crate::card::CardId, incoming: bool) -> bool {
         .                   use crate::effect::StaticEffect;
       354 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   self.battlefield.iter().any(|src| {
     5,120 ( 0.00%)              src.attached_to == Some(who)
         .                           && src.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, StaticEffect::PreventAllDamageToAndFromEnchanted)
         .                                   || matches!(sa.effect, StaticEffect::PreventAllDamageToEnchanted if incoming)
         .                                   || matches!(sa.effect, StaticEffect::PreventAllDamageByEnchanted if !incoming)
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — true when `tgt` prevents combat damage from creatures blocking
         .               /// it (Armored Transport) and prevention isn't off this turn. The resolver
         .               /// consults this only in the attacker-takes-from-blocker branch, where the
         .               /// dealer is by construction one of `tgt`'s blockers.
         .               pub fn combat_damage_from_blockers_prevented(&self, tgt: crate::card::CardId) -> bool {
         .                   use crate::effect::StaticEffect;
     4,444 ( 0.00%)          !self.damage_cant_be_prevented_this_turn
         .                       && self.battlefield_find(tgt).is_some_and(|c| {
         .                           c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, StaticEffect::PreventAllCombatDamageToThisFromBlockers)
         .                           })
         .                       })
         .               }
         .           
         .               /// CR 615 — true when `tgt` prevents combat damage from creatures matching
         .               /// its own filter (Enchanted Being, Marble Priest) and `dealer` matches.
    35,648 ( 0.00%)      pub fn combat_damage_from_matching_prevented(
         .                   &self,
         .                   tgt: crate::card::CardId,
         .                   dealer: crate::card::CardId,
         .               ) -> bool {
         .                   use crate::effect::StaticEffect;
     8,912 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   let filters: Vec<_> = self
         .                       .battlefield_find(tgt)
         .                       .into_iter()
         .                       .flat_map(|c| c.definition.static_abilities.iter())
         .                       .filter_map(|sa| match &sa.effect {
         .                           StaticEffect::PreventCombatDamageToThisFromMatching { filter } => {
-- line 5661 ----------------------------------------
-- line 5667 ----------------------------------------
         .                   filters.iter().any(|f| {
         .                       self.evaluate_requirement_static(
         .                           f,
         .                           &crate::game::types::Target::Permanent(dealer),
         .                           self.battlefield_find(tgt).map(|c| c.controller).unwrap_or(0),
         .                           None,
         .                       )
         .                   })
    40,104 ( 0.00%)      }
         .           
         .               /// CR 615 — true when `tgt` is blocking `dealer` and prevents damage from
         .               /// the creatures it blocks (Wall of Vapor). Any damage, not just combat.
         .               pub fn damage_from_blocked_prevented(
         .                   &self,
         .                   tgt: crate::card::CardId,
         .                   dealer: crate::card::CardId,
         .               ) -> bool {
         .                   use crate::effect::StaticEffect;
     8,912 ( 0.00%)          !self.damage_cant_be_prevented_this_turn
    40,104 ( 0.00%)              && self.blocks(tgt, dealer)
   207,458 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::blocks (4,456x)
         .                       && self.battlefield_find(tgt).is_some_and(|c| {
         .                           c.definition
         .                               .static_abilities
         .                               .iter()
         .                               .any(|sa| matches!(sa.effect, StaticEffect::PreventAllDamageToThisFromBlocked))
         .                       })
         .               }
         .           
         .               /// CR 615 — true when `tgt` prevents all combat damage to itself and
         .               /// prevention isn't switched off this turn. Consulted by the combat-damage
         .               /// resolver to zero damage marked on Fog Bank / Guard Gomazoa.
         .               pub fn combat_damage_prevented_to_self(&self, tgt: crate::card::CardId) -> bool {
     8,900 ( 0.00%)          !self.damage_cant_be_prevented_this_turn
         .                       && (self.permanent_prevents_all_combat_damage_to_self(tgt)
         .                           || self.combat_damage_prevented_to_this_turn.contains(&tgt)
         .                           || self.damage_sealed_by_aura(tgt, true)
         .                           || self.combat_damage_sealed_by_aura(tgt)
         .                           || self.combat_damage_sealed_for_your_creatures(tgt))
     4,450 ( 0.00%)      }
         .           
         .               /// CR 615 — "prevent all damage that would be dealt to this by [filter]
         .               /// sources" (Uncle Istvan, Argothian Treefolk). Source-aware, so it covers
         .               /// combat damage too; the noncombat path checks the same keyword inline.
    35,600 ( 0.00%)      pub fn damage_from_source_prevented_by_keyword(
         .                   &self,
         .                   tgt: crate::card::CardId,
         .                   src: crate::card::CardId,
         .               ) -> bool {
     8,900 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
    13,350 ( 0.00%)          let controller = self.battlefield_find(tgt).map_or(0, |c| c.controller);
     4,450 ( 0.00%)          self.computed_permanent(tgt)
15,594,601 ( 0.50%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,450x)
         .                       .into_iter()
     2,922 ( 0.00%)              .flat_map(|cp| cp.keywords.to_vec())
    18,458 ( 0.00%)              .any(|k| match k {
         .                           crate::card::Keyword::PreventDamageFromMatching(f) => self
         .                               .evaluate_requirement_static(
         .                                   &f,
         .                                   &crate::game::types::Target::Permanent(src),
         .                                   controller,
         .                                   Some(src),
         .                               ),
         .                           _ => false,
    14,142 ( 0.00%)              })
    35,054 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::Keyword> (4,846x)
    40,050 ( 0.00%)      }
         .           
         .               /// CR 615 — true when all combat damage `dealer` would *deal* is prevented
         .               /// this turn (Azorius Ploy's first clause). Mirror of
         .               /// `combat_damage_prevented_to_self`.
         .               pub fn combat_damage_prevented_from(&self, dealer: crate::card::CardId) -> bool {
    13,544 ( 0.00%)          !self.damage_cant_be_prevented_this_turn
         .                       && (self.combat_damage_prevented_by_this_turn.contains(&dealer)
         .                           || self.all_damage_prevented_by_this_turn.contains(&dealer)
         .                           || self.damage_sealed_by_aura(dealer, false)
         .                           || self.combat_damage_sealed_by_aura(dealer)
         .                           || self.combat_damage_sealed_for_your_creatures(dealer))
     6,772 ( 0.00%)      }
         .           
         .               /// Scale a pending damage event by the global doubling/halving
         .               /// replacements (CR 614.2 / 614.5): every doubler ×2, then every
         .               /// halver ÷2 rounded down.
         .               pub fn scale_damage(&self, amount: u32) -> u32 {
         .                   let d = self.damage_doublers().min(16);
         .                   let h = self.damage_halvers().min(16);
         .                   amount.saturating_mul(1 << d) >> h
-- line 5752 ----------------------------------------
-- line 5763 ----------------------------------------
         .               pub fn noncombat_damage_bonus_of_seat(&self, seat: usize) -> u32 {
         .                   self.noncombat_damage_bonus_this_turn
         .                       .iter()
         .                       .filter(|(s, _)| *s == seat)
         .                       .map(|(_, n)| *n)
         .                       .sum()
         .               }
         .           
    73,120 ( 0.00%)      pub fn scale_damage_to(
         .                   &self,
         .                   source: Option<CardId>,
         .                   ent: crate::game::effects::EntityRef,
         .                   amount: u32,
         .               ) -> u32 {
         .                   use crate::effect::StaticEffect;
         .                   use crate::game::effects::EntityRef;
    38,160 ( 0.00%)          let affected = match ent {
     8,568 ( 0.00%)              EntityRef::Player(p) => Some(p),
    17,818 ( 0.00%)              EntityRef::Permanent(c) => self.battlefield_find(c).map(|c| c.controller),
         .                       EntityRef::Card(_) => None,
         .                   };
         .                   // Source identity: a battlefield permanent's computed colors +
         .                   // controller, else the resolving spell stamped by `resolve_spell`.
         .                   // One layer read for both this and the "is the source a permanent?"
         .                   // test below — each `computed_permanent` outside a freeze scope
         .                   // re-gathers every continuous effect in the game.
    14,624 ( 0.00%)          let source_cp = source.and_then(|s| self.computed_permanent(s));
18,848,885 ( 0.60%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (7,312x)
         .                   let source_info: Option<(usize, Vec<crate::mana::Color>)> = source.and_then(|s| {
         .                       source_cp
         .                           .as_ref()
    21,438 ( 0.00%)                  .map(|cp| (cp.controller, cp.colors.to_vec()))
 1,244,486 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs:crabomination_base::mana::ColorSet::to_vec (7,146x)
       830 ( 0.00%)                  .or_else(|| match &self.resolving_source {
       166 ( 0.00%)                      Some((id, caster, colors, _)) if *id == s => {
       166 ( 0.00%)                          Some((*caster, colors.clone()))
         .                               }
         .                               _ => None,
         .                           })
         .                   });
         .                   let mut amount = amount;
         .                   let mut d = self.damage_doublers();
         .                   let mut h = self.damage_halvers();
         .                   // Sulfuric Vapors — "if a [color] SPELL would deal damage to a
         .                   // permanent or player". Recipient-agnostic, so it sits outside the
         .                   // player-only block below; the source must be a resolving spell
         .                   // (no battlefield permanent behind it).
    14,624 ( 0.00%)          if source.is_some()
    21,936 ( 0.00%)              && source_cp.is_none()
         .                       && let Some((_, spell_colors)) = &source_info
         .                   {
         .                       for c in &self.battlefield {
         .                           for sa in &c.definition.static_abilities {
         .                               match &sa.effect {
         .                                   StaticEffect::AddDamageFromColorSpells { color, amount: bonus }
         .                                       if spell_colors.contains(color) =>
         .                                   {
-- line 5817 ----------------------------------------
-- line 5823 ----------------------------------------
         .                                   }
         .                                   _ => {}
         .                               }
         .                           }
         .                       }
         .                   }
         .                   // Overblaze — this source's damage is doubled for the rest of the turn.
         .                   if let Some(s) = source {
    14,624 ( 0.00%)              d += self.doubled_damage_sources_this_turn.iter().filter(|c| **c == s).count() as u32;
         .                   }
    21,936 ( 0.00%)          if let Some(p) = affected {
         .                       // Stagger (Lightning, Army of One): damage to a staggered player
         .                       // or their permanents is doubled until the registrant's next turn.
    14,620 ( 0.00%)              d += self.staggered_damage_players.iter().filter(|(v, _)| *v == p).count() as u32;
         .                       for c in &self.battlefield {
         .                           for sa in &c.definition.static_abilities {
         .                               match &sa.effect {
         .                                   StaticEffect::DoubleDamageToOpponents
         .                                       if !self.same_team(c.controller, p) =>
         .                                   {
         .                                       d += 1;
         .                                   }
-- line 5844 ----------------------------------------
-- line 5948 ----------------------------------------
         .                                   }
         .                                   _ => {}
         .                               }
         .                           }
         .                       }
         .                   }
         .                   // Quest for Pure Flame — the source's controller doubles all damage
         .                   // their sources deal this turn (CR 614.5, any permanent or player).
    44,038 ( 0.00%)          if let Some((src_ctrl, _)) = &source_info
    36,560 ( 0.00%)              && self.players[*src_ctrl].double_your_source_damage_this_turn
         .                   {
         .                       d += 1;
         .                   }
         .                   // Neriv, Heart of the Storm — a creature you control that entered this
         .                   // turn deals double damage (combat and noncombat alike).
    14,624 ( 0.00%)          if let Some(src) = source
         .                       && let Some(sc) = self.battlefield_find(src)
    21,438 ( 0.00%)              && sc.definition.is_creature()
    92,898 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,146x)
     7,146 ( 0.00%)              && sc.entered_turn == Some(self.turn_number)
         .                   {
       268 ( 0.00%)              d += self
         .                           .battlefield
         .                           .iter()
       986 ( 0.00%)                  .filter(|c| c.controller == sc.controller)
         .                           .flat_map(|c| &c.definition.static_abilities)
         .                           .filter(|sa| matches!(sa.effect, StaticEffect::DoubleDamageFromCreaturesEnteredThisTurn))
         .                           .count() as u32;
         .                   }
         .                   // Gratuitous Violence — a creature you control deals double damage to
         .                   // any permanent or player (CR 614.2), source-controller-restricted.
         .                   if let Some(src) = source
         .                       && let Some(sc) = self.battlefield_find(src)
    21,438 ( 0.00%)              && sc.definition.is_creature()
    92,898 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,146x)
         .                   {
    14,292 ( 0.00%)              d += self
         .                           .battlefield
         .                           .iter()
   158,564 ( 0.01%)                  .filter(|c| c.controller == sc.controller)
         .                           .flat_map(|c| &c.definition.static_abilities)
         .                           .filter(|sa| matches!(sa.effect, StaticEffect::DoubleDamageFromControlledCreatures))
         .                           .count() as u32;
         .                   }
         .                   // Trance Kuja — the filtered sibling: only sources you control that
         .                   // match the filter (Wizards) deal double damage.
    14,624 ( 0.00%)          if let Some(src) = source
         .                       && let Some(sc) = self.battlefield_find(src)
         .                   {
         .                       let ctrl = sc.controller;
     7,146 ( 0.00%)              d += self
         .                           .battlefield
         .                           .iter()
   158,564 ( 0.01%)                  .filter(|c| c.controller == ctrl)
         .                           .flat_map(|c| &c.definition.static_abilities)
         .                           .filter(|sa| match &sa.effect {
         .                               StaticEffect::DoubleDamageFromControlledMatching { filter } => self
         .                                   .evaluate_requirement_static(filter, &Target::Permanent(src), ctrl, None),
         .                               _ => false,
         .                           })
         .                           .count() as u32;
         .                   }
         .                   // Anthem of Rakdos (Hellbent) — while the static's controller has an
         .                   // empty hand, any source they control deals double damage (CR 614.5).
     2,694 ( 0.00%)          if let Some((src_ctrl, _)) = &source_info
    29,248 ( 0.00%)              && self.players[*src_ctrl].hand.is_empty()
         .                   {
     5,388 ( 0.00%)              d += self
         .                           .battlefield
         .                           .iter()
    69,094 ( 0.00%)                  .filter(|c| c.controller == *src_ctrl)
         .                           .flat_map(|c| &c.definition.static_abilities)
         .                           .filter(|sa| matches!(sa.effect, StaticEffect::DoubleYourSourcesDamageWhileHellbent))
         .                           .count() as u32;
         .                   }
         .                   // Valley Flamecaller — a creature you control of a listed type deals +1
         .                   // damage to any target (combat or noncombat alike).
    14,624 ( 0.00%)          if let Some(src) = source
    36,394 ( 0.00%)              && let Some(cp) = self.computed_permanent(src)
 9,219,374 ( 0.30%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (7,312x)
         .                   {
         .                       for c in &self.battlefield {
         .                           for sa in &c.definition.static_abilities {
         .                               if let StaticEffect::ControlledCreatureTypesDealExtraDamage { types, amount: bonus } =
         .                                   &sa.effect
         .                                   && c.controller == cp.controller
         .                                   && types.iter().any(|t| cp.subtypes.creature_types.contains(t))
         .                               {
         .                                   amount = amount.saturating_add(*bonus);
         .                               }
         .                           }
         .                       }
         .                   }
    21,936 ( 0.00%)          let amount = amount.saturating_mul(1 << d.min(16)) >> h.min(16);
         .                   // CR 615 — Equal Treatment: "if any source would deal 1 or more
         .                   // damage this turn, it deals 2 damage instead."
    14,624 ( 0.00%)          let amount = match self.damage_becomes_this_turn {
         .                       Some((at_least, becomes)) if amount >= at_least => becomes,
         .                       _ => amount,
         .                   };
         .                   // Divine Presence — a big event is replaced by a small one (CR 614).
         .                   // Applied last so it caps the doubled/halved result.
     7,312 ( 0.00%)          self.battlefield
         .                       .iter()
         .                       .flat_map(|c| &c.definition.static_abilities)
         .                       .filter_map(|sa| match sa.effect {
         .                           StaticEffect::CapLargeDamage { at_least, capped } if amount >= at_least => {
         .                               Some(capped)
         .                           }
         .                           _ => None,
         .                       })
         .                       .min()
         .                       .unwrap_or(amount)
    65,808 ( 0.00%)      }
         .           
         .               /// CR 122.1 — true if any active `StaticEffect::CountersCantBePlaced`
         .               /// (Solemnity) is on the battlefield. While set, every counter-placement
         .               /// site drops the counters instead of adding them.
         .               pub fn counters_locked(&self) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|c| {
         .                       c.definition
-- line 6066 ----------------------------------------
-- line 6070 ----------------------------------------
         .                   })
         .               }
         .           
         .               /// CR 614.6 — true if `card` bound for its owner's graveyard is exiled
         .               /// instead (Rest in Peace exiles everything; Leyline of the Void only
         .               /// an opponent's cards; Sanctifier en-Vec only black/red cards).
         .               /// Consulted by `route_to_graveyard` at every graveyard-placement site.
         .               pub fn graveyard_exiled_for(&self, card: &crate::card::CardInstance) -> bool {
    19,508 ( 0.00%)          self.graveyard_exile_redirects(card).0
   952,992 ( 0.03%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::graveyard_exile_redirects (3,882x)
         .               }
         .           
         .               /// `(redirects, void_counter, stamped_by)` for `card`: whether some
         .               /// `ExileCardsBoundForGraveyard` static redirects it to exile, whether any
         .               /// applicable redirect stamps a void counter on it (Dauthi Voidwalker), and
         .               /// which source claims it via `exiled_with` (Valgavoth, Terror Eater).
    87,786 ( 0.00%)      pub(crate) fn graveyard_exile_redirects(
         .                   &self,
         .                   card: &crate::card::CardInstance,
         .               ) -> (bool, bool, Option<CardId>) {
         .                   use crate::effect::StaticEffect;
     9,754 ( 0.00%)          let owner = card.owner;
         .                   // Gaea's Will — the owner's graveyard-bound cards exile this turn.
         .                   let mut redirects = self
         .                       .players
         .                       .get(owner)
     9,754 ( 0.00%)              .is_some_and(|pl| pl.graveyard_bound_exiled_this_turn);
         .                   let mut void = false;
         .                   let mut stamped_by = None;
         .                   for c in &self.battlefield {
         .                       for sa in &c.definition.static_abilities {
         .                           if let StaticEffect::ExileCardsBoundForGraveyard {
         .                               opponents_only,
         .                               own_only,
         .                               colors,
-- line 6103 ----------------------------------------
-- line 6119 ----------------------------------------
         .                                   void |= void_counter;
         .                                   if *stamp_source {
         .                                       stamped_by = Some(c.id);
         .                                   }
         .                               }
         .                           }
         .                       }
         .                   }
    58,524 ( 0.00%)          (redirects, void, stamped_by)
    78,032 ( 0.00%)      }
         .           
         .               /// CR 614.5 — the actual mill count for `p` after doubling replacements
         .               /// (Bruvac the Grandiloquent: an opponent's mill is doubled, once per
         .               /// active static). 0 stays 0 (no event to replace).
         .               pub(crate) fn mill_count_for(&self, p: usize, n: usize) -> usize {
         .                   use crate::effect::StaticEffect;
         .                   if n == 0 {
         .                       return 0;
-- line 6136 ----------------------------------------
-- line 6203 ----------------------------------------
         .               /// unless another player is controlling them this turn (Mindslaver).
         .               pub fn acting_seat_for(&self, seat: usize) -> usize {
         .                   self.controlled_by.get(seat).copied().flatten().unwrap_or(seat)
         .               }
         .           
         .               /// CR 723.1 — hand a pending player-control entry to the seat whose turn is
         .               /// starting, and drop any control that expired with the previous turn.
         .               /// Called from the turn-begin path.
    15,462 ( 0.00%)      pub fn apply_pending_player_control(&mut self, seat: usize) {
         .                   self.controlled_by.resize(self.players.len(), None);
         .                   for slot in self.controlled_by.iter_mut() {
     3,436 ( 0.00%)              *slot = None;
         .                   }
         .                   // CR 723.1a — the most recently created effect wins.
     3,436 ( 0.00%)          if let Some(pos) = self.pending_player_control.iter().rposition(|(c, _)| *c == seat) {
         .                       let (_, controller) = self.pending_player_control.remove(pos);
         .                       self.pending_player_control.retain(|(c, _)| *c != seat);
         .                       self.controlled_by[seat] = Some(controller);
         .                       // CR 723.4 — the controller sees everything the controlled player
         .                       // can see, starting with their hand.
         .                       if !self.hands_revealed_to.contains(&(controller, seat)) {
         .                           self.hands_revealed_to.push((controller, seat));
         .                       }
         .                   }
    13,744 ( 0.00%)      }
         .           
         .               /// Controller of a damage source: the battlefield permanent's controller,
         .               /// else the caster of the spell currently resolving under that id.
         .               pub(crate) fn damage_source_controller(&self, id: crate::card::CardId) -> Option<usize> {
         .                   self.computed_permanent(id).map(|cp| cp.controller).or_else(|| {
         .                       match &self.resolving_source {
         .                           Some((sid, caster, _, _)) if *sid == id => Some(*caster),
         .                           _ => None,
-- line 6235 ----------------------------------------
-- line 6409 ----------------------------------------
         .                                   )
         .                               })
         .                       })
         .               }
         .           
         .               /// The Ozolith — when a creature leaves the battlefield with counters,
         .               /// move them onto a `CollectsLeaverCounters` permanent its controller
         .               /// controls. Called at the leave funnels with the just-removed card.
    34,938 ( 0.00%)      pub(crate) fn collect_leaver_counters(&mut self, card: &crate::card::CardInstance) {
         .                   use crate::effect::StaticEffect;
    11,646 ( 0.00%)          if !card.definition.is_creature() || card.counters.values().all(|&n| n == 0) {
    50,898 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,882x)
         .                       return;
         .                   }
         .                   let collector = self.battlefield.iter().find(|c| {
         .                       c.controller == card.controller
         .                           && c.definition
         .                               .static_abilities
         .                               .iter()
         .                               .any(|sa| matches!(sa.effect, StaticEffect::CollectsLeaverCounters))
-- line 6427 ----------------------------------------
-- line 6430 ----------------------------------------
         .                   let counters = card.counters.clone();
         .                   if let Some(c) = self.battlefield_find_mut(cid) {
         .                       for (kind, n) in counters {
         .                           if n > 0 {
         .                               c.add_counters(kind, n);
         .                           }
         .                       }
         .                   }
    31,056 ( 0.00%)      }
         .           
         .               /// True when a static forbids casting `def` from `zone` (Cage: any
         .               /// spell from graveyards/libraries; Jailer: noncreature spells from
         .               /// graveyards or exile).
         .               pub fn cast_from_zone_blocked(
         .                   &self,
         .                   caster: usize,
         .                   def: &crate::card::CardDefinition,
-- line 6446 ----------------------------------------
-- line 6472 ----------------------------------------
         .               /// CR 614 (Gather Specimens): if a creature would enter the battlefield
         .               /// under `intended`'s control while an opponent of theirs registered the
         .               /// steal-replacement this turn, it enters under that opponent instead.
         .               pub(crate) fn apply_etb_control_replacement(
         .                   &self,
         .                   card: &crate::card::CardInstance,
         .                   intended: usize,
         .               ) -> usize {
     6,522 ( 0.00%)          if !card.definition.is_creature() {
    28,952 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (2,174x)
         .                       return intended;
         .                   }
         .                   self.creature_etb_steal_this_turn
         .                       .iter()
         .                       .copied()
         .                       .find(|b| !self.same_team(*b, intended))
         .                       .unwrap_or(intended)
         .               }
-- line 6488 ----------------------------------------
-- line 6683 ----------------------------------------
         .                       })
         .                       .count() as i32
         .               }
         .           
         .               /// Record a dying creature's definition for the turn, so
         .               /// `Predicate::CreatureDiedThisTurnMatching` can ask what *kind* of
         .               /// creature died (Undead Sprinter's non-Zombie gate). Called from both
         .               /// death funnels while the permanent is still on the battlefield.
    14,648 ( 0.00%)      pub(crate) fn note_creature_death(&mut self, id: CardId) {
     7,324 ( 0.00%)          if let Some(c) = self.battlefield.iter().find(|c| c.id == id).cloned() {
     7,250 ( 0.00%)              self.creature_deaths_this_turn.push(c);
         .                   }
    18,310 ( 0.00%)      }
         .           
         .               /// Bookkeeping for a card leaving `p`'s graveyard: bumps the per-turn
         .               /// tally and emits `CardLeftGraveyard` so leaves-graveyard payoffs see
         .               /// mass exilers too (Rest in Peace, Go Blank, Surgical Extraction).
         .               pub(crate) fn note_left_graveyard(
         .                   &mut self,
         .                   p: usize,
         .                   card_id: CardId,
-- line 6703 ----------------------------------------
-- line 6708 ----------------------------------------
         .                   events.push(crate::game::GameEvent::CardLeftGraveyard { player: p, card_id });
         .               }
         .           
         .               /// A spell removed from the stack by a counter / ward effect goes to
         .               /// its owner's graveyard — unless it's a copy (`is_token`), which
         .               /// simply ceases to exist (CR 707.10a): it never transits the
         .               /// graveyard, so no `CardPutIntoGraveyard` / descend bookkeeping may
         .               /// fire for it.
     1,530 ( 0.00%)      pub(crate) fn countered_spell_off_stack(
         .                   &mut self,
         .                   card: crate::card::CardInstance,
         .                   counterer: usize,
         .                   events: &mut Vec<crate::game::GameEvent>,
         .               ) {
         .                   // CR 701.5 — "a spell or ability you control counters a spell"
         .                   // (Lullmage Mentor) plus the Summoning Trap per-turn tally, both keyed
         .                   // on the counterer rather than the countered spell's caster.
       680 ( 0.00%)          events.push(crate::game::GameEvent::SpellCountered { card_id: card.id, player: counterer });
       510 ( 0.00%)          if let Some(caster) = self.countered_spell_controller
       510 ( 0.00%)              && card.definition.is_creature()
     2,210 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (170x)
     1,020 ( 0.00%)              && !self.same_team(caster, counterer)
     5,950 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (170x)
         .                   {
       510 ( 0.00%)              self.players[caster].creature_spell_countered_by_opponent_this_turn = true;
   114,331 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (170x)
         .                   }
       340 ( 0.00%)          if card.is_token {
         .                       return;
         .                   }
     1,360 ( 0.00%)          self.route_to_graveyard(card, events);
   290,876 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::route_to_graveyard (170x)
         .               }
         .           
         .               /// Place `card` into its owner's graveyard, or exile it instead when a
         .               /// graveyard-hate static (Rest in Peace / Leyline of the Void) is active
         .               /// for that owner. Pushes a `PermanentExiled` event and returns `true`
         .               /// when the card was redirected to exile, so callers can suppress their
         .               /// own graveyard-specific event (CardMilled, etc.).
    16,040 ( 0.00%)      pub fn route_to_graveyard(
         .                   &mut self,
         .                   mut card: crate::card::CardInstance,
         .                   events: &mut Vec<crate::game::GameEvent>,
         .               ) -> bool {
         .                   // CR 702.183 — an Omen spell put into the graveyard from the stack
         .                   // (countered, or fizzled on an illegal target) shuffles into its
         .                   // owner's library instead.
     3,208 ( 0.00%)          if card.omen_casting {
         .                       
         .                       let owner = card.owner;
         .                       card.omen_casting = false;
         .                       card.spliced_effects.clear();
         .                       card.counters.clear();
         .                       self.players[owner].library.push(card);
     1,604 ( 0.00%)              self.shuffle_library(owner, events);
         .                       return false;
         .                   }
         .                   // CR 702.47e — splice changes are lost when the spell leaves the stack.
         .                   card.spliced_effects.clear();
         .                   // CR 122.2 — counters don't survive the zone change (replacement
         .                   // riders below add to the new object afterward).
         .                   card.counters.clear();
     4,812 ( 0.00%)          card.keyword_counters.clear();
    30,476 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::KeywordCounters::clear (1,604x)
         .                   // CR 712.16 — a melded shell dies as its two component cards.
     3,208 ( 0.00%)          if !card.meld_parts.is_empty() {
         .                       let mut card = card;
         .                       let mut any_exiled = false;
         .                       for part in std::mem::take(&mut card.meld_parts) {
         .                           any_exiled |= self.route_to_graveyard(part, events);
         .                       }
         .                       return any_exiled;
         .                   }
         .                   // CR 702.140e — a merged (mutated) permanent dies as its components.
     3,208 ( 0.00%)          if !card.mutate_stack.is_empty() {
         .                       let mut card = card;
         .                       let mut any_exiled = false;
         .                       for part in std::mem::take(&mut card.mutate_stack) {
         .                           any_exiled |= self.route_to_graveyard(part, events);
         .                       }
         .                       return any_exiled;
         .                   }
     1,604 ( 0.00%)          let owner = card.owner;
         .                   // CR 717.6 — an Astrotorium-backed Attraction card never reaches a
         .                   // graveyard; it goes to its owner's face-up junkyard pile instead.
     3,208 ( 0.00%)          if !card.definition.attraction_lights.is_empty() {
         .                       let mut card = card;
         .                       card.controller = owner;
         .                       self.players[owner].attraction_junkyard.push(card);
         .                       return false;
         .                   }
         .                   // CR 614.6 — "shuffle into its owner's library instead" (Darksteel
         .                   // Colossus). The card never touches the graveyard.
     3,208 ( 0.00%)          if card.definition.shuffles_into_library_instead {
         .                       use rand::seq::SliceRandom;
         .                       self.players[owner].library.push(card);
         .                       let mut rng = self.rng.draw();
         .                       self.players[owner].library.shuffle(&mut rng);
         .                       return false;
         .                   }
     8,020 ( 0.00%)          if self.graveyard_exiled_for(&card) || card.disturb_back_exiles() {
     8,020 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::disturb_back_exiles (1,604x)
         .                       let cid = card.id;
         .                       let mut card = card;
         .                       let (_, void, stamped_by) = self.graveyard_exile_redirects(&card);
         .                       if void {
         .                           card.add_counters(crate::card::CounterType::Void, 1);
         .                       }
         .                       if let Some(src) = stamped_by {
         .                           card.exiled_with = Some(src);
         .                       }
         .                       self.exile.push(card);
         .                       events.push(crate::game::GameEvent::PermanentExiled { card_id: cid });
         .                       true
         .                   } else {
     1,604 ( 0.00%)              let cid = card.id;
         .                       let is_land = card.definition.card_types.contains(&crate::card::CardType::Land);
     6,416 ( 0.00%)              self.players[owner].send_to_graveyard(card);
 1,190,517 ( 0.04%)  => /home/user/Crabomination/crabomination/src/player.rs:crabomination::player::PlayerData::send_to_graveyard (1,604x)
   480,347 ( 0.02%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,604x)
     8,020 ( 0.00%)              events.push(crate::game::GameEvent::CardPutIntoGraveyard {
         .                           player: owner,
         .                           card_id: cid,
         .                           is_land,
         .                       });
         .                       false
         .                   }
    16,040 ( 0.00%)      }
         .           
         .               /// CR 702.66 — true if player `seat` controls a permanent granting
         .               /// "spells you cast have delve" (Teval, Arbiter of Virtue). Lets the
         .               /// cast path accept a delve-cards list on any spell, not just those
         .               /// printed with `Keyword::Delve`.
         .               pub fn controller_grants_spells_delve(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|c| {
-- line 6835 ----------------------------------------
-- line 6845 ----------------------------------------
         .               /// 1. The directly-settable `Player.cannot_gain_life` flag (set by
         .               ///    emblems / once-per-game state — currently dormant; reserved for
         .               ///    permanent grants).
         .               /// 2. Any active `StaticEffect::PlayerCannotGainLife` on the
         .               ///    battlefield whose `target` resolves to include `seat`.
         .               ///
         .               /// Consulted by `GameState::adjust_life` to drop positive deltas
         .               /// targeting `seat` on the floor.
     1,318 ( 0.00%)      pub fn player_cannot_gain_life_now(&self, seat: usize) -> bool {
         .                   use crate::effect::{PlayerStaticTarget, StaticEffect};
     2,636 ( 0.00%)          if self.players[seat].life_locked_this_turn {
         .                       return true;
         .                   }
     5,272 ( 0.00%)          if self.players[seat].cannot_gain_life || self.players[seat].cannot_gain_life_this_turn {
         .                       return true;
         .                   }
         .                   self.battlefield.iter().any(|src| {
         .                       src.definition.static_abilities.iter().any(|sa| {
         .                           if let StaticEffect::PlayerCannotGainLife { target } = &sa.effect {
         .                               match target {
         .                                   PlayerStaticTarget::Controller => src.controller == seat,
         .                                   PlayerStaticTarget::EachOpponent => src.controller != seat,
-- line 6866 ----------------------------------------
-- line 6869 ----------------------------------------
         .                                       src.attached_to_player == Some(seat)
         .                                   }
         .                               }
         .                           } else {
         .                               false
         .                           }
         .                       })
         .                   })
     2,636 ( 0.00%)      }
         .           
         .               /// CR 615.12 — True if any active `StaticEffect::DamageCantBePrevented`
         .               /// is on the battlefield (Sulfuric Vortex, Sunspine Lynx). Consulted by
         .               /// `apply_prevention_shields` to bypass every shield.
         .               pub fn damage_cant_be_prevented_now(&self) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|src| {
         .                       src.definition
-- line 6885 ----------------------------------------
-- line 6890 ----------------------------------------
         .               }
         .           
         .               /// CR 614 — True if `seat`'s life loss should be doubled right now: it's an
         .               /// opponent of a player who controls an `OpponentLifeLossDoubledDuringYourTurn`
         .               /// permanent and it's that controller's turn (Bloodletter of Aclazotz).
         .               pub fn life_loss_doubled_now(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|src| {
   113,588 ( 0.00%)              src.controller == self.active_player_idx
   296,140 ( 0.01%)                  && !self.same_team(seat, src.controller)
 1,036,490 ( 0.03%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (29,614x)
         .                           && src.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, StaticEffect::OpponentLifeLossDoubledDuringYourTurn)
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 119.8 — True if `seat` cannot lose life right now. Mirror of
         .               /// `player_cannot_gain_life_now`. Scans the battlefield for any
         .               /// `StaticEffect::PlayerCannotLoseLife` whose `target` resolves to
         .               /// include `seat`. Consulted by `adjust_life` (negative deltas) and
         .               /// by the lose-life paths (`Effect::LoseLife`, drain-target gates).
         .               pub fn player_cannot_lose_life_now(&self, seat: usize) -> bool {
         .                   use crate::effect::{PlayerStaticTarget, StaticEffect};
     5,712 ( 0.00%)          if self.players[seat].life_locked_this_turn {
         .                       return true;
         .                   }
         .                   self.battlefield.iter().any(|src| {
         .                       src.definition.static_abilities.iter().any(|sa| {
         .                           if let StaticEffect::PlayerCannotLoseLife { target } = &sa.effect {
         .                               match target {
         .                                   PlayerStaticTarget::Controller => src.controller == seat,
         .                                   PlayerStaticTarget::EachOpponent => src.controller != seat,
-- line 6921 ----------------------------------------
-- line 6930 ----------------------------------------
         .                       })
         .                   })
         .               }
         .           
         .               /// CR 104.3d — true when `seat` can't lose the game right now: Angel's
         .               /// Grace this turn, a Platinum-Angel static they control, or an
         .               /// opponent's Abyssal-Persecutor static ("your opponents can't lose").
         .               /// Concession (CR 104.3a) bypasses this.
     1,400 ( 0.00%)      pub fn player_cant_lose_game(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
       400 ( 0.00%)          if self.players[seat].cant_lose_this_turn {
         .                       return true;
         .                   }
         .                   self.battlefield.iter().any(|src| {
         .                       src.definition.static_abilities.iter().any(|sa| match sa.effect {
         .                           StaticEffect::ControllerCantLoseGame => src.controller == seat,
         .                           StaticEffect::ControllerCantWinGame => !self.same_team(src.controller, seat),
         .                           _ => false,
         .                       })
         .                   })
     1,600 ( 0.00%)      }
         .           
         .               /// Phyrexian Unlife — true when `seat` controls a
         .               /// `ControllerDoesntLoseFromLife` static: the life-≤-0 loss SBA is
         .               /// skipped, and at ≤ 0 life all damage to them lands as poison.
         .               /// CR 609.4b — true while any permanent grants every player "you may spend
         .               /// mana as though it were mana of any color" (Mycosynth Lattice). Costs
         .               /// paid through `try_pay_after_snapshot_mode` have their coloured pips
         .               /// relaxed to generic while this holds.
-- line 6958 ----------------------------------------
-- line 6964 ----------------------------------------
         .               /// permission.
         .               pub fn spend_mana_as_any_color_active_for(&self, seat: Option<usize>) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   if seat.is_some_and(|s| self.players[s].may_spend_any_color_this_turn) {
         .                       return true;
         .                   }
         .                   // False Dawn folds every colour into white and then lets white pay for
         .                   // anything, so the relaxed-cost path covers both clauses.
    25,404 ( 0.00%)          if !self.colored_mana_becomes_this_turn.is_empty() {
         .                       return true;
         .                   }
         .                   self.battlefield.iter().any(|src| {
         .                       src.definition
         .                           .static_abilities
         .                           .iter()
         .                           .any(|sa| matches!(sa.effect, StaticEffect::PlayersMaySpendManaAsAnyColor))
         .                   })
-- line 6980 ----------------------------------------
-- line 6985 ----------------------------------------
         .               /// chosen name, Emissary's Ploy's chosen mana value.
         .               ///
         .               /// One battlefield pass, not two: the seat-agnostic
         .               /// `PlayersMaySpendManaAsAnyColor` scan that
         .               /// [`spend_mana_as_any_color_active_for`](Self::spend_mana_as_any_color_active_for)
         .               /// would take is folded into the same walk. Every payment asks this
         .               /// question (`try_pay_after_snapshot_mode` is the only caller of the
         .               /// relaxation), so the walk count is per-payment.
    62,244 ( 0.00%)      pub fn spend_mana_as_any_color_for_spell(
         .                   &self,
         .                   seat: Option<usize>,
         .                   kind: &crate::mana::SpellKind,
         .               ) -> bool {
         .                   use crate::effect::StaticEffect;
    17,784 ( 0.00%)          if seat.is_some_and(|s| self.players[s].may_spend_any_color_this_turn) {
         .                       return true;
         .                   }
         .                   // False Dawn folds every colour into white and then lets white pay for
         .                   // anything, so the relaxed-cost path covers both clauses.
    17,784 ( 0.00%)          if !self.colored_mana_becomes_this_turn.is_empty() {
         .                       return true;
         .                   }
         .                   let spell_permission = |c: &crate::card::CardInstance| {
   476,724 ( 0.02%)              seat.is_some_and(|s| c.controller == s && !c.face_down)
         .                           && c.definition.static_abilities.iter().any(|sa| match sa.effect {
         .                               StaticEffect::MaySpendManaAsAnyColorForNamedSpells => {
         .                                   kind.name.is_some() && c.named_card.as_deref() == kind.name
         .                               }
         .                               StaticEffect::MaySpendManaAsAnyColorForCreaturesWithChosenMv => {
         .                                   kind.creature && c.chosen_number == Some(kind.mana_value)
         .                               }
         .                               _ => false,
         .                           })
   159,570 ( 0.01%)          };
         .                   let board = self.battlefield.iter().any(|c| {
         .                       c.definition
         .                           .static_abilities
         .                           .iter()
         .                           .any(|sa| matches!(sa.effect, StaticEffect::PlayersMaySpendManaAsAnyColor))
 1,116,990 ( 0.04%)                  || spell_permission(c)
 2,533,260 ( 0.08%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell::{{closure}} (159,570x)
         .                   });
    53,352 ( 0.00%)          board
         .                       || seat.is_some_and(|s| self.players[s].command.iter().any(spell_permission))
    80,028 ( 0.00%)      }
         .           
         .               /// The cost as it must actually be paid: with a Lattice-style
         .               /// spend-as-any-color permission active, every coloured pip becomes generic
         .               /// (`{C}` pips are unaffected — CR 609.4b speaks of colours only).
   101,616 ( 0.00%)      pub fn relax_cost_colors(&self, cost: &crate::mana::ManaCost) -> crate::mana::ManaCost {
         .                   self.relax_cost_colors_for(None, cost)
   101,616 ( 0.00%)      }
         .           
         .               /// `relax_cost_colors_for`, but consulting the name-restricted permission
         .               /// too (Unexpected Potential's chosen name).
         .               ///
         .               /// Borrowed when no permission is active — which is every payment in a
         .               /// game without a Lattice-style effect — so the common path costs no
         .               /// `Vec` clone.
         .               pub fn relax_cost_colors_for_spell<'a>(
         .                   &self,
         .                   seat: Option<usize>,
         .                   cost: &'a crate::mana::ManaCost,
         .                   kind: &crate::mana::SpellKind,
         .               ) -> std::borrow::Cow<'a, crate::mana::ManaCost> {
    71,136 ( 0.00%)          Self::relax_cost_colors_known(self.spend_mana_as_any_color_for_spell(seat, kind), cost)
 6,754,674 ( 0.22%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::spend_mana_as_any_color_for_spell (8,892x)
   195,624 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::relax_cost_colors_known (8,892x)
         .               }
         .           
         .               /// The relaxation itself, with the permission already answered — the one
         .               /// body the three `relax_cost_colors*` forms share. Borrowed when the
         .               /// permission is inactive, which is every board without a Lattice-style
         .               /// effect.
         .               ///
         .               /// Each coloured / hybrid pip becomes one mana of any type. Phyrexian pips
         .               /// keep their pay-2-life alternative and so are left for the normal
         .               /// payment path.
   172,752 ( 0.01%)      pub(crate) fn relax_cost_colors_known(
         .                   active: bool,
         .                   cost: &crate::mana::ManaCost,
         .               ) -> std::borrow::Cow<'_, crate::mana::ManaCost> {
         .                   use crate::mana::ManaSymbol;
    43,188 ( 0.00%)          if !active {
    86,376 ( 0.00%)              return std::borrow::Cow::Borrowed(cost);
         .                   }
         .                   let mut relaxed = 0;
         .                   let mut symbols: Vec<ManaSymbol> = Vec::with_capacity(cost.symbols.len());
         .                   for s in &cost.symbols {
         .                       match s {
         .                           ManaSymbol::Colored(_)
         .                           | ManaSymbol::Hybrid(..)
         .                           | ManaSymbol::MonoHybrid(..) => relaxed += 1,
         .                           other => symbols.push(*other),
         .                       }
         .                   }
         .                   if relaxed > 0 {
         .                       symbols.push(ManaSymbol::Generic(relaxed));
         .                   }
         .                   std::borrow::Cow::Owned(crate::mana::ManaCost::new(symbols))
   172,752 ( 0.01%)      }
         .           
         .               /// The seat-aware form: also relaxes for a seat holding this turn's North
         .               /// Star permission.
         .               pub fn relax_cost_colors_for(
         .                   &self,
         .                   seat: Option<usize>,
         .                   cost: &crate::mana::ManaCost,
         .               ) -> crate::mana::ManaCost {
    38,106 ( 0.00%)          Self::relax_cost_colors_known(self.spend_mana_as_any_color_active_for(seat), cost)
   279,444 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::relax_cost_colors_known (12,702x)
         .                       .into_owned()
         .               }
         .           
         .               /// Short human-readable lines for the turn-scoped continuous effects that
         .               /// leave no board trace — a land-tap mana replacement and any floating
         .               /// "this turn, whenever …" watcher. Surfaced as `ClientView.turn_effects`
         .               /// so a player can tell why their Island is producing {C}.
         .               pub fn turn_effect_notes(&self) -> Vec<String> {
-- line 7099 ----------------------------------------
-- line 7139 ----------------------------------------
         .               #[doc(hidden)]
         .               pub fn damage_prevented_sources_debug(&self) -> Vec<crate::game::types::PreventedSource> {
         .                   self.damage_prevented_sources.clone()
         .               }
         .           
         .               pub fn player_unlife_active(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   self.battlefield.iter().any(|src| {
   751,068 ( 0.02%)              src.controller == seat
         .                           && src.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, StaticEffect::ControllerDoesntLoseFromLife)
         .                           })
         .                   })
       102 ( 0.00%)      }
         .           
         .               /// CR 104.3d — true when `seat` can't win the game right now: their own
         .               /// Abyssal-Persecutor static, an opponent's Platinum-Angel static, or an
         .               /// opponent under Angel's Grace this turn. Gates the win *effects*
         .               /// (`Effect::WinGame`, `WinInsteadOfDrawFromEmpty`); elimination-based
         .               /// endings are already blocked by the paired can't-lose checks.
         .               pub fn player_cant_win_game(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
-- line 7160 ----------------------------------------
-- line 7174 ----------------------------------------
         .                       })
         .                   })
         .               }
         .           
         .               /// Angel's Grace / Worship — clamp a would-be damage life delta so it
         .               /// can't take `seat` below 1 life while a floor effect is active. The
         .               /// damage itself is still dealt (CR 614 — only the life reduction is
         .               /// replaced); a life total already ≤ 1 just doesn't move.
    25,704 ( 0.00%)      pub(crate) fn clamp_damage_to_life_floor(&self, seat: usize, amount: u32) -> u32 {
         .                   use crate::effect::StaticEffect;
     5,712 ( 0.00%)          let floored = self.players[seat].damage_floor_this_turn
         .                       || self.battlefield.iter().any(|src| {
   113,588 ( 0.00%)                  src.controller == seat
         .                               && src.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(
         .                                       sa.effect,
         .                                       StaticEffect::DamageWontReduceControllerLifeBelowOne { requires_creature }
         .                                       if !requires_creature
         .                                           || self.battlefield.iter().any(|c| {
         .                                               c.controller == seat && c.definition.is_creature()
         .                                           })
-- line 7194 ----------------------------------------
-- line 7195 ----------------------------------------
         .                                   )
         .                               })
         .                       });
         .                   if floored {
         .                       amount.min(self.effective_life(seat).saturating_sub(1).max(0) as u32)
         .                   } else {
         .                       amount
         .                   }
    22,848 ( 0.00%)      }
         .           
         .               /// CR 705.3 — coin-flip advantage for `seat` from active
         .               /// `StaticEffect::CoinFlipAdvantage` permanents (Krark's Thumb). Summed
         .               /// so multiple sources stack; added to `Player.coin_flip_advantage` by
         .               /// the `Effect::FlipCoin` resolver.
         .               /// CR 705.1/705.3 — flip one coin for `player`, honoring Krark's-Thumb
         .               /// style advantage (replay + treat as heads if any replay is heads).
         .               /// Returns true for heads.
-- line 7211 ----------------------------------------
-- line 7268 ----------------------------------------
         .                           } else {
         .                               false
         .                           }
         .                       })
         .                   })
         .               }
         .           
         .               /// CR 614 — Nefarious Lich: `seat`'s life gains become card draws.
    10,544 ( 0.00%)      pub fn life_gain_becomes_draw_now(&self, seat: usize) -> bool {
         .                   use crate::effect::StaticEffect;
     1,318 ( 0.00%)          self.battlefield.iter().any(|src| {
    64,676 ( 0.00%)              src.controller == seat
         .                           && src.definition.static_abilities.iter().any(|sa| {
         .                               matches!(
         .                                   self.active_static(&sa.effect, src),
         .                                   Some(StaticEffect::LifeGainBecomesDraw)
         .                               )
         .                           })
         .                   })
    10,544 ( 0.00%)      }
         .           
         .               /// CR 119.10 / 614 — total life-gain bonus currently applied to `seat`
         .               /// by `StaticEffect::LifeGainBonus` statics (Honor Troll's "+1 to each
         .               /// gain"). Bonuses from multiple sources stack additively.
         .               pub fn life_gain_bonus_now(&self, seat: usize) -> i32 {
         .                   use crate::effect::{PlayerStaticTarget, StaticEffect};
         .                   self.battlefield
         .                       .iter()
-- line 7295 ----------------------------------------
-- line 7350 ----------------------------------------
         .               }
         .           
         .               /// CR 615 — the turn's global damage rewrite `(at_least, becomes)`
         .               /// (Equal Treatment), or `None`.
         .               pub fn damage_rewritten_this_turn(&self) -> Option<(u32, u32)> {
         .                   self.damage_becomes_this_turn
         .               }
         .           
    14,200 ( 0.00%)      pub fn draw_cap_for(&self, seat: usize) -> Option<u32> {
         .                   use crate::effect::{PlayerStaticTarget, StaticEffect};
         .                   self.battlefield
         .                       .iter()
         .                       .flat_map(|src| {
         .                           src.definition.static_abilities.iter().filter_map(move |sa| {
         .                               if let StaticEffect::CapDrawsPerTurn { target, max } = &sa.effect {
         .                                   let hits = match target {
         .                                       PlayerStaticTarget::Controller => src.controller == seat,
-- line 7366 ----------------------------------------
-- line 7372 ----------------------------------------
         .                                   };
         .                                   hits.then_some(*max)
         .                               } else {
         .                                   None
         .                               }
         .                           })
         .                       })
         .                       .min()
    17,040 ( 0.00%)      }
         .           
         .               /// CR 121.2b / 121.3 — a player who can't draw may not *choose* to.
         .               /// An empty library does not block the choice (CR 121.3 says it can still
         .               /// be taken); only an active can't-draw / draw-cap effect does.
         .               pub fn may_choose_to_draw(&self, seat: usize, amount: i32) -> bool {
         .                   if amount <= 0 {
         .                       return true;
         .                   }
-- line 7388 ----------------------------------------
-- line 7443 ----------------------------------------
         .           
         .               /// The player who currently holds priority.
         .               pub fn player_with_priority(&self) -> usize {
         .                   self.priority.player_with_priority
         .               }
         .           
         .               /// Give priority to the active player and reset consecutive passes.
         .               pub fn give_priority_to_active(&mut self) {
    82,644 ( 0.00%)          self.priority.player_with_priority = self.active_player_idx;
    46,294 ( 0.00%)          self.priority.consecutive_passes = 0;
     6,710 ( 0.00%)      }
         .           
         .               // ── Layer system ──────────────────────────────────────────────────────────
         .           
         .               /// Compute the current derived state of all battlefield permanents after
         .               /// applying all active continuous effects in layer order.
     2,790 ( 0.00%)      pub fn compute_battlefield(&self) -> Vec<ComputedPermanent> {
     1,240 ( 0.00%)          if let Some(fx) = self.frozen_effects() {
    13,640 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (310x)
         .                       return crate::game::layers::apply_layers(&self.battlefield, &fx);
         .                   }
       310 ( 0.00%)          crate::game::layers::apply_layers(&self.battlefield, &self.gather_continuous_effects())
     2,790 ( 0.00%)      }
         .           
         .               /// The computed views for just `ids`, in that order, from one gather.
         .               ///
         .               /// `apply_layers` is exactly `apply_layers_one` mapped over the
         .               /// battlefield, so these are the same answers `compute_battlefield` gives
         .               /// — for a caller that reads a handful of permanents by id, at a
         .               /// fraction of the per-card work. Ids not on the battlefield are skipped,
         .               /// so `find(id)` on the result misses exactly as it would on the whole
         .               /// view. Use it only where the consumers are all id lookups; a
         .               /// whole-board scan of the result would silently see a subset.
    96,520 ( 0.00%)      pub(crate) fn compute_permanents(&self, ids: &[CardId]) -> Vec<ComputedPermanent> {
         .                   fn go(
         .                       bf: &[crate::card::CardInstance],
         .                       fx: &[ContinuousEffect],
         .                       ids: &[CardId],
         .                   ) -> Vec<ComputedPermanent> {
         .                       ids.iter()
         .                           .filter_map(|id| bf.iter().find(|c| c.id == *id))
         .                           .map(|c| crate::game::layers::apply_layers_one(c, fx))
         .                           .collect()
         .                   }
    51,364 ( 0.00%)          if let Some(fx) = self.frozen_effects() {
   462,956 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (9,652x)
     6,378 ( 0.00%)              return go(&self.battlefield, &fx, ids);
         .                   }
     3,274 ( 0.00%)          go(&self.battlefield, &self.gather_continuous_effects(), ids)
    67,564 ( 0.00%)      }
         .           
         .               /// Cheap over-approximation of "some battlefield permanent's *computed*
         .               /// keyword set can contain one of `kws`", answered without a layer pass.
         .               /// `false` is authoritative, so a whole-board requirement loop keyed on
         .               /// one of `kws` can be skipped — and with it the whole-board pass its
         .               /// consumers would otherwise need (see [`compute_permanents`]).
         .               ///
         .               /// `compute_permanent` seeds `keywords` from exactly four places
-- line 7498 ----------------------------------------
-- line 7501 ----------------------------------------
         .               /// `Modification::AddKeyword` in the gathered set. `AddKeyword` is the
         .               /// only *additive* keyword modification — `RemoveKeyword` /
         .               /// `CantHaveKeyword` / `RemoveAllAbilities` only shrink, and the two
         .               /// text-changing rewrites retype `Protection` / `Landwalk` in place — so
         .               /// the disjunction over those four never misses. Debug builds re-run the
         .               /// whole-board pass whenever this says `false`, so the suite audits it.
         .               ///
         .               /// [`compute_permanents`]: Self::compute_permanents
    72,864 ( 0.00%)      pub(crate) fn board_keyword_in_scope(&self, kws: &[Keyword]) -> bool {
         .                   self.board_keyword_matching(|k| kws.contains(k))
    72,864 ( 0.00%)      }
         .           
         .               /// [`board_keyword_in_scope`] over a predicate, for the keywords that
         .               /// carry a payload (`CumulativeUpkeep(_)`, `DoesntUntapWhileCounter(_)`).
         .               ///
         .               /// [`board_keyword_in_scope`]: Self::board_keyword_in_scope
         .               pub(crate) fn board_keyword_matching(&self, pred: impl Fn(&Keyword) -> bool) -> bool {
         .                   let granted = |fx: &[ContinuousEffect]| {
         .                       fx.iter()
    30,714 ( 0.00%)                  .any(|e| matches!(&e.modification, Modification::AddKeyword(k) if pred(k)))
         .                   };
    35,844 ( 0.00%)          let hit = self.battlefield.iter().any(|c| {
    81,152 ( 0.00%)              c.definition.keywords.iter().any(&pred)
         .                           || c.granted_keywords_eot.iter().any(&pred)
         .                           || c.keyword_counters.iter().any(|(k, n)| *n > 0 && pred(k))
    46,224 ( 0.00%)          }) || match self.frozen_effects() {
 3,784,254 ( 0.12%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (1,718x)
    13,456 ( 0.00%)              Some(fx) => granted(&fx),
         .                       None => granted(&self.gather_continuous_effects()),
         .                   };
         .                   #[cfg(debug_assertions)]
         .                   if !hit {
         .                       debug_assert!(
         .                           !self.compute_battlefield().iter().any(|c| c.keywords.iter().any(&pred)),
         .                           "board_keyword_matching said no, but the computed board has a match"
         .                       );
-- line 7535 ----------------------------------------
-- line 7545 ----------------------------------------
         .               /// is the same answer the full view gives. With one live the whole board
         .               /// is computed as before. Roughly half a bench board is non-creature.
         .               pub(crate) fn compute_battlefield_creatures(&self) -> Vec<ComputedPermanent> {
         .                   fn go(
         .                       bf: &[crate::card::CardInstance],
         .                       fx: &[ContinuousEffect],
         .                   ) -> Vec<ComputedPermanent> {
         .                       let type_changing = fx.iter().any(|e| {
    14,904 ( 0.00%)                  matches!(
         .                               e.modification,
         .                               Modification::AddCardType(_)
         .                                   | Modification::RemoveCardType(_)
         .                                   | Modification::SetCardTypes(_)
         .                           )
         .                       });
         .                       if type_changing {
         .                           return crate::game::layers::apply_layers(bf, fx);
         .                       }
         .                       bf.iter()
   190,616 ( 0.01%)                  .filter(|c| c.definition.is_creature())
 2,932,508 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (190,616x)
         .                           .map(|c| crate::game::layers::apply_layers_one(c, fx))
         .                           .collect()
         .                   }
    32,010 ( 0.00%)          if let Some(fx) = self.frozen_effects() {
   469,480 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (10,670x)
         .                       return go(&self.battlefield, &fx);
         .                   }
    10,670 ( 0.00%)          go(&self.battlefield, &self.gather_continuous_effects())
         .               }
         .           
         .               /// CR 113.10b — the battlefield permanents a `RemoveAllAbilities`
         .               /// continuous effect currently strips (Turn to Frog, Lignify).
         .               ///
         .               /// The trigger dispatcher wants only this one bit per permanent, and
         .               /// running a whole-board layer pass for it was ~10 % of simulator
         .               /// instructions. A board with no strip effect in scope — the
-- line 7579 ----------------------------------------
-- line 7580 ----------------------------------------
         .               /// overwhelming majority — answers "nothing is stripped" from the
         .               /// gathered effect set alone, and only the rare board that has one
         .               /// pays for the layer pass.
         .               ///
         .               /// `strip_on_battlefield` is the presence gate's battlefield leg
         .               /// (`battlefield.iter().any(card_can_strip_abilities)`), supplied by the
         .               /// caller because both call sites already walk the battlefield for it —
         .               /// see [`Self::dispatch_board_scan`].
   593,780 ( 0.02%)      pub(crate) fn permanents_with_abilities_removed(
         .                   &self,
         .                   strip_on_battlefield: bool,
         .               ) -> Vec<CardId> {
         .                   fn strip(state: &GameState, fx: &[ContinuousEffect]) -> Vec<CardId> {
         .                       if !fx.iter().any(|e| matches!(e.modification, Modification::RemoveAllAbilities)) {
         .                           return Vec::new();
         .                       }
         .                       crate::game::layers::apply_layers(&state.battlefield, fx)
         .                           .into_iter()
         .                           .filter(|c| c.lost_all_abilities)
         .                           .map(|c| c.id)
         .                           .collect()
         .                   }
   237,512 ( 0.01%)          if let Some(fx) = self.frozen_effects() {
 2,612,632 ( 0.08%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (59,378x)
         .                       return strip(self, &fx);
         .                   }
         .                   // The gather is the whole cost here: `dispatch_triggers_for_events` is
         .                   // `&mut self`, so it runs unfrozen and rebuilds the full effect set
         .                   // once per dispatch to answer one bit that is false on every board
         .                   // without a strip effect. Ask the cheap presence gate first — it walks
         .                   // the battlefield once instead of gathering.
   118,756 ( 0.00%)          if !(strip_on_battlefield || self.ability_strip_off_battlefield()) {
         .                       // The cross-check gathers, so it must not run re-entrantly: a
         .                       // gather started inside one would clear `in_layer_gather` on the
         .                       // way out and let the outer one serve the memo mid-gather.
         .                       debug_assert!(
         .                           self.in_layer_gather.load(std::sync::atomic::Ordering::Relaxed)
         .                               || !self
         .                                   .gather_continuous_effects()
         .                                   .iter()
         .                                   .any(|e| matches!(e.modification, Modification::RemoveAllAbilities)),
         .                           "the ability-strip presence gate missed a RemoveAllAbilities source",
         .                       );
         .                       return Vec::new();
         .                   }
         .                   strip(self, &self.gather_continuous_effects())
   475,024 ( 0.02%)      }
         .           
         .               /// The off-battlefield half of a cheap over-approximation of "the gathered
         .               /// effect set can contain a layer-6 `RemoveAllAbilities`", answered
         .               /// without gathering. `false` on both halves is authoritative; `true`
         .               /// only means the gather has to run. The battlefield half is
         .               /// `battlefield.iter().any(card_can_strip_abilities)`, split out because
         .               /// the trigger dispatcher already walks the battlefield for it
         .               /// ([`Self::dispatch_board_scan`]).
-- line 7633 ----------------------------------------
-- line 7662 ----------------------------------------
         .               /// `computed_permanent` / `compute_battlefield` call inside reuses one
         .               /// gather instead of rebuilding the full effect set per call. Sound by
         .               /// construction: `f` only receives `&GameState`, so none of the gather's
         .               /// inputs (battlefield, continuous effects, attachments…) can change
         .               /// while frozen. The memo fills lazily on the first computed read, so a
         .               /// scope that never needs the layer system costs nothing. Nested freezes
         .               /// reuse the outer memo. Use this around any read-only loop that filters
         .               /// or inspects many permanents.
   495,578 ( 0.02%)      pub fn with_frozen_layers<R>(&self, f: impl FnOnce(&Self) -> R) -> R {
   168,176 ( 0.01%)          self.layer_freeze.lock().depth += 1;
         .                   // Decrement on drop (not after `f`) so a panicking assertion inside
         .                   // a test closure can't leave a stale memo behind.
         .                   struct Unfreeze<'a>(&'a GameState);
         .                   impl Drop for Unfreeze<'_> {
   961,520 ( 0.03%)              fn drop(&mut self) {
   274,720 ( 0.01%)                  let mut st = self.0.layer_freeze.lock();
   137,360 ( 0.00%)                  st.depth -= 1;
   137,360 ( 0.00%)                  if st.depth == 0 {
    69,670 ( 0.00%)                      st.memo = None;
         .                               st.perms.clear();
         .                           }
 1,098,880 ( 0.04%)              }
         .                   }
   139,958 ( 0.00%)          let _guard = Unfreeze(self);
    37,416 ( 0.00%)          f(self)
   476,958 ( 0.02%)      }
         .           
         .               /// Enter a freeze scope without a closure — pair with
         .               /// [`freeze_layers_pop`](Self::freeze_layers_pop). Prefer
         .               /// [`with_frozen_layers`](Self::with_frozen_layers) (panic-safe) except
         .               /// on recursion-hot paths where the closure+guard frame cost matters.
         .               pub(crate) fn freeze_layers_push(&self) {
     2,170 ( 0.00%)          self.layer_freeze.lock().depth += 1;
         .               }
         .           
         .               /// Exit a freeze scope opened by [`freeze_layers_push`](Self::freeze_layers_push).
    17,360 ( 0.00%)      pub(crate) fn freeze_layers_pop(&self) {
     2,170 ( 0.00%)          let mut st = self.layer_freeze.lock();
     2,170 ( 0.00%)          st.depth -= 1;
     2,170 ( 0.00%)          if st.depth == 0 {
         .                       st.memo = None;
         .                       st.perms.clear();
         .                   }
    17,360 ( 0.00%)      }
         .           
         .               /// The memoized continuous-effect set when inside a
         .               /// [`with_frozen_layers`](Self::with_frozen_layers) scope (gathering and
         .               /// caching it on first use), else `None`.
 1,022,952 ( 0.03%)      fn frozen_effects(&self) -> Option<std::sync::Arc<Vec<ContinuousEffect>>> {
         .                   // Mid-gather reads see the printed-types fallback (`in_layer_gather`
         .                   // reentrancy guard); don't serve or populate the memo from them.
   292,272 ( 0.01%)          if self.in_layer_gather.load(std::sync::atomic::Ordering::Relaxed) {
         .                       return None;
         .                   }
         .                   {
   146,136 ( 0.00%)              let st = self.layer_freeze.lock();
   292,272 ( 0.01%)              if st.depth == 0 {
         .                           return None;
         .                       }
   290,016 ( 0.01%)              if let Some(fx) = &st.memo {
         .                           return Some(fx.clone());
         .                       }
         .                   }
         .                   // First computed read in this scope: gather outside the lock (the
         .                   // gather itself re-enters `frozen_effects` via guarded eval paths).
         .                   let fx = std::sync::Arc::new(self.gather_continuous_effects());
         .                   let mut st = self.layer_freeze.lock();
    18,916 ( 0.00%)          st.memo = Some(fx.clone());
    18,916 ( 0.00%)          Some(fx)
 1,169,088 ( 0.04%)      }
         .           
         .               /// Collect every continuous effect currently active in the game: the
         .               /// resolved-spell/ability effects in `continuous_effects`, plus the
         .               /// implicit effects derived each recompute from static abilities,
         .               /// equipment attachments, combat keyword grants, characteristic-
         .               /// defining P/T, life-gain pump/anthem tables, and graveyard-resident
         .               /// anthems. Shared by [`compute_battlefield`] (applies to every
         .               /// permanent) and [`computed_permanent`] (applies to just one).
         .               fn gather_continuous_effects(&self) -> Vec<ContinuousEffect> {
         .                   // Reentrancy guard — see `in_layer_gather`. Passes below that
         .                   // evaluate selection requirements must not recurse back into
         .                   // `computed_permanent`.
         .                   use std::sync::atomic::Ordering;
         .                   self.in_layer_gather.store(true, Ordering::Relaxed);
   245,086 ( 0.01%)          let out = self.gather_continuous_effects_inner();
21,356,953 ( 0.69%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::gather_continuous_effects_inner (10,670x)
         .                   self.in_layer_gather.store(false, Ordering::Relaxed);
   421,512 ( 0.01%)          out
         .               }
         .           
 1,918,170 ( 0.06%)      fn gather_continuous_effects_inner(&self) -> Vec<ContinuousEffect> {
         .                   // Include static-ability effects from permanents currently on the battlefield.
   255,756 ( 0.01%)          let mut all_effects: Vec<ContinuousEffect> = (*self.continuous_effects).clone();
 4,219,974 ( 0.14%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (127,878x)
         .                   // Most passes below are gated on `definition.static_abilities`, and a
         .                   // typical board is vanilla creatures and basic lands. Filter once so
         .                   // those passes walk the handful of cards that can contribute instead
         .                   // of striding the whole battlefield ~40 times. Order is preserved, so
         .                   // the emitted effect sequence is unchanged.
         .                   //
         .                   // The same walk answers "can this whole-board pass fire at all" for the
         .                   // ten passes further down that iterate the battlefield directly. Each
-- line 7761 ----------------------------------------
-- line 7765 ----------------------------------------
         .                   // place and iterates an empty slice when its flag is clear, so the
         .                   // emitted effect sequence is unchanged.
         .                   let mut sa_cards: Vec<&CardInstance> = Vec::new();
         .                   let (mut any_equipped_bonus, mut any_soulbond_bonus, mut any_attached) =
         .                       (false, false, false);
         .                   let (mut any_reconfigure, mut any_impending, mut any_unleash) = (false, false, false);
         .                   let (mut any_hexproof_unless, mut any_dynamic_pt) = (false, false);
         .                   let (mut any_level_bands, mut any_station, mut any_living_metal) = (false, false, false);
   127,878 ( 0.00%)          for card in self.battlefield.iter() {
         .                       let def = &card.definition;
 5,113,186 ( 0.16%)              if !def.static_abilities.is_empty() {
         .                           sa_cards.push(card);
         .                       }
         .                       any_equipped_bonus |= def.equipped_bonus.is_some();
         .                       any_soulbond_bonus |= def.soulbond_bonus.is_some();
 2,492,666 ( 0.08%)              any_attached |= card.attached_to.is_some();
         .                       any_dynamic_pt |= def.dynamic_pt.is_some();
         .                       any_level_bands |= !def.level_bands.is_empty();
         .                       any_station |= !def.station.is_empty();
         .                       for kw in &def.keywords {
 6,420,524 ( 0.21%)                  match kw {
         .                               Keyword::Impending(_) => any_impending = true,
         .                               Keyword::Reconfigure(_) => any_reconfigure = true,
         .                               Keyword::Unleash => any_unleash = true,
         .                               Keyword::HexproofUnlessAttackingOrBlocking => any_hexproof_unless = true,
         .                               Keyword::LivingMetal => any_living_metal = true,
         .                               _ => {}
         .                           }
         .                       }
-- line 7793 ----------------------------------------
-- line 7845 ----------------------------------------
         .                   }
         .                   // CR 114 — static-ability emblems (Vivien Reid's −8 anthem). Emblems
         .                   // have no battlefield object, so synthesize a CardInstance per emblem
         .                   // (controller = owner) and reuse `static_ability_to_effects`. The
         .                   // source id sits in a high sentinel range so it can't collide with a
         .                   // real card; the duration is remapped to `Indefinite` since emblems
         .                   // never leave the command zone.
         .                   for (seat, player) in self.players.iter().enumerate() {
   255,756 ( 0.01%)              for (ei, emblem) in player.emblems.iter().enumerate() {
         .                           if emblem.statics.is_empty() {
         .                               continue;
         .                           }
         .                           let synth_def = crate::card::CardDefinition {
         .                               name: "Emblem",
         .                               static_abilities: emblem.statics.clone(),
         .                               ..Default::default()
         .                           };
-- line 7861 ----------------------------------------
-- line 7925 ----------------------------------------
         .                               }
         .                           }
         .                       }
         .                   }
         .                   // CR 315.5 / 902.5 — a face-up Conspiracy (or a Vanguard avatar) in
         .                   // the command zone has its static abilities affect the game from
         .                   // there. The card never leaves, so the effects are indefinite.
         .                   for (seat, player) in self.players.iter().enumerate() {
   511,512 ( 0.02%)              for card in player.command.iter().filter(|c| c.command_zone_abilities_active()) {
         .                           for mut e in
         .                               static_ability_to_effects(card, card.object_timestamp(), self.active_player_idx == seat)
         .                           {
         .                               e.duration = EffectDuration::Indefinite;
         .                               // CR 702.106 — a revealed hidden agenda's "with the chosen
         .                               // name" filters concretize against the name it named.
         .                               let named = card.named_card.as_deref();
         .                               match &mut e.affected {
-- line 7941 ----------------------------------------
-- line 7958 ----------------------------------------
         .                               }
         .                               all_effects.push(e);
         .                           }
         .                       }
         .                   }
         .                   // Bludgeon Brawl (CR 613 layers 4/7c) — while it's out, each
         .                   // noncreature, non-Equipment artifact gains the Equipment subtype and,
         .                   // once attached, hands its host +X/+0 for its own mana value.
   255,756 ( 0.01%)          for card in if any_artifacts_are_equipment { &self.battlefield[..] } else { &[] } {
         .                       let Some(x) = self.brawl_equip_mv(card) else { continue };
         .                       all_effects.push(ContinuousEffect {
         .                           timestamp: card.object_timestamp(),
         .                           source: card.id,
         .                           affected: AffectedPermanents::Specific(vec![card.id]),
         .                           layer: Layer::L4Type,
         .                           sublayer: None,
         .                           duration: EffectDuration::Indefinite,
-- line 7974 ----------------------------------------
-- line 7991 ----------------------------------------
         .                       });
         .                   }
         .                   // CR 702.6 — Equipment attachment statics. Each Equipment with a
         .                   // live `attached_to` link and an `equipped_bonus` confers +P/+T
         .                   // (layer 7c) and keyword grants (layer 6) on the creature it's
         .                   // attached to, for as long as the Equipment stays on the battlefield.
         .                   // The stale-link SBA in `stack.rs` clears `attached_to` when the
         .                   // equipped creature leaves, so a dangling link can't leak a bonus.
   420,128 ( 0.01%)          for card in if any_equipped_bonus { &self.battlefield[..] } else { &[] } {
 1,562,004 ( 0.05%)              let Some(bonus) = &card.definition.equipped_bonus else { continue };
   311,050 ( 0.01%)              let Some(target) = card.attached_to else { continue };
         .                       // Only apply while the target is still a creature on the bf.
    62,210 ( 0.00%)              let Some(host) = self.battlefield.iter().find(|c| c.id == target) else { continue };
         .                       // Volrath's Curse — the host's controller bought a pass out of this
         .                       // source's effect for the turn.
   119,700 ( 0.00%)              if self.players[host.controller].statics_ignored_this_turn.contains(&card.id) {
         .                           continue;
         .                       }
         .                       // Flat bonus plus optional board-scaled bonus (Nettlecyst: +1/+1
         .                       // for each artifact/enchantment the Equipment's controller controls).
   179,550 ( 0.01%)              let (mut bp, mut bt) = (bonus.power, bonus.toughness);
   179,550 ( 0.01%)              if let Some(scale) = &bonus.scale {
         .                           // The host creature's controller (Righteous Authority /
         .                           // Death's Approach scale on the *enchanted* creature's
         .                           // controller's zones, which may be an opponent).
         .                           let host_controller = self
         .                               .battlefield
         .                               .iter()
         .                               .find(|c| c.id == target)
         .                               .map(|c| c.controller);
-- line 8020 ----------------------------------------
-- line 8122 ----------------------------------------
         .                                           )
         .                                   })
         .                                   .count() as i32,
         .                               }
         .                           };
         .                           bp += n * scale.per_power;
         .                           bt += n * scale.per_toughness;
         .                       }
   179,550 ( 0.01%)              if bp != 0 || bt != 0 {
   319,124 ( 0.01%)                  all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
    24,548 ( 0.00%)                      source: card.id,
    49,096 ( 0.00%)                      affected: AffectedPermanents::Specific(vec![target]),
         .                               layer: Layer::L7PowerTough,
         .                               sublayer: Some(PtSublayer::Modify),
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::ModifyPowerToughness(bp, bt),
         .                           });
         .                       }
         .                       // "Loses all abilities AND has [keywords]" auras (Heliod's
         .                       // Punishment): the removal must precede the aura's own keyword
         .                       // grants so they survive (same timestamp → stable insertion
         .                       // order; CR 613.7 grant-after-removal).
   119,700 ( 0.00%)              if bonus.remove_abilities {
         .                           debug_assert!(card_can_strip_abilities(card), "strip gate: equipped_bonus");
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
         .                               source: card.id,
         .                               affected: AffectedPermanents::Specific(vec![target]),
         .                               layer: Layer::L6Ability,
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::RemoveAllAbilities,
         .                           });
         .                       }
   239,400 ( 0.01%)              for kw in &bonus.keywords {
 1,903,040 ( 0.06%)                  all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
    95,152 ( 0.00%)                      source: card.id,
   761,216 ( 0.02%)                      affected: AffectedPermanents::Specific(vec![target]),
         .                               layer: Layer::L6Ability,
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
   951,520 ( 0.03%)                      modification: Modification::AddKeyword(kw.clone()),
 1,236,976 ( 0.04%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::Keyword as core::clone::Clone>::clone (95,152x)
         .                           });
         .                       }
         .                       for kw in &bonus.remove_keywords {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
         .                               source: card.id,
         .                               affected: AffectedPermanents::Specific(vec![target]),
         .                               layer: Layer::L6Ability,
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::RemoveKeyword(kw.clone()),
         .                           });
         .                       }
         .                       // "During your turn, equipped creature has [keyword]" (Dragoon's
         .                       // Lance) — layer-6 grant gated on the source controller's turn.
   239,400 ( 0.01%)              if self.active_player_idx == card.controller {
         .                           for kw in &bonus.during_your_turn_keywords {
         .                               all_effects.push(ContinuousEffect {
         .                                   timestamp: card.object_timestamp(),
         .                                   source: card.id,
         .                                   affected: AffectedPermanents::Specific(vec![target]),
         .                                   layer: Layer::L6Ability,
         .                                   sublayer: None,
         .                                   duration: EffectDuration::WhileSourceOnBattlefield,
         .                                   modification: Modification::AddKeyword(kw.clone()),
         .                               });
         .                           }
    53,668 ( 0.00%)                  let (dp, dt) = bonus.during_your_turn_pt;
    80,502 ( 0.00%)                  if dp != 0 || dt != 0 {
         .                               all_effects.push(ContinuousEffect {
         .                                   timestamp: card.object_timestamp(),
         .                                   source: card.id,
         .                                   affected: AffectedPermanents::Specific(vec![target]),
         .                                   layer: Layer::L7PowerTough,
         .                                   sublayer: Some(PtSublayer::Modify),
         .                                   duration: EffectDuration::WhileSourceOnBattlefield,
         .                                   modification: Modification::ModifyPowerToughness(dp, dt),
         .                               });
         .                           }
         .                       }
         .                       // "Enchanted creature gets +P/+T while `condition`" (Taste for
         .                       // Mayhem's Hellbent rider) — layer-7c, gated on a predicate against
         .                       // the source's controller so it tracks the live game state.
   239,400 ( 0.01%)              if let Some((cp, ct, condition)) = &bonus.conditional_pt
         .                           && (*cp != 0 || *ct != 0)
         .                       {
         .                           let ctx = crate::game::effects::EffectContext::for_ability(
         .                               card.id,
         .                               card.controller,
         .                               None,
         .                           );
         .                           if self.evaluate_predicate(condition, &ctx) {
-- line 8217 ----------------------------------------
-- line 8235 ----------------------------------------
         .                               source: card.id,
         .                               affected: AffectedPermanents::Specific(vec![target]),
         .                               layer,
         .                               sublayer,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: m,
         .                           });
         .                       };
   119,700 ( 0.00%)              if let Some((p, t)) = bonus.set_base_pt {
         .                           push_mod(&mut all_effects, Layer::L7PowerTough, Some(PtSublayer::SetValue),
         .                               Modification::SetPowerToughness(p, t));
         .                       }
         .                       // Aettir and Priwen — base P/T X/X where X is the Equipment
         .                       // controller's life total (layer 7b). Reads life directly (no
         .                       // layer dependency), so it's reentrancy-safe.
   119,700 ( 0.00%)              if bonus.set_base_pt_controller_life {
         .                           let x = self.players[card.controller].life.max(0);
         .                           push_mod(&mut all_effects, Layer::L7PowerTough, Some(PtSublayer::SetValue),
         .                               Modification::SetPowerToughness(x, x));
         .                       }
   119,700 ( 0.00%)              if let Some(types) = &bonus.set_card_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::SetCardTypes(types.clone()));
         .                       }
   179,550 ( 0.01%)              if let Some(types) = &bonus.set_creature_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::SetCreatureTypes(types.clone()));
         .                       }
         .                       for ct in &bonus.add_creature_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::AddCreatureType(*ct));
         .                       }
         .                       for ct in &bonus.add_card_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::AddCardType(ct.clone()));
         .                       }
   119,700 ( 0.00%)              if let Some(types) = &bonus.set_land_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::SetLandTypes(types.clone()));
         .                       }
   119,700 ( 0.00%)              if let Some(types) = &bonus.set_artifact_types {
         .                           push_mod(&mut all_effects, Layer::L4Type, None,
         .                               Modification::SetArtifactSubtypes(types.clone()));
         .                       }
   119,700 ( 0.00%)              if let Some(colors) = &bonus.set_colors {
         .                           push_mod(&mut all_effects, Layer::L5Color, None,
         .                               Modification::SetColors(colors.clone()));
         .                       }
         .                       // Host-conditional riders ("as long as enchanted creature is
         .                       // green, …" — Shield of the Oversoul). Evaluated against the
         .                       // host's pre-layer state, like `EquipScale` above.
         .                       for cond in &bonus.conditional {
         .                           let host_matches = self
-- line 8287 ----------------------------------------
-- line 8337 ----------------------------------------
         .                                   modification: Modification::SetPowerToughness(p, t),
         .                               });
         .                           }
         .                       }
         .                   }
         .                   // CR 702.95 — Soulbond. A creature carrying a `soulbond_bonus` that's
         .                   // paired confers the bonus on BOTH itself and its partner (P/T layer
         .                   // 7c, keywords layer 6), for as long as both stay on the battlefield.
   383,634 ( 0.01%)          for card in if any_soulbond_bonus { &self.battlefield[..] } else { &[] } {
         .                       let Some(bonus) = &card.definition.soulbond_bonus else { continue };
         .                       let Some(partner) = card.soulbond_partner else { continue };
         .                       if !self.battlefield.iter().any(|c| c.id == partner) {
         .                           continue;
         .                       }
         .                       for &id in &[card.id, partner] {
         .                           if bonus.power != 0 || bonus.toughness != 0 {
         .                               all_effects.push(ContinuousEffect {
-- line 8353 ----------------------------------------
-- line 8375 ----------------------------------------
         .                               });
         .                           }
         .                       }
         .                   }
         .                   // CR 702.151c — a Reconfigure Equipment isn't a creature while it's
         .                   // attached to a creature. Strip the Creature card type at layer 4
         .                   // (the +1/+1 it confers still scales off its own counters; its equip
         .                   // bonus and exile ability are unaffected). Lion Sash.
   383,610 ( 0.01%)          for card in if any_reconfigure && any_attached { &self.battlefield[..] } else { &[] } {
         .                       if card.attached_to.is_some() && card.definition.has_reconfigure().is_some() {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
         .                               source: card.id,
         .                               affected: AffectedPermanents::Specific(vec![card.id]),
         .                               layer: Layer::L4Type,
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
-- line 8391 ----------------------------------------
-- line 8392 ----------------------------------------
         .                               modification: Modification::RemoveCardType(crate::card::CardType::Creature),
         .                           });
         .                       }
         .                   }
         .                   // "Attacking creatures you control have <keyword>" (Blade Historian).
         .                   // Resolved here because `affects()` can't see combat state — we read
         .                   // the live `attacking` list and scope the grant to the source's own
         .                   // attackers. Layer-6 keyword addition, like the equipment grants.
   255,756 ( 0.01%)          if !self.attacking.is_empty() {
         .                       for &card in &sa_cards {
         .                           for sa in &card.definition.static_abilities {
         .                               let crate::effect::StaticEffect::GrantKeywordToAttackers { keyword } =
         .                                   &sa.effect
         .                               else {
         .                                   continue;
         .                               };
         .                               let ids: Vec<CardId> = self
-- line 8408 ----------------------------------------
-- line 8733 ----------------------------------------
         .                               });
         .                           }
         .                       }
         .                   }
         .                   // Coat of Arms — each creature gets +P/+T for each OTHER creature that
         .                   // shares ≥1 creature type with it (Changeling shares all types). The
         .                   // bonus is per-creature (shared-type count differs per subject), so it's
         .                   // gathered state-aware like Sliver Legion above.
   383,634 ( 0.01%)          if any_pump_per_shared_type {
         .                       use crate::card::Keyword;
         .                       let creatures: Vec<(CardId, &Vec<crate::card::CreatureType>, bool)> = self
         .                           .battlefield
         .                           .iter()
         .                           .filter(|c| c.definition.is_creature())
         .                           .map(|c| {
         .                               (c.id, &c.definition.subtypes.creature_types,
         .                                c.definition.keywords.contains(&Keyword::Changeling))
-- line 8749 ----------------------------------------
-- line 8828 ----------------------------------------
         .                                   modification: Modification::AddKeyword(keyword.clone()),
         .                               });
         .                           }
         .                       }
         .                   }
         .                   // CR 702.183 — Impending: a permanent with the Impending keyword isn't
         .                   // a creature while it has a time counter. Emit a layer-4
         .                   // RemoveCardType(Creature) self-effect while counters remain.
   255,756 ( 0.01%)          for card in if any_impending { &self.battlefield[..] } else { &[] } {
         .                       let is_impending = card
         .                           .definition
         .                           .keywords
         .                           .iter()
         .                           .any(|k| matches!(k, crate::card::Keyword::Impending(_)));
         .                       if is_impending && card.counter_count(crate::card::CounterType::Time) > 0 {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
-- line 8844 ----------------------------------------
-- line 9970 ----------------------------------------
         .                   // formula lives on `CardDefinition.dynamic_pt`; we resolve it here
         .                   // on every layer recompute and emit a layer-7 SetPT effect.
         .                   //
         .                   // The three graveyard tallies feed Tarmogoyf-shaped formulas only, so
         .                   // they are gated on a `dynamic_pt` being present at all: computing
         .                   // them unconditionally cost three full graveyard walks (one of them
         .                   // building a `HashSet`) on every gather, and the gather runs about a
         .                   // million times per six bot games.
   255,756 ( 0.01%)          let (goyf_n, lands_in_gys, creatures_in_gys) = if any_dynamic_pt {
         .                       (
         .                           self.distinct_card_types_in_all_graveyards() as i32,
         .                           self.players
         .                               .iter()
         .                               .map(|p| p.graveyard.iter().filter(|c| c.definition.is_land()).count() as i32)
         .                               .sum(),
         .                           self.players
         .                               .iter()
-- line 9986 ----------------------------------------
-- line 10591 ----------------------------------------
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::AddKeyword(crate::card::Keyword::Annihilator(n)),
         .                           });
         .                       }
         .                   }
         .                   // "Has hexproof unless it's attacking or blocking" (Tromokratis) —
         .                   // injected as a computed layer-6 Hexproof so every existing
         .                   // targeting gate honors it.
   255,756 ( 0.01%)          for card in if any_hexproof_unless { &self.battlefield[..] } else { &[] } {
         .                       if !card
         .                           .definition
         .                           .keywords
         .                           .contains(&crate::card::Keyword::HexproofUnlessAttackingOrBlocking)
         .                       {
         .                           continue;
         .                       }
         .                       let in_combat = self.attacking.iter().any(|a| a.attacker == card.id)
-- line 10607 ----------------------------------------
-- line 10677 ----------------------------------------
         .                               affected,
         .                               layer: Layer::L7PowerTough,
         .                               sublayer: Some(PtSublayer::SetValue),
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::SetPowerToughness(p, t),
         .                           });
         .                       }
         .                   }
   255,756 ( 0.01%)          for card in if any_level_bands { &self.battlefield[..] } else { &[] } {
         .                       if card.definition.level_bands.is_empty() {
         .                           continue;
         .                       }
         .                       let lvl = card.counter_count(crate::card::CounterType::Level);
         .                       let Some(band) = card
         .                           .definition
         .                           .level_bands
         .                           .iter()
-- line 10693 ----------------------------------------
-- line 10715 ----------------------------------------
         .                               modification: Modification::AddKeyword(kw.clone()),
         .                           });
         .                       }
         .                   }
         .                   // CR 721.2 — Station symbols. Every band whose `{N+}` threshold is met
         .                   // by the permanent's charge-counter count grants its abilities (layer
         .                   // 6); a band with a `[P/T]` box also makes it a creature with that base
         .                   // P/T (CR 721.2b — layers 4 + 7a).
   383,634 ( 0.01%)          for card in if any_station { &self.battlefield[..] } else { &[] } {
         .                       if card.definition.station.is_empty() {
         .                           continue;
         .                       }
         .                       let charges = card.counter_count(crate::card::CounterType::Charge);
         .                       for band in card.definition.station.iter().filter(|b| charges >= b.min) {
         .                           for kw in &band.keywords {
         .                               all_effects.push(ContinuousEffect {
         .                                   timestamp: card.object_timestamp(),
-- line 10731 ----------------------------------------
-- line 10772 ----------------------------------------
         .                   // (the CR 611.2 predicate gate, its sibling, and the suspect/
         .                   // living-metal statics) live for cards this walk is the only reach
         .                   // to. The two keyword scans inside it are gated instead.
         .                   for card in &self.battlefield {
         .                       // CR 702.98 — Unleash's second static: a creature with the
         .                       // Unleash keyword can't block while it has a +1/+1 counter.
         .                       // Injected as a computed `CantBlock` so the existing block-
         .                       // legality enforcement (`declare_blockers`) honors it.
 4,985,332 ( 0.16%)              if any_unleash
         .                           && card.definition.keywords.contains(&Keyword::Unleash)
         .                           && card.counters.get(&crate::card::CounterType::PlusOnePlusOne).copied().unwrap_or(0) > 0
         .                       {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
         .                               source: card.id,
         .                               affected: AffectedPermanents::Source,
         .                               layer: Layer::L6Ability,
-- line 10788 ----------------------------------------
-- line 10849 ----------------------------------------
         .                                   sublayer: None,
         .                                   duration: EffectDuration::WhileSourceOnBattlefield,
         .                                   modification: Modification::AddKeyword(keyword.clone()),
         .                               });
         .                           }
         .                       }
         .                       // CR 702.161 — Living metal: a Vehicle with this keyword is an
         .                       // artifact creature during its controller's turn, no crew needed.
 7,477,998 ( 0.24%)              if any_living_metal
         .                           && card.definition.keywords.contains(&Keyword::LivingMetal)
         .                           && card.controller == self.active_player_idx
         .                       {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: card.object_timestamp(),
         .                               source: card.id,
         .                               affected: AffectedPermanents::Source,
         .                               layer: Layer::L4Type,
-- line 10865 ----------------------------------------
-- line 10866 ----------------------------------------
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::AddCardType(crate::card::CardType::Creature),
         .                           });
         .                       }
         .                       // CR 701.60 — a suspected creature has menace and can't block.
         .                       // Injected as computed keywords so combat-legality enforcement
         .                       // honors them.
 7,477,998 ( 0.24%)              if card.suspected && card.definition.is_creature() {
         .                           for kw in [Keyword::Menace, Keyword::CantBlock] {
         .                               all_effects.push(ContinuousEffect {
         .                                   timestamp: card.object_timestamp(),
         .                                   source: card.id,
         .                                   affected: AffectedPermanents::Source,
         .                                   layer: Layer::L6Ability,
         .                                   sublayer: None,
         .                                   duration: EffectDuration::WhileSourceOnBattlefield,
-- line 10882 ----------------------------------------
-- line 10888 ----------------------------------------
         .                   // Graveyard-resident static-ability injection — the Incarnation
         .                   // cycle's `StaticEffect::GraveyardAnthem` ("As long as this card is
         .                   // in your graveyard and you control a [Land subtype], creatures you
         .                   // control have [keyword]"). Zone-special: read off graveyard cards'
         .                   // printed statics, gated on the owner controlling a land of the
         .                   // required subtype. The effect's `source` is the gy card's id, so
         .                   // removing the gy card causes the effect to fall out.
         .                   for player in &self.players {
   723,552 ( 0.02%)              for card in &player.graveyard {
         .                           for sa in &card.definition.static_abilities {
         .                               let crate::effect::StaticEffect::GraveyardAnthem { land_type, keyword } =
         .                                   &sa.effect
         .                               else {
         .                                   continue;
         .                               };
         .                               let (land_subtype, kw) = (*land_type, keyword.clone());
         .                               let controller_has_land = self.battlefield.iter().any(|c| {
-- line 10904 ----------------------------------------
-- line 10954 ----------------------------------------
         .                               modification: Modification::AddSupertype(crate::card::Supertype::Legendary),
         .                           });
         .                       }
         .                   }
         .                   // CR 701.54c — the Ring's level-1 emblem makes its controller's
         .                   // Ring-bearer legendary (in addition to the can't-be-blocked rider,
         .                   // which is enforced directly in `blocker_can_block_attacker`).
         .                   for seat in 0..self.players.len() {
   767,268 ( 0.02%)              if self.players[seat].ring_temptations >= 1
         .                           && let Some(bearer) = self.effective_ring_bearer(seat)
         .                       {
         .                           all_effects.push(ContinuousEffect {
         .                               timestamp: 0,
         .                               source: bearer,
         .                               affected: AffectedPermanents::Specific(vec![bearer]),
         .                               layer: Layer::L4Type,
         .                               sublayer: None,
         .                               duration: EffectDuration::WhileSourceOnBattlefield,
         .                               modification: Modification::AddSupertype(crate::card::Supertype::Legendary),
         .                           });
         .                       }
         .                   }
   639,390 ( 0.02%)          all_effects
 1,023,024 ( 0.03%)      }
         .           
         .               /// Count of distinct card types (Artifact, Creature, Enchantment,
         .               /// Instant, Land, Planeswalker, Sorcery, Battle, Tribal) across every
         .               /// player's graveyard. Used by Tarmogoyf-style dynamic P/T.
         .               pub fn distinct_card_types_in_all_graveyards(&self) -> usize {
         .                   let mut seen: crate::fxhash::HashSet<CardType> = crate::fxhash::HashSet::default();
         .                   for player in &self.players {
         .                       for card in &player.graveyard {
-- line 10985 ----------------------------------------
-- line 11026 ----------------------------------------
         .               /// applies the layer pass to only the one target card, instead of
         .               /// building a `ComputedPermanent` for every permanent and discarding
         .               /// all but one.
         .               /// Inside a freeze scope the result is memoized per card and handed back
         .               /// as a shared `Arc`, so the combat helpers that ask about the same two
         .               /// or three permanents dozens of times per simulated attack pay one
         .               /// layer pass between them. `Arc` derefs, so callers read fields as
         .               /// before.
 2,894,472 ( 0.09%)      pub fn computed_permanent(&self, id: CardId) -> Option<std::sync::Arc<ComputedPermanent>> {
         .                   // Mid-gather reads take the printed-types fallback (see
         .                   // `frozen_effects`), so they must neither read nor write the memo.
         .                   // One lock covers both memos: a hit costs exactly one acquisition,
         .                   // a miss two, and only the scope's first computed read pays three.
   321,608 ( 0.01%)          let mut frozen_fx = None;
   643,216 ( 0.02%)          if !self.in_layer_gather.load(std::sync::atomic::Ordering::Relaxed) {
   321,608 ( 0.01%)              let st = self.layer_freeze.lock();
   643,216 ( 0.02%)              if st.depth > 0 {
         .                           if let Some((_, cp)) = st.perms.iter().find(|(k, _)| *k == id) {
         .                               return Some(cp.clone());
         .                           }
   249,000 ( 0.01%)                  frozen_fx = Some(st.memo.clone());
         .                       }
         .                   }
   170,058 ( 0.01%)          let card = self.battlefield.iter().find(|c| c.id == id)?;
   461,402 ( 0.01%)          if let Some(memo) = frozen_fx {
         .                       // Inside a scope; `memo` is `None` only on its first computed
         .                       // read, which fills it exactly as `frozen_effects` would.
   248,996 ( 0.01%)              let fx = match memo {
   147,488 ( 0.00%)                  Some(fx) => fx,
         .                           None => {
         .                               let fx = std::sync::Arc::new(self.gather_continuous_effects());
    50,754 ( 0.00%)                      let mut st = self.layer_freeze.lock();
   101,508 ( 0.00%)                      if st.depth > 0 {
   101,508 ( 0.00%)                          st.memo = Some(fx.clone());
         .                               }
   152,262 ( 0.00%)                      fx
         .                           }
         .                       };
         .                       let cp = std::sync::Arc::new(crate::game::layers::apply_layers_one(card, &fx));
         .                       // Re-check: an entry stored after the scope closed would outlive
         .                       // the state it describes, and nothing would clear it.
   124,498 ( 0.00%)              let mut st = self.layer_freeze.lock();
   248,996 ( 0.01%)              if st.depth > 0 {
         .                           st.perms.push((id, cp.clone()));
         .                       }
         .                       return Some(cp);
         .                   }
         .                   Some(std::sync::Arc::new(crate::game::layers::apply_layers_one(
         .                       card,
         .                       &self.gather_continuous_effects(),
         .                   )))
 3,200,784 ( 0.10%)      }
         .           
         .               /// CR 603.10 — a last-known-information snapshot of a battlefield permanent
         .               /// that is about to leave (die/sacrifice). Clones the live `CardInstance`
         .               /// but stamps its *computed* creature types (layer 4) onto the definition
         .               /// so a "whenever a [type] you control dies" trigger reads types the
         .               /// creature gained from a continuous effect (Jenova's granted Mutant), not
         .               /// just its printed subtypes. Only pays the layer cost when a grant is
         .               /// actually present.
-- line 11085 ----------------------------------------
-- line 11130 ----------------------------------------
         .                   }
         .                   if self.resolving_lki_source == Some(cid) || self.resolving_lki_subject == Some(cid) {
         .                       self.leaves_bf_lki.get(&cid)
         .                   } else {
         .                       None
         .                   }
         .               }
         .           
    20,520 ( 0.00%)      pub fn dying_snapshot(&self, id: CardId) -> Option<CardInstance> {
     3,420 ( 0.00%)          let mut snap = self.battlefield.iter().find(|c| c.id == id)?.clone();
    13,680 ( 0.00%)          if let Some(cp) = self.computed_permanent(id) {
11,816,512 ( 0.38%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (3,420x)
         .                       let printed = &snap.definition.subtypes.creature_types;
         .                       if cp.subtypes.creature_types.iter().any(|t| !printed.contains(t)) {
         .                           std::sync::Arc::make_mut(&mut snap.definition).subtypes.creature_types =
         .                               cp.subtypes.creature_types.clone();
         .                       }
         .                   }
     3,420 ( 0.00%)          Some(snap)
    23,940 ( 0.00%)      }
         .           
         .               /// CR 702.16 — true if `target` has protection from any of `source`'s
         .               /// (computed) colors. Reads both sides through the layer system so granted
         .               /// protection / color-setting effects count. Backs damage prevention
         .               /// (702.16e) and equip/attach legality (702.16f).
         .               pub(crate) fn is_protected_from(&self, source: CardId, target: CardId) -> bool {
    10,794 ( 0.00%)          self.damage_prevented_by_protection(source, target)
28,499,507 ( 0.91%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::damage_prevented_by_protection (3,598x)
         .               }
         .           
         .               /// CR 702.16e — damage from a source is prevented if the target permanent
         .               /// has protection from any of that source's (computed) colors. Reads both
         .               /// sides through the layer system so granted protection / color-setting
         .               /// effects count.
         .               /// CR 106.4 override — empty every player's mana pool, except that a
         .               /// player with an `UnspentManaBecomesColorless` static (Kruphix) keeps
         .               /// the total as colorless mana.
   204,352 ( 0.01%)      pub fn empty_mana_pools(&mut self) {
         .                   use crate::effect::StaticEffect;
         .                   // Cheap global gate before the three battlefield scans below (two of
         .                   // which allocate a `Vec`). The per-seat fast path in the loop skips a
         .                   // seat with no floating mana, no kept mana and no firebending mana
         .                   // *whatever* the board says, so when every seat is in that state the
         .                   // scans answer a question nobody goes on to ask. This runs at every
         .                   // step change and every resolution — 51 k times per six bot games —
         .                   // and the overwhelming majority have nothing to empty.
         .                   if self.players.iter().all(|p| {
   102,176 ( 0.00%)              p.firebending_kept_red == 0
   204,352 ( 0.01%)                  && p.mana_pool.is_empty()
 1,532,640 ( 0.05%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::is_empty (51,088x)
    51,088 ( 0.00%)                  && p.kept_mana_this_turn.total() == 0
         .                   }) {
         .                       return;
         .                   }
         .                   let keepers: Vec<usize> = self
         .                       .battlefield
         .                       .iter()
         .                       .filter(|c| {
         .                           c.definition.static_abilities.iter().any(|sa| {
-- line 11185 ----------------------------------------
-- line 11257 ----------------------------------------
         .                       if !all_persist && player.kept_mana_this_turn.total() > 0 {
         .                           // Take-and-restore rather than a clone: `Player` is a CoW
         .                           // handle, so `&mut` one field borrows the whole seat.
         .                           let kept = std::mem::take(&mut player.kept_mana_this_turn);
         .                           player.mana_pool.absorb(&kept);
         .                           player.kept_mana_this_turn = kept;
         .                       }
         .                   }
   204,352 ( 0.01%)      }
         .           
         .               /// CR 615 — true if `target` is an attacking creature whose controller
         .               /// has a `PreventDamageToYourAttackers` static in play (Iroas, God of
         .               /// Victory). Overridden by "damage can't be prevented" (CR 615.12).
         .               pub(crate) fn damage_to_attacker_prevented(&self, target: CardId) -> bool {
     3,876 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   if !self.attacking.iter().any(|a| a.attacker == target) {
         .                       return false;
         .                   }
         .                   let Some(controller) = self.battlefield_find(target).map(|c| c.controller) else {
         .                       return false;
         .                   };
         .                   self.battlefield.iter().any(|c| {
    86,832 ( 0.00%)              c.controller == controller
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, crate::effect::StaticEffect::PreventDamageToYourAttackers)
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — true if `player` controls a permanent with a blanket
         .               /// "prevent all damage that would be dealt to you" static (Glacial Chasm),
         .               /// unless prevention is shut off this turn (615.12).
    22,848 ( 0.00%)      pub(crate) fn all_damage_to_player_prevented(&self, player: usize) -> bool {
     5,712 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
     2,856 ( 0.00%)          let ids: Vec<CardId> = self
         .                       .battlefield
         .                       .iter()
   118,294 ( 0.00%)              .filter(|c| c.controller == player)
    27,180 ( 0.00%)              .map(|c| c.id)
         .                       .collect();
         .                   ids.into_iter().any(|id| {
         .                       let Some(c) = self.battlefield_find(id) else { return false };
         .                       c.definition
         .                           .static_abilities
         .                           .iter()
         .                           .any(|sa| self.blanket_player_shield_active(&sa.effect, id, player))
         .                   })
    22,848 ( 0.00%)      }
         .           
         .               /// True when `e` is a blanket "prevent all damage that would be dealt to
         .               /// you" static that is currently ON — unwrapping the CR 611.2 conditional
         .               /// wrappers (`WhileCondition` — Spirit of Resistance's five-colour gate,
         .               /// `WhileYourTurn`) rather than only matching the bare variants.
         .               fn blanket_player_shield_active(
         .                   &self,
         .                   e: &crate::effect::StaticEffect,
-- line 11316 ----------------------------------------
-- line 11341 ----------------------------------------
         .               /// CR 615 — true if `target` is a creature whose controller has a
         .               /// "prevent all noncombat damage to creatures you control" static (Mark of
         .               /// Asylum). Consulted only at the noncombat damage funnel.
         .               pub(crate) fn noncombat_damage_to_creature_prevented(&self, target: CardId) -> bool {
         .                   if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   let Some(tgt) = self.battlefield_find(target) else { return false };
        12 ( 0.00%)          if !tgt.definition.is_creature() {
        52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
         .                       return false;
         .                   }
         .                   let controller = tgt.controller;
         .                   self.battlefield.iter().any(|c| {
       168 ( 0.00%)              c.controller == controller
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, crate::effect::StaticEffect::PreventNoncombatDamageToYourCreatures)
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — The Wanderer: "prevent all noncombat damage to you and other
         .               /// permanents you control." True when `victim` (a player, or a permanent)
         .               /// is shielded by a `PreventNoncombatDamageToYouAndYourPermanents` static
         .               /// its controller/owner controls. Consulted only at the noncombat funnel.
         .               pub(crate) fn noncombat_damage_to_you_and_permanents_prevented(&self, victim: crate::game::effects::EntityRef) -> bool {
         .                   use crate::game::effects::EntityRef;
       348 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
       348 ( 0.00%)          let seat = match victim {
       168 ( 0.00%)              EntityRef::Player(p) => p,
         6 ( 0.00%)              EntityRef::Permanent(id) | EntityRef::Card(id) => {
         .                           let Some(c) = self.battlefield_find(id) else { return false };
         4 ( 0.00%)                  c.controller
         .                       }
         .                   };
         .                   self.battlefield.iter().any(|c| {
     2,920 ( 0.00%)              c.controller == seat
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, crate::effect::StaticEffect::PreventNoncombatDamageToYouAndYourPermanents)
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — Emmara Tandris: "prevent all damage that would be dealt to
         .               /// creature tokens you control." True when `target` is a token creature
         .               /// whose controller has the static. Consulted on both damage paths.
         .               pub(crate) fn all_damage_to_creature_token_prevented(&self, target: CardId) -> bool {
     4,456 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   let Some(tgt) = self.battlefield_find(target) else { return false };
    23,028 ( 0.00%)          if !tgt.definition.is_creature() || !tgt.is_token {
        52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
         .                       return false;
         .                   }
         .                   let controller = tgt.controller;
         .                   self.battlefield.iter().any(|c| {
         .                       c.controller == controller
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(
         .                                   sa.effect,
-- line 11401 ----------------------------------------
-- line 11403 ----------------------------------------
         .                               )
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — Rune-Tail's Essence: "prevent all damage that would be dealt
         .               /// to creatures you control." Consulted on both damage paths.
         .               pub(crate) fn all_damage_to_your_creature_prevented(&self, target: CardId) -> bool {
     8,332 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   let Some(tgt) = self.battlefield_find(target) else { return false };
    14,700 ( 0.00%)          if !tgt.definition.is_creature() {
        52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
         .                       return false;
         .                   }
         .                   let controller = tgt.controller;
         .                   self.battlefield.iter().any(|c| {
   190,152 ( 0.01%)              c.controller == controller
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(
         .                                   sa.effect,
         .                                   crate::effect::StaticEffect::PreventAllDamageToYourCreatures
         .                               )
         .                           })
         .                   }) || self.battlefield.iter().any(|c| {
         .                       c.definition
-- line 11428 ----------------------------------------
-- line 11435 ----------------------------------------
         .               /// Mana Maze — the colours of the turn's most recently cast spell, which
         .               /// no player may share while such a lock is on the battlefield.
         .               pub fn locked_cast_colors(&self) -> Vec<crate::mana::Color> {
         .                   self.last_cast_spell_colors.clone()
         .               }
         .           
         .               /// CR 615 — Well-Laid Plans: true when `src` and `tgt` are both creatures
         .               /// sharing a colour while any such static is on the battlefield.
        12 ( 0.00%)      pub(crate) fn shared_color_creature_damage_prevented(
         .                   &self,
         .                   src: CardId,
         .                   tgt: CardId,
         .               ) -> bool {
        18 ( 0.00%)          if self.damage_cant_be_prevented_this_turn || src == tgt {
         .                       return false;
         .                   }
         6 ( 0.00%)          let live = self.battlefield.iter().any(|c| {
         .                       c.definition.static_abilities.iter().any(|sa| {
         .                           matches!(
         .                               sa.effect,
         .                               crate::effect::StaticEffect::PreventDamageBetweenSharedColorCreatures
         .                           )
         .                       })
         .                   });
         .                   if !live {
-- line 11459 ----------------------------------------
-- line 11464 ----------------------------------------
         .                           c.card_types.contains(&crate::card::CardType::Creature)
         .                       })
         .                       .map(|c| c.colors)
         .                   };
         .                   match (creature_colors(src), creature_colors(tgt)) {
         .                       (Some(a), Some(b)) => a.intersects(b),
         .                       _ => false,
         .                   }
         6 ( 0.00%)      }
         .           
         .               /// CR 614.9 — Harsh Judgment: the seat an instant/sorcery spell's damage
         .               /// to `p` is redirected to (its own controller), or `None`.
         .               pub(crate) fn chosen_color_spell_damage_redirect(
         .                   &self,
         .                   p: usize,
         .                   source: Option<CardId>,
         .               ) -> Option<usize> {
         .                   let src = source?;
         .                   // The spell is in no visible zone mid-resolution, so read its caster,
         .                   // colours and types off `resolving_source` when it's the one running.
     1,668 ( 0.00%)          let (caster, colors, types) = match &self.resolving_source {
       166 ( 0.00%)              Some((id, caster, colors, types)) if *id == src => {
       996 ( 0.00%)                  (*caster, colors.clone(), types.clone())
    53,963 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (332x)
         .                       }
         .                       _ => {
         2 ( 0.00%)                  let caster = self.stack_spell_caster(src)?;
         .                           let card = self.find_card_anywhere(src)?;
         .                           (caster, card.definition.printed_colors(), card.definition.card_types.clone())
         .                       }
         .                   };
       332 ( 0.00%)          if caster == p
         .                       || !(types.contains(&CardType::Instant) || types.contains(&CardType::Sorcery))
         .                   {
         .                       return None;
         .                   }
         .                   self.battlefield
         .                       .iter()
         .                       .find(|c| {
     2,632 ( 0.00%)                  c.controller == p
         .                               && c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(
         .                                       sa.effect,
         .                                       crate::effect::StaticEffect::RedirectChosenColorSpellDamageToController
         .                                   )
         .                               })
         .                               && c.chosen_color.is_some_and(|k| colors.contains(&k))
         .                       })
         .                       .map(|_| caster)
         .               }
         .           
         .               /// CR 601.3 — Cornered Market: true when `card` shares its name with a
         .               /// nontoken permanent on the battlefield while the lock is active.
         .               pub(crate) fn name_locked_by_a_permanent(&self, card: Option<CardId>) -> bool {
    14,912 ( 0.00%)          let Some(name) = card.and_then(|id| self.find_card_anywhere(id)).map(|c| c.definition.name)
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
         .                   else {
         .                       return false;
         .                   };
         .                   self.battlefield.iter().any(|c| {
         .                       c.definition
         .                           .static_abilities
         .                           .iter()
         .                           .any(|sa| {
         .                               matches!(
         .                                   sa.effect,
         .                                   crate::effect::StaticEffect::NoSpellOrNonbasicLandSharingAPermanentName
         .                               )
         .                           })
         .                   }) && self.battlefield.iter().any(|c| !c.is_token && c.definition.name == name)
     7,456 ( 0.00%)      }
         .           
         .               /// CR 615 — Statecraft: "prevent all combat damage that would be dealt to
         .               /// and dealt by creatures you control." True when `who` is a creature whose
         .               /// controller has the static; both combat funnels consult it.
         .               pub(crate) fn combat_damage_sealed_for_your_creatures(&self, who: CardId) -> bool {
         .                   let Some(c) = self.battlefield_find(who) else { return false };
    33,666 ( 0.00%)          c.definition.is_creature()
    57,850 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4,450x)
         .                       && self.battlefield.iter().any(|s| {
   503,344 ( 0.02%)                  s.controller == c.controller
         .                               && s.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(
         .                                       sa.effect,
         .                                       crate::effect::StaticEffect::PreventAllCombatDamageToAndFromYourCreatures
         .                                   )
         .                               })
         .                       })
         .               }
-- line 11549 ----------------------------------------
-- line 11552 ----------------------------------------
         .               /// control by sources you control." True when `source` and `target` share a
         .               /// controller who has the static and `target` is a creature. Consulted on
         .               /// both the combat and noncombat damage paths.
         .               pub(crate) fn damage_from_your_source_to_your_creature_prevented(
         .                   &self,
         .                   source: CardId,
         .                   target: CardId,
         .               ) -> bool {
     8,900 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
     4,456 ( 0.00%)          let (Some(tgt), Some(src)) =
     4,456 ( 0.00%)              (self.battlefield_find(target), self.battlefield_find(source))
         .                   else {
         .                       return false;
         .                   };
    28,956 ( 0.00%)          if !tgt.definition.is_creature() || src.controller != tgt.controller {
        52 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (4x)
         .                       return false;
         .                   }
         .                   let controller = tgt.controller;
         .                   self.battlefield.iter().any(|c| {
         .                       c.controller == controller
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(
         .                                   sa.effect,
-- line 11576 ----------------------------------------
-- line 11578 ----------------------------------------
         .                               )
         .                           })
         .                   })
         .               }
         .           
         .               /// CR 615 — Indentured Oaf: "prevent all damage this creature would deal to
         .               /// [color] creatures." True when `source` has the static and `target` is a
         .               /// creature of the named color.
    17,824 ( 0.00%)      pub(crate) fn source_damage_to_color_prevented(&self, source: CardId, target: CardId) -> bool {
     8,912 ( 0.00%)          if self.damage_cant_be_prevented_this_turn {
         .                       return false;
         .                   }
         .                   let Some(colors) = self.battlefield_find(source).map(|src| {
         .                       src.definition
         .                           .static_abilities
         .                           .iter()
         .                           .filter_map(|sa| match sa.effect {
         .                               crate::effect::StaticEffect::PreventThisDamageToColor(c) => Some(c),
         .                               _ => None,
         .                           })
         .                           .collect::<Vec<_>>()
         .                   }) else {
         .                       return false;
         .                   };
     8,912 ( 0.00%)          if colors.is_empty() {
         .                       return false;
         .                   }
         .                   let Some(tgt) = self.computed_permanent(target) else { return false };
         .                   tgt.card_types.contains(&crate::card::CardType::Creature)
         .                       && colors.iter().any(|c| tgt.colors.contains(c))
    22,280 ( 0.00%)      }
         .           
         .               /// The keywords [`damage_prevented_by_protection_inner`] can answer `true`
         .               /// for. Kept next to it: adding a protection keyword without adding it
         .               /// here silently turns that protection off.
         .               #[inline]
         .               fn protection_keyword(kw: &Keyword) -> bool {
    52,068 ( 0.00%)          matches!(
         .                       kw,
         .                       Keyword::Protection(_)
         .                           | Keyword::ProtectionFromOwnColors
         .                           | Keyword::ProtectionFromCreatures
         .                           | Keyword::ProtectionFromCreatureType(_)
         .                           | Keyword::ProtectionFromMatching(_)
         .                           | Keyword::ProtectionFromManaValueExcept(_)
         .                           | Keyword::ProtectionFromManaValueParity { .. }
         .                           | Keyword::ProtectionFromMulticolored
         .                           | Keyword::ProtectionFromMonocolored
         .                           | Keyword::ProtectionFromCardType(_)
         .                           | Keyword::ProtectionFromEverything
         .                   )
         .               }
         .           
   183,540 ( 0.01%)      pub fn damage_prevented_by_protection(&self, source: CardId, target: CardId) -> bool {
         .                   // Both sides read through the layer system — share one gather.
         .                   self.with_frozen_layers(|g| g.damage_prevented_by_protection_inner(source, target))
   165,186 ( 0.01%)      }
         .           
         .               fn damage_prevented_by_protection_inner(&self, source: CardId, target: CardId) -> bool {
   110,122 ( 0.00%)          let Some(tgt) = self.computed_permanent(target) else { return false };
38,369,888 ( 1.23%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (18,354x)
         .                   // Everything below this reads the *source* — five `computed_permanent`
         .                   // lookups and two `Vec` clones — and none of it can change the answer
         .                   // unless the target carries a protection keyword. Most permanents
         .                   // never do, so gate on the one thing that is already computed.
         .                   // `is_protection_keyword` must list exactly the keywords the
         .                   // `ProtectionFromCreatures` check and the final `match` can answer
         .                   // `true` for.
         .                   if !tgt.keywords.iter().any(Self::protection_keyword) {
         .                       return false;
         .                   }
         .                   let src_colors: crate::mana::ColorSet = self
       316 ( 0.00%)              .computed_permanent(source)
   148,316 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (158x)
       158 ( 0.00%)              .map(|c| c.colors)
         .                       .unwrap_or_else(|| {
         .                           self.battlefield_find(source)
         .                               .map(|c| c.definition.cost.colors().into_iter().collect())
         .                               .unwrap_or_default()
         .                       });
         .                   // CR 702.16 — protection from creatures prevents all damage from a
         .                   // creature source (Spirit Mantle).
         .                   let src_is_creature = self
       316 ( 0.00%)              .computed_permanent(source)
    11,392 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (158x)
         .                       .map(|c| c.card_types.contains(&crate::card::CardType::Creature))
         .                       .unwrap_or_else(|| {
         .                           self.battlefield_find(source)
         .                               .map(|c| c.definition.is_creature())
         .                               .unwrap_or(false)
         .                       });
       158 ( 0.00%)          if src_is_creature && tgt.keywords.contains(&Keyword::ProtectionFromCreatures) {
         .                       return true;
         .                   }
         .                   // CR 702.16e — protection from a creature type prevents damage from a
         .                   // source of that type.
         .                   let src_creature_types = self
       316 ( 0.00%)              .computed_permanent(source)
    11,392 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (158x)
         .                       .map(|c| c.subtypes.creature_types.clone())
         .                       .unwrap_or_else(|| {
         .                           self.battlefield_find(source)
         .                               .map(|c| c.definition.subtypes.creature_types.clone())
         .                               .unwrap_or_default()
         .                       });
         .                   let src_mv = self
         .                       .battlefield_find(source)
       316 ( 0.00%)              .map(|c| c.definition.cost.cmc())
     4,966 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (158x)
         .                       .unwrap_or(0);
         .                   let src_card_types = self
       474 ( 0.00%)              .computed_permanent(source)
    11,392 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (158x)
       158 ( 0.00%)              .map(|c| c.card_types.to_vec())
         .                       .unwrap_or_else(|| {
         .                           self.battlefield_find(source)
         .                               .map(|c| c.definition.card_types.clone())
         .                               .unwrap_or_default()
         .                       });
     5,334 ( 0.00%)          tgt.keywords.iter().any(|kw| match kw {
         .                       Keyword::Protection(color) => src_colors.contains(color),
         .                       // CR 702.16 — "protection from its colors" (Earnest Fellowship).
         .                       Keyword::ProtectionFromOwnColors => tgt.colors.intersects(src_colors),
         .                       Keyword::ProtectionFromCreatureType(ty) => src_creature_types.contains(ty),
         .                       Keyword::ProtectionFromMatching(f) => self.evaluate_requirement_static(
         .                           f,
         .                           &Target::Permanent(source),
         .                           tgt.controller,
-- line 11699 ----------------------------------------
-- line 11707 ----------------------------------------
         .                       Keyword::ProtectionFromEverything => true,
         .                       _ => false,
         .                   })
         .               }
         .           
         .               /// CR 702.89 — Umbra armor: if the creature would be destroyed, instead
         .               /// remove all damage from it and destroy one umbra-armor Aura attached
         .               /// to it. Returns true when the destruction was replaced.
    36,620 ( 0.00%)      pub(crate) fn apply_umbra_armor(
         .                   &mut self,
         .                   id: CardId,
         .                   events: &mut Vec<GameEvent>,
         .               ) -> bool {
    14,648 ( 0.00%)          let Some(aura_id) = self.battlefield.iter().find_map(|c| {
     1,676 ( 0.00%)              (c.attached_to == Some(id)
         .                           && c.definition.keywords.contains(&Keyword::UmbraArmor))
         .                       .then_some(c.id)
         .                   }) else {
         .                       return false;
         .                   };
         .                   if let Some(c) = self.battlefield_find_mut(id) {
         .                       c.damage = 0;
         .                   }
         .                   let mut evs = self.remove_to_graveyard_with_triggers(aura_id);
         .                   events.append(&mut evs);
         .                   true
    36,620 ( 0.00%)      }
         .           
         .               /// Add a transient continuous effect (from a spell/ability resolution).
         .               pub fn add_continuous_effect(&mut self, effect: ContinuousEffect) {
         .                   self.continuous_effects.push(effect);
         .               }
         .           
         .               /// CR 702.107 — the replicate cost a static grants this spell (Djinn
         .               /// Illuminatus: every instant/sorcery you cast replicates for its own
-- line 11741 ----------------------------------------
-- line 11772 ----------------------------------------
         .                   let ts = self.next_timestamp();
         .                   if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == cid) {
         .                       c.granted_keywords_eot.push(kw);
         .                       c.granted_keywords_eot_ts.push(ts);
         .                   }
         .               }
         .           
         .               pub fn next_timestamp(&mut self) -> u64 {
     3,904 ( 0.00%)          let ts = self.next_effect_timestamp;
     7,808 ( 0.00%)          self.next_effect_timestamp += 1;
         .                   ts
         .               }
         .           
         .               /// Remove the continuous effects whose source is `id` (source left the
         .               /// battlefield). CR 611.2b — a one-shot effect that created a continuous
         .               /// effect with no duration (`EffectDuration::Indefinite`) doesn't depend on
         .               /// its source sticking around, so those survive (Chainer's reanimated
         .               /// Nightmares keep the type after he dies).
         .               pub(crate) fn remove_effects_from_source(&mut self, id: CardId) {
         .                   self.continuous_effects
     7,764 ( 0.00%)              .retain(|e| e.source != id || e.duration == EffectDuration::Indefinite);
     1,368 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (76x)
         .               }
         .           
         .               /// Expire all `UntilEndOfTurn` continuous effects (called during Cleanup).
         .               /// Also sweeps `UntilEndOfCombat` for cards that registered combat-
         .               /// scoped effects during a turn that ended without an actual combat
         .               /// phase (defensive cleanup so they don't leak indefinitely).
         .               /// Map a card-level [`crate::effect::Duration`] onto a layer-system
         .               /// duration. CR 611.2b — "until your next untap/turn" is scoped to
-- line 11800 ----------------------------------------
-- line 11814 ----------------------------------------
         .                           player: controller,
         .                           installed_turn: self.turn_number,
         .                       },
         .                       other => crate::game::effects::map_effect_duration(other),
         .                   }
         .               }
         .           
         .               pub fn expire_end_of_turn_effects(&mut self) {
     3,436 ( 0.00%)          self.continuous_effects.retain(|e| {
    30,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (1,718x)
         .                       e.duration != EffectDuration::UntilEndOfTurn
         .                           && e.duration != EffectDuration::UntilEndOfCombat
         .                   });
         .               }
         .           
         .               /// Revert temporary control changes (Act of Treason / Threaten) whose
         .               /// `Duration` is in `which`. The stolen permanent returns to whoever
         .               /// controlled it immediately before the steal. Entries whose card has
         .               /// since left the battlefield are dropped without effect (CR 800.4 —
         .               /// the control-changing effect simply ends).
    31,014 ( 0.00%)      pub(crate) fn revert_temporary_control(&mut self, which: &[crate::effect::Duration]) {
         .                   let mut kept = Vec::new();
         .                   for tc in std::mem::take(&mut self.temporary_control) {
         .                       let on_battlefield = self.battlefield.iter().any(|c| c.id == tc.card);
         .                       if !on_battlefield {
         .                           continue; // card left play — nothing to revert
         .                       }
         .                       if which.contains(&tc.duration) {
         .                           self.change_control(tc.card, tc.original_controller);
         .                       } else {
         .                           kept.push(tc);
         .                       }
         .                   }
    24,122 ( 0.00%)          self.temporary_control = kept;
    27,568 ( 0.00%)      }
         .           
         .               /// CR 707 / 611.2c — a "becomes a copy" effect ends when the object
         .               /// leaves the battlefield: restore the pre-copy definition on the
         .               /// departing card and drop its pending revert entries. Called from the
         .               /// battlefield-leave funnels alongside `turn_face_up`.
    19,410 ( 0.00%)      pub(crate) fn revert_copy_on_leave(&mut self, card: &mut crate::card::CardInstance) {
         .                   // The oldest entry holds the original printed definition.
     3,882 ( 0.00%)          if let Some(pos) = self.temporary_copies.iter().position(|tc| tc.card == card.id) {
         .                       if let Some(def) = self.temporary_copies[pos].original_def() {
         .                           card.definition = def;
         .                       }
         .                       self.temporary_copies.retain(|tc| tc.card != card.id);
         .                   }
    23,292 ( 0.00%)      }
         .           
         .               /// Revert temporary "becomes a copy" definition swaps
         .               /// (`Effect::BecomeCopyOfFor`) whose `Duration` is in `which`. Reverted
         .               /// in reverse order so stacked copies unwind to the oldest original.
         .               /// Entries whose card left the battlefield are dropped (the copy effect
         .               /// ended with the object).
    34,460 ( 0.00%)      pub(crate) fn revert_temporary_copies(&mut self, which: &[crate::effect::Duration]) {
         .                   let mut kept = Vec::new();
    13,784 ( 0.00%)          for tc in std::mem::take(&mut self.temporary_copies).into_iter().rev() {
         .                       if !self.battlefield.iter().any(|c| c.id == tc.card) {
         .                           continue; // card left play — nothing to revert
         .                       }
         .                       if which.contains(&tc.duration) {
         .                           if let Some(def) = tc.original_def()
         .                               && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == tc.card)
         .                           {
         .                               c.definition = def;
         .                           }
         .                       } else {
         .                           kept.push(tc);
         .                       }
         .                   }
         .                   kept.reverse();
    37,906 ( 0.00%)          self.temporary_copies = kept;
    27,568 ( 0.00%)      }
         .           
         .               /// Expire all `UntilEndOfCombat` continuous effects (CR 511.2 —
         .               /// "Effects that last 'until end of combat' expire at the end of the
         .               /// combat phase"). Invoked from `do_combat_end` once the end-of-
         .               /// combat step finishes.
         .               pub fn expire_end_of_combat_effects(&mut self) {
         .                   self.continuous_effects
     3,456 ( 0.00%)              .retain(|e| e.duration != EffectDuration::UntilEndOfCombat);
    31,104 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (1,728x)
         .               }
         .           
         .               /// Sacrifice/exile Mobilize/Myriad tokens registered by
         .               /// `Effect::CreateTokenAttacking` as the combat phase ends (CR 511.3).
    15,552 ( 0.00%)      pub fn process_attacking_token_cleanup(&mut self) -> Vec<GameEvent> {
         .                   use crate::effect::AttackingTokenCleanup;
         .                   let mut events = Vec::new();
         .                   for (id, kind) in std::mem::take(&mut self.attacking_token_cleanup) {
         .                       if !self.battlefield.iter().any(|c| c.id == id) {
         .                           continue; // already gone (died in combat, bounced, etc.)
         .                       }
         .                       let who = self.battlefield_find(id).map(|c| c.controller).unwrap_or(0);
         .                       match kind {
-- line 11907 ----------------------------------------
-- line 11910 ----------------------------------------
         .                               self.sacrifice_one(id, who, &mut events);
         .                           }
         .                           AttackingTokenCleanup::ExileAtEndOfCombat => {
         .                               self.remove_from_battlefield_to_exile(id);
         .                           }
         .                           AttackingTokenCleanup::None => {}
         .                       }
         .                   }
     3,456 ( 0.00%)          events.append(&mut self.check_state_based_actions());
31,942,946 ( 1.02%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (1,728x)
     8,640 ( 0.00%)          events
    13,824 ( 0.00%)      }
         .           
         .               /// True if the stack is empty and it is `player`'s main phase — sorcery timing.
         .               pub fn can_cast_sorcery_speed(&self, player: usize) -> bool {
    13,508 ( 0.00%)          self.stack.is_empty()
    13,392 ( 0.00%)              && self.step.is_main_phase()
         .                       // CR 805.5a — under shared team turns a teammate acts on the
         .                       // team's turn (and so gets their own land drop, CR 805.4c).
     6,598 ( 0.00%)              && self.on_active_team(player)
     9,736 ( 0.00%)              && self.priority.player_with_priority == player
     4,946 ( 0.00%)      }
         .           
         .               pub fn next_id(&mut self) -> CardId {
       660 ( 0.00%)          let id = CardId(self.next_id);
     1,320 ( 0.00%)          self.next_id += 1;
         .                   id
         .               }
         .           
         .               // ── Public setup helpers (useful in tests) ────────────────────────────────
         .           
         .               /// Add a card to a player's hand without going through library/draw mechanics.
         .               pub fn add_card_to_hand(&mut self, player_idx: usize, def: CardDefinition) -> CardId {
         .                   let id = self.next_id();
-- line 11942 ----------------------------------------
-- line 12132 ----------------------------------------
         .                       inst.battlefield_timestamp = self.next_timestamp();
         .                       self.battlefield.push(inst);
         .                   }
         .                   events.push(GameEvent::GameRestarted { starter });
         .               }
         .           
         .               pub fn add_card_to_library(&mut self, player_idx: usize, def: CardDefinition) -> CardId {
         .                   let id = self.next_id();
     6,600 ( 0.00%)          self.players[player_idx].add_to_library_bottom(id, def);
17,268,510 ( 0.55%)  => /home/user/Crabomination/crabomination/src/player.rs:crabomination::player::PlayerData::add_to_library_bottom (660x)
 5,427,840 ( 0.17%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (660x)
    19,800 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (660x)
         .                   id
         .               }
         .           
         .               /// Put a card into `player_idx`'s Lessons sideboard ("outside the
         .               /// game"). A Learn ability may later reveal it into hand. Used by deck
         .               /// construction and test fixtures exercising the Learn mechanic.
         .               pub fn add_card_to_sideboard(&mut self, player_idx: usize, def: CardDefinition) -> CardId {
         .                   let id = self.next_id();
-- line 12148 ----------------------------------------
-- line 12207 ----------------------------------------
         .               /// declaration-order proxy used as the damage-order default).
         .               /// CR 509.1h — is `attacker` a *blocked* creature? Stays true after every
         .               /// blocker leaves combat, which is why this is separate from
         .               /// `blockers_of(..).is_empty()`.
         .               pub fn attacker_is_blocked(&self, attacker: CardId) -> bool {
         .                   self.blocked_attackers.contains(&attacker)
         .               }
         .           
    51,940 ( 0.00%)      pub fn blockers_of(&self, attacker: CardId) -> Vec<CardId> {
         .                   let mut ids: Vec<CardId> = self
         .                       .block_map
         .                       .iter()
         .                       .filter(|(_, atks)| atks.contains(&attacker))
         .                       .map(|(&bid, _)| bid)
         .                       .collect();
         .                   ids.sort_by_key(|id| id.0);
    41,552 ( 0.00%)          ids
    51,940 ( 0.00%)      }
         .           
         .               /// How many creatures are blocking `attacker`.
         .               pub fn blocker_count_of(&self, attacker: CardId) -> usize {
         .                   self.block_map.values().filter(|atks| atks.contains(&attacker)).count()
         .               }
         .           
         .               /// The attackers `blocker` is blocking, in declaration order.
         .               pub fn attackers_blocked_by(&self, blocker: CardId) -> &[CardId] {
-- line 12232 ----------------------------------------
-- line 12234 ----------------------------------------
         .               }
         .           
         .               /// Is `id` blocking anything this combat?
         .               pub fn is_blocking(&self, id: CardId) -> bool {
         .                   self.block_map.contains_key(&id)
         .               }
         .           
         .               /// Is `blocker` blocking `attacker`?
    13,368 ( 0.00%)      pub fn blocks(&self, blocker: CardId, attacker: CardId) -> bool {
         .                   self.attackers_blocked_by(blocker).contains(&attacker)
    13,368 ( 0.00%)      }
         .           
         .               /// Record `blocker` as blocking `attacker` (idempotent, order-preserving).
    12,120 ( 0.00%)      pub(crate) fn add_block(&mut self, blocker: CardId, attacker: CardId) {
         .                   let entry = self.block_map.entry(blocker).or_default();
         .                   if !entry.contains(&attacker) {
         .                       entry.push(attacker);
         .                   }
    14,544 ( 0.00%)      }
         .           
         .               pub fn blockers_declared(&self) -> bool {
         .                   self.blockers_declared
         .               }
         .               pub fn skip_first_draw(&self) -> bool {
         .                   self.skip_first_draw
         .               }
         .               pub fn peek_next_id(&self) -> u32 {
-- line 12260 ----------------------------------------
-- line 12290 ----------------------------------------
         .               /// view layer expose blocks per-permanent without making `block_map`
         .               /// public.
         .               /// CR 510.1c — attackers that became blocked this combat (they stay
         .               /// blocked even if every blocker has since left combat).
         .               pub fn blocked_attackers(&self) -> &[CardId] {
         .                   &self.blocked_attackers
         .               }
         .           
     1,848 ( 0.00%)      pub fn block_map_snapshot(&self) -> Vec<(CardId, CardId)> {
         .                   let mut out: Vec<(CardId, CardId)> = self
         .                       .block_map
         .                       .iter()
         .                       .flat_map(|(&b, atks)| atks.iter().map(move |&a| (b, a)))
         .                       .collect();
         .                   out.sort_by_key(|(b, a)| (b.0, a.0));
     1,848 ( 0.00%)          out
     2,310 ( 0.00%)      }
         .           
         .               /// Look up the attack record for a given attacker id, if any.
         .               pub fn attack_for(&self, attacker: CardId) -> Option<&Attack> {
         .                   self.attacking.iter().find(|a| a.attacker == attacker)
         .               }
         .           
         .               /// Resolve the defending player for a given attack target.
         .               pub fn defender_for(&self, target: AttackTarget) -> Option<usize> {
    73,492 ( 0.00%)          match target {
    27,964 ( 0.00%)              AttackTarget::Player(p) => Some(p),
         .                       AttackTarget::Planeswalker(pw) => {
         .                           self.battlefield_find(pw).map(|c| c.controller)
         .                       }
         .                       // CR 508.4 — the defending player for an attack on a battle is its
         .                       // protector, who defends it with their creatures.
         .                       AttackTarget::Battle(b) => {
         .                           self.battlefield_find(b).and_then(|c| c.protected_by)
         .                       }
         .                   }
     4,232 ( 0.00%)      }
         .           
         .           
         .               /// True if `blocker_id` can legally block at least one current attacker.
         .               /// Each declared attacker's live `CardInstance` paired with its computed
         .               /// view, gathered once. Hoisted out of the per-blocker scan so
         .               /// `legal_blockers` / the bot's block planner pay `apply_layers_one`
         .               /// per *attacker*, not per attacker × blocker (audit P2).
         .               pub(crate) fn computed_attackers(
-- line 12334 ----------------------------------------
-- line 12347 ----------------------------------------
         .               /// True if `blocker_id` can legally block at least one of the prebuilt
         .               /// attacker views (build them with [`computed_attackers`]).
         .               ///
         .               /// [`computed_attackers`]: Self::computed_attackers
         .               /// CR 509.1a/b — the blocker-side gates that don't depend on which
         .               /// attacker is being blocked. Shared by `can_block_any_computed_attacker`
         .               /// (the affordance/bot scan) and `blocker_can_block_attacker` (the
         .               /// per-pair check) so the two can't drift apart.
    62,656 ( 0.00%)      fn blocker_side_gates_allow_block(
         .                   &self,
         .                   blocker: &CardInstance,
         .                   blocker_cp: &ComputedPermanent,
         .               ) -> bool {
     7,832 ( 0.00%)          let owner = blocker.controller;
         .                   if blocker_cp.keywords.contains(&Keyword::CantBlock)
         .                       || blocker_cp.keywords.contains(&Keyword::Decayed)
         .                   {
         .                       return false;
         .                   }
         .                   if blocker_cp.keywords.contains(&Keyword::CantAttackOrBlockUnlessEvenCounters)
         .                       && blocker.counters.values().sum::<u32>() % 2 != 0
         .                   {
         .                       return false;
         .                   }
         .                   // CR 508.1g — the whole "unless its controller pays {N}" family (flat,
         .                   // per-counter, per-enchanter-hand-card, per-permanent) is legal only if
         .                   // the seat can actually produce the tax.
    37,680 ( 0.00%)          let tax = self.attack_block_keyword_tax(blocker.id, &blocker_cp.keywords, false);
   319,956 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::attack_block_keyword_tax (7,536x)
    22,608 ( 0.00%)          if tax > 0 && !self.could_pay_generic(owner, tax) {
         .                       return false;
         .                   }
         .                   // Hazoret-class hand-size gate, delirium, "a creature died this turn",
         .                   // Descend N and the city's blessing.
    27,522 ( 0.00%)          if blocker_cp.keywords.iter().any(|k| match k {
         .                       Keyword::CantAttackOrBlockUnlessHandSizeAtMost(n) => {
         .                           self.players[owner].hand.len() as u32 > *n
         .                       }
         .                       Keyword::CantAttackOrBlockUnlessDelirium => !self.delirium_active(owner),
         .                       Keyword::CantAttackOrBlockUnlessCreatureDiedThisTurn => {
         .                           self.players[owner].creatures_died_this_turn == 0
         .                       }
         .                       Keyword::CantAttackOrBlockUnlessDescend(n) => {
-- line 12388 ----------------------------------------
-- line 12396 ----------------------------------------
         .                           .iter()
         .                           .any(|c| c.controller == owner && c.definition.is_land() && !c.tapped),
         .                       _ => false,
         .                   }) {
         .                       return false;
         .                   }
         .                   // CR 509.1b — Hollow Warrior: a spare untapped match must exist to tap,
         .                   // and it can be neither the blocker nor a declared attacker.
    18,348 ( 0.00%)          if let Some(f) = blocker_cp.keywords.iter().find_map(|kw| match kw {
         .                       Keyword::AttackBlockCostTapAnother(f) => Some((**f).clone()),
     9,174 ( 0.00%)              _ => None,
         .                   }) && !self.battlefield.iter().any(|c| {
         .                       c.controller == owner
         .                           && !c.tapped
         .                           && c.id != blocker.id
         .                           && !self.attacking.iter().any(|a| a.attacker == c.id)
         .                           && self.evaluate_requirement_on_card(&f, c, owner)
         .                   }) {
         .                       return false;
         .                   }
         .                   // "Can't block unless you control N+ [filter]" (Topiary Stomper).
         .                   // Attack-only gates (Lambholt Pacifist) don't restrict blocking.
    33,456 ( 0.00%)          if let Some((req, min, excl)) = blocker_cp.keywords.iter().find_map(|kw| match kw {
         .                       Keyword::CantAttackOrBlockUnlessYouControlCount {
         .                           filter, min, attack_only: false, exclude_self, ..
         .                       } => Some((filter.clone(), *min, *exclude_self)),
         .                       _ => None,
         .                   }) {
         .                       let n = self
         .                           .battlefield
         .                           .iter()
-- line 12426 ----------------------------------------
-- line 12430 ----------------------------------------
         .                                   && self.evaluate_requirement_on_card(&req, c, owner)
         .                           })
         .                           .count();
         .                       if (n as u32) < min {
         .                           return false;
         .                       }
         .                   }
         .                   true
    62,656 ( 0.00%)      }
         .           
         .               pub(crate) fn can_block_any_computed_attacker(
         .                   &self,
         .                   blocker_id: CardId,
         .                   attackers: &[(&CardInstance, std::sync::Arc<ComputedPermanent>)],
         .               ) -> bool {
         .                   let Some(blocker) = self.battlefield.iter().find(|c| c.id == blocker_id) else {
         .                       return false;
-- line 12446 ----------------------------------------
-- line 12480 ----------------------------------------
         .               /// unlike the keyword-only restrictions it needs `self`; every
         .               /// block-legality path routes through here.
         .               pub(crate) fn blocker_matching_restriction_bars(
         .                   &self,
         .                   blocker: &CardInstance,
         .                   blocker_kws: &[Keyword],
         .                   attacker_id: CardId,
         .               ) -> bool {
    24,948 ( 0.00%)          blocker_kws.iter().any(|k| match k {
         .                       Keyword::CantBlockMatching(f) => self.evaluate_requirement_static(
         .                           f,
         .                           &Target::Permanent(attacker_id),
         .                           blocker.controller,
         .                           Some(blocker.id),
         .                       ),
         .                       _ => false,
         .                   })
     7,536 ( 0.00%)      }
         .           
         .               /// True if `blocker_id` can legally block `attacker_id`.
    70,488 ( 0.00%)      pub fn blocker_can_block_attacker(&self, blocker_id: CardId, attacker_id: CardId) -> bool {
     7,832 ( 0.00%)          let Some(blocker) = self.battlefield.iter().find(|c| c.id == blocker_id) else {
         .                       return false;
         .                   };
         .                   let Some(attacker) = self.battlefield.iter().find(|c| c.id == attacker_id) else {
         .                       return false;
         .                   };
         .                   // Per-id computed views — see `can_block_any_attacker`.
    39,160 ( 0.00%)          let Some(blocker_cp) = self.computed_permanent(blocker_id) else {
 3,896,705 ( 0.13%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (7,832x)
         .                       return false;
         .                   };
         .                   let blocker_cp = &blocker_cp;
    15,664 ( 0.00%)          if !blocker_cp.card_types.contains(&crate::card::CardType::Creature) || blocker.tapped {
         .                       return false;
         .                   }
         .                   // CR 702.158d — Space Beleren's +1: creatures can be blocked this turn
         .                   // only by creatures in the same sector.
         .                   if self.sector_block_lock_turn == Some(self.turn_number)
         .                       && blocker.sector != attacker.sector
         .                   {
         .                       return false;
         .                   }
         .                   // CR 509.1a/b — every attacker-independent gate (CantBlock, Decayed,
         .                   // the hand-size / pay / delirium / descend / blessing gates, Branded
         .                   // Brawlers, Hollow Warrior's tap cost, Topiary Stomper's count).
    39,160 ( 0.00%)          if !self.blocker_side_gates_allow_block(blocker, blocker_cp) {
 2,099,718 ( 0.07%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::blocker_side_gates_allow_block (7,832x)
         .                       return false;
         .                   }
         .                   // CR 509.1b — Mogg Toady: strictly more creatures than the attacker's
         .                   // controller. Attacker-dependent, so it stays here.
         .                   if blocker_cp.keywords.contains(&Keyword::CantBlockUnlessMoreCreaturesThanAttacker)
         .                       && self.creature_count(blocker.controller) <= self.creature_count(attacker.controller)
         .                   {
         .                       return false;
         .                   }
    30,144 ( 0.00%)          let atk_cp = self.computed_permanent(attacker_id);
   505,416 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (7,536x)
         .                   let atk_kws = atk_cp.as_ref().map(|c| c.keywords.as_slice()).unwrap_or(&[]);
     7,536 ( 0.00%)          let atk_colors = atk_cp.as_ref().map(|c| c.colors).unwrap_or_default();
    15,072 ( 0.00%)          let atk_power = atk_cp.as_ref().map(|c| c.power).unwrap_or_else(|| attacker.power());
         .                   // CR 509.1b — "can't be blocked as long as defending player controls a
         .                   // [filter]" (Neurok Spy, Hazy Homunculus). Enforced in
         .                   // `declare_blockers` too; mirrored here so the UI/bot never offers the
         .                   // block.
    60,580 ( 0.00%)          if atk_kws.iter().any(|kw| match kw {
   243,700 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (7,536x)
         .                       Keyword::CantBeBlockedIfDefenderControls(f) => self.battlefield.iter().any(|c| {
         .                           c.controller == blocker.controller
         .                               && self.evaluate_requirement_static(
         .                                   f.as_ref(),
         .                                   &Target::Permanent(c.id),
         .                                   blocker.controller,
         .                                   None,
         .                               )
-- line 12551 ----------------------------------------
-- line 12552 ----------------------------------------
         .                       }),
         .                       _ => false,
         .                   }) {
         .                       return false;
         .                   }
         .                   // CR 701.54c (level 1+) — "Your Ring-bearer … can't be blocked by
         .                   // creatures with greater power." Same shape as Skulk, but keyed on the
         .                   // attacker being its controller's Ring-bearer.
    22,608 ( 0.00%)          if self.effective_ring_bearer(attacker.controller) == Some(attacker_id)
   180,864 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::effective_ring_bearer (7,536x)
         .                       && self.players[attacker.controller].ring_temptations >= 1
         .                       && blocker_cp.power > atk_power
         .                   {
         .                       return false;
         .                   }
    60,288 ( 0.00%)          if self.block_barred_by_protection_filter(atk_kws, attacker.controller, blocker.id) {
   166,630 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::block_barred_by_protection_filter (7,536x)
         .                       return false;
         .                   }
    45,216 ( 0.00%)          if self.blocker_matching_restriction_bars(blocker, &blocker_cp.keywords, attacker_id) {
   188,214 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::blocker_matching_restriction_bars (7,536x)
         .                       return false;
         .                   }
    45,216 ( 0.00%)          can_block_attacker_computed(blocker, blocker_cp, atk_kws, atk_colors, atk_power)
 3,875,506 ( 0.12%)  => crabomination/src/game/mod.rs:crabomination::game::can_block_attacker_computed (7,536x)
    62,656 ( 0.00%)      }
         .           
         .               /// True when some opponent of `seat` controls a battlefield permanent with
         .               /// a static matching `pred`. The shared shape behind the "an opponent
         .               /// controls X, so you can't Y" locks.
    22,022 ( 0.00%)      pub fn opponent_has_static(
         .                   &self,
         .                   seat: usize,
         .                   pred: impl Fn(&crate::effect::StaticEffect) -> bool,
         .               ) -> bool {
         .                   self.battlefield.iter().any(|c| {
   354,372 ( 0.01%)              !self.same_team(c.controller, seat)
 1,128,490 ( 0.04%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (59,062x)
         .                           && c.definition.static_abilities.iter().any(|sa| pred(&sa.effect))
         .                   })
    25,168 ( 0.00%)      }
         .           
         .               /// CR 702.16 — the state-aware half of the protection block gate: an
         .               /// attacker with `Keyword::ProtectionFromMatching` can't be blocked by a
         .               /// creature matching that filter (Harbinger of Spring). The keyword's
         .               /// filter needs game state, so it lives here rather than in the pure
         .               /// `can_block_attacker_computed`.
         .               pub(crate) fn block_barred_by_protection_filter(
         .                   &self,
         .                   attacker_kws: &[Keyword],
         .                   attacker_controller: usize,
         .                   blocker_id: CardId,
         .               ) -> bool {
         .                   attacker_kws.iter().any(|kw| {
    19,660 ( 0.00%)              matches!(kw, Keyword::ProtectionFromMatching(f)
         .                           if self.evaluate_requirement_static(
         .                               f,
         .                               &Target::Permanent(blocker_id),
         .                               attacker_controller,
         .                               None,
         .                           ))
         .                   })
     7,536 ( 0.00%)      }
         .           
         .               // ── Main action dispatch ──────────────────────────────────────────────────
         .           
         .               /// Transactional entry point (TODO "Rollback / Undo" Phase 1): every
         .               /// rejected action restores the exact pre-action state, structurally
         .               /// killing the partial-mutation bug family (under-paid costs,
         .               /// mid-loop combat corruption, half-applied casts). The checkpoint is
         .               /// a CoW snapshot (`crate::cow`) — reference bumps, not deep copies —
-- line 12617 ----------------------------------------
-- line 12619 ----------------------------------------
         .               /// erroring.
         .               ///
         .               /// Suspension is NOT failure: a mid-action `pending_decision` /
         .               /// `suspend_signal` exit returns `Ok` and keeps its partial state for
         .               /// the resume path. And the *live* decider survives a restore — the
         .               /// checkpoint's clone holds a fresh decider (see `Clone for
         .               /// GameState`), and swapping that blank box in would wipe a
         .               /// `ScriptedDecider` mid-script on any rejected action.
   244,260 ( 0.01%)      pub fn perform_action(&mut self, action: GameAction) -> Result<Vec<GameEvent>, GameError> {
         .                   // The no-mutation rejections skip the checkpoint entirely — they
         .                   // are the hot rejection paths under bot polling.
    27,140 ( 0.00%)          if self.is_game_over() {
         .                       return Err(GameError::GameAlreadyOver);
         .                   }
   135,520 ( 0.00%)          if !matches!(action, GameAction::SubmitDecision(_)) && self.pending_decision.is_some() {
         .                       return Err(GameError::DecisionPending);
         .                   }
         .                   // The checkpoint exists to restore a *failed* action's partial
         .                   // mutation, and the mid-round priority pass has neither half: it
         .                   // cannot fail, and it writes two integers. Every `return Err` between
         .                   // here and `perform_action_inner`'s dispatch is a pure validation
         .                   // guard that has not touched the state, so an `Err` on this path
         .                   // needs no restore either. This is 41 % of the actions a bot game
         .                   // takes — and the checkpoint is not just its own clone and drop: it
         .                   // shares every CoW zone, so the action's first write to one deep-
         .                   // copies it.
    73,458 ( 0.00%)          if matches!(action, GameAction::PassPriority) && self.pass_priority_is_trivial() {
   165,240 ( 0.01%)              return self.perform_action_inner(action);
 4,485,008 ( 0.14%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner (9,720x)
         .                   }
    34,840 ( 0.00%)          let mut checkpoint = self.clone();
40,144,242 ( 1.29%)  => crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (17,420x)
   278,720 ( 0.01%)          let result = self.perform_action_inner(action);
871,676,706 (27.97%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner (17,420x)
         .                   // `ManualTapRequired` is a suspension dressed as an `Err`: the
         .                   // engine deliberately leaves the forced pips auto-tapped and their
         .                   // mana floating for the client's pending-cast driver to finish the
         .                   // payment. Rolling that back would break the interactive tap flow.
    34,868 ( 0.00%)          if matches!(&result, Err(e) if !matches!(e, GameError::ManualTapRequired { .. })) {
         .                       std::mem::swap(&mut checkpoint.decider, &mut self.decider);
        98 ( 0.00%)              *self = checkpoint;
    15,685 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (14x)
    11,564 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (28x)
         .                   }
    87,086 ( 0.00%)          result
   197,666 ( 0.01%)      }
93,058,945 ( 2.99%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (17,406x)
         .           
         .               /// [`perform_action`] without the transaction checkpoint. Directly
         .               /// used by the affordance probes (`would_accept*`): a probe's state is
         .               /// thrown away either way, so restoring it on `Err` is pure waste.
         .               ///
         .               /// [`perform_action`]: Self::perform_action
   554,800 ( 0.02%)      pub(crate) fn perform_action_inner(
         .                   &mut self,
         .                   action: GameAction,
         .               ) -> Result<Vec<GameEvent>, GameError> {
    69,350 ( 0.00%)          if self.is_game_over() {
         .                       return Err(GameError::GameAlreadyOver);
         .                   }
         .                   // Routing for decision answers is unconditional; everything else must
         .                   // wait until the pending decision is resolved.
   332,226 ( 0.01%)          if let GameAction::SubmitDecision(answer) = action {
       694 ( 0.00%)              return self.submit_decision(answer);
104,805,587 ( 3.36%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::submit_decision (694x)
         .                   }
    68,656 ( 0.00%)          if self.pending_decision.is_some() {
         .                       return Err(GameError::DecisionPending);
         .                   }
         .                   // Revel in Silence-style lock: a silenced player can't cast spells
         .                   // or activate loyalty abilities this turn. Gated here so every
         .                   // Cast* action variant is covered at once.
         .                   if action.is_cast_or_loyalty()
    29,824 ( 0.00%)              && self.players[self.priority.player_with_priority].silenced_this_turn
         .                   {
         .                       return Err(GameError::SilencedThisTurn);
         .                   }
         .                   // CR 702.50b — a player can't cast spells once an epic spell they
         .                   // control resolves (the per-upkeep copies are put on the stack by
         .                   // the epic ability itself, not cast).
         .                   if action.is_cast()
    29,824 ( 0.00%)              && !self.players[self.priority.player_with_priority].epic_spells.is_empty()
         .                   {
         .                       return Err(GameError::EpicLocked);
         .                   }
         .                   // Rule of Law-style one-spell-per-turn locks — gated here so every
         .                   // Cast* variant is covered at once. The plain `OneSpellPerTurn` lock
         .                   // (Rule of Law) applies to any spell; `OneNoncreatureSpellPerTurn`
         .                   // (Deafening Silence) and `OneNonartifactSpellPerTurn` (Ethersworn
         .                   // Canonist) only count spells of the matching type.
         .                   if action.is_cast() {
         .                       use crate::effect::StaticEffect;
     7,456 ( 0.00%)              let pl = &self.players[self.priority.player_with_priority];
         .                       // The card types of the spell being cast (None for prepare spells,
         .                       // which don't carry the cast card in a `card_id` field).
         .                       let cast_types = action
     7,456 ( 0.00%)                  .cast_card_id()
    22,368 ( 0.00%)                  .and_then(|id| self.find_card_anywhere(id))
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
    22,368 ( 0.00%)                  .map(|c| c.definition.card_types.clone());
   848,166 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (7,456x)
         .                       let is_creature =
         .                           cast_types.as_ref().is_some_and(|t| t.contains(&CardType::Creature));
         .                       let is_artifact =
         .                           cast_types.as_ref().is_some_and(|t| t.contains(&CardType::Artifact));
    22,368 ( 0.00%)              let blocked = self.battlefield.iter().any(|c| {
         .                           c.definition.static_abilities.iter().any(|sa| match sa.effect {
         .                               StaticEffect::OneSpellPerTurn => pl.spells_cast_this_game_turn >= 1,
         .                               StaticEffect::EnchantedPlayerOneSpellPerTurn => {
         .                                   c.attached_to_player == Some(self.priority.player_with_priority)
         .                                       && pl.spells_cast_this_game_turn >= 1
         .                               }
         .                               StaticEffect::OneNoncreatureSpellPerTurn => {
         .                                   !is_creature && pl.noncreature_spells_cast_this_game_turn >= 1
-- line 12723 ----------------------------------------
-- line 12725 ----------------------------------------
         .                               StaticEffect::OneNonartifactSpellPerTurn => {
         .                                   !is_artifact && pl.nonartifact_spells_cast_this_game_turn >= 1
         .                               }
         .                               _ => false,
         .                           })
         .                       });
         .                       // CR 904.8 — a face-up scheme's statics function from the command
         .                       // zone, so the opponents-only spell lock is scanned there.
    14,912 ( 0.00%)              let caster = self.priority.player_with_priority;
    26,210 ( 0.00%)              let scheme_locked = pl.spells_cast_this_game_turn >= 1
         .                           && self.players.iter().enumerate().any(|(seat, owner)| {
     4,488 ( 0.00%)                      seat != caster
         .                                   && owner.command.iter().any(|c| {
         .                                       c.definition.is_scheme()
         .                                           && c.definition.static_abilities.iter().any(|sa| {
         .                                               matches!(
         .                                                   sa.effect,
         .                                                   StaticEffect::OpponentsOneSpellPerTurn
         .                                               )
         .                                           })
         .                                   })
         .                           });
    21,246 ( 0.00%)              if blocked || scheme_locked {
         .                           return Err(GameError::SpellLimitReached);
         .                       }
         .                       // Mana Maze — no spell may share a colour with the turn's last
         .                       // cast. Vacuous while nothing has been cast this turn.
    22,368 ( 0.00%)              if !self.last_cast_spell_colors.is_empty()
     1,666 ( 0.00%)                  && self.battlefield.iter().any(|c| {
         .                               c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(sa.effect, StaticEffect::CantCastSharingColorWithLastCastSpell)
         .                               })
         .                           })
         .                           && action
         .                               .cast_card_id()
         .                               .and_then(|id| self.find_card_anywhere(id))
         .                               .is_some_and(|c| {
-- line 12761 ----------------------------------------
-- line 12763 ----------------------------------------
         .                                       .printed_colors()
         .                                       .iter()
         .                                       .any(|k| self.last_cast_spell_colors.contains(k))
         .                               })
         .                       {
         .                           return Err(GameError::SpellLimitReached);
         .                       }
         .                       // Cornered Market — no spell may share a nontoken permanent's name.
    59,648 ( 0.00%)              if self.name_locked_by_a_permanent(action.cast_card_id()) {
 3,826,972 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs:crabomination::game::GameState::name_locked_by_a_permanent (7,456x)
         .                           return Err(GameError::SpellLimitReached);
         .                       }
         .                       // Single Combat — while any lock is active, no player may cast a
         .                       // creature or planeswalker spell (CR 601 cast restriction).
    29,824 ( 0.00%)              if !self.creature_pw_cast_locks.is_empty()
         .                           && cast_types.as_ref().is_some_and(|t| {
         .                               t.contains(&CardType::Creature) || t.contains(&CardType::Planeswalker)
         .                           })
         .                       {
         .                           return Err(GameError::SpellLimitReached);
         .                       }
         .                   }
         .                   // CR 702.61 — split second: while such a spell is on the stack no
         .                   // player may cast spells or activate non-mana abilities. Special
         .                   // actions (land drops, foretell, plot, turning face up, suspend…)
         .                   // and triggered abilities are unaffected (702.61b).
    68,656 ( 0.00%)          if self.stack_has_split_second() && self.split_second_blocks(&action) {
         .                       return Err(GameError::SplitSecondLock);
         .                   }
         .                   // Iona, Shield of Emeria — opponents can't cast spells of the
         .                   // chosen color (read off the printed cost's pips).
         .                   if action.is_cast() {
    14,912 ( 0.00%)              let caster = self.priority.player_with_priority;
         .                       let cast_colors: Vec<crate::mana::Color> = action
     7,456 ( 0.00%)                  .cast_card_id()
    22,368 ( 0.00%)                  .and_then(|id| self.find_card_anywhere(id))
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
     7,456 ( 0.00%)                  .map(|c| c.definition.printed_colors())
 1,399,760 ( 0.04%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_colors (7,456x)
         .                           .unwrap_or_default();
    67,104 ( 0.00%)              let locked = self.battlefield.iter().any(|c| {
 4,423,472 ( 0.14%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (7,456x)
   759,372 ( 0.02%)                  !self.same_team(c.controller, caster)
 2,495,952 ( 0.08%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (126,562x)
    64,184 ( 0.00%)                      && c.chosen_color.is_some_and(|col| cast_colors.contains(&col))
         .                               && c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(
         .                                       sa.effect,
         .                                       crate::effect::StaticEffect::OpponentsCantCastChosenColor
         .                                   )
         .                               })
         .                       });
    14,912 ( 0.00%)              if locked {
         .                           return Err(GameError::SilencedThisTurn);
         .                       }
         .                   }
         .                   // Void Winnower — opponents can't cast spells with even mana values
         .                   // (zero is even; read off the printed cost, X counts as 0).
         .                   if action.is_cast() {
         .                       let caster = self.priority.player_with_priority;
         .                       let even_mv = action
     7,456 ( 0.00%)                  .cast_card_id()
    22,368 ( 0.00%)                  .and_then(|id| self.find_card_anywhere(id))
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
    22,368 ( 0.00%)                  .is_some_and(|c| c.definition.cost.cmc() % 2 == 0);
   206,696 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (7,456x)
    29,824 ( 0.00%)              if even_mv {
     6,864 ( 0.00%)                  let locked = self.battlefield.iter().any(|c| {
 1,573,428 ( 0.05%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (2,288x)
   257,508 ( 0.01%)                      !self.same_team(c.controller, caster)
   848,402 ( 0.03%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (42,918x)
         .                                   && c.definition.static_abilities.iter().any(|sa| {
         .                                       matches!(
         .                                           sa.effect,
         .                                           crate::effect::StaticEffect::OpponentsCantCastEvenMv
         .                                       )
         .                                   })
         .                           });
     4,576 ( 0.00%)                  if locked {
         .                               return Err(GameError::SilencedThisTurn);
         .                           }
         .                       }
         .                   }
         .                   // Lavinia, Azorius Renegade — opponents can't cast noncreature spells
         .                   // whose mana value exceeds their own land count.
         .                   if action.is_cast() {
         .                       let caster = self.priority.player_with_priority;
         .                       let over_land_count = action
     7,456 ( 0.00%)                  .cast_card_id()
    22,368 ( 0.00%)                  .and_then(|id| self.find_card_anywhere(id))
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
    74,560 ( 0.00%)                  .is_some_and(|c| {
    26,504 ( 0.00%)                      !c.definition.is_creature() && {
   106,888 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,456x)
         .                                   let lands = self
         .                                       .battlefield
         .                                       .iter()
   149,966 ( 0.00%)                              .filter(|p| p.controller == caster && p.definition.is_land())
   406,762 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (29,782x)
         .                                       .count() as u32;
    13,280 ( 0.00%)                          c.definition.cost.cmc() > lands
    81,086 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (3,320x)
         .                               }
    59,648 ( 0.00%)                  });
    29,824 ( 0.00%)              if over_land_count {
         6 ( 0.00%)                  let locked = self.battlefield.iter().any(|c| {
       298 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (2x)
        36 ( 0.00%)                      !self.same_team(c.controller, caster)
       148 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (6x)
         .                                   && c.definition.static_abilities.iter().any(|sa| {
         .                                       matches!(
         .                                           sa.effect,
         .                                           crate::effect::StaticEffect::OpponentsCantCastNoncreatureAboveLandCount
         .                                       )
         .                                   })
         .                           });
         4 ( 0.00%)                  if locked {
         .                               return Err(GameError::SilencedThisTurn);
         .                           }
         .                       }
         .                   }
         .                   // Damping Engine — the player ahead on permanents can't cast artifact,
         .                   // creature or enchantment spells.
         .                   if action.is_cast() {
         .                       let caster = self.priority.player_with_priority;
         .                       let permanent_spell = action
     7,456 ( 0.00%)                  .cast_card_id()
    22,368 ( 0.00%)                  .and_then(|id| self.find_card_anywhere(id))
 2,166,374 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,456x)
    14,912 ( 0.00%)                  .is_some_and(|c| {
         .                               let d = &c.definition;
    51,376 ( 0.00%)                      d.is_artifact() || d.is_creature() || d.is_enchantment()
   106,888 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,456x)
   119,296 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_artifact (7,456x)
    51,746 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_enchantment (3,320x)
    12,408 ( 0.00%)                  });
    61,982 ( 0.00%)              if permanent_spell && self.damping_engine_locks(caster) {
 1,406,102 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks (4,594x)
         .                           return Err(GameError::SilencedThisTurn);
         .                       }
         .                   }
         .                   // Angelic Arbiter — an opponent who attacked with a creature this turn
         .                   // can't cast spells for the rest of it.
         .                   if action.is_cast() {
     7,456 ( 0.00%)              let caster = self.priority.player_with_priority;
    22,368 ( 0.00%)              if self.players[caster].attacked_this_turn
    12,612 ( 0.00%)                  && self.opponent_has_static(caster, |e| {
 1,345,084 ( 0.04%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::opponent_has_static (2,102x)
         .                               matches!(e, crate::effect::StaticEffect::OpponentsWhoAttackedCantCast)
         .                           })
         .                       {
         .                           return Err(GameError::SilencedThisTurn);
         .                       }
         .                   }
         .                   // Voice of Victory — the active player's opponents can't cast spells
         .                   // during that player's turn. Dosan the Falling Leaf is the symmetric
         .                   // sibling: nobody casts off-turn, whoever controls it.
         .                   // City of Solitude is the broader sibling: it locks activations too,
         .                   // mana abilities included (they route through the same action).
    61,200 ( 0.00%)          if (action.is_cast() || matches!(action, GameAction::ActivateAbility { .. }))
    22,506 ( 0.00%)              && self.priority.player_with_priority != self.active_player_idx
       758 ( 0.00%)              && self.battlefield.iter().any(|c| {
         .                           c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(sa.effect, crate::effect::StaticEffect::PlayersActOnlyOnTheirOwnTurn)
         .                           })
         .                       })
         .                   {
         .                       return Err(GameError::SilencedThisTurn);
         .                   }
         .                   if action.is_cast() {
     7,456 ( 0.00%)              let caster = self.priority.player_with_priority;
     7,456 ( 0.00%)              let active = self.active_player_idx;
    14,912 ( 0.00%)              if caster != active
       750 ( 0.00%)                  && self.battlefield.iter().any(|c| {
         .                               c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(sa.effect, crate::effect::StaticEffect::PlayersCastOnlyOnOwnTurn)
         .                               })
         .                           })
         .                       {
         .                           return Err(GameError::SilencedThisTurn);
         .                       }
         .                       let locked = caster != active
     4,500 ( 0.00%)                  && !self.same_team(caster, active)
    26,250 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (750x)
     2,250 ( 0.00%)                  && self.battlefield.iter().any(|c| {
   171,462 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (750x)
    34,772 ( 0.00%)                      c.controller == active
         .                                   && c.definition.static_abilities.iter().any(|sa| {
         .                                       matches!(
         .                                           sa.effect,
         .                                           crate::effect::StaticEffect::OpponentsCantCastDuringYourTurn
         .                                               | crate::effect::StaticEffect::OpponentsCantActDuringYourTurn
         .                                       )
         .                                   })
         .                           });
     2,250 ( 0.00%)              if locked {
         .                           return Err(GameError::SilencedThisTurn);
         .                       }
         .                   }
   342,204 ( 0.01%)          let events = match action {
         .                       GameAction::PlayLand(id) => self.play_land(id),
         .                       GameAction::PlayLandBack(id) => self.play_land_with_face(id, true),
         .                       GameAction::PlayLandFromGraveyard(id) => self.play_land_from_graveyard(id),
         .                       GameAction::CompanionToHand(card_id) => self.companion_to_hand(card_id),
         .                       GameAction::CastSpell {
     7,456 ( 0.00%)                  card_id,
    14,912 ( 0.00%)                  target,
     7,456 ( 0.00%)                  additional_targets,
     7,456 ( 0.00%)                  mode,
    14,912 ( 0.00%)                  x_value,
   208,768 ( 0.01%)              } => self.cast_spell(card_id, target, additional_targets, mode, x_value),
768,949,071 (24.67%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell (7,456x)
         .                       GameAction::CastSpellKicked {
         .                           card_id,
         .                           target,
         .                           additional_targets,
         .                           mode,
         .                           x_value,
         .                       } => self.cast_spell_kicked(card_id, target, additional_targets, mode, x_value),
         .                       GameAction::CastSpellKickers {
-- line 12958 ----------------------------------------
-- line 13255 ----------------------------------------
         .                       GameAction::CastPrepareSpell {
         .                           creature_id,
         .                           target,
         .                           additional_targets,
         .                           mode,
         .                           x_value,
         .                       } => self.cast_prepare_spell(creature_id, target, additional_targets, mode, x_value),
         .                       GameAction::ActivateAbility {
        46 ( 0.00%)                  card_id,
        46 ( 0.00%)                  ability_index,
        92 ( 0.00%)                  target,
        46 ( 0.00%)                  additional_targets,
        92 ( 0.00%)                  x_value,
       184 ( 0.00%)                  mode,
     1,288 ( 0.00%)              } => self.activate_ability(
 1,206,299 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability (46x)
         .                           card_id,
         .                           ability_index,
         .                           target,
         .                           additional_targets,
         .                           x_value,
         .                           mode,
         .                       ),
         .                       GameAction::ActivateAbilityWaterbend {
-- line 13277 ----------------------------------------
-- line 13285 ----------------------------------------
         .                           card_id, ability_index, target, additional_targets, x_value, &helpers,
         .                       ),
         .                       GameAction::ActivateLoyaltyAbility {
         .                           card_id,
         .                           ability_index,
         .                           target,
         .                           x_value,
         .                       } => self.activate_loyalty_ability(card_id, ability_index, target, x_value),
    15,120 ( 0.00%)              GameAction::DeclareAttackers(ids) => self.declare_attackers(ids),
         .                       GameAction::DeclareAttackersBanded { attacks, bands } => {
         .                           self.declare_attackers_banded(attacks, bands)
         .                       }
   259,800 ( 0.01%)              GameAction::DeclareBlockers(assignments) => self.declare_blockers(assignments),
80,659,278 ( 2.59%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::declare_blockers (2,598x)
 1,165,296 ( 0.04%)              GameAction::PassPriority => self.pass_priority(),
886,986,438 (28.46%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority (52,968x)
         .                       GameAction::SubmitDecision(_) => unreachable!(),
         .                       GameAction::Cycle { card_id, x_value } => self.cycle_card(card_id, x_value),
         .                       GameAction::Reinforce { card_id, target } => self.reinforce_card(card_id, target),
         .                       GameAction::ActivateDiscardAbility { card_id } => {
         .                           self.activate_discard_ability(card_id)
         .                       }
         .                       GameAction::Landcycle { card_id } => self.landcycle_card(card_id),
         .                       GameAction::Equip { equipment, target } => self.equip(equipment, target),
-- line 13306 ----------------------------------------
-- line 13318 ----------------------------------------
         .                       GameAction::RevealConspiracy { card_id } => {
         .                           let seat = self.priority.player_with_priority;
         .                           if self.reveal_hidden_agenda(seat, card_id) {
         .                               Ok(Vec::new())
         .                           } else {
         .                               Err(GameError::InvalidTarget)
         .                           }
         .                       }
     1,076 ( 0.00%)          }?;
         .                   let mut events = events;
         .                   // CR 119.3c — life paid as a cost (Phyrexian pips, life costs) is a
         .                   // life-loss event; surface it after the action so loss triggers fire.
         .                   events.extend(std::mem::take(&mut self.pending_cost_events));
    68,118 ( 0.00%)          self.dispatch_triggers_for_events(&events);
234,654,947 ( 7.53%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (68,118x)
   408,708 ( 0.01%)          Ok(events)
   634,932 ( 0.02%)      }
         .           
         .               /// CR 104.3a — `seat` concedes and leaves the game immediately. Legal at
         .               /// any time, regardless of priority, so this does *not* go through the
         .               /// priority-gated action path. Marks the player eliminated, removes the
         .               /// objects that leave with them (CR 800.4a), then runs state-based
         .               /// actions, which resolve the win/draw for the remaining team(s).
         .               ///
         .               /// No-ops (returns no events) if `seat` is out of range, already
-- line 13341 ----------------------------------------
-- line 13873 ----------------------------------------
         .               /// instead" static (Laboratory Maniac, Jace, Wielder of Mysteries):
         .               /// then every other player is eliminated and the SBA pass promotes the
         .               /// win.
         .               /// CR 704.7 — Lich's Mirror: replace `p`'s loss with a full reset (hand,
         .               /// graveyard and every permanent they own shuffled into their library,
         .               /// draw seven, life becomes 20). Returns true when a replacement applied;
         .               /// a single application covers every loss SBA that fired at once. The
         .               /// Mirror itself is among the shuffled permanents, so it is consumed.
     1,600 ( 0.00%)      pub(crate) fn apply_loss_reset(&mut self, p: usize) -> bool {
         .                   use crate::effect::StaticEffect;
         .                   
       200 ( 0.00%)          let has = self.battlefield.iter().any(|c| {
     8,992 ( 0.00%)              c.controller == p
         .                           && c.definition
         .                               .static_abilities
         .                               .iter()
         .                               .any(|sa| matches!(sa.effect, StaticEffect::ReplaceControllerLossWithReset))
         .                   });
         .                   if !has {
         .                       return false;
         .                   }
-- line 13893 ----------------------------------------
-- line 13919 ----------------------------------------
         .                   self.shuffle_library(p, &mut events);
         .                   for _ in 0..7 {
         .                       self.draw_one(p, &mut events);
         .                   }
         .                   self.players[p].life = 20;
         .                   self.players[p].poison_counters = 0;
         .                   self.commander_damage.retain(|(victim, _), _| *victim != p);
         .                   true
     1,600 ( 0.00%)      }
         .           
       144 ( 0.00%)      pub fn lose_to_empty_draw(&mut self, p: usize) {
        16 ( 0.00%)          let wins = self.battlefield.iter().any(|c| {
     1,248 ( 0.00%)              c.controller == p
         .                           && c.definition.static_abilities.iter().any(|sa| {
         .                               matches!(
         .                                   sa.effect,
         .                                   crate::effect::StaticEffect::WinInsteadOfDrawFromEmpty
         .                               )
         .                           })
         .                   });
         .                   use crate::player::LossCause;
-- line 13939 ----------------------------------------
-- line 13945 ----------------------------------------
         .                           return;
         .                       }
         .                       for idx in 0..self.players.len() {
         .                           if idx != p && !self.player_cant_lose_game(idx) {
         .                               self.players[idx].eliminated = true;
         .                               self.players[idx].loss_cause.get_or_insert(LossCause::Other);
         .                           }
         .                       }
       160 ( 0.00%)          } else if !self.player_cant_lose_game(p) && !self.apply_loss_reset(p) {
     6,800 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::player_cant_lose_game (16x)
     6,304 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::apply_loss_reset (16x)
        32 ( 0.00%)              self.players[p].eliminated = true;
       480 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (16x)
        32 ( 0.00%)              self.players[p].loss_cause.get_or_insert(LossCause::Decked);
       480 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (16x)
         .                   }
       128 ( 0.00%)      }
         .           
         .               /// Draw one card for `p`, first offering the Dredge replacement
         .               /// (CR 702.52). Returns `false` only when the draw couldn't be
         .               /// satisfied (empty library) and no dredge replacement applied — the
         .               /// caller is responsible for the resulting loss SBA. Pushes
         .               /// `CardDrawn` for a normal draw, or `CardMilled` ×N +
         .               /// `CardLeftGraveyard` for a dredge.
         .               /// CR 121.2a — true while `p` controls an *active*
         .               /// `StaticEffect::MayReplaceDrawWithTutor` (Archmage Ascension at six
         .               /// quest counters). Peeled through the gating wrappers by `active_static`.
         .               fn player_may_tutor_instead_of_drawing(&self, p: usize) -> bool {
    47,252 ( 0.00%)          self.battlefield.iter().filter(|c| c.controller == p).any(|c| {
         .                       c.definition.static_abilities.iter().any(|sa| {
         .                           matches!(
         .                               self.active_static(&sa.effect, c),
         .                               Some(crate::effect::StaticEffect::MayReplaceDrawWithTutor)
         .                           )
         .                       })
         .                   })
         .               }
         .           
         .               /// Parallel Thoughts — the id of a permanent `p` controls whose
         .               /// `MayDrawFromSourceExilePile` static offers its exiled pile as a draw.
    19,520 ( 0.00%)      fn source_exile_draw_pile_for(&self, p: usize) -> Option<crate::card::CardId> {
    47,252 ( 0.00%)          self.battlefield.iter().filter(|c| c.controller == p).find_map(|c| {
         .                       c.definition
         .                           .static_abilities
         .                           .iter()
         .                           .any(|sa| {
         .                               matches!(
         .                                   self.active_static(&sa.effect, c),
         .                                   Some(crate::effect::StaticEffect::MayDrawFromSourceExilePile)
         .                               )
         .                           })
         .                           .then_some(c.id)
         .                   })
    19,520 ( 0.00%)      }
         .           
         .               /// `StaticEffect::ControllerMaySkipDraws` (Obstinate Familiar).
         .               fn controller_may_skip_draws(&self, p: usize) -> bool {
    47,252 ( 0.00%)          self.battlefield.iter().filter(|c| c.controller == p).any(|c| {
         .                       c.definition.static_abilities.iter().any(|sa| {
         .                           matches!(
         .                               self.active_static(&sa.effect, c),
         .                               Some(crate::effect::StaticEffect::ControllerMaySkipDraws)
         .                           )
         .                       })
         .                   })
         .               }
         .           
         .               /// `StaticEffect::MayReplaceDrawWithRevealUntilKind` (Abundance).
         .               fn player_may_reveal_until_kind_instead_of_drawing(&self, p: usize) -> bool {
    47,252 ( 0.00%)          self.battlefield.iter().filter(|c| c.controller == p).any(|c| {
         .                       c.definition.static_abilities.iter().any(|sa| {
         .                           matches!(
         .                               self.active_static(&sa.effect, c),
         .                               Some(crate::effect::StaticEffect::MayReplaceDrawWithRevealUntilKind)
         .                           )
         .                       })
         .                   })
         .               }
         .           
         .               /// CR 121.2a — the look-N-keep-one draw replacement `p` controls
         .               /// (Tomorrow, Azami's Familiar), peeled through the gating wrappers.
    17,080 ( 0.00%)      fn look_instead_of_drawing(&self, p: usize) -> Option<u32> {
    47,252 ( 0.00%)          self.battlefield.iter().filter(|c| c.controller == p).find_map(|c| {
         .                       c.definition.static_abilities.iter().find_map(|sa| {
         .                           match self.active_static(&sa.effect, c) {
         .                               Some(crate::effect::StaticEffect::ReplaceDrawWithLookN { count }) => {
         .                                   Some(*count)
         .                               }
         .                               _ => None,
         .                           }
         .                       })
         .                   })
    19,520 ( 0.00%)      }
         .           
         .               /// True while `seat`'s library top is public because a one-shot effect
         .               /// revealed it (Aven Windreader) — the reveal lasts only as long as that
         .               /// card stays on top.
         .               pub(crate) fn library_top_revealed_by_effect(&self, seat: usize) -> bool {
         .                   self.library_tops_revealed.contains(&seat)
         .               }
         .           
         .               /// Test/inspection accessor for `library_top_revealed_by_effect`.
         .               #[doc(hidden)]
         .               pub fn library_top_revealed_by_effect_for_test(&self, seat: usize) -> bool {
         .                   self.library_top_revealed_by_effect(seat)
         .               }
         .           
    31,720 ( 0.00%)      pub fn draw_one(&mut self, p: usize, events: &mut Vec<GameEvent>) -> bool {
         .                   // A revealed top only stays public while it stays on top.
         .                   self.library_tops_revealed.retain(|s| *s != p);
         .                   // CR 121.2b — a per-turn draw cap applies to *individual* card draws,
         .                   // so it gates every draw source, not just `Effect::Draw`'s count.
     7,320 ( 0.00%)          if self.draw_cap_for(p).is_some_and(|cap| self.players[p].cards_drawn_this_turn >= cap) {
   862,736 ( 0.03%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::draw_cap_for (2,440x)
         .                       return false;
         .                   }
         .                   // CR 614 — Possessed Portal: "If a player would draw a card, that
         .                   // player skips that draw instead."
         .                   // CR 614 — Plagiarize: "if that player would draw a card, instead
         .                   // they skip that draw and you draw a card."
         .                   if let Some((_, thief)) =
     2,440 ( 0.00%)              self.draws_redirected_this_turn.iter().find(|(from, _)| *from == p).copied()
         .                       && thief != p
         .                   {
         .                       return self.draw_one(thief, events);
         .                   }
         .                   let global_static = |want: &crate::effect::StaticEffect| {
     7,320 ( 0.00%)              self.battlefield.iter().any(|c| {
         .                           c.definition.static_abilities.iter().any(|sa| sa.effect == *want)
         .                       })
         .                   };
         .                   if global_static(&crate::effect::StaticEffect::PlayersSkipDraws) {
         .                       return false;
         .                   }
         .                   // CR 121.2a — Obstinate Familiar: "you may skip that draw instead."
         .                   // Controller-scoped and optional; the auto policy takes the skip only
-- line 14075 ----------------------------------------
-- line 14128 ----------------------------------------
         .                       self.exile.push(card);
         .                       events.push(GameEvent::PermanentExiled { card_id });
         .                       return true;
         .                   }
         .                   // CR 614 — a queued "the next time you would draw a card this turn,
         .                   // [X] instead" charge (the Words cycle). Spent front-first, before the
         .                   // optional replacements below, since the printed effect is mandatory
         .                   // once the charge is bought.
     7,320 ( 0.00%)          if !self.players[p].next_draw_replacements.is_empty() {
         .                       let (src, body, x) = self.players[p].next_draw_replacements.remove(0);
         .                       // The body picks its own target as it applies (Words of War's
         .                       // "deals 2 damage to any target").
         .                       let target = body
         .                           .requires_target()
         .                           .then(|| self.auto_target_for_effect_avoiding(&body, p, Some(src)))
         .                           .flatten();
         .                       let mut ctx = crate::game::effects::EffectContext::for_ability(src, p, target);
-- line 14144 ----------------------------------------
-- line 14152 ----------------------------------------
         .                       return true;
         .                   }
         .                   // CR 616.1e — several "instead of drawing, dig" replacements can
         .                   // apply to the same draw; the affected player picks which one applies.
         .                   // A declined optional pick drops out and the choice is made again.
         .                   let mut declined: Vec<DrawDig> = Vec::new();
         .                   loop {
         .                       let mut applicable: Vec<DrawDig> = Vec::new();
     7,320 ( 0.00%)              if self.source_exile_draw_pile_for(p).is_some_and(|src| {
   539,046 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::source_exile_draw_pile_for (2,440x)
         .                           self.exile.iter().any(|c| c.exiled_with == Some(src))
         .                       }) {
         .                           applicable.push(DrawDig::ExilePile);
         .                       }
     7,320 ( 0.00%)              if self.look_instead_of_drawing(p).is_some() {
   516,272 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::look_instead_of_drawing (2,440x)
         .                           applicable.push(DrawDig::LookN);
         .                       }
         .                       if self.player_may_tutor_instead_of_drawing(p) {
         .                           applicable.push(DrawDig::Tutor);
         .                       }
         .                       if self.player_may_reveal_until_kind_instead_of_drawing(p) {
         .                           applicable.push(DrawDig::RevealUntilKind);
         .                       }
         .                       applicable.retain(|k| !declined.contains(k));
         .                       let Some(kind) = self.choose_draw_replacement(p, &applicable) else { break };
         .                       if self.apply_draw_dig(kind, p, events) {
         .                           return true;
         .                       }
         .                       declined.push(kind);
     2,440 ( 0.00%)          }
         .                   // CR 121.2a — Chains of Mephistopheles: every draw but the turn-based
         .                   // draw-step draw becomes "discard a card; if you do, draw a card,
         .                   // otherwise mill a card". CR 614.5 — the draw it hands back isn't
         .                   // replaced again.
     8,764 ( 0.00%)          if !self.in_turn_based_draw
         .                       && !self.in_chains_replacement
     2,888 ( 0.00%)              && self.battlefield.iter().any(|c| {
   190,004 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (722x)
         .                           c.definition
         .                               .static_abilities
         .                               .iter()
         .                               .any(|sa| sa.effect == crate::effect::StaticEffect::ChainsOfMephistopheles)
         .                       })
         .                   {
         .                       self.in_chains_replacement = true;
         .                       let pick = self.players[p]
-- line 14195 ----------------------------------------
-- line 14214 ----------------------------------------
         .                               true
         .                           }
         .                       };
         .                       self.in_chains_replacement = false;
         .                       return drew;
         .                   }
         .                   // CR 614 — Notion Thief: redirect an opponent's draw (except the
         .                   // turn-based first draw of their draw step) to the thief's controller.
     9,486 ( 0.00%)          if !self.in_turn_based_draw
         .                       && !self.in_draw_redirect
     5,054 ( 0.00%)              && let Some(thief) = self.notion_thief_for_draw(p)
   505,352 ( 0.02%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::notion_thief_for_draw (722x)
         .                   {
         .                       self.in_draw_redirect = true;
         .                       let drew = self.draw_one(thief, events);
         .                       self.in_draw_redirect = false;
         .                       return drew;
         .                   }
         .                   // CR 121.2a — empty-hand draw replacement (Blood Scrivener). Snapshot
         .                   // the bonus before the draw (the hand must be empty at draw time) and
         .                   // apply the extra draws + life loss after, guarded against recursion.
     9,760 ( 0.00%)          let empty_hand_bonus = (!self.in_draw_double && self.players[p].hand.is_empty())
         .                       .then(|| {
         .                           self.battlefield.iter().find_map(|c| {
    19,206 ( 0.00%)                      (c.controller == p).then(|| {
         .                                   c.definition.static_abilities.iter().find_map(|sa| match &sa.effect {
         .                                       crate::effect::StaticEffect::EmptyHandDrawBonus { extra, life_loss } => {
         .                                           Some((*extra, *life_loss))
         .                                       }
         .                                       _ => None,
         .                                   })
         .                               })?
         .                           })
       722 ( 0.00%)              })
         .                       .flatten();
    14,640 ( 0.00%)          let drew = match self.players[p].draw_top() {
 1,185,847 ( 0.04%)  => /home/user/Crabomination/crabomination/src/player.rs:crabomination::player::PlayerData::draw_top (2,440x)
   302,298 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,440x)
         .                       Some(id) => {
     2,424 ( 0.00%)                  self.cards_drawn_this_resolution += 1;
     9,696 ( 0.00%)                  self.players[p].last_drawn_card = Some(id);
    72,720 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,424x)
    12,120 ( 0.00%)                  events.push(GameEvent::CardDrawn { player: p, card_id: id });
     7,272 ( 0.00%)                  if self.players[p].cards_drawn_this_turn == 1 {
     8,590 ( 0.00%)                      events.push(GameEvent::FirstCardDrawnThisTurn { player: p, card_id: id });
         .                           }
     4,848 ( 0.00%)                  self.maybe_grant_miracle(p, id);
   212,180 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::maybe_grant_miracle (2,424x)
         .                           true
         .                       }
         .                       None => false,
         .                   };
     7,272 ( 0.00%)          if drew && let Some((extra, life_loss)) = empty_hand_bonus {
         .                       self.in_draw_double = true;
         .                       for _ in 0..extra {
         .                           self.draw_one(p, events);
         .                       }
         .                       self.in_draw_double = false;
         .                       if life_loss > 0 {
         .                           let applied = self.adjust_life_applied(p, -(life_loss as i32));
         .                           if applied < 0 {
         .                               events.push(GameEvent::LifeLost { player: p, amount: (-applied) as u32 });
         .                           }
         .                       }
         .                   }
         .                   // CR 121.2a — Breathstealer's Crypt: the drawn card is revealed, and a
         .                   // matching one is discarded unless its drawer pays the toll.
         .                   if drew
     9,696 ( 0.00%)              && let Some((filter, life)) = self.battlefield.iter().find_map(|c| {
   681,656 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (2,424x)
         .                           c.definition.static_abilities.iter().find_map(|sa| match &sa.effect {
         .                               crate::effect::StaticEffect::DrawsRevealedTaxed { filter, life } => {
         .                                   Some((filter.clone(), *life))
         .                               }
         .                               _ => None,
         .                           })
         .                       })
         .                       && let Some(drawn) = self.players[p].last_drawn_card
-- line 14285 ----------------------------------------
-- line 14311 ----------------------------------------
         .                               events.push(GameEvent::CardDiscarded { player: p, card_id: cid });
         .                           }
         .                       }
         .                   }
         .                   // CR 121.2a / 614 — "If you would draw a card, draw two instead"
         .                   // (Thought Reflection). Each doubler applies once per draw event
         .                   // (n doublers: 1 → 2^n); the replacement draws themselves aren't
         .                   // re-doubled (CR 614.5), enforced by the reentrancy flag.
     4,848 ( 0.00%)          if drew && !self.in_draw_double {
     2,424 ( 0.00%)              let doublers = self
         .                           .battlefield
         .                           .iter()
         .                           .filter(|c| {
   132,564 ( 0.00%)                      c.controller == p
         .                                   && c.definition.static_abilities.iter().any(|sa| match &sa.effect {
         .                                       crate::effect::StaticEffect::ControllerDrawsDoubled => true,
         .                                       crate::effect::StaticEffect::ControllerDrawsDoubledIf { condition } => {
         .                                           let ctx = crate::game::effects::EffectContext::for_ability(
         .                                               c.id,
         .                                               c.controller,
         .                                               None,
         .                                           );
         .                                           self.evaluate_predicate(condition, &ctx)
         .                                       }
         .                                       _ => false,
         .                                   })
         .                           })
     2,424 ( 0.00%)                  .count() as u32;
   694,912 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs:<core::iter::adapters::filter::Filter<I,P> as core::iter::traits::iterator::Iterator>::count (2,424x)
     4,848 ( 0.00%)              if doublers > 0 {
         .                           self.in_draw_double = true;
         .                           for _ in 0..(1u32 << doublers.min(8)) - 1 {
         .                               self.draw_one(p, events);
         .                           }
         .                           self.in_draw_double = false;
         .                       }
         .                   }
         .                   drew
    26,840 ( 0.00%)      }
         .           
         .               /// CR 616.1e — ask `p` which applicable draw replacement to apply. `None`
         .               /// when nothing applies; the canonical order stands for a headless seat.
         .               fn choose_draw_replacement(&mut self, p: usize, applicable: &[DrawDig]) -> Option<DrawDig> {
         .                   let first = *applicable.first()?;
         .                   if applicable.len() < 2 || !self.players[p].wants_ui {
         .                       return Some(first);
         .                   }
-- line 14356 ----------------------------------------
-- line 14471 ----------------------------------------
         .                       }
         .                   }
         .               }
         .           
         .               /// CR 614 — the controller of an opponent-of-`p` Notion Thief whose draw
         .               /// redirect (`OpponentExtraDrawsRedirected`) applies to `p`'s draw. Returns
         .               /// the first such controller (multiple redirects are a choice; approximated
         .               /// as first-found).
     4,332 ( 0.00%)      fn notion_thief_for_draw(&self, p: usize) -> Option<usize> {
         .                   use crate::effect::StaticEffect;
     1,444 ( 0.00%)          let opps = self.opponents_of(p);
   193,088 ( 0.01%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::opponents_of (722x)
         .                   self.battlefield
         .                       .iter()
         .                       .find(|c| {
         .                           opps.contains(&c.controller)
         .                               && c.definition.static_abilities.iter().any(|sa| {
         .                                   matches!(sa.effect, StaticEffect::OpponentExtraDrawsRedirected)
         .                               })
         .                       })
         .                       .map(|c| c.controller)
     4,332 ( 0.00%)      }
         .           
         .               /// CR 702.94 — Miracle. If `card_id` was the first card `p` drew this
         .               /// turn and it has a printed miracle cost, grant the miracle alt-cost
         .               /// until end of turn (the owner may then cast it for that cost via
         .               /// `GameAction::CastFromZoneWithoutPaying`). The reveal is treated as
         .               /// automatic — the grant only adds a cheaper *option*, so revealing is
         .               /// never a downside for the engine; a human simply declines to cast.
         .               /// Clear every step-bounded (`MayPlayDuration::EndOfThisStep`) miracle
         .               /// window across all zones. Called at each step transition — the reveal
         .               /// offer can't be banked for a later step. The granted alt-cost shares
         .               /// the permission's lifetime.
         .               pub(crate) fn clear_step_bounded_may_play(&mut self) {
         .                   let clear = |c: &mut crate::card::CardInstance| {
 1,638,800 ( 0.05%)              if matches!(
 1,638,800 ( 0.05%)                  c.may_play_until,
         .                           Some(p) if p.duration == crate::card::MayPlayDuration::EndOfThisStep
         .                       ) {
         .                           c.may_play_until = None;
         .                           c.granted_alt_cast_cost_eot = None;
         .                       }
         .                   };
         .                   for pl in &mut self.players {
   102,176 ( 0.00%)              pl.hand.iter_mut().for_each(clear);
61,299,016 ( 1.97%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (44,216x)
   102,176 ( 0.00%)              pl.graveyard.iter_mut().for_each(clear);
 1,326,480 ( 0.04%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (44,216x)
   102,176 ( 0.00%)              pl.library.iter_mut().for_each(clear);
 1,326,480 ( 0.04%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (44,216x)
         .                   }
    25,544 ( 0.00%)          self.exile.iter_mut().for_each(clear);
         .               }
         .           
    19,392 ( 0.00%)      pub(crate) fn maybe_grant_miracle(&mut self, p: usize, card_id: CardId) {
     7,272 ( 0.00%)          if self.players[p].cards_drawn_this_turn != 1 {
         .                       return;
         .                   }
     3,436 ( 0.00%)          if let Some(card) = self.players[p].hand.iter_mut().find(|c| c.id == card_id)
    51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
         .                       && let Some(cost) = card.definition.miracle.clone()
         .                   {
         .                       card.may_play_until = Some(crate::card::MayPlayPermission {
         .                           player: p,
         .                           granted_turn: self.turn_number,
         .                           // CR 702.94 — the window is the reveal offer, not the whole
         .                           // turn: cleared at the next step transition.
         .                           duration: crate::card::MayPlayDuration::EndOfThisStep,
         .                           exile_after: false,
         .                           miracle: true,
         .                       });
         .                       card.granted_alt_cast_cost_eot = Some(cost);
         .                   }
    19,392 ( 0.00%)      }
         .           
         .               /// CR 702.52 — Dredge. If `p` has a card with `Keyword::Dredge(n)` in
         .               /// their graveyard and at least `n` cards in their library, the player
         .               /// may replace a draw by milling `n` cards and returning the dredge
         .               /// card to hand instead. Returns `true` when a dredge replacement was
         .               /// applied (caller skips the normal draw). The decision is surfaced as
         .               /// an `OptionalTrigger`, so the `AutoDecider` declines by default and
         .               /// ordinary games keep drawing normally.
-- line 14547 ----------------------------------------
-- line 14549 ----------------------------------------
         .                   &mut self,
         .                   p: usize,
         .                   events: &mut Vec<GameEvent>,
         .               ) -> bool {
         .                   use crate::card::Keyword;
         .                   use crate::decision::{Decision, DecisionAnswer};
         .                   // First dredge card in the graveyard whose count the library can
         .                   // satisfy (CR 702.52a — you can't dredge with fewer than N cards).
     9,118 ( 0.00%)          let cand = self.players[p].graveyard.iter().find_map(|c| {
    27,482 ( 0.00%)              c.definition.keywords.iter().find_map(|kw| match kw {
         .                           Keyword::Dredge(n) if self.players[p].library.len() >= *n as usize => {
         .                               Some((c.id, *n))
         .                           }
         .                           _ => None,
         .                       })
         .                   });
         .                   let Some((card_id, n)) = cand else { return false; };
         .                   // AutoDecider's blanket "no" made Dredge fully inert (this ask sits
-- line 14566 ----------------------------------------
-- line 15180 ----------------------------------------
         .               /// Everything else (TurnBegins, CardDrawn, LandPlayed, LifeGained, etc.)
         .               /// gains trigger capability through this path.
         .               /// CR 702.95 — when `entered` (a creature) comes onto the battlefield,
         .               /// pair it with an eligible unpaired creature its controller controls. The
         .               /// "may" is auto-resolved (pairing is value-positive); the partner with
         .               /// the lowest CardId is chosen for determinism. A Soulbond creature can
         .               /// initiate the pair; a non-Soulbond creature only pairs if its controller
         .               /// already has an unpaired Soulbond creature waiting.
    35,136 ( 0.00%)      pub fn apply_soulbond_pairing(&mut self, entered: CardId) {
         .                   use crate::card::Keyword;
         .                   let Some(card) = self.battlefield_find(entered) else { return };
    15,600 ( 0.00%)          if !card.definition.is_creature() || card.soulbond_partner.is_some() {
    56,632 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,904x)
         .                       return;
         .                   }
     3,888 ( 0.00%)          let controller = card.controller;
         .                   let entered_has_soulbond = card.definition.keywords.contains(&Keyword::Soulbond);
         .                   let partner = self
         .                       .battlefield
         .                       .iter()
         .                       .filter(|c| {
    30,978 ( 0.00%)                  c.id != entered
    87,102 ( 0.00%)                      && c.controller == controller
    42,720 ( 0.00%)                      && c.definition.is_creature()
   220,352 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (14,240x)
     4,992 ( 0.00%)                      && c.soulbond_partner.is_none()
     4,992 ( 0.00%)                      && (entered_has_soulbond
         .                                   || c.definition.keywords.contains(&Keyword::Soulbond))
         .                       })
         .                       .map(|c| c.id)
         .                       .min_by_key(|id| id.0);
         .                   let Some(p) = partner else { return };
         .                   if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == entered) {
         .                       c.soulbond_partner = Some(p);
         .                   }
         .                   if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == p) {
         .                       c.soulbond_partner = Some(entered);
         .                   }
    31,232 ( 0.00%)      }
         .           
         .               /// The `delayed_triggers` watchers an event batch can fire: the
         .               /// event-keyed "when [card] dies this turn" set, Tamiyo's
         .               /// "whenever a creature attacks you", and the two turn-scoped
         .               /// CR 603.4 watchers (First Day of Class, Waltz of Rage).
         .               ///
         .               /// Split out of [`dispatch_triggers_for_events`] so one `is_empty()`
         .               /// skips the whole block — five event scans and their `collect`s —
-- line 15224 ----------------------------------------
-- line 15457 ----------------------------------------
         .                                           .build(),
         .                                   );
         .                               }
         .                           }
         .                       }
         .                   }
         .               }
         .           
   880,420 ( 0.03%)      pub fn dispatch_triggers_for_events(&mut self, events: &[GameEvent]) {
         .                   // Cost-payment events (paid life) queued since the last dispatch —
         .                   // fold them in so resumed-decision paths that bypass
         .                   // `perform_action`'s drain still fire their triggers.
   176,084 ( 0.01%)          if !self.pending_cost_events.is_empty() {
         .                       let pending = std::mem::take(&mut self.pending_cost_events);
         .                       self.dispatch_triggers_for_events(&pending);
         .                   }
         .                   // CR 700.4 — fold in `PermanentDied` events synthesized from the deaths
         .                   // recorded at the raw removal chokepoint since the last dispatch, so
         .                   // non-creature deaths (which emit no `CreatureDied`) still reach
         .                   // "creature or artifact you control dies" triggers.
         .                   let deaths = std::mem::take(&mut self.pending_permanent_deaths);
         .                   // Replay the deaths' causing spell/ability over the dispatch: the
         .                   // batch is synthesized after the resolution that killed them ended.
         .                   if let Some(causer) = deaths.iter().find_map(|(.., causer)| *causer) {
       640 ( 0.00%)              self.resolution_causer = Some(causer);
         .                   }
         .                   // CR 800.4 — control changes recorded at the `change_control`
         .                   // chokepoint since the last dispatch (Risky Move's hand-off).
         .                   let control_changes = std::mem::take(&mut self.pending_control_changes);
         .                   let synthesized: Vec<GameEvent> = deaths
         .                       .into_iter()
         .                       // CR 700.4 — a death redirected away from the graveyard (Rest in
         .                       // Peace, void counters, Kalitas, Pulmonic Sliver) never happened;
-- line 15489 ----------------------------------------
-- line 15497 ----------------------------------------
         .                       })
         .                       .chain(
         .                           control_changes
         .                               .into_iter()
         .                               .map(|(card_id, from, to)| GameEvent::ControlChanged { card_id, from, to }),
         .                       )
         .                       .collect();
         .                   let folded: Vec<GameEvent>;
   264,126 ( 0.01%)          let events: &[GameEvent] = if synthesized.is_empty() {
         .                       events
         .                   } else {
     8,460 ( 0.00%)              folded = events.iter().cloned().chain(synthesized).collect();
         .                       &folded
         .                   };
         .                   // "Whenever one or more cards leave your graveyard" fires once per
         .                   // simultaneous batch (every catalog `CardLeftGraveyard` listener is
         .                   // of that shape). The emission sites push one event per card — the
         .                   // per-turn tally and the client event mirror want the per-card
         .                   // granularity — so collapse to the first event per player here, at
         .                   // trigger dispatch only.
         .                   let gy_batched: Vec<GameEvent>;
   156,996 ( 0.01%)          let events: &[GameEvent] = if events
         .                       .iter()
   199,320 ( 0.01%)              .filter(|e| matches!(e, GameEvent::CardLeftGraveyard { .. }))
         .                       .count()
         .                       > 1
         .                   {
         .                       let mut seen_players: Vec<usize> = Vec::new();
         .                       gy_batched = events
         .                           .iter()
         .                           .filter(|e| match e {
         .                               GameEvent::CardLeftGraveyard { player, .. } => {
-- line 15528 ----------------------------------------
-- line 15546 ----------------------------------------
         .                   }
         .                   // CR 702.95 — Soulbond pairing. When a creature enters, attempt to pair
         .                   // it (auto-resolved "may"). Done before trigger dispatch so a paired
         .                   // creature's bonus is live for any subsequent ETB-trigger evaluation.
         .                   // CR 603.4 — stamp the entry turn on every permanent that entered in
         .                   // this batch, so `SelectionRequirement::EnteredThisTurn` (Shaile) can
         .                   // compare against the current turn. Centralized here because every
         .                   // battlefield-entry path emits a `PermanentEntered` event.
    52,332 ( 0.00%)          let turn = self.turn_number;
         .                   for e in events {
 1,138,642 ( 0.04%)              match e {
         .                           GameEvent::PermanentEntered { card_id } => {
         .                               // CR 613.7d — the new object's timestamp is its entry
         .                               // time, drawn from the same counter as resolved-effect
         .                               // timestamps so static-vs-spell ordering is coherent.
         .                               let ts = self.next_timestamp();
         .                               let mut face_down_ctrl = None;
         .                               let mut pw_ctrl = None;
         .                               let mut acted = None;
         .                               let mut land_ctrl = None;
     3,904 ( 0.00%)                      if let Some(c) = self.battlefield_find_mut(*card_id) {
    11,712 ( 0.00%)                          c.entered_turn = Some(turn);
     3,904 ( 0.00%)                          c.battlefield_timestamp = ts;
    15,616 ( 0.00%)                          if c.face_down {
         .                                       face_down_ctrl = Some(c.controller);
         .                                   }
    11,712 ( 0.00%)                          if c.definition.is_planeswalker() {
    62,464 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (3,904x)
         .                                       pw_ctrl = Some(c.controller);
         .                                   }
    11,712 ( 0.00%)                          if !c.is_token {
     3,904 ( 0.00%)                              acted = Some(c.controller);
         .                                   }
    19,520 ( 0.00%)                          if c.definition.is_land() {
    57,274 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (3,904x)
     1,730 ( 0.00%)                              land_ctrl = Some(c.controller);
         .                                   }
         .                               }
         .                               // Arboria — "put a nontoken permanent onto the battlefield
         .                               // during their last turn".
    15,616 ( 0.00%)                      if let Some(p) = acted {
         .                                   self.note_acted_on_own_turn(p);
         .                               }
         .                               // Land Equilibrium — the entering land brings a sacrifice
         .                               // with it (CR 614, so ahead of trigger dispatch).
     7,808 ( 0.00%)                      if let Some(p) = land_ctrl {
         .                                   self.apply_land_equilibrium(p);
         .                               }
         .                               // "If a planeswalker entered under your control this turn"
         .                               // (Oath of Chandra) — counted on the one path every entry
         .                               // takes.
    11,712 ( 0.00%)                      if let Some(p) = pw_ctrl {
         .                                   self.players[p].planeswalkers_entered_this_turn += 1;
         .                               }
         .                               // CR 708 — track "a permanent entered face down under your
         .                               // control this turn" (Oblivious Bookworm).
     7,808 ( 0.00%)                      if let Some(p) = face_down_ctrl {
         .                                   self.players[p].face_down_activity_this_turn = true;
         .                               }
     7,808 ( 0.00%)                      self.apply_soulbond_pairing(*card_id);
 1,326,836 ( 0.04%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::apply_soulbond_pairing (3,904x)
         .                           }
         .                           // CR 613.7e/f/g — attach, turn face up, and transform each
         .                           // give the object a new timestamp.
         .                           GameEvent::AttachmentMoved { attachment, attached_to: Some(_) } => {
         .                               let ts = self.next_timestamp();
         .                               if let Some(c) = self.battlefield_find_mut(*attachment) {
         .                                   c.battlefield_timestamp = ts;
         .                               }
-- line 15611 ----------------------------------------
-- line 15644 ----------------------------------------
         .                               }
         .                           }
         .                           _ => {}
         .                       }
         .                   }
         .                   // Every block in `fire_delayed_event_watchers` fires a
         .                   // `delayed_triggers` entry, so with none registered — nearly every
         .                   // dispatch — the whole thing is dead. Ask once.
   104,664 ( 0.00%)          if !self.delayed_triggers.is_empty() {
         .                       self.fire_delayed_event_watchers(events);
         .                   }
         .                   // Phase 1: collect candidate triggers while the borrow on
         .                   // `self.battlefield` is shared. Phase 2 will mutate `self.stack`
         .                   // and call `&self.evaluate_predicate` to gate each candidate by
         .                   // the optional `EventSpec::filter`.
         .                   let mut candidates: Vec<TriggerCandidate> = Vec::new();
         .                   // One battlefield pass for the four board-level facts below, instead
-- line 15660 ----------------------------------------
-- line 15661 ----------------------------------------
         .                   // of one walk each. Hushbringer (CR 614) suppression of reaction
         .                   // creature-death triggers ("whenever a creature dies") while a
         .                   // `SuppressCreatureEtbTriggers { also_dies }` static is in play
         .                   // (self-death + SBA paths gate separately in `stack.rs`); the
         .                   // ability-strip presence gate; and both grant lists, peeled and
         .                   // resolved once for the whole batch instead of once per battlefield
         .                   // permanent (the per-card form was O(cards x sources) with an
         .                   // allocation per pair).
   104,664 ( 0.00%)          let scan = self.dispatch_board_scan();
23,534,274 ( 0.76%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_board_scan (52,332x)
   104,664 ( 0.00%)          let dies_suppressed = scan.dies_suppressed;
         .                   // Which permanents have lost their abilities (Turn to Frog,
         .                   // Mercurial Transformation, Lignify) — printed triggered abilities
         .                   // are skipped while a strip-abilities effect is in scope per CR
         .                   // 113.10b. Empty (and free) on a board with no such effect.
   209,328 ( 0.01%)          let stripped_ids = self.permanents_with_abilities_removed(scan.strip_on_battlefield);
 6,227,508 ( 0.20%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::permanents_with_abilities_removed (52,332x)
   261,660 ( 0.01%)          let trigger_grants = scan.trigger_grants;
   313,992 ( 0.01%)          let equip_grants = scan.equip_grants;
         .                   // CR 603.3d — keys for `once_per_turn` triggers that fire in this
         .                   // batch; merged into the turn-scoped set after the battlefield walk
         .                   // (deferred so we don't mutate `self` mid-immutable-borrow).
         .                   let mut once_fired_this_batch: crate::fxhash::HashSet<(CardId, usize)> =
         .                       crate::fxhash::HashSet::default();
         .                   // `EventSpec::per_subject_cap` fires in this batch (deferred merge).
         .                   let mut capped_fired_this_batch: Vec<(CardId, CardId)> = Vec::new();
         .                   // Presence gates for the three per-card grant lookups below. On a
         .                   // board that grants nothing each returns empty for every permanent,
         .                   // and the calls were 36 / 4 / 1 Ir of pure overhead — paid 945,812
         .                   // times each over six bench games. The station leg of
         .                   // `statics_granted_triggers_with` is per-card, so it stays per-card.
   104,664 ( 0.00%)          let any_static_grant = !trigger_grants.is_empty() || !self.turn_granted_triggers.is_empty();
         .                   let any_own_grant = !self.granted_triggers_eot.is_empty();
         .                   let any_equip_grant = !equip_grants.is_empty();
         .                   for card in &self.battlefield {
         .                       let stripped = stripped_ids.contains(&card.id);
         .                       // Walk printed triggered abilities AND any transient
         .                       // granted_triggers_eot for this permanent (Root Manipulation,
         .                       // Rabid Attack-style "creatures gain '…trigger…' EOT"). Printed
         .                       // triggers carry their definition index so `once_per_turn`
         .                       // (CR 603.3d) can be tracked per (source, index); granted
         .                       // triggers are never once-per-turn and use a sentinel index.
         .                       let n_printed = card.definition.triggered_abilities.len();
 3,783,248 ( 0.12%)              let static_granted = if any_static_grant || !card.definition.station.is_empty() {
         .                           self.statics_granted_triggers_with(card, &trigger_grants)
         .                       } else {
         .                           Vec::new()
         .                       };
         .                       let own_granted: &[crate::card::TriggeredAbility] =
   945,812 ( 0.03%)                  if any_own_grant { self.granted_triggers(card.id) } else { &[] };
         .                       // Equipment/Aura-granted triggers belong to the *attachment*, not
         .                       // the host, so they fire even when the host lost all abilities
         .                       // (Contaminated Ground's tap trigger on a land it turned into a
         .                       // Swamp — CR 613; the type change strips the land, not the Aura).
   945,812 ( 0.03%)              let equip_granted = if any_equip_grant {
   214,068 ( 0.01%)                  self.equip_granted_triggers_with(card, &equip_grants)
         .                       } else {
         .                           Vec::new()
         .                       };
         .                       // A host that lost all abilities (Turn to Frog, "is a Swamp")
         .                       // contributes none of its *own* triggers, but still carries its
         .                       // attachments' equip-granted ones.
         .                       let mut all_triggers: Vec<(usize, &crate::card::TriggeredAbility)> = Vec::new();
   945,812 ( 0.03%)              if !stripped {
    33,980 ( 0.00%)                  all_triggers.extend(card.definition.triggered_abilities.iter().enumerate());
         .                           all_triggers.extend(own_granted.iter().map(|t| (usize::MAX, t)));
         .                           all_triggers.extend(static_granted.iter().map(|t| (usize::MAX, t)));
         .                       }
         .                       all_triggers.extend(equip_granted.iter().map(|t| (usize::MAX, t)));
         .                       for (trig_idx, ta) in all_triggers {
         .                           // A `FromYourGraveyard`-scoped trigger functions ONLY while
         .                           // its card is in a graveyard (CR 603.3d zone-scoping —
         .                           // Bloodghast, Voidwing Hybrid); the graveyard walk below
         .                           // gathers those. Skip them on the battlefield.
    67,960 ( 0.00%)                  if matches!(ta.event.scope, crate::effect::EventScope::FromYourGraveyard) {
         .                               continue;
         .                           }
         .                           // CR 603.3d — "triggers only once each turn": skip if it has
         .                           // already fired this turn or earlier in this same batch.
    33,980 ( 0.00%)                  let once_key = (card.id, trig_idx);
   203,880 ( 0.01%)                  if ta.event.once_per_turn
         .                               && trig_idx < n_printed
         .                               && (self.triggered_once_per_turn_used.contains(&once_key)
         .                                   || once_fired_this_batch.contains(&once_key))
         .                           {
         .                               continue;
         .                           }
         .                           // For batch-fanout-friendly event kinds (Attacks,
         .                           // CreatureDied, CardDrawn, CardDiscarded, CardLeftGraveyard,
         .                           // CounterAdded, BlockerDeclared, AttackerWentUnblocked,
         .                           // CardMilled, LifeGained, LifeLost) the trigger fires
         .                           // ONCE PER MATCHING EVENT — CR 603.6 "whenever X happens"
         .                           // fan-out. For other event kinds (ETB, StepBegins, …) we
         .                           // fire at most once per (source, trigger) pair because
         .                           // they don't naturally produce duplicate events in a
         .                           // single batch.
   200,558 ( 0.01%)                  let fanout = matches!(
    67,960 ( 0.00%)                      ta.event.kind,
         .                               crate::effect::EventKind::Attacks
         .                                   | crate::effect::EventKind::CreatureDied
         .                                   | crate::effect::EventKind::CreatureOrArtifactDied
         .                                   | crate::effect::EventKind::PermanentDied
         .                                   | crate::effect::EventKind::CreatureSacrificed
         .                                   | crate::effect::EventKind::PermanentSacrificed
         .                                   | crate::effect::EventKind::PermanentLeavesBattlefield
         .                                   | crate::effect::EventKind::CardDrawn
-- line 15764 ----------------------------------------
-- line 15794 ----------------------------------------
         .                           // attackers, or one attacker under several blockers). The
         .                           // batch carries one `BlockerDeclared` per pair, so fan out but
         .                           // dedupe on the trigger's own side of the pair. Per-object
         .                           // wordings (509.3b/d — "blocks *a creature*") read the whole
         .                           // partner set via `Selector::BlockedAttacker` /
         .                           // `BlockingCreatures`, so one trigger instance still covers all.
         .                           let mut block_sides_seen: Vec<CardId> = Vec::new();
         .                           for ev in events {
   284,502 ( 0.01%)                      if is_event_hardcoded(ev, &ta.event) {
         .                                   continue;
         .                               }
   127,524 ( 0.00%)                      if dies_suppressed && matches!(ev, GameEvent::CreatureDied { .. }) {
         .                                   continue;
         .                               }
         .                               // CR 700.4 — a creature whose death-placement was
         .                               // redirected away from the graveyard (Rest in Peace, void
         .                               // counters, Kalitas exile; Pulmonic Sliver library-top)
         .                               // never died; "whenever a creature dies" watchers don't
         .                               // fire. The redirected card sits in exile or a library at
         .                               // dispatch time.
   127,524 ( 0.00%)                      if let GameEvent::CreatureDied { card_id } = ev
        84 ( 0.00%)                          && self.death_was_replaced(*card_id)
         .                               {
         .                                   continue;
         .                               }
   383,076 ( 0.01%)                      if crate::game::effects::event_matches_spec(self, ev, &ta.event, card) {
 1,915,380 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/effects/events.rs:crabomination::game::effects::events::event_matches_spec (63,846x)
         .                                   let subject = crate::game::effects::event_subject(ev, &ta.event.kind);
         .                                   if matches!(
         .                                       ta.event.kind,
         .                                       crate::effect::EventKind::Blocks
         .                                           | crate::effect::EventKind::BecomesBlocked
         .                                           | crate::effect::EventKind::BlocksNOrMore(_)
         .                                           | crate::effect::EventKind::BecomesBlockedByNOrMore(_)
         .                                   ) && let Some(crate::game::effects::EntityRef::Permanent(sid)) = subject
-- line 15827 ----------------------------------------
-- line 15871 ----------------------------------------
         .                                           bargained: false,
         .                                           cast_via_mayhem: false,
         .                                           cast_via_waterbend: false,
         .                                           cast_collected_evidence: false,
         .                                           entwined: false,
         .                                           spree_modes: Vec::new(),
         .                                       };
         .                                       if !self.evaluate_predicate(filter, &ctx) {
    16,312 ( 0.00%)                                  if !fanout {
         .                                               break;
         .                                           }
         .                                           continue;
         .                                       }
         .                                   }
         .                                   // Per-subject cap ("triggers only twice each turn"
         .                                   // counted per creature — Nadu). Deferred bump (the
         .                                   // battlefield is immutably borrowed here).
-- line 15887 ----------------------------------------
-- line 15936 ----------------------------------------
         .                                   }
         .                               }
         .                           }
         .                       }
         .                   }
         .                   for key in once_fired_this_batch.drain() {
         .                       self.triggered_once_per_turn_used.insert(key);
         .                   }
   261,660 ( 0.01%)          for key in capped_fired_this_batch {
         .                       *self.per_subject_trigger_uses.entry(key).or_insert(0) += 1;
         .                   }
         .                   // CR 702.130a / 603.10a — Enrage on lethal damage. A creature that
         .                   // dies from the same damage that would trigger its "whenever this is
         .                   // dealt damage" ability still triggers (the ability uses last-known
         .                   // information). Such a creature is no longer on the battlefield by
         .                   // dispatch time, so walk the just-died snapshots for SelfSource
         .                   // `DealtDamage` triggers matching a `DamageDealt` event in this batch.
         .                   // (Other SelfSource trigger kinds — die/leave — are handled via their
         .                   // own dedicated paths, so this is scoped to DealtDamage only.)
         .                   //
         .                   // The walk order decides where these land on the stack relative to
         .                   // each other, so `died_card_snapshots` is an insertion-ordered
         .                   // [`IdMap`](crate::game::types::IdMap), not a `HashMap` — see its doc.
    52,332 ( 0.00%)          for snap in self.died_card_snapshots.values() {
         .                       // CR 603.10a — a leaves-the-battlefield ability granted by a static
         .                       // (Endless Whispers' "each creature has 'when this dies …'") looks
         .                       // back in time, so gather the grants against the death snapshot.
         .                       let granted = self.statics_granted_dying_triggers(snap);
         .                       let all = snap
         .                           .definition
         .                           .triggered_abilities
         .                           .iter()
         .                           .map(|t| (t, false))
         .                           .chain(granted.iter().map(|t| (t, true)));
         .                       for (ta, is_granted) in all {
         .                           // SelfSource `DealtDamage` (Enrage on lethal damage) and
         .                           // `PermanentSacrificed`/`CreatureSacrificed` ("when you
         .                           // sacrifice this") all fire from LKI — the source has left
         .                           // the battlefield by dispatch.
       334 ( 0.00%)                  let lki_self = matches!(
       668 ( 0.00%)                      ta.event.kind,
         .                               crate::effect::EventKind::DealtDamage
         .                                   | crate::effect::EventKind::PermanentSacrificed
         .                                   | crate::effect::EventKind::CreatureSacrificed
         .                           ) && ta.event.scope == crate::effect::EventScope::SelfSource
         .                               // A *granted* "when this dies" also needs the LKI path; the
         .                               // printed one already fires through the battlefield walk.
       668 ( 0.00%)                      || is_granted
         .                                   && matches!(
         .                                       ta.event.kind,
         .                                       crate::effect::EventKind::CreatureDied
         .                                           | crate::effect::EventKind::PermanentDied
         .                                   )
         .                                   && ta.event.scope == crate::effect::EventScope::SelfSource;
         .                           // "When enchanted permanent dies or is exiled" on a leaving Aura
         .                           // (Minion's Return, Kaya's Ghostform) — the snapshot is the
         .                           // orphaned Aura, scope keys on the departed host recorded in
         .                           // `auras_at_death`.
         .                           // `DealtDamage` joins them for the lethal case: the host died
         .                           // to the very damage that triggered (CR 603.2 — Soul Link).
     1,406 ( 0.00%)                  let lki_enchanted = matches!(
         .                               ta.event.kind,
         .                               crate::effect::EventKind::CreatureDied
         .                                   | crate::effect::EventKind::PermanentDied
         .                                   | crate::effect::EventKind::PermanentLeavesBattlefield
         .                                   | crate::effect::EventKind::CardExiled
         .                                   | crate::effect::EventKind::DealtDamage
         .                           ) && ta.event.scope == crate::effect::EventScope::EnchantedBySource;
     1,064 ( 0.00%)                  if !(lki_self || lki_enchanted) {
         .                               continue;
         .                           }
         .                           for ev in events {
         .                               if crate::game::effects::event_matches_spec(self, ev, &ta.event, snap) {
         .                                   candidates.push(TriggerCandidate {
         .                                       from_mana_ability: false,
         .                                       actor: None,
         .                                       source: snap.id,
-- line 16012 ----------------------------------------
-- line 16025 ----------------------------------------
         .                   }
         .                   // "When this permanent is put into exile from the battlefield" — a
         .                   // SelfSource `CardExiled` trigger fires from LKI: the source sits in
         .                   // the exile zone by dispatch time (God-Eternal Kefnet/Bontu's recur).
         .                   // `event_matches_spec`'s SelfSource id-check confines this to the
         .                   // card actually exiled in this batch, so already-exiled cards don't
         .                   // re-fire.
         .                   for card in &self.exile {
    66,880 ( 0.00%)              for ta in &card.definition.triggered_abilities {
         .                           if ta.event.kind != crate::effect::EventKind::CardExiled
         .                               || ta.event.scope != crate::effect::EventScope::SelfSource
         .                           {
         .                               continue;
         .                           }
         .                           for ev in events {
         .                               if crate::game::effects::event_matches_spec(self, ev, &ta.event, card) {
         .                                   candidates.push(TriggerCandidate {
-- line 16041 ----------------------------------------
-- line 16058 ----------------------------------------
         .                   // Also walk every player's graveyard for triggers scoped
         .                   // `FromYourGraveyard` — recursion creatures (Bloodghast,
         .                   // Ichorid, Silversmote Ghoul) fire from there. The trigger's
         .                   // effective controller is the card's owner. Per CR 702.29c,
         .                   // SelfSource cycle triggers ("When you cycle this card") also
         .                   // fire here — the cycled card is in graveyard at dispatch
         .                   // time, and the trigger's source matches the cycled card by id.
         .                   for player in &self.players {
   289,776 ( 0.01%)              for card in &player.graveyard {
         .                           for ta in &card.definition.triggered_abilities {
     9,500 ( 0.00%)                      let from_gy_scope = matches!(
    19,000 ( 0.00%)                          ta.event.scope,
         .                                   crate::effect::EventScope::FromYourGraveyard
         .                               );
    19,000 ( 0.00%)                      let self_scope = matches!(
         .                                   ta.event.scope,
         .                                   crate::effect::EventScope::SelfSource
         .                               );
         .                               // CR 702.29c cycle triggers and "when this card is
         .                               // milled" triggers both fire off the card in the
         .                               // graveyard.
         .                               let cycle_self = matches!(
     9,500 ( 0.00%)                          ta.event.kind,
         .                                   crate::effect::EventKind::CardCycled
         .                               ) && self_scope;
         .                               let milled_self = matches!(
         .                                   ta.event.kind,
         .                                   crate::effect::EventKind::CardMilled
         .                               ) && self_scope;
         .                               // "When … causes you to discard this card" — the card is
         .                               // already in the graveyard at dispatch (Pure Intentions).
    19,000 ( 0.00%)                      let discarded_self = matches!(
         .                                   ta.event.kind,
         .                                   crate::effect::EventKind::CardDiscarded
         .                               ) && self_scope;
         .                               // "When this is put into a graveyard from anywhere"
         .                               // (Emrakul) — also fires off the card in the graveyard.
    19,000 ( 0.00%)                      let putgy_self = matches!(
         .                                   ta.event.kind,
         .                                   crate::effect::EventKind::PutIntoGraveyard
         .                               ) && self_scope;
   104,500 ( 0.00%)                      if !from_gy_scope
         .                                   && !cycle_self
         .                                   && !milled_self
         .                                   && !putgy_self
         .                                   && !discarded_self
         .                               {
         .                                   continue;
         .                               }
         .                               for ev in events {
-- line 16107 ----------------------------------------
-- line 16135 ----------------------------------------
         .                                   }
         .                               }
         .                           }
         .                       }
         .                   }
         .                   // CR 902.5 / 901.7 — a Vanguard avatar's and a face-up plane's triggers
         .                   // fire from the command zone. A plane's controller is the planar
         .                   // controller (CR 901.6), not its owner.
   156,996 ( 0.01%)          let planar_controller = self.planar_controller();
 4,238,892 ( 0.14%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::planar_controller (52,332x)
         .                   for player in &self.players {
   209,328 ( 0.01%)              for card in player.command.iter().filter(|c| c.command_zone_abilities_active()) {
         .                           let controller =
         .                               if card.definition.is_plane() { planar_controller } else { card.owner };
         .                           for ta in &card.definition.triggered_abilities {
         .                               for ev in events {
         .                                   if is_event_hardcoded(ev, &ta.event) {
         .                                       continue;
         .                                   }
         .                                   if crate::game::effects::event_matches_spec(self, ev, &ta.event, card) {
-- line 16153 ----------------------------------------
-- line 16174 ----------------------------------------
         .                           }
         .                       }
         .                   }
         .                   // Walk every player's hand for `PutIntoHandFromGraveyard` SelfSource
         .                   // triggers: the card has already been moved to hand by the time the
         .                   // event dispatches, so it fires from there (Golgari Brownscale's "gain
         .                   // 2 life when this is put into your hand from your graveyard").
         .                   for player in &self.players {
   253,120 ( 0.01%)              for card in &player.hand {
         .                           for ta in &card.definition.triggered_abilities {
     9,352 ( 0.00%)                      if !matches!(ta.event.kind, crate::effect::EventKind::PutIntoHandFromGraveyard)
         .                                   || !matches!(ta.event.scope, crate::effect::EventScope::SelfSource)
         .                               {
         .                                   continue;
         .                               }
         .                               for ev in events {
         .                                   if is_event_hardcoded(ev, &ta.event) {
         .                                       continue;
         .                                   }
-- line 16192 ----------------------------------------
-- line 16214 ----------------------------------------
         .                   // triggered abilities that fire from the command zone alongside
         .                   // battlefield permanents. Event-keyed emblem triggers are handled
         .                   // here (step-keyed ones — "at the beginning of your upkeep" — fire
         .                   // in `fire_step_triggers`). `event_amount` carries the magnitude
         .                   // through to the body via `Value::TriggerEventAmount`. Professor
         .                   // Dellian Fel's -6 emblem ("Whenever you gain life, each opponent
         .                   // loses that much life") rides this path.
         .                   for seat_idx in 0..self.players.len() {
   104,664 ( 0.00%)              for em_idx in 0..self.players[seat_idx].emblems.len() {
         .                           let triggers = self.players[seat_idx].emblems[em_idx].triggered.clone();
         .                           for ta in &triggers {
         .                               if matches!(
         .                                   ta.event.kind,
         .                                   crate::effect::EventKind::StepBegins(_) | crate::effect::EventKind::TurnBegins
         .                               ) {
         .                                   continue;
         .                               }
-- line 16230 ----------------------------------------
-- line 16257 ----------------------------------------
         .                   // observable wrong orderings the moment more than one player
         .                   // controlled a triggering permanent (acute for 4-player FFA, 2HG,
         .                   // and Commander — invisible in 1v1 where there's only one
         .                   // non-active player). Within a player's group we keep the
         .                   // gathered order: stable sort means each player's
         .                   // battlefield-iteration order is preserved as their chosen
         .                   // order — fine for AutoDecider; a real UI player would pick.
         .                   let n_players = self.players.len();
   104,664 ( 0.00%)          let active = self.active_player_idx;
   261,660 ( 0.01%)          let apnap_rank = |seat: usize| -> usize {
         .                       if seat == active {
         .                           return 0;
         .                       }
         .                       let mut s = active;
         .                       for r in 1..=n_players {
         .                           s = self.next_alive_seat(s);
         .                           if s == seat {
         .                               return r;
-- line 16274 ----------------------------------------
-- line 16286 ----------------------------------------
         .                   candidates.sort_by_key(|c| apnap_rank(c.controller));
         .           
         .                   // Prowess: inject +1/+1 EOT pump for each creature with the
         .                   // Prowess keyword that does NOT already carry its own prowess()
         .                   // triggered ability. Cards wired via shortcut::prowess() already
         .                   // have a SpellCast trigger on their definition; we skip those to
         .                   // avoid doubling the pump.
         .                   for ev in events {
   314,144 ( 0.01%)              if let GameEvent::SpellCast { player, card_id, .. } = ev {
    14,812 ( 0.00%)                  let is_creature_spell = self.stack.iter().any(|si| matches!(
     7,766 ( 0.00%)                      si,
    14,718 ( 0.00%)                      crate::game::types::StackItem::Spell { card, .. } if card.id == *card_id && card.definition.is_creature()
   101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
         .                           ));
         .                           if !is_creature_spell {
     3,144 ( 0.00%)                      let prowess_ids: Vec<_> = self.battlefield.iter()
         .                                   .filter(|c| {
   171,756 ( 0.01%)                              c.controller == *player
   141,650 ( 0.00%)                                  && c.has_keyword(&Keyword::Prowess)
 1,682,278 ( 0.05%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (28,330x)
         .                                           && !c.definition.triggered_abilities.iter().any(|ta| {
        88 ( 0.00%)                                      matches!(ta.event.kind, crate::effect::EventKind::SpellCast)
         .                                           })
         .                                   })
         .                                   .map(|c| c.id)
         .                                   .collect();
         .                               for pid in prowess_ids {
         .                                   candidates.push(TriggerCandidate {
         .                                       from_mana_ability: false,
         .                                       actor: None,
-- line 16313 ----------------------------------------
-- line 16336 ----------------------------------------
         .                   // from each player's `ring_temptations` level rather than synthesized
         .                   // as a literal emblem). Level 2+: "Whenever your Ring-bearer attacks,
         .                   // draw a card, then discard a card." Level 3+: "Whenever your
         .                   // Ring-bearer becomes blocked by a creature, the blocking creature's
         .                   // controller sacrifices it at end of combat." Level-4 combat-damage
         .                   // drain rides the dedicated combat-damage path in `combat.rs`.
         .                   let mut ring_blocked_done = vec![false; self.players.len()];
         .                   for ev in events {
   776,896 ( 0.02%)              match ev {
         .                           GameEvent::AttackerDeclared(attacker) => {
         .                               for seat in 0..self.players.len() {
    25,392 ( 0.00%)                          if self.players[seat].ring_temptations >= 2
         .                                       && self.effective_ring_bearer(seat) == Some(*attacker)
         .                                   {
         .                                       candidates.push(TriggerCandidate {
         .                                           from_mana_ability: false,
         .                                       actor: None,
         .                                           source: *attacker,
         .                                           effect: Effect::Seq(vec![
         .                                               Effect::Draw {
-- line 16355 ----------------------------------------
-- line 16370 ----------------------------------------
         .                                       triggered_by_death: false,
         .                                       triggered_by_attack: false,
         .                                       });
         .                                   }
         .                               }
         .                           }
         .                           GameEvent::BlockerDeclared { attacker, .. } => {
         .                               for (seat, done) in ring_blocked_done.iter_mut().enumerate() {
     9,696 ( 0.00%)                          if !*done
    14,544 ( 0.00%)                              && self.players[seat].ring_temptations >= 3
         .                                       && self.effective_ring_bearer(seat) == Some(*attacker)
         .                                   {
         .                                       *done = true;
         .                                       candidates.push(TriggerCandidate {
         .                                           from_mana_ability: false,
         .                                       actor: None,
         .                                           source: *attacker,
         .                                           effect: Effect::SacrificeAtEndOfCombat {
-- line 16387 ----------------------------------------
-- line 16410 ----------------------------------------
         .                   // so AutoDecider/bot games (and the bulk of the test suite) are
         .                   // untouched; AutoDecider would keep the default order anyway.
         .                   candidates.sort_by_key(|c| apnap_rank(c.controller));
         .                   // Queue the "gained life earlier this turn" flag flips for this
         .                   // batch's LifeGained recipients — applied AFTER filter evaluation
         .                   // in `push_ordered_trigger_candidates`, so "first time each turn"
         .                   // filters in this very batch still see the pre-batch state.
         .                   for ev in events {
   314,144 ( 0.01%)              if let GameEvent::LifeGained { player, .. } = ev
     1,318 ( 0.00%)                  && !self.life_gain_flag_pending.contains(player)
         .                       {
     4,548 ( 0.00%)                  self.life_gain_flag_pending.push(*player);
         .                       }
         .                   }
         .                   // CR 603.4 — "until end of turn, whenever you gain life" delayed
         .                   // triggers (Vizkopa Guildmage). Fire per LifeGained event for a watcher
         .                   // whose controller is the recipient; the amount rides in for the body.
         .                   for ev in events {
   314,144 ( 0.01%)              if let GameEvent::LifeGained { player, amount } = ev {
     6,590 ( 0.00%)                  self.fire_life_gained_watchers(*player, *amount);
   123,892 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_life_gained_watchers (1,318x)
         .                       }
         .                   }
         .                   // CR 603.4 — "whenever a card is put into an opponent's graveyard this
         .                   // turn" delayed triggers (Duskmantle Guildmage).
         .                   for ev in events {
   314,144 ( 0.01%)              if let GameEvent::CardPutIntoGraveyard { player, .. } = ev {
    21,640 ( 0.00%)                  self.fire_opponent_graveyard_watchers(*player);
   530,180 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::fire_opponent_graveyard_watchers (5,410x)
         .                       }
         .                   }
         .                   // May suspend on a networked controller's `OrderTriggers` pick
         .                   // (CR 603.3b) — the resume path re-enters
         .                   // `push_ordered_trigger_candidates` with the finished order.
   994,308 ( 0.03%)          let Some(candidates) = self.continue_trigger_ordering(Vec::new(), candidates) else {
 2,511,936 ( 0.08%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::continue_trigger_ordering (52,332x)
         .                       return;
         .                   };
   104,664 ( 0.00%)          self.push_ordered_trigger_candidates(candidates);
42,714,254 ( 1.37%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::push_ordered_trigger_candidates (52,332x)
   844,710 ( 0.03%)      }
         .           
         .               /// Phase 2 of trigger dispatch: enforce each candidate's
         .               /// `EventSpec::filter`, expand ETB multipliers/taxes, and drain the
         .               /// resulting queue onto the stack. Split from
         .               /// `dispatch_triggers_for_events` so the `OrderTriggers` resume path
         .               /// can re-enter after a networked controller picks their order.
         .               /// Count active Isshin-style attack-trigger doublers a player controls
         .               /// (`StaticEffect::DoubleControllerAttackTriggers` — Windcrag Siege's Mardu
         .               /// mode). Each adds one extra fire to a permanent's attack-caused trigger.
         .               pub(crate) fn attack_trigger_extra_fires(&self, controller: usize) -> usize {
         .                   self.battlefield
         .                       .iter()
     2,502 ( 0.00%)              .filter(|c| c.controller == controller)
         .                       .flat_map(|c| &c.definition.static_abilities)
         .                       .filter(|sa| {
         .                           matches!(
         .                               sa.effect,
         .                               crate::effect::StaticEffect::DoubleControllerAttackTriggers
         .                           )
         .                       })
         .                       .count()
       264 ( 0.00%)      }
         .           
   418,656 ( 0.01%)      pub(crate) fn push_ordered_trigger_candidates(
         .                   &mut self,
         .                   candidates: Vec<TriggerCandidate>,
         .               ) {
         .                   // Phase 2: enforce the optional `EventSpec::filter` predicate now
         .                   // that we're free to call `&self.evaluate_predicate`. The trigger's
         .                   // source permanent is bound as `ctx.source`, and the event's
         .                   // subject (cast spell, dying creature, attacker, etc.) is bound as
         .                   // `Selector::TriggerSource` so filters can reference it.
-- line 16478 ----------------------------------------
-- line 16479 ----------------------------------------
         .                   // Build the queue of triggers waiting to be pushed onto the
         .                   // stack. `drain_trigger_queue` walks the queue and either
         .                   // pushes each trigger with an auto-picked target, or — when
         .                   // the controller has `wants_ui` and the effect needs a target
         .                   // — suspends on `Decision::ChooseTarget` so the human can
         .                   // pick. Remaining queue items are saved in
         .                   // `ResumeContext::TriggerTargetPick` and drained on answer.
         .                   let mut queue: Vec<PendingTriggerPush> = Vec::new();
   313,992 ( 0.01%)          for candidate in candidates {
         .                       let TriggerCandidate {
         .                           actor,
         .                           source,
         .                           effect,
         .                           controller,
         .                           filter,
         .                           subject,
         .                           event_amount,
-- line 16495 ----------------------------------------
-- line 16613 ----------------------------------------
         .                           }
         .                       }
         .                   }
         .                   // Filter evaluation for this batch is done — flip the queued
         .                   // "gained life earlier this turn" flags (Leech Collector's
         .                   // "first time each turn" gate).
         .                   for p in std::mem::take(&mut self.life_gain_flag_pending) {
         .                       if let Some(pl) = self.players.get_mut(p) {
     2,548 ( 0.00%)                  pl.gained_life_earlier_this_turn = true;
    38,220 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,274x)
         .                       }
         .                   }
   366,324 ( 0.01%)          self.drain_trigger_queue(queue);
 3,035,256 ( 0.10%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::drain_trigger_queue (52,332x)
         .                   // Clear the per-die-event snapshot cache
         .                   // after the dispatcher finishes with this event batch. Any
         .                   // subsequent SBA cycle re-populates the entries it needs at
         .                   // that cycle's die-time, so stale entries from prior batches
         .                   // can't leak into later trigger resolution.
         .                   self.died_card_snapshots.clear();
   418,656 ( 0.01%)      }
         .           
         .               /// CR 603.3b — reorder each contiguous run of same-controller triggers
         .               /// per the controller's chosen stack-push order. Only consults the
         .               /// decider for a `wants_ui` controller whose run has ≥2 triggers; every
         .               /// other run is returned unchanged (AutoDecider keeps the default order
         .               /// regardless). The decider's `TriggerOrder(ids)` lists the desired
         .               /// push order; ids it omits keep their original relative order at the
         .               /// end, so a partial or empty answer is always legal.
         .               /// CR 603.3b — walk contiguous same-controller runs of simultaneous
         .               /// triggers, letting each `wants_ui` controller pick a stack-push order.
         .               /// Suspends on `Decision::OrderTriggers` (parking progress in
         .               /// `ResumeContext::TriggerOrder`) and returns `None`; `submit_decision`
         .               /// re-enters with the answered run applied. Returns the fully ordered
         .               /// list otherwise. AutoDecider/bot seats keep the default order.
   627,984 ( 0.02%)      pub(crate) fn continue_trigger_ordering(
         .                   &mut self,
         .                   mut ordered: Vec<TriggerCandidate>,
         .                   rest: Vec<TriggerCandidate>,
         .               ) -> Option<Vec<TriggerCandidate>> {
         .                   let mut i = 0;
   104,664 ( 0.00%)          while i < rest.len() {
         .                       let ctrl = rest[i].controller;
         .                       let mut j = i + 1;
         .                       while j < rest.len() && rest[j].controller == ctrl {
         .                           j += 1;
         .                       }
         .                       let run = &rest[i..j];
         .                       // A decision already pending (e.g. a racing combat choice) has
         .                       // nowhere to park this batch — keep the default order, matching
-- line 16660 ----------------------------------------
-- line 16685 ----------------------------------------
         .                                   run: run.to_vec(),
         .                                   rest: rest[j..].to_vec(),
         .                               },
         .                           });
         .                           return None;
         .                       }
         .                       i = j;
         .                   }
   261,660 ( 0.01%)          Some(ordered)
   418,656 ( 0.01%)      }
         .           
         .               /// Apply a `TriggerOrder(ids)` answer to `run`: entries named in `ids`
         .               /// first (in that order), unnamed ones after in original order — a
         .               /// partial or empty answer is always legal.
         .               pub(crate) fn apply_trigger_order(
         .                   ordered: &mut Vec<TriggerCandidate>,
         .                   run: Vec<TriggerCandidate>,
         .                   order: Vec<CardId>,
-- line 16702 ----------------------------------------
-- line 16715 ----------------------------------------
         .               }
         .               /// Walk a queue of pending triggers, pushing each onto the stack.
         .               /// Suspends on the first trigger whose controller has `wants_ui`
         .               /// and whose effect needs a target — emits
         .               /// `Decision::ChooseTarget` and parks the remaining queue in
         .               /// `ResumeContext::TriggerTargetPick`. The resume path
         .               /// (`submit_decision`) re-enters this function with the remaining
         .               /// queue once the user picks.
   534,352 ( 0.02%)      pub fn drain_trigger_queue(&mut self, queue: Vec<PendingTriggerPush>) {
         .                   // Don't stack up multiple pending decisions — if the engine
         .                   // already suspended on something else we can't surface a target
         .                   // picker, so the whole batch falls back to auto-targeting (the
         .                   // triggers still hit the stack; they must not vanish).
         .                   let force_auto = self.pending_decision.is_some();
         .                   // Walk the queue in *forward* (APNAP) order so the active
         .                   // player's triggers push first and resolve last, matching CR
         .                   // 603.3b. Using an iterator lets us collect the unconsumed
-- line 16731 ----------------------------------------
-- line 16851 ----------------------------------------
         .                           pending.controller,
         .                           &avoid,
         .                       );
         .                       if let Some(Target::Permanent(tid)) = &auto {
         .                           picked_this_batch.push((pending.source, *tid));
         .                       }
         .                       self.push_pending_trigger(pending, auto);
         .                   }
   534,352 ( 0.02%)      }
         .           
         .               /// CR 605.1b — could this triggered ability's body add mana to a pool?
         .               /// (The caller checks the other two criteria: no target, and fired from a
         .               /// mana ability.) Only mana-adding leaves qualify; a body that also does
         .               /// something else (Overabundance's ping) is still a mana ability.
         .               fn is_triggered_mana_ability(effect: &Effect) -> bool {
         .                   match effect {
         .                       Effect::AddMana { .. }
-- line 16867 ----------------------------------------
-- line 16993 ----------------------------------------
         .                       self.dispatch_triggers_for_events(&became);
         .                   }
         .               }
         .           
         .               /// Pick the slot-1+ targets for an engine-resolved `Effect::ApplyToTargets`
         .               /// (an "up to N target" triggered ability). Returns up to `max_targets - 1`
         .               /// distinct legal targets beyond `primary`, preferring permanents other
         .               /// than the source. Empty for any other effect or for `max_targets <= 1`.
     5,814 ( 0.00%)      pub(crate) fn auto_extra_targets_for(
         .                   &self,
         .                   eff: &Effect,
         .                   source: CardId,
         .                   controller: usize,
         .                   primary: Option<Target>,
         .               ) -> Vec<Target> {
         .                   // Peel the transparent wrappers a multi-target body sits under — a
         .                   // trigger's "you may" (`MayDo`) and the resolution-time slot caps
         .                   // (`CapTargetsAt` / `TargetsExactlyX` / `OptionalTargets`) — so
         .                   // Terastodon and Voyager Drake fan out like a bare `ApplyToTargets`.
         .                   let mut eff = eff;
         .                   let mut cap = usize::MAX;
         .                   loop {
     3,264 ( 0.00%)              eff = match eff {
         .                           Effect::MayDo { body, .. } | Effect::OptionalTargets { body, .. } => body,
         .                           Effect::TargetsExactlyX { body } => body,
         .                           Effect::CapTargetsAt { amount, body } => {
         .                               let ctx = crate::game::effects::EffectContext::for_trigger(
         .                                   source,
         .                                   controller,
         .                                   primary.clone(),
         .                                   0,
-- line 17023 ----------------------------------------
-- line 17033 ----------------------------------------
         .                               match (targeting.next(), targeting.next()) {
         .                                   (Some(only), None) => only,
         .                                   _ => break,
         .                               }
         .                           }
         .                           _ => break,
         .                       };
         .                   }
     1,710 ( 0.00%)          let max = match eff {
         .                       Effect::ApplyToTargets { max_targets, .. }
         .                       // CR 701.32 — a trigger-side "support N" spreads over up to N
         .                       // *other* target creatures, not just the one auto-bound slot.
         .                       | Effect::SupportCounters { max_targets, .. } => (*max_targets as usize).min(cap),
         .                       // Effects whose slots carry *distinct* per-slot filters (Kor
         .                       // Outfitter's ETB `Attach { what: target Equipment, to: target
         .                       // creature }`) can't be filled by the same-filter loop below —
         .                       // they walk each slot's own filter instead.
     1,368 ( 0.00%)              _ => return self.auto_extra_distinct_slot_targets(eff, source, controller, primary),
         .                   };
         .                   if max <= 1 || primary.is_none() {
         .                       return vec![];
         .                   }
         .                   let mut chosen: Vec<Target> = Vec::new();
         .                   // First avoid entry doubles as the OtherThanSource avoid-source; keep
         .                   // the trigger source there, then grow the set with each pick.
         .                   let mut avoid: Vec<CardId> = vec![source];
-- line 17058 ----------------------------------------
-- line 17064 ----------------------------------------
         .                           Some(t @ Target::Permanent(cid)) if !avoid.contains(&cid) => {
         .                               avoid.push(cid);
         .                               chosen.push(t);
         .                           }
         .                           _ => break,
         .                       }
         .                   }
         .                   chosen
     2,736 ( 0.00%)      }
         .           
         .               /// Fill slots 1.. of a triggered ability whose effect surfaces a *distinct*
         .               /// target filter per slot (Kor Outfitter's `Attach`, where slot 0 = the
         .               /// Equipment and slot 1 = the creature). Each slot is matched against its
         .               /// own `target_filter_for_slot` filter, preferring the controller's own
         .               /// permanents and avoiding anything an earlier slot already claimed. Empty
         .               /// for single-slot effects (the common case).
         .               pub(crate) fn auto_extra_distinct_slot_targets(
         .                   &self,
         .                   eff: &Effect,
         .                   source: CardId,
         .                   controller: usize,
         .                   primary: Option<Target>,
         .               ) -> Vec<Target> {
     2,394 ( 0.00%)          let slot1 = match eff.target_filter_for_slot_in_mode_kicked(1, None, false) {
    17,052 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (342x)
         .                       Some(f) => f,
         .                       None => return vec![],
         .                   };
         .                   // Same-filter slot 1: fanning out a *divide* effect (DealDamageDivided,
         .                   // DistributeCounters, SupportCounters — `distinct_target_count` is Some)
         .                   // would wrongly split its single multi-target across slots, so those
         .                   // keep their dedicated auto/divide pickers. But a `Seq`/`OptionalTargets`
         .                   // of independent single-target clauses (Ral Zarek's tap-one/untap-
-- line 17095 ----------------------------------------
-- line 17389 ----------------------------------------
         .                   let mut sba = self.check_state_based_actions();
         .                   events.append(&mut sba);
         .                   Ok(events)
         .               }
         .           
         .               /// Begin the pre-game London-mulligan phase. Deals 7 cards to each player
         .               /// and sets `pending_decision` for seat 0's opening-hand choice.
         .               /// Call this after constructing the `GameState` and before the first turn.
       168 ( 0.00%)      pub fn start_mulligan_phase(&mut self) {
         .                   let n = self.players.len();
         .                   for i in 0..n {
         .                       // CR 103.4 / Backup Plan — one extra seven-card hand per source;
         .                       // all but one are shuffled back before mulligans, so the seat keeps
         .                       // the best of them. The engine keeps the first hand it likes: the
         .                       // one with the most lands, then the most cards it could cast.
        96 ( 0.00%)              let extra = self.extra_opening_hands_for(i);
        96 ( 0.00%)              self.deal_to_hand(i, 7);
   119,357 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::deal_to_hand (48x)
         .                       for _ in 0..extra {
         .                           let kept = std::mem::take(&mut *self.players[i].hand);
         .                           self.deal_to_hand(i, 7);
         .                           let fresh = std::mem::replace(&mut *self.players[i].hand, kept);
         .                           if Self::opening_hand_score(&fresh) > Self::opening_hand_score(&self.players[i].hand) {
         .                               let discarded = std::mem::replace(&mut *self.players[i].hand, fresh);
         .                               self.players[i].library.extend(discarded);
         .                           } else {
         .                               self.players[i].library.extend(fresh);
         .                           }
         .                           self.shuffle_library_silently(i);
         .                       }
         .                   }
       192 ( 0.00%)          self.set_mulligan_decision(0, 0, if n > 1 { Some(1) } else { None });
    39,706 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::set_mulligan_decision (24x)
       192 ( 0.00%)      }
         .           
         .               /// CR 103.4 — seven, less any command-zone reduction (Sovereign's Realm's
         .               /// "your starting hand size is five").
         .               pub fn starting_hand_size(&self, seat: usize) -> u32 {
         .                   use crate::effect::StaticEffect;
         .                   let cut: u32 = self.players[seat]
         .                       .command
         .                       .iter()
-- line 17428 ----------------------------------------
-- line 17458 ----------------------------------------
         .                   let lands = hand.iter().filter(|c| c.definition.is_land()).count() as i32;
         .                   let cheap = hand
         .                       .iter()
         .                       .filter(|c| !c.definition.is_land() && c.definition.cost.cmc() <= 3)
         .                       .count() as i32;
         .                   cheap - (lands - 3).abs() * 2
         .               }
         .           
       372 ( 0.00%)      fn deal_to_hand(&mut self, seat: usize, count: usize) {
         .                   // Top of library is `library[0]` — `pop()` would deal from the
         .                   // bottom, which produces the wrong opening hand for unshuffled
         .                   // (test-fixture) decks. Drain the top `count` cards instead.
         .                   for _ in 0..count {
     1,736 ( 0.00%)              if self.players[seat].library.is_empty() {
         .                           break;
         .                       }
     2,170 ( 0.00%)              let card = self.players[seat].library.remove(0);
    13,020 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (434x)
     1,736 ( 0.00%)              self.players[seat].hand.push(card);
    13,020 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (434x)
         .                   }
       372 ( 0.00%)      }
         .           
         .               /// CR 103.2c — shuffle `seat`'s library because a spell or ability said so,
         .               /// emitting the `LibraryShuffled` event that "whenever a player shuffles"
         .               /// triggers key on (Psychogenic Probe). Game-setup shuffles bypass this.
         .               pub(crate) fn shuffle_library(&mut self, seat: usize, events: &mut Vec<GameEvent>) {
         .                   use rand::seq::SliceRandom;
         .                   self.players[seat].library.shuffle(&mut self.rng.draw());
         .                   // A one-shot top reveal (Aven Windreader) only covers the card that was
         .                   // on top; a shuffle moves it.
         .                   self.library_tops_revealed.retain(|s| *s != seat);
         .                   events.push(GameEvent::LibraryShuffled { player: seat });
         .               }
         .           
       112 ( 0.00%)      fn shuffle_hand_to_library(&mut self, seat: usize) {
         .                   use rand::seq::SliceRandom;
        42 ( 0.00%)          let hand = std::mem::take(&mut *self.players[seat].hand);
    33,352 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (14x)
       154 ( 0.00%)          for card in hand {
       406 ( 0.00%)              self.players[seat].library.push(card);
     2,940 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (98x)
         .                   }
        28 ( 0.00%)          let mut rng = self.rng.draw();
        42 ( 0.00%)          self.players[seat].library.shuffle(&mut rng);
       420 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (14x)
       112 ( 0.00%)      }
         .           
       496 ( 0.00%)      fn set_mulligan_decision(&mut self, player: usize, mulligans_taken: usize, next_player: Option<usize>) {
         .                   let hand: Vec<_> = self.players[player].hand
         .                       .iter()
         .                       .map(|c| (c.id, c.definition.name.to_string()))
         .                       .collect();
         .                   // Surface any in-hand Serum Powder–style mulligan helpers so the
         .                   // decider can pick an alternative answer.
         .                   let serum_powders: Vec<_> = self.players[player].hand
         .                       .iter()
       434 ( 0.00%)              .filter(|c| matches!(
         .                           c.definition.opening_hand,
         .                           Some(crate::effect::OpeningHandEffect::MulliganHelper),
         .                       ))
         .                       .map(|c| c.id)
         .                       .collect();
       992 ( 0.00%)          self.pending_decision = Some(PendingDecision {
         .                       decision: Decision::Mulligan { player, hand, mulligans_taken, serum_powders },
         .                       resume: ResumeContext::Mulligan { player, mulligans_taken, next_player },
         .                   });
       496 ( 0.00%)      }
         .           
         .               /// Submit an answer to the currently-pending decision and resume resolution.
         .               /// Fails if no decision is pending, or the answer shape doesn't match the
         .               /// decision kind.
     5,552 ( 0.00%)      pub fn submit_decision(&mut self, answer: DecisionAnswer) -> Result<Vec<GameEvent>, GameError> {
     4,164 ( 0.00%)          let pd = self
         .                       .pending_decision
         .                       .take()
         .                       .ok_or(GameError::NoDecisionPending)?;
     5,552 ( 0.00%)          let mut events = match pd.resume {
         .                       ResumeContext::Spell {
         .                           card,
         .                           caster,
         .                           target,
         .                           additional_targets,
         .                           mode,
         .                           x_value,
         .                           converged_value,
-- line 17538 ----------------------------------------
-- line 17589 ----------------------------------------
         .                           let mut evs = self.apply_pending_effect_answer(in_progress, &answer)?;
         .                           let mut more = self.continue_ability_resolution(
         .                               source, controller, remaining, target,
         .                           )?;
         .                           evs.append(&mut more);
         .                           evs
         .                       }
         .                       ResumeContext::Mulligan { player, mulligans_taken, next_player } => {
     1,036 ( 0.00%)                  match answer {
         .                               DecisionAnswer::TakeMulligan => {
        28 ( 0.00%)                          self.shuffle_hand_to_library(player);
    84,189 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::shuffle_hand_to_library (14x)
       112 ( 0.00%)                          self.deal_to_hand(player, 7);
    29,105 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::deal_to_hand (14x)
       154 ( 0.00%)                          self.set_mulligan_decision(player, mulligans_taken + 1, next_player);
    24,171 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::set_mulligan_decision (14x)
         .                                   return Ok(vec![]);
         .                               }
         .                               DecisionAnswer::Keep => {
        96 ( 0.00%)                          if mulligans_taken > 0 {
        24 ( 0.00%)                              let hand = self.players[player].hand
         .                                           .iter()
         .                                           .map(|c| (c.id, c.definition.name.to_string()))
         .                                           .collect();
       216 ( 0.00%)                              self.pending_decision = Some(PendingDecision {
        36 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<crabomination::game::types::PendingDecision>> (12x)
         .                                           decision: Decision::PutOnLibrary {
         .                                               player,
         .                                               count: mulligans_taken,
         .                                               hand,
         .                                           },
         .                                           // Carry the mulligan count forward so the
         .                                           // PutOnLibrary handler below knows how many
         .                                           // cards to bottom.
         .                                           resume: ResumeContext::Mulligan { player, mulligans_taken, next_player },
         .                                       });
         .                                       return Ok(vec![]);
         .                                   }
       108 ( 0.00%)                          self.advance_mulligan(next_player);
    56,503 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::advance_mulligan (36x)
         .                                   return Ok(vec![]);
         .                               }
        36 ( 0.00%)                      DecisionAnswer::PutOnLibrary(ids) => {
         .                                   // London mulligan: chosen cards go to the BOTTOM of
         .                                   // the library. CR 103.5a — exactly `mulligans_taken`
         .                                   // cards must leave the hand; re-pose on a short or
         .                                   // bogus answer so a hostile client can't keep a
         .                                   // fresh seven.
         .                                   let mut bottomed = 0usize;
         .                                   for card_id in ids.iter().take(mulligans_taken) {
        70 ( 0.00%)                              if let Some(card) = Self::take_card(&mut self.players[player].hand, *card_id) {
    28,162 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (14x)
        56 ( 0.00%)                                  self.players[player].library.push(card);
       420 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (14x)
        14 ( 0.00%)                                  bottomed += 1;
         .                                       }
         .                                   }
         .                                   let still_owed = mulligans_taken - bottomed;
        24 ( 0.00%)                          if still_owed > 0 && !self.players[player].hand.is_empty() {
         .                                       let hand = self.players[player].hand
         .                                           .iter()
         .                                           .map(|c| (c.id, c.definition.name.to_string()))
         .                                           .collect();
         .                                       self.pending_decision = Some(PendingDecision {
         .                                           decision: Decision::PutOnLibrary {
         .                                               player,
         .                                               count: still_owed,
-- line 17648 ----------------------------------------
-- line 17651 ----------------------------------------
         .                                           resume: ResumeContext::Mulligan {
         .                                               player,
         .                                               mulligans_taken: still_owed,
         .                                               next_player,
         .                                           },
         .                                       });
         .                                       return Ok(vec![]);
         .                                   }
        48 ( 0.00%)                          self.advance_mulligan(next_player);
    13,184 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::advance_mulligan (12x)
         .                                   return Ok(vec![]);
         .                               }
         .                               DecisionAnswer::SerumPowder(powder_id) => {
         .                                   // Serum Powder: exile the entire current hand (the
         .                                   // powder card itself goes with it), then draw a new
         .                                   // seven. Doesn't bump `mulligans_taken` — Serum
         .                                   // Powder is intentionally separate from the London
         .                                   // mulligan ladder (so multiple powders can stack
-- line 17667 ----------------------------------------
-- line 17769 ----------------------------------------
         .                               self.set_cleanup_discard_decision(player, excess);
         .                               return Ok(evs);
         .                           }
         .                           return match self.finish_cleanup(&mut evs) {
         .                               crate::game::stack::CleanupOutcome::TurnOver => self.advance_step(evs),
         .                               _ => Ok(evs),
         .                           };
         .                       }
       620 ( 0.00%)              ResumeContext::CombatDamage { player: _, attacker, kind } => {
         .                           // CR 510.1c-d — cache the answered ordering/assignment choice,
         .                           // then re-enter the current damage step. It re-runs the (now
         .                           // cached) gather and either suspends on the next choice or
         .                           // applies all combat damage. Mirrors the pass_priority combat
         .                           // arms (give priority + dispatch triggers) on completion.
         .                           self.apply_combat_decision_answer(attacker, kind, &answer);
     3,100 ( 0.00%)                  let evs = match self.step {
         .                               TurnStep::FirstStrikeDamage => self.resolve_first_strike_damage()?,
     1,860 ( 0.00%)                      TurnStep::CombatDamage => self.resolve_combat()?,
94,082,063 ( 3.02%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat (620x)
         .                               _ => Vec::new(),
         .                           };
       620 ( 0.00%)                  if self.pending_decision.is_none() {
         .                               self.give_priority_to_active();
       604 ( 0.00%)                      self.dispatch_triggers_for_events(&evs);
 3,331,959 ( 0.11%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (302x)
         .                           }
     1,860 ( 0.00%)                  return Ok(evs);
         .                       }
         .                       ResumeContext::CastAdditionalCost {
         .                           caster,
         .                           card_id,
         .                           target,
         .                           additional_targets,
         .                           mode,
         .                           x_value,
-- line 17801 ----------------------------------------
-- line 18025 ----------------------------------------
         .                               None,
         .                           );
         .                       }
         .                   };
         .                   let mut sba = self.check_state_based_actions();
         .                   events.append(&mut sba);
         .                   self.dispatch_triggers_for_events(&events);
         .                   Ok(events)
    18,056 ( 0.00%)      }
   280,374 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::decision::Decision> (694x)
    12,090 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::decision::DecisionAnswer> (682x)
         .           
       192 ( 0.00%)      fn advance_mulligan(&mut self, next_player: Option<usize>) {
        96 ( 0.00%)          match next_player {
         .                       Some(p) => {
        48 ( 0.00%)                  let after = (p + 1 < self.players.len()).then_some(p + 1);
       192 ( 0.00%)                  self.set_mulligan_decision(p, 0, after);
    43,639 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::set_mulligan_decision (24x)
         .                       }
         .                       None => {
         .                           // All players kept — apply opening-hand effects (Leyline of
         .                           // Sanctity / Gemstone Caverns start in play; Chancellor reveals
         .                           // schedule delayed triggers) and start the game with priority
         .                           // on seat 0.
        48 ( 0.00%)                  self.apply_opening_hand_effects();
    25,208 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::apply_opening_hand_effects (24x)
        24 ( 0.00%)                  self.pending_decision = None;
         .                           self.give_priority_to_active();
         .                       }
         .                   }
        96 ( 0.00%)      }
         .           
         .               /// Walk every player's opening hand and apply each card's
         .               /// `OpeningHandEffect`. The default `Decider` answers "yes" to every
         .               /// optional reveal — the `AutoDecider` and the bot benefit from these
         .               /// effects in the demo decks, and a future UI can deny the reveal by
         .               /// returning `Bool(false)` from an `OptionalTrigger` decision (not yet
         .               /// surfaced — opening-hand effects auto-fire today).
         .               /// Backwards-compat alias used by some tests — fires every player's
         .               /// opening-hand effects immediately. Equivalent to (and delegates to)
         .               /// `apply_opening_hand_effects`.
         .               pub fn fire_start_of_game_effects(&mut self) {
         .                   self.apply_opening_hand_effects();
         .               }
         .           
       168 ( 0.00%)      pub(crate) fn apply_opening_hand_effects(&mut self) {
         .                   let n = self.players.len();
         .                   for p in 0..n {
         .                       // Snapshot ids first so we can iterate without aliasing the hand.
         .                       let ids: Vec<crate::card::CardId> =
       466 ( 0.00%)                  self.players[p].hand.iter().map(|c| c.id).collect();
         .                       for cid in ids {
       644 ( 0.00%)                  let oh = self.players[p]
         .                               .hand
         .                               .iter()
         .                               .find(|c| c.id == cid)
         .                               .and_then(|c| c.definition.opening_hand.clone());
         .                           let Some(oh) = oh else { continue };
         .                           match oh {
         .                               crate::effect::OpeningHandEffect::StartInPlay { tapped, extra } => {
         .                                   // Pull the card out of hand and place it on the
-- line 18081 ----------------------------------------
-- line 18119 ----------------------------------------
         .                                   });
         .                               }
         .                               crate::effect::OpeningHandEffect::MulliganHelper => {
         .                                   // Surfaces during mulligan only; nothing to do here.
         .                               }
         .                           }
         .                       }
         .                   }
       192 ( 0.00%)      }
         .           
         .               /// Complete the suspended effect using the player's answer. Returns the
         .               /// events generated by the now-finished effect (e.g. `ScryPerformed`).
         .               pub(crate) fn apply_pending_effect_answer(
         .                   &mut self,
         .                   state: PendingEffectState,
         .                   answer: &DecisionAnswer,
         .               ) -> Result<Vec<GameEvent>, GameError> {
-- line 18135 ----------------------------------------
-- line 19221 ----------------------------------------
         .               // The argument list is wide because the spell-state quartet (target, mode,
         .               // x_value, converged_value) must be preserved across suspend/resume so the
         .               // spell can re-run its effect tree with the original cast-time choices.
         .               // The two callers (initial cast in `stack.rs` and resume in
         .               // `submit_decision`) both hand off these fields directly from a
         .               // `StackItem::Spell` / `ResumeContext::Spell`, so wrapping them in a
         .               // struct doesn't reduce coupling at the call sites.
         .               #[allow(clippy::too_many_arguments)]
    31,548 ( 0.00%)      pub(crate) fn continue_spell_resolution(
         .                   &mut self,
         .                   card: CardInstance,
         .                   caster: usize,
         .                   target: Option<Target>,
         .                   additional_targets: Vec<Target>,
         .                   mode: usize,
         .                   x_value: u32,
         .                   converged_value: u32,
-- line 19237 ----------------------------------------
-- line 19239 ----------------------------------------
         .                   override_effect: Option<Effect>,
         .               ) -> Result<Vec<GameEvent>, GameError> {
         .                   let is_initial_pass = override_effect.is_none();
         .                   let effect = override_effect.unwrap_or_else(|| {
         .                       if let Some(half) = card.alt_spell_half() {
         .                           // CR 715 / 702.183 — resolve the Adventure/Omen half's effect,
         .                           // not the creature body.
         .                           half.effect.clone()
     2,868 ( 0.00%)              } else if card.gift_promised
         .                           && let Some(gift) = card.definition.gift.as_ref()
         .                       {
         .                           // CR 702.165 — the gift was promised: resolve the enhanced
         .                           // effect (which itself bestows the gift on the opponent).
         .                           gift.gifted_effect.clone()
         .                       } else if let (Some(half), Some(split)) =
         .                           (card.split_cast, card.definition.split.as_ref())
         .                       {
-- line 19255 ----------------------------------------
-- line 19271 ----------------------------------------
         .                   // graveyard cards are unaffected): the spell fizzles if the target
         .                   // left the battlefield, stopped matching the spell's target filter,
         .                   // or became illegal to target (granted Hexproof/Shroud). Token
         .                   // copies additionally keep the bare filter re-check.
         .                   // CR 702.172 — Spree spells validate each chosen mode's target at
         .                   // resolution (per-mode), not through this single-`mode` fizzle path;
         .                   // the chosen modes aren't encoded in `mode`, so a slot-0 filter here
         .                   // could belong to an unchosen mode. Skip the blanket fizzle for Spree.
     7,170 ( 0.00%)          let is_spree = matches!(effect, Effect::Spree { .. } | Effect::Tiered { .. });
         .                   if !is_spree
     8,604 ( 0.00%)              && additional_targets.is_empty()
         .                       && let Some(t) = &target
         .                   {
         .                       let filter_fails = |g: &Self| {
         .                           effect
     3,624 ( 0.00%)                      .target_filter_for_slot_in_mode_kicked(0, Some(mode), card.kicked)
    53,752 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (604x)
         .                               .is_some_and(|f| {
     2,416 ( 0.00%)                          let f = f.resolve_x(x_value);
   103,650 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::SelectionRequirement::resolve_x (604x)
     4,228 ( 0.00%)                          !g.evaluate_requirement_static(&f, t, caster, Some(card.id))
 2,675,100 ( 0.09%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (604x)
       604 ( 0.00%)                      })
    97,754 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement> (604x)
         .                       };
     2,224 ( 0.00%)              let fizzled = if card.cast_target_was_battlefield
         .                           && let Target::Permanent(tid) = t
         .                       {
       606 ( 0.00%)                  self.battlefield_find(*tid).is_none()
     2,416 ( 0.00%)                      || filter_fails(self)
     3,624 ( 0.00%)                      || self.check_target_legality_with_source(t, caster, Some(card.id)).is_err()
 2,299,158 ( 0.07%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source (604x)
         .                       } else {
     1,012 ( 0.00%)                  card.is_token && filter_fails(self)
         .                       };
         .                       if fizzled {
         .                           // A fizzled token copy ceases to exist (already off the
         .                           // stack); a real card is countered into its owner's
         .                           // graveyard — except a flashbacked/aftermath cast, whose
         .                           // CR 702.34d exile rider applies wherever it leaves the
         .                           // stack, so a fizzle can't make it re-flashbackable.
         .                           let mut events = Vec::new();
         4 ( 0.00%)                  if !card.is_token {
         4 ( 0.00%)                      if card.cast_via_flashback {
         .                                   self.exile.push(card);
         .                               } else {
         4 ( 0.00%)                          self.route_to_graveyard(card, &mut events);
     5,461 ( 0.00%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::route_to_graveyard (2x)
         .                               }
         .                           }
        16 ( 0.00%)                  return Ok(events);
         .                       }
       604 ( 0.00%)          } else if !is_spree
       966 ( 0.00%)              && card.cast_target_was_battlefield
         .                       && let Some(t0) = &target
         .                   {
         .                       // CR 608.2b — a multi-target spell fizzles only if EVERY target
         .                       // is illegal on resolution; effects already skip individual
         .                       // missing targets. Scoped to battlefield-aimed casts (slot 0 was
         .                       // a battlefield permanent at cast time) so zone-loose multi-
         .                       // target spells (graveyard returns) are unaffected.
         .                       let slot_illegal = |g: &Self, slot: u8, t: &Target| {
-- line 19326 ----------------------------------------
-- line 19348 ----------------------------------------
         .                                   self.exile.push(card); // CR 702.34d
         .                               } else {
         .                                   self.route_to_graveyard(card, &mut events);
         .                               }
         .                           }
         .                           return Ok(events);
         .                       }
         .                   }
    22,912 ( 0.00%)          let mut ctx = EffectContext::for_spell_with_source_and_origin(
   209,936 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::EffectContext::for_spell_with_source_and_origin (1,432x)
     1,432 ( 0.00%)              card.id,
     2,864 ( 0.00%)              card.definition.name,
         .                       caster,
         .                       target.clone(),
         .                       additional_targets.clone(),
         .                       mode,
         .                       x_value,
         .                       converged_value,
         .                       mana_spent,
     1,432 ( 0.00%)              card.cast_from_hand,
         .                   );
     2,864 ( 0.00%)          ctx.kicked = card.kicked;
     4,296 ( 0.00%)          ctx.kicked_options = card.kicked_options.clone();
     2,864 ( 0.00%)          ctx.kick_count = card.kick_count;
     2,864 ( 0.00%)          ctx.bargained = card.bargained;
     2,864 ( 0.00%)          ctx.cast_via_mayhem = card.cast_via_mayhem;
     2,864 ( 0.00%)          ctx.cast_via_waterbend = card.cast_via_waterbend;
     2,864 ( 0.00%)          ctx.cast_collected_evidence = card.cast_collected_evidence;
     2,864 ( 0.00%)          ctx.entwined = card.entwined;
     4,296 ( 0.00%)          ctx.spree_modes = card.spree_modes.clone();
     4,296 ( 0.00%)          ctx.mana_spent_by_color = card.cast_mana_spent_by_color.clone();
         .                   // Stamp the resolving spell's identity so source-aware damage
         .                   // replacements (Torbran) can read its controller/colors while the
         .                   // card is in no visible zone.
     7,160 ( 0.00%)          let prev_src = self.resolving_source.replace((
     1,432 ( 0.00%)              card.id,
         .                       caster,
     1,432 ( 0.00%)              card.definition.printed_colors(),
   278,052 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_colors (1,432x)
         .                       card.definition.card_types.clone(),
         .                   ));
     2,864 ( 0.00%)          let res = self.resolve_effect(&effect, &ctx);
17,896,988 ( 0.57%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect (1,432x)
    11,456 ( 0.00%)          self.resolving_source = prev_src;
     7,160 ( 0.00%)          let mut events = res?;
         .                   // CR 702.165 — a promised gift is given as the spell resolves its
         .                   // gifted effect. Emit once on the initial pass so "whenever you give a
         .                   // gift" payoffs (Jolly Gerbils) can trigger.
     5,728 ( 0.00%)          if is_initial_pass && card.gift_promised && card.definition.gift.is_some() {
         .                       events.push(GameEvent::GiftGiven { player: caster });
         .                   }
         .                   // CR 709 / 702.102 — a fused split cast resolves its right half in a
         .                   // second pass, reading its target from `additional_targets` slot 0
         .                   // (the left half consumed `target`). Fusable halves are single-target.
         .                   if card.split_cast == Some(2)
         .                       && let Some(split) = card.definition.split.as_ref()
         .                   {
-- line 19401 ----------------------------------------
-- line 19413 ----------------------------------------
         .                           card.cast_from_hand,
         .                       );
         .                       let mut right_events = self.resolve_effect(&right_effect, &right_ctx)?;
         .                       events.append(&mut right_events);
         .                   }
         .                   // CR 702.47b — spliced rules text resolves after the main spell's
         .                   // effect; spliced effect `i` reads its target from
         .                   // `additional_targets[i]`.
    14,320 ( 0.00%)          for (i, spliced) in card.spliced_effects.clone().into_iter().enumerate() {
    45,824 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (1,432x)
         .                       let splice_ctx = EffectContext::for_spell_with_source_and_origin(
         .                           card.id,
         .                           card.definition.name,
         .                           caster,
         .                           additional_targets.get(i).cloned(),
         .                           Vec::new(),
         .                           mode,
         .                           x_value,
         .                           converged_value,
         .                           mana_spent,
         .                           card.cast_from_hand,
         .                       );
         .                       let mut splice_events = self.resolve_effect(&spliced, &splice_ctx)?;
         .                       events.append(&mut splice_events);
         .                   }
     2,864 ( 0.00%)          if let Some((decision, in_progress, remaining)) = self.suspend_signal.take() {
         .                       self.pending_decision = Some(PendingDecision {
         .                           decision,
         .                           resume: ResumeContext::Spell {
         .                               card: Box::new(card),
         .                               caster,
         .                               target,
         .                               additional_targets,
         .                               mode,
-- line 19445 ----------------------------------------
-- line 19450 ----------------------------------------
         .                               remaining,
         .                           },
         .                       });
         .                       return Ok(events);
         .                   }
         .                   // CR 702.50 — Epic: on resolution the caster snapshots the spell
         .                   // (copied at each of their upkeeps) and can't cast spells for the
         .                   // rest of the game. Copies don't carry the epic ability (702.50a).
     2,864 ( 0.00%)          if !card.is_token && card.definition.keywords.contains(&crate::card::Keyword::Epic) {
         .                       self.players[caster].epic_spells.push(crate::player::EpicSpell {
         .                           name: card.definition.name.to_string(),
         .                           target: target.clone(),
         .                           additional_targets: additional_targets.clone(),
         .                           mode: Some(mode),
         .                           x_value,
         .                       });
         .                   }
         .                   // Esper Origins — "exile it, then put it onto the battlefield
         .                   // transformed under its owner's control". The front face is a sorcery,
         .                   // so the flip happens on the card the routing already owns.
         .                   // CR 712.13a — an instant/sorcery back face can't enter, so the card
         .                   // falls through to the graveyard.
     2,864 ( 0.00%)          if let Some(counter) = self.resolving_spell_to_battlefield_transformed.take()
         .                       && card
         .                           .definition
         .                           .back_face
         .                           .as_ref()
         .                           .is_some_and(|b| !b.is_instant() && !b.is_sorcery())
         .                   {
         .                       let (owner, cid) = (card.owner, card.id);
         .                       let mut card = card;
-- line 19480 ----------------------------------------
-- line 19499 ----------------------------------------
         .                           events.push(GameEvent::CounterAdded { card_id: cid, counter_type: kind, count: 1 });
         .                       }
         .                       return Ok(events);
         .                   }
         .                   // Rebound: if this card has Keyword::Rebound and was cast from
         .                   // hand, exile it instead of sending it to the graveyard, and
         .                   // schedule a delayed trigger at the caster's next upkeep that
         .                   // re-runs the spell's effect with a fresh auto-target.
     2,864 ( 0.00%)          if card.cast_from_hand
         .                       && card.definition.keywords.contains(&crate::card::Keyword::Rebound)
         .                   {
         .                       use crate::game::types::{DelayedKind, DelayedTrigger};
         .                       let source = card.id;
         .                       let body = card.definition.effect.clone();
         .                       self.delayed_triggers.push(DelayedTrigger {
         .                           controller: caster,
         .                           source,
-- line 19515 ----------------------------------------
-- line 19524 ----------------------------------------
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // Flashback (CR 702.34d): a spell cast via its Flashback cost is
         .                   // exiled on resolution instead of going to the graveyard.
         .                   // `cast_flashback` sets `cast_via_flashback = true`; the
         .                   // resolver consults that flag (it used to overload `kicked`,
         .                   // which collided with cards that have both Kicker and Flashback).
     2,864 ( 0.00%)          if card.cast_via_flashback {
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // CR 701.x — "Then exile this spell" rider. Cards with
         .                   // `exile_on_resolve = true` route to exile after resolution
         .                   // instead of their owner's graveyard. Used by Awaken the Ages,
         .                   // Divergent Equation, Settle the Score's printed rider.
         .                   // Bump the owner's `cards_exiled_this_turn` so the Ennis-style
         .                   // "cards put into exile this turn" payoffs see the exile.
     2,864 ( 0.00%)          if card.definition.exile_on_resolve {
         .                       self.players[caster].cards_exiled_this_turn =
         .                           self.players[caster].cards_exiled_this_turn.saturating_add(1);
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // CR 702.170 — Lilah, Undefeated Slickshot: exile the resolving spell
         .                   // plotted instead of routing it to the graveyard.
     2,864 ( 0.00%)          if card.plot_on_resolve {
         .                       let cid = card.id;
         .                       self.players[caster].cards_exiled_this_turn =
         .                           self.players[caster].cards_exiled_this_turn.saturating_add(1);
         .                       self.exile.push(card);
         .                       self.plotted_cards.insert(cid);
         .                       self.plotted_this_turn.insert(cid);
         .                       self.fire_becomes_plotted_triggers(cid, caster);
         .                       return Ok(events);
         .                   }
         .                   // Feather, the Redeemed — exile the resolving I/S instead of the
         .                   // graveyard and schedule its return to the caster's hand at the next
         .                   // end step (CR 603.7b).
     2,864 ( 0.00%)          if card.feather_exile_return {
         .                       use crate::game::types::{DelayedKind, DelayedTrigger};
         .                       let src = card.id;
         .                       self.players[caster].cards_exiled_this_turn =
         .                           self.players[caster].cards_exiled_this_turn.saturating_add(1);
         .                       self.exile.push(card);
         .                       self.delayed_triggers.push(DelayedTrigger {
         .                           controller: caster,
         .                           source: src,
-- line 19571 ----------------------------------------
-- line 19580 ----------------------------------------
         .                           fires_once: true,
         .                           expires_after_turn: None,
         .                       });
         .                       return Ok(events);
         .                   }
         .                   // CR 702.55 — Haunt. `Effect::HauntCreature` set `haunt_pending` to the
         .                   // creature this resolving instant/sorcery should haunt. Exile the spell
         .                   // card (not the graveyard) and register the death-watch delayed trigger.
     2,864 ( 0.00%)          if let Some((haunted, body)) = self.haunt_pending.take() {
         .                       use crate::game::types::{DelayedKind, DelayedTrigger};
         .                       let src = card.id;
         .                       self.exile.push(card);
         .                       self.delayed_triggers.push(DelayedTrigger {
         .                           controller: caster,
         .                           source: src,
         .                           kind: DelayedKind::WhenHauntedCreatureDies(haunted),
         .                           effect: body,
-- line 19596 ----------------------------------------
-- line 19600 ----------------------------------------
         .                           fires_once: true,
         .                           expires_after_turn: None,
         .                       });
         .                       return Ok(events);
         .                   }
         .                   // CR 702.46 — Cipher. `Effect::Cipher` set `cipher_encode_pending` to
         .                   // the creature this spell should be encoded on. Route the card to exile
         .                   // with `encoded_on` stamped instead of the graveyard.
     2,864 ( 0.00%)          if let Some(creature) = self.cipher_encode_pending.take() {
         .                       let mut card = card;
         .                       card.encoded_on = Some(creature);
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // Beacon cycle: "Shuffle this card into its owner's library."
         .                   // `Effect::ShuffleSelfIntoLibrary` flagged the resolving spell — route
         .                   // it to its owner's library and shuffle instead of the graveyard.
     2,864 ( 0.00%)          if self.shuffle_resolving_spell_into_library {
         .                       self.shuffle_resolving_spell_into_library = false;
         .                       let owner = card.owner;
         .                       self.players[owner].library.push(card);
         .                       self.shuffle_library(owner, &mut events);
         .                       return Ok(events);
         .                   }
         .                   // Approach of the Second Sun — "put this card into its owner's
         .                   // library seventh from the top."
     2,864 ( 0.00%)          if let Some(from_top) = self.resolving_spell_library_from_top.take() {
         .                       let owner = card.owner;
         .                       let idx = (from_top as usize).min(self.players[owner].library.len());
         .                       self.players[owner].library.insert(idx, card);
         .                       return Ok(events);
         .                   }
         .                   // Revel in Silence's "exile this" rider.
     2,864 ( 0.00%)          if self.exile_resolving_spell {
         .                       self.exile_resolving_spell = false;
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // Journey to the Oracle's "return this to its owner's hand" rider.
     2,864 ( 0.00%)          if self.return_resolving_spell_to_hand {
         .                       self.return_resolving_spell_to_hand = false;
         .                       let owner = card.owner;
         .                       self.players[owner].hand.push(card);
         .                       return Ok(events);
         .                   }
         .                   // Buyback (CR 702.27e): a spell cast paying its buyback cost returns
         .                   // to its owner's hand instead of the graveyard as it resolves.
     2,864 ( 0.00%)          if card.bought_back {
         .                       let owner = card.owner;
         .                       self.players[owner].hand.push(card);
         .                       return Ok(events);
         .                   }
         .                   // CR 702.127e — an Aftermath half (right half cast from the graveyard)
         .                   // is exiled on resolution rather than returning to the graveyard.
         .                   if card.split_cast == Some(1)
         .                       && card.definition.split.as_ref().is_some_and(|s| s.aftermath)
         .                   {
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // CR 715 — an adventure spell goes to exile (not the graveyard) on
         .                   // resolution, marked so its creature half can be cast from exile.
     2,864 ( 0.00%)          if card.adventuring {
         .                       let mut card = card;
         .                       card.adventuring = false;
         .                       card.on_adventure = true;
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // CR 724.1a — a spell that ended the turn is exiled along with the
         .                   // rest of the stack instead of going to the graveyard (Day's
         .                   // Undoing). The flag stays set; `resolve_top_of_stack` consumes it.
     2,864 ( 0.00%)          if self.end_turn_requested {
         .                       self.exile.push(card);
         .                       return Ok(events);
         .                   }
         .                   // All Hallow's Eve — the spell exiled itself instead of resolving into
         .                   // the graveyard; light the fuse as it lands (CR 400.7).
     2,864 ( 0.00%)          if self.exile_resolving_spell_with_countdown
         .                       && let Some(fuse) = card.definition.exile_countdown.clone()
         .                   {
         .                       self.exile_resolving_spell_with_countdown = false;
         .                       let mut card = card;
         .                       let card_id = card.id;
         .                       card.add_counters(fuse.counter, fuse.count);
         .                       self.exile.push(card);
         .                       events.push(GameEvent::PermanentExiled { card_id });
-- line 19686 ----------------------------------------
-- line 19688 ----------------------------------------
         .                           card_id,
         .                           counter_type: fuse.counter,
         .                           count: fuse.count,
         .                       });
         .                       return Ok(events);
         .                   }
         .                   // CR 614.6 — an instant/sorcery bound for the graveyard is exiled
         .                   // instead under Rest in Peace / Leyline of the Void.
     4,296 ( 0.00%)          self.route_to_graveyard(card, &mut events);
 2,562,672 ( 0.08%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::route_to_graveyard (1,432x)
     8,592 ( 0.00%)          Ok(events)
    20,070 ( 0.00%)      }
   291,488 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::effects::EffectContext> (1,432x)
   244,592 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (1,434x)
         .           
         .               /// Resolve a triggered ability's effect tree, carrying the
         .               /// trigger's "source entity" (the just-cast spell, the dying
         .               /// creature, etc.) into `ctx.trigger_source`. Used by spell-cast
         .               /// triggers whose body looks up the cast spell on the stack
         .               /// (e.g. Aziza's Magecraft copy, Conciliator's Duelist's Repartee
         .               /// exile-target). When `trigger_source_ent` is `None`, falls back
         .               /// to the legacy behavior (trigger_source = source permanent).
         .               ///
         .               /// `event_amount` carries the firing event's amount (life gained,
         .               /// life lost, damage dealt, …) so trigger bodies can read it via
         .               /// `Value::TriggerEventAmount` — used by Light of Promise's
         .               /// "Whenever you gain life, put that many +1/+1 counters …".
         .               #[allow(clippy::too_many_arguments)]
         .               #[allow(clippy::too_many_arguments)]
     7,224 ( 0.00%)      pub fn continue_trigger_resolution_with_source(
         .                   &mut self,
         .                   source: CardId,
         .                   controller: usize,
         .                   effect: crate::effect::Effect,
         .                   target: Option<Target>,
         .                   mode: usize,
         .                   x_value: u32,
         .                   converged_value: u32,
-- line 19722 ----------------------------------------
-- line 19723 ----------------------------------------
         .                   mana_spent: u32,
         .                   trigger_source_ent: Option<crate::game::effects::EntityRef>,
         .                   event_amount: u32,
         .                   trigger_player: Option<usize>,
         .                   additional_targets: Vec<Target>,
         .               ) -> Result<Vec<GameEvent>, GameError> {
         .                   // Event-amount-relative filters re-checked at resolution
         .                   // (ManaValueLessThanEventAmount) read this scratch.
       516 ( 0.00%)          self.trigger_event_amount_scratch = event_amount;
         .                   // The seat the firing event named: the stamped `trigger_player` (the
         .                   // damaged seat for a combat-damage trigger), else a player subject or
         .                   // a player target. Read back by `ControlledByTriggerPlayer`.
     1,032 ( 0.00%)          self.trigger_event_player_scratch = trigger_player
     2,580 ( 0.00%)              .or(match trigger_source_ent {
         .                           Some(crate::game::effects::EntityRef::Player(p)) => Some(p),
         .                           _ => None,
         .                       })
       516 ( 0.00%)              .or(match target.as_ref() {
         .                           Some(Target::Player(p)) => Some(*p),
         .                           _ => None,
         .                       });
         .                   // CR 608.2b — if the trigger's stored sole target is no longer legal
         .                   // at resolution (left the zone, stopped matching the filter), the
         .                   // ability doesn't resolve: none of its effects happen. It must NOT
         .                   // re-aim at a fresh target. Concretize BOTH runtime atoms — X and
         .                   // converge — before evaluating; an unresolved `ManaValueAtMost
         .                   // Converged` reads false-for-everything and fizzled every
         .                   // correctly-chosen Sundering Archaic target.
         .                   let resolved_target = match target.as_ref() {
         .                       Some(t) => match effect
     1,032 ( 0.00%)                  .target_filter_for_slot(0)
    30,100 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot (516x)
         .                           .map(|f| f.resolve_x(x_value).resolve_converge(converged_value))
         .                       {
         .                           Some(filter)
         .                               if !self.evaluate_requirement_static(&filter, t, controller, Some(source)) =>
         .                           {
         .                               return Ok(vec![]);
         .                           }
     2,064 ( 0.00%)                  _ => Some(t.clone()),
         .                       },
         .                       None => None,
         .                   };
         .                   let mut ctx =
         .                       EffectContext::for_trigger(source, controller, resolved_target.clone(), mode);
         .                   // Append slot-1+ targets (two-target activated abilities) after slot 0.
         .                   ctx.targets.extend(additional_targets.iter().cloned());
     1,032 ( 0.00%)          ctx.x_value = x_value;
     1,032 ( 0.00%)          ctx.converged_value = converged_value;
     2,064 ( 0.00%)          ctx.mana_spent_by_color = std::mem::take(&mut self.activation_mana_colors_scratch);
         .                   // CR 702.32 — an ETB/other trigger on a permanent reads the
         .                   // source's `kicked` flag so "when ~ enters, if it was kicked, …"
         .                   // riders (Goblin Bushwhacker) can branch on `SpellWasKicked`.
       516 ( 0.00%)          if let Some(src) = self.battlefield.iter().find(|c| c.id == source) {
       812 ( 0.00%)              ctx.kicked = src.kicked;
     1,218 ( 0.00%)              ctx.kicked_options = src.kicked_options.clone();
       812 ( 0.00%)              ctx.kick_count = src.kick_count;
       812 ( 0.00%)              ctx.bargained = src.bargained;
         .                       // "Escapes with …" / cast-zone riders (Tizerus Charger) read
         .                       // the source's cast zone through `Predicate::CastFromGraveyard`.
       812 ( 0.00%)              ctx.cast_from_hand = src.cast_from_hand;
         .                       // CR 701.59 — a self-ETB trigger reads whether the collect-evidence
         .                       // cost was paid ("if evidence was collected" — Crimestopper Sprite).
     1,624 ( 0.00%)              ctx.cast_collected_evidence = src.cast_collected_evidence;
         .                   }
       516 ( 0.00%)          if let Some(ts) = trigger_source_ent {
       402 ( 0.00%)              ctx.trigger_source = Some(ts);
         .                   }
       516 ( 0.00%)          ctx.mana_spent = mana_spent;
     1,548 ( 0.00%)          ctx.event_amount = event_amount;
     3,612 ( 0.00%)          let events = self.resolve_effect(&effect, &ctx)?;
 2,270,816 ( 0.07%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect (516x)
     1,032 ( 0.00%)          if let Some((decision, in_progress, remaining)) = self.suspend_signal.take() {
         .                       self.pending_decision = Some(PendingDecision {
         .                           decision,
         .                           resume: ResumeContext::Trigger {
         .                               source,
         .                               controller,
         .                               target,
         .                               mode,
         .                               in_progress,
-- line 19801 ----------------------------------------
-- line 19805 ----------------------------------------
         .                               mana_spent,
         .                               trigger_source_ent,
         .                               event_amount,
         .                               trigger_player,
         .                               additional_targets,
         .                           },
         .                       });
         .                   }
     2,580 ( 0.00%)          Ok(events)
     5,160 ( 0.00%)      }
    39,714 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (516x)
         .           
         .               /// Resolve an activated ability's effect tree.
         .               pub(crate) fn continue_ability_resolution(
         .                   &mut self,
         .                   source: CardId,
         .                   controller: usize,
         .                   effect: crate::effect::Effect,
         .                   target: Option<Target>,
-- line 19822 ----------------------------------------
-- line 19823 ----------------------------------------
         .               ) -> Result<Vec<GameEvent>, GameError> {
         .                   self.continue_ability_resolution_x(source, controller, effect, target, 0)
         .               }
         .           
         .               /// `continue_ability_resolution` with the activation's chosen X threaded
         .               /// in, so an inline-resolving mana ability's body can read
         .               /// `Value::XFromCost` (the MMQ storage lands' "remove any number of
         .               /// storage counters: add that much mana").
   219,552 ( 0.01%)      pub(crate) fn continue_ability_resolution_x(
         .                   &mut self,
         .                   source: CardId,
         .                   controller: usize,
         .                   effect: crate::effect::Effect,
         .                   target: Option<Target>,
         .                   x_value: u32,
         .               ) -> Result<Vec<GameEvent>, GameError> {
         .                   let mut ctx = EffectContext::for_ability(source, controller, target.clone());
    54,888 ( 0.00%)          ctx.x_value = x_value;
   128,072 ( 0.00%)          let events = self.resolve_effect(&effect, &ctx)?;
25,780,118 ( 0.83%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_effect (18,296x)
    36,592 ( 0.00%)          if let Some((decision, in_progress, remaining)) = self.suspend_signal.take() {
         .                       self.pending_decision = Some(PendingDecision {
         .                           decision,
         .                           resume: ResumeContext::Ability {
         .                               source,
         .                               controller,
         .                               target,
         .                               in_progress,
         .                               remaining,
         .                           },
         .                       });
         .                   }
    91,480 ( 0.00%)          Ok(events)
   164,664 ( 0.01%)      }
 2,763,368 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (18,296x)
         .           
         .               /// Evaluate whether `target` satisfies `req` given the current game state.
         .               ///
         .               /// `controller` is the player who controls the spell or ability (used for
         .               /// `ControlledByYou` / `ControlledByOpponent` checks).
         .               pub fn evaluate_requirement(
         .                   &self,
         .                   req: &SelectionRequirement,
         .                   target: &Target,
         .                   controller: usize,
         .               ) -> bool {
    21,588 ( 0.00%)          self.evaluate_requirement_static(req, target, controller, None)
27,426,105 ( 0.88%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (3,598x)
         .               }
         .           
         .               pub fn battlefield_find(&self, id: CardId) -> Option<&CardInstance> {
         .                   self.battlefield.iter().find(|c| c.id == id)
         .               }
         .           
         .               /// CR 700.4 — true when a death's graveyard placement was replaced away
         .               /// (Finality/void exile, Rest in Peace, Valentin, Pulmonic Sliver's
-- line 19875 ----------------------------------------
-- line 19884 ----------------------------------------
         .                           .any(|p| p.library.iter().any(|c| c.id == card_id))
         .               }
         .           
         .               /// The firing event's magnitude for `Value::TriggerEventAmount` /
         .               /// `ManaValueLessThanEventAmount`. Mostly the event payload's `amount`;
         .               /// for died events it's the dying card's mana value (Scrap Trawler's
         .               /// "lesser mana value than that artifact"), read from the death
         .               /// snapshot cache (tokens are already gone from every zone).
    10,260 ( 0.00%)      pub(crate) fn event_amount_for(&self, ev: &GameEvent) -> u32 {
    27,360 ( 0.00%)          match ev {
         .                       GameEvent::CreatureDied { card_id } => self
         .                           .died_card_snapshots
         .                           .get(card_id)
         .                           .or_else(|| self.find_card_anywhere(*card_id))
         .                           .map(|c| c.definition.cost.cmc())
         .                           .unwrap_or(0),
         .                       // "with lesser mana value than the creature that entered" (Clement).
         .                       GameEvent::PermanentEntered { card_id } => self
-- line 19901 ----------------------------------------
-- line 20017 ----------------------------------------
         .                   for card in taken.iter_mut() {
         .                       card.owner = winner;
         .                       card.controller = winner;
         .                   }
         .                   self.players[winner].ante.extend(taken);
         .               }
         .           
         .               pub(crate) fn take_card(zone: &mut Vec<CardInstance>, id: CardId) -> Option<CardInstance> {
     7,792 ( 0.00%)          let pos = zone.iter().position(|c| c.id == id)?;
         .                   Some(zone.remove(pos))
         .               }
         .           
         .               /// Which graveyard `p` may currently play `card_id` out of: their own, or
         .               /// — while Shaman's Trance has pooled the graveyards for them — whichever
         .               /// seat's graveyard holds it.
         .               pub(crate) fn playable_graveyard_seat(&self, p: usize, card_id: CardId) -> Option<usize> {
         .                   if self.players[p].graveyard.iter().any(|c| c.id == card_id) {
-- line 20033 ----------------------------------------
-- line 20131 ----------------------------------------
         .                   }
         .                   self.computed_permanent(card.id).map(|cp| cp.toughness).unwrap_or_else(|| card.toughness())
         .               }
         .           
         .               pub fn find_card_anywhere(&self, id: CardId) -> Option<&CardInstance> {
         .                   if let Some(c) = self.battlefield_find(id) {
         .                       return Some(c);
         .                   }
   171,680 ( 0.01%)          for p in &self.players {
         .                       if let Some(c) = p.graveyard.iter().find(|c| c.id == id) {
         .                           return Some(c);
         .                       }
         .                       if let Some(c) = p.hand.iter().find(|c| c.id == id) {
         .                           return Some(c);
         .                       }
         .                       if let Some(c) = p.library.iter().find(|c| c.id == id) {
         .                           return Some(c);
-- line 20147 ----------------------------------------
-- line 20151 ----------------------------------------
         .                       if let Some(c) = p.ante.iter().find(|c| c.id == id) {
         .                           return Some(c);
         .                       }
         .                   }
         .                   if let Some(c) = self.exile.iter().find(|c| c.id == id) {
         .                       return Some(c);
         .                   }
         .                   for si in &self.stack {
    46,068 ( 0.00%)              if let crate::game::types::StackItem::Spell { card, .. } = si
    67,170 ( 0.00%)                  && card.id == id
         .                       {
         .                           return Some(card);
         .                       }
         .                   }
         .                   None
   104,240 ( 0.00%)      }
         .           
         .               /// Mutable variant of `find_card_anywhere` — walks battlefield,
         .               /// each player's hand/library/graveyard, and exile (in that order).
         .               /// Used by `Effect::GrantMayPlay` to stamp `may_play_until` on a
         .               /// card regardless of where the granting effect happens to find it.
    23,292 ( 0.00%)      pub fn find_card_anywhere_mut(
         .                   &mut self,
         .                   id: CardId,
         .               ) -> Option<&mut CardInstance> {
     7,764 ( 0.00%)          if self.battlefield.iter().any(|c| c.id == id) {
         .                       return self.battlefield.iter_mut().find(|c| c.id == id);
         .                   }
         .                   // Locate first with shared borrows, then take the one `&mut`: `Player`
         .                   // is a CoW handle, so an `iter_mut()` per zone would both borrow the
         .                   // whole seat *and* unshare it on every miss.
         .                   let found = self.players.iter().enumerate().find_map(|(pi, p)| {
         .                       let zone = |z: usize, v: &CowBox<Vec<CardInstance>>| {
         .                           v.iter().position(|c| c.id == id).map(|i| (pi, z, i))
         .                       };
     5,808 ( 0.00%)              zone(0, &p.hand)
     5,808 ( 0.00%)                  .or_else(|| zone(1, &p.graveyard))
     2,002 ( 0.00%)                  .or_else(|| zone(2, &p.library))
     2,002 ( 0.00%)                  .or_else(|| zone(3, &p.ante))
         .                   });
         .                   if let Some((pi, z, i)) = found {
         .                       let p = &mut self.players[pi];
    11,418 ( 0.00%)              return Some(match z {
         .                           0 => &mut p.hand[i],
     3,806 ( 0.00%)                  1 => &mut p.graveyard[i],
         .                           2 => &mut p.library[i],
         .                           _ => &mut p.ante[i],
         .                       });
         .                   }
        76 ( 0.00%)          if let Some(c) = self.exile.iter_mut().find(|c| c.id == id) {
         .                       return Some(c);
         .                   }
         .                   // A spell resolving its own effect (Spoils of the Vault's NameCard)
         .                   // is still on the stack.
         .                   for si in &mut self.stack {
         .                       if let crate::game::types::StackItem::Spell { card, .. } = si
         .                           && card.id == id
         .                       {
         .                           return Some(card);
         .                       }
         .                   }
         .                   None
    23,292 ( 0.00%)      }
         .           
         .               /// Look up which zone a card currently occupies. Returns `None` if
         .               /// the card isn't in any visible zone (battlefield, hand, library,
         .               /// graveyard, exile, stack). Used by the cast-from-zone path to
         .               /// confirm the card is still in the expected zone before lifting it.
         .               pub(crate) fn find_card_zone(&self, id: CardId) -> Option<crate::card::Zone> {
         .                   use crate::card::Zone;
         .                   if self.battlefield.iter().any(|c| c.id == id) {
-- line 20221 ----------------------------------------
-- line 20243 ----------------------------------------
         .           
         .               /// Look up the owner (seat index) of `id` across every public zone:
         .               /// battlefield, each player's graveyard, each player's hand, the
         .               /// stack, and exile. Returns `None` if no card with that id exists
         .               /// in any visible zone. Used by `PlayerRef::OwnerOf(...)` resolution
         .               /// to find the original owner of a target whose card has changed
         .               /// zones (e.g. destroyed and now in graveyard) by the time the
         .               /// owner-targeted effect resolves.
       256 ( 0.00%)      pub(crate) fn find_card_owner(&self, id: CardId) -> Option<usize> {
         .                   if let Some(c) = self.battlefield_find(id) {
         .                       return Some(c.owner);
         .                   }
         .                   for (i, p) in self.players.iter().enumerate() {
         .                       if p.graveyard.iter().any(|c| c.id == id)
         .                           || p.hand.iter().any(|c| c.id == id)
         .                           || p.library.iter().any(|c| c.id == id)
         .                       {
-- line 20259 ----------------------------------------
-- line 20271 ----------------------------------------
         .                   for item in &self.stack {
         .                       if let crate::game::types::StackItem::Spell { card, .. } = item
         .                           && card.id == id
         .                       {
         .                           return Some(card.owner);
         .                       }
         .                   }
         .                   None
       384 ( 0.00%)      }
         .           
         .               /// Look up the caster (current controller) of a stack-resident spell
         .               /// by card id. Used by `PlayerRef::ControllerOf` to resolve "this
         .               /// spell's controller" — distinct from `find_card_owner`, which
         .               /// returns the printed `owner` even on the stack. Returns `None` if
         .               /// `id` is not currently a spell on the stack.
         .               pub(crate) fn stack_caster_for_card(&self, id: CardId) -> Option<usize> {
         .                   for item in &self.stack {
         6 ( 0.00%)              if let crate::game::types::StackItem::Spell { card, caster, .. } = item
         4 ( 0.00%)                  && card.id == id
         .                       {
         .                           return Some(*caster);
         .                       }
         .                   }
         .                   None
         .               }
         .           
         .               /// Returns true if the permanent `id` has `kw` after all layer effects are applied.
         .               /// Falls back to `false` if the permanent is not on the battlefield.
    49,968 ( 0.00%)      pub fn permanent_has_keyword(&self, id: CardId, kw: &Keyword) -> bool {
     8,328 ( 0.00%)          self.computed_permanent(id)
11,803,928 ( 0.38%)  => crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (8,328x)
         .                       .is_some_and(|c| c.keywords.contains(kw))
    58,296 ( 0.00%)      }
         .           }
         .           
         .           /// Whether `ev` is already handled by a hardcoded trigger site for the
         .           /// given `spec.scope`. Dispatched triggers should skip events for which
         .           /// the hardcoded site would already fire — but other scopes still need
         .           /// the unified dispatcher.
         .           ///
         .           /// Coverage of hardcoded sites:
-- line 20310 ----------------------------------------
-- line 20315 ----------------------------------------
         .           ///   plus `fire_spell_cast_triggers` (YourControl/AnyPlayer)
         .           /// - `StepBegins` (any scope) → `fire_step_triggers`
         .           ///
         .           /// Non-SelfSource scopes for ETB / Attacks / CreatureDied are NOT covered
         .           /// by a hardcoded site and need the unified dispatcher (Temur Ascendancy's
         .           /// "another creature you control enters" trigger, etc.).
         .           fn is_event_hardcoded(ev: &GameEvent, spec: &crate::effect::EventSpec) -> bool {
         .               use crate::effect::EventScope;
   682,136 ( 0.02%)      match ev {
         .                   GameEvent::PermanentEntered { .. } => matches!(spec.scope, EventScope::SelfSource),
         .                   // SelfSource mutate triggers are pushed inline by `resolve_top_of_stack`.
         .                   GameEvent::Mutated { .. } => matches!(spec.scope, EventScope::SelfSource),
         .                   GameEvent::AttackerDeclared(_) => matches!(spec.scope, EventScope::SelfSource),
     2,864 ( 0.00%)          GameEvent::CreatureDied { .. } => matches!(spec.scope, EventScope::SelfSource),
         .                   GameEvent::SpellCast { .. } => true,
         .                   GameEvent::StepChanged(_) => true,
         .                   _ => false,
         .               }
         .           }
         .           
         .           /// Extract the per-event scalar amount carried by `event` — the life
         .           /// gained on a `LifeGained`, life lost on a `LifeLost`, the count of
-- line 20336 ----------------------------------------
-- line 22122 ----------------------------------------
         .           }
         .           
         .           
         .           // ── Helpers ───────────────────────────────────────────────────────────────────
         .           
         .           /// Returns true if `blocker` is legally allowed to block `attacker`.
         .           /// Uses `blocker_kws` / `attacker_kws` as the effective keyword sets
         .           /// (from `ComputedPermanent`) instead of the raw definition keywords.
   119,664 ( 0.00%)  pub fn can_block_attacker_computed(
         .               blocker: &CardInstance,
         .               blocker_computed: &ComputedPermanent,
         .               attacker_kws: &[Keyword],
         .               attacker_colors: crate::mana::ColorSet,
         .               attacker_power: i32,
         .           ) -> bool {
         .               let blocker_kws = &blocker_computed.keywords;
         .               // Unblockable: can't be blocked at all.
-- line 22138 ----------------------------------------
-- line 22179 ----------------------------------------
         .               if attacker_kws.contains(&Keyword::CantBeBlockedByPowerLess)
         .                   && blocker_computed.power < attacker_power
         .               {
         .                   return false;
         .               }
         .               // Questing Beast (CR 509.1b): can't be blocked by creatures with power N
         .               // or less — a fixed threshold, not relative to the attacker's power.
         .               if attacker_kws.iter().any(|k| {
    28,590 ( 0.00%)          matches!(k, Keyword::CantBeBlockedByPowerAtMost(n) if blocker_computed.power <= *n as i32)
         .               }) {
         .                   return false;
         .               }
         .               // Juggernaut (CR 509.1b): can't be blocked by a given creature type.
         .               if attacker_kws.iter().any(|k| {
    19,060 ( 0.00%)          matches!(k, Keyword::CantBeBlockedByCreatureType(t)
         .                       if blocker_computed.subtypes.creature_types.contains(t))
         .               }) {
         .                   return false;
         .               }
         .               // Squeak By (CR 509.1b): can't be blocked by creatures with power N or
         .               // greater — the fixed-threshold mirror of `CantBeBlockedByPowerAtMost`.
         .               if attacker_kws.iter().any(|k| {
    28,590 ( 0.00%)          matches!(k, Keyword::CantBeBlockedByPowerAtLeast(n) if blocker_computed.power >= *n as i32)
         .               }) {
         .                   return false;
         .               }
         .               // Ironclaw Orcs (CR 509.1b): this blocker can't block creatures with power
         .               // N or greater — a restriction on the blocker keyed off the attacker.
         .               if blocker_kws.iter().any(|k| {
    37,170 ( 0.00%)          matches!(k, Keyword::CantBlockPowerAtLeast(n) if attacker_power >= *n as i32)
         .               }) {
         .                   return false;
         .               }
         .               // Sunweb (CR 509.1b): the low-power mirror — this blocker only stops real
         .               // threats.
         .               if blocker_kws
         .                   .iter()
    37,170 ( 0.00%)          .any(|k| matches!(k, Keyword::CantBlockPowerAtMost(n) if attacker_power <= *n as i32))
         .               {
         .                   return false;
         .               }
         .               // Spitfire Handler (CR 509.1b): the self-relative threshold.
         .               if blocker_kws.contains(&Keyword::CantBlockGreaterPowerThanSelf)
         .                   && attacker_power > blocker_computed.power
         .               {
         .                   return false;
-- line 22224 ----------------------------------------
-- line 22243 ----------------------------------------
         .                   if !blocker_is_artifact && !shares_color {
         .                       return false;
         .                   }
         .               }
         .               // Protection from a color (CR 702.16e): the attacker can't be blocked
         .               // by a creature of a color it has protection from. Read the blocker's
         .               // computed colors so hybrid-pip and effect-granted colors count.
         .               for kw in attacker_kws {
    38,600 ( 0.00%)          if let Keyword::Protection(color) = kw
         .                       && blocker_computed.colors.contains(color)
         .                   {
         .                       return false;
         .                   }
         .                   // CR 702.16e — "protection from its colors": can't be blocked by a
         .                   // creature sharing any of the attacker's own colors.
         .                   if matches!(kw, Keyword::ProtectionFromOwnColors)
         .                       && blocker_computed.colors.intersects(attacker_colors)
-- line 22259 ----------------------------------------
-- line 22310 ----------------------------------------
         .                   if matches!(kw, Keyword::ProtectionFromEverything) {
         .                       return false;
         .                   }
         .               }
         .               // CR 509.1b "can't be blocked except by [filter]" / "can't be blocked by
         .               // [filter]" — evaluate the blocker's computed characteristics against the
         .               // attacker's filter keywords.
         .               for kw in attacker_kws {
    47,650 ( 0.00%)          match kw {
         .                       Keyword::CantBeBlockedExceptBy(filter)
         .                           if !blocker_matches_block_filter(blocker, blocker_computed, filter) => {
         .                               return false;
         .                           }
         .                       Keyword::CantBeBlockedBy(filter)
         .                           if blocker_matches_block_filter(blocker, blocker_computed, filter) =>
         .                       {
         .                           return false;
         .                       }
         .                       _ => {}
         .                   }
         .               }
         .               true
    89,748 ( 0.00%)  }
         .           
         .           /// Lightweight evaluation of a block-restriction filter against a blocker's
         .           /// *computed* characteristics. Covers the subset of `SelectionRequirement`
         .           /// that "can't be blocked except by [filter]" cards actually use (type,
         .           /// color, keyword, power/toughness thresholds). Unsupported variants resolve
         .           /// to `false` (conservatively excluding the blocker).
         .           fn blocker_matches_block_filter(
         .               blocker: &CardInstance,
-- line 22340 ----------------------------------------

48,460,136 ( 1.55%)  <counts for unidentified lines in crabomination/src/game/mod.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/effects/mod.rs
--------------------------------------------------------------------------------
Ir               

-- line 168 ----------------------------------------
      .               pub entwined: bool,
      .               /// CR 702.172 — Spree mode indices chosen at cast time. Stamped from the
      .               /// resolving `CardInstance.spree_modes`; read by `Effect::Spree`. Empty
      .               /// for non-Spree contexts.
      .               pub spree_modes: Vec<u8>,
      .           }
      .           
      .           impl EffectContext {
  1,210 ( 0.00%)      pub fn for_spell(caster: usize, target: Option<Target>, mode: usize, x_value: u32) -> Self {
  1,980 ( 0.00%)          Self {
      .                       controller: caster,
      .                       source: None,
      .                       targets: target.into_iter().collect(),
      .                       trigger_source: None,
      .                       mode,
      .                       x_value,
      .                       converged_value: 0,
      .                       mana_spent: 0,
-- line 185 ----------------------------------------
-- line 192 ----------------------------------------
      .                       kick_count: 0,
      .                       bargained: false,
      .                       cast_via_mayhem: false,
      .                       cast_via_waterbend: false,
      .                       cast_collected_evidence: false,
      .                       entwined: false,
      .                       spree_modes: Vec::new(),
      .                   }
    990 ( 0.00%)      }
      .               /// Spell-resolution context with the resolving spell's
      .               /// `CardId` + printed name stamped onto `ctx.source` /
      .               /// `ctx.source_name` and all cast-time scalars (X, Converge, total
      .               /// mana spent) threaded in. Lets predicates that introspect the
      .               /// resolving spell (e.g. `Predicate::SameNamedInZoneAtLeast` for
      .               /// Dragon's Approach's "same name in your graveyard" rider) read
      .               /// the spell's identity without needing to find the card in any
      .               /// game zone — during spell resolution the card is in transient
-- line 208 ----------------------------------------
-- line 236 ----------------------------------------
      .           
      .               /// Variant of `for_spell_with_source` that also stamps the
      .               /// `cast_from_hand` flag so the resolution-time predicate
      .               /// `Predicate::CastFromGraveyard` (Increasing Vengeance) can read
      .               /// whether the spell came from hand vs graveyard / flashback. The
      .               /// no-origin sibling defaults to `cast_from_hand = true` since the
      .               /// vast majority of spell resolutions are hand casts.
      .               #[allow(clippy::too_many_arguments)]
 15,752 ( 0.00%)      pub fn for_spell_with_source_and_origin(
      .                   spell_card: CardId,
      .                   spell_name: &'static str,
      .                   caster: usize,
      .                   target: Option<Target>,
      .                   additional_targets: Vec<Target>,
      .                   mode: usize,
      .                   x_value: u32,
      .                   converged_value: u32,
-- line 252 ----------------------------------------
-- line 253 ----------------------------------------
      .                   mana_spent: u32,
      .                   cast_from_hand: bool,
      .               ) -> Self {
      .                   // Merge slot 0 (`target`) + slots 1+ (`additional_targets`) into
      .                   // a single `targets` Vec so `Selector::Target(n)` reads slot n
      .                   // for any n. Single-target spells leave `additional_targets`
      .                   // empty.
      .                   let mut targets: Vec<Target> = target.into_iter().collect();
  4,296 ( 0.00%)          targets.extend(additional_targets);
 40,096 ( 0.00%)          Self {
      .                       controller: caster,
      .                       source: Some(spell_card),
  7,160 ( 0.00%)              targets,
      .                       trigger_source: None,
      .                       mode,
      .                       x_value,
      .                       converged_value,
      .                       mana_spent,
      .                       mana_spent_by_color: Vec::new(),
      .                       source_name: Some(spell_name),
      .                       cast_from_hand,
-- line 273 ----------------------------------------
-- line 277 ----------------------------------------
      .                       kick_count: 0,
      .                       bargained: false,
      .                       cast_via_mayhem: false,
      .                       cast_via_waterbend: false,
      .                       cast_collected_evidence: false,
      .                       entwined: false,
      .                       spree_modes: Vec::new(),
      .                   }
 12,888 ( 0.00%)      }
      .               pub fn for_trigger(
      .                   source: CardId,
      .                   controller: usize,
      .                   target: Option<Target>,
      .                   mode: usize,
      .               ) -> Self {
 10,320 ( 0.00%)          Self {
      .                       controller,
      .                       source: Some(source),
      .                       targets: target.into_iter().collect(),
      .                       trigger_source: Some(EntityRef::Permanent(source)),
      .                       mode,
      .                       x_value: 0,
      .                       converged_value: 0,
      .                       mana_spent: 0,
-- line 300 ----------------------------------------
-- line 308 ----------------------------------------
      .                       bargained: false,
      .                       cast_via_mayhem: false,
      .                       cast_via_waterbend: false,
      .                       cast_collected_evidence: false,
      .                       entwined: false,
      .                       spree_modes: Vec::new(),
      .                   }
      .               }
 30,240 ( 0.00%)      pub fn for_ability(
      .                   source: CardId,
      .                   controller: usize,
      .                   target: Option<Target>,
      .               ) -> Self {
429,916 ( 0.01%)          Self {
      .                       controller,
      .                       source: Some(source),
      .                       targets: target.into_iter().collect(),
      .                       trigger_source: Some(EntityRef::Permanent(source)),
      .                       mode: 0,
      .                       x_value: 0,
      .                       converged_value: 0,
      .                       mana_spent: 0,
-- line 329 ----------------------------------------
-- line 336 ----------------------------------------
      .                       kick_count: 0,
      .                       bargained: false,
      .                       cast_via_mayhem: false,
      .                       cast_via_waterbend: false,
      .                       cast_collected_evidence: false,
      .                       entwined: false,
      .                       spree_modes: Vec::new(),
      .                   }
 26,460 ( 0.00%)      }
      .           }
      .           
      .           /// A resolved reference to something in the game (used internally for selector
      .           /// resolution and `ForEach` iteration).
    176 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
      .           pub enum EntityRef {
      .               Player(usize),
      .               Permanent(CardId),
      .               /// A card in a non-battlefield zone (library/graveyard/exile/hand).
      .               Card(CardId),
      .           }
      .           
      .           impl EntityRef {
      .               /// Extract the `CardId` if this entity is a battlefield permanent.
      .               pub fn as_permanent_id(&self) -> Option<CardId> {
    686 ( 0.00%)          match *self {
      .                       EntityRef::Permanent(c) => Some(c),
      .                       _ => None,
      .                   }
      .               }
      .               /// The `CardId` behind a card-or-permanent reference. Unlike
      .               /// `as_permanent_id`, this also unwraps `EntityRef::Card` — used by the
      .               /// counter-class effects, which match a spell on the stack. A cast
      .               /// spell's `Selector::TriggerSource` resolves to `EntityRef::Card`
      .               /// (see `event_subject`), so a SpellCast-triggered "counter that spell"
      .               /// (Chalice of the Void) needs this broader unwrap to find its target.
      .               pub fn as_card_id(&self) -> Option<CardId> {
    408 ( 0.00%)          match *self {
      .                       EntityRef::Permanent(c) | EntityRef::Card(c) => Some(c),
      .                       _ => None,
      .                   }
      .               }
      .           }
      .           
      .           impl GameState {
      .               /// Petals of Insight — look at the top `count`, then either bottom the
-- line 380 ----------------------------------------
-- line 1103 ----------------------------------------
      .                       .run_effect(chosen, ctx, events)
      .                       .and_then(|()| self.run_effect(other, ctx, events));
      .                   self.separated_piles = (Vec::new(), Vec::new());
      .                   run
      .               }
      .           
      .               /// Drop the multi-question replay log — call when a log-using effect
      .               /// reaches any completing path (see `ask_seat_bool`).
    792 ( 0.00%)      pub(crate) fn clear_answer_log(&mut self) {
      .                   self.resolution_answer_log.clear();
  1,056 ( 0.00%)      }
      .           
      .               /// CR 707.10 — push `n` copies of the spell `cid` (if it's on the
      .               /// stack and copyable) directly above it. Copies inherit the
      .               /// original's target / mode / x / converged value and are flagged
      .               /// uncounterable + token (so they cease to exist off the stack).
      .               /// When `choose_new_targets` is set (Reverberate / Fork), the copy's
      .               /// controller may repoint the primary target via `Decision::ChooseTarget`
      .               /// (the original target is offered first, so AutoDecider keeps it).
-- line 1121 ----------------------------------------
-- line 1414 ----------------------------------------
      .           
      .               /// CR 707 — apply a permanent's `enters_as_copy` replacement as it
      .               /// enters the battlefield. The controller picks which matching permanent
      .               /// to copy (`Decision::ChooseCards`); an unanswered prompt takes the
      .               /// highest-power match, which is also what a bot seat wants (the copier
      .               /// is typically a printed 0/0 that would otherwise die to SBA). No-op
      .               /// when nothing matches. Declining the printed "you may" isn't offered.
      .               /// Called from the spell-resolution ETB path before SBA.
 21,740 ( 0.00%)      pub(crate) fn apply_enters_as_copy(
      .                   &mut self,
      .                   card_id: CardId,
      .                   controller: usize,
      .                   events: &mut Vec<GameEvent>,
      .               ) -> bool {
  4,348 ( 0.00%)          let spec = self
      .                       .battlefield
      .                       .iter()
      .                       .find(|c| c.id == card_id)
      .                       .and_then(|c| c.definition.enters_as_copy.clone());
      .                   let Some(spec) = spec else { return false };
      .                   // Capture the copier's own printed name before the copy rewrite, for
      .                   // the CR 707.2 name-retention exception (Mockingbird).
      .                   let original_name: &'static str = self
-- line 1436 ----------------------------------------
-- line 1526 ----------------------------------------
      .                           def.name = original_name;
      .                       }
      .                       // CR 707.2e — strip Legendary so the copy dodges the legend rule.
      .                       if spec.non_legendary {
      .                           def.supertypes.retain(|s| *s != crate::card::Supertype::Legendary);
      .                       }
      .                   }
      .                   true
 19,566 ( 0.00%)      }
      .           
      .               /// CR 614 — apply a permanent's `enters_as_choice` replacement as it
      .               /// enters. The controller picks a mode via a `ChooseMode` decision; the
      .               /// chosen mode's P/T overwrite the printed base and its keywords are
      .               /// granted, all before the first SBA sweep (so a printed `*/*` body never
      .               /// dies as a 0/0). No-op when the card has no such spec. Corrupted
      .               /// Shapeshifter.
      .               /// CR 614 — resolve a permanent's `as_enters_effect` replacement during the
      .               /// battlefield hop. Runs before the first SBA sweep, so an effect the
      .               /// permanent's own CDA reads (Ixidron's mass turn-face-down) has already
      .               /// happened by the time its 0/0 body would be checked.
 10,870 ( 0.00%)      pub(crate) fn apply_as_enters_effect(&mut self, card_id: CardId) {
      .                   let Some((effect, controller, x)) = self.battlefield_find(card_id).and_then(|c| {
      .                       c.definition.as_enters_effect.clone().map(|e| (e, c.controller, c.cast_x_value))
      .                   }) else {
      .                       return;
      .                   };
      .                   // The replacement reads the cast's own `{X}` (Frankenstein's Monster).
      .                   let mut ctx = EffectContext::for_ability(card_id, controller, None);
      .                   ctx.x_value = x;
      .                   let _ = self.resolve_effect(&effect, &ctx);
 13,044 ( 0.00%)      }
      .           
      .               /// CR 701.28 — resolve the face a permanent just turned to's
      .               /// `as_transforms_effect`, inside the face swap. Uses `run_effect` (not
      .               /// `resolve_effect`) because the flip usually happens mid-resolution and
      .               /// the outer resolution's scratch must survive. Sephiroth's Super Nova.
      .               pub(crate) fn apply_as_transforms_effect(
      .                   &mut self,
      .                   card_id: CardId,
-- line 1564 ----------------------------------------
-- line 1570 ----------------------------------------
      .                   else {
      .                       return;
      .                   };
      .                   let ctx = EffectContext::for_ability(card_id, controller, None);
      .                   let _ = self.run_effect(&effect, &ctx, events);
      .               }
      .           
      .               pub(crate) fn apply_enters_as_choice(&mut self, card_id: CardId) -> bool {
  2,174 ( 0.00%)          let modes = self
      .                       .battlefield
      .                       .iter()
      .                       .find(|c| c.id == card_id)
      .                       .and_then(|c| c.definition.enters_as_choice.clone());
      .                   let Some(modes) = modes else { return false };
      .                   if modes.is_empty() {
      .                       return false;
      .                   }
-- line 1586 ----------------------------------------
-- line 1635 ----------------------------------------
      .           
      .               /// CR 614 — apply a permanent's `enter_modes` persistent mode choice as it
      .               /// enters. The controller picks a mode via a `ChooseMode` decision; that
      .               /// mode's triggered/static abilities and keywords are baked onto the
      .               /// permanent's definition (surviving as long as it stays on the
      .               /// battlefield) and the pick is recorded in `chosen_mode`. No-op when the
      .               /// card has no such spec. The Tarkir Siege cycle.
      .               pub(crate) fn apply_enters_mode_choice(&mut self, card_id: CardId) -> bool {
  2,174 ( 0.00%)          let modes = self
      .                       .battlefield
      .                       .iter()
      .                       .find(|c| c.id == card_id)
      .                       .and_then(|c| c.definition.enter_modes.clone());
      .                   let Some(modes) = modes else { return false };
      .                   if modes.is_empty() {
      .                       return false;
      .                   }
-- line 1651 ----------------------------------------
-- line 1792 ----------------------------------------
      .                       mode_texts: options.iter().map(|t| format!("{t:?}")).collect(),
      .                   }) {
      .                       DecisionAnswer::Mode(m) => m.min(options.len() - 1),
      .                       _ => 0,
      .                   };
      .                   options[n].clone()
      .               }
      .           
303,660 ( 0.01%)      pub fn resolve_effect(
      .                   &mut self,
      .                   effect: &Effect,
      .                   ctx: &EffectContext,
      .               ) -> Result<Vec<GameEvent>, GameError> {
      .                   // Reset sacrificed-power / sacrificed-toughness / sacrificed-mana-value
      .                   // scratch for this independent resolution.
 20,244 ( 0.00%)          self.sacrificed_power = None;
 20,244 ( 0.00%)          self.sacrificed_total_power = 0;
      .                   self.sacrificed_count = 0;
 20,244 ( 0.00%)          self.sacrificed_toughness = None;
 20,244 ( 0.00%)          self.sacrificed_mana_value = None;
 20,244 ( 0.00%)          self.last_discarded_mana_value = None;
 20,244 ( 0.00%)          self.last_revealed_from_hand = None;
 20,244 ( 0.00%)          self.tapped_for_cost_power = None;
      .                   // Everything this resolution causes is caused by this spell/ability
      .                   // (Pure Intentions' discard, Sacred Ground's land destruction).
 60,732 ( 0.00%)          self.resolution_causer = Some(ctx.controller);
 40,488 ( 0.00%)          self.source_name_scratch = ctx.source_name;
      .                   // Reset last-created-token scratch — `Selector::LastCreatedToken`
      .                   // (singular) and `Selector::LastCreatedTokens` (plural) only refer
      .                   // to tokens created by *this* resolution.
 20,244 ( 0.00%)          self.last_created_token = None;
      .                   self.last_created_tokens.clear();
      .                   // "Tokens created with this permanent" (Saproling Burst) — stamp the
      .                   // resolving source on every token this resolution mints.
 40,488 ( 0.00%)          self.token_minting_source = ctx.source;
 20,244 ( 0.00%)          self.chosen_creature_type_scratch = None;
      .                   // Reset last-moved-cards scratch — `Selector::LastMoved` only
      .                   // refers to cards moved by *this* resolution (Practiced
      .                   // Scrollsmith's ETB chains Move → GrantMayPlay on the same
      .                   // moved card via this scratch).
      .                   self.last_moved_cards.clear();
      .                   // Reset cards-discarded scratch — `Value::CardsDiscardedThisEffect`
      .                   // only counts discards from *this* resolution (Borrowed Knowledge
      .                   // mode 1's "draw cards equal to the number discarded this way").
 60,732 ( 0.00%)          self.cards_discarded_this_resolution = 0;
      .                   self.cards_drawn_this_resolution = 0;
      .                   self.energy_paid_this_resolution = 0;
      .                   self.permanents_returned_this_resolution = 0;
      .                   self.permanents_tapped_this_resolution = 0;
      .                   self.cards_revealed_this_resolution = 0;
      .                   self.creature_cards_discarded_this_resolution = 0;
      .                   self.greatest_discarded_mv_this_resolution = 0;
      .                   self.cards_discarded_per_player_this_resolution.clear();
      .                   self.nonland_cards_discarded_per_player_this_resolution.clear();
      .                   self.discarded_card_ids_this_resolution.clear();
      .                   self.exiled_card_ids_this_resolution.clear();
 20,244 ( 0.00%)          self.permanents_destroyed_this_resolution = 0;
      .                   self.destroyed_this_resolution.clear();
      .                   self.excess_damage_this_resolution = 0;
      .                   // Deaths caused by this resolution are tallied for the OUTERMOST
      .                   // resolution only — a sweeper's own kills can re-enter `resolve_effect`
      .                   // (replacements, death triggers) and would otherwise zero the running
      .                   // count mid-sweep (Hellfire).
 60,732 ( 0.00%)          if self.resolution_depth == 0 {
 20,244 ( 0.00%)              self.creatures_died_this_resolution = 0;
      .                   }
 40,488 ( 0.00%)          self.resolution_depth += 1;
 20,244 ( 0.00%)          self.damage_dealt_this_resolution = 0;
      .                   self.damaged_this_resolution.clear();
 20,244 ( 0.00%)          self.countered_spell_mana_spent = 0;
 20,244 ( 0.00%)          self.countered_spell_controller = None;
 20,244 ( 0.00%)          self.counters_removed_this_effect = 0;
      .                   self.countered_spell_mana_value = 0;
      .                   self.players_sacrificed_this_resolution.clear();
      .                   self.cards_sacrificed_this_resolution.clear();
 40,488 ( 0.00%)          self.named_card_this_resolution = None;
      .                   self.names_this_resolution.clear();
      .                   let mut events = vec![];
      .                   let prev_targets = std::mem::replace(
      .                       &mut self.resolution_targets,
      .                       ctx.targets
      .                           .iter()
      .                           .filter_map(|t| match t {
      .                               Target::Permanent(id) => Some(*id),
      .                               _ => None,
      .                           })
      .                           .collect(),
      .                   );
 80,976 ( 0.00%)          let ran = self.run_effect(effect, ctx, &mut events);
41,622,570 ( 1.34%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect (20,244x)
 60,732 ( 0.00%)          self.resolution_targets = prev_targets;
 40,488 ( 0.00%)          self.resolution_depth = self.resolution_depth.saturating_sub(1);
      .                   // Events outside a resolution (CR 514.3 cleanup discards, cost
      .                   // payments) have no causing spell or ability.
 80,976 ( 0.00%)          if self.resolution_depth == 0 {
 20,244 ( 0.00%)              self.resolution_causer = None;
      .                   }
 20,244 ( 0.00%)          ran?;
      .                   // Quina — a resolution that minted one or more tokens under a
      .                   // player's control mints one extra rider token per
      .                   // `TokenCreationAddsToken` static that player controls (single
      .                   // application per resolution, CR 614.13-flavor).
      .                   let minted_for: Vec<usize> = {
      .                       let mut v: Vec<usize> = self
      .                           .last_created_tokens
      .                           .iter()
      .                           .filter_map(|id| self.battlefield_find(*id).map(|c| c.controller))
      .                           .collect();
      .                       v.sort_unstable();
      .                       v.dedup();
 20,244 ( 0.00%)              v
      .                   };
      .                   for p in minted_for {
      .                       // Chatterfang scales by the tokens this resolution minted for
      .                       // `p`; count before the riders mint their own.
      .                       let minted_count = self
      .                           .last_created_tokens
      .                           .iter()
      .                           .filter(|id| {
-- line 1909 ----------------------------------------
-- line 1933 ----------------------------------------
      .                                   false,
      .                                   &mut events,
      .                               );
      .                           }
      .                       }
      .                   }
      .                   // CR 701.9 — emit a single "discarded one or more cards" batch per
      .                   // player who discarded during this resolution (Magmakin Artillerist).
 40,488 ( 0.00%)          if !self.cards_discarded_per_player_this_resolution.is_empty() {
      .                       let mut batches: Vec<(usize, u32)> = self
      .                           .cards_discarded_per_player_this_resolution
      .                           .iter()
      .                           .filter(|(_, n)| **n > 0)
      .                           .map(|(p, n)| (*p, *n))
      .                           .collect();
      .                       batches.sort_unstable_by_key(|(p, _)| *p);
      .                       for (player, count) in batches {
      .                           events.push(GameEvent::DiscardedBatch { player, count });
      .                       }
      .                   }
121,464 ( 0.00%)          Ok(events)
161,952 ( 0.01%)      }
      .           
      .               /// Sacrifice one permanent `id` controlled by `who` (CR 701.16): emit the
      .               /// sacrifice/death events (creature-specific ones first, then the generic
      .               /// `PermanentSacrificed`) and route it to the graveyard, firing dies/LTB
      .               /// triggers. Shared by the auto-pick path and the interactive
      .               /// `SacrificePending` resume so both behave identically.
      .               pub fn sacrifice_one(&mut self, id: CardId, who: usize, events: &mut Vec<GameEvent>) {
      .                   // Stamp the resolution scratch so `Value::Sacrificed*` and the
-- line 1962 ----------------------------------------
-- line 2071 ----------------------------------------
      .               }
      .           
      .               /// Destroy a single permanent (CR 701.7), honoring the destruction
      .               /// replacements in CR order: Indestructible (no-op), Shield counter
      .               /// (CR 122.1c), regeneration (CR 701.15, unless `no_regen`), and Umbra
      .               /// Armor (CR 702.89). Returns `true` when the permanent actually moved
      .               /// to the graveyard. Shared by `Effect::Destroy` and the Kill-Suit
      .               /// Cultist damage→destroy shield.
  3,784 ( 0.00%)      pub fn destroy_permanent(
      .                   &mut self,
      .                   cid: CardId,
      .                   no_regen: bool,
      .                   events: &mut Vec<GameEvent>,
      .               ) -> bool {
      .                   // Read the *computed* keyword so a layer-6 grant counts, not just
      .                   // the printed keyword / Indestructible counter on the raw instance.
    242 ( 0.00%)          let indestructible = self
    344 ( 0.00%)              .computed_permanent(cid)
641,474 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (344x)
      .                       .map(|cp| cp.keywords.contains(&crate::card::Keyword::Indestructible))
      .                       .unwrap_or(false)
    242 ( 0.00%)              || self.battlefield_find(cid).map(|c| c.is_indestructible()).unwrap_or(true);
 18,146 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::is_indestructible (242x)
    484 ( 0.00%)          if indestructible {
      .                       return false;
      .                   }
      .                   // CR 122.1c — a Shield counter replaces destruction, removing itself.
      .                   let has_shield = self
      .                       .battlefield_find(cid)
    726 ( 0.00%)              .map(|c| c.counter_count(crate::card::CounterType::Shield) > 0)
  2,420 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (242x)
      .                       .unwrap_or(false);
    242 ( 0.00%)          if has_shield {
      .                       if let Some(c) = self.battlefield_find_mut(cid) {
      .                           c.remove_counters(crate::card::CounterType::Shield, 1);
      .                           if c.counter_count(crate::card::CounterType::Shield) == 0 {
      .                               c.counters.remove(&crate::card::CounterType::Shield);
      .                           }
      .                       }
      .                       return false;
      .                   }
      .                   // CR 701.15 — regeneration shield replaces destruction (skipped for
      .                   // `DestroyNoRegen`, CR 701.15g).
    726 ( 0.00%)          if !no_regen
      .                       && self
      .                           .battlefield_find(cid)
    484 ( 0.00%)                  .map(|c| c.regeneration_shields > 0 && !c.cant_regenerate_this_turn)
      .                           .unwrap_or(false)
      .                   {
      .                       self.apply_regeneration(cid, events);
      .                       return false;
      .                   }
      .                   // CR 702.89 — umbra armor replaces destruction.
  1,452 ( 0.00%)          if self.apply_umbra_armor(cid, events) {
 36,486 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::apply_umbra_armor (242x)
      .                       return false;
      .                   }
    968 ( 0.00%)          let is_creature = self.permanent_is_creature(cid);
665,608 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::permanent_is_creature (242x)
      .                   // Cache the dying card's snapshot for AnotherOfYours-scope triggers,
      .                   // type filter predicates (token deaths vanish before dispatch) and
      .                   // post-destruction LKI reads of its counters (CR 603.10 — Dismantle).
      .                   // Snapshot every permanent, like the sacrifice path.
      .                   if let Some(c) = self.battlefield_find(cid) {
    242 ( 0.00%)              self.died_card_snapshots.insert(cid, c.clone());
      .                   }
    484 ( 0.00%)          if is_creature {
    484 ( 0.00%)              events.push(GameEvent::CreatureDied { card_id: cid });
      .                   }
    726 ( 0.00%)          let mut dies = self.remove_to_graveyard_with_triggers(cid);
2,406,046 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_to_graveyard_with_triggers (242x)
      .                   events.append(&mut dies);
    242 ( 0.00%)          self.permanents_destroyed_this_resolution =
    242 ( 0.00%)              self.permanents_destroyed_this_resolution.saturating_add(1);
    206 ( 0.00%)          self.destroyed_this_resolution.push(cid);
      .                   true
  2,752 ( 0.00%)      }
      .           
      .               /// Myr Incubator — "search your library for any number of [filter] cards,
      .               /// exile them, then create that many tokens. Then shuffle." Taking every
      .               /// match is strictly best, so the pick isn't surfaced.
      .               #[inline(never)]
      .               fn search_exile_then_tokens(
      .                   &mut self,
      .                   filter: &crate::card::SelectionRequirement,
-- line 2149 ----------------------------------------
-- line 2436 ----------------------------------------
      .                               ctx,
      .                               events,
      .                           )?;
      .                       }
      .                   }
      .                   Ok(())
      .               }
      .           
163,456 ( 0.01%)      fn run_effect(
      .                   &mut self,
      .                   effect: &Effect,
      .                   ctx: &EffectContext,
      .                   events: &mut Vec<GameEvent>,
      .               ) -> Result<(), GameError> {
122,592 ( 0.00%)          match effect {
      .                       Effect::Noop => Ok(()),
      .           
      .                       Effect::ExileRandomFromHandMayPlayThisTurn { who } => {
      .                           let Some(seat) = self.resolve_player(who, ctx) else { return Ok(()) };
      .                           let len = self.players[seat].hand.len();
      .                           if len == 0 {
      .                               return Ok(());
      .                           }
-- line 2458 ----------------------------------------
-- line 3714 ----------------------------------------
      .                           }
      .                           for cid in bounce {
      .                               self.move_card_to(cid, &ZoneDest::Hand(PlayerRef::OwnerOfMoved), ctx, events);
      .                           }
      .                           Ok(())
      .                       }
      .           
      .                       Effect::Seq(steps) => {
    376 ( 0.00%)                  for (idx, step) in steps.iter().enumerate() {
  1,128 ( 0.00%)                      self.run_effect(step, ctx, events)?;
1,016,281 ( 0.03%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect'2 (188x)
      .                               // A child effect signalled suspension — append the rest of
      .                               // this Seq after whatever remaining effects it already
      .                               // saved (the child's own continuation runs first).
      .                               if let Some((_, _, remaining)) = self.suspend_signal.as_mut() {
      .                                   let tail: Vec<Effect> = steps[idx + 1..].to_vec();
      .                                   if !tail.is_empty() {
      .                                       let carried = std::mem::replace(remaining, Effect::Noop);
      .                                       let mut combined = Vec::with_capacity(tail.len() + 1);
-- line 3731 ----------------------------------------
-- line 5901 ----------------------------------------
      .                               }
      .                               self.run_effect(then, ctx, events)?;
      .                           } else if let Some(e) = else_ {
      .                               self.run_effect(e, ctx, events)?;
      .                           }
      .                           Ok(())
      .                       }
      .           
  1,044 ( 0.00%)              Effect::DealDamage { to, amount } => {
    522 ( 0.00%)                  let amt = self.evaluate_value(amount, ctx).max(0) as u32;
  4,872 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_value (174x)
    174 ( 0.00%)                  if amt == 0 { return Ok(()); }
  1,914 ( 0.00%)                  for ent in self.resolve_selector(to, ctx) {
 43,325 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (174x)
      .                               // CR 702.90b — pass the source so infect routes
      .                               // player-target damage to poison counters.
  1,044 ( 0.00%)                      self.deal_damage_to_from(ent, amt, ctx.source, events);
1,880,779 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::deal_damage_to_from (174x)
      .                           }
    174 ( 0.00%)                  let mut sba = self.check_state_based_actions();
1,543,497 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (174x)
      .                           events.append(&mut sba);
      .                           Ok(())
      .                       }
      .           
      .                       Effect::DealDamageExcessTo { to, amount, excess_to, condition } => {
      .                           // CR 120.4a — the damage event is split before it's dealt: the
      .                           // creature takes exactly lethal and the remainder lands on
      .                           // `excess_to`.
-- line 5925 ----------------------------------------
-- line 6522 ----------------------------------------
      .                           for r in recipients {
      .                               self.deal_damage_to_from(r, power as u32, Some(src_id), events);
      .                           }
      .                           let mut sba = self.check_state_based_actions();
      .                           events.append(&mut sba);
      .                           Ok(())
      .                       }
      .           
    420 ( 0.00%)              Effect::GainLife { who, amount } => {
    282 ( 0.00%)                  let amt = self.evaluate_value(amount, ctx).max(0) as u32;
 57,676 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_value (94x)
     94 ( 0.00%)                  if amt == 0 { return Ok(()); }
    230 ( 0.00%)                  for ent in self.resolve_selector(who, ctx) {
 49,922 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (46x)
      .                               if let EntityRef::Player(p) = ent {
      .                                   // CR 119.10 — the event carries the APPLIED amount
      .                                   // (post replacement/bonus); a fully-replaced gain
      .                                   // fires no lifegain trigger.
    138 ( 0.00%)                          let applied = self.adjust_life_applied(p, amt as i32);
 48,649 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::adjust_life_applied (46x)
     92 ( 0.00%)                          if applied > 0 {
    184 ( 0.00%)                              events.push(GameEvent::LifeGained { player: p, amount: applied as u32 });
      .                                   } else if applied < 0 {
      .                                       // Tainted Remedy turned the gain into a loss.
      .                                       events.push(GameEvent::LifeLost { player: p, amount: (-applied) as u32 });
      .                                   }
      .                               }
      .                           }
      .                           Ok(())
      .                       }
-- line 6548 ----------------------------------------
-- line 7570 ----------------------------------------
      .                                   None,
      .                                   false,
      .                               )?;
      .                               events.extend(cast_events);
      .                           }
      .                           Ok(())
      .                       }
      .           
  1,200 ( 0.00%)              Effect::Draw { who, amount } => {
  2,000 ( 0.00%)                  let n = self.evaluate_value(amount, ctx).max(0) as usize;
 11,200 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_value (400x)
  2,400 ( 0.00%)                  for ent in self.resolve_selector(who, ctx) {
 95,704 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (400x)
      .                               if let EntityRef::Player(p) = ent {
      .                                   // CR 121.2b — a per-turn draw cap truncates the draw.
  2,000 ( 0.00%)                          let n = match self.draw_cap_for(p) {
128,936 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::draw_cap_for (400x)
      .                                       Some(cap) => {
      .                                           let remaining = (cap as usize)
      .                                               .saturating_sub(self.players[p].cards_drawn_this_turn as usize);
      .                                           n.min(remaining)
      .                                       }
      .                                       None => n,
      .                                   };
    400 ( 0.00%)                          for _ in 0..n {
      .                                       // `draw_one` applies the Dredge replacement
      .                                       // (CR 702.52) before falling back to a normal
      .                                       // draw; AutoDecider declines dredge by default.
  3,610 ( 0.00%)                              if !self.draw_one(p, events) {
3,444,953 ( 0.11%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::draw_one (722x)
      .                                           // CR 104.3c (or the Lab-Man win override);
      .                                           // the SBA pass decides game-over.
     32 ( 0.00%)                                  self.lose_to_empty_draw(p);
 20,768 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::lose_to_empty_draw (16x)
     16 ( 0.00%)                                  return Ok(());
      .                                       }
      .                                   }
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
      .                       Effect::DiscardHandDrawThatMany { who } => {
-- line 7607 ----------------------------------------
-- line 8369 ----------------------------------------
      .                           let mut revealed: Vec<(usize, CardId)> = Vec::new();
      .                           for seat in 0..self.players.len() {
      .                               if self.players[seat].hand.is_empty() {
      .                                   continue;
      .                               }
      .                               let candidates: Vec<(CardId, String)> = self.players[seat]
      .                                   .hand
      .                                   .iter()
  1,628 ( 0.00%)                          .map(|c| (c.id, c.definition.name.to_string()))
      .                                   .collect();
      .                               let auto = self.players[seat]
      .                                   .hand
      .                                   .iter()
      .                                   .filter(|c| c.definition.is_creature())
      .                                   .min_by_key(|c| c.definition.cost.cmc())
      .                                   .or_else(|| {
      .                                       self.players[seat].hand.iter().min_by_key(|c| c.definition.cost.cmc())
-- line 8385 ----------------------------------------
-- line 10044 ----------------------------------------
      .                                   explored_land: is_land == Some(true),
      .                               });
      .                           }
      .                           let mut sba = self.check_state_based_actions();
      .                           events.append(&mut sba);
      .                           Ok(())
      .                       }
      .           
 54,888 ( 0.00%)              Effect::AddMana { who, pool } => {
 91,480 ( 0.00%)                  let Some(p) = self.resolve_player(who, ctx) else { return Ok(()); };
457,400 ( 0.01%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_player (18,296x)
      .                           // Unwrap a spend-restriction wrapper. The inner payload
      .                           // resolves exactly as normal; each colored pip it produces
      .                           // is tagged with `restriction` so `pay_for_spell` can gate
      .                           // its use. Colorless pips stay unrestricted (no card needs
      .                           // restricted colorless mana).
292,736 ( 0.01%)                  let (pool, restriction) = match pool {
      .                               ManaPayload::Restricted(inner, r) => (inner.as_ref(), Some(*r)),
      .                               // Cavern of Souls: tag pips with the source's chosen
      .                               // creature type; no chosen type → unrestricted.
      .                               ManaPayload::RestrictedToChosenType(inner) => {
      .                                   let chosen = ctx
      .                                       .source
      .                                       .and_then(|cid| self.battlefield_find(cid))
      .                                       .and_then(|c| c.chosen_creature_type);
-- line 10067 ----------------------------------------
-- line 10082 ----------------------------------------
      .                                       chosen.map(crate::mana::SpendRestriction::CreatureOfType),
      .                                   )
      .                               }
      .                               other => (other, None),
      .                           };
      .                           // CR 701.10f / 614.5 — mana-production multiplier (Mana
      .                           // Reflection, Nyxbloom Ancient). 1× outside a tapped mana
      .                           // ability; 0 (serde default) also reads as 1×.
 36,592 ( 0.00%)                  let mult = self.mana_production_multiplier.max(1);
      .                           // False Dawn — every colour this seat would add becomes the
      .                           // recorded colour instead (CR 614).
 18,296 ( 0.00%)                  let recolor = self
      .                               .colored_mana_becomes_this_turn
      .                               .iter()
      .                               .find(|(seat, _)| *seat == p)
      .                               .map(|(_, c)| *c);
219,552 ( 0.01%)                  let add_one = |state: &mut Self, p: usize, c: Color| {
 18,296 ( 0.00%)                      let c = recolor.unwrap_or(c);
 73,184 ( 0.00%)                      match restriction {
      .                                   Some(r) => state.players[p].mana_pool.add_restricted(c, mult, r),
 54,888 ( 0.00%)                          None => state.players[p].mana_pool.add(c, mult),
548,880 ( 0.02%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (18,296x)
      .                               }
109,776 ( 0.00%)                  };
      .                           // Contamination — "if a land is tapped for mana, it produces
      .                           // [color] instead of any other type and amount." Replaces the
      .                           // whole payload for a land source.
 36,592 ( 0.00%)                  if ctx
      .                               .source
      .                               .and_then(|s| self.battlefield_find(s))
 36,592 ( 0.00%)                      .is_some_and(|c| c.definition.is_land())
238,418 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,296x)
 36,212 ( 0.00%)                      && let Some(color) = self
      .                                   .battlefield
      .                                   .iter()
 36,212 ( 0.00%)                          .find_map(|c| {
3,447,014 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (18,106x)
      .                                       c.definition.static_abilities.iter().find_map(|sa| match sa.effect {
      .                                           crate::effect::StaticEffect::LandsProduceColorInstead(col) => {
      .                                               Some(col)
      .                                           }
      .                                           _ => None,
      .                                       })
      .                                   })
      .                                   // Deep Water — the turn-scoped, controller-scoped twin.
 18,106 ( 0.00%)                          .or(self.players[p].lands_produce_color_this_turn)
      .                           {
      .                               add_one(self, p, color);
      .                               events.push(GameEvent::ManaAdded { player: p, color, source: ctx.source });
      .                               return Ok(());
      .                           }
      .                           // CR 605 — Pulse of Llanowar: your *basic* lands produce the
      .                           // enchantment's chosen colour instead. Scoped to the static's
      .                           // controller, so an opponent's basics are untouched.
 36,592 ( 0.00%)                  if ctx
      .                               .source
      .                               .and_then(|s| self.battlefield_find(s))
128,262 ( 0.00%)                      .is_some_and(|c| c.definition.is_land() && c.definition.is_basic())
238,418 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,296x)
235,378 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_basic (18,106x)
108,636 ( 0.00%)                      && let Some(color) = self.battlefield.iter().find_map(|c| {
4,944,344 ( 0.16%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find_map (18,106x)
656,812 ( 0.02%)                          (c.controller == p
      .                                       && c.definition.static_abilities.iter().any(|sa| {
      .                                           matches!(
      .                                               sa.effect,
      .                                               crate::effect::StaticEffect::YourBasicLandsProduceChosenColorInstead
      .                                           )
      .                                       }))
      .                                   .then_some(c.chosen_color)
      .                                   .flatten()
-- line 10146 ----------------------------------------
-- line 10149 ----------------------------------------
      .                               add_one(self, p, color);
      .                               events.push(GameEvent::ManaAdded { player: p, color, source: ctx.source });
      .                               return Ok(());
      .                           }
      .                           // CR 614 — the turn-scoped land-tap replacements (Pale Moon,
      .                           // Harvest Mage). Same "instead of any other type and amount"
      .                           // shape as Contamination, but installed by a spell.
      .                           if let Some(land) = ctx.source.and_then(|s| self.battlefield_find(s))
 73,184 ( 0.00%)                      && land.definition.is_land()
238,418 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,296x)
      .                           {
 54,318 ( 0.00%)                      let basic = land.definition.is_basic();
235,378 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_basic (18,106x)
 18,106 ( 0.00%)                      let out = self
      .                                   .land_mana_replacements_this_turn
      .                                   .iter()
144,848 ( 0.00%)                          .find(|r| {
108,636 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::find (18,106x)
      .                                       r.who.is_none_or(|w| w == p)
      .                                           && !(r.nonbasic_only && basic)
      .                                           && r.land.is_none_or(|id| Some(id) == ctx.source)
      .                                   })
      .                                   .map(|r| r.output);
      .                               if let Some(out) = out {
      .                                   match out {
      .                                       crate::game::types::LandManaOutput::Colorless => {
-- line 10171 ----------------------------------------
-- line 10198 ----------------------------------------
      .                                               color,
      .                                               source: ctx.source,
      .                                           });
      .                                       }
      .                                   }
      .                                   return Ok(());
      .                               }
      .                           }
182,960 ( 0.01%)                  match pool {
      .                               ManaPayload::Colors(colors) => {
      .                                   for c in colors {
146,368 ( 0.00%)                              add_one(self, p, *c);
1,079,464 ( 0.03%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::run_effect::{{closure}} (18,296x)
164,664 ( 0.01%)                              events.push(GameEvent::ManaAdded { player: p, color: *c, source: ctx.source });
      .                                   }
      .                               }
      .                               ManaPayload::Colorless(v) => {
      .                                   let n = self.evaluate_value(v, ctx).max(0) as u32;
      .                                   for _ in 0..n {
      .                                       match restriction {
      .                                           // Powerstone tokens — restricted {C}.
      .                                           Some(r) => self.players[p]
-- line 10218 ----------------------------------------
-- line 10730 ----------------------------------------
      .                                           amount: applied as u32,
      .                                       });
      .                                   }
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
    344 ( 0.00%)              Effect::Destroy { what } | Effect::DestroyNoRegen { what } => {
      .                           // CR 701.15g — `DestroyNoRegen` ("can't be regenerated")
      .                           // bypasses regeneration shields; everything else (the
      .                           // Indestructible check, Shield-counter replacement) is
      .                           // identical to plain `Destroy`.
  1,274 ( 0.00%)                  let no_regen = matches!(effect, Effect::DestroyNoRegen { .. });
  1,032 ( 0.00%)                  let entities = self.resolve_selector(what, ctx);
 90,526 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (344x)
    688 ( 0.00%)                  for ent in entities {
    344 ( 0.00%)                      if let Some(cid) = ent.as_permanent_id() {
      .                                   let victim = self
      .                                       .battlefield_find(cid)
    484 ( 0.00%)                              .map(|c| (c.controller, c.definition.is_creature()));
  3,146 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (242x)
  1,516 ( 0.00%)                          if self.destroy_permanent(cid, no_regen, events)
4,074,728 ( 0.13%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::destroy_permanent (344x)
      .                                       && let Some((owner, is_creature)) = victim
  1,210 ( 0.00%)                              && !self.same_team(owner, ctx.controller)
  4,192 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (242x)
      .                                   {
    208 ( 0.00%)                              if !is_creature {
      .                                           self.players[owner]
      .                                               .noncreature_destroyed_by_opponent_this_turn = true;
      .                                       }
      .                                       // CR 701.7 — "a spell or ability an opponent
      .                                       // controls destroys a permanent you control"
      .                                       // (Karmic Justice). Only the cross-team case is
      .                                       // emitted, so the scope reads off the victim.
    728 ( 0.00%)                              events.push(GameEvent::PermanentDestroyedByEffect {
      .                                           card_id: cid,
      .                                           controller: owner,
      .                                           destroyer: ctx.controller,
      .                                           is_creature,
      .                                       });
      .                                   }
      .                               }
      .                           }
    344 ( 0.00%)                  let mut sba = self.check_state_based_actions();
4,674,034 ( 0.15%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (344x)
      .                           events.append(&mut sba);
      .                           Ok(())
      .                       }
      .           
      .                       Effect::DestroyThenVictimControllersMakeToken { what, definition, no_regen } => {
      .                           // Terastodon — destroy the chosen permanents, then each one
      .                           // that actually reached a graveyard pays its controller a
      .                           // token (indestructible / replaced victims pay nothing).
-- line 10779 ----------------------------------------
-- line 11003 ----------------------------------------
      .                               {
      .                                   let hm_cost = card.definition.cost.clone();
      .                                   card.granted_harmonize_eot = Some(hm_cost);
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
    282 ( 0.00%)              Effect::Exile { what } => {
      .                           // Exile accepts both `EntityRef::Permanent` (battlefield)
      .                           // and `EntityRef::Card` (any other zone). Battlefield exits
      .                           // emit `PermanentExiled` and walk through the standard
      .                           // remove-from-battlefield path so leaves-the-battlefield
      .                           // hooks fire; non-battlefield zones (graveyards, hand,
      .                           // exile→exile re-routes) just relocate via `move_card_to`.
    564 ( 0.00%)                  for ent in self.resolve_selector(what, ctx) {
 22,085 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (94x)
      .                               match ent {
      .                                   // A `Permanent` ref whose object already left the
      .                                   // battlefield (died mid-trigger — Kaldra Compleat's
      .                                   // "exile that creature") is exiled from wherever it
      .                                   // went instead of silently no-oping.
      .                                   EntityRef::Permanent(cid)
     94 ( 0.00%)                              if !self.battlefield.iter().any(|c| c.id == cid) =>
      .                                   {
      .                                       self.move_card_to(cid, &ZoneDest::Exile, ctx, events);
      .                                   }
      .                                   EntityRef::Permanent(cid) => {
    152 ( 0.00%)                              self.remove_from_battlefield_to_exile(cid);
659,340 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_exile (76x)
      .                                       // Bump the controller's per-turn exile tally
      .                                       // for Ennis-style "if a card was put into
      .                                       // exile this turn" payoffs.
    152 ( 0.00%)                              if ctx.controller < self.players.len() {
    152 ( 0.00%)                                  self.players[ctx.controller].cards_exiled_this_turn =
 36,722 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (76x)
    152 ( 0.00%)                                      self.players[ctx.controller].cards_exiled_this_turn.saturating_add(1);
      .                                       }
    228 ( 0.00%)                              events.push(GameEvent::PermanentExiled { card_id: cid });
    228 ( 0.00%)                              events.push(GameEvent::CardExiledFromPlayOrGraveyard {
      .                                           card_id: cid,
      .                                       });
      .                                   }
      .                                   EntityRef::Card(cid) => {
      .                                       self.move_card_to(cid, &ZoneDest::Exile, ctx, events);
      .                                   }
      .                                   _ => {}
      .                               }
-- line 11047 ----------------------------------------
-- line 13476 ----------------------------------------
      .                                   && self.untap_permanent(cid, events)
      .                               {
      .                                   count += 1;
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
  1,710 ( 0.00%)              Effect::PumpPT { what, power, toughness, duration } => {
  1,710 ( 0.00%)                  let p = self.evaluate_value(power, ctx);
  9,576 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_value (342x)
  2,052 ( 0.00%)                  let t = self.evaluate_value(toughness, ctx);
  9,576 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_value (342x)
  2,052 ( 0.00%)                  for ent in self.resolve_selector(what, ctx) {
 79,871 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (342x)
    342 ( 0.00%)                      let Some(cid) = ent.as_permanent_id() else { continue };
      .                               if self.battlefield_find(cid).is_none() {
      .                                   // CR 112.4 — a permanent spell pumped on the stack
      .                                   // keeps the bonus once it resolves (the stack card is
      .                                   // the same object that becomes the permanent).
      .                                   if let Some(card) = self.stack.iter_mut().find_map(|si| match si {
      .                                       StackItem::Spell { card, .. } if card.id == cid => Some(card),
      .                                       _ => None,
      .                                   }) {
-- line 13496 ----------------------------------------
-- line 13499 ----------------------------------------
      .                                       events.push(GameEvent::PumpApplied {
      .                                           card_id: cid,
      .                                           power: p,
      .                                           toughness: t,
      .                                       });
      .                                   }
      .                                   continue;
      .                               }
  1,026 ( 0.00%)                      match duration {
      .                                   // Fast path: the EOT fields are wiped at Cleanup.
      .                                   Duration::EndOfTurn => {
      .                                       if let Some(c) = self.battlefield_find_mut(cid) {
    342 ( 0.00%)                                  c.power_bonus += p;
    684 ( 0.00%)                                  c.toughness_bonus += t;
      .                                       }
      .                                   }
      .                                   // Permanent pumps survive Cleanup (Wall of Roots).
      .                                   Duration::Permanent => {
      .                                       if let Some(c) = self.battlefield_find_mut(cid) {
      .                                           c.perm_power_bonus += p;
      .                                           c.perm_toughness_bonus += t;
      .                                       }
-- line 13520 ----------------------------------------
-- line 13535 ----------------------------------------
      .                                           affected: AffectedPermanents::Specific(vec![cid]),
      .                                           layer: Layer::L7PowerTough,
      .                                           sublayer: Some(PtSublayer::Modify),
      .                                           duration: duration_kind,
      .                                           modification: Modification::ModifyPowerToughness(p, t),
      .                                       });
      .                                   }
      .                               }
  2,394 ( 0.00%)                      events.push(GameEvent::PumpApplied { card_id: cid, power: p, toughness: t });
      .                           }
      .                           Ok(())
      .                       }
      .           
      .                       Effect::DoublePower { what, times, duration: _ } => {
      .                           // Double each resolved creature's current power `times` times
      .                           // (Exponential Growth). Adds power*(2^times - 1) as an EOT pump
      .                           // so the live power ends at power * 2^times.
-- line 13551 ----------------------------------------
-- line 18136 ----------------------------------------
      .                           }
      .                           card.class_level = card.class_level.saturating_add(1);
      .                           let level = card.class_level;
      .                           let player = card.controller;
      .                           events.push(GameEvent::ClassLevelReached { source: src, player, level });
      .                           Ok(())
      .                       }
      .           
    660 ( 0.00%)              Effect::CounterSpell { what } => {
      .                           // With only a single stack target, we pop the top of the
      .                           // stack if it's a spell (matching by target id when
      .                           // available). Spells flagged `uncounterable` (Cavern of
      .                           // Souls) are skipped — the counter has no effect on them.
    660 ( 0.00%)                  let targets = self.resolve_selector(what, ctx);
 49,263 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (220x)
      .                           let mut to_remove: Vec<usize> = Vec::new();
      .                           for t in &targets {
    660 ( 0.00%)                      if let Some(cid) = t.as_card_id()
  1,646 ( 0.00%)                          && let Some(pos) = self.stack.iter().position(|si| matches!(
  3,722 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::position (220x)
    182 ( 0.00%)                              si,
      .                                       StackItem::Spell { card, uncounterable: false, .. }
    182 ( 0.00%)                                  if card.id == cid
      .                                   ))
      .                               {
      .                                   to_remove.push(pos);
      .                               }
      .                           }
    440 ( 0.00%)                  to_remove.sort_unstable_by(|a, b| b.cmp(a));
  1,540 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs:core::slice::<impl [T]>::sort_unstable_by (220x)
  1,100 ( 0.00%)                  for pos in to_remove {
  1,190 ( 0.00%)                      if let StackItem::Spell { card, mana_spent, caster, .. } = self.stack.remove(pos)
      .                               {
      .                                   // Mana Sculpt — record the countered spell's paid
      .                                   // mana for `Value::CounteredSpellManaSpent`; Fold into
      .                                   // Aether reads the caster.
    170 ( 0.00%)                          self.countered_spell_mana_spent = mana_spent;
    340 ( 0.00%)                          self.countered_spell_controller = Some(caster);
    510 ( 0.00%)                          self.countered_spell_mana_value = card.definition.cost.cmc();
  6,290 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (170x)
    680 ( 0.00%)                          self.countered_spell_off_stack(*card, ctx.controller, events);
444,817 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::countered_spell_off_stack (170x)
    510 ( 0.00%)                      }
      .                           }
    220 ( 0.00%)                  Ok(())
    220 ( 0.00%)              }
      .           
      .                       Effect::CounterSpellExileSameNamed { what } => {
      .                           // Test of Talents: counter (same machinery as CounterSpell),
      .                           // then strip every same-named card from the owner's
      .                           // graveyard / hand / library, shuffle, and draw per
      .                           // hand-exile. The countered copy itself lands in the
      .                           // graveyard first, so the sweep exiles it too.
      .                           
-- line 18184 ----------------------------------------
-- line 19078 ----------------------------------------
      .                               for id in ids {
      .                                   self.move_card_to(id, &ZoneDest::Hand(PlayerRef::OwnerOfMoved), ctx, events);
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
      .                       Effect::SacrificeSource => {
    264 ( 0.00%)                  if let Some(id) = ctx.source
      .                               && let Some(c) = self.battlefield_find(id)
      .                           {
      .                               let p = c.controller;
      .                               let is_creature = c.definition.is_creature();
      .                               if is_creature {
      .                                   self.died_card_snapshots.insert(id, c.clone());
      .                                   events.push(GameEvent::CreatureSacrificed { card_id: id, who: p });
      .                                   events.push(GameEvent::CreatureDied { card_id: id });
-- line 19094 ----------------------------------------
-- line 19424 ----------------------------------------
      .                               if let EntityRef::Player(p) = ent {
      .                                   self.players[p].rad_counters =
      .                                       self.players[p].rad_counters.saturating_add(n);
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
    440 ( 0.00%)              Effect::Move { what, to } => {
    550 ( 0.00%)                  for ent in self.resolve_selector(what, ctx) {
 28,330 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector (110x)
      .                               let cid = match ent {
      .                                   EntityRef::Permanent(c) | EntityRef::Card(c) => c,
      .                                   _ => continue,
      .                               };
    550 ( 0.00%)                      self.move_card_to(cid, to, ctx, events);
281,111 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::move_card_to (110x)
      .                               // Stash the moved id so a downstream
      .                               // `Selector::LastMoved` in the same Seq can target
      .                               // it (Practiced Scrollsmith's Move → GrantMayPlay
      .                               // chain).
      .                               self.last_moved_cards.push(cid);
      .                           }
      .                           Ok(())
      .                       }
-- line 19446 ----------------------------------------
-- line 20140 ----------------------------------------
      .                       e @ (Effect::Search { .. }
      .                       | Effect::SearchPickedBy { .. }
      .                       | Effect::SearchZones { .. }) => {
      .                           use crate::card::Zone;
      .                           use crate::decision::Decision;
      .                           let mut include_graveyard = false;
      .                           let mut include_hand = false;
      .                           let mut include_library = true;
 20,432 ( 0.00%)                  let (who, picker_ref, filter, to) = match e {
      .                               Effect::Search { who, filter, to } => (who, None, filter, to),
      .                               Effect::SearchPickedBy { who, picker, filter, to } => {
      .                                   (who, Some(picker), filter, to)
      .                               }
      .                               Effect::SearchZones { who, zones, filter, to } => {
      .                                   include_graveyard = zones.contains(&Zone::Graveyard);
      .                                   include_hand = zones.contains(&Zone::Hand);
      .                                   include_library = zones.contains(&Zone::Library);
-- line 20156 ----------------------------------------
-- line 25132 ----------------------------------------
      .                                       ctx,
      .                                       events,
      .                                   );
      .                               }
      .                           }
      .                           Ok(())
      .                       }
      .           
    264 ( 0.00%)              Effect::RevealTopAndDrawIf { who, reveal_filter, may_graveyard_miss } => {
      .                           // Each resolved player reveals the top card of their library;
      .                           // if it matches `reveal_filter`, that player puts it into
      .                           // their hand (otherwise it stays on top).
      .                           //
      .                           // CR 121.5 compliance — "If an effect moves cards from a
      .                           // player's library to that player's hand without using
      .                           // the word 'draw,' the player has not drawn those cards.
      .                           // This makes a difference for abilities that trigger on
-- line 25148 ----------------------------------------
-- line 25149 ----------------------------------------
      .                           // drawing and effects that count cards drawn." Goblin
      .                           // Guide and its kin say "puts it into their hand", not
      .                           // "draws"; so we do NOT emit a `CardDrawn` event here
      .                           // and do NOT increment `cards_drawn_this_turn`. A
      .                           // dedicated `CardPutIntoHand` event would let cards
      .                           // listen to *all* library→hand moves, but no current
      .                           // card needs that; if/when one lands, add the event
      .                           // here in front of the silent move.
  1,056 ( 0.00%)                  let mut cursor = 0;
    264 ( 0.00%)                  for p in self.resolve_players(who, ctx) {
 57,931 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_players (264x)
    792 ( 0.00%)                      let Some(top) = self.players[p].library.first() else {
      .                                   continue;
      .                               };
    528 ( 0.00%)                      let card_name = top.definition.name;
    264 ( 0.00%)                      let is_land = top.definition.is_land();
  3,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (264x)
      .                               // `evaluate_requirement_on_card` works on any
      .                               // `CardInstance` (the battlefield-only variant would fail
      .                               // here since the card is in the library).
      .                               let matches =
    792 ( 0.00%)                          self.evaluate_requirement_on_card(reveal_filter, top, ctx.controller);
 11,052 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_on_card (264x)
      .                               // On a miss the revealer may bin the card instead of
      .                               // leaving it on top (Nylea, Keen-Eyed). Seat-routed; the
      .                               // only carded users resolve a single player, so the
      .                               // pre-ask reveal event can't double-emit on re-run.
    528 ( 0.00%)                      let bin = !matches
    328 ( 0.00%)                          && *may_graveyard_miss
      .                                   && match self.ask_seat_bool(
      .                                       &mut cursor,
      .                                       p,
      .                                       format!("Revealed {card_name} — put it into your graveyard?"),
      .                                       ctx.source.unwrap_or(CardId(0)),
      .                                       effect,
      .                                   ) {
      .                                       Some(b) => b,
      .                                       None => return Ok(()),
      .                                   };
  1,584 ( 0.00%)                      events.push(GameEvent::TopCardRevealed {
      .                                   player: p,
      .                                   card_name,
      .                                   is_land,
      .                               });
      .                               if matches {
    900 ( 0.00%)                          let card = self.players[p].library.remove(0);
198,593 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (100x)
  5,500 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::remove (100x)
    300 ( 0.00%)                          self.players[p].hand.push(card);
  3,000 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (100x)
      .                                   // Intentionally no CardDrawn event (CR 121.5).
    328 ( 0.00%)                      } else if bin {
      .                                   let card = self.players[p].library.remove(0);
      .                                   self.route_to_graveyard(card, events);
      .                               }
      .                           }
      .                           self.clear_answer_log();
      .                           Ok(())
      .                       }
      .           
-- line 25202 ----------------------------------------
-- line 33338 ----------------------------------------
      .                           let n = self.evaluate_value(count, ctx).max(0) as u32;
      .                           if let Some(target) = self.resolve_player(who, ctx) {
      .                               self.players[target].extra_land_plays =
      .                                   self.players[target].extra_land_plays.saturating_add(n);
      .                           }
      .                           Ok(())
      .                       }
      .                   }
183,744 ( 0.01%)      }
      .           
      .               pub(crate) fn execute_put_on_library(
      .                   &mut self,
      .                   player: usize,
      .                   chosen: &[crate::card::CardId],
      .                   _events: &mut Vec<crate::game::GameEvent>,
      .               ) {
      .                   // Remove chosen cards from hand (in reverse order to preserve indices).
-- line 33354 ----------------------------------------
-- line 33522 ----------------------------------------
      .                       })
      .                       .collect();
      .                   match seats.as_slice() {
      .                       [seat] => Some((*seat, n)),
      .                       _ => None,
      .                   }
      .               }
      .           
 20,140 ( 0.00%)      pub(crate) fn resolve_selector(&self, sel: &Selector, ctx: &EffectContext) -> Vec<EntityRef> {
      .                   // Multi-candidate selectors run layer-aware filters per permanent;
      .                   // freeze the gather so the whole resolution shares one effect set.
      .                   // Hand-rolled (no closure/guard) — this sits inside the effect
      .                   // recursion, where debug-build frame size is at a premium.
      .                   self.freeze_layers_push();
  6,666 ( 0.00%)          let mut out = self.resolve_selector_inner(sel, ctx);
 16,677 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector_inner (110x)
  4,948 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_selector_inner'2 (46x)
  4,340 ( 0.00%)          self.freeze_layers_pop();
  7,542 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::freeze_layers_pop (156x)
      .                   // CR 801.10 — the parts of an effect that would reach an object or
      .                   // player outside its controller's range of influence do nothing.
      .                   if self.limited_range() {
      .                       out.retain(|e| match e {
      .                           EntityRef::Player(p) => self.player_in_range_of(ctx.controller, *p),
      .                           EntityRef::Permanent(c) | EntityRef::Card(c) => {
      .                               self.object_in_range_of(ctx.controller, *c)
      .                           }
      .                       });
      .                   }
  8,524 ( 0.00%)          out
 14,098 ( 0.00%)      }
      .           
 21,700 ( 0.00%)      fn resolve_selector_inner(&self, sel: &Selector, ctx: &EffectContext) -> Vec<EntityRef> {
 10,850 ( 0.00%)          match sel {
      .                       Selector::None => vec![],
    276 ( 0.00%)              Selector::This => ctx.source.map(EntityRef::Permanent).into_iter().collect(),
  1,600 ( 0.00%)              Selector::You => vec![EntityRef::Player(ctx.controller)],
      .                       Selector::Target(idx) | Selector::TargetFiltered { slot: idx, .. } => ctx
      .                           .targets
  1,258 ( 0.00%)                  .get(*idx as usize)
      .                           .map(target_to_entity)
      .                           .into_iter()
  1,258 ( 0.00%)                  .collect(),
      .                       Selector::AllTargets => ctx
      .                           .targets
      .                           .iter()
      .                           .filter_map(|t| match target_to_entity(t) {
      .                               EntityRef::Permanent(id) if self.battlefield_find(id).is_some() => {
      .                                   Some(EntityRef::Permanent(id))
      .                               }
      .                               EntityRef::Player(p) => Some(EntityRef::Player(p)),
-- line 33569 ----------------------------------------
-- line 33573 ----------------------------------------
      .                       Selector::RandomOf(inner) => {
      .                           use rand::seq::IteratorRandom;
      .                           self.resolve_selector_inner(inner, ctx)
      .                               .into_iter()
      .                               .choose(&mut self.rng.draw())
      .                               .into_iter()
      .                               .collect()
      .                       }
    190 ( 0.00%)              Selector::TriggerSource => ctx.trigger_source.into_iter().collect(),
      .                       Selector::ChosenPermanentOfSource => ctx
      .                           .source
      .                           // The source may already have left the battlefield when this
      .                           // resolves (Dauntless Bodyguard sacrifices itself as the cost),
      .                           // so consult the die-snapshot / leaves-LKI for its stamp.
      .                           .and_then(|s| {
      .                               self.battlefield_find(s)
      .                                   .or_else(|| self.died_card_snapshots.get(&s))
-- line 33589 ----------------------------------------
-- line 34426 ----------------------------------------
      .                               // wherever it landed (graveyard, exile, …).
      .                               self.find_card_anywhere(**cid).is_some_and(|c| {
      .                                   self.evaluate_requirement_on_card(filter, c, ctx.controller)
      .                               })
      .                           })
      .                           .map(|cid| EntityRef::Card(*cid))
      .                           .collect(),
      .           
     46 ( 0.00%)              Selector::Player(p) => self
    138 ( 0.00%)                  .resolve_players(p, ctx)
 30,336 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_players (46x)
      .                           .into_iter()
      .                           .map(EntityRef::Player)
      .                           .collect(),
      .           
      .                       Selector::MatchingAmong { inner, filter } => {
      .                           let all = self.resolve_selector(inner, ctx);
      .                           all.into_iter()
      .                               .filter(|e| match e {
-- line 34443 ----------------------------------------
-- line 34508 ----------------------------------------
      .                               }
      .                               // Otherwise skip this candidate; iteration continues so
      .                               // smaller items can still fit. Greedy walk gives the
      .                               // AutoDecider a deterministic pick.
      .                           }
      .                           kept
      .                       }
      .                   }
 15,840 ( 0.00%)      }
      .           
      .               /// Multi-player resolver. `EachPlayer` and `EachOpponent` return all
      .               /// matching alive seats so effects like Wheel of Fortune actually hit
      .               /// every player. Non-collective `PlayerRef` variants resolve to a single
      .               /// seat (or empty if the reference can't be resolved).
  3,410 ( 0.00%)      pub fn resolve_players(&self, pref: &PlayerRef, ctx: &EffectContext) -> Vec<usize> {
      .                   // CR 801.10 — an effect can't touch players outside its controller's
      .                   // range of influence; the rest of it works normally.
    310 ( 0.00%)          let seats = self.resolve_players_unranged(pref, ctx);
 73,077 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_players_unranged (310x)
      .                   if !self.limited_range() {
  1,240 ( 0.00%)              return seats;
      .                   }
      .                   seats.into_iter().filter(|p| self.player_in_range_of(ctx.controller, *p)).collect()
  3,100 ( 0.00%)      }
      .           
  2,790 ( 0.00%)      fn resolve_players_unranged(&self, pref: &PlayerRef, ctx: &EffectContext) -> Vec<usize> {
  2,170 ( 0.00%)          match pref {
      .                       PlayerRef::CurrentVoter => vec![self.current_voter.unwrap_or(ctx.controller)],
      .                       // CR 101.4 / 121.2c — "each player"/"each opponent" fan-outs
      .                       // resolve in APNAP order (active player first, then turn order),
      .                       // not raw seat index.
      .                       PlayerRef::EachOpponent => self.apnap_sort(
      .                           self.opponents_of(ctx.controller)
      .                               .into_iter()
      .                               .filter(|i| self.players[*i].is_alive())
-- line 34541 ----------------------------------------
-- line 34614 ----------------------------------------
      .                           let Some(of) = self.resolve_player(inner, ctx) else { return Vec::new() };
      .                           self.apnap_sort(
      .                               self.opponents_of(of)
      .                                   .into_iter()
      .                                   .filter(|i| self.players[*i].is_alive())
      .                                   .collect(),
      .                           )
      .                       }
  1,860 ( 0.00%)              _ => self.resolve_player(pref, ctx).into_iter().collect(),
 40,070 ( 0.00%)  => crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::resolve_player (310x)
      .                   }
  2,480 ( 0.00%)      }
      .           
      .               /// Apply a resolved `LearnChoice` (CR 701.45): reveal a Lesson from the
      .               /// sideboard into hand, rummage (discard then draw), or decline. Shared
      .               /// by the synchronous `Effect::Learn` path and the UI resume in
      .               /// `apply_pending_effect_answer`.
      .               pub(crate) fn apply_learn_choice(
      .                   &mut self,
      .                   p: usize,
-- line 34632 ----------------------------------------
-- line 34647 ----------------------------------------
      .                                   self.lose_to_empty_draw(p);
      .                               }
      .                           }
      .                       }
      .                       LearnChoice::Decline => {}
      .                   }
      .               }
      .           
131,782 ( 0.00%)      pub(crate) fn resolve_player(&self, pref: &PlayerRef, ctx: &EffectContext) -> Option<usize> {
112,956 ( 0.00%)          match pref {
 18,296 ( 0.00%)              PlayerRef::You => Some(ctx.controller),
      .                       PlayerRef::CurrentVoter => Some(self.current_voter.unwrap_or(ctx.controller)),
    110 ( 0.00%)              PlayerRef::Seat(p) => Some(*p),
      .                       PlayerRef::ActivePlayer => Some(self.active_player_idx),
      .                       PlayerRef::PlayerWithMostLife => {
      .                           let mut best: Option<(usize, i32)> = None;
      .                           let mut tied = false;
      .                           for seat in 0..self.players.len() {
      .                               if !self.players[seat].is_alive() {
      .                                   continue;
      .                               }
-- line 34667 ----------------------------------------
-- line 34794 ----------------------------------------
      .                       }
      .                       PlayerRef::EachPlayerExceptControllerOf(_) => {
      .                           self.resolve_players(pref, ctx).into_iter().next()
      .                       }
      .                       PlayerRef::EnchantedPlayer => ctx
      .                           .source
      .                           .and_then(|src| self.battlefield_find(src))
      .                           .and_then(|c| c.attached_to_player),
    264 ( 0.00%)              PlayerRef::DefendingPlayer => ctx
      .                           .source
    264 ( 0.00%)                  .and_then(|src| self.attack_for(src).map(|a| a.target))
    264 ( 0.00%)                  .and_then(|target| self.defender_for(target))
      .                           // "Whenever a [creature] you control attacks" triggers fire
      .                           // off *another* permanent, so `ctx.source` (the ability's
      .                           // owner) isn't the attacker — read the defending player from
      .                           // the triggering attacker instead. (Leeching Sliver.)
      .                           .or_else(|| match ctx.trigger_source {
      .                               Some(EntityRef::Permanent(att)) => self
      .                                   .attack_for(att)
      .                                   .map(|a| a.target)
-- line 34813 ----------------------------------------
-- line 34821 ----------------------------------------
      .                           // `Target::Player`, so read it back here.
      .                           .or_else(|| {
      .                               ctx.targets.iter().find_map(|t| match t {
      .                                   Target::Player(p) => Some(*p),
      .                                   _ => None,
      .                               })
      .                           }),
      .                       PlayerRef::OwnerOf(sel) => self
    110 ( 0.00%)                  .resolve_selector(sel, ctx)
      .                           .into_iter()
    220 ( 0.00%)                  .find_map(|e| match e {
      .                               EntityRef::Permanent(cid) | EntityRef::Card(cid) => {
    110 ( 0.00%)                          self.find_card_owner(cid)
 37,610 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::find_card_owner (110x)
      .                               }
      .                               _ => None,
      .                           }),
      .                       PlayerRef::ControllerOf(sel) => self
     46 ( 0.00%)                  .resolve_selector(sel, ctx)
      .                           .into_iter()
     92 ( 0.00%)                  .find_map(|e| match e {
      .                               // "Controller of a player" is that player — lets a
      .                               // "target player or planeswalker" spell bind its follow-up
      .                               // to the damaged player (Rakdos's Return's discard).
      .                               EntityRef::Player(p) => Some(p),
      .                               EntityRef::Permanent(cid) | EntityRef::Card(cid) => self
      .                                   .battlefield_find(cid)
     28 ( 0.00%)                          .map(|c| c.controller)
      .                                   .or_else(|| self.stack_caster_for_card(cid))
      .                                   // LKI (CR 608.2h) — "its controller" for a permanent
      .                                   // destroyed earlier in the same resolution (Erode's
      .                                   // searcher, Foolish Fate's life loss) is the
      .                                   // controller at death, NOT the card's owner; the two
      .                                   // differ for stolen creatures.
      .                                   .or_else(|| self.died_card_snapshots.get(&cid).map(|c| c.controller))
      .                                   .or_else(|| self.leaves_bf_lki.get(&cid).map(|c| c.controller))
     36 ( 0.00%)                          .or_else(|| self.find_card_owner(cid)),
  2,326 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::find_card_owner (10x)
      .                           }),
      .                       PlayerRef::CombatDamagerController(sel) => self
      .                           .resolve_selector(sel, ctx)
      .                           .into_iter()
      .                           .find_map(|e| match e {
      .                               EntityRef::Permanent(cid) | EntityRef::Card(cid) => self
      .                                   .battlefield_find(cid)
      .                                   .and_then(|c| c.combat_damager_controller)
-- line 34864 ----------------------------------------
-- line 34878 ----------------------------------------
      .                                       .and_then(|c| c.damaged_by_this_turn.last().copied())?;
      .                                   self.battlefield_find(src)
      .                                       .map(|c| c.controller)
      .                                       .or_else(|| self.find_card_anywhere(src).map(|c| c.owner))
      .                               }
      .                               _ => None,
      .                           }),
      .                   }
169,434 ( 0.01%)      }
      .           
      .               fn entities_in_zone(
      .                   &self,
      .                   zone: &ZoneRef,
      .                   filter: &SelectionRequirement,
      .                   ctx: &EffectContext,
      .               ) -> Vec<EntityRef> {
      .                   match zone {
-- line 34894 ----------------------------------------
-- line 34948 ----------------------------------------
      .                           .collect(),
      .                       ZoneRef::Command => vec![],
      .                   }
      .               }
      .           
      .           }
      .           
      .           fn target_to_entity(t: &Target) -> EntityRef {
  2,516 ( 0.00%)      match t {
    336 ( 0.00%)          Target::Player(p) => EntityRef::Player(*p),
  2,180 ( 0.00%)          Target::Permanent(c) => EntityRef::Permanent(*c),
      .               }
      .           }
      .           
      .           /// UI noun for an `Effect::DistributeCounters` division (Jugan → "+1/+1
      .           /// counter"). Falls back to the debug name for exotic counter kinds.
      .           fn counter_noun(counter: CounterType) -> &'static str {
      .               match counter {
      .                   CounterType::PlusOnePlusOne => "+1/+1 counter",
-- line 34966 ----------------------------------------

256,140 ( 0.01%)  <counts for unidentified lines in crabomination/src/game/effects/mod.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/mana.rs
--------------------------------------------------------------------------------
Ir                 

        .           use std::fmt;
        .           
        .           use serde::{Deserialize, Serialize};
        .           
  255,448 ( 0.01%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
        .           pub enum Color {
        .               White,
        .               Blue,
        .               Black,
        .               Red,
        .               Green,
        .           }
        .           
-- line 13 ----------------------------------------
-- line 46 ----------------------------------------
        .           /// `contains` / `insert` / `is_subset_of` helpers. Used by Phase K's
        .           /// color-identity validator for Commander deckbuilding.
        .           #[derive(Debug, Default, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
        .           pub struct ColorSet(pub u8);
        .           
        .           impl ColorSet {
        .               /// The empty set (colorless).
        .               pub const fn empty() -> Self {
  104,118 ( 0.00%)          Self(0)
        .               }
        .           
        .               /// All five colors set ("WUBRG").
        .               pub const fn all() -> Self {
        .                   Self(0b11111)
        .               }
        .           
        .               pub const fn single(c: Color) -> Self {
        .                   Self(Self::bit_for(c))
        .               }
        .           
        .               const fn bit_for(c: Color) -> u8 {
  756,750 ( 0.02%)          1 << match c {
        .                       Color::White => 0,
        .                       Color::Blue => 1,
        .                       Color::Black => 2,
        .                       Color::Red => 3,
        .                       Color::Green => 4,
        .                   }
        .               }
        .           
        .               pub fn insert(&mut self, c: Color) {
  434,726 ( 0.01%)          self.0 |= Self::bit_for(c);
        .               }
        .           
        .               /// Generic over `Color` / `&Color` so the `Vec<Color>` readers this
        .               /// replaced (`colors.contains(&c)`, `.iter().any(|k| set.contains(k))`)
        .               /// compile unchanged.
        .               pub fn contains<C: core::borrow::Borrow<Color>>(self, c: C) -> bool {
  144,512 ( 0.00%)          self.0 & Self::bit_for(*c.borrow()) != 0
        .               }
        .           
        .               pub fn remove(&mut self, c: Color) {
        .                   self.0 &= !Self::bit_for(c);
        .               }
        .           
        .               /// Drop every color (layer 5's `LoseAllColors`).
        .               pub fn clear(&mut self) {
-- line 92 ----------------------------------------
-- line 112 ----------------------------------------
        .               pub fn is_empty(self) -> bool {
        .                   self.0 == 0
        .               }
        .           
        .               /// True iff every color in `self` is also in `other`. Used by
        .               /// Commander color-identity: a non-commander card is legal iff
        .               /// `card_identity.is_subset_of(commander_identity)`.
        .               pub fn is_subset_of(self, other: ColorSet) -> bool {
       78 ( 0.00%)          self.0 & !other.0 == 0
        .               }
        .           
        .               /// Union of two sets.
        .               pub fn union(self, other: ColorSet) -> ColorSet {
  112,644 ( 0.00%)          ColorSet(self.0 | other.0)
        .               }
        .           
        .               /// Number of colors in the set.
        .               pub fn len(self) -> u32 {
        .                   self.0.count_ones()
        .               }
        .           
        .               /// CR 105.2a — true iff this set is exactly one color. Used by
-- line 133 ----------------------------------------
-- line 198 ----------------------------------------
        .           #[derive(Debug, Clone)]
        .           pub struct ColorSetIter {
        .               bits: u8,
        .           }
        .           
        .           impl Iterator for ColorSetIter {
        .               type Item = Color;
        .               fn next(&mut self) -> Option<Color> {
  161,010 ( 0.01%)          if self.bits == 0 {
        .                       return None;
        .                   }
        .                   let idx = self.bits.trailing_zeros();
        .                   self.bits &= self.bits - 1;
  214,680 ( 0.01%)          Some(Color::ALL[idx as usize])
        .               }
        .               fn size_hint(&self) -> (usize, Option<usize>) {
        .                   let n = self.bits.count_ones() as usize;
        .                   (n, Some(n))
        .               }
        .           }
        .           
        .           impl ExactSizeIterator for ColorSetIter {}
-- line 219 ----------------------------------------
-- line 241 ----------------------------------------
        .               MonoHybrid(u32, Color),
        .               /// {S}: pay with mana from a snow source.
        .               Snow,
        .               /// {X}: variable cost determined at cast time.
        .               X,
        .           }
        .           
        .           /// The full mana cost of a card or ability (e.g. {3}{W}{W} for Serra Angel).
  351,676 ( 0.01%)  #[derive(Debug, Clone, Default, PartialEq, Eq, Hash, Serialize, Deserialize)]
1,526,875 ( 0.05%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (7,370x)
        .           pub struct ManaCost {
        .               pub symbols: Vec<ManaSymbol>,
        .           }
        .           
        .           impl ManaCost {
        .               pub fn new(symbols: Vec<ManaSymbol>) -> Self {
    5,718 ( 0.00%)          Self { symbols }
        .               }
        .           
        .               /// Mana value (formerly converted mana cost): sum of all pip values.
        .               /// CR 107.4f — does this cost contain a Phyrexian mana symbol?
        .               /// ("a spell with {H} in its mana cost" — Rage Extractor.)
        .               pub fn has_phyrexian(&self) -> bool {
        .                   self.symbols.iter().any(|s| {
        .                       matches!(s, ManaSymbol::Phyrexian(_) | ManaSymbol::PhyrexianHybrid(_, _))
-- line 264 ----------------------------------------
-- line 265 ----------------------------------------
        .                   })
        .               }
        .           
        .               pub fn cmc(&self) -> u32 {
        .                   self.symbols
        .                       .iter()
        .                       .map(|s| match s {
        .                           ManaSymbol::Colored(_) => 1,
    9,418 ( 0.00%)                  ManaSymbol::Generic(n) => *n,
        .                           ManaSymbol::Colorless(n) => *n,
        .                           ManaSymbol::Hybrid(_, _) => 1,
        .                           ManaSymbol::Phyrexian(_) => 1,
        .                           ManaSymbol::PhyrexianHybrid(_, _) => 1,
        .                           // CR 202.3f: monocolored hybrid MV is the generic amount.
        .                           ManaSymbol::MonoHybrid(n, _) => *n,
        .                           ManaSymbol::Snow => 1,
        .                           ManaSymbol::X => 0, // X is 0 everywhere except on the stack
        .                       })
        .                       .sum()
   74,272 ( 0.00%)      }
        .           
        .               /// True if this cost contains any X symbols.
        .               pub fn has_x(&self) -> bool {
   87,366 ( 0.00%)          self.symbols.iter().any(|s| matches!(s, ManaSymbol::X))
   84,732 ( 0.00%)      }
        .           
        .               /// Number of *distinct* colors referenced by this cost. Hybrid pips
        .               /// (`{W/B}`) contribute both halves; Phyrexian pips (`{B/P}`) contribute
        .               /// their colored half. Generic / Colorless / Snow / X return 0.
        .               /// Used by `SelectionRequirement::Multicolored` / `Colorless` and any
        .               /// Converge-style payoff that wants to peek at the printed pip set.
        .               pub fn distinct_colors(&self) -> u32 {
        .                   let mut seen = [false; 5];
-- line 297 ----------------------------------------
-- line 380 ----------------------------------------
        .                           ManaSymbol::Hybrid(a, _) | ManaSymbol::PhyrexianHybrid(a, _) => Some(*a),
        .                           ManaSymbol::MonoHybrid(_, c) => Some(*c),
        .                           _ => None,
        .                       })
        .                       .collect()
        .               }
        .           
        .               /// Returns the set of colors present in this mana cost.
  197,216 ( 0.01%)      pub fn colors(&self) -> Vec<Color> {
        .                   let mut result = Vec::new();
        .                   for s in &self.symbols {
  291,024 ( 0.01%)              match s {
        .                           ManaSymbol::Colored(c) | ManaSymbol::Phyrexian(c)
        .                               if !result.contains(c) => {
        .                                   result.push(*c);
        .                               }
        .                           ManaSymbol::Hybrid(a, b) | ManaSymbol::PhyrexianHybrid(a, b) => {
        .                               if !result.contains(a) {
        .                                   result.push(*a);
        .                               }
-- line 399 ----------------------------------------
-- line 403 ----------------------------------------
        .                           }
        .                           ManaSymbol::MonoHybrid(_, c)
        .                               if !result.contains(c) => {
        .                                   result.push(*c);
        .                               }
        .                           _ => {}
        .                       }
        .                   }
  172,564 ( 0.01%)          result
  197,216 ( 0.01%)      }
        .           
        .               /// Return a copy of this cost with X symbols replaced by Generic(x_value).
        .               pub fn with_x_value(&self, x_value: u32) -> ManaCost {
        .                   ManaCost {
        .                       symbols: self
        .                           .symbols
        .                           .iter()
        .                           .map(|s| match s {
-- line 420 ----------------------------------------
-- line 472 ----------------------------------------
        .               /// Add `amount` generic pips to this cost (a state-defined additional
        .               /// generic cost — `ActivatedAbility::generic_cost_value`).
        .               pub fn add_generic(&mut self, amount: u32) {
        .                   if amount > 0 {
        .                       self.symbols.push(ManaSymbol::Generic(amount));
        .                   }
        .               }
        .           
   58,960 ( 0.00%)      pub fn reduce_generic(&mut self, amount: u32) -> u32 {
        .                   let mut remaining = amount;
        .                   let mut applied = 0u32;
        .                   let mut new_syms = Vec::with_capacity(self.symbols.len());
        .                   for sym in &self.symbols {
   38,676 ( 0.00%)              match sym {
        .                           ManaSymbol::Generic(n) if remaining > 0 => {
        .                               let drained = remaining.min(*n);
        .                               remaining -= drained;
        .                               applied += drained;
        .                               let kept = n - drained;
        .                               if kept > 0 {
        .                                   new_syms.push(ManaSymbol::Generic(kept));
        .                               }
        .                           }
   14,552 ( 0.00%)                  other => new_syms.push(*other),
        .                       }
        .                   }
   88,440 ( 0.00%)          self.symbols = new_syms;
        .                   applied
   66,330 ( 0.00%)      }
        .           
        .               /// Reduce this cost by another whole mana cost, color included
        .               /// (CR 702.48 Offering — "paying the difference in mana costs … Mana cost
        .               /// includes color."). Each colored pip in `other` removes one matching
        .               /// colored pip here; everything else (generic/colorless/hybrid/Phyrexian/
        .               /// snow, plus colored pips with no match) folds into a generic reduction.
   51,590 ( 0.00%)      pub fn reduce_by_cost(&mut self, other: &ManaCost) -> u32 {
        .                   let mut generic_pool = 0u32;
        .                   for sym in &other.symbols {
        .                       match sym {
        .                           ManaSymbol::Colored(c) => {
        .                               if let Some(pos) = self
        .                                   .symbols
        .                                   .iter()
        .                                   .position(|s| matches!(s, ManaSymbol::Colored(sc) if sc == c))
-- line 515 ----------------------------------------
-- line 523 ----------------------------------------
        .                           ManaSymbol::Hybrid(..)
        .                           | ManaSymbol::Phyrexian(_)
        .                           | ManaSymbol::PhyrexianHybrid(..)
        .                           | ManaSymbol::MonoHybrid(..)
        .                           | ManaSymbol::Snow => generic_pool += 1,
        .                           ManaSymbol::X => {}
        .                       }
        .                   }
   66,330 ( 0.00%)          self.reduce_generic(generic_pool)
2,306,709 ( 0.07%)  => crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::reduce_generic (7,370x)
        .               }
        .           }
        .           
        .           /// A "spend this mana only to …" restriction carried by certain mana
        .           /// sources (the Strixhaven school mana dorks/artifacts/lands: Abstract
        .           /// Paintmage, Tablet of Discovery, Hydro-Channeler, Great Hall of the
        .           /// Biblioplex, Resonating Lute). Mana tagged with a restriction lives in
        .           /// a separate pool bucket and can only pay for things the restriction
-- line 539 ----------------------------------------
-- line 703 ----------------------------------------
        .           }
        .           
        .           /// What a payment is funding, so spend-restricted mana can decide whether
        .           /// it is allowed to contribute. Spell casts build this from the card being
        .           /// cast ([`crate::card::CardDefinition::spell_kind`]); ability activations
        .           /// describe their source ([`crate::card::CardDefinition::ability_spend_kind`]);
        .           /// everything else pays with the empty [`SpellKind::default`], which no
        .           /// restricted mana may fund.
   13,698 ( 0.00%)  #[derive(Debug, Clone, Default, PartialEq, Eq)]
        .           pub struct SpellKind {
        .               /// Casting an instant or sorcery spell.
        .               pub instant_or_sorcery: bool,
        .               /// Casting an artifact spell, or activating an ability of an artifact.
        .               pub artifact: bool,
        .               /// The creature types of a creature spell being cast (empty otherwise).
        .               pub creature_types: Vec<crate::card::CreatureType>,
        .               /// The spell is a Changeling (every creature type, CR 702.73).
-- line 719 ----------------------------------------
-- line 785 ----------------------------------------
        .                   Color::Blue => 1,
        .                   Color::Black => 2,
        .                   Color::Red => 3,
        .                   Color::Green => 4,
        .               }
        .           }
        .           
        .           /// Available mana in a player's pool during their turn.
1,703,180 ( 0.05%)  #[derive(Debug, Clone, Default, Serialize, Deserialize)]
        .           pub struct ManaPool {
        .               white: u32,
        .               blue: u32,
        .               black: u32,
        .               red: u32,
        .               green: u32,
        .               /// True colorless mana (from Wastes, Eldrazi sources, Sol Ring, etc.).
        .               colorless: u32,
-- line 801 ----------------------------------------
-- line 814 ----------------------------------------
        .               #[serde(default)]
        .               restricted_colorless: Vec<(u32, SpendRestriction)>,
        .               /// Provenance counter, WUBRG then colorless: how much of each bucket was
        .               /// produced by a *creature*. Freely spendable like any other mana (unlike
        .               /// `restricted`); only a "spend only mana produced by creatures" cost
        .               /// (Myr Superion) reads it. Clamped to its bucket after every spend, in
        .               /// the same conservative spirit as `snow`.
        .               #[serde(default)]
  668,408 ( 0.02%)      creature: [u32; 6],
        .           }
        .           
        .           /// Index into [`ManaPool::creature`] for a color (5 = colorless).
        .           const fn prov_index(c: Option<Color>) -> usize {
        .               match c {
        .                   Some(c) => color_index(c),
        .                   None => 5,
        .               }
-- line 830 ----------------------------------------
-- line 869 ----------------------------------------
        .                       }
        .                   }
        .               }
        .           }
        .           
        .           impl std::error::Error for ManaError {}
        .           
        .           impl ManaPool {
       32 ( 0.00%)      pub fn new() -> Self {
        .                   Self::default()
       32 ( 0.00%)      }
        .           
        .               pub fn add(&mut self, color: Color, amount: u32) {
   18,296 ( 0.00%)          *self.slot_mut(color) += amount;
        .               }
        .           
        .               pub fn add_colorless(&mut self, amount: u32) {
        .                   self.colorless += amount;
        .               }
        .           
        .               /// "Double the amount of each type of unspent mana you have" (Doubling
        .               /// Cube). Every bucket — colored, colorless, snow, and the spend-
-- line 890 ----------------------------------------
-- line 918 ----------------------------------------
        .                   }
        .                   self.creature[prov_index(color)] += amount;
        .               }
        .           
        .               /// Tag `amount` mana already in `color`'s bucket as creature-produced.
        .               /// Used by the mana-ability activation path, which sees the pool delta
        .               /// after the payload resolved rather than at each individual add.
        .               pub fn mark_from_creature(&mut self, color: Option<Color>, amount: u32) {
      380 ( 0.00%)          self.creature[prov_index(color)] += amount;
        .                   self.clamp_creature();
      190 ( 0.00%)      }
        .           
        .               /// How much of `color`'s bucket (`None` = colorless) came from a creature.
        .               pub fn creature_amount(&self, color: Option<Color>) -> u32 {
        .                   self.creature[prov_index(color)]
        .               }
        .           
        .               /// Total creature-produced mana floating.
        .               pub fn creature_total(&self) -> u32 {
-- line 936 ----------------------------------------
-- line 938 ----------------------------------------
        .               }
        .           
        .               /// Clamp each provenance counter to the bucket it describes. Spending
        .               /// paths don't know *which* mana of a color left the pool, so — exactly
        .               /// like `snow` — we never claim more provenance than the bucket holds.
        .               fn clamp_creature(&mut self) {
        .                   for c in Color::ALL {
        .                       let idx = color_index(c);
   86,820 ( 0.00%)              self.creature[idx] = self.creature[idx].min(*self.slot(c));
        .                   }
   17,136 ( 0.00%)          self.creature[5] = self.creature[5].min(self.colorless);
        .               }
        .           
        .               /// Add mana from a snow source. The mana is both colored and snow.
        .               pub fn add_snow(&mut self, color: Color, amount: u32) {
        .                   *self.slot_mut(color) += amount;
        .                   self.snow += amount;
        .               }
        .           
-- line 956 ----------------------------------------
-- line 1011 ----------------------------------------
        .               }
        .           
        .               pub fn restricted_total(&self) -> u32 {
        .                   self.restricted.iter().map(|(_, n, _)| *n).sum::<u32>()
        .                       + self.restricted_colorless.iter().map(|(n, _)| *n).sum::<u32>()
        .               }
        .           
        .               pub fn amount(&self, color: Color) -> u32 {
   87,300 ( 0.00%)          *self.slot(color)
        .               }
        .           
        .               pub fn colorless_amount(&self) -> u32 {
    9,172 ( 0.00%)          self.colorless
        .               }
        .           
        .               pub fn snow_amount(&self) -> u32 {
        .                   self.snow
        .               }
        .           
        .               pub fn total(&self) -> u32 {
1,216,664 ( 0.04%)          self.white + self.blue + self.black + self.red + self.green + self.colorless
        .               }
        .           
        .               /// Render the floating mana as Oracle-style pips (`{R}{R}{U}{C}`) for UI
        .               /// prompts (e.g. the "spend floating mana?" confirmation). Empty pool →
        .               /// `{0}`. Spend-restricted mana isn't freely spendable, so it's omitted.
        .               pub fn summary(&self) -> String {
        .                   let mut s = String::new();
        .                   for (color, n) in [
-- line 1039 ----------------------------------------
-- line 1067 ----------------------------------------
        .               /// 1. Pay each `Colored` pip from the matching bucket.
        .               /// 2. Pay `Colorless` pips from the colorless bucket only.
        .               /// 3. Pay `Hybrid` pips from whichever color is available (tries A first).
        .               /// 4. Pay `Phyrexian` pips from color if available, else 2 life.
        .               /// 5. Pay `Snow` pips from the snow counter (any color).
        .               /// 6. Pay all `Generic` pips from whatever remains (any color or colorless).
        .               ///
        .               /// X symbols should be replaced before calling this (via `ManaCost::with_x_value`).
   62,244 ( 0.00%)      pub fn pay(&mut self, cost: &ManaCost) -> Result<PaymentSideEffects, ManaError> {
        .                   let mut tmp = self.clone();
        .                   let mut side_effects = PaymentSideEffects::default();
        .           
        .                   // Pass 1: colored pips
        .                   for sym in &cost.symbols {
   32,008 ( 0.00%)              if let ManaSymbol::Colored(c) = sym {
    9,696 ( 0.00%)                  let have = tmp.amount(*c);
   27,036 ( 0.00%)                  if have == 0 {
      972 ( 0.00%)                      return Err(ManaError::InsufficientColored {
        .                                   color: *c,
        .                                   needed: 1,
        .                                   have: 0,
        .                               });
        .                           }
   18,744 ( 0.00%)                  *tmp.slot_mut(*c) -= 1;
        .                       }
        .                   }
        .           
        .                   // Pass 2: colorless-specific pips ({C})
        .                   let colorless_needed: u32 = cost
        .                       .symbols
        .                       .iter()
   15,320 ( 0.00%)              .filter_map(|s| if let ManaSymbol::Colorless(n) = s { Some(*n) } else { None })
        .                       .sum();
   17,136 ( 0.00%)          if colorless_needed > 0 {
        .                       if colorless_needed > tmp.colorless {
        .                           return Err(ManaError::InsufficientColorless {
        .                               needed: colorless_needed,
        .                               have: tmp.colorless,
        .                           });
        .                       }
        .                       tmp.colorless -= colorless_needed;
        .                   }
-- line 1108 ----------------------------------------
-- line 1116 ----------------------------------------
        .                   // later generic / snow drains.
        .                   enum HyPip {
        .                       Hybrid(Color, Color),
        .                       Mono(u32, Color),
        .                   }
        .                   let pips: Vec<HyPip> = cost
        .                       .symbols
        .                       .iter()
   76,600 ( 0.00%)              .filter_map(|s| match s {
        .                           ManaSymbol::Hybrid(a, b) => Some(HyPip::Hybrid(*a, *b)),
        .                           ManaSymbol::MonoHybrid(n, c) => Some(HyPip::Mono(*n, *c)),
        .                           _ => None,
        .                       })
        .                       .collect();
   17,136 ( 0.00%)          if !pips.is_empty() {
        .                       let downstream: u32 = cost
        .                           .symbols
        .                           .iter()
        .                           .map(|s| match s {
        .                               ManaSymbol::Generic(n) => *n,
        .                               ManaSymbol::Snow => 1,
        .                               _ => 0,
        .                           })
-- line 1138 ----------------------------------------
-- line 1199 ----------------------------------------
        .                                   pips.iter().map(|p| if let HyPip::Mono(n, _) = p { *n } else { 0 }).sum();
        .                               return Err(ManaError::InsufficientGeneric { needed, have: tmp.total() });
        .                           }
        .                       }
        .                   }
        .           
        .                   // Pass 4: phyrexian pips (pay color if available, else 2 life)
        .                   for sym in &cost.symbols {
   76,600 ( 0.00%)              match sym {
        .                           ManaSymbol::Phyrexian(c) => {
        .                               if tmp.amount(*c) > 0 {
        .                                   *tmp.slot_mut(*c) -= 1;
        .                               } else {
        .                                   side_effects.life_lost += 2;
        .                               }
        .                           }
        .                           // {A/B/P}: either color, else 2 life (CR 107.4f).
-- line 1215 ----------------------------------------
-- line 1225 ----------------------------------------
        .                           _ => {}
        .                       }
        .                   }
        .           
        .                   // Pass 5: snow pips
        .                   let snow_needed: u32 = cost
        .                       .symbols
        .                       .iter()
   45,960 ( 0.00%)              .filter(|s| matches!(s, ManaSymbol::Snow))
        .                       .count() as u32;
   17,136 ( 0.00%)          if snow_needed > 0 {
        .                       if snow_needed > tmp.snow {
        .                           return Err(ManaError::InsufficientSnow {
        .                               needed: snow_needed,
        .                               have: tmp.snow,
        .                           });
        .                       }
        .                       tmp.snow -= snow_needed;
        .                       // Also drain one mana from any bucket per snow pip
-- line 1243 ----------------------------------------
-- line 1261 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // Pass 6: generic pips (drain from any bucket)
        .                   let generic: u32 = cost
        .                       .symbols
        .                       .iter()
        .                       .filter_map(|s| {
   15,320 ( 0.00%)                  if let ManaSymbol::Generic(n) = s {
        .                               Some(*n)
        .                           } else {
        .                               None
        .                           }
        .                       })
        .                       .sum();
        .           
   17,136 ( 0.00%)          if generic > 0 {
        .                       let have = tmp.total();
    9,264 ( 0.00%)              if generic > have {
        .                           return Err(ManaError::InsufficientGeneric {
        .                               needed: generic,
        .                               have,
        .                           });
        .                       }
        .                       // Drain colorless first — generic pips shouldn't eat colored
        .                       // mana a follow-up payment (splice cost, kicker, a second
        .                       // spell) might need. Any {C} pips in THIS cost were already
        .                       // taken in pass 2.
        .                       let mut rem = generic;
        .                       let drain = rem.min(tmp.colorless);
   13,896 ( 0.00%)              tmp.colorless -= drain;
    4,632 ( 0.00%)              rem -= drain;
   18,020 ( 0.00%)              for color in Color::ALL {
        .                           if rem == 0 { break; }
        .                           let drain = rem.min(tmp.amount(color));
   37,212 ( 0.00%)                  *tmp.slot_mut(color) -= drain;
    7,772 ( 0.00%)                  rem -= drain;
        .                       }
        .                   }
        .           
        .                   // The snow counter tracks "of the mana in the pool, how many
        .                   // came from snow sources." Passes 1/2/3/4/6 drain colored or
        .                   // colorless buckets without knowing which underlying mana was
        .                   // snow, so the counter can drift above the remaining total.
        .                   // Clamp it down — we can't distinguish which mana was snow, so
        .                   // conservatively cap snow at the actual remaining pool size.
   17,136 ( 0.00%)          tmp.snow = tmp.snow.min(tmp.total());
        .                   tmp.clamp_creature();
        .           
  119,952 ( 0.00%)          *self = tmp;
   34,272 ( 0.00%)          Ok(side_effects)
   80,028 ( 0.00%)      }
        .           
        .               /// Pay `cost` using **only** mana a creature produced (CR 106.6b —
        .               /// Myr Superion). The creature-produced portion of the pool is lifted
        .               /// into a scratch pool, paid there with the ordinary [`pay`] rules, and
        .               /// the per-bucket difference is charged back to the real pool. On
        .               /// failure the pool is left unchanged.
        .               pub fn pay_creature_only(&mut self, cost: &ManaCost) -> Result<PaymentSideEffects, ManaError> {
        .                   let mut work = ManaPool::default();
-- line 1320 ----------------------------------------
-- line 1341 ----------------------------------------
        .               /// mana whose restriction permits `kind` to contribute. Restricted
        .               /// mana of a color is drained ahead of unrestricted mana of that color
        .               /// so it isn't wasted when the pool empties (CR 605 lets restricted
        .               /// mana be applied freely; spending it first is always legal once the
        .               /// full cost is covered). When the pool holds no usable restricted
        .               /// mana — the overwhelmingly common case — this is exactly [`pay`].
        .               ///
        .               /// On failure the pool is left unchanged.
   62,244 ( 0.00%)      pub fn pay_for_spell(
        .                   &mut self,
        .                   cost: &ManaCost,
        .                   kind: &SpellKind,
        .               ) -> Result<PaymentSideEffects, ManaError> {
        .                   // CR 106.6b — a creature-mana-only spell ignores the restricted
        .                   // pools entirely; only tagged provenance can fund it.
   17,784 ( 0.00%)          if kind.creature_mana_only {
        .                       return self.pay_creature_only(cost);
        .                   }
        .                   // How much restricted mana, per color, may fund a spell of `kind`?
   26,676 ( 0.00%)          let mut spendable = [0u32; 5];
        .                   for (c, n, r) in &self.restricted {
        .                       if r.allows(kind) {
        .                           spendable[color_index(*c)] += *n;
        .                       }
        .                   }
        .                   let spendable_colorless: u32 = self
        .                       .restricted_colorless
        .                       .iter()
        .                       .filter(|(_, r)| r.allows(kind))
        .                       .map(|(n, _)| *n)
        .                       .sum();
    8,892 ( 0.00%)          if spendable.iter().all(|n| *n == 0) && spendable_colorless == 0 {
        .                       // No usable restricted mana — identical to the plain path,
        .                       // which leaves the restricted bucket untouched.
   35,568 ( 0.00%)              return self.pay(cost);
3,018,802 ( 0.10%)  => crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::pay (8,892x)
        .                   }
        .           
        .                   // Fold the spendable restricted mana into a working clone's flat
        .                   // buckets and pay there. Within a color, restricted and
        .                   // unrestricted mana are fungible for payment, so the total spent
        .                   // per color is fixed by `pay`; we only have to split the drain
        .                   // afterward (restricted first).
        .                   let mut work = self.clone();
-- line 1383 ----------------------------------------
-- line 1446 ----------------------------------------
        .                   // Folding restricted mana into the flat buckets inflated `total()`
        .                   // for the snow clamp in `pay`; re-clamp against the real total now
        .                   // that the restricted remainder is back out of the buckets.
        .                   result.snow = result.snow.min(result.total());
        .                   result.clamp_creature();
        .           
        .                   *self = result;
        .                   Ok(side_effects)
   80,028 ( 0.00%)      }
        .           
        .               /// Spend `n` generic mana from the pool, draining colorless first, then
        .               /// colors in WUBRG order. Panics if `total() < n`.
        .               pub fn spend_generic(&mut self, mut n: u32) {
        .                   let drain = n.min(self.colorless);
        .                   self.colorless -= drain;
        .                   n -= drain;
        .                   for color in [Color::White, Color::Blue, Color::Black, Color::Red, Color::Green] {
-- line 1462 ----------------------------------------
-- line 1467 ----------------------------------------
        .                       n -= drain;
        .                   }
        .                   // See `pay()`: snow counter doesn't track which mana was snow.
        .                   // Clamp conservatively so we never claim more snow than total.
        .                   self.snow = self.snow.min(self.total());
        .                   self.clamp_creature();
        .               }
        .           
    6,872 ( 0.00%)      pub fn empty(&mut self) {
   30,924 ( 0.00%)          *self = Self::default();
    6,872 ( 0.00%)      }
        .           
        .               /// True iff the pool holds nothing at all. Stricter than `total() == 0`,
        .               /// which by design ignores the snow/creature provenance counters and both
        .               /// spend-restricted lists — `empty()` clears those too, so a caller
        .               /// skipping the call needs this.
        .               pub fn is_empty(&self) -> bool {
  204,352 ( 0.01%)          self.total() == 0
        .                       && self.snow == 0
  102,176 ( 0.00%)              && self.restricted.is_empty()
  766,320 ( 0.02%)              && self.restricted_colorless.is_empty()
        .                       && self.creature.iter().all(|&n| n == 0)
   51,088 ( 0.00%)      }
        .           
        .               /// Fold every bucket of `other` into this pool (colors, colorless, snow,
        .               /// and spend-restricted entries). Used to restore protected floating mana
        .               /// after paying a cost from freshly-tapped sources (the "keep my floating
        .               /// mana" branch of the float-spend confirmation).
        .               pub fn absorb(&mut self, other: &ManaPool) {
        .                   self.white += other.white;
        .                   self.blue += other.blue;
-- line 1497 ----------------------------------------
-- line 1523 ----------------------------------------
        .                   self.red = self.red.saturating_sub(other.red);
        .                   self.green = self.green.saturating_sub(other.green);
        .                   self.colorless = self.colorless.saturating_sub(other.colorless);
        .                   self.snow = self.snow.min(self.total());
        .                   self.clamp_creature();
        .               }
        .           
        .               fn slot(&self, color: Color) -> &u32 {
   74,014 ( 0.00%)          match color {
        .                       Color::White => &self.white,
        .                       Color::Blue => &self.blue,
        .                       Color::Black => &self.black,
        .                       Color::Red => &self.red,
        .                       Color::Green => &self.green,
        .                   }
        .               }
        .           
        .               fn slot_mut(&mut self, color: Color) -> &mut u32 {
   18,296 ( 0.00%)          match color {
        .                       Color::White => &mut self.white,
        .                       Color::Blue => &mut self.blue,
        .                       Color::Black => &mut self.black,
        .                       Color::Red => &mut self.red,
        .                       Color::Green => &mut self.green,
        .                   }
        .               }
        .           }
-- line 1549 ----------------------------------------
-- line 1611 ----------------------------------------
        .           }
        .           pub fn r() -> ManaSymbol {
        .               ManaSymbol::Colored(Color::Red)
        .           }
        .           pub fn g() -> ManaSymbol {
        .               ManaSymbol::Colored(Color::Green)
        .           }
        .           
   13,342 ( 0.00%)  pub fn cost(symbols: &[ManaSymbol]) -> ManaCost {
        .               ManaCost::new(symbols.to_vec())
   13,342 ( 0.00%)  }
        .           
        .           #[cfg(test)]
        .           #[path = "tests/mana.rs"]
        .           mod tests;
        .           
        .           #[cfg(test)]
        .           mod hybrid_solver_tests {
        .               use super::*;
-- line 1629 ----------------------------------------

  546,568 ( 0.02%)  <counts for unidentified lines in crabomination_base/src/mana.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/affordances.rs
--------------------------------------------------------------------------------
Ir              

-- line 20 ----------------------------------------
     .               /// Damping Sphere mana tax, hexproof targets, stolen permanents,
     .               /// summoning sickness, …) without re-implementing every engine
     .               /// rule on the bot side.
     .               ///
     .               /// Cost: one full `GameState::clone` + one `perform_action`. The
     .               /// random bot does this only on actions it's about to submit, so
     .               /// the overhead is bounded by the number of bot ticks, not by
     .               /// the size of the search space.
45,918 ( 0.00%)      pub fn would_accept(&self, action: GameAction) -> bool {
     .                   // pending_decision routes through `submit_decision`, which
     .                   // both reads AND writes `self.pending_decision`. Cloning is
     .                   // safe but the dry-run can spuriously reject a legal answer
     .                   // because the cloned decider drops scripted state. For the
     .                   // bot's purposes — filtering out illegal `CastSpell` / land
     .                   // taps / attacks — the no-pending-decision path is what
     .                   // matters; if a decision is pending the bot uses
     .                   // `SubmitDecision` directly which doesn't go through here.
     .                   //
     .                   // `perform_action_inner`: the probe is discarded either way, so
     .                   // the transactional checkpoint would be pure waste here.
10,204 ( 0.00%)          let mut probe = self.clone();
11,738,714 ( 0.38%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (5,102x)
 5,102 ( 0.00%)          let acting = self.priority.player_with_priority;
86,734 ( 0.00%)          let ok = probe.perform_action_inner(action).is_ok();
436,500,892 (14.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::perform_action_inner (5,102x)
1,453,040 ( 0.05%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (5,102x)
14,788 ( 0.00%)          ok && !self.suspended_without_completing(&probe, acting)
40,816 ( 0.00%)      }
31,414,547 ( 1.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::GameState> (5,102x)
     .           
     .               /// Whether `probe` came back `Ok` only because the action *suspended*
     .               /// into a replay modal — cost pick, {X} pick, float-spend confirm —
     .               /// rather than completing.
     .               ///
     .               /// Only applied to seats that can't be prompted (`!manual_mana`, i.e.
     .               /// bots). For a hand-paying seat a suspend genuinely means "you can
     .               /// begin casting this", which is what the client's castable-hand
-- line 52 ----------------------------------------
-- line 55 ----------------------------------------
     .               /// For a bot it is a lie with teeth: the bot reads `Ok`, commits a cast
     .               /// it cannot pay for, answers the modal, and the failed replay is rolled
     .               /// back with the decision restored — the deadlock that silently dropped
     .               /// 14 % of cube games. Moving the individual cost modals to
     .               /// `manual_mana` fixed the cases the evidence named; this closes the
     .               /// class, so a modal that is added later (or one of the targeting
     .               /// suspends still keyed on `wants_ui`) can't reopen it.
     .               fn suspended_without_completing(&self, probe: &GameState, acting: usize) -> bool {
13,752 ( 0.00%)          if self.players.get(acting).is_some_and(|p| p.manual_mana) {
     .                       return false;
     .                   }
     .                   probe.pending_decision.as_ref().is_some_and(|pd| pd.resume.is_action_replay())
     .               }
     .           
     .               /// A clone of `self` with every player's library emptied, for use as a
     .               /// reusable dry-run *template* by the from-hand affordance probes
     .               /// ([`would_accept_on`](Self::would_accept_on)).
-- line 71 ----------------------------------------
-- line 99 ----------------------------------------
     .               /// The general form of [`could_pay_generic`](Self::could_pay_generic):
     .               /// whether `seat` could pay an arbitrary cost right now. Probed on a clone,
     .               /// so nothing is spent.
     .               pub(crate) fn could_pay_cost(&self, seat: usize, cost: &crate::mana::ManaCost) -> bool {
     .                   cost.cmc() == 0
     .                       || self.affordance_probe_template().try_pay_with_auto_tap(seat, cost).is_ok()
     .               }
     .           
63,216 ( 0.00%)      pub(crate) fn affordance_probe_template(&self) -> GameState {
 7,024 ( 0.00%)          let mut template = self.clone();
16,001,685 ( 0.51%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:<crabomination::game::GameState as core::clone::Clone>::clone (7,024x)
     .                   for p in &mut template.players {
     .                       // A fresh empty box, not `.clear()` — clearing would CoW-copy
     .                       // the shared library first only to drop its contents.
98,336 ( 0.00%)              p.library = Default::default();
42,719,202 ( 1.37%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (14,048x)
     .                   }
28,096 ( 0.00%)          template
2,900,912 ( 0.09%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (7,024x)
63,216 ( 0.00%)      }
     .           
     .               /// Dry-run `action` against a prebuilt [`affordance_probe_template`]
     .               /// instead of cloning the whole `GameState`. Equivalent to
     .               /// [`would_accept`](Self::would_accept) for cast / activate / play-land
     .               /// actions (see the template doc for why library contents are
     .               /// irrelevant to their legality), but cheap to repeat across a hand.
     .               ///
     .               /// [`affordance_probe_template`]: Self::affordance_probe_template
-- line 123 ----------------------------------------
-- line 1523 ----------------------------------------
     .               /// hand whose quality matches and whose splice cost (mana plus any
     .               /// `splice_extra_cost`) is payable on top. Dry-run-gated per candidate so
     .               /// a splicer the seat can't afford alongside the host never shows up.
     .               /// Hosts with no affordable splicer are omitted.
     .               ///
     .               /// `pub(crate)` so the bot can ask for *this* category alone against the
     .               /// probe template it already built, instead of paying for the whole
     .               /// [`compute_hand_affordances`](Self::compute_hand_affordances) sweep.
56,192 ( 0.00%)      pub(crate) fn spliceable_hand_cards_on(
     .                   &self,
     .                   template: &GameState,
     .                   seat: usize,
     .               ) -> Vec<(CardId, Vec<CardId>)> {
     .                   use crate::card::Keyword;
     .                   let splicers: Vec<(CardId, crate::card::SpellSubtype)> = self.players[seat]
     .                       .hand
     .                       .iter()
     .                       .filter_map(|c| {
13,296 ( 0.00%)                  c.definition.keywords.iter().find_map(|k| match k {
     .                               Keyword::Splice(_, quality) => Some((c.id, *quality)),
     .                               _ => None,
     .                           })
     .                       })
     .                       .collect();
14,048 ( 0.00%)          if splicers.is_empty() {
     .                       return Vec::new();
     .                   }
     .                   let hosts: Vec<(CardId, Vec<crate::card::SpellSubtype>)> = self.players[seat]
     .                       .hand
     .                       .iter()
     .                       .filter(|c| !c.definition.subtypes.spell_subtypes.is_empty())
     .                       .map(|c| (c.id, c.definition.subtypes.spell_subtypes.clone()))
     .                       .collect();
-- line 1555 ----------------------------------------
-- line 1570 ----------------------------------------
     .                                           x_value: None,
     .                                       })
     .                               })
     .                               .map(|(sid, _)| *sid)
     .                               .collect();
     .                           (!usable.is_empty()).then_some((host, usable))
     .                       })
     .                       .collect()
56,192 ( 0.00%)      }
     .           
     .               /// CR 702.160 — hand cards with a Prototype face castable for the
     .               /// prototype cost right now. Auto-targets the body's effect (ETB
     .               /// triggers, not a cast target — prototype creatures don't target on
     .               /// cast), so cost payability and timing are what gate the report.
     .               fn prototypable_hand_cards_on(&self, template: &GameState, seat: usize) -> Vec<CardId> {
     .                   self.players[seat]
     .                       .hand
-- line 1586 ----------------------------------------
-- line 1795 ----------------------------------------
     .               /// Extra generic mana the caster owes on top of `card`'s printed
     .               /// cost — Damping Sphere's "+1 after the first spell each turn,"
     .               /// Chancellor of the Annex's first-spell tax, etc. Public so the
     .               /// bot's affordability check can match the engine's payment path:
     .               /// `can_afford` ignoring this tax causes the bot to repeatedly
     .               /// submit a `CastSpell` that the engine then rejects with a mana
     .               /// shortfall, which (in spectate mode) deadlocks the match.
     .               pub fn extra_cost_for_card_in_hand(&self, caster: usize, card_id: CardId) -> u32 {
25,404 ( 0.00%)          let Some(card) = self.players[caster]
     .                       .hand
     .                       .iter()
     .                       .find(|c| c.id == card_id)
     .                   else {
     .                       return 0;
     .                   };
50,808 ( 0.00%)          crate::game::actions::extra_cost_for_spell(self, caster, card, None)
2,650,624 ( 0.09%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::extra_cost_for_spell (12,702x)
     .               }
     .           }
     .           
     .           /// True if `effect` is a pure mana ability (CR 605.1a) — it only adds mana
     .           /// and has no other rider. Such abilities don't use the stack and are
     .           /// auto-tapped on demand during cost payment, so the affordance probe
     .           /// excludes them from `activatable_permanents` (a mana dork is not an
     .           /// instant-speed "play" that should hold priority).
-- line 1818 ----------------------------------------

43,726 ( 0.00%)  <counts for unidentified lines in crabomination/src/game/affordances.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/player.rs
--------------------------------------------------------------------------------
Ir                 

-- line 67 ----------------------------------------
        .               /// The player conceded (CR 104.3a — a player can concede at any time).
        .               Conceded,
        .               /// A "you lose the game" effect or other cause.
        .               Other,
        .           }
        .           
        .           /// A seat's mutable state. Reached only through [`Player`], which owns it
        .           /// behind an `Arc` — see that type for why.
4,450,960 ( 0.14%)  #[derive(Debug, Clone, Serialize, Deserialize)]
        .           pub struct PlayerData {
   54,280 ( 0.00%)      pub id: PlayerId,
  162,840 ( 0.01%)      pub name: String,
6,464,316 ( 0.21%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/string.rs:<alloc::string::String as core::clone::Clone>::clone (54,280x)
        .               pub life: i32,
        .               /// The life total this player began the game with (CR 103.4). Set by
        .               /// `apply_format`; drives "N more/less than your starting life total"
        .               /// thresholds (Righteous Valkyrie, Speaker of the Heavens). `#[serde(default
        .               /// = "default_starting_life")]` for snapshot back-compat.
        .               #[serde(default = "default_starting_life")]
        .               pub starting_life: i32,
        .               pub mana_pool: ManaPool,
        .               /// Top of library is `library[0]`.
        .               ///
        .               /// The card zones are [`CowBox`]-wrapped so a `GameState` clone
        .               /// (affordance probes, the `perform_action` checkpoint) shares them
        .               /// until written — see `crate::cow`.
  108,560 ( 0.00%)      pub library: CowBox<Vec<CardInstance>>,
  108,560 ( 0.00%)      pub hand: CowBox<Vec<CardInstance>>,
  108,560 ( 0.00%)      pub graveyard: CowBox<Vec<CardInstance>>,
        .               /// The command zone — Commander commanders, Conspiracies, etc.
        .               /// (Phase I.) Cards arrive here either at game start (initial
        .               /// commander seating via `seat_commanders`) or via a zone-change
        .               /// replacement effect when they would otherwise leave the
        .               /// battlefield (CR 903.9b).
        .               ///
        .               /// `#[serde(default)]` so snapshots written before the field
        .               /// existed deserialize cleanly as empty.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub command: CowBox<Vec<CardInstance>>,
        .               /// CR 609.4b — "you may spend mana as though it were mana of any type"
        .               /// for the rest of this turn (North Star). Cleared at cleanup.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub may_spend_any_color_this_turn: bool,
        .               /// CR 407 — cards this player owns in the ante zone. Only ever non-empty
        .               /// while `GameState.playing_for_ante`; the winner takes all of it
        .               /// (CR 407.2). `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub ante: CowBox<Vec<CardInstance>>,
        .               /// CR 904.3 — the archenemy's face-down scheme deck. Like the command
        .               /// zone it never empties into another zone: a scheme is set in motion off
        .               /// the top (CR 904.9) into `command`, and abandoned back to the bottom
        .               /// here (CR 904.10). `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub scheme_deck: CowBox<Vec<CardInstance>>,
        .               /// CR 901.3 — this player's face-down planar deck. Like the scheme deck it
        .               /// never empties into another zone: planeswalking moves the top card into
        .               /// `command` face up (CR 901.11) and puts the plane left behind on the
        .               /// bottom. `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub planar_deck: CowBox<Vec<CardInstance>>,
        .               /// CR 717.2 — this player's face-down Attraction deck, in the command
        .               /// zone. Cards leave it only by being opened onto the battlefield
        .               /// (CR 701.51b). `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub attraction_deck: CowBox<Vec<CardInstance>>,
        .               /// CR 717.6a — the face-up "junkyard" pile of Attraction cards that would
        .               /// have gone anywhere but the battlefield, exile, or the command zone.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub attraction_junkyard: CowBox<Vec<CardInstance>>,
        .               /// CR 901.9 — how many times this player has taken the roll-the-planar-die
        .               /// special action this turn; the next roll costs that much generic mana.
        .               /// Reset at the turn boundary.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub planar_die_rolls_this_turn: u32,
        .               /// CR 406 / 701.45 — the Lessons "sideboard" (cards owned from outside
        .               /// the game). A Learn ability may reveal a Lesson card here and put it
        .               /// into hand. Populated by deck construction; empty by default (in
        .               /// which case Learn falls back to the legacy `Draw 1` approximation).
        .               /// `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub sideboard: CowBox<Vec<CardInstance>>,
        .               /// CardIds of cards this player has designated as Commanders
        .               /// (Phase J). Populated by `GameState::seat_commanders`. Read by
        .               /// the Phase M 21-commander-damage SBA via
        .               /// `GameState::is_commander`. Note this is *which cards are
        .               /// commanders for this player*, independent of the command zone
        .               /// — a commander on the battlefield (or any other zone) is
        .               /// still a commander, so the entry survives zone changes.
        .               #[serde(default)]
        .               pub commanders: Vec<CardId>,
        .               /// How many lands this player has played on their current turn.
  108,560 ( 0.00%)      pub lands_played_this_turn: u32,
        .               /// CR 605 — this player tapped a land for mana this turn (Desolation).
        .               /// Cleared at cleanup.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub tapped_land_for_mana_this_turn: bool,
        .               /// Extra land plays granted this turn (Explore, Oracle of Mul Daya,
        .               /// Dryad of the Ilysian Grove, etc.). Defaults to 0. The player can
        .               /// play `1 + extra_land_plays` lands per turn total.
        .               #[serde(default)]
        .               pub extra_land_plays: u32,
        .               /// The Chain Veil — extra loyalty activations available this turn on top
        .               /// of the CR 606.3 one-per-planeswalker limit. Cleared each turn.
        .               #[serde(default)]
        .               pub extra_loyalty_activations: u32,
        .               /// Whether this player has activated a loyalty ability this turn (The
        .               /// Chain Veil's end-step "if you didn't …" check). Cleared each turn.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub activated_loyalty_this_turn: bool,
        .               /// How many spells this player has cast this turn. Reset on
        .               /// `TurnStarted`. Powers Damping Sphere's "second-and-onward spells
        .               /// cost {1} more" static.
        .               pub spells_cast_this_turn: u32,
        .               /// Sorcery spells cast this turn — Backdraft's "a player who cast one or
        .               /// more sorcery spells this turn". Reset at Cleanup.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub sorceries_cast_this_turn: u32,
        .               /// Per-name lifetime cast counter — "you've cast another spell named X
        .               /// this game" (Approach of the Second Sun). Bumped in `finalize_cast`
        .               /// with the cast card's printed name; never reset. Defaults empty for
        .               /// snapshot back-compat.
        .               /// Keyed by the printed `&'static str` name, not an owned `String`: this
        .               /// map is deep-cloned with every `GameState` clone (millions per bot
        .               /// self-play game) and an owned key allocates per entry per clone.
        .               #[serde(with = "crate::static_str_serde::map_u32", default)]
-- line 190 ----------------------------------------
-- line 273 ----------------------------------------
        .               /// Last turn's `creatures_entered_this_turn` (see above).
        .               #[serde(default)]
        .               pub creatures_entered_last_turn: Vec<crate::card::CardId>,
        .               /// CR 603 — count of artifacts that entered under this player's control
        .               /// this turn. Drives "if an artifact entered the battlefield under your
        .               /// control this turn" intervening-`if` gates (Akal Pakal). Reset at the
        .               /// active player's turn boundary.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub artifacts_entered_this_turn: u32,
        .               /// Count of planeswalkers that entered under this player's control this
        .               /// turn — the `Predicate::PlaneswalkerEnteredThisTurn` gate (Oath of
        .               /// Chandra). Reset at the active player's turn boundary.
        .               #[serde(default)]
        .               pub planeswalkers_entered_this_turn: u32,
        .               /// CR 702.176-era **Celebration** (WOE) — count of nonland permanents that
        .               /// entered under this player's control this turn. Gates "if two or more
        .               /// nonland permanents entered … this turn" (`Predicate::CelebrationActive`
-- line 289 ----------------------------------------
-- line 293 ----------------------------------------
        .               /// DFT — count of Mounts and/or Vehicles that entered under this player's
        .               /// control this turn (`Value::MountsVehiclesEnteredThisTurn` — Cloudspire
        .               /// Coordinator's token count). Reset at the active player's turn boundary.
        .               #[serde(default)]
        .               pub mounts_vehicles_entered_this_turn: u32,
        .               /// Multicolored spells this player has cast this turn (Zenith
        .               /// Chronicler's "first multicolored spell each turn"). Reset at cleanup.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub multicolored_spells_cast_this_turn: u32,
        .               /// Churning Reservoir's gate: an oil counter left one of this player's
        .               /// permanents this turn, or an oil-countered permanent of theirs hit a
        .               /// graveyard. Reset at cleanup.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub oil_activity_this_turn: bool,
        .               /// Channel — until end of turn this player may pay 1 life per missing
        .               /// generic/colorless mana ("pay 1 life: add {C}"). The payment funnel
        .               /// converts life into the shortfall on demand. Reset at cleanup.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub channel_life_for_mana: bool,
        .               /// CR 309 — the dungeon this player is currently in: (name, room index).
        .               /// `None` between dungeons; venturing with `None` enters a new dungeon.
        .               #[serde(default)]
        .               pub dungeon: Option<(String, u8)>,
        .               /// CR 701.49d — dungeons this player has completed this game.
        .               #[serde(default)]
        .               pub dungeons_completed: u32,
        .               /// CR 603.7e — pending "your next creature spell this turn enters with N
-- line 319 ----------------------------------------
-- line 324 ----------------------------------------
        .               /// expires with the turn. `#[serde(default)]`.
        .               #[serde(default)]
        .               pub pending_creature_etb_counters: Vec<(crate::card::CounterType, u32)>,
        .               /// CR 603.7e — pending "your next creature spell this turn enters with these
        .               /// keywords" riders (Summon: Brynhildr's "Gestalt Mode" haste). Applied to
        .               /// the next creature spell's permanent as it enters and cleared at cleanup.
        .               /// `#[serde(default)]`.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub pending_creature_etb_keywords: Vec<crate::card::Keyword>,
1,791,240 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// CR 702.139 — true if a permanent left the battlefield under this
        .               /// player's control so far this turn (Revolt). Set from the battlefield-
        .               /// removal funnels keyed off the leaving permanent's controller; reset at
        .               /// the player's `do_untap`. Defaults to false for snapshot back-compat.
        .               #[serde(default)]
        .               pub permanent_left_battlefield_this_turn: bool,
        .               /// True if this player has been dealt damage so far this turn. Set in
        .               /// `deal_damage_to_from`'s player branch (combat or non-combat, incl.
-- line 340 ----------------------------------------
-- line 400 ----------------------------------------
        .               /// to a player this turn (CR 702.76 Prowl). Stamped at the combat-damage
        .               /// funnels, cleared at the turn boundary. `#[serde(default)]` for
        .               /// snapshot back-compat.
        .               #[serde(default)]
        .               pub prowl_types_this_turn: Vec<crate::card::CreatureType>,
        .               /// A Changeling of this player's dealt combat damage to a player this
        .               /// turn — it counts as every creature type for the prowl window.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub prowl_any_type_this_turn: bool,
        .               /// True once this player has declared an attacker this turn (Raid, CR
        .               /// 702.108 ability word). Set in `declare_attackers`, reset at the turn
        .               /// boundary in `do_untap`. `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
        .               pub attacked_this_turn: bool,
        .               /// Number of creatures this player declared as attackers this turn.
        .               /// Powers `Value::CreaturesAttackedWithThisTurn` (Windbrisk Heights).
        .               #[serde(default)]
  108,560 ( 0.00%)      pub creatures_attacked_this_turn: u32,
        .               /// True once a creature this player controlled dealt combat damage to a
        .               /// player this turn (CR 702.179 — Freerunning). Set in
        .               /// `fire_combat_damage_to_player_triggers`, reset at the turn boundary.
        .               #[serde(default)]
        .               pub dealt_combat_damage_to_player_this_turn: bool,
        .               /// CR 614.5 — "sources you control deal double damage this turn" (Quest
        .               /// for Pure Flame). Read in `scale_damage_to`, reset at the turn boundary.
        .               #[serde(default)]
        .               pub double_your_source_damage_this_turn: bool,
        .               /// CR 305.1 — "target player can't play lands this turn" (Turf Wound).
        .               /// Consulted by `can_player_play_land`, reset at the turn boundary.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub cant_play_lands_this_turn: bool,
        .               /// "You can't cast [filter] spells this turn" (Cease-Fire). Cleared at the
        .               /// turn boundary alongside `cant_play_lands_this_turn`.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub cant_cast_matching_this_turn: Vec<crate::card::SelectionRequirement>,
1,845,520 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// Abeyance — "that player can't activate abilities that aren't mana
        .               /// abilities" this turn. Cleared at the turn boundary.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub cant_activate_nonmana_abilities_this_turn: bool,
        .               /// Winding Canyons — "you may cast creature spells this turn as though they
        .               /// had flash". Cleared at the turn boundary.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub creature_spells_as_flash_this_turn: bool,
        .               /// Bösium Strip — "until end of turn, you may cast instant and sorcery
        .               /// spells from the top of your graveyard". Cleared at the turn boundary.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub cast_from_graveyard_top_this_turn: bool,
        .               /// "The next [filter] spell you cast this turn can't be countered"
        .               /// (Insist, Overmaster). Consumed by the next matching cast; cleared at
        .               /// end of turn.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub next_spell_uncounterable: Vec<crate::card::SelectionRequirement>,
1,845,520 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// CR 700.13 — this player has committed a crime this turn (cast a spell or
        .               /// activated an ability targeting an opponent / their stuff). Set when a
        .               /// `CommittedCrime` event fires, reset at the turn boundary. Powers
        .               /// "if you've committed a crime this turn" gates (Nimble Brigand).
        .               #[serde(default)]
  108,560 ( 0.00%)      pub committed_crime_this_turn: bool,
        .               /// CR 708 — a permanent entered the battlefield face down under this
        .               /// player's control, or they turned a permanent face up, this turn. Powers
        .               /// Oblivious Bookworm's "unless … entered face down / you turned a permanent
        .               /// face up this turn" discard-skip. Reset at the turn boundary.
        .               #[serde(default)]
        .               pub face_down_activity_this_turn: bool,
        .               /// Warped Space — this player has already used their once-per-turn
        .               /// "pay {0} rather than pay the mana cost for a spell you cast from
        .               /// exile". Reset at the turn boundary.
        .               #[serde(default)]
        .               pub free_exile_cast_used_this_turn: bool,
        .               /// Creatures exiled from under this player's control this turn (Vren, the
        .               /// Relentless' end-step Rat count). Reset at the turn boundary.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub creatures_exiled_from_control_this_turn: u32,
        .               /// Revel in Silence: this player can't cast spells or activate loyalty
        .               /// abilities for the rest of the turn. Reset at the turn boundary.
        .               #[serde(default)]
        .               pub silenced_this_turn: bool,
        .               /// True once this player has cast a spell for its Warp cost this turn (EOE).
        .               /// Reset at the turn boundary; the right half of `Predicate::VoidActive`.
        .               #[serde(default)]
        .               pub warped_spell_this_turn: bool,
-- line 480 ----------------------------------------
-- line 494 ----------------------------------------
        .               pub protected_from_everything: bool,
        .               /// CR 401.6 — turn-scoped "you may look at the top card of your library
        .               /// any time, and you may play lands and cast spells from the top of your
        .               /// library" grant (The Belligerent, Bonehoard's Dracosaur-style player
        .               /// permissions). Unlike `StaticEffect::PlayFromLibraryTop` this is a
        .               /// player flag set at resolution and cleared at end of turn. Read by
        .               /// `library_top_playable` and the top-revealed view.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub play_from_top_this_turn: bool,
        .               /// CR 603.3d-adjacent — set once this player casts/plays a card from the
        .               /// top of their library under a `PlayFromLibraryTopOncePerTurn` grant
        .               /// (Johann, Apprentice Sorcerer). Cleared at end of turn. Read by
        .               /// `library_top_playable` to enforce the once-per-turn cap.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub cast_from_library_top_this_turn: bool,
        .               /// Number of cards this player has caused to be put into exile on
        .               /// the current turn. Reset to 0 in `do_untap`. Powers Strixhaven
        .               /// "if one or more cards were put into exile this turn" payoffs
        .               /// (Ennis the Debate Moderator). Bumped from `place_card_in_dest`'s
        .               /// exile branch and the battlefield-to-exile path in
        .               /// `Effect::Exile`. Defaults to 0 for snapshot back-compat.
        .               #[serde(default)]
        .               pub cards_exiled_this_turn: u32,
-- line 516 ----------------------------------------
-- line 578 ----------------------------------------
        .               pub pending_is_discounts: Vec<(u32, u32)>,
        .               /// Like `pending_is_discounts` but for *any* next spell this turn
        .               /// (Mutated Cultist): `(amount, spells_cast_this_turn at grant)`.
        .               #[serde(default)]
        .               pub pending_spell_discounts: Vec<(u32, u32)>,
        .               /// Cards this player discarded this turn (Hollow One's cost reduction).
        .               /// Bumped in `discard_card`; reset in `do_untap`.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub cards_discarded_this_turn: u32,
        .               /// CR 702.187 — ids of cards this player discarded this turn, so Mayhem
        .               /// can offer a graveyard cast only for cards actually discarded this turn.
        .               /// Populated in `discard_card`; cleared in `do_untap`.
        .               #[serde(default)]
        .               pub discarded_this_turn: crate::fxhash::HashSet<crate::card::CardId>,
        .               /// Number of permanents this player has sacrificed so far this turn.
        .               /// Bumped in `dispatch_triggers_for_events` per `PermanentSacrificed`
        .               /// event; reset in `do_untap`. Powers "if you sacrificed a permanent
        .               /// this turn" payoffs (Sawblade Skinripper).
        .               #[serde(default)]
  108,560 ( 0.00%)      pub permanents_sacrificed_this_turn: u32,
        .               /// Number of *artifacts* this player has sacrificed so far this turn (a
        .               /// subset of `permanents_sacrificed_this_turn`). Bumped in
        .               /// `dispatch_triggers_for_events` per `PermanentSacrificed` event whose
        .               /// subject snapshot is an artifact; reset in `do_untap`. Powers "if you've
        .               /// sacrificed an artifact this turn" riders (Suspicious Detonation,
        .               /// Furtive Courier).
        .               #[serde(default)]
        .               pub artifacts_sacrificed_this_turn: u32,
        .               /// "[Filter] spells you cast this turn cost {N} less" grants
        .               /// (`Effect::SpellsCostLessThisTurn` — Urza, Planeswalker's +2).
        .               /// Each entry applies to every matching spell for the rest of the
        .               /// turn; cleared in `finish_cleanup` (CR 514.2).
        .               #[serde(default)]
  162,840 ( 0.01%)      pub turn_spell_discounts: Vec<(crate::card::SelectionRequirement, u32)>,
1,736,960 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// "Face-down spells you cast this turn cost {N} less to cast" (Goblin
        .               /// Maskmaker). Summed into `face_down_cast_cost`; cleared in
        .               /// `finish_cleanup` alongside `turn_spell_discounts`.
        .               #[serde(default)]
        .               pub face_down_discount_this_turn: u32,
        .               /// Number of creature spells this player has cast on the current
        .               /// turn. Reset to 0 in `do_untap`. Powers creature-cast magecraft
        .               /// payoffs ("if you've cast a creature spell this turn, …") and
-- line 619 ----------------------------------------
-- line 762 ----------------------------------------
        .               /// Turn number through which this player may cast creature spells from
        .               /// their graveyard by foraging (Osteomancer Adept). Cleared at cleanup.
        .               #[serde(default)]
        .               pub forage_graveyard_casts_turn: Option<u32>,
        .               /// CR 502.3 — number of this player's upcoming untap steps to skip
        .               /// (Yosei, the Morning Star; Frost Titan-style locks). Decremented when
        .               /// their untap step would run; while > 0 their permanents don't untap.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub skip_next_untap_step: u32,
        .               /// CR 504 — charges of "skip your next draw step" (Fatigue). Consumed one
        .               /// per draw step.
        .               #[serde(default)]
        .               pub skip_next_draw_step: u32,
        .               /// CR 614 — queued one-shot draw replacements: "the next time you would
        .               /// draw a card this turn, [effect] instead" (the Onslaught Words cycle).
        .               /// Each entry is `(source, effect, x)`; `draw_one` pops the front instead
        .               /// of drawing, replaying the activation's X (Aladdin's Lamp). Cleared at
        .               /// the turn boundary.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub next_draw_replacements: Vec<(crate::card::CardId, crate::effect::Effect, u32)>,
1,736,960 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// CR 506 — number of this player's upcoming combat phases to skip
        .               /// (Stonehorn Dignitary). Consumed when their turn reaches Begin Combat,
        .               /// jumping straight to the postcombat main. Defaults to 0 for snapshot
        .               /// back-compat.
        .               #[serde(default)]
        .               pub skip_next_combat: u32,
        .               /// Number of this player's upcoming untap steps in which the **lands** they
        .               /// control don't untap (Bontu's Last Reckoning). Decremented when their
-- line 789 ----------------------------------------
-- line 825 ----------------------------------------
        .               /// `#[serde(default)]`.
        .               #[serde(default)]
        .               pub kept_mana_this_turn: ManaPool,
        .               /// CR 500.7 — extra turns this player will take. When `advance_turn`
        .               /// would pass the turn, an active player with `extra_turns > 0`
        .               /// decrements it and keeps the turn instead (Time Walk, Ral Zarek's
        .               /// -7 coin-flip emblem). `#[serde(default)]` for snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub extra_turns: u32,
        .               /// CR 702.50 — resolved Epic spells this player controls. Each entry is
        .               /// copied onto the stack at the beginning of this player's upkeep for
        .               /// the rest of the game; while non-empty the player can't cast spells.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub epic_spells: Vec<EpicSpell>,
1,736,960 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// CR 114 — emblems this player owns. Each carries a name (for
        .               /// display) and a set of triggered abilities that fire from the
        .               /// command zone; emblems never leave once created. The trigger
        .               /// dispatcher walks every player's emblems alongside battlefield
        .               /// permanents (event-keyed kinds in `dispatch_triggers_for_events`,
        .               /// step-keyed kinds in `fire_step_triggers`). Created by
        .               /// `Effect::CreateEmblem` (planeswalker ultimates). `#[serde(default)]`
        .               /// for snapshot back-compat.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub emblems: Vec<Emblem>,
1,791,240 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// True while a continuous effect on the battlefield prevents this
        .               /// player from gaining life (CR 119.7). Set by
        .               /// `StaticEffect::CannotGainLife` in `compute_battlefield`'s player-
        .               /// static pass, reset there each recompute. Honored by
        .               /// `GameState::adjust_life` — a positive delta is dropped while the
        .               /// flag is set. Powers Tainted Remedy / Erebos / Sulfuric Vortex
        .               /// style effects.
        .               #[serde(default)]
-- line 856 ----------------------------------------
-- line 891 ----------------------------------------
        .               #[serde(default)]
        .               pub hexproof_from_colors_this_turn: Vec<crate::mana::Color>,
        .               /// True once this player has cast a blue or black spell this turn. Set
        .               /// in `finalize_cast`; reset for every player at the active player's
        .               /// `do_untap`. Powers Veil of Summer's "draw a card if an opponent has
        .               /// cast a blue or black spell this turn" gate. `#[serde(default)]` for
        .               /// snapshot back-compat.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub cast_blue_or_black_this_turn: bool,
        .               /// Colors + card types of each spell this player cast this turn, in cast
        .               /// order. Stamped in `finalize_cast` and cleared alongside
        .               /// `cast_blue_or_black_this_turn`; read by
        .               /// `Predicate::CastSpellThisTurnWith` — the Trap alternative costs
        .               /// ("if an opponent cast a blue spell this turn", Ricochet Trap).
        .               #[serde(default)]
  162,840 ( 0.01%)      pub spell_casts_this_turn: Vec<crate::game::types::CastProfile>,
8,004,522 ( 0.26%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// True while this player has hexproof until the start of their next
        .               /// turn (Blossoming Calm). Set by `Effect::GainHexproofUntilYourNextTurn`;
        .               /// cleared at this player's `do_untap`. `#[serde(default)]`.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub hexproof_until_next_turn: bool,
        .               /// True while this player can't cast noncreature spells for the rest of
        .               /// the turn (Ranger-Captain of Eos's sacrifice ability). Set by
        .               /// `Effect::CantCastNoncreatureThisTurn`; reset for every player at the
        .               /// active player's `do_untap`. Consulted by the cast-legality gate.
        .               #[serde(default)]
        .               pub cant_cast_noncreature_this_turn: bool,
        .               /// "You may cast spells from your hand this turn without paying their
        .               /// mana costs" (Yusri's five-win jackpot). Cleared at end-of-turn.
-- line 919 ----------------------------------------
-- line 920 ----------------------------------------
        .               #[serde(default)]
        .               pub free_spells_from_hand_this_turn: bool,
        .               /// Gaea's Will — lands playable from this player's graveyard this turn.
        .               #[serde(default)]
        .               pub play_from_graveyard_this_turn: bool,
        .               /// Gaea's Will — this player's graveyard-bound cards exile instead this
        .               /// turn (CR 614.6, own cards only).
        .               #[serde(default)]
  108,560 ( 0.00%)      pub graveyard_bound_exiled_this_turn: bool,
        .               /// Card names this player's opponents can't cast until this player's next
        .               /// turn (Academic Probation mode 0 — "Opponents can't cast spells with the
        .               /// chosen name until your next turn"). Reset for every player at the active
        .               /// player's `do_untap`; consulted by the cast-legality gate.
        .               #[serde(default)]
  162,840 ( 0.01%)      pub opponents_cant_cast_named: Vec<String>,
1,791,240 ( 0.06%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (54,280x)
        .               /// Sources whose static effect this player has bought their way out of for
        .               /// the turn (`Effect::IgnoreStaticFromSourceThisTurn` — Damping Engine).
        .               /// Cleared at cleanup.
        .               #[serde(default)]
        .               pub statics_ignored_this_turn: Vec<crate::card::CardId>,
        .               /// CR 905.2b — the notes this seat took during the draft that produced
        .               /// their pool. Empty in every non-drafted game.
        .               #[serde(default)]
   54,280 ( 0.00%)      pub draft_notes: crate::draft::DraftNotes,
        .               /// When true, decisions this player would make suspend via
        .               /// `pending_decision` so a UI can respond; when false, the engine calls
        .               /// the installed `Decider` synchronously (bot / tests).
   54,280 ( 0.00%)      pub wants_ui: bool,
        .               /// CR 601.2g "proper tapping": when true this player chooses their own
        .               /// mana sources, so the engine auto-taps only when the payment is
        .               /// forced and otherwise rejects the cast with `ManualTapRequired` for
        .               /// them to tap manually.
        .               ///
        .               /// Split out from [`wants_ui`](Self::wants_ui), which used to stand in
        .               /// for it. The two are different questions — a bot wants its decisions
        .               /// surfaced as `pending_decision` (that is how its policies run) but
-- line 955 ----------------------------------------
-- line 973 ----------------------------------------
        .               /// the Commander bot test past its 600 s timeout. Kept, off, ready to
        .               /// re-measure.
        .               ///
        .               /// A per-player flag rather than a constant so the ladder can measure
        .               /// it at all: the tapping happens inside the engine, so applied to
        .               /// both seats of a mirror the comparison is structurally incapable of
        .               /// showing anything.
        .               #[serde(default)]
  108,560 ( 0.00%)      pub smart_tap: bool,
        .               /// CR 705.3 — Krark's Thumb-style coin-flip advantage. When non-zero,
        .               /// every coin flip this player makes is replayed `coin_flip_advantage`
        .               /// extra times and they get to keep the result they prefer. Practically
        .               /// modelled in `Effect::FlipCoin` as "do `1 + N` flips and treat the
        .               /// flipper as winning if any of them came up heads" — the standard
        .               /// rules interpretation of stacking Krark's Thumbs (each Thumb lets
        .               /// you "ignore one and choose the other," so two Thumbs = three flips,
        .               /// pick the best).
-- line 989 ----------------------------------------
-- line 990 ----------------------------------------
        .               ///
        .               /// `#[serde(default)]` keeps snapshots from before this field forward-
        .               /// compatible. Stacks additively when multiple Krark's Thumbs are on
        .               /// the battlefield (compute_battlefield sums the contributing
        .               /// static-ability counts when this primitive is eventually wired to
        .               /// a permanent — for now only one Krark's Thumb is needed and we set
        .               /// the value directly via the Thumb card body).
        .               #[serde(default)]
   54,280 ( 0.00%)      pub coin_flip_advantage: u32,
        .           }
        .           
        .           /// A seat: a copy-on-write handle around [`PlayerData`].
        .           ///
        .           /// `PlayerData` is ~165 fields — a `String`, ~30 per-turn `Vec`/`HashSet`
        .           /// tallies, two `HashMap`s — and `GameState::clone` copies every one of them
        .           /// per checkpoint (the `perform_action` checkpoint, every bot dry-run probe).
        .           /// Behind the `Arc` that clone is a refcount bump and only a seat actually
-- line 1006 ----------------------------------------
-- line 1017 ----------------------------------------
        .                   &self.0
        .               }
        .           }
        .           
        .           /// The unshare point: every `&mut` reach into a seat goes through here, so a
        .           /// shared seat is cloned exactly once and then written in place.
        .           impl std::ops::DerefMut for Player {
        .               #[inline]
6,447,712 ( 0.21%)      fn deref_mut(&mut self) -> &mut PlayerData {
        .                   std::sync::Arc::make_mut(&mut self.0)
7,253,676 ( 0.23%)      }
        .           }
        .           
        .           impl std::fmt::Debug for Player {
        .               fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        .                   std::fmt::Debug::fmt(&*self.0, f)
        .               }
        .           }
        .           
-- line 1035 ----------------------------------------
-- line 1049 ----------------------------------------
        .           
        .           impl<'de> Deserialize<'de> for Player {
        .               fn deserialize<D: serde::Deserializer<'de>>(d: D) -> Result<Self, D::Error> {
        .                   PlayerData::deserialize(d).map(Self::from)
        .               }
        .           }
        .           
        .           impl Player {
      144 ( 0.00%)      pub fn new(idx: usize, name: impl Into<String>) -> Self {
      512 ( 0.00%)          Self::from(PlayerData {
        .                       id: PlayerId(idx),
        .                       name: name.into(),
        .                       life: 20,
        .                       starting_life: 20,
       16 ( 0.00%)              mana_pool: ManaPool::new(),
      192 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::new (16x)
       16 ( 0.00%)              kept_mana_this_turn: ManaPool::new(),
      192 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::new (16x)
       16 ( 0.00%)              library: CowBox::default(),
       16 ( 0.00%)              hand: CowBox::default(),
       16 ( 0.00%)              graveyard: CowBox::default(),
       16 ( 0.00%)              command: CowBox::default(),
        .                       may_spend_any_color_this_turn: false,
       16 ( 0.00%)              ante: CowBox::default(),
       16 ( 0.00%)              scheme_deck: CowBox::default(),
       16 ( 0.00%)              planar_deck: CowBox::default(),
       16 ( 0.00%)              attraction_deck: CowBox::default(),
       16 ( 0.00%)              attraction_junkyard: CowBox::default(),
        .                       planar_die_rolls_this_turn: 0,
        .                       opponent_cast_spell_since_your_turn: false,
        .                       sideboard: CowBox::default(),
        .                       commanders: Vec::new(),
        .                       lands_played_this_turn: 0,
        .                       tapped_land_for_mana_this_turn: false,
        .                       extra_land_plays: 0,
        .                       cant_play_lands_this_turn: false,
-- line 1082 ----------------------------------------
-- line 1217 ----------------------------------------
        .                       epic_spells: Vec::new(),
        .                       emblems: Vec::new(),
        .                       cannot_gain_life: false,
        .                       wants_ui: false,
        .                       smart_tap: false,
        .                       manual_mana: false,
        .                       coin_flip_advantage: 0,
        .                   })
      144 ( 0.00%)      }
        .           }
        .           
        .           impl PlayerData {
        .               pub fn is_alive(&self) -> bool {
  302,328 ( 0.01%)          !self.eliminated
        .               }
        .           
        .               /// Baseline per-turn land-play check — `true` iff this player has
        .               /// not yet played any land this turn. NOTE: this is a vanilla CR
        .               /// 305.2 default and **does not** consult
        .               /// `StaticEffect::ExtraLandPerTurn` (Exploration, Azusa). For the
        .               /// CR-correct check that honors continuous-effect grants, use
        .               /// `GameState::can_player_play_land(seat)` which sums
-- line 1238 ----------------------------------------
-- line 1239 ----------------------------------------
        .               /// `extra_land_plays_per_turn(seat)` into the cap.
        .               pub fn can_play_land(&self) -> bool {
        .                   self.lands_played_this_turn < 1 + self.extra_land_plays
        .               }
        .           
        .               /// Draw the top card into hand.  Returns `None` if the library is empty.
        .               /// Increments `cards_drawn_this_turn` so per-turn draw payoffs (e.g.
        .               /// Strixhaven's Quandrix scaling) see a fresh count.
   14,640 ( 0.00%)      pub fn draw_top(&mut self) -> Option<CardId> {
    4,880 ( 0.00%)          if self.library.is_empty() {
        .                       return None;
        .                   }
    4,848 ( 0.00%)          let card = self.library.remove(0);
    2,424 ( 0.00%)          let id = card.id;
    2,424 ( 0.00%)          self.hand.push(card);
    9,696 ( 0.00%)          self.cards_drawn_this_turn = self.cards_drawn_this_turn.saturating_add(1);
        .                   self.cards_drawn_this_step = self.cards_drawn_this_step.saturating_add(1);
        .                   Some(id)
   19,520 ( 0.00%)      }
        .           
        .               /// Return all hand cards to the bottom of the library.
        .               /// Call `library.shuffle(&mut rng)` afterwards to randomize.
        .               pub fn return_hand_to_library(&mut self) {
        .                   while let Some(card) = self.hand.pop() {
        .                       self.library.push(card);
        .                   }
        .               }
        .           
        .               pub fn has_in_hand(&self, id: CardId) -> bool {
        .                   self.hand.iter().any(|c| c.id == id)
        .               }
        .           
    8,650 ( 0.00%)      pub fn remove_from_hand(&mut self, id: CardId) -> Option<CardInstance> {
        .                   self.hand
        .                       .iter()
        .                       .position(|c| c.id == id)
    7,456 ( 0.00%)              .map(|i| self.hand.remove(i))
   10,380 ( 0.00%)      }
        .           
   37,870 ( 0.00%)      pub fn send_to_graveyard(&mut self, mut card: CardInstance) {
        .                   // CR 122.2 — counters cease to exist on zone change; the graveyard
        .                   // object carries none (dies-with-counters triggers read LKI caches).
        .                   card.counters.clear();
   16,230 ( 0.00%)          card.keyword_counters.clear();
  102,790 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::KeywordCounters::clear (5,410x)
    5,410 ( 0.00%)          self.cards_to_graveyard_this_turn += 1;
   16,230 ( 0.00%)          if card.definition.is_creature() {
   75,064 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (5,410x)
    3,832 ( 0.00%)              self.creature_cards_to_graveyard_this_turn += 1;
        .                   }
    5,410 ( 0.00%)          self.graveyard_ids_this_turn.insert(card.id);
        .                   // CR 700.11 — descending requires a *permanent* card hitting the gy.
   16,230 ( 0.00%)          if card.definition.is_permanent() {
   74,632 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_permanent (5,410x)
    3,976 ( 0.00%)              self.descended_this_turn = true;
    3,976 ( 0.00%)              self.descend_count_this_turn += 1;
        .                   }
   16,230 ( 0.00%)          self.graveyard.push(card);
   27,050 ( 0.00%)      }
        .           
        .               /// Push a card definition directly into the library (top of deck = index 0).
        .               pub fn add_to_library_top(&mut self, id: CardId, definition: CardDefinition) {
        .                   self.library.insert(0, CardInstance::new(id, definition, self.id.0));
        .               }
        .           
        .               /// Push a card definition to the bottom of the library.
    8,580 ( 0.00%)      pub fn add_to_library_bottom(&mut self, id: CardId, definition: CardDefinition) {
    6,600 ( 0.00%)          self.library.push(CardInstance::new(id, definition, self.id.0));
11,764,247 ( 0.38%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::new (660x)
5,417,280 ( 0.17%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (660x)
    4,620 ( 0.00%)      }
        .           }

3,216,554 ( 0.10%)  <counts for unidentified lines in crabomination/src/player.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/cow.rs
--------------------------------------------------------------------------------
Ir                 

-- line 50 ----------------------------------------
        .           impl<T: Clone> Deref for CowBox<T> {
        .               type Target = T;
        .               fn deref(&self) -> &T {
        .                   &self.0
        .               }
        .           }
        .           
        .           impl<T: Clone> DerefMut for CowBox<T> {
2,445,920 ( 0.08%)      fn deref_mut(&mut self) -> &mut T {
  515,262 ( 0.02%)          Arc::make_mut(&mut self.0)
  356,529 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::make_mut (5,410x)
2,743,242 ( 0.09%)      }
        .           }
        .           
        .           impl<T: Clone> From<T> for CowBox<T> {
        .               fn from(value: T) -> Self {
        .                   Self::new(value)
        .               }
        .           }
        .           
-- line 68 ----------------------------------------
-- line 78 ----------------------------------------
        .           // to `for` loops, so forward IntoIterator explicitly.
        .           impl<'a, T: Clone> IntoIterator for &'a CowBox<T>
        .           where
        .               &'a T: IntoIterator,
        .           {
        .               type Item = <&'a T as IntoIterator>::Item;
        .               type IntoIter = <&'a T as IntoIterator>::IntoIter;
        .               fn into_iter(self) -> Self::IntoIter {
  889,082 ( 0.03%)          (&*self.0).into_iter()
        .               }
        .           }
        .           
        .           impl<'a, T: Clone> IntoIterator for &'a mut CowBox<T>
        .           where
        .               &'a mut T: IntoIterator,
        .           {
        .               type Item = <&'a mut T as IntoIterator>::Item;
        .               type IntoIter = <&'a mut T as IntoIterator>::IntoIter;
        .               fn into_iter(self) -> Self::IntoIter {
   25,624 ( 0.00%)          Arc::make_mut(&mut self.0).into_iter()
   85,900 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::make_mut (3,436x)
        .               }
        .           }
        .           
        .           impl<T: Clone + serde::Serialize> serde::Serialize for CowBox<T> {
        .               fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        .                   self.0.serialize(serializer)
        .               }
        .           }
-- line 105 ----------------------------------------

   27,868 ( 0.00%)  <counts for unidentified lines in crabomination/src/cow.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/effect.rs
--------------------------------------------------------------------------------
Ir                 

-- line 18 ----------------------------------------
        .           use serde::{Deserialize, Serialize};
        .           
        .           use crate::card::{CounterType, Keyword, LandType, SelectionRequirement, TokenDefinition, Zone};
        .           use crate::mana::{Color, SpendRestriction};
        .           
        .           // ── PlayerRef / ZoneRef ───────────────────────────────────────────────────────
        .           
        .           /// Lightweight reference to one or more players.
  905,966 ( 0.03%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum PlayerRef {
        .               /// The controller of the spell this resolution just countered (Fold into
        .               /// Aether's "its controller may put a creature card …"). Resolves to
        .               /// `GameState.countered_spell_controller`, stamped by the counter path.
        .               CounteredSpellController,
        .               /// The seat that said yes to the innermost [`Effect::AnyPlayerMayAccept`]
        .               /// offer (the punisher family's "have this deal damage to *them*").
        .               /// Resolves to `GameState.accepting_player`.
-- line 34 ----------------------------------------
-- line 175 ----------------------------------------
        .           
        .           // ── Selector ─────────────────────────────────────────────────────────────────
        .           
        .           /// A lazy reference to a (possibly empty, possibly multi-) set of game
        .           /// objects — permanents, cards in other zones, or players.
        .           ///
        .           /// Resolved by the effect engine at execution time against the current game
        .           /// state; used as the operand of most [`Effect`] mutations.
  108,152 ( 0.00%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum Selector {
        .               /// The source permanent/spell/ability itself.
        .               This,
        .               /// The ability/spell's controller as a "player object" (for damage, etc).
        .               You,
        .               /// A chosen target slot from the cast-time target list.
        .               Target(u8),
        .               /// A chosen target slot with a restriction that must be validated at cast time.
    1,980 ( 0.00%)      TargetFiltered { slot: u8, filter: SelectionRequirement },
  118,491 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone (660x)
        .               /// Every declared target slot (permanents/players still legal). Powers
        .               /// "then [do X] to each of those [targets]" riders that operate on the
        .               /// whole cast-time target list rather than a single slot — Biogenic
        .               /// Upgrade ("distribute counters among 1–3 creatures, then double the
        .               /// counters on each of those creatures").
        .               AllTargets,
        .               /// "…a card at random from among them" — resolve the inner selector and
        .               /// keep one entity chosen uniformly at random (Volatile Chimera).
-- line 200 ----------------------------------------
-- line 441 ----------------------------------------
        .               /// Cards destroyed earlier in this same resolution that match `filter`,
        .               /// read from `GameState.destroyed_this_resolution` (wherever they ended
        .               /// up). "Destroy all enchantments, then return all enchantment cards put
        .               /// into graveyards this way to the battlefield" — Cleansing Meditation's
        .               /// Threshold half.
        .               DestroyedThisResolution { filter: SelectionRequirement },
        .           
        .               /// A single player, lifted to selector form.
      188 ( 0.00%)      Player(PlayerRef),
   12,126 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::PlayerRef as core::clone::Clone>::clone (94x)
        .           
        .               /// Narrow another selector's result set to the entities matching `filter`
        .               /// ("the *nonblack* creature blocking or blocked by this" — Deathgazer).
        .               /// Player entities never match a permanent filter and are dropped.
        .               MatchingAmong { inner: Box<Selector>, filter: SelectionRequirement },
        .               /// Take at most `count` entities from `inner` (in resolution order).
        .               /// Wraps another selector to clamp how many entities flow through —
        .               /// used by SOS Heated Argument's "you may exile *a card* from your
-- line 457 ----------------------------------------
-- line 540 ----------------------------------------
        .           pub enum DraftNoteAgg {
        .               /// Highest note (Lurking Automaton).
        .               Max,
        .               /// Sum of every note (Cogwork Grinder).
        .               Sum,
        .           }
        .           
        .           /// A numeric expression evaluated at effect-time.
   93,086 ( 0.00%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum Value {
        .               /// Half of a player's library size, rounded up (Ulamog, the Defiler's
        .               /// cast trigger).
        .               HalfLibrarySizeRoundedUp(PlayerRef),
        .               /// Half the player's life total, rounded up (Garza's Assassin's
        .               /// "Recover—Pay half your life, rounded up").
        .               HalfLifeRoundedUp(PlayerRef),
        .               /// The greatest mana value among cards in exile (Ulamog, the Defiler's
-- line 556 ----------------------------------------
-- line 575 ----------------------------------------
        .               /// Lets a count read a filtered slice of a non-zone selector (e.g. count
        .               /// the land/blue/red cards among `Selector::LastMoved`). Culmination of
        .               /// Studies.
        .               CountMatching { sel: Box<Selector>, filter: SelectionRequirement },
        .               /// Sum of the mana values of the entities the selector resolves to
        .               /// (Summon: Bahamut's IV — "total mana value of other permanents you
        .               /// control"). Off-battlefield objects contribute their printed CMC.
        .               TotalManaValueOf(Box<Selector>),
      282 ( 0.00%)      PowerOf(Box<Selector>),
   12,690 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone (94x)
        .               /// CR 700.18 — the size of the controller's party: the number of distinct
        .               /// roles (Cleric, Rogue, Warrior, Wizard) among creatures they control,
        .               /// capped at 4. A single creature counts for at most one role. Powers
        .               /// Zendikar Rising party payoffs (Squad Commander, Tajuru Paragon).
        .               PartyCount,
        .               /// Number of creatures currently blocking the resolved permanent
        .               /// (Spined Sliver's "+1/+1 for each creature blocking it").
        .               BlockersOf(Box<Selector>),
-- line 591 ----------------------------------------
-- line 1234 ----------------------------------------
        .               pub const ZERO: Value = Value::Const(0);
        .               pub const ONE: Value = Value::Const(1);
        .               pub fn count(sel: Selector) -> Self { Value::CountOf(Box::new(sel)) }
        .           }
        .           
        .           // ── Predicate ────────────────────────────────────────────────────────────────
        .           
        .           /// A boolean game-state condition (for `Effect::If` / cast-time checks).
    7,030 ( 0.00%)  #[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum Predicate {
        .               #[default]
        .               True,
        .               False,
        .               Not(Box<Predicate>),
        .               All(Vec<Predicate>),
        .               Any(Vec<Predicate>),
        .               /// At least one entity matches the selector.
-- line 1250 ----------------------------------------
-- line 1329 ----------------------------------------
        .               /// Wanderlust): pair with an `AnyPlayer` upkeep trigger so it fires only on
        .               /// the enchanted permanent's controller's upkeep.
        .               ActivePlayerControls(Box<Selector>),
        .               /// The game is currently in the given turn step (CR 500). Gates
        .               /// "activate only during [your] upkeep / end step" abilities; pair with
        .               /// `IsTurnOf(You)` for the "your" qualifier.
        .               CurrentStepIs(crate::turn_step::TurnStep),
        .               /// The given entity's properties match the filter.
    1,140 ( 0.00%)      EntityMatches { what: Selector, filter: SelectionRequirement },
   23,180 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone (190x)
    3,990 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (190x)
        .               /// At least one entity the selector resolves to matches the filter
        .               /// (false when the selector is empty — unlike `EntityMatches`, which is
        .               /// vacuously true). Gates "if a [creature] card was exiled this way, …"
        .               /// over an optional ("up to one") target — Diregraf Scavenger.
        .               EntityMatchesAny { what: Selector, filter: SelectionRequirement },
        .               /// `who` has gained at least `at_least` total life this turn.
        .               /// Backed by `Player.life_gained_this_turn`. Used by Strixhaven's
        .               /// **Infusion** rider — "If you gained life this turn, …".
-- line 1345 ----------------------------------------
-- line 2234 ----------------------------------------
        .               /// cards revealed this way" (Sacred Guide).
        .               Exile,
        .               /// Misses join the found card in its destination — "reveal cards from the
        .               /// top of your library until you reveal a nonland card, then put all cards
        .               /// revealed this way into your hand" (Treasure Hunt).
        .               WithFind,
        .           }
        .           
    5,294 ( 0.00%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum ZoneDest {
      816 ( 0.00%)      Hand(PlayerRef),
   32,096 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::PlayerRef as core::clone::Clone>::clone (272x)
        .               Library { who: PlayerRef, pos: LibraryPosition },
        .               Graveyard,
        .               Exile,
        .               /// CR 702.170 — exile face up and mark **plotted**, so the card's owner
        .               /// may cast it for free as a sorcery on a later turn. The effect-granted
        .               /// half of plot: no plot cost is paid (Kellan Joins Up, Jace Reawakened,
        .               /// Make Your Own Luck, Aven Interrupter).
        .               ExilePlotted,
-- line 2252 ----------------------------------------
-- line 2281 ----------------------------------------
        .               /// name what it took later (Shell of the Last Kappa).
        .               ExileWithSource,
        .               /// "…put that card onto the battlefield under your control instead"
        .               /// (Desertion). Cards that fail the filter fall back to the graveyard.
        .               CountererBattlefieldIfMatching(Box<SelectionRequirement>),
        .           }
        .           
        .           /// What mana to add to a pool.
1,667,136 ( 0.05%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum ManaPayload {
        .               /// Add one mana of any of the colors of the card imprinted on the source
        .               /// (a card in exile with `exiled_with == ctx.source`). The player chooses
        .               /// among those colors; produces nothing if there is no imprinted card or it
        .               /// is colorless. Chrome Mox.
        .               ImprintedCardColor,
        .               /// Add each listed color once.
        .               Colors(Vec<Color>),
-- line 2297 ----------------------------------------
-- line 2369 ----------------------------------------
        .               /// colorless outside a drafted game.
        .               DraftNotedColorOfSource,
        .           }
        .           
        .           // ── Event specification (triggers) ───────────────────────────────────────────
        .           
        .           /// Kinds of game events a trigger can watch for. Mirrors the `GameEvent`
        .           /// stream in [`GameEvent`].
  134,592 ( 0.00%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum EventKind {
        .               /// A permanent entered the battlefield.
        .               EntersBattlefield,
        .               /// A creature died (hit a graveyard from the battlefield).
        .               CreatureDied,
        .               /// CR 700.4 — a creature *or* artifact hit a graveyard from the
        .               /// battlefield. Matches `GameEvent::PermanentDied` whose `is_creature`
        .               /// or `is_artifact` flag is set (an artifact creature counts once).
-- line 2385 ----------------------------------------
-- line 2877 ----------------------------------------
        .               SpellCountered,
        .               /// CR 725.3 — a player became the monarch. The event's player is the
        .               /// new monarch, so `EventScope::OpponentControl` reads "an opponent
        .               /// becomes the monarch" (Knights of the Black Rose).
        .               BecameMonarch,
        .           }
        .           
        .           /// Whose events does this trigger listen for?
      602 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum EventScope {
        .               /// Only events whose subject is the source permanent itself
        .               /// ("When this ... enters", "When this attacks").
        .               SelfSource,
        .               /// Events caused/controlled by the source's controller ("whenever you
        .               /// cast a spell", "whenever you gain life").
        .               YourControl,
        .               /// Events caused/controlled by an opponent.
-- line 2893 ----------------------------------------
-- line 3007 ----------------------------------------
        .               /// events, so the targeting spell/ability source can't be reached through
        .               /// `filter`. Only meaningful for `EventKind::BecameTarget`.
        .               #[serde(default)]
        .               pub causer_filter: Option<crate::card::SelectionRequirement>,
        .           }
        .           
        .           impl EventSpec {
        .               pub fn new(kind: EventKind, scope: EventScope) -> Self {
      120 ( 0.00%)          Self {
        .                       kind,
        .                       scope,
        .                       filter: None,
        .                       once_per_turn: false,
        .                       per_subject_cap: None,
        .                       actor_is_opponent: false,
        .                       exclude_attacker_taps: false,
        .                       exclude_tap_cost_abilities: false,
-- line 3023 ----------------------------------------
-- line 3031 ----------------------------------------
        .                   self
        .               }
        .               /// "…activates an ability without {T} in its activation cost" (Haunting Wind).
        .               pub fn without_tap_cost(mut self) -> Self {
        .                   self.exclude_tap_cost_abilities = true;
        .                   self
        .               }
        .               pub fn with_filter(mut self, p: Predicate) -> Self {
       48 ( 0.00%)          self.filter = Some(p);
       24 ( 0.00%)          self
      636 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (12x)
        .               }
        .               /// "Whenever a [filter] deals damage …" — gate on the damage's dealer.
        .               pub fn dealt_by(mut self, f: crate::card::SelectionRequirement) -> Self {
        .                   self.dealer_filter = Some(f);
        .                   self
        .               }
        .               /// "Whenever [this] becomes the target of a [filter] spell or ability …"
        .               /// — gate on the object that declared the target.
-- line 3048 ----------------------------------------
-- line 3130 ----------------------------------------
        .               /// A colour, counting nonland cards only (Sphinx's Tutelage).
        .               NonlandColor,
        .               /// A colour, counting every milled card (Grindstone).
        .               AnyColor,
        .               /// A card type (The Tale of Tamiyo).
        .               CardType,
        .           }
        .           
2,289,118 ( 0.07%)  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
        .           pub enum Effect {
        .               // ── Combinators ──────────────────────────────────────────────────────────
        .               /// Do nothing.
        .               Noop,
        .               /// Execute each inner effect in order.
      282 ( 0.00%)      Seq(Vec<Effect>),
   68,942 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (94x)
        .               /// If `cond` holds, execute `then`, else `else_`.
        .               If { cond: Predicate, then: Box<Effect>, else_: Box<Effect> },
        .               /// Execute `body` once per entity the `selector` resolves to.
        .               /// Inside `body`, `Selector::TriggerSource` refers to the current entity.
        .               ForEach { selector: Selector, body: Box<Effect> },
        .               /// Execute `body` `count` times.
        .               Repeat { count: Value, body: Box<Effect> },
        .               /// Run `body` once per player `who` resolves to, each time with that player
-- line 3152 ----------------------------------------
-- line 3561 ----------------------------------------
        .               /// where you can't).
        .               IfRevealFromHand {
        .                   filter: SelectionRequirement,
        .                   then: Box<Effect>,
        .                   else_: Box<Effect>,
        .               },
        .           
        .               // ── Damage / life ────────────────────────────────────────────────────────
    6,270 ( 0.00%)      DealDamage { to: Selector, amount: Value },
   40,128 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (1,254x)
   38,874 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Value as core::clone::Clone>::clone (1,254x)
        .               /// CR 702 Radiance (Ravnica) — deal `amount` damage to the creature `subject`
        .               /// resolves to *and* each other creature that shares a color with it
        .               /// (Cleansing Beam, Wojek Embermage). `subject` carries the target slot
        .               /// (`TargetFiltered { slot: 0, filter: Creature }`), so cast/auto-target
        .               /// legality flows through the normal slot-0 path; the fan-out then reads the
        .               /// chosen creature's computed colors. A colorless subject hits only itself.
        .               RadianceDamage { subject: Selector, amount: Value },
        .               /// Deal `amount` damage to the creature `subject` resolves to *and* each
-- line 3577 ----------------------------------------
-- line 3703 ----------------------------------------
        .                   chosen: Box<Effect>,
        .                   other: Box<Effect>,
        .               },
        .               /// CR 701.12 — exchange control of a permanent you control matching
        .               /// `filter` (chosen at resolution: `Decision::ChooseCards` for a
        .               /// `wants_ui` controller, lowest CardId otherwise) and the permanent
        .               /// `with` resolves to. Vedalken Plotter's ETB land swap.
        .               ExchangeControlChoosing { filter: SelectionRequirement, with: Selector },
      470 ( 0.00%)      GainLife  { who: Selector, amount: Value },
   14,570 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (94x)
   15,228 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Value as core::clone::Clone>::clone (94x)
        .               LoseLife  { who: Selector, amount: Value },
        .               /// CR 701.10d — "double a player's life total." Each player the selector
        .               /// resolves to gains life equal to their current total (so 20 → 40). A
        .               /// player at 0 or negative is unaffected (no negative-doubling). Beacon
        .               /// of Immortality.
        .               DoubleLife { who: Selector },
        .               /// Each player the selector resolves to loses life equal to half their
        .               /// *own* current life total (rounded up when `rounded_up`, else down).
-- line 3719 ----------------------------------------
-- line 3908 ----------------------------------------
        .               GenesisWave,
        .               /// CR 107.16 — exile the top card of the controller's library, then they
        .               /// may pay `energy` {E}; if they do, they may cast that card without
        .               /// paying its mana cost (the spell stays exiled if not cast). Amped
        .               /// Raptor's "exile the top card, you may pay {E}{E}{E}{E} to cast it".
        .               ExileTopMayPayEnergyToCast { energy: u32 },
        .           
        .               // ── Cards / draw / discard / mill ────────────────────────────────────────
    2,480 ( 0.00%)      Draw    { who: Selector, amount: Value },
   10,416 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (496x)
   15,376 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Value as core::clone::Clone>::clone (496x)
        .               /// CR 701.45 — Learn. `who` may reveal a Lesson card they own from their
        .               /// sideboard ("outside the game") and put it into their hand, or discard
        .               /// a card to draw a card. Resolved via `Decision::Learn`. When `who`'s
        .               /// sideboard holds no Lesson, falls back to the legacy `Draw 1`
        .               /// approximation so no-sideboard games behave as before.
        .               Learn   { who: PlayerRef },
        .               /// Discard `amount` cards. If `random`, chosen randomly; else by `who`.
        .               Discard { who: Selector, amount: Value, random: bool },
-- line 3924 ----------------------------------------
-- line 4823 ----------------------------------------
        .               /// Each player in `who` exiles all but the bottom `keep` cards of their
        .               /// library (face down — face-down exile isn't modeled, so the cards are
        .               /// exiled plainly). Doomsday Excruciator's "each player exiles all but the
        .               /// bottom six cards of their library."
        .               ExileLibraryExceptBottom { who: PlayerRef, keep: Value },
        .           
        .               // ── Zone moves ───────────────────────────────────────────────────────────
        .               /// Move every entity the selector resolves to into `to`.
    1,360 ( 0.00%)      Move { what: Selector, to: ZoneDest },
    5,712 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (272x)
   38,080 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::ZoneDest as core::clone::Clone>::clone (272x)
        .               /// "[Return/put/exile] up to `count` cards of your choice from the set
        .               /// `from` resolves to" — the player-chooses sibling of
        .               /// `Move { what: Take {..} }` (which auto-takes in iteration order).
        .               /// Resolution-time `Decision::ChooseCards`; the auto decider takes the
        .               /// first `count` so bot play keeps maximizing. `filter` narrows the
        .               /// resolved set (Bind to Life's "a creature card from AMONG the milled
        .               /// seven" — `from: LastMoved`, filter Creature). Divergent Equation,
        .               /// Pull from the Grave, Return to the Ranks, Emeritus of Ideation.
-- line 4839 ----------------------------------------
-- line 5095 ----------------------------------------
        .               /// way." Nebuchadnezzar.
        .               NameCardRevealRandomDiscardNamed { who: PlayerRef, count: Value },
        .               SilencePlayersThisTurn { who: PlayerRef },
        .               /// "Exile [this spell]" as part of its own resolution (Revel in
        .               /// Silence). Same flag pattern as `ShuffleSelfIntoLibrary`.
        .               ExileResolvingSpell,
        .           
        .               // ── Mana ─────────────────────────────────────────────────────────────────
  277,670 ( 0.01%)      AddMana { who: PlayerRef, pool: ManaPayload },
7,420,706 ( 0.24%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::ManaPayload as core::clone::Clone>::clone (55,534x)
  888,544 ( 0.03%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::PlayerRef as core::clone::Clone>::clone (55,534x)
        .               /// "Add mana equal to [permanent]'s mana cost" (Elemental Resonance).
        .               /// Reads the resolved permanent's printed cost pip-by-pip: colored →
        .               /// that color, generic/{C} → colorless, hybrid → its first color, X →
        .               /// skipped. Mana goes to the resolving controller's pool.
        .               AddManaEqualToPermanentCost { permanent: Selector },
        .               /// CR 500.4 exception — add these fixed color pips to the resolving
        .               /// player's pool and mark them "you don't lose this mana as steps and
        .               /// phases end" (Savage Ventmaw's attack trigger). The mana survives every
-- line 5111 ----------------------------------------
-- line 5112 ----------------------------------------
        .               /// step/phase empty this turn and clears at cleanup.
        .               AddManaKeptThisTurn { who: PlayerRef, colors: Vec<Color> },
        .               /// Like `AddManaKeptThisTurn` but adds `amount` mana of a single `color`
        .               /// (Neheb, Dreadhorde Champion — "add that much {R}", where the amount is
        .               /// the number of cards discarded this resolution).
        .               AddManaKeptThisTurnCount { who: PlayerRef, color: Color, amount: Value },
        .           
        .               // ── Permanent mutations ──────────────────────────────────────────────────
    1,038 ( 0.00%)      Destroy { what: Selector },
  124,539 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (346x)
        .               /// CR 701.15g — "Destroy ... It can't be regenerated." Behaves like
        .               /// `Destroy` but bypasses regeneration shields (Terminate, Putrefy,
        .               /// Day of Judgment, Vindicate, ...). Indestructible and Shield-counter
        .               /// replacements still apply — only regeneration is denied.
        .               DestroyNoRegen { what: Selector },
        .               /// "Destroy all [filter]. They can't be regenerated. The controller of
        .               /// each of those permanents gains life equal to its mana value" (Seeds of
        .               /// Innocence). Life is paid out per destroyed permanent, to its own
-- line 5128 ----------------------------------------
-- line 5194 ----------------------------------------
        .               GrantHarmonizeThisTurn { what: Selector },
        .               /// "[Each resolved card] gains miracle `cost` until end of turn." Stamps
        .               /// an until-end-of-turn `may_play_until` permission **plus** a
        .               /// `granted_alt_cast_cost_eot` of `cost`, so the controller may cast the
        .               /// card this turn by paying `cost` (rather than its full mana cost or
        .               /// for free). Used by Lorehold, the Historian's "instant and sorcery
        .               /// cards in your hand have miracle {2}" grant.
        .               GrantMiracle { what: Selector, cost: crate::mana::ManaCost },
      282 ( 0.00%)      Exile   { what: Selector },
    3,008 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (94x)
        .               /// The "Enduring" cycle (Bloomburrow): "When this dies, if it was a
        .               /// creature, return it to the battlefield. It's an enchantment." Returns
        .               /// the source from its owner's graveyard to the battlefield under their
        .               /// control, then strips the Creature card type from the returned object
        .               /// so it comes back as a noncreature enchantment (and the gate self-
        .               /// limits — a noncreature can't satisfy "if it was a creature", so it
        .               /// won't loop). No-op if the source isn't a creature card in a graveyard.
        .               ReturnSelfAsEnchantment,
-- line 5210 ----------------------------------------
-- line 5757 ----------------------------------------
        .               /// in resolution order; auto-resolution favors highest-CMC lands
        .               /// for max mana refund.
        .               Untap   { what: Selector, #[serde(default)] up_to: Option<Value> },
        .               /// "Simultaneously untap all tapped [what] and tap all untapped [what]"
        .               /// (Breaking Wave). A sequenced Untap-then-Tap would re-tap everything it
        .               /// just untapped, so the swap is read once and applied in one pass.
        .               SwapTappedState { what: Selector },
        .               /// Give a temporary +P/+T bonus.
    4,720 ( 0.00%)      PumpPT  { what: Selector, power: Value, toughness: Value, duration: Duration },
   15,536 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (590x)
   36,580 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Value as core::clone::Clone>::clone (1,180x)
        .               /// Double the resolved creature's power `times` times for `duration`
        .               /// (CR — Exponential Growth: "double target creature's power X times").
        .               /// Computes the creature's current power and adds `power * (2^times - 1)`
        .               /// as a pump bonus, so power ends at `power * 2^times`. `times` ≤ 0 is a
        .               /// no-op. Reusable for any "double power N times" card.
        .               DoublePower { what: Selector, times: Value, duration: Duration },
        .               /// CR 701.10 — "double the number of `kind` counters on each [selector]."
        .               /// For every resolved permanent, add counters of `kind` equal to the number
-- line 5773 ----------------------------------------
-- line 6825 ----------------------------------------
        .               /// riders ride the printed `equipped_bonus`, inert until attached.
        .               LicidAttach { host: Selector, end_cost: crate::mana::ManaCost },
        .               /// The `end_cost` half of [`Effect::LicidAttach`] — the source unattaches
        .               /// and goes back to being a creature.
        .               LicidDetach,
        .           
        .               // ── Stack interaction ────────────────────────────────────────────────────
        .               /// Counter target spell (removes from stack; sends to owner's graveyard).
      660 ( 0.00%)      CounterSpell { what: Selector },
   10,560 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::Selector as core::clone::Clone>::clone (220x)
        .               /// CR 115.7 — "Change the target of target spell with a single target."
        .               /// Re-enumerates the spell's legal targets and lets this effect's
        .               /// controller repoint it (the current target is offered first, so an
        .               /// auto-decider is a no-op). A spell with zero or several targets is
        .               /// left alone. Shunt, Deflection.
        .               ChangeSpellTarget { what: Selector },
        .               /// Test of Talents — "Counter target instant or sorcery spell. Search
        .               /// its controller's graveyard, hand, and library for any number of
-- line 6841 ----------------------------------------
-- line 7494 ----------------------------------------
        .               // ── Counters on players ──────────────────────────────────────────────────
        .               AddPoison { who: Selector, amount: Value },
        .               /// CR 122.1i / 728 — give each resolved player `amount` rad counters.
        .               AddRadCounters { who: Selector, amount: Value },
        .           
        .               // ── Misc atomic operations needed by existing cards ──────────────────────
        .               /// Reveal the top card of `who`'s library; if `reveal_filter` matches, draw it.
        .               RevealTopAndDrawIf {
    1,400 ( 0.00%)          who: PlayerRef,
   11,200 ( 0.00%)  => crabomination_base/src/effect.rs:<crabomination_base::effect::PlayerRef as core::clone::Clone>::clone (700x)
    2,100 ( 0.00%)          reveal_filter: SelectionRequirement,
   11,200 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone (700x)
        .                   /// On a miss, the player may put the revealed card into their
        .                   /// graveyard instead of leaving it on top (Nylea, Keen-Eyed).
        .                   #[serde(default)]
      700 ( 0.00%)          may_graveyard_miss: bool,
        .               },
        .           
        .               /// Reveal the top card of `who`'s library (fires `TopCardRevealed` event for
        .               /// the animation) without moving it. Used by Chaos Warp's "reveal top card"
        .               /// step where the put-onto-battlefield clause is handled separately.
        .               RevealTopCard { who: PlayerRef },
        .           
        .               /// Reveal the top card of `who`'s library; if it matches `filter`, run
-- line 7515 ----------------------------------------

  130,386 ( 0.00%)  <counts for unidentified lines in crabomination_base/src/effect.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/layers.rs
--------------------------------------------------------------------------------
Ir                 

-- line 57 ----------------------------------------
        .               /// (Assimilation Aegis). Swept alongside `WhileSourceTapped`.
        .               WhileSourceAttached,
        .               /// Indefinite (e.g. counters, "for as long as" effects).
        .               Indefinite,
        .           }
        .           
        .           // ── Layer classification ───────────────────────────────────────────────────────
        .           
   55,528 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
        .           #[derive(Serialize, Deserialize)]
        .           pub enum Layer {
        .               L1Copy        = 1,
        .               L2Control     = 2,
        .               L3Text        = 3,
        .               L4Type        = 4,
        .               L5Color       = 5,
        .               L6Ability     = 6,
-- line 73 ----------------------------------------
-- line 316 ----------------------------------------
        .           }
        .           
        .           impl<T: Clone> Printed<T> {
        .               #[inline]
        .               pub(crate) fn new(
        .                   src: std::sync::Arc<crate::card::CardDefinition>,
        .                   proj: fn(&crate::card::CardDefinition) -> &T,
        .               ) -> Self {
3,802,432 ( 0.12%)          Self { src, proj, over: None }
        .               }
        .           }
        .           
        .           impl<T: Clone> std::ops::Deref for Printed<T> {
        .               type Target = T;
        .               #[inline]
        .               fn deref(&self) -> &T {
2,230,242 ( 0.07%)          match &self.over {
    1,620 ( 0.00%)              Some(v) => v,
  968,054 ( 0.03%)              None => (self.proj)(&self.src),
  394,056 ( 0.01%)  => crabomination/src/game/layers.rs:core::ops::function::FnOnce::call_once (197,028x)
        .                   }
        .               }
        .           }
        .           
        .           /// The clone point: the first layer write materializes the printed value.
        .           impl<T: Clone> std::ops::DerefMut for Printed<T> {
        .               #[inline]
        .               fn deref_mut(&mut self) -> &mut T {
        .                   let proj = self.proj;
        .                   let src = &self.src;
   51,720 ( 0.00%)          self.over.get_or_insert_with(|| Box::new(proj(src).clone()))
2,263,295 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (17,240x)
   34,480 ( 0.00%)  => crabomination/src/game/layers.rs:core::ops::function::FnOnce::call_once (17,240x)
        .               }
        .           }
        .           
        .           impl<T: Clone> Clone for Printed<T> {
        .               fn clone(&self) -> Self {
        .                   Self { src: self.src.clone(), proj: self.proj, over: self.over.clone() }
        .               }
        .           }
-- line 353 ----------------------------------------
-- line 402 ----------------------------------------
        .           ///
        .           /// `effects` must already be filtered to only active (non-expired) effects.
        .           pub fn apply_layers(
        .               battlefield: &[crate::card::CardInstance],
        .               effects: &[ContinuousEffect],
        .           ) -> Vec<ComputedPermanent> {
        .               battlefield
        .                   .iter()
   42,010 ( 0.00%)          .map(|card| compute_permanent(card, effects))
4,024,042 ( 0.13%)  => crabomination/src/game/layers.rs:crabomination::game::layers::compute_permanent (8,402x)
        .                   .collect()
        .           }
        .           
        .           /// Apply all active continuous effects to a single permanent, returning
        .           /// its `ComputedPermanent`. Same per-card work as one iteration of
        .           /// [`apply_layers`] — for callers that only need one card's computed
        .           /// state and shouldn't pay to build (and discard) every other
        .           /// permanent's view.
        .           pub fn apply_layers_one(
        .               card: &crate::card::CardInstance,
        .               effects: &[ContinuousEffect],
        .           ) -> ComputedPermanent {
  690,640 ( 0.02%)      compute_permanent(card, effects)
39,219,937 ( 1.26%)  => crabomination/src/game/layers.rs:crabomination::game::layers::compute_permanent (60,798x)
        .           }
        .           
2,138,868 ( 0.07%)  fn compute_permanent(
        .               card: &crate::card::CardInstance,
        .               effects: &[ContinuousEffect],
        .           ) -> ComputedPermanent {
        .               // Power-gated effects (CR 613.8) need the card's gate-free power first:
        .               // pass 1 excludes them, pass 2 re-runs the full walk with each gate
        .               // resolved against that power. Keyword grants don't feed back into
        .               // layer 7, so the result is a fixpoint after one re-run.
        .               let has_power_gated = effects
        .                   .iter()
  208,516 ( 0.01%)          .any(|e| matches!(e.affected, AffectedPermanents::CardMatchPowerGated { .. }));
        .               // CR 613.8 — a creature-type lord (AllWithCreatureType) must see the
        .               // *computed* types, so a creature animated/retyped into the lord's tribe
        .               // (Turn to Frog into a Frog lord, Arcane Adaptation, etc.) gets the buff.
        .               // Only re-run when both a type-changer and a type lord are present.
        .               let has_type_changer = effects.iter().any(|e| {
  417,032 ( 0.01%)          matches!(
        .                       e.modification,
        .                       Modification::SetCreatureTypes(_) | Modification::AddCreatureType(_)
        .                   )
        .               });
        .               let has_type_lord = effects
        .                   .iter()
  208,516 ( 0.01%)          .any(|e| matches!(e.affected, AffectedPermanents::AllWithCreatureType { .. }));
  237,652 ( 0.01%)      let has_type_gated = has_type_changer && has_type_lord;
  712,956 ( 0.02%)      if !has_power_gated && !has_type_gated {
  950,608 ( 0.03%)          return compute_permanent_pass(card, effects, None, None);
134,328,545 ( 4.31%)  => crabomination/src/game/layers.rs:crabomination::game::layers::compute_permanent_pass (237,652x)
        .               }
        .               let pass1 = compute_permanent_pass(card, effects, None, None);
        .               let gate_types = has_type_gated.then(|| pass1.subtypes.creature_types.clone());
        .               compute_permanent_pass(card, effects, Some(pass1.power), gate_types.as_deref())
2,376,520 ( 0.08%)  }
        .           
3,089,476 ( 0.10%)  fn compute_permanent_pass(
        .               card: &crate::card::CardInstance,
        .               effects: &[ContinuousEffect],
        .               gate_power: Option<i32>,
        .               gate_types: Option<&[CreatureType]>,
        .           ) -> ComputedPermanent {
        .               // Start from the base card definition — borrowed, not cloned: each of
        .               // these materializes only if a layer below actually writes to it.
  237,652 ( 0.01%)      let mut controller = card.controller;
        .               let def = &card.definition;
  143,320 ( 0.00%)      let mut card_types = Printed::new(def.clone(), |d| &d.card_types);
   22,038 ( 0.00%)      let mut supertypes = Printed::new(def.clone(), |d| &d.supertypes);
   26,380 ( 0.00%)      let mut subtypes = Printed::new(def.clone(), |d| &d.subtypes);
        .               // CR 702.103d — while bestowed, the permanent is an Aura enchantment,
        .               // not a creature. Strip the Creature type and add the Aura subtype so
        .               // it isn't a valid blocker/attacker/removal target and the orphan-Aura
        .               // checks treat it correctly.
  712,956 ( 0.02%)      if card.bestowed {
        .                   card_types.retain(|t| !matches!(t, CardType::Creature));
        .                   if !card_types.contains(&CardType::Enchantment) {
        .                       card_types.push(CardType::Enchantment);
        .                   }
        .                   if !subtypes
        .                       .enchantment_subtypes
        .                       .contains(&crate::card::EnchantmentSubtype::Aura)
        .                   {
        .                       subtypes
        .                           .enchantment_subtypes
        .                           .push(crate::card::EnchantmentSubtype::Aura);
        .                   }
        .               }
        .               let mut colors = colors_from_card(card);
  614,144 ( 0.02%)      let mut keywords = Printed::new(def.clone(), |d| &d.keywords);
        .               // EOT-granted keywords join the layer walk as synthetic L6 effects at
        .               // their grant timestamps (CR 613.7), so a grant resolved *after* a
        .               // RemoveAllAbilities / RemoveKeyword effect survives it while an
        .               // earlier one is stripped. Untracked grants (tests, legacy snapshots)
        .               // default to timestamp 0 — ordered first, like the old pre-merge.
        .               // Cleared at Cleanup via `clear_end_of_turn_effects`.
        .               let eot_grants: Vec<ContinuousEffect> = card
        .                   .granted_keywords_eot
-- line 499 ----------------------------------------
-- line 516 ----------------------------------------
        .                   if *count > 0 && !keywords.contains(kw) {
        .                       keywords.push(kw.clone());
        .                   }
        .               }
        .           
        .               // Base P/T from the definition. Direct pump bonuses (`power_bonus`,
        .               // from `Effect::PumpPT`) are layer-7c modifications and so apply
        .               // *after* a 7b set (CR 613.7c — an animated manland keeps its pumps).
  237,652 ( 0.01%)      let base_power = card.definition.base_power();
3,341,698 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::base_power (237,652x)
  237,652 ( 0.01%)      let base_toughness = card.definition.base_toughness();
3,341,698 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::base_toughness (237,652x)
        .               // CR 613.7f — +1/+1, -1/-1, and the rarer one-sided counters
        .               // (-0/-1, -0/-2, -1/-0, +1/+0, +0/+1, +2/+0, +0/+2), which affect only one
        .               // of power/toughness, so track per-stat deltas.
        .               // One pass over the permanent's counters rather than ten keyed lookups:
        .               // this runs per card per layer pass (12.0 M per-card computations for six
        .               // bot games), and a permanent carries one or two counter kinds when it
        .               // carries any at all. Summing over the map is order-independent, so the
        .               // `HashMap` iteration order cannot leak into game state.
-- line 533 ----------------------------------------
-- line 561 ----------------------------------------
        .               let mut mod_toughness: i32 = 0;
        .               let mut switched = false;
        .               let mut lost_all_abilities = false;
        .               let mut cant_have_keywords: Vec<Keyword> = Vec::new();
        .           
        .               // Sort effects by layer, then sublayer, then timestamp.
        .               let mut sorted: Vec<&ContinuousEffect> = effects
        .                   .iter()
  834,064 ( 0.03%)          .filter(|e| affects(e, card, gate_power, gate_types))
        .                   .chain(eot_grants.iter())
        .                   .collect();
        .               sorted.sort_by(|a, b| {
        .                   a.layer.cmp(&b.layer)
        .                       .then(a.sublayer.cmp(&b.sublayer))
        .                       .then(a.timestamp.cmp(&b.timestamp))
        .               });
        .           
  237,652 ( 0.01%)      for effect in sorted {
  298,200 ( 0.01%)          match &effect.modification {
        .                       // Layer 2
        .                       Modification::ChangeController(c) => controller = *c,
        .           
        .                       // Layer 3 — text-changing (CR 612). Runs before type/color/
        .                       // ability layers, so the swapped words feed everything below.
        .                       Modification::ReplaceColorWord(from, to) => {
        .                           for kw in keywords.iter_mut() {
        .                               if matches!(kw, crate::card::Keyword::Protection(c) if c == from) {
-- line 587 ----------------------------------------
-- line 634 ----------------------------------------
        .                       Modification::AddColor(c) => colors.insert(*c),
        .                       Modification::SetColors(cs) => colors = cs.iter().collect(),
        .                       Modification::LoseAllColors => colors.clear(),
        .           
        .                       // Layer 6. Duplicate grants of a binary keyword collapse to one,
        .                       // but Toxic/Poisonous instances are cumulative (CR 702.180b /
        .                       // 702.70b — "total toxic value" sums every instance; Plague
        .                       // Nurse's granted toxic 1 stacks with a printed toxic 1).
   32,516 ( 0.00%)              Modification::AddKeyword(kw) => {
  130,064 ( 0.00%)                  let cumulative = matches!(kw, Keyword::Toxic(_) | Keyword::Poisonous(_));
        .                           if cumulative || !keywords.contains(kw) {
   51,096 ( 0.00%)                      keywords.push(kw.clone());
  332,124 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:<crabomination_base::card::Keyword as core::clone::Clone>::clone (25,548x)
        .                           }
        .                       }
        .                       Modification::RemoveKeyword(kw) => keywords.retain(|k| k != kw),
        .                       Modification::CantHaveKeyword(kw) => {
        .                           keywords.retain(|k| k != kw);
        .                           cant_have_keywords.push(kw.clone());
        .                       }
        .                       Modification::RemoveAllAbilities => {
-- line 653 ----------------------------------------
-- line 688 ----------------------------------------
        .               // Until-EOT keyword removals (Shadowspear) apply last — the removal's
        .               // timestamp is by construction later than any static grant this turn.
        .               // CR 702.22 — "loses all 'bands with other' abilities" (Shelkin Brownie,
        .               // Tolaria) can't name a payload, so a removal of `BandsWithOther` strips
        .               // every instance whatever its quality.
        .               // Both retains are gated on their input being non-empty: `retain` takes
        .               // `&mut`, which materializes the printed keyword list even when it
        .               // removes nothing, and the empty case is the overwhelming majority.
  950,608 ( 0.03%)      if !card.removed_keywords_eot.is_empty() || !card.removed_keywords.is_empty() {
        .                   let removes_all_bands = card
        .                       .removed_keywords_eot
        .                       .iter()
        .                       .chain(&card.removed_keywords)
        .                       .any(|k| matches!(k, Keyword::BandsWithOther(_)));
        .                   keywords.retain(|k| {
        .                       !(card.removed_keywords_eot.contains(k)
        .                           || card.removed_keywords.contains(k)
        .                           || (removes_all_bands && matches!(k, Keyword::BandsWithOther(_))))
        .                   });
        .               }
        .               // CR 113.11 — "can't have or gain" beats any grant, whatever its
        .               // timestamp (Archetype of Courage vs. a later first-strike anthem).
  475,304 ( 0.02%)      if !cant_have_keywords.is_empty() {
        .                   keywords.retain(|k| !cant_have_keywords.contains(k));
        .               }
        .           
        .               // Compute final P/T.
  712,956 ( 0.02%)      let (mut power, mut toughness) = if let Some((p, t)) = set_pt {
        .                   (p, t)
        .               } else {
        .                   (base_power, base_toughness)
        .               };
        .               // Layer-7b "base power becomes N" (power only) overrides the power half.
  475,304 ( 0.02%)      if let Some(p) = set_power_only {
        .                   power = p;
        .               }
        .               // Layer-7b "base toughness becomes N" (toughness only) overrides the
        .               // toughness half (Maha's opponent debuff).
  475,304 ( 0.02%)      if let Some(t) = set_toughness_only {
        .                   toughness = t;
        .               }
  712,956 ( 0.02%)      power += mod_power + card.power_bonus + card.perm_power_bonus;
  950,608 ( 0.03%)      toughness += mod_toughness + card.toughness_bonus + card.perm_toughness_bonus;
        .               // Counters applied after 7c (CR 613.7f).
  237,652 ( 0.01%)      power += counter_power_delta;
  237,652 ( 0.01%)      toughness += counter_toughness_delta;
  950,608 ( 0.03%)      if switched {
        .                   std::mem::swap(&mut power, &mut toughness);
        .               }
        .           
1,901,216 ( 0.06%)      ComputedPermanent {
  237,652 ( 0.01%)          id: card.id,
        .                   controller,
1,188,260 ( 0.04%)          card_types,
  950,608 ( 0.03%)          supertypes,
  950,608 ( 0.03%)          subtypes,
        .                   colors,
  950,608 ( 0.03%)          keywords,
        .                   power,
        .                   toughness,
        .                   lost_all_abilities,
        .               }
1,901,216 ( 0.06%)  }
        .           
        .           /// Determine which colors a card has from its mana cost symbols
        .           /// (CR 105.2 + the Devoid CDA).
        .           fn colors_from_card(card: &crate::card::CardInstance) -> ColorSet {
  475,304 ( 0.02%)      card.definition.printed_color_set()
17,572,024 ( 0.56%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_color_set (237,652x)
        .           }
        .           
        .           /// Returns true if `effect` affects `card`. `gate_power` is the card's
        .           /// gate-free computed power for `CardMatchPowerGated` effects; `None` (pass 1)
        .           /// excludes them.
        .           fn affects(
        .               effect: &ContinuousEffect,
        .               card: &crate::card::CardInstance,
        .               gate_power: Option<i32>,
        .               gate_types: Option<&[CreatureType]>,
        .           ) -> bool {
1,042,580 ( 0.03%)      affected_includes_gated(&effect.affected, effect.source, card, gate_power, gate_types)
4,086,736 ( 0.13%)  => crabomination/src/game/layers.rs:crabomination::game::layers::affected_includes_gated (91,256x)
        .           }
        .           
        .           /// Whether `card` is one of the permanents described by `affected`, given the
        .           /// describing effect's `source` permanent. Split out of [`affects`] so the
        .           /// enters-tapped ETB replacement (CR 614.13) can reuse the same selector
        .           /// matching without constructing a throwaway `ContinuousEffect`.
        .           /// Power-gated matches resolve against the card's printed power + counters
        .           /// (callers with a computed view use [`affected_includes_gated`]).
-- line 775 ----------------------------------------
-- line 779 ----------------------------------------
        .               card: &crate::card::CardInstance,
        .           ) -> bool {
        .               let printed_power = card.definition.base_power()
        .                   + card.counter_count(CounterType::PlusOnePlusOne) as i32
        .                   - card.counter_count(CounterType::MinusOneMinusOne) as i32;
        .               affected_includes_gated(affected, source, card, Some(printed_power), None)
        .           }
        .           
1,876,644 ( 0.06%)  fn affected_includes_gated(
        .               affected: &AffectedPermanents,
        .               source: CardId,
        .               card: &crate::card::CardInstance,
        .               gate_power: Option<i32>,
        .               // CR 613.8 — the affected card's *computed* creature types (pass 2 of a
        .               // type lord recompute). `None` falls back to printed types.
        .               gate_types: Option<&[CreatureType]>,
        .           ) -> bool {
2,085,160 ( 0.07%)      match affected {
        .                   AffectedPermanents::Source => source == card.id,
        .                   AffectedPermanents::Specific(ids) => ids.contains(&card.id),
        .                   AffectedPermanents::All {
        .                       controller,
        .                       card_types,
        .                       exclude_source,
        .                       color,
        .                       token,
-- line 804 ----------------------------------------
-- line 894 ----------------------------------------
        .                           return false;
        .                       }
        .                       if requirement_mentions_other_than_source(requirement) && source == card.id {
        .                           return false;
        .                       }
        .                       requirement_matches_card(requirement, card, *source_controller)
        .                   }
        .               }
1,876,644 ( 0.06%)  }
        .           
        .           /// True when `req`'s every leaf is computable from a card's *printed*
        .           /// characteristics alone (type/supertype/subtype/color/token/controller and
        .           /// boolean combinations) — i.e. no power/toughness, combat, counter, or zone
        .           /// state. Such requirements can be matched by [`requirement_matches_card`]
        .           /// without a `GameState`, so `affected_from_requirement` may safely route them
        .           /// through [`AffectedPermanents::CardMatch`].
        .           pub(crate) fn requirement_is_card_only(req: &SelectionRequirement) -> bool {
-- line 910 ----------------------------------------

4,084,532 ( 0.13%)  <counts for unidentified lines in crabomination/src/game/layers.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/draft.rs
--------------------------------------------------------------------------------
Ir               

-- line 787 ----------------------------------------
      .           }
      .           
      .           // ── CR 905.2 draft-matters shell ─────────────────────────────────────────────
      .           
      .           /// CR 905.2b — the values a player noted as they drafted cards with a
      .           /// given name, keyed by that card's name. Carried out of the draft on
      .           /// `Player.draft_notes` so Aether Searcher, Lurking Automaton, Cogwork
      .           /// Grinder and Paliano can read them in the game that follows.
922,952 ( 0.03%)  #[derive(Debug, Clone, Default, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
      .           pub struct DraftNotes {
      .               pub numbers: HashMap<String, Vec<u32>>,
      .               pub names: HashMap<String, Vec<String>>,
      .               pub colors: HashMap<String, Vec<Color>>,
      .               #[serde(default)]
      .               pub keywords: HashMap<String, Vec<Keyword>>,
      .               #[serde(default)]
      .               pub creature_types: HashMap<String, Vec<CreatureType>>,
-- line 803 ----------------------------------------

     16 ( 0.00%)  <counts for unidentified lines in crabomination/src/draft.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/actions.rs
--------------------------------------------------------------------------------
Ir                 

-- line 7 ----------------------------------------
        .           /// `(id, power, is_creature, toughness, mana_value, is_artifact, is_vehicle,
        .           /// colors)`. Stamped onto the resolution scratch so the spell body can read
        .           /// `Value::Sacrificed*` and `Predicate::SacrificedWas*`.
        .           type SacrificeSnapshot = (CardId, u32, bool, i32, u32, bool, bool, Vec<ManaColor>);
        .           
        .           /// The grant sources live on the board right now, independent of which
        .           /// permanent is asking — see [`GameState::grant_scan`]. Borrows the state it
        .           /// was scanned from, so it can't outlive a board change.
  284,000 ( 0.01%)  #[derive(Default)]
        .           pub(crate) struct GrantScan<'a> {
        .               /// Live `GrantActivatedAbility` statics past their CR 611.2 wrapper and
        .               /// `condition` gate: `(applies_to, ability, source)`.
        .               statics: Vec<(
        .                   &'a crate::effect::Selector,
        .                   &'a crate::effect::ActivatedAbility,
        .                   &'a CardInstance,
        .               )>,
-- line 23 ----------------------------------------
-- line 155 ----------------------------------------
        .                   None
        .               }
        .           }
        .           
        .           /// The "remove N counters from among permanents you control" cost of an
        .           /// activated ability, normalized across the any-kind
        .           /// (`remove_counter_among_filter`) and kind-restricted
        .           /// (`remove_counter_among_kinds`) spellings. `None` kinds = any kind.
  329,814 ( 0.01%)  fn counter_drain_cost(
        .               ability: &crate::effect::ActivatedAbility,
        .           ) -> Option<(Option<Vec<crate::card::CounterType>>, u32, crate::card::SelectionRequirement)> {
        .               if let Some((kind, count, filter)) = ability.remove_counter_among_filter.as_ref() {
        .                   return Some((kind.map(|k| vec![k]), *count, filter.clone()));
        .               }
        .               let (kinds, count, filter) = ability.remove_counter_among_kinds.as_ref()?;
        .               Some((Some(kinds.clone()), *count, filter.clone()))
  329,814 ( 0.01%)  }
        .           
        .           /// Counters on `c` that a `counter_drain_cost` of the given kinds could take.
        .           fn drainable_counters(c: &CardInstance, kinds: Option<&[crate::card::CounterType]>) -> u32 {
        .               match kinds {
        .                   Some(ks) => ks.iter().map(|k| c.counter_count(*k)).sum(),
        .                   None => c.counters.values().sum(),
        .               }
        .           }
-- line 179 ----------------------------------------
-- line 184 ----------------------------------------
        .           /// (Spreading Seas, Blood Moon, Urborg, Magical Hack). With none of them in
        .           /// scope a permanent's computed land types are its printed ones, which is
        .           /// what lets `effective_mana_abilities_with` skip the per-card layer pass.
        .           /// Keep in step with `layers::compute_permanent_pass` — a fourth land-type
        .           /// writer added there must be added here or lands stop losing their mana
        .           /// abilities.
        .           fn rewrites_land_types(e: &crate::game::layers::ContinuousEffect) -> bool {
        .               use crate::game::layers::Modification as M;
   80,064 ( 0.00%)      matches!(
        .                   e.modification,
        .                   M::AddLandType(_) | M::SetLandTypes(_) | M::ReplaceBasicLandType(..)
        .               )
        .           }
        .           
        .           /// Public wrapper for `is_mana_ability` — read by `SelectionRequirement::
        .           /// HasNonManaActivatedAbility` (Tsabo's Web).
        .           pub fn is_mana_ability_public(effect: &Effect) -> bool {
        .               is_mana_ability(effect)
        .           }
        .           
        .           pub(crate) fn is_mana_ability(effect: &Effect) -> bool {
        .               // CR 605.1a — a mana ability could add mana, isn't a loyalty ability,
        .               // doesn't target, and doesn't have an illegal trigger. It may still carry
        .               // incidental non-stack riders (Altar of the Pantheon's "gain 1 life").
  996,512 ( 0.03%)      fn produces_mana(e: &Effect) -> bool {
1,496,584 ( 0.05%)          match e {
        .                       Effect::AddMana { .. } => true,
        .                       Effect::Seq(steps) => steps.iter().any(produces_mana),
        .                       Effect::If { then, else_, .. } => produces_mana(then) || produces_mana(else_),
        .                       _ => false,
        .                   }
1,245,640 ( 0.04%)      }
1,066,240 ( 0.03%)      fn mana_compatible(e: &Effect) -> bool {
2,399,040 ( 0.08%)          match e {
        .                       Effect::AddMana { .. } | Effect::Noop => true,
        .                       // Incidental you-only life gain (Altar of the Pantheon).
        .                       Effect::GainLife { who: crate::effect::Selector::You, .. } => true,
        .                       // Incidental non-targeting self-counter (Twitching Doll's "put a
        .                       // nest counter on this creature") — CR 605.1a rider, no stack use.
        .                       Effect::AddCounter { what: crate::effect::Selector::This, .. } => true,
        .                       // "This land doesn't untap during your next untap step" (the CHK
        .                       // slow duals) — another non-targeting CR 605.1a rider.
-- line 225 ----------------------------------------
-- line 232 ----------------------------------------
        .                       Effect::Seq(steps) => steps.iter().all(mana_compatible),
        .                       // A board-state-conditional that only ever adds mana on both
        .                       // branches is still a mana ability (Ilysian Caryatid's "add one
        .                       // of any color; add two instead if you control a power-4+
        .                       // creature").
        .                       Effect::If { then, else_, .. } => mana_compatible(then) && mana_compatible(else_),
        .                       _ => false,
        .                   }
1,332,800 ( 0.04%)      }
1,584,792 ( 0.05%)      produces_mana(effect) && mana_compatible(effect)
4,615,212 ( 0.15%)  => crabomination/src/game/actions.rs:crabomination::game::actions::is_mana_ability::mana_compatible (219,772x)
3,028,392 ( 0.10%)  => crabomination/src/game/actions.rs:crabomination::game::actions::is_mana_ability::produces_mana (201,816x)
        .           }
        .           
        .           /// The set of colours `card`'s untapped mana abilities can produce, in
        .           /// WUBRG order (a Forest → `[Green]`, a dual → two, Birds → all five, a
        .           /// colorless rock → `[]`). Used as a source's "signature" so the
        .           /// manual-tap decision can tell interchangeable sources (same signature)
        .           /// from genuinely different ones.
        .           fn source_color_signature(card: &crate::card::CardInstance) -> Vec<ManaColor> {
-- line 249 ----------------------------------------
-- line 348 ----------------------------------------
        .               card: &crate::card::CardInstance,
        .           ) -> Vec<(Option<crate::card::Predicate>, Effect)> {
        .               use crate::effect::{EventKind, EventScope};
        .               card.definition
        .                   .triggered_abilities
        .                   .iter()
        .                   .filter(|t| {
        .                       t.event.kind == EventKind::SpellCast
       40 ( 0.00%)                  && matches!(t.event.scope, EventScope::SelfSource)
        .                   })
        .                   .map(|t| (t.event.filter.clone(), t.effect.clone()))
        .                   .collect()
        .           }
        .           
        .           /// Count distinct colors of mana that decreased between two pool
        .           /// snapshots — i.e. the spell's converge value.
        .           /// Colored "or pay" halves of additional cast costs whose resource half
        .           /// isn't available right now (`ExileFromGraveyardOrPay` with a thin
        .           /// graveyard): the printed pay cost joins the spell's cost
        .           /// symbol-for-symbol — colored pips included, which the generic
        .           /// [`extra_cost_for_spell`] tax channel can't express.
   58,960 ( 0.00%)  pub(crate) fn or_pay_cost_symbols(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .           ) -> Vec<crate::mana::ManaSymbol> {
        .               let mut out = Vec::new();
        .               for c in &card.definition.additional_cast_cost {
        .                   if let crate::card::AdditionalCastCost::ExileFromGraveyardOrPay { filter, count, pay } = c
        .                   {
-- line 377 ----------------------------------------
-- line 380 ----------------------------------------
        .                           .iter()
        .                           .filter(|c| state.evaluate_requirement_on_card(filter, c, caster))
        .                           .count() as u32;
        .                       if matches < *count {
        .                           out.extend(pay.symbols.iter().cloned());
        .                       }
        .                   }
        .               }
   51,590 ( 0.00%)      out
   58,960 ( 0.00%)  }
        .           
        .           fn converge_count(before: &crate::mana::ManaPool, after: &crate::mana::ManaPool) -> u32 {
        .               use crate::mana::Color;
        .               let mut count = 0u32;
        .               for color in Color::ALL {
   77,506 ( 0.00%)          if before.amount(color) > after.amount(color) {
        .                       count += 1;
        .                   }
        .               }
        .               count
    7,046 ( 0.00%)  }
        .           
        .           /// Per-color breakdown of the mana drained between two pool snapshots
        .           /// (CR 601 — stamped onto `CardInstance.cast_mana_spent_by_color` for
        .           /// Adamant / Void Mirror reads). Colorless is not a color and is omitted.
        .           fn spent_by_color(
        .               before: &crate::mana::ManaPool,
        .               after: &crate::mana::ManaPool,
        .           ) -> Vec<(crate::mana::Color, u32)> {
        .               use crate::mana::Color;
        .               Color::ALL
        .                   .iter()
        .                   .filter_map(|&c| {
        .                       let spent = before.amount(c).saturating_sub(after.amount(c));
   35,230 ( 0.00%)              (spent > 0).then_some((c, spent))
        .                   })
        .                   .collect()
        .           }
        .           
        .           /// CR 701.67 — resolve a card's printed waterbend amount given the chosen X.
        .           /// Returns `(amount, optional)`, or `None` if the card has no waterbend.
        .           /// Supports `Value::Const(n)` and the chosen-X form (`Value::XFromCost`);
        .           /// other expressions resolve to 0.
-- line 422 ----------------------------------------
-- line 466 ----------------------------------------
        .                       other => other.clone(),
        .                   })
        .                   .collect()
        .           }
        .           
        .           /// CR 702.122 Strive (and Fireball's generic sibling): "this spell costs
        .           /// [cost] more to cast for each target beyond the first". Returns the total
        .           /// surcharge for `extra_targets` filled additional slots.
   58,960 ( 0.00%)  pub fn strive_cost_for_spell(
        .               card: &crate::card::CardInstance,
        .               extra_targets: usize,
        .           ) -> crate::mana::ManaCost {
        .               let mut out = crate::mana::ManaCost::default();
   14,740 ( 0.00%)      if let Some(per) = &card.definition.cost_per_extra_target {
        .                   for _ in 0..extra_targets {
        .                       out.symbols.extend(per.symbols.iter().cloned());
        .                   }
        .               }
   44,220 ( 0.00%)      out
   66,330 ( 0.00%)  }
        .           
  160,576 ( 0.01%)  pub fn extra_cost_for_spell(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .               target: Option<&crate::game::Target>,
        .           ) -> u32 {
        .               use crate::effect::StaticEffect;
        .               let mut tax = 0u32;
   80,288 ( 0.00%)      if state.players[caster].first_spell_tax_charges > 0 {
        .                   tax += 1;
        .               }
        .               // "Costs {N} more if it targets a [filter]" (Vanish into Eternity).
   28,154 ( 0.00%)      if let Some((filter, n)) = &card.definition.cost_increase_if_targets
        .                   && let Some(t) = target
        .                   && state.evaluate_requirement_static(filter, t, caster, None)
        .               {
        .                   tax += n;
        .               }
        .               // "Reveal a [filter] card from your hand or pay {N}" (Silvergill Adept):
        .               // no matching hand card → the pay half joins the cost. Same shape for
        .               // "sacrifice a [filter] or pay {N}" (Bayou Groff) against the battlefield.
-- line 507 ----------------------------------------
-- line 615 ----------------------------------------
        .                               }
        .                           }
        .                           _ => {}
        .                       }
        .                   }
        .               }
        .               // Turn-scoped taxes (Elspeth Conquers Death II): opponents of the
        .               // entry's controller pay until that player's next turn.
   20,072 ( 0.00%)      for t in &state.turn_scoped_spell_taxes {
        .                   if t.controller != caster && state.evaluate_requirement_on_card(&t.filter, card, caster) {
        .                       tax += t.amount;
        .                   }
        .               }
        .           
        .               tax
  180,648 ( 0.01%)  }
        .           
        .           /// Consume one Chancellor-of-the-Annex tax charge from `caster`, if any.
        .           /// Called by every cast path immediately after the spell successfully
        .           /// resolves payment, so each first-spell-tax charge is single-use.
        .           pub(crate) fn consume_first_spell_tax(state: &mut crate::game::GameState, caster: usize) {
   21,138 ( 0.00%)      if state.players[caster].first_spell_tax_charges > 0 {
        .                   state.players[caster].first_spell_tax_charges -= 1;
        .               }
        .           }
        .           
        .           /// Sum all generic-mana cost reductions applicable to a spell being cast.
        .           ///
        .           /// Supports two flavors:
        .           ///   * `StaticEffect::CostReduction { filter, amount }` — flat per-spell
-- line 644 ----------------------------------------
-- line 666 ----------------------------------------
        .                   _ => false,
        .               }
        .           }
        .           
        .           /// CR 601.2f — the COLORED half of static cost reduction
        .           /// (`StaticEffect::ColoredCostReduction`, Ragemonger). Returns the summed
        .           /// reduction cost; callers fold it in with `ManaCost::reduce_by_cost` right
        .           /// after the generic reduction.
   73,700 ( 0.00%)  pub fn colored_cost_reduction_for_spell(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .           ) -> crate::mana::ManaCost {
        .               use crate::effect::StaticEffect;
        .               let mut out = crate::mana::ManaCost::default();
        .               for src in &state.battlefield {
  376,056 ( 0.01%)          if src.controller != caster {
        .                       continue;
        .                   }
        .                   for sa in &src.definition.static_abilities {
        .                       if let StaticEffect::ColoredCostReduction { filter, less } = &sa.effect
        .                           && state.evaluate_requirement_on_card(filter, card, caster)
        .                       {
        .                           out.symbols.extend(less.symbols.iter().cloned());
        .                       }
        .                   }
        .               }
   51,590 ( 0.00%)      out
   58,960 ( 0.00%)  }
        .           
        .           /// CR 601.2f — the COLORED half of static cost *increases*
        .           /// (`StaticEffect::ColoredSpellTax`, the Invasion Leech cycle). Only the
        .           /// source's controller pays it. Callers append the returned pips to the cost
        .           /// before any reduction runs, so a discount can never eat the surcharge.
  200,720 ( 0.01%)  pub fn colored_spell_tax_for_spell(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .           ) -> crate::mana::ManaCost {
        .               use crate::effect::StaticEffect;
        .               let mut out = crate::mana::ManaCost::default();
        .               for src in &state.battlefield {
  887,826 ( 0.03%)          if src.controller != caster {
        .                       continue;
        .                   }
        .                   for sa in &src.definition.static_abilities {
        .                       if let StaticEffect::ColoredSpellTax { filter, more } = &sa.effect
        .                           && state.evaluate_requirement_on_card(filter, card, caster)
        .                       {
        .                           out.symbols.extend(more.symbols.iter().cloned());
        .                       }
        .                   }
        .               }
  140,504 ( 0.00%)      out
  160,576 ( 0.01%)  }
        .           
        .           pub fn cost_reduction_for_spell(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .               target: Option<&crate::game::Target>,
        .           ) -> u32 {
        .               cost_reduction_for_spell_zoned(state, caster, card, target, false)
-- line 728 ----------------------------------------
-- line 760 ----------------------------------------
        .           
        .           pub fn cost_reduction_for_spell_zoned(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .               target: Option<&crate::game::Target>,
        .               from_graveyard: bool,
        .           ) -> u32 {
  136,428 ( 0.00%)      cost_reduction_for_spell_full(state, caster, card, target, from_graveyard, false)
3,394,928 ( 0.11%)  => crabomination/src/game/actions.rs:crabomination::game::actions::cost_reduction_for_spell_full (7,370x)
        .           }
        .           
        .           /// Full cost-reduction scan with both `from_graveyard` and `from_exile` zone
        .           /// toggles. Exile-cast paths (foretell / adventure-creature / plotted / impulse
        .           /// pay-own-cost) pass `from_exile: true` so `ExileCastCostReduction` (Doc
        .           /// Aurlock) and every zone-agnostic reduction apply.
  260,936 ( 0.01%)  pub fn cost_reduction_for_spell_full(
        .               state: &crate::game::GameState,
        .               caster: usize,
        .               card: &crate::card::CardInstance,
        .               target: Option<&crate::game::Target>,
        .               from_graveyard: bool,
        .               from_exile: bool,
        .           ) -> u32 {
        .               use crate::effect::StaticEffect;
-- line 783 ----------------------------------------
-- line 1015 ----------------------------------------
        .                   }
        .               }
        .               // Card-intrinsic Affinity-for-[filter] cost reduction: "{1} less for
        .               // each [filter]" baked onto the spell card itself. Counts every
        .               // battlefield permanent matching `affinity_filter`. CR 601.2f / 117.7c —
        .               // generic-only, the colored-pip clamp happens in
        .               // `ManaCost::reduce_generic` once the caller folds this back into the
        .               // cost.
   60,216 ( 0.00%)      if let Some(filter) = &card.definition.affinity_filter {
        .                   let count = state
        .                       .battlefield
        .                       .iter()
        .                       .filter(|c| state.evaluate_requirement_on_card(filter, c, caster))
        .                       .count();
        .                   reduction = reduction.saturating_add(count as u32);
        .               }
        .               // Card-intrinsic "Affinity-for-cards-in-your-graveyard" cost reduction:
        .               // "{1} less for each [filter] card in your graveyard" (The Dawning
        .               // Archaic, Tolarian Terror). Generic-only, clamped by the caller.
   60,216 ( 0.00%)      if let Some(filter) = &card.definition.affinity_graveyard_filter {
        .                   let count = state.players[caster]
        .                       .graveyard
        .                       .iter()
        .                       .filter(|c| state.evaluate_requirement_on_card(filter, c, caster))
        .                       .count();
        .                   reduction = reduction.saturating_add(count as u32);
        .               }
        .               // Card-intrinsic target-conditional reduction (Ride's End): "{amount}
        .               // less if it targets a permanent matching `filter`." Generic-only.
   25,460 ( 0.00%)      if let Some((filter, amount)) = &card.definition.self_cost_reduction_if_target
        .                   && let Some(tgt) = target
        .                   && state.evaluate_requirement_static(filter, tgt, caster, Some(card.id))
        .               {
        .                   reduction = reduction.saturating_add(*amount);
        .               }
        .               // Card-intrinsic board-state-gated flat reductions (Pearl of Wisdom,
        .               // Geistlight Snare): each "{amount} less if you control a permanent
        .               // matching `filter`" clause applies independently.
-- line 1052 ----------------------------------------
-- line 1055 ----------------------------------------
        .                       .battlefield
        .                       .iter()
        .                       .any(|c| state.evaluate_requirement_on_card(filter, c, caster))
        .                   {
        .                       reduction = reduction.saturating_add(*amount);
        .                   }
        .               }
        .               // Card-intrinsic "costs {amount} less if it's night" (Moonrager's Slash).
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_night
        .                   && state.day_night == Some(crate::game::types::DayNight::Night)
        .               {
        .                   reduction = reduction.saturating_add(amount);
        .               }
        .               // Card-intrinsic Delirium reduction — "{amount} less while four or more
        .               // card types are in your graveyard" (Drag to the Roots).
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_delirium
        .                   && state.delirium_active(caster)
        .               {
        .                   reduction = reduction.saturating_add(amount);
        .               }
        .               // Card-intrinsic crime reduction — "{amount} less if you've committed a
        .               // crime this turn" (Seize the Secrets, CR 700.13).
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_crime
        .                   && state.players[caster].committed_crime_this_turn
        .               {
        .                   reduction = reduction.saturating_add(amount);
        .               }
        .               // Card-intrinsic "costs {amount} less if you've sacrificed an artifact this
        .               // turn" (Suspicious Detonation).
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_sacrificed_artifact
        .                   && state.players[caster].artifacts_sacrificed_this_turn > 0
        .               {
        .                   reduction = reduction.saturating_add(amount);
        .               }
        .               // Card-intrinsic "costs {1} less for each card you've drawn this turn"
        .               // (Deem Inferior). Generic-only, clamped by the caller.
   40,144 ( 0.00%)      if card.definition.self_cost_reduction_per_cards_drawn {
        .                   reduction = reduction.saturating_add(state.players[caster].cards_drawn_this_turn);
        .               }
        .               // Card-intrinsic "costs {amount} less if you've cast another spell this
        .               // turn" (Rally the Monastery). `spells_cast_this_turn` excludes the spell
        .               // being cast, so `> 0` means a prior spell went off.
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_cast_spell
        .                   && state.players[caster].spells_cast_this_turn > 0
        .               {
        .                   reduction = reduction.saturating_add(amount);
        .               }
        .               // Card-intrinsic predicate-gated discount (the Prophecy Avatars).
   40,144 ( 0.00%)      if let Some((cond, amount)) = &card.definition.self_cost_reduction_if {
        .                   let ctx = crate::game::effects::EffectContext::for_spell(caster, None, 0, 0);
        .                   if state.evaluate_predicate(cond, &ctx) {
        .                       reduction = reduction.saturating_add(*amount);
        .                   }
        .               }
        .               // Card-intrinsic scaled discount — "{per} less for each [value]" (Domain:
        .               // Draco, Stratadon). Generic-only, clamped by the caller.
   40,144 ( 0.00%)      if let Some((value, per)) = &card.definition.self_cost_reduction_per {
        .                   let ctx = crate::game::effects::EffectContext::for_spell(caster, None, 0, 0);
        .                   let units = state.evaluate_value(value, &ctx).max(0) as u32;
        .                   reduction = reduction.saturating_add(units.saturating_mul(*per));
        .               }
        .               // Card-intrinsic "costs {amount} less if evidence was collected" (Bite Down
        .               // on Crime, CR 701.59). The collect is an optional additional cost announced
        .               // during casting; the auto-decider collects whenever the graveyard can
        .               // afford it, so mirror that here to keep cost-calc and payment consistent.
   40,144 ( 0.00%)      if let Some(amount) = card.definition.self_cost_reduction_if_collect_evidence
        .                   && card
        .                       .definition
        .                       .additional_cast_cost
        .                       .iter()
        .                       .any(|c| matches!(c, crate::card::AdditionalCastCost::CollectEvidence { amount, .. }
        .                           if state.graveyard_can_collect_evidence(caster, *amount)))
        .               {
        .                   reduction = reduction.saturating_add(amount);
-- line 1128 ----------------------------------------
-- line 1295 ----------------------------------------
        .                       .map(|c| c.definition.name)
        .                       .collect();
        .                   names.sort_unstable();
        .                   names.dedup();
        .                   reduction = reduction.saturating_add(names.len() as u32);
        .               }
        .               // Card-intrinsic "costs {amount} less during your turn" (Mental Modulation).
        .               // Generic-only, clamped by the caller.
   60,216 ( 0.00%)      if state.active_player_idx == caster {
        .                   for sa in &card.definition.static_abilities {
        .                       if let StaticEffect::SelfCostReducedDuringYourTurn { amount } = sa.effect {
        .                           reduction = reduction.saturating_add(amount);
        .                       }
        .                   }
        .               }
        .               // Card-intrinsic "costs {X} less, where X is your devotion to [colors]"
        .               // (Theros — Daybreak Chimera). Generic-only, clamped by the caller.
-- line 1311 ----------------------------------------
-- line 1315 ----------------------------------------
        .                   }
        .               }
        .               // One-shot "the next instant or sorcery you cast this turn costs {N}
        .               // less" discounts (Thundertrap Trainer). Each was stamped with the
        .               // caster's instant/sorcery tally at grant time; it applies only while
        .               // that tally is unchanged — i.e. to the *next* such spell — and then
        .               // naturally lapses once the tally ticks up on cast (no consume hook
        .               // needed). The tally increments at spell-commit, after this read.
  160,836 ( 0.01%)      if card.definition.is_instant() || card.definition.is_sorcery() {
  301,340 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_instant (20,072x)
  211,204 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_sorcery (13,468x)
    8,032 ( 0.00%)          let cast_so_far = state.players[caster].instants_or_sorceries_cast_this_turn;
        .                   for &(amount, granted_at) in &state.players[caster].pending_is_discounts {
        .                       if granted_at == cast_so_far {
        .                           reduction = reduction.saturating_add(amount);
        .                       }
        .                   }
        .               }
        .               // One-shot "the next spell you cast this turn costs {N} less"
        .               // discounts (Mutated Cultist) — any spell type; same lapse-by-tally
        .               // scheme as `pending_is_discounts` above.
        .               {
   20,072 ( 0.00%)          let cast_so_far = state.players[caster].spells_cast_this_turn;
        .                   for &(amount, granted_at) in &state.players[caster].pending_spell_discounts {
        .                       if granted_at == cast_so_far {
        .                           reduction = reduction.saturating_add(amount);
        .                       }
        .                   }
        .               }
        .               // Card-intrinsic "costs {N} less if you control a permanent matching each
        .               // filter" (Of One Mind). Generic-only, clamped by the caller.
-- line 1343 ----------------------------------------
-- line 1399 ----------------------------------------
        .                       } else {
        .                           state.players[caster].creatures_attacked_this_turn
        .                       };
        .                       reduction = reduction.saturating_add(per * count);
        .                   }
        .               }
        .               // Turn-scoped "[filter] spells you cast this turn cost {N} less"
        .               // grants (Urza, Planeswalker's +2). Cleared at cleanup.
   20,072 ( 0.00%)      for (filter, amount) in &state.players[caster].turn_spell_discounts {
        .                   if state.evaluate_requirement_on_card(filter, card, caster) {
        .                       reduction = reduction.saturating_add(*amount);
        .                   }
        .               }
        .               // Transient "sacrifice any number, {N} less each" additional-cost
        .               // reduction (Awaken the Blood Avatar). Stamped on the state for the
        .               // duration of one cast by `cast_spell_sacrifice_reduce`.
        .               reduction = reduction.saturating_add(state.extra_cast_reduction);
        .               reduction
  160,576 ( 0.01%)  }
        .           
        .           /// Trinisphere floor: the largest `StaticEffect::SpellCostFloor` amount
        .           /// among untapped battlefield permanents (affects every player's spells).
        .           /// Returns 0 if none in play.
        .           pub(crate) fn spell_cost_floor(state: &crate::game::GameState) -> u32 {
        .               use crate::effect::StaticEffect;
        .               state
        .                   .battlefield
-- line 1425 ----------------------------------------
-- line 1432 ----------------------------------------
        .                   })
        .                   .max()
        .                   .unwrap_or(0)
        .           }
        .           
        .           /// Raise `cost` so its mana value is at least the active Trinisphere floor
        .           /// by padding generic mana. No-op when no floor is in play or the cost is
        .           /// already at/above it.
   44,220 ( 0.00%)  pub(crate) fn apply_spell_cost_floor(
        .               state: &crate::game::GameState,
        .               cost: &mut crate::mana::ManaCost,
        .           ) {
    7,370 ( 0.00%)      let floor = spell_cost_floor(state);
   51,590 ( 0.00%)      let mv = cost.cmc();
  204,474 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (7,370x)
        .               if floor > mv {
        .                   cost.symbols
        .                       .push(crate::mana::ManaSymbol::Generic(floor - mv));
        .               }
        .           }
        .           
        .           /// True if `player` controls a permanent granting Omniscience-style free
        .           /// casting of hand spells (`StaticEffect::CastHandSpellsFree`).
-- line 1453 ----------------------------------------
-- line 1575 ----------------------------------------
        .           ///   permanent's controller has an Elesh Norn, the trigger is suppressed
        .           ///   (returns 0).
        .           /// - "If a permanent entering the battlefield causes a triggered ability of
        .           ///   a permanent you control to trigger, that ability triggers an additional
        .           ///   time" → each Elesh Norn on the trigger-source's side adds one extra fire.
        .           ///
        .           /// `etb_controller` is the controller of the ability's source — for self-ETB
        .           /// triggers, that's the entering permanent itself.
   31,232 ( 0.00%)  pub fn etb_trigger_multiplier(
        .               state: &crate::game::GameState,
        .               etb_controller: usize,
        .               entering: Option<CardId>,
        .           ) -> usize {
        .               use crate::effect::StaticEffect;
        .               // Torpor Orb / Tocatli Honor Guard (CR 614): an entering *creature*
        .               // causes no triggered abilities to trigger while a suppressor is in play.
    7,808 ( 0.00%)      if let Some(id) = entering
    9,768 ( 0.00%)          && state
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
    7,808 ( 0.00%)              .is_some_and(|c| c.definition.is_creature())
   56,632 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,904x)
        .                   && creature_etb_triggers_suppressed(state)
        .               {
        .                   return 0;
        .               }
        .               // Doorkeeper Thrull — an entering *artifact* likewise causes no triggers
        .               // while an `also_artifacts` suppressor is in play.
        .               if let Some(id) = entering
        .                   && state
-- line 1604 ----------------------------------------
-- line 1630 ----------------------------------------
        .                       .filter(|sa| matches!(sa.effect, StaticEffect::EtbTriggerSpotlight))
        .                       .count();
        .                   let count_doubler = c
        .                       .definition
        .                       .static_abilities
        .                       .iter()
        .                       .filter(|sa| matches!(sa.effect, StaticEffect::DoubleControllerEtbTriggers))
        .                       .count();
  520,524 ( 0.02%)          if c.controller == etb_controller {
        .                       your_norns += count_spotlight;
        .                       your_doublers += count_doubler;
        .                   } else {
        .                       opp_norns += count_spotlight;
        .                   }
        .               }
    7,808 ( 0.00%)      if opp_norns > 0 {
        .                   0
        .               } else {
    3,904 ( 0.00%)          1 + your_norns + your_doublers
        .               }
   31,232 ( 0.00%)  }
        .           
        .           /// True when any battlefield permanent carries a
        .           /// `SuppressCreatureEtbTriggers` static (Torpor Orb, Tocatli Honor Guard,
        .           /// Hushbringer). Applies globally — both players' creature ETB triggers.
        .           pub(crate) fn creature_etb_triggers_suppressed(state: &crate::game::GameState) -> bool {
        .               use crate::effect::StaticEffect;
        .               state.battlefield.iter().any(|c| {
        .                   c.definition
-- line 1658 ----------------------------------------
-- line 1665 ----------------------------------------
        .           /// CR 603-style "triggers an additional time" — extra fires a triggered
        .           /// ability gets because its `source` is a creature controlled by a player who
        .           /// controls a subtype-keyed trigger doubler: `DoubleControllerAllyTriggers`
        .           /// (Katara, the Fearless — Allies) or the general
        .           /// `DoubleControllerTriggersOfType` (Harmonic Prodigy — Shaman / another
        .           /// Wizard), `DoubleControllerLegendaryCreatureTriggers` (Annie Joins Up) or
        .           /// the unconditional `DoubleControllerPermanentTriggers` (Fractured Realm).
        .           /// 0 unless the source is a matching permanent `controller` controls.
   42,560 ( 0.00%)  pub(crate) fn ally_trigger_extra_fires(
        .               state: &crate::game::GameState,
        .               controller: usize,
        .               source: crate::card::CardId,
        .           ) -> usize {
        .               use crate::effect::StaticEffect;
        .               // The triggering source's current creature types (read live off the
        .               // battlefield; a source no longer in play or not `controller`'s → 0).
   21,280 ( 0.00%)      let Some(cp) = state.computed_permanent(source) else { return 0 };
11,465,829 ( 0.37%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (4,256x)
    8,512 ( 0.00%)      if state.battlefield_find(source).is_none_or(|c| c.controller != controller) {
        .                   return 0;
        .               }
        .               let source_types = cp.subtypes.creature_types.clone();
        .               let is_ally = source_types.contains(&crate::card::CreatureType::Ally);
        .               let legendary_creature = cp.card_types.contains(&crate::card::CardType::Creature)
        .                   && cp.supertypes.contains(&crate::card::Supertype::Legendary);
    4,256 ( 0.00%)      state
        .                   .battlefield
        .                   .iter()
   61,006 ( 0.00%)          .filter(|c| c.controller == controller)
        .                   .map(|c| {
        .                       c.definition
        .                           .static_abilities
        .                           .iter()
        .                           .filter(|sa| match &sa.effect {
        .                               StaticEffect::DoubleControllerAllyTriggers => is_ally,
        .                               StaticEffect::DoubleControllerTriggersOfType { types, exclude_source } => {
        .                                   (!*exclude_source || c.id != source)
-- line 1700 ----------------------------------------
-- line 1709 ----------------------------------------
        .                                   ),
        .                               StaticEffect::DoubleControllerLegendaryCreatureTriggers => legendary_creature,
        .                               StaticEffect::DoubleControllerPermanentTriggers => true,
        .                               _ => false,
        .                           })
        .                           .count()
        .                   })
        .                   .sum()
   38,304 ( 0.00%)  }
        .           
        .           /// True when any battlefield permanent carries a
        .           /// `SuppressCreatureEtbTriggers { also_dies: true }` static (Hushbringer).
        .           /// Suppresses creature-death triggers globally (CR 614).
        .           pub(crate) fn creature_dies_triggers_suppressed(state: &crate::game::GameState) -> bool {
        .               use crate::effect::StaticEffect;
        .               state.battlefield.iter().any(|c| {
        .                   c.definition.static_abilities.iter().any(|sa| {
-- line 1725 ----------------------------------------
-- line 1737 ----------------------------------------
        .           ///
        .           /// Returns `true` if the trigger should fire (controller paid or no tax in
        .           /// play), `false` if it should be countered (controller declined or
        .           /// couldn't pay). Countering the ability has no other consequence — the
        .           /// trigger simply never fires (CR 701.5a).
        .           ///
        .           /// `trigger_source` is the permanent whose ability is triggering (used to
        .           /// aim the pay-or-counter decision at something visible).
      546 ( 0.00%)  pub(crate) fn apply_etb_trigger_tax(
        .               state: &mut crate::game::GameState,
        .               trigger_source: crate::card::CardId,
        .               trigger_controller: usize,
        .           ) -> bool {
        .               use crate::decision::{Decision, DecisionAnswer};
        .               use crate::effect::StaticEffect;
        .               use crate::mana::ManaCost;
        .           
        .               // Sum tax amounts from every Strict Proctor on the battlefield.
        .               // Each Strict Proctor demands its own payment per the printed
        .               // "counter that ability unless its controller pays {2}" wording —
        .               // but applied as a single rolled-up amount via additive tax
        .               // (matching the existing engine's handling of stacking-tax effects).
       78 ( 0.00%)      let total_tax: u32 = state
        .                   .battlefield
        .                   .iter()
        .                   .flat_map(|c| c.definition.static_abilities.iter())
        .                   .filter_map(|sa| {
        .                       if let StaticEffect::EtbTriggerTax { amount } = &sa.effect {
        .                           Some(*amount)
        .                       } else {
        .                           None
        .                       }
        .                   })
        .                   .sum();
      156 ( 0.00%)      if total_tax == 0 {
        .                   return true;
        .               }
        .               // Build a "Pay {total_tax}" decision aimed at the trigger's controller.
        .               let answer = state.decider.decide(&Decision::OptionalTrigger {
        .                   source: trigger_source,
        .                   description: format!("Pay {{{}}} to keep this trigger?", total_tax),
        .               });
        .               if matches!(answer, DecisionAnswer::Bool(true)) {
-- line 1779 ----------------------------------------
-- line 1782 ----------------------------------------
        .                       return true;
        .                   }
        .                   // Couldn't actually afford the tax — fall through and counter.
        .               }
        .               // Counter the triggered ability (CR 701.5a): it never fires, and
        .               // nothing else happens — the printed card counters the ability, it
        .               // does NOT touch the trigger's source permanent.
        .               false
      702 ( 0.00%)  }
        .           
        .           /// Cast-time "can't be countered" checks (printed keyword, turn-scoped
        .           /// grants, Banefire X gates). Cavern of Souls rides mana provenance
        .           /// instead — see `note_cast_payment_riders`.
        .           impl crate::game::GameState {
        .               /// Legacy entrypoint kept for symmetry; new call sites should use
        .               /// `caster_grants_uncounterable_with_x` to thread the cast's X
        .               /// value. Internally delegates with X = 0.
-- line 1798 ----------------------------------------
-- line 1804 ----------------------------------------
        .               ) -> bool {
        .                   self.caster_grants_uncounterable_with_x(caster, card, 0)
        .               }
        .           
        .               /// X-aware variant. Threaded by `finalize_cast` so cards whose
        .               /// "can't be countered" rider is gated on the X value (Banefire's
        .               /// "if X is 5 or more, this spell can't be countered") see the
        .               /// correct flag at cast time.
   77,506 ( 0.00%)      pub(crate) fn caster_grants_uncounterable_with_x(
        .                   &self,
        .                   caster: usize,
        .                   card: &crate::card::CardInstance,
        .                   x_value: u32,
        .               ) -> bool {
        .                   // The card itself is uncounterable (Dovin's Veto, Stubborn Denial,
        .                   // etc. — `Keyword::CantBeCountered`).
        .                   if card.definition.keywords.contains(&Keyword::CantBeCountered) {
        .                       return true;
        .                   }
        .                   // Turn-scoped grant — Veil of Summer's "spells your opponents
        .                   // control can't counter spells you control this turn."
   28,184 ( 0.00%)          if self.players[caster].spells_uncounterable_this_turn {
        .                       return true;
        .                   }
        .                   // Creature-spell-only grant (Domri, Anarch of Bolas's +1).
   14,092 ( 0.00%)          if self.players[caster].creature_spells_uncounterable_this_turn
        .                       && card.definition.is_creature()
        .                   {
        .                       return true;
        .                   }
        .                   // "The next [filter] spell you cast this turn can't be countered"
        .                   // (Insist, Overmaster). Consumed by `consume_next_spell_uncounterable`
        .                   // once the cast goes through.
    7,046 ( 0.00%)          if self.players[caster]
        .                       .next_spell_uncounterable
        .                       .iter()
        .                       .any(|f| self.evaluate_requirement_on_card(f, card, caster))
        .                   {
        .                       return true;
        .                   }
        .                   // Conditional "if X is N or more, this spell can't be countered"
        .                   // rider (Banefire-style). Threshold lives on the card's printed
        .                   // keywords as `CantBeCounteredIfXAtLeast(threshold)`; checked
        .                   // against the actual paid X. Any future card with the same
        .                   // shape plugs in by carrying the keyword — no engine change.
        .                   let xcc_threshold = card.definition.keywords.iter().find_map(|kw| {
    8,224 ( 0.00%)              if let Keyword::CantBeCounteredIfXAtLeast(n) = kw {
        .                           Some(*n)
        .                       } else {
        .                           None
        .                       }
        .                   });
   14,092 ( 0.00%)          if let Some(n) = xcc_threshold
        .                       && x_value >= n
        .                   {
        .                       return true;
        .                   }
        .                   // Battlefield statics — "creature and enchantment spells you
        .                   // control can't be countered" (Destiny Spinner). The card is mid-cast
        .                   // (in no zone yet), so match the filter on the card itself.
  128,252 ( 0.00%)          for src in self.battlefield.iter().filter(|c| c.controller == caster) {
        .                       for sa in &src.definition.static_abilities {
        .                           if let crate::effect::StaticEffect::SpellsUncounterable { filter } = &sa.effect
        .                               && self.evaluate_requirement_on_card(filter, card, caster)
        .                           {
        .                               return true;
        .                           }
        .                       }
        .                   }
        .                   // Symmetric "creature spells can't be countered" (Leyline of Lifeforce):
        .                   // any player's copy protects every player's creature spells, so scan the
        .                   // whole battlefield rather than just the caster's permanents.
   21,138 ( 0.00%)          if card.definition.is_creature()
  101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
        .                       && self.battlefield.iter().any(|c| {
        .                           c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, crate::effect::StaticEffect::CreatureSpellsCantBeCountered)
        .                           })
        .                       })
        .                   {
        .                       return true;
        .                   }
        .                   // The filtered sibling, also symmetric (Root Sliver).
    7,046 ( 0.00%)          let matching: Vec<crate::card::SelectionRequirement> = self
        .                       .battlefield
        .                       .iter()
        .                       .flat_map(|c| c.definition.static_abilities.iter())
        .                       .filter_map(|sa| match &sa.effect {
        .                           crate::effect::StaticEffect::SpellsCantBeCounteredMatching { filter } => {
        .                               Some(filter.clone())
        .                           }
        .                           _ => None,
-- line 1894 ----------------------------------------
-- line 1900 ----------------------------------------
        .                   }) {
        .                       return true;
        .                   }
        .                   // Cavern of Souls' "can't be countered" rider is provenance-based:
        .                   // it rides the spent mana (`SpendRestriction::
        .                   // CreatureOfTypeUncounterable` → `cast_paid_uncounterable`), not a
        .                   // battlefield scan — see `note_cast_payment_riders`.
        .                   false
   63,414 ( 0.00%)      }
        .           
        .               /// Lier, Disciple of the Drowned: if `seat` controls a permanent granting
        .               /// "each instant and sorcery card in your graveyard has flashback (= its
        .               /// mana cost)", returns the flashback cost for an I/S `card` that lacks a
        .               /// printed/granted flashback of its own. Consulted by the flashback-cast
        .               /// path and the graveyard view.
        .               pub fn graveyard_flashback_grant(
        .                   &self,
-- line 1916 ----------------------------------------
-- line 1939 ----------------------------------------
        .                   (granted || strip).then(|| card.definition.cost.clone())
        .               }
        .           
        .               /// CR 702.97 — the extra activated abilities a card in `owner`'s graveyard
        .               /// has by grant (Varolz grants scavenge, cost = mana cost, to that player's
        .               /// creature cards). Surfaced as virtual `from_graveyard` abilities at
        .               /// indices ≥ the card's printed activated-ability count. Empty for the
        .               /// common case.
  104,080 ( 0.00%)      pub(crate) fn graveyard_granted_abilities(
        .                   &self,
        .                   owner: usize,
        .                   card: &crate::card::CardInstance,
        .               ) -> Vec<crate::effect::ActivatedAbility> {
        .                   use crate::effect::StaticEffect;
        .                   let mut out = Vec::new();
        .                   // Instance grants first (Cursecloth Wrappings' until-EOT embalm), so
        .                   // their indices match `granted_abilities_for`'s off-battlefield order.
        .                   out.extend(card.granted_activated_abilities.iter().cloned());
        .                   out.extend(card.granted_activated_eot.iter().cloned());
        .                   // Varolz — scavenge on creature cards, unless the card prints its own.
   31,224 ( 0.00%)          if card.definition.is_creature()
  152,194 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (10,408x)
        .                       && !card
        .                           .definition
        .                           .activated_abilities
        .                           .iter()
        .                           .any(|a| a.exile_self_cost && a.from_graveyard)
    4,778 ( 0.00%)              && self.battlefield.iter().any(|c| {
  218,880 ( 0.01%)                  c.controller == owner
        .                               && c.definition
        .                                   .static_abilities
        .                                   .iter()
        .                                   .any(|sa| matches!(sa.effect, StaticEffect::GraveyardCreaturesHaveScavenge))
        .                       })
        .                   {
        .                       out.push(crate::effect::shortcut::scavenge(card.definition.cost.clone()));
        .                   }
   41,632 ( 0.00%)          out
   93,672 ( 0.00%)      }
        .           
        .               /// The scavenge cost (CR 702.97) a graveyard creature card can be exiled for
        .               /// — its printed scavenge ability, or a Varolz-style grant — so the client
        .               /// can offer the activation. `None` for non-scavengers.
        .               pub fn effective_scavenge_cost(
        .                   &self,
        .                   owner: usize,
        .                   card: &crate::card::CardInstance,
-- line 1984 ----------------------------------------
-- line 2005 ----------------------------------------
        .                               .find(is_scavenge)
        .                               .map(|ab| ab.mana_cost)
        .                       })
        .               }
        .           
        .               /// Note restricted-mana riders from a cast's payment: spending
        .               /// Cavern-of-Souls mana stamps the cast uncounterable (consumed by
        .               /// `finalize_cast`).
   21,138 ( 0.00%)      pub(crate) fn note_cast_payment_riders(
        .                   &mut self,
        .                   receipt: &PaymentReceipt,
        .                   kind: &crate::mana::SpellKind,
        .               ) {
        .                   use crate::mana::SpendRestriction;
        .                   if receipt.side_effects.spent_restrictions.iter().any(|r| match r {
        .                       SpendRestriction::CreatureOfTypeUncounterable(_) => true,
        .                       // Boseiju — only an instant/sorcery funded this way is stamped.
-- line 2021 ----------------------------------------
-- line 2026 ----------------------------------------
        .                   }
        .                   // Generator Servant — mana spent on a creature spell grants it haste.
        .                   if kind.creature
        .                       && receipt.side_effects.spent_restrictions.contains(&SpendRestriction::CreatureHaste)
        .                   {
        .                       let p = self.priority.player_with_priority;
        .                       self.players[p].pending_creature_etb_keywords.push(crate::card::Keyword::Haste);
        .                   }
   28,184 ( 0.00%)      }
        .           
        .               /// True if any battlefield permanent's static abilities include
        .               /// `StaticEffect::LandsTapColorlessOnly` (Damping Sphere). Used by
        .               /// `play_land` to downgrade multi-mana lands to colorless on entry.
        .               pub(crate) fn lands_tap_colorless_only_active(&self) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
        .                       c.definition
-- line 2042 ----------------------------------------
-- line 2051 ----------------------------------------
        .               /// the given player controls whose static abilities include
        .               /// `StaticEffect::ExtraLandPerTurn` (Exploration, Azusa Lost But
        .               /// Seeking, Wayward Swordtooth). Each granting permanent adds one,
        .               /// so two Explorations stack to "three lands per turn."
        .               pub fn extra_land_plays_per_turn(&self, player: usize) -> u32 {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield
        .                       .iter()
  165,802 ( 0.01%)              .filter(|c| c.controller == player)
        .                       .map(|c| {
        .                           c.definition
        .                               .static_abilities
        .                               .iter()
        .                               .filter(|sa| matches!(sa.effect, StaticEffect::ExtraLandPerTurn))
        .                               .count() as u32
        .                       })
        .                       .sum()
        .               }
        .           
        .               /// CR 305.2 — Total lands `player` may play this turn. Defaults to
        .               /// 1, plus any `ExtraLandPerTurn` static grants, plus the player's
        .               /// manually-set `extra_land_plays` field (set by resolved effects
        .               /// like Explore).
        .               pub fn max_lands_per_turn(&self, player: usize) -> u32 {
   43,504 ( 0.00%)          1 + self.extra_land_plays_per_turn(player) + self.players[player].extra_land_plays
        .               }
        .           
        .               /// CR 305.2a — Whether `player` may legally play another land this
        .               /// turn. Compares lands already played to the active per-turn cap
        .               /// (which honors `ExtraLandPerTurn` static effects).
   76,888 ( 0.00%)      pub fn can_player_play_land(&self, player: usize) -> bool {
        .                   // CR 305.1 — "You can't play lands" (Aggressive Mining) is absolute.
        .                   if self.battlefield.iter().any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| {
        .                           matches!(sa.effect, crate::effect::StaticEffect::NoPlayerCanPlayLands)
        .                               || (c.controller == player
        .                                   && matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::ControllerCantPlayLands
        .                                   ))
        .                       })
        .                   }) {
        .                       return false;
        .                   }
        .                   // Turf Wound — a turn-scoped, player-scoped lock.
   21,968 ( 0.00%)          if self.players[player].cant_play_lands_this_turn {
        .                       return false;
        .                   }
        .                   // Damping Engine — the player ahead on permanents can't play lands.
   32,952 ( 0.00%)          if self.damping_engine_locks(player) {
3,282,308 ( 0.11%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::damping_engine_locks (10,984x)
        .                       return false;
        .                   }
   43,936 ( 0.00%)          self.players[player].lands_played_this_turn < self.max_lands_per_turn(player)
   87,872 ( 0.00%)      }
        .           
        .               /// Damping Engine (CR 611) — true when `player` controls strictly more
        .               /// permanents than each other player and a Damping Engine they haven't
        .               /// bought out of this turn is on the battlefield.
   77,890 ( 0.00%)      pub(crate) fn damping_engine_locks(&self, player: usize) -> bool {
        .                   let count = |seat: usize| {
1,460,858 ( 0.05%)              self.battlefield.iter().filter(|c| c.controller == seat).count()
        .                   };
        .                   let mine = count(player);
   67,494 ( 0.00%)          if (0..self.players.len()).any(|s| s != player && count(s) >= mine) {
        .                       return false;
        .                   }
        .                   self.battlefield.iter().any(|c| {
        .                       c.definition
        .                           .static_abilities
        .                           .iter()
        .                           .any(|sa| matches!(sa.effect, crate::effect::StaticEffect::MostPermanentsCantPlay))
        .                           && !self.players[player].statics_ignored_this_turn.contains(&c.id)
        .                   })
   93,468 ( 0.00%)      }
        .           
        .               /// CR 402.2 — `player`'s effective maximum hand size, honoring any
        .               /// `StaticEffect::NoMaximumHandSize` permanent they control (Reliquary
        .               /// Tower, Thought Vessel). `None` means "no maximum" (skip cleanup
        .               /// discard entirely).
   17,180 ( 0.00%)      pub fn effective_max_hand_size(&self, player: usize) -> Option<usize> {
        .                   use crate::effect::StaticEffect;
        .                   let no_max = self.battlefield.iter().any(|c| {
   64,956 ( 0.00%)              c.controller == player
        .                           && c.definition
        .                               .static_abilities
        .                               .iter()
        .                               .any(|sa| matches!(sa.effect, StaticEffect::NoMaximumHandSize))
        .                   });
        .                   if no_max {
        .                       return None;
        .                   }
        .                   // Jin-Gitaxias — "each opponent's maximum hand size is reduced by N."
    1,718 ( 0.00%)          let reduction: usize = self
        .                       .battlefield
        .                       .iter()
  129,912 ( 0.00%)              .filter(|c| !self.same_team(c.controller, player))
  609,544 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (32,478x)
        .                       .flat_map(|c| c.definition.static_abilities.iter())
        .                       .map(|sa| match sa.effect {
        .                           StaticEffect::OpponentsMaxHandSizeReduced(n) => n as usize,
        .                           _ => 0,
        .                       })
        .                       .sum::<usize>()
        .                       // Thought Nibbler — the controller-scoped reduction.
        .                       + self
        .                           .battlefield
        .                           .iter()
   32,478 ( 0.00%)                  .filter(|c| c.controller == player)
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               StaticEffect::ControllerMaxHandSizeReduced(n) => n as usize,
        .                               _ => 0,
        .                           })
        .                           .sum::<usize>();
        .                   // Set-to-N overrides (Necrodominance) plus Cursed Rack's chosen-player
        .                   // cap. CR 613.11 — game-rule-modifying effects apply in timestamp
-- line 2165 ----------------------------------------
-- line 2179 ----------------------------------------
        .                       })
        .                       .max_by_key(|(ts, _)| *ts)
        .                       .map(|(_, n)| n);
        .                   // Minamo Scrollkeeper / Trusted Advisor — "your maximum hand size is
        .                   // increased by N"; copies stack, applied after any set-to override.
        .                   let increase: usize = self
        .                       .battlefield
        .                       .iter()
   32,478 ( 0.00%)              .filter(|c| c.controller == player)
        .                       .flat_map(|c| c.definition.static_abilities.iter())
        .                       .map(|sa| match sa.effect {
        .                           StaticEffect::ControllerMaxHandSizeIncreased(n) => n as usize,
        .                           _ => 0,
        .                       })
        .                       .sum();
    1,718 ( 0.00%)          let base = set_to.or(self.players[player].max_hand_size);
    1,718 ( 0.00%)          base.map(|m| (m + increase).saturating_sub(reduction))
   13,744 ( 0.00%)      }
        .           
        .               /// CR 305 — Whether `player` may play lands from their graveyard
        .               /// (Crucible of Worlds, Ramunap Excavator) via a
        .               /// `StaticEffect::MayPlayLandsFromGraveyard` permanent.
        .               pub fn player_may_play_lands_from_graveyard(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
    1,928 ( 0.00%)          if self.players[player].play_from_graveyard_this_turn {
        .                       return true;
        .                   }
        .                   self.battlefield.iter().any(|c| {
   38,836 ( 0.00%)              c.controller == player
        .                           && c.definition
        .                               .static_abilities
        .                               .iter()
        .                               .any(|sa| {
        .                                   matches!(sa.effect, StaticEffect::MayPlayLandsFromGraveyard)
        .                                       || (self.active_player_idx == player
        .                                           && matches!(
        .                                               sa.effect,
-- line 2215 ----------------------------------------
-- line 2219 ----------------------------------------
        .                   })
        .               }
        .           
        .               /// CR 701.10f / 614.5 — combined mana-production multiplier from the
        .               /// `ManaProductionDoubled` (Mana Reflection) and `ManaProductionTripled`
        .               /// (Nyxbloom Ancient) permanents `player` controls. Replacements compose
        .               /// multiplicatively: 2^doublers × 3^triplers, clamped to keep pip math
        .               /// sane.
   54,888 ( 0.00%)      pub fn mana_production_multiplier_for(&self, player: usize) -> u32 {
        .                   use crate::effect::StaticEffect;
        .                   let mut mult: u32 = 1;
  330,746 ( 0.01%)          for c in self.battlefield.iter().filter(|c| c.controller == player) {
        .                       for sa in &c.definition.static_abilities {
        .                           match sa.effect {
        .                               StaticEffect::ManaProductionDoubled => mult = mult.saturating_mul(2),
        .                               StaticEffect::ManaProductionTripled => mult = mult.saturating_mul(3),
        .                               _ => {}
        .                           }
        .                       }
        .                   }
        .                   mult.min(1 << 16)
   91,480 ( 0.00%)      }
        .           
        .               /// Whether `cost` still has an unpaid `color` pip after `p`'s current
        .               /// floating pool is applied. Drives the CR 702.51 convoke choice: a
        .               /// tapped creature contributes a colored pip only where the cost wants
        .               /// one, otherwise {1}.
        .               fn cost_still_needs_color(
        .                   &self,
        .                   p: usize,
-- line 2248 ----------------------------------------
-- line 2363 ----------------------------------------
        .                       })
        .                       .unwrap_or_default()
        .               }
        .           
        .               /// CR 605.1b — resolve `ExtraManaOnLandTap` triggered mana abilities
        .               /// after `land_id` (controlled by `p`) was tapped for mana. `resolved`
        .               /// is the tapping ability's event batch — `Mirror` reads the produced
        .               /// color off its `ManaAdded` events.
  237,848 ( 0.01%)      fn resolve_extra_mana_on_land_tap(
        .                   &mut self,
        .                   land_id: crate::card::CardId,
        .                   p: usize,
        .                   resolved: &[GameEvent],
        .                   events: &mut Vec<GameEvent>,
        .               ) {
        .                   use crate::effect::{ExtraManaKind, StaticEffect};
   18,296 ( 0.00%)          let Some(land) = self.battlefield.iter().find(|c| c.id == land_id) else { return };
   54,888 ( 0.00%)          if !land.definition.is_land() {
  238,418 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,296x)
        .                       return;
        .                   }
        .                   let land = land.clone();
   18,106 ( 0.00%)          let grants: Vec<(crate::card::CardId, ExtraManaKind)> = self
        .                       .battlefield
        .                       .iter()
        .                       .flat_map(|src| src.definition.static_abilities.iter().map(move |sa| (src, sa)))
   18,106 ( 0.00%)              .filter_map(|(src, sa)| {
        .                           let StaticEffect::ExtraManaOnLandTap { enchanted_only, filter, extra, while_monarch } =
        .                               &sa.effect
        .                           else {
        .                               return None;
        .                           };
        .                           if *enchanted_only && src.attached_to != Some(land_id) {
        .                               return None;
        .                           }
-- line 2396 ----------------------------------------
-- line 2397 ----------------------------------------
        .                           if *while_monarch && self.monarch != Some(src.controller) {
        .                               return None;
        .                           }
        .                           (crate::game::layers::requirement_matches_card(filter, &land, src.controller))
        .                               .then_some((src.id, *extra))
        .                       })
        .                       .collect();
        .                   // Bubbling Muck — the turn-scoped floating version of the same grant.
   72,424 ( 0.00%)          let mut grants = grants;
   18,106 ( 0.00%)          for (land_type, color) in self.extra_mana_on_land_tap_this_turn.clone() {
        .                       if land.definition.subtypes.land_types.contains(&land_type) {
        .                           grants.push((land_id, ExtraManaKind::Fixed(color)));
        .                       }
        .                   }
   90,530 ( 0.00%)          for (src_id, extra) in grants {
        .                       // Colorless-only mirror: fires only when the tap produced {C}.
        .                       if matches!(extra, ExtraManaKind::MirrorColorless) {
        .                           if resolved.iter().any(|e| matches!(e,
        .                               GameEvent::ColorlessManaAdded { player, .. } if *player == p))
        .                           {
        .                               self.players[p].mana_pool.add_colorless(1);
        .                               events.push(GameEvent::ColorlessManaAdded { player: p, source: Some(src_id) });
        .                           }
-- line 2419 ----------------------------------------
-- line 2474 ----------------------------------------
        .                                   GameEvent::ColorlessManaAdded { player, .. } if *player == p))
        .                               {
        .                                   self.players[p].mana_pool.add_colorless(1);
        .                                   events.push(GameEvent::ColorlessManaAdded { player: p, source: Some(src_id) });
        .                               }
        .                           }
        .                       }
        .                   }
  146,368 ( 0.00%)      }
        .           }
        .           
        .           impl GameState {
        .               /// The colours a permanent's mana abilities can produce (Mana Web's
        .               /// "could produce any type of mana that land could produce").
        .               pub(crate) fn colors_produced_by(&self, id: CardId) -> Vec<ManaColor> {
        .                   let Some(c) = self.battlefield_find(id) else { return Vec::new() };
        .                   ManaColor::ALL
-- line 2490 ----------------------------------------
-- line 2495 ----------------------------------------
        .                               .activated_abilities
        .                               .iter()
        .                               .any(|a| is_mana_ability(&a.effect) && effect_produces_color(&a.effect, *col))
        .                       })
        .                       .collect()
        .               }
        .           }
        .           
1,871,520 ( 0.06%)  fn effect_produces_color(effect: &Effect, color: ManaColor) -> bool {
1,169,700 ( 0.04%)      match effect {
2,339,400 ( 0.08%)          Effect::AddMana { pool, .. } => match pool {
        .                       ManaPayload::Colors(cs) => cs.contains(&color),
        .                       ManaPayload::AnyOneColor(_)
        .                       | ManaPayload::AnyColors(_)
        .                       | ManaPayload::AnyColorOpponentCouldProduce
        .                       | ManaPayload::AnyColorYouCouldProduce => true,
        .                       // Color set depends on live board state — not auto-tapped.
        .                       ManaPayload::AnyColorAmongLegendaries
        .                       | ManaPayload::AnyColorAmongYourPermanents
-- line 2513 ----------------------------------------
-- line 2539 ----------------------------------------
        .                       ManaPayload::ImprintedCardColor => false,
        .                   },
        .                   Effect::Seq(steps) => steps.iter().any(|s| effect_produces_color(s, color)),
        .                   Effect::If { then, else_, .. } => {
        .                       effect_produces_color(then, color) || effect_produces_color(else_, color)
        .                   }
        .                   _ => false,
        .               }
1,637,580 ( 0.05%)  }
        .           
        .           impl GameState {
        .               // ── Play land ─────────────────────────────────────────────────────────────
        .           
        .               /// Whether `p` holds an outstanding `may_play_until` permission on an
        .               /// exiled `card_id`. The cast path reads the permission off the card
        .               /// directly; land plays go through here so "you may play that card"
        .               /// grants (impulse exile) cover lands as well as spells.
-- line 2555 ----------------------------------------
-- line 2565 ----------------------------------------
        .                   self.play_land_with_face(card_id, /* back_face */ false)
        .               }
        .           
        .               /// Shared implementation for `PlayLand` and `PlayLandBack`. When
        .               /// `back_face` is true and the card has a `back_face`, the card's
        .               /// definition is swapped to the back face's definition before placing on
        .               /// the battlefield — so the resulting permanent has the back face's
        .               /// types, mana abilities, and ETB triggers.
   21,696 ( 0.00%)      pub(crate) fn play_land_with_face(
        .                   &mut self,
        .                   card_id: CardId,
        .                   back_face: bool,
        .               ) -> Result<Vec<GameEvent>, GameError> {
    3,616 ( 0.00%)          let p = self.priority.player_with_priority;
        .                   if !self.can_cast_sorcery_speed(p) {
      234 ( 0.00%)              return Err(GameError::SorcerySpeedOnly);
        .                   }
        .                   // CR 305.2 — honor ExtraLandPerTurn static grants (Exploration,
        .                   // Azusa, Wayward Swordtooth) when checking the per-turn cap.
    8,650 ( 0.00%)          if !self.can_player_play_land(p) {
  884,154 ( 0.03%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::can_player_play_land (1,730x)
        .                       return Err(GameError::AlreadyPlayedLand);
        .                   }
        .                   // City in a Bottle — a symmetric play-lock binds land plays too.
    8,650 ( 0.00%)          if let Some(c) = self.find_card_anywhere(card_id)
  496,056 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (1,730x)
        .                       && self.play_locked_for_all(p, c)
        .                   {
        .                       return Err(GameError::SpellNameLocked);
        .                   }
        .                   // Cornered Market — a nonbasic land sharing a nontoken permanent's
        .                   // name can't be played.
    6,920 ( 0.00%)          if !self.find_card_anywhere(card_id).is_some_and(|c| c.definition.is_basic())
  496,056 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (1,730x)
   22,490 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_basic (1,730x)
        .                       && self.name_locked_by_a_permanent(Some(card_id))
        .                   {
        .                       return Err(GameError::AlreadyPlayedLand);
        .                   }
        .                   // CR 401.6 — a PlayFromLibraryTop static (Courser of Kruphix,
        .                   // Oracle of Mul Daya) lets the land be played off the library top.
    1,730 ( 0.00%)          let from_top = !self.players[p].has_in_hand(card_id)
        .                       && self.library_top_playable(p, card_id);
    1,730 ( 0.00%)          let from_top_capped = from_top && self.library_top_cast_is_capped(p, card_id);
        .                   // CR 118.x — "you may play that card" grants cover lands too. An
        .                   // impulse-exiled land (Light Up the Stage, Gonti Night Minister,
        .                   // Chandra Torch of Defiance) is played from exile, not cast.
        .                   // CR 715.3d — a card whose permanent half is a land is *played* out of
        .                   // adventure exile, not cast (the FIN Town // Adventure cycle).
        .                   let from_adventure = !from_top
    1,730 ( 0.00%)              && !self.players[p].has_in_hand(card_id)
        .                       && self.exile.iter().any(|c| {
        .                           c.id == card_id && c.on_adventure && c.owner == p && c.definition.is_land()
        .                       });
    5,190 ( 0.00%)          let from_exile = !from_top
    1,730 ( 0.00%)              && !self.players[p].has_in_hand(card_id)
        .                       && (from_adventure || self.may_play_grant_for(p, card_id));
        .                   let mut card = if from_top {
        .                       self.players[p].library.remove(0)
        .                   } else if self.players[p].has_in_hand(card_id) {
    6,920 ( 0.00%)              self.players[p].remove_from_hand(card_id).unwrap()
5,095,928 ( 0.16%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,730x)
  675,849 ( 0.02%)  => /home/user/Crabomination/crabomination/src/player.rs:crabomination::player::PlayerData::remove_from_hand (1,730x)
        .                   } else if from_exile {
        .                       Self::take_card(&mut self.exile, card_id)
        .                           .ok_or(GameError::CardNotInHand(card_id))?
        .                   } else {
        .                       return Err(GameError::CardNotInHand(card_id));
        .                   };
    8,650 ( 0.00%)          let restore = |state: &mut Self, card: crate::card::CardInstance| {
        .                       if from_top {
        .                           state.players[p].library.insert(0, card);
        .                       } else if from_exile {
        .                           state.exile.push(card);
        .                       } else {
        .                           state.players[p].hand.push(card);
        .                       }
        .                   };
    3,460 ( 0.00%)          if back_face {
        .                       // Swap to the back face's definition. Reject if there isn't one.
        .                       let Some(back) = card.definition.back_face.clone() else {
        .                           restore(self, card);
        .                           return Err(GameError::NotALand(card_id));
        .                       };
        .                       // Keep the front installed until the play is accepted — a
        .                       // rejected play must restore the card unmodified.
        .                       let front = card.definition.clone();
        .                       card.definition = std::sync::Arc::new(*back);
        .                       if !card.definition.is_land() {
        .                           card.definition = front;
        .                           restore(self, card);
        .                           return Err(GameError::NotALand(card_id));
        .                       }
    5,190 ( 0.00%)          } else if !card.definition.is_land() {
   22,490 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (1,730x)
        .                       restore(self, card);
        .                       return Err(GameError::NotALand(card_id));
        .                   }
    3,460 ( 0.00%)          if from_exile {
        .                       // The permission is consumed by the play (CR 608.2 — a one-shot
        .                       // permission doesn't survive the card changing zones anyway).
        .                       // Applied only once the play is accepted, so a rejected play
        .                       // restores the card unmodified.
        .                       card.may_play_until = None;
        .                       card.granted_alt_cast_cost_eot = None;
        .                       card.face_down = false;
        .                       card.on_adventure = false;
        .                       card.adventuring = false;
        .                   }
    3,460 ( 0.00%)          if from_top_capped {
        .                       self.players[p].cast_from_library_top_this_turn = true;
        .                   }
    8,650 ( 0.00%)          self.place_land_card(p, card)
11,352,912 ( 0.36%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::place_land_card (1,730x)
   16,194 ( 0.00%)      }
        .           
        .               /// CR 305 — Play a land from the controller's graveyard, legal only while
        .               /// a `StaticEffect::MayPlayLandsFromGraveyard` permanent (Crucible of
        .               /// Worlds, Ramunap Excavator) is in play. Honors the same sorcery-speed
        .               /// and one-land-per-turn restrictions as a hand land play.
        .               /// CR 702.139 — pay {3} at sorcery speed to move a companion from the
        .               /// sideboard to its owner's hand (once per game; it leaves the
        .               /// sideboard for good).
-- line 2679 ----------------------------------------
-- line 2734 ----------------------------------------
        .                   self.entered_from_graveyard_this_turn.insert(card_id);
        .                   self.place_land_card(p, card)
        .               }
        .           
        .               /// Shared land-placement tail for `play_land_with_face` and
        .               /// `play_land_from_graveyard`: applies Damping Sphere mana downgrades,
        .               /// pushes the card to the battlefield, fires ETB triggers, and returns
        .               /// the land-played events.
   19,030 ( 0.00%)      fn place_land_card(
        .                   &mut self,
        .                   p: usize,
        .                   mut card: crate::card::CardInstance,
        .               ) -> Result<Vec<GameEvent>, GameError> {
    1,730 ( 0.00%)          let card_id = card.id;
        .                   // Damping Sphere: if any battlefield permanent grants
        .                   // `LandsTapColorlessOnly`, downgrade this land's mana abilities
        .                   // to `{T}: Add {C}` if the original would have produced more than
        .                   // one mana per tap. Applied in-place on the new instance's
        .                   // definition before the card lands on the battlefield, so all
        .                   // downstream activations see the replaced ability set.
    1,730 ( 0.00%)          if self.lands_tap_colorless_only_active()
        .                       && multi_mana_ability_count(&card.definition)
        .                   {
        .                       // Drop the printed mana abilities and replace them with a
        .                       // single `{T}: Add {C}`. Non-mana activated abilities
        .                       // (fetchland sac, manland animate, channel, cycling) survive
        .                       // — Damping Sphere's Oracle only affects mana production.
        .                       let mut kept: Vec<crate::card::ActivatedAbility> = card
        .                           .definition
-- line 2762 ----------------------------------------
-- line 2785 ----------------------------------------
        .                           exile_self_cost: false,
        .                           exile_other_filter: None,
        .                           self_counter_cost_reduction: None, sac_other_filter: None,
        .                           tap_other_filter: None, from_hand: false,
        .                           ..Default::default()
        .                       });
        .                       std::sync::Arc::make_mut(&mut card.definition).activated_abilities = kept;
        .                   }
    3,460 ( 0.00%)          self.players[p].lands_played_this_turn += 1;
   51,900 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,730x)
    3,460 ( 0.00%)          self.battlefield.push(card);
        .                   // Fire self-source ETB triggers for the land (shockland pay-or-tap,
        .                   // surveil-land tap-and-surveil, etc.). The cast path inlines the same
        .                   // logic in `resolve_top_of_stack`; play_land needs an analogous push
        .                   // so triggered abilities on lands actually fire.
    8,650 ( 0.00%)          self.fire_self_etb_triggers(card_id, p);
8,014,002 ( 0.26%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::fire_self_etb_triggers (1,730x)
        .                   // CR 614.1c — a printed "enters with N counters" land (the MMQ
        .                   // depletion cycle) gets them off the land drop too.
        .                   let mut counter_events = Vec::new();
        .                   self.apply_printed_etb_counters(card_id, &mut counter_events);
        .                   // CR 714.2b — a Saga land (Urza's Saga) enters with its first lore
        .                   // counter; chapter I fires off the land drop too.
    1,730 ( 0.00%)          if self
        .                       .battlefield
        .                       .iter()
   22,348 ( 0.00%)              .any(|c| c.id == card_id && !c.definition.saga_chapters.is_empty())
        .                   {
        .                       self.saga_enter_advance(card_id);
        .                   }
        .                   let mut out =
    5,190 ( 0.00%)              vec![GameEvent::LandPlayed { player: p, card_id, played: true }];
        .                   out.append(&mut counter_events);
    3,460 ( 0.00%)          out.push(GameEvent::PermanentEntered { card_id });
    6,920 ( 0.00%)          Ok(out)
   13,840 ( 0.00%)      }
        .           
        .               /// Push the source-itself ETB triggered abilities for a permanent that
        .               /// has just entered the battlefield. Used by `play_land` and by Move →
        .               /// Battlefield zone changes so triggered abilities fire consistently
        .               /// regardless of how the permanent arrived.
        .               /// CR 614.13 — apply "[permanents] enter the battlefield tapped"
        .               /// replacement effects from *other* permanents to a permanent that has
        .               /// just entered. Powers Authority of the Consuls / Imposing Sovereign /
-- line 2826 ----------------------------------------
-- line 2831 ----------------------------------------
        .               /// permanent entered" hook, so every enter path (cast, token, reanimate,
        .               /// land drop) is covered.
        .               /// CR 614 — a permanent printed `enters_under_opponent_control` enters
        .               /// under an opponent of its controller's choice. Applied at the entry hook,
        .               /// before any ETB trigger reads a controller. With one opponent alive the
        .               /// choice is forced; with several the controller picks the first alive
        .               /// opponent in seat order (a bot-policy default, like the other
        .               /// auto-resolved entry choices).
   35,136 ( 0.00%)      pub(crate) fn apply_enters_under_opponent_control(&mut self, card_id: CardId) {
        .                   let Some(c) = self.battlefield_find(card_id) else { return };
        .                   // The guard keeps the replacement one-shot: every entry path funnels
        .                   // through here, and once applied the controller is no longer the owner.
    7,808 ( 0.00%)          if !c.definition.enters_under_opponent_control || c.controller != c.owner {
        .                       return;
        .                   }
        .                   let owner = c.controller;
        .                   let Some(victim) = (0..self.players.len())
        .                       .find(|&p| p != owner && self.players[p].is_alive() && !self.same_team(p, owner))
        .                   else {
        .                       return;
        .                   };
        .                   if let Some(c) = self.battlefield_find_mut(card_id) {
        .                       c.controller = victim;
        .                   }
   31,232 ( 0.00%)      }
        .           
   27,328 ( 0.00%)      pub(crate) fn apply_enters_tapped_replacement(&mut self, card_id: CardId) {
        .                   use crate::effect::StaticEffect;
        .                   use crate::game::layers::{AffectedPermanents, affected_includes};
   15,616 ( 0.00%)          let Some(idx) = self.battlefield.iter().position(|c| c.id == card_id) else {
        .                       return;
        .                   };
        .                   // CR 614 — "permanents enter tapped this turn" (Due Respect).
    3,904 ( 0.00%)          let mut should_tap = self.permanents_enter_tapped_this_turn;
        .                   // A permanent's own `EntersTapped { applies_to: This/Source }` (e.g.
        .                   // Overlord of the Hauntwoods' "Everywhere" land token) taps itself —
        .                   // the cross-permanent loop below skips the entering card, so handle the
        .                   // self case up front. `EntersTappedUnless` (Horned Loch-Whale) taps
        .                   // itself unless its predicate holds, evaluated with the entrant as
        .                   // source/controller.
        .                   let self_seat = self.battlefield[idx].controller;
        .                   let self_id = self.battlefield[idx].id;
-- line 2872 ----------------------------------------
-- line 2891 ----------------------------------------
        .                               if !self.evaluate_predicate(condition, &ctx) {
        .                                   should_tap = true;
        .                                   break;
        .                               }
        .                           }
        .                           _ => {}
        .                       }
        .                   }
   27,328 ( 0.00%)          for src in &self.battlefield {
   57,836 ( 0.00%)              if src.id == card_id {
        .                           continue;
        .                       }
  107,864 ( 0.00%)              if should_tap {
        .                           break;
        .                       }
        .                       for sa in &src.definition.static_abilities {
        .                           let StaticEffect::EntersTapped { applies_to } = &sa.effect else {
        .                               continue;
        .                           };
        .                           let Some(mut affected) = super::selector_to_affected(applies_to, src) else {
        .                               continue;
-- line 2911 ----------------------------------------
-- line 2925 ----------------------------------------
        .                           }
        .                       }
        .                       if should_tap {
        .                           break;
        .                       }
        .                   }
        .                   // CR 614 — an "enters untapped" replacement (Spelunking) overrides the
        .                   // enters-tapped effects for lands the static-source's controller owns.
    7,808 ( 0.00%)          if should_tap && self.battlefield[idx].definition.is_land() {
        .                       let entrant_controller = self.battlefield[idx].controller;
        .                       let overridden = self.battlefield.iter().any(|src| {
        .                           src.controller == entrant_controller
        .                               && src
        .                                   .definition
        .                                   .static_abilities
        .                                   .iter()
        .                                   .any(|sa| matches!(sa.effect, StaticEffect::LandsEnterUntapped))
-- line 2941 ----------------------------------------
-- line 2942 ----------------------------------------
        .                       });
        .                       if overridden {
        .                           should_tap = false;
        .                       }
        .                   }
        .                   if should_tap {
        .                       self.battlefield[idx].tapped = true;
        .                   }
   31,232 ( 0.00%)      }
        .           
        .               /// CR 616.1c / 616.1g — the "enters as a copy" replacement is applied
        .               /// *before* the enters-tapped one, so the entrant enters tapped only if its
        .               /// **copied** characteristics say so (Essence of the Wild's copy of Rusted
        .               /// Sentinel loses the printed enters-tapped ability and enters untapped).
        .               /// The engine applies enters-tapped first, so re-decide once a copy lands.
        .               pub(crate) fn reapply_enters_tapped_after_copy(&mut self, card_id: CardId) {
        .                   if let Some(c) = self.battlefield_find_mut(card_id) {
-- line 2958 ----------------------------------------
-- line 3009 ----------------------------------------
        .                       ctx.kick_count = c.kick_count;
        .                       ctx.bargained = c.bargained;
        .                       ctx.cast_from_hand = c.cast_from_hand;
        .                       ctx.mana_spent_by_color = c.cast_mana_spent_by_color.clone();
        .                   }
        .                   ctx
        .               }
        .           
   24,220 ( 0.00%)      pub fn fire_self_etb_triggers(&mut self, card_id: CardId, controller: usize) {
        .                   // CR 614 — "enters under the control of an opponent of your choice"
        .                   // (Captive Audience). A control-setting entry replacement, so it lands
        .                   // before enters-tapped and before any ETB trigger sees a controller.
    1,730 ( 0.00%)          self.apply_enters_under_opponent_control(card_id);
  179,068 ( 0.01%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_under_opponent_control (1,730x)
        .                   // CR 614.13 — apply enters-tapped replacements before ETB triggers fire.
    5,190 ( 0.00%)          self.apply_enters_tapped_replacement(card_id);
  557,396 ( 0.02%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement (1,730x)
        .                   // CR 702.179 — a "Start your engines!" permanent entering gives its
        .                   // controller speed 1 if they have none.
    5,190 ( 0.00%)          if controller < self.players.len()
    5,190 ( 0.00%)              && self.players[controller].speed == 0
    1,730 ( 0.00%)              && self
        .                           .battlefield
        .                           .iter()
   22,348 ( 0.00%)                  .any(|c| c.id == card_id && c.definition.keywords.contains(&crate::card::Keyword::StartYourEngines))
        .                   {
        .                       self.players[controller].speed = 1;
        .                   }
        .                   use crate::effect::{EventKind, EventScope};
        .                   #[allow(clippy::type_complexity)]
    1,730 ( 0.00%)          let etb_triggers: Vec<(Effect, Option<crate::effect::Predicate>)> = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == card_id)
        .                       .map(|c| {
        .                           // Printed + statics-granted ETBs ("Slivers you control have
        .                           // 'When this enters…'" — Lavabelly) fire alike.
    5,190 ( 0.00%)                  let static_granted = self.statics_granted_triggers_for(c);
  723,618 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_for (1,730x)
        .                           c.definition
        .                               .triggered_abilities
        .                               .iter()
        .                               .chain(static_granted.iter())
        .                               .filter(|t| t.event.kind == EventKind::EntersBattlefield
        .                                   && matches!(t.event.scope, EventScope::SelfSource))
        .                               .map(|t| (t.effect.clone(), t.event.filter.clone()))
        .                               .collect()
        .                       })
        .                       .unwrap_or_default();
        .                   // Elesh Norn replacement: zero or more copies depending on which
        .                   // side controls a Mother of Machines. Katara, the Fearless adds an
        .                   // extra fire for a self-source Ally ETB trigger (unless suppressed).
   12,110 ( 0.00%)          let etb_mult = etb_trigger_multiplier(self, controller, Some(card_id));
  860,456 ( 0.03%)  => crabomination/src/game/actions.rs:crabomination::game::actions::etb_trigger_multiplier (1,730x)
    3,460 ( 0.00%)          let multiplier = if etb_mult == 0 {
        .                       0
        .                   } else {
   12,110 ( 0.00%)              etb_mult + ally_trigger_extra_fires(self, controller, card_id)
4,891,854 ( 0.16%)  => crabomination/src/game/actions.rs:crabomination::game::actions::ally_trigger_extra_fires (1,730x)
        .                   };
        .                   // CR 601.2b — an ETB triggered ability reads the cast's X (stamped on
        .                   // the permanent at resolution) so filters like `ManaValueAtMostXFromCost`
        .                   // evaluate against the real X rather than 0 (Dune Drifter).
    1,730 ( 0.00%)          let cast_x = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == card_id)
    5,190 ( 0.00%)              .map(|c| c.cast_x_value)
        .                       .unwrap_or(0);
   12,110 ( 0.00%)          for (effect, filter) in etb_triggers {
        .                       // CR 603.4 — the trigger's own condition ("When this enters, if it
        .                       // was kicked / if delirium …") is read at fire time against the
        .                       // entering permanent's cast flags.
        .                       if let Some(predicate) = filter {
        .                           let ctx = self.etb_filter_context(card_id, controller);
        .                           if !self.evaluate_predicate(&predicate, &ctx) {
        .                               continue;
        .                           }
-- line 3081 ----------------------------------------
-- line 3122 ----------------------------------------
        .                                       Some(GameEvent::BecameTarget { target: *id, caster: controller, by: Some(card_id) })
        .                                   }
        .                                   _ => None,
        .                               }
        .                           }));
        .                           self.dispatch_triggers_for_events(&became);
        .                       }
        .                   }
   13,840 ( 0.00%)      }
        .           
        .               // ── Cast spell ────────────────────────────────────────────────────────────
        .           
        .               /// Cast a modal-double-faced card via its back face. Mirrors
        .               /// `play_land_with_face` but for non-land back faces (creature /
        .               /// instant / sorcery). The card's `definition` is swapped to the back
        .               /// face's definition before payment + cast, so cost / type / effect
        .               /// all resolve against the back face. Errors with `NotALand` (reused
-- line 3138 ----------------------------------------
-- line 3430 ----------------------------------------
        .                   // — unmaterialize it.
        .                   self.pending_prepare_copies.remove(idx);
        .                   for player in &mut self.players {
        .                       player.hand.retain(|c| c.id != copy_id);
        .                   }
        .                   result
        .               }
        .           
   89,472 ( 0.00%)      pub fn cast_spell(
        .                   &mut self,
        .                   card_id: CardId,
        .                   target: Option<Target>,
        .                   additional_targets: Vec<Target>,
        .                   mode: Option<usize>,
        .                   x_value: Option<u32>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
        .                   // CR 601.3e — a card with no mana cost can't be cast by paying it
        .                   // (suspend / free-cast paths bypass this method).
        .                   // CR 300.2a — a card that's a land and another type (artifact lands,
        .                   // Dryad Arbor) can only be *played*, never cast.
        .                   {
    7,456 ( 0.00%)              let p = self.priority.player_with_priority;
   14,912 ( 0.00%)              if let Some(c) = self.players[p].hand.iter().find(|c| c.id == card_id) {
   29,824 ( 0.00%)                  if c.definition.no_mana_cost {
        .                               return Err(GameError::NoManaCost);
        .                           }
   22,368 ( 0.00%)                  if c.definition.is_land() {
  119,296 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (7,456x)
        .                               return Err(GameError::CannotCastLand);
        .                           }
        .                       }
        .                   }
        .                   // {X} cast costs: a hand-paying caster who didn't send an X picks
        .                   // one via a `ChooseAmount` modal (suspend + clean replay — nothing
        .                   // has been paid yet). Without this the client's `x_value: None`
        .                   // resolved every human-cast X spell at X=0 (a 0/0 Fractal Summoning
-- line 3464 ----------------------------------------
-- line 3471 ----------------------------------------
        .                   // outright: the suspend returns `Ok`, so `would_accept` reported an
        .                   // unaffordable X spell as castable; the bot committed it, answered
        .                   // the modal, and the replayed cast then failed on mana or on timing.
        .                   // `perform_action`'s rollback restored the pending decision along
        .                   // with everything else, so the bot answered the same decision the
        .                   // same way forever. That livelocked ~14 % of cube games, and they
        .                   // were dropped from every ladder and recommender measurement.
        .                   {
   14,912 ( 0.00%)              let p = self.priority.player_with_priority;
    7,456 ( 0.00%)              if x_value.is_none()
   22,368 ( 0.00%)                  && self.players[p].manual_mana
        .                           && let Some(card) = self.find_card_anywhere(card_id)
        .                           && (card.definition.cost.has_x() || card.definition.additional_cost_pay_x_life)
        .                       {
        .                           // A "pay X life" additional cost with no {X} mana pip is
        .                           // bounded by the caster's life total (CR 119.4), not mana.
        .                           let max = if card.definition.additional_cost_pay_x_life
        .                               && !card.definition.cost.has_x()
        .                           {
-- line 3489 ----------------------------------------
-- line 3512 ----------------------------------------
        .                           return Ok(vec![]);
        .                       }
        .                   }
        .                   // Slot-0 targets that live in an off-board zone ("return target
        .                   // nonland card from your graveyard …"): the client's targeting
        .                   // cursor can't select graveyard/exile cards, so it submits the cast
        .                   // with no target — gather the pick here via a `ChooseCards` modal.
        .                   {
    7,456 ( 0.00%)              let p = self.priority.player_with_priority;
   21,652 ( 0.00%)              let slot0 = if target.is_none() && self.players[p].wants_ui {
   14,196 ( 0.00%)                  self.find_card_anywhere(card_id).and_then(|card| {
1,331,416 ( 0.04%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (4,732x)
    9,464 ( 0.00%)                      card.definition
        .                                   .effect
   18,928 ( 0.00%)                          .target_filter_for_slot_in_mode(0, mode)
  198,996 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode (4,732x)
        .                                   .filter(|f| f.mentions_offboard_zone())
        .                                   .map(|f| {
        .                                       (
        .                                           f.resolve_x(x_value.unwrap_or(0)),
        .                                           card.definition.name.to_string(),
        .                                       )
        .                                   })
        .                           })
-- line 3533 ----------------------------------------
-- line 3580 ----------------------------------------
        .                   // target creature an opponent controls"): the client's cast flow only
        .                   // collects slot 0, so slots 1+ arrive empty and the extra half of the
        .                   // effect silently no-ops. Bind the next missing slot here — a
        .                   // `wants_ui` caster picks via a `ChooseTarget` cursor decision
        .                   // (suspend + clean replay; nothing has been paid yet), everyone else
        .                   // auto-fills. No legal candidate leaves the slot empty, which is the
        .                   // printed "up to one" behavior.
        .                   {
   14,912 ( 0.00%)              let p = self.priority.player_with_priority;
        .                       // A `DeclineTarget` answer replays the cast with this card id
        .                       // stamped in the suppress scratch — consume it and skip the
        .                       // prompt so declining ends target selection.
    7,456 ( 0.00%)              let suppressed = if self.suppress_extra_target_prompts == Some(card_id) {
        .                           self.suppress_extra_target_prompts = None;
        .                           true
        .                       } else {
        .                           false
        .                       };
   15,628 ( 0.00%)              let slot_info = if !suppressed && target.is_some() && self.players[p].wants_ui {
    8,172 ( 0.00%)                  self.find_card_anywhere(card_id).and_then(|card| {
  834,958 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (2,724x)
        .                               // "Extra target only on your main phase" spells (Return
        .                               // to Dust) genuinely cast single-target off-main — don't
        .                               // prompt for the slot the rules forbid.
    5,448 ( 0.00%)                      if card.definition.extra_targets_main_phase_only
        .                                   && !(self.active_player_idx == p && self.step.is_main_phase())
        .                               {
        .                                   return None;
        .                               }
    2,724 ( 0.00%)                      let slot = 1 + additional_targets.len() as u8;
   10,896 ( 0.00%)                      card.definition
        .                                   .effect
   13,620 ( 0.00%)                          .target_filter_for_slot_in_mode(slot, mode)
  253,556 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode (2,724x)
        .                                   .map(|f| {
        .                                       (
        .                                           f.resolve_x(x_value.unwrap_or(0)),
        .                                           card.definition.name.to_string(),
        .                                           // "Up to N targets" slots past the printed
        .                                           // minimum may be declined.
        .                                           card.definition.effect.target_slot_optional_x(
        .                                               slot,
-- line 3619 ----------------------------------------
-- line 3669 ----------------------------------------
        .                               return Ok(vec![]);
        .                           }
        .                       }
        .                   }
        .                   // Muldrotha — cast a permanent spell of each permanent type from
        .                   // your graveyard during each of your turns. Hop the card into hand
        .                   // for the normal cast pipeline; restore on failure, record the
        .                   // consumed permanent type on success.
    7,456 ( 0.00%)          let p = self.priority.player_with_priority;
   14,912 ( 0.00%)          if !self.players[p].hand.iter().any(|c| c.id == card_id)
        .                       && !self.players[p].graveyard.iter().any(|c| {
        .                           c.id == card_id
        .                               && self.cast_from_zone_blocked(p, &c.definition, crate::card::Zone::Graveyard)
        .                       })
        .                       && let Some(used_type) = self.graveyard_cast_type_available(p, card_id)
        .                   {
        .                       let card = Self::take_card(&mut self.players[p].graveyard, card_id)
        .                           .ok_or(GameError::CardNotInHand(card_id))?;
-- line 3686 ----------------------------------------
-- line 3701 ----------------------------------------
        .                               self.entered_from_graveyard_this_turn.insert(card_id);
        .                           }
        .                       }
        .                       return r;
        .                   }
        .                   // Noctis — cast a covered spell from your graveyard by paying the
        .                   // static's life surcharge; it enters with a finality counter. Hop the
        .                   // card into hand for the normal cast pipeline; restore on failure.
   14,912 ( 0.00%)          if !self.players[p].hand.iter().any(|c| c.id == card_id)
        .                       && !self.players[p].graveyard.iter().any(|c| {
        .                           c.id == card_id
        .                               && self.cast_from_zone_blocked(p, &c.definition, crate::card::Zone::Graveyard)
        .                       })
        .                       && let Some(life) = self.graveyard_cast_life_surcharge(p, card_id)
        .                   {
        .                       let mut card = Self::take_card(&mut self.players[p].graveyard, card_id)
        .                           .ok_or(GameError::CardNotInHand(card_id))?;
-- line 3717 ----------------------------------------
-- line 3732 ----------------------------------------
        .                               self.entered_from_graveyard_this_turn.insert(card_id);
        .                           }
        .                       }
        .                       return r;
        .                   }
        .                   // Osteomancer Adept — cast a creature spell from your graveyard by
        .                   // foraging in addition to its other costs; it enters with a finality
        .                   // counter. Same hop-into-hand shape as the Noctis branch above.
   14,912 ( 0.00%)          if !self.players[p].hand.iter().any(|c| c.id == card_id)
        .                       && self.players[p].forage_graveyard_casts_turn == Some(self.turn_number)
        .                       && self.can_forage(p)
        .                       && self.players[p].graveyard.iter().any(|c| {
        .                           c.id == card_id
        .                               && c.definition.is_creature()
        .                               && !self.cast_from_zone_blocked(p, &c.definition, crate::card::Zone::Graveyard)
        .                       })
        .                   {
-- line 3748 ----------------------------------------
-- line 3766 ----------------------------------------
        .                               self.entered_from_graveyard_this_turn.insert(card_id);
        .                               return Ok(evs);
        .                           }
        .                       }
        .                   }
        .                   // Bolas's Citadel — cast a spell off the library top paying life equal
        .                   // to its mana value instead of its mana cost. Hop to hand, pay life,
        .                   // and free-cast; restore the card to the top on failure.
   14,912 ( 0.00%)          if !self.players[p].hand.iter().any(|c| c.id == card_id)
        .                       && !self.players[p].library.first().is_some_and(|c| {
        .                           self.cast_from_zone_blocked(p, &c.definition, crate::card::Zone::Library)
        .                       })
        .                       && let Some(life) = self.library_top_pay_life_cost(p, card_id)
        .                   {
        .                       if self.players[p].life < life as i32 {
        .                           return Err(GameError::InsufficientLife);
        .                       }
-- line 3782 ----------------------------------------
-- line 3796 ----------------------------------------
        .                               self.pay_life_cost(p, life);
        .                               return Ok(events);
        .                           }
        .                       }
        .                   }
        .                   // CR 401.6 — cast off the library top when a PlayFromLibraryTop
        .                   // static covers the card (Mystic Forge). Hop the card into hand for
        .                   // the normal cast pipeline; restore it to the top on failure.
   14,912 ( 0.00%)          if !self.players[p].hand.iter().any(|c| c.id == card_id)
        .                       && !self.players[p].library.first().is_some_and(|c| {
        .                           self.cast_from_zone_blocked(p, &c.definition, crate::card::Zone::Library)
        .                       })
        .                       && self.library_top_playable(p, card_id)
        .                   {
        .                       let capped = self.library_top_cast_is_capped(p, card_id);
        .                       let card = self.players[p].library.remove(0);
        .                       self.players[p].hand.push(card);
-- line 3812 ----------------------------------------
-- line 3821 ----------------------------------------
        .                           if let Some(card) = Self::take_card(&mut self.players[p].hand, card_id) {
        .                               self.players[p].library.insert(0, card);
        .                           }
        .                       } else if capped {
        .                           self.players[p].cast_from_library_top_this_turn = true;
        .                       }
        .                       return r;
        .                   }
  141,664 ( 0.00%)          self.cast_spell_with_convoke(card_id, target, additional_targets, mode, x_value, &[], &[], CastFlags::default())
764,297,493 (24.52%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke (7,456x)
   59,648 ( 0.00%)      }
        .           
        .               /// When `card_id` sits in `p`'s graveyard on `p`'s turn and a
        .               /// graveyard-cast permission is active, say whether it can be cast:
        .               /// `Some(None)` for an unlimited permission (Hades, Sorcerer of Eld —
        .               /// nothing is consumed), `Some(Some(type))` for Muldrotha's one-per-
        .               /// permanent-type budget, `None` when no permission covers it.
        .               pub(crate) fn graveyard_cast_type_available(
        .                   &self,
-- line 3838 ----------------------------------------
-- line 5856 ----------------------------------------
        .                       helpers,
        .                       &[],
        .                       CastFlags { waterbend: Some(amt), ..Default::default() },
        .                   )
        .               }
        .           
        .               /// CR 702.51 — true when a `StaticEffect::GrantConvokeToSpells` permanent
        .               /// `p` controls (Chief Engineer) grants convoke to this spell.
   52,192 ( 0.00%)      pub(crate) fn spell_granted_convoke(&self, p: usize, card: &CardInstance) -> bool {
        .                   self.battlefield.iter().any(|c| {
  253,124 ( 0.01%)              c.controller == p
        .                           && c.definition.static_abilities.iter().any(|sa| match &sa.effect {
        .                               crate::effect::StaticEffect::GrantConvokeToSpells { filter } => {
        .                                   crate::game::layers::requirement_matches_card(filter, card, p)
        .                               }
        .                               _ => false,
        .                           })
        .                   })
   59,648 ( 0.00%)      }
        .           
        .               /// Internal cast-spell helper with optional convoke creatures and delve
        .               /// cards. Each convoke creature must be untapped + controlled by the
        .               /// caster + the spell must have `Keyword::Convoke`; each tap adds {1}
        .               /// generic mana to the player's pool. Each delve card must be in the
        .               /// caster's graveyard + the spell must have `Keyword::Delve`; each one
        .               /// exiled reduces the generic cost by {1} (CR 702.66).
        .               #[allow(clippy::too_many_arguments)]
  156,576 ( 0.01%)      pub(crate) fn cast_spell_with_convoke(
        .                   &mut self,
        .                   card_id: CardId,
        .                   target: Option<Target>,
        .                   additional_targets: Vec<Target>,
        .                   mode: Option<usize>,
        .                   x_value: Option<u32>,
        .                   convoke_creatures: &[CardId],
        .                   delve_cards: &[CardId],
        .                   flags: CastFlags,
        .               ) -> Result<Vec<GameEvent>, GameError> {
  119,296 ( 0.00%)          let CastFlags { kicked, buyback, bestow, entwine, room_door, gift, mut waterbend } = flags;
   14,912 ( 0.00%)          let p = self.priority.player_with_priority;
        .           
    7,456 ( 0.00%)          if !self.players[p].has_in_hand(card_id) {
        .                       return Err(GameError::CardNotInHand(card_id));
        .                   }
        .           
        .                   // Meddling Mage — spells with the chosen name can't be cast.
        .                   let spell_name = self.players[p]
        .                       .hand
        .                       .iter()
        .                       .find(|c| c.id == card_id)
   14,912 ( 0.00%)              .map(|c| c.definition.name);
        .                   if let Some(name) = spell_name
    7,456 ( 0.00%)              && self.battlefield.iter().any(|c| {
        .                           c.named_card.as_deref() == Some(name)
        .                               && c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(sa.effect, crate::effect::StaticEffect::NamedSpellCantBeCast)
        .                               })
        .                       })
        .                   {
        .                       return Err(GameError::SpellNameLocked);
        .                   }
-- line 5916 ----------------------------------------
-- line 5948 ----------------------------------------
        .                   {
        .                       return Err(GameError::SpellNameLocked);
        .                   }
        .                   // Academic Probation mode 0 — an opponent of the caster named this
        .                   // spell ("Opponents can't cast spells with the chosen name until your
        .                   // next turn"); the lock lives on the naming player until their turn.
        .                   if let Some(name) = spell_name
        .                       && self.players.iter().enumerate().any(|(i, pl)| {
   89,472 ( 0.00%)                  !self.same_team(i, p) && pl.opponents_cant_cast_named.iter().any(|n| n == name)
  290,784 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (14,912x)
        .                       })
        .                   {
        .                       return Err(GameError::SpellNameLocked);
        .                   }
        .           
        .                   // CR 601.2b — interactive additional-cost choices for a hand-paying
        .                   // caster on the plain cast path: which permanent to sacrifice
        .                   // ("sacrifice a …" — Crop Rotation, Reckless Abandon) or which card to
-- line 5964 ----------------------------------------
-- line 5972 ----------------------------------------
        .                   // delve paths are excluded so their own choice plumbing isn't disturbed.
        .                   //
        .                   // [`manual_mana`], not `wants_ui`: an additional cost is a cost, and
        .                   // prompting a bot for one livelocks the game (see the {X} prompt in
        .                   // `cast_spell` for the full chain). This was the residual 1-in-96
        .                   // deadlock left after the {X} and ability-cost gates were fixed —
        .                   // Bonfire of the Damned and friends, whose discard cost suspended a
        .                   // cast the bot could not actually pay for.
  119,296 ( 0.00%)          if self.players[p].manual_mana
        .                       && convoke_creatures.is_empty()
        .                       && delve_cards.is_empty()
        .                       && !buyback
        .                       && !bestow
        .                       && !entwine
        .                       && room_door.is_none()
        .                   {
        .                       let card_info = self.players[p].hand.iter().find(|c| c.id == card_id).map(|c| {
-- line 5988 ----------------------------------------
-- line 6125 ----------------------------------------
        .                   // (timing, mana) can't leak them onto the next cast.
        .                   let chosen_sacrifices = self.pending_cast_sacrifices.take();
        .                   let chosen_discards = self.pending_cast_discards.take();
        .                   // CR 601.2g float-spend confirmation answer (None until the player
        .                   // has answered the prompt). Taken up front so a later failure can't
        .                   // leak it onto the next cast.
        .                   let spend_float_choice = self.pending_cast_spend_float.take();
        .           
    7,456 ( 0.00%)          let mut card = self.players[p].remove_from_hand(card_id).unwrap();
31,095,273 ( 1.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,456x)
    7,456 ( 0.00%)          card.cast_from_hand = true;
    7,456 ( 0.00%)          card.cast_from_exile = false;
   22,368 ( 0.00%)          card.cast_from_library = self.casting_from_library_top == Some(card_id);
        .                   // CR 701.67 — a mandatory "waterbend {N}" rider is paid even on the
        .                   // plain cast path; only the optional "you may waterbend" form is
        .                   // skippable. When no explicit amount arrived via CastSpellWaterbend,
        .                   // derive it for mandatory cards (helpers stay empty — paid from mana).
    7,456 ( 0.00%)          if waterbend.is_none()
   44,736 ( 0.00%)              && let Some((amt, false)) = waterbend_amount(&card.definition, x_value)
        .                   {
        .                       waterbend = Some(amt);
        .                   }
        .                   // CR 702.32 — opt-in Kicker. Only stamp `kicked` if the card
        .                   // actually has a kicker cost (mana or action); a mana kicker is
        .                   // folded into the spell's mana cost below, an action kicker joins
        .                   // the additional-cast-cost payment.
        .                   // CR 702.32b — an "and/or" kicker cast carries its chosen option
        .                   // indices instead of a single kicker cost.
        .                   let kicker_options: Vec<u8> = std::mem::take(&mut self.cast_kicker_options)
        .                       .into_iter()
        .                       .filter(|i| (*i as usize) < card.definition.kicker_options.len())
        .                       .collect();
   14,912 ( 0.00%)          let kicked = kicked
        .                       && (card.definition.has_kicker().is_some()
        .                           || card.definition.kicker_action_cost.is_some()
        .                           || !kicker_options.is_empty());
    7,456 ( 0.00%)          card.kicked = kicked;
   22,368 ( 0.00%)          card.kicked_options = kicker_options;
        .                   // CR 702.27 — opt-in Buyback; folded into the cost below and read
        .                   // at resolution to return the spell to hand instead of the gy.
   14,912 ( 0.00%)          let buyback = buyback && card.definition.has_buyback().is_some();
    7,456 ( 0.00%)          card.bought_back = buyback;
        .                   // CR 702.172 — Spree: stamp the chosen modes so `Effect::Spree` runs
        .                   // exactly those at resolution. Their mana costs fold into the total
        .                   // cost below.
        .                   let spree_modes = self.pending_spree_modes.take().unwrap_or_default();
   37,280 ( 0.00%)          card.spree_modes = spree_modes.clone();
        .                   // CR 702.41 — opt-in Entwine; only sticks when the card has it (either
        .                   // a mana cost or a non-mana one — "Entwine—Sacrifice two lands").
   14,912 ( 0.00%)          let entwine = entwine
        .                       && (card.definition.has_entwine().is_some()
        .                           || card.definition.entwine_additional_cost.is_some());
    7,456 ( 0.00%)          card.entwined = entwine;
        .                   // CR 702.165 — opt-in Gift; only sticks when the card has it. The
        .                   // promised gift carries no mana cost, so nothing folds into the cost.
   22,368 ( 0.00%)          card.gift_promised = gift && card.definition.gift.is_some();
        .                   // CR 709.5 — a Room cast remembers which door was cast (reusing the
        .                   // split-cast slot); resolution unlocks that door.
        .                   let room_door = room_door.filter(|_| card.definition.room.is_some());
        .                   if let Some(d) = room_door {
        .                       card.split_cast = Some(d);
        .                   }
        .                   // CR 702.103 — Bestow: cast as an Aura targeting a creature. The
        .                   // bestow cost replaces the regular cost (below); `bestowed` flags
        .                   // the resolving permanent as an Aura that attaches to its target.
   22,368 ( 0.00%)          let bestow = bestow && card.definition.has_bestow().is_some();
        .                   if bestow {
        .                       // Bestow requires a creature target up front (the spell's own
        .                       // effect doesn't carry one), so an unbestowable cast reverts.
        .                       let creature_target = matches!(
        .                           target,
        .                           Some(Target::Permanent(tid))
        .                               if self.battlefield.iter().any(|c| c.id == tid && c.definition.is_creature())
        .                       );
-- line 6197 ----------------------------------------
-- line 6199 ----------------------------------------
        .                           self.players[p].hand.push(card);
        .                           return Err(GameError::SelectionRequirementViolated);
        .                       }
        .                       card.bestowed = true;
        .                   }
        .           
        .                   // Ranger-Captain of Eos lock — this player can't cast noncreature
        .                   // spells for the rest of the turn.
   22,368 ( 0.00%)          if self.players[p].cant_cast_noncreature_this_turn
        .                       && !card.definition.is_creature()
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Cease-Fire — a turn-scoped, filtered cast lock.
   22,368 ( 0.00%)          if !self.players[p].cant_cast_matching_this_turn.is_empty() {
        .                       let locks = self.players[p].cant_cast_matching_this_turn.clone();
        .                       if locks.iter().any(|f| self.evaluate_requirement_on_card(f, &card, p)) {
        .                           self.players[p].hand.push(card);
        .                           return Err(GameError::CantCastNoncreature);
        .                       }
        .                   }
        .           
        .                   // City in a Bottle — a symmetric play-lock binds every seat.
-- line 6223 ----------------------------------------
-- line 6228 ----------------------------------------
        .           
        .                   // Llawan — an opponent's static locks a whole class of spells.
        .                   if self.opponent_locks_cast_of(p, &card) {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Codie lock — this player can't cast permanent spells.
   34,418 ( 0.00%)          if card.definition.is_permanent() && self.player_cant_cast_permanent_spells(p) {
  105,514 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_permanent (7,456x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastPermanentSpells);
        .                   }
        .           
        .                   // Gaddock Teeg lock — high-mana-value / {X} noncreature spells can't be cast.
   22,368 ( 0.00%)          if self.noncreature_spell_cast_locked(&card.definition) {
  951,978 ( 0.03%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::noncreature_spell_cast_locked (7,456x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Nikya of the Old Ways — its controller can't cast noncreature spells.
   38,968 ( 0.00%)          if !card.definition.is_creature() && self.player_cant_cast_noncreature_spells(p) {
  671,946 ( 0.02%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_noncreature_spells (3,320x)
  106,888 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,456x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Hymn of the Wilds — its controller can't cast instants or sorceries.
   37,938 ( 0.00%)          if (card.definition.is_instant() || card.definition.is_sorcery())
  112,498 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_instant (7,456x)
   81,252 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_sorcery (5,190x)
   14,310 ( 0.00%)              && self.player_cant_cast_instants_or_sorceries(p)
  576,934 ( 0.02%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_instants_or_sorceries (2,862x)
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Hand to Hand — nobody casts instants during combat.
   29,166 ( 0.00%)          if card.definition.is_instant() && self.combat_spell_lock_active() {
  112,498 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_instant (7,456x)
  138,394 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::combat_spell_lock_active (2,266x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Grid Monitor — its controller can't cast creature spells.
   59,592 ( 0.00%)          if card.definition.is_creature() && self.player_cant_cast_creature_spells(p) {
  655,944 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_cant_cast_creature_spells (4,136x)
  106,888 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,456x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::CantCastNoncreature);
        .                   }
        .           
        .                   // Validate convoke/improvise helpers up-front (before any state
        .                   // mutation). Convoke taps untapped creatures (CR 702.52); Improvise
        .                   // taps untapped artifacts (CR 702.126); each pays {1}.
        .                   let has_convoke = card.definition.keywords.contains(&crate::card::Keyword::Convoke)
   29,824 ( 0.00%)              || self.spell_granted_convoke(p, &card);
1,217,652 ( 0.04%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::spell_granted_convoke (7,456x)
        .                   let has_improvise = card.definition.keywords.contains(&crate::card::Keyword::Improvise);
        .                   // CR 701.67 — waterbend helpers ride the same `convoke_creatures` slot;
        .                   // any untapped artifact or creature you control may tap to pay {1} of
        .                   // the waterbend sub-cost. Count is clamped to the waterbend amount below.
        .                   let has_waterbend = waterbend.is_some();
    7,456 ( 0.00%)          if !convoke_creatures.is_empty() && !has_convoke && !has_improvise && !has_waterbend {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SorcerySpeedOnly); // reuse: spell can't tap helpers
        .                   }
        .                   if let Some(amt) = waterbend
        .                       && convoke_creatures.len() > amt as usize
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SorcerySpeedOnly); // reuse: too many waterbend helpers
-- line 6291 ----------------------------------------
-- line 6305 ----------------------------------------
        .                           return Err(GameError::CardNotOnBattlefield(*cid));
        .                       }
        .                   }
        .           
        .                   // Validate delve cards up-front (CR 702.66): the spell must have
        .                   // Keyword::Delve and every listed card must currently sit in the
        .                   // caster's graveyard. The cards aren't exiled here — only after the
        .                   // reduced cost is paid — so a rejected cast leaves them in place.
    7,456 ( 0.00%)          if !delve_cards.is_empty()
        .                       && !card.definition.keywords.contains(&crate::card::Keyword::Delve)
        .                       && !self.controller_grants_spells_delve(p)
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SorcerySpeedOnly); // reuse: spell doesn't have delve
        .                   }
        .                   for cid in delve_cards {
        .                       if !self.players[p].graveyard.iter().any(|c| c.id == *cid) {
-- line 6321 ----------------------------------------
-- line 6329 ----------------------------------------
        .                   // Teferi, Time Raveler's +1 sets `sorceries_as_flash` on its
        .                   // controller — those casters can ignore the sorcery-timing gate
        .                   // until their next turn (when do_untap clears the flag).
        .                   // Teferi's static (`OpponentsSorceryTimingOnly`) flips the rule for
        .                   // opponents: even instants must wait until their main phase.
        .                   // Sigarda's Aid — a battlefield static can grant flash timing to
        .                   // matching spells (Auras + Equipment). Serpent of the Pass — a
        .                   // card-intrinsic `SelfFlashIf` condition on the spell being cast.
   44,736 ( 0.00%)          let flash_granted = self.flash_granted_for(p, &card);
4,249,406 ( 0.14%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_granted_for (7,456x)
   37,280 ( 0.00%)          let must_be_sorcery_speed = !(card.definition.is_instant_speed() || flash_granted)
  314,916 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::is_instant_speed (7,456x)
   17,570 ( 0.00%)              || self.player_locked_to_sorcery_timing(p);
  442,512 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_locked_to_sorcery_timing (2,510x)
    5,020 ( 0.00%)          if must_be_sorcery_speed
   42,078 ( 0.00%)              && !self.can_cast_sorcery_speed(p)
   74,014 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::can_cast_sorcery_speed (4,946x)
      234 ( 0.00%)              && !self.players[p].sorceries_as_flash
        .                   {
      234 ( 0.00%)              self.players[p].hand.push(card);
    2,340 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (78x)
        .                       return Err(GameError::SorcerySpeedOnly);
        .                   }
        .           
        .                   // Validate that the chosen target is legally targetable. Also
        .                   // enforces CR 115.5 — the spell being cast cannot target itself —
        .                   // by threading the cast card's id as `source`.
   81,150 ( 0.00%)          if let Some(ref tgt) = target
   29,722 ( 0.00%)              && let Err(e) = self.check_target_legality_with_source(tgt, p, Some(card_id))
7,991,383 ( 0.26%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source (2,702x)
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(e);
        .                   }
        .                   // Same legality check for each additional target slot (hexproof,
        .                   // shroud, protection, Leyline-of-Sanctity, CR 115.5 self-target).
        .                   for tgt in &additional_targets {
        .                       if let Err(e) = self.check_target_legality_with_source(tgt, p, Some(card_id)) {
-- line 6360 ----------------------------------------
-- line 6365 ----------------------------------------
        .           
        .                   // CR 601.2c — an opponent's Flagbearer must be chosen if any declared
        .                   // slot could take it (Standard Bearer).
        .                   {
        .                       let chosen: Vec<Target> =
        .                           target.iter().cloned().chain(additional_targets.iter().cloned()).collect();
        .                       let slots: Vec<Option<crate::card::SelectionRequirement>> = (0..chosen.len())
        .                           .map(|i| {
    8,106 ( 0.00%)                      card.definition
        .                                   .effect
   13,510 ( 0.00%)                          .target_filter_for_slot_in_mode_kicked(i as u8, mode, kicked)
  234,908 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (2,702x)
        .                                   .cloned()
        .                           })
        .                           .collect();
   51,646 ( 0.00%)              if self.flagbearer_violation(p, &chosen, &slots) {
1,667,670 ( 0.05%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation (7,378x)
        .                           self.players[p].hand.push(card);
        .                           return Err(GameError::InvalidTarget);
        .                       }
   14,756 ( 0.00%)          }
  751,038 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<core::option::Option<crabomination_base::card::SelectionRequirement>>> (7,378x)
        .           
        .                   // CR 115.3 — within a *single* multi-target instance ("up to N / any
        .                   // number of / N target …"), the same object can't be chosen twice.
        .                   // Separate "target" clauses (a Seq of single-target effects) may share
        .                   // a target, so this only fires for the divide/support-style effects.
   51,646 ( 0.00%)          if let Some(n) = card.definition.effect.distinct_target_count(mode) {
  148,558 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::distinct_target_count (7,378x)
        .                       let mut chosen: Vec<&Target> = Vec::with_capacity(1 + additional_targets.len());
        .                       if let Some(t) = target.as_ref() {
        .                           chosen.push(t);
        .                       }
        .                       chosen.extend(additional_targets.iter());
        .                       chosen.truncate(n as usize);
        .                       for i in 0..chosen.len() {
        .                           for j in (i + 1)..chosen.len() {
-- line 6397 ----------------------------------------
-- line 6401 ----------------------------------------
        .                               }
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 702.16: Protection from [color] prevents targeting by spells
        .                   // of that color. Check the spell's colors against the target's
        .                   // protection keywords.
   19,568 ( 0.00%)          if let Some(Target::Permanent(cid)) = target
        .                       && let Some(target_card) = self.battlefield_find(cid)
    4,860 ( 0.00%)              && target_card.controller != p
        .                   {
      834 ( 0.00%)              let spell_colors = card.definition.cost.colors();
   54,792 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (278x)
        .                       // Read computed keywords so granted protection (Mother of Runes,
        .                       // Gods Willing) is honored, not just printed protection.
        .                       let kws = self
      278 ( 0.00%)                  .computed_permanent(cid)
  701,703 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (278x)
    4,682 ( 0.00%)                  .map(|cp| cp.keywords.to_vec())
        .                           .unwrap_or_else(|| target_card.definition.keywords.clone());
        .                       for kw in &kws {
      648 ( 0.00%)                  if let Keyword::Protection(prot_color) = kw
        .                               && spell_colors.contains(prot_color)
        .                           {
        .                               self.players[p].hand.push(card);
        .                               return Err(GameError::TargetHasProtection(cid));
        .                           }
        .                           // CR 702.16h — protection from colored spells (Emrakul).
        .                           if matches!(kw, Keyword::ProtectionFromColoredSpells)
        .                               && !spell_colors.is_empty()
-- line 6429 ----------------------------------------
-- line 6538 ----------------------------------------
        .                           if let Keyword::HexproofExceptColors(colors) = kw
        .                               && self.battlefield_find(cid).is_some_and(|tc| tc.controller != p)
        .                               && !colors.iter().any(|c| spell_colors.contains(c))
        .                           {
        .                               self.players[p].hand.push(card);
        .                               return Err(GameError::TargetHasProtection(cid));
        .                           }
        .                       }
      278 ( 0.00%)          }
   19,986 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::card::Keyword>> (278x)
        .           
        .                   // CR 702.11e — hexproof from [color]: this object can't be targeted
        .                   // by opponents' spells of that color. Covers both printed
        .                   // `Keyword::HexproofFromColor` and the turn-scoped controller grant
        .                   // (Veil of Summer's "you and permanents you control gain hexproof
        .                   // from blue and black"). Applies to permanent and player targets.
   29,512 ( 0.00%)          let spell_colors = card.definition.cost.colors();
1,787,074 ( 0.06%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (7,378x)
    5,404 ( 0.00%)          let hexproof_violation = match target {
    4,812 ( 0.00%)              Some(Target::Permanent(cid)) => self
        .                           .battlefield_find(cid)
    3,240 ( 0.00%)                  .filter(|tc| tc.controller != p)
   12,404 ( 0.00%)                  .is_some_and(|tc| {
      278 ( 0.00%)                      let controller = tc.controller;
      278 ( 0.00%)                      let printed = self
    1,112 ( 0.00%)                          .computed_permanent(cid)
  697,382 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (278x)
      106 ( 0.00%)                          .map(|cp| cp.keywords.to_vec())
        .                                   .unwrap_or_else(|| tc.definition.keywords.clone())
        .                                   .iter()
    1,178 ( 0.00%)                          .any(|kw| match kw {
        .                                       Keyword::HexproofFromColor(c) => spell_colors.contains(c),
        .                                       // CR 702.11f — exactly one color on the spell.
        .                                       Keyword::HexproofFromMonocolored => spell_colors.len() == 1,
        .                                       Keyword::HexproofFromMulticolored => spell_colors.len() >= 2,
        .                                       _ => false,
        .                                   });
      556 ( 0.00%)                      printed
      278 ( 0.00%)                          || self.players[controller]
        .                                       .hexproof_from_colors_this_turn
        .                                       .iter()
        .                                       .any(|c| spell_colors.contains(c))
    2,224 ( 0.00%)                  }),
      296 ( 0.00%)              Some(Target::Player(tp)) => {
      888 ( 0.00%)                  tp != p
      296 ( 0.00%)                      && self.players[tp]
        .                                   .hexproof_from_colors_this_turn
        .                                   .iter()
        .                                   .any(|c| spell_colors.contains(c))
        .                       }
        .                       None => false,
        .                   };
      556 ( 0.00%)          if hexproof_violation {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::TargetHasHexproof(crate::card::CardId(0)));
        .                   }
        .           
        .                   // CR 702.16j — a player with protection from a card type (Serra's
        .                   // Emissary) can't be targeted by a spell of that type.
    2,406 ( 0.00%)          if let Some(Target::Player(tp)) = target
        .                       && tp != p
        .                       && self
      592 ( 0.00%)                  .player_protection_card_types(tp)
        .                           .iter()
      296 ( 0.00%)                  .any(|t| card.definition.card_types.contains(t))
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::TargetHasProtection(crate::card::CardId(0)));
        .                   }
        .           
        .                   // Enforce the spell's target selection requirement (e.g. Terror's
        .                   // "non-black, non-artifact creature"): if the effect binds a filter to
        .                   // slot N and the chosen target doesn't match, reject the cast.
-- line 6607 ----------------------------------------
-- line 6614 ----------------------------------------
        .                   // Power) thread additional slots through `additional_targets`.
        .                   // CR 702.165 — a promised Gift validates (and later resolves) against
        .                   // its enhanced `gifted_effect`, whose target filter can be broader than
        .                   // the printed one (Into the Flood Maw: creature → nonland permanent).
        .                   // Compute the per-slot violation up front (releasing the borrow on
        .                   // `card`) so a rejected cast can move the card back to hand.
        .                   // Cross-slot filters (Barrin's Spite's "controlled by the same player")
        .                   // read the whole chosen slot vector, not just their own target.
   45,712 ( 0.00%)          self.target_slots_scratch = std::iter::once(target.clone())
        .                       .chain(additional_targets.iter().cloned().map(Some))
        .                       .collect();
        .                   let filter_violation = {
   14,756 ( 0.00%)              let target_effect = if card.gift_promised {
        .                           card.definition.gift.as_ref().map(|g| &g.gifted_effect)
        .                       } else {
        .                           None
        .                       }
   14,756 ( 0.00%)              .unwrap_or(&card.definition.effect);
  112,126 ( 0.00%)              let slot_bad = |slot: u8, tgt: &Target| {
    2,702 ( 0.00%)                  target_effect
   16,212 ( 0.00%)                      .target_filter_for_slot_in_mode_kicked(slot, mode, kicked)
  234,908 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (2,702x)
    9,608 ( 0.00%)                      .map(|f| f.resolve_x(x_value.unwrap_or(0)))
  285,984 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::SelectionRequirement::resolve_x (2,402x)
    7,206 ( 0.00%)                      .is_some_and(|filter| {
   21,618 ( 0.00%)                          !self.evaluate_requirement_static(&filter, tgt, p, Some(card.id))
8,303,680 ( 0.27%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (2,402x)
    2,402 ( 0.00%)                      })
  274,216 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::card::SelectionRequirement> (2,402x)
   16,212 ( 0.00%)              };
   18,914 ( 0.00%)              target.as_ref().is_some_and(|tgt| slot_bad(0, tgt))
9,227,890 ( 0.30%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::cast_spell_with_convoke::{{closure}} (2,702x)
        .                           || additional_targets
        .                               .iter()
        .                               .enumerate()
        .                               .any(|(idx, tgt)| slot_bad((idx + 1) as u8, tgt))
        .                   };
        .                   self.target_slots_scratch.clear();
        .                   if filter_violation {
       24 ( 0.00%)              self.players[p].hand.push(card);
      240 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (8x)
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // "If you cast this spell during your main phase, you may [pick an
        .                   // extra target]" (Return to Dust) — extra slots only on the caster's
        .                   // own main phase.
   14,740 ( 0.00%)          if card.definition.extra_targets_main_phase_only
        .                       && !additional_targets.is_empty()
        .                       && !(self.active_player_idx == p && self.step.is_main_phase())
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // "Cast only during combat after blockers are declared" (Flash
        .                   // Foliage).
   14,740 ( 0.00%)          if card.definition.cast_only_after_blockers
        .                       && !(self.step.is_combat_phase() && self.blockers_declared)
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // "Cast this spell only during combat" (Cauldron Dance).
   14,740 ( 0.00%)          if card.definition.cast_only_during_combat && !self.step.is_combat_phase() {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // "Cast only during combat before blockers are declared" (Blaze of
        .                   // Glory).
   14,740 ( 0.00%)          if card.definition.cast_only_before_blockers
        .                       && (!self.step.is_combat_phase() || self.blockers_declared)
        .                   {
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // "Cast only before attackers are declared" (Master Warcraft) — legal
        .                   // any time up to and including the Declare Attackers step, until an
        .                   // attacker is actually on the board.
   14,740 ( 0.00%)          if card.definition.cast_only_before_attackers
        .                       && (!self.attacking.is_empty()
        .                           || !matches!(
        .                               self.step,
        .                               crate::TurnStep::Untap
        .                                   | crate::TurnStep::Upkeep
        .                                   | crate::TurnStep::Draw
        .                                   | crate::TurnStep::PreCombatMain
        .                                   | crate::TurnStep::BeginCombat
-- line 6698 ----------------------------------------
-- line 6711 ----------------------------------------
        .                           return Err(GameError::SelectionRequirementViolated);
        .                       }
        .                   }
        .           
        .                   // CR 601.2b — additional cast costs ("As an additional cost to cast
        .                   // this spell, sacrifice / discard …"). Validate payability up front
        .                   // so an unpayable spell reverts to hand before any mana is spent;
        .                   // the costs themselves are paid after the mana cost succeeds.
   44,220 ( 0.00%)          let mut additional_costs = card.definition.additional_cast_cost.clone();
  250,580 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (7,370x)
        .                   // CR 702.41b — an entwined cast also pays the non-mana entwine cost.
   14,740 ( 0.00%)          if entwine && let Some(ec) = &card.definition.entwine_additional_cost {
        .                       additional_costs.push(ec.clone());
        .                   }
        .                   // CR 702.27 — "Buyback—Sacrifice a land" (Constant Mists).
   14,740 ( 0.00%)          if buyback && let Some(bc) = &card.definition.buyback_additional_cost {
        .                       additional_costs.push(bc.clone());
        .                   }
        .                   // CR 702.32b — a paid action kicker ("Kicker—Sacrifice an artifact")
        .                   // is an additional cost of the kicked cast.
   14,740 ( 0.00%)          if kicked && let Some(kc) = &card.definition.kicker_action_cost {
        .                       additional_costs.push(kc.clone());
        .                   }
        .                   // "Sacrifice one or more [filter]" (Plumb the Forbidden): the caster
        .                   // picked the count as the cast's X — concretize into the shared
        .                   // SacrificePermanent payment path.
        .                   for c in additional_costs.iter_mut() {
        .                       match c {
        .                           crate::card::AdditionalCastCost::SacrificeAnyNumber { filter } => {
-- line 6738 ----------------------------------------
-- line 6812 ----------------------------------------
        .                       // CR 709.5 — each door is cast for its own cost.
        .                       if d == 1 { room.right.cost.clone() } else { room.left.cost.clone() }
        .                   } else {
        .                       match (bestow, card.definition.has_bestow()) {
        .                           (true, Some(bc)) => bc.clone(),
        .                           _ => card.definition.cost.clone(),
        .                       }
        .                   };
   29,480 ( 0.00%)          let mut cost = if base_cost.has_x() {
  168,382 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (7,370x)
        .                       base_cost.with_x_value(x_value.unwrap_or(0))
        .                   } else {
   29,480 ( 0.00%)              base_cost
        .                   };
        .                   // CR 702.32b — fold the optional kicker cost into the total cost.
   14,740 ( 0.00%)          if kicked && let Some(kick) = card.definition.has_kicker() {
        .                       cost.symbols.extend(kick.symbols.iter().cloned());
        .                   }
        .                   // …and each chosen "and/or" kicker option (the Volver cycle).
        .                   for i in &card.kicked_options {
        .                       if let Some(k) = card.definition.kicker_options.get(*i as usize) {
        .                           cost.symbols.extend(k.symbols.iter().cloned());
        .                       }
        .                   }
        .                   // CR 702.172 / FIN Tiered — fold each chosen mode's mana cost into the
        .                   // total.
   14,740 ( 0.00%)          if !spree_modes.is_empty()
        .                       && let crate::effect::Effect::Spree { modes } | crate::effect::Effect::Tiered { modes } =
        .                           &card.definition.effect
        .                   {
        .                       for &i in &spree_modes {
        .                           if let Some(m) = modes.get(i as usize) {
        .                               cost.symbols.extend(m.cost.symbols.iter().cloned());
        .                           }
        .                       }
        .                   }
        .                   // CR 702.27b — fold the optional buyback cost into the total cost,
        .                   // less any "buyback costs cost {N} less" the caster controls
        .                   // (Memory Crystal).
   14,740 ( 0.00%)          if buyback && let Some(bb) = card.definition.has_buyback() {
        .                       let mut bb = bb.clone();
        .                       let discount: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p)
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .filter_map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::BuybackCostsLess { amount } => Some(amount),
-- line 6858 ----------------------------------------
-- line 6860 ----------------------------------------
        .                           })
        .                           .sum();
        .                       if discount > 0 {
        .                           bb.reduce_generic(discount);
        .                       }
        .                       cost.symbols.extend(bb.symbols.iter().cloned());
        .                   }
        .                   // CR 702.41b — fold the optional entwine cost into the total cost.
   14,740 ( 0.00%)          if entwine && let Some(ec) = card.definition.has_entwine() {
        .                       for s in &ec.symbols {
        .                           cost.symbols.push(*s);
        .                       }
        .                   }
        .                   // CR 701.67 — fold the waterbend additional cost ({N} generic) into the
        .                   // total. Helpers tap to pay it below; leftover comes from real mana.
   22,110 ( 0.00%)          if let Some(amt) = waterbend
        .                       && amt > 0
        .                   {
        .                       cost.symbols.push(crate::mana::ManaSymbol::Generic(amt));
        .                   }
   36,850 ( 0.00%)          let tax = extra_cost_for_spell(self, p, &card, target.as_ref());
1,839,218 ( 0.06%)  => crabomination/src/game/actions.rs:crabomination::game::actions::extra_cost_for_spell (7,370x)
   14,740 ( 0.00%)          if tax > 0 {
        .                       cost.symbols.push(crate::mana::ManaSymbol::Generic(tax));
        .                   }
   58,960 ( 0.00%)          cost.symbols.extend(colored_spell_tax_for_spell(self, p, &card).symbols);
1,662,838 ( 0.05%)  => crabomination/src/game/actions.rs:crabomination::game::actions::colored_spell_tax_for_spell (7,370x)
   44,220 ( 0.00%)          if let Some(extra) = self.flash_surcharge_for(p, &card) {
   88,440 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flash_surcharge_for (7,370x)
        .                       cost.symbols.extend(extra.symbols.iter().cloned());
        .                   }
        .                   // Strive (CR 702.122) / Fireball — per-extra-target surcharge.
   29,480 ( 0.00%)          cost.symbols.extend(strive_cost_for_spell(&card, additional_targets.len()).symbols);
  228,470 ( 0.01%)  => crabomination/src/game/actions.rs:crabomination::game::actions::strive_cost_for_spell (7,370x)
   36,850 ( 0.00%)          cost.symbols.extend(or_pay_cost_symbols(self, p, &card));
  228,470 ( 0.01%)  => crabomination/src/game/actions.rs:crabomination::game::actions::or_pay_cost_symbols (7,370x)
        .                   // Apply static cost-reduction effects (Killian's "spells that target
        .                   // a creature cost {2} less"). Tax is applied first so reductions
        .                   // never make the spell free of its tax.
    7,370 ( 0.00%)          let mut reduction = cost_reduction_for_spell(self, p, &card, target.as_ref());
        .                   // "…costs {1} less for each permanent sacrificed this way" — the
        .                   // announced sacrifice count rides the cast's X (Rottenmouth Viper).
   14,740 ( 0.00%)          if card.definition.self_cost_reduction_per_sacrificed {
        .                       reduction = reduction.saturating_add(x_value.unwrap_or(0));
        .                   }
   14,740 ( 0.00%)          if reduction > 0 {
        .                       cost.reduce_generic(reduction);
        .                   }
   44,220 ( 0.00%)          cost.reduce_by_cost(&colored_cost_reduction_for_spell(self, p, &card));
2,454,109 ( 0.08%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::reduce_by_cost (7,370x)
1,662,838 ( 0.05%)  => crabomination/src/game/actions.rs:crabomination::game::actions::colored_cost_reduction_for_spell (7,370x)
        .                   // Colored-aware target-conditional reduction (Brush Off's "{1}{U}
        .                   // less if it targets an instant or sorcery spell") — mandatory per
        .                   // CR 601.2f, removed pip-by-pip.
   20,840 ( 0.00%)          if let Some((filter, less)) = &card.definition.self_cost_reduction_cost_if_target
        .                       && let Some(tgt) = target.as_ref()
        .                       && self.evaluate_requirement_static(filter, tgt, p, Some(card.id))
        .                   {
        .                       cost.reduce_by_cost(less);
        .                   }
        .                   // Delve (CR 702.66): each graveyard card to be exiled pays {1} of the
        .                   // generic cost. The reduction is clamped to the generic portion by
        .                   // `reduce_generic`; the cards themselves are exiled only after a
        .                   // successful payment (below).
    7,370 ( 0.00%)          if !delve_cards.is_empty() {
        .                       cost.reduce_generic(delve_cards.len() as u32);
        .                   }
        .                   // Trinisphere floor (CR 117.7 / replacement-style): applied after
        .                   // every reduction so a discounted spell still owes the minimum.
    7,370 ( 0.00%)          apply_spell_cost_floor(self, &mut cost);
1,687,716 ( 0.05%)  => crabomination/src/game/actions.rs:crabomination::game::actions::apply_spell_cost_floor (7,370x)
        .                   // Yusri's jackpot — "you may cast spells from your hand this turn
        .                   // without paying their mana costs" zeroes the whole cost.
   22,110 ( 0.00%)          if self.players[p].free_spells_from_hand_this_turn {
        .                       cost.symbols.clear();
        .                   }
        .           
        .                   // "Pay X life" additional cost — pre-flight before any payment
        .                   // mutation (CR 119.4: paying down to exactly 0 is legal; below is
        .                   // not). The life itself is paid after the mana receipt succeeds.
   44,220 ( 0.00%)          let pay_x_life: u32 = if card.definition.additional_cost_pay_x_life {
        .                       x_value.unwrap_or(0)
        .                   } else {
        .                       0
        .                   };
   22,110 ( 0.00%)          if self.effective_life(p) < pay_x_life as i32 {
  324,146 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (7,370x)
        .                       self.players[p].hand.push(card);
        .                       return Err(GameError::InsufficientLife);
        .                   }
        .           
        .                   // Snapshot pristine state before convoke + auto-tap mutate it, so a
        .                   // failed payment can revert both convoke taps and any lands that
        .                   // auto-tap tapped.
   29,480 ( 0.00%)          let snapshot = self.snapshot_payment_state(p);
8,175,692 ( 0.26%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state (7,370x)
        .           
        .                   // Convoke (CR 702.51): tap each chosen creature; each pays {1} OR one
        .                   // mana of a color that creature is. A tapped creature is credited with
        .                   // a colored pip its color set covers and the cost still needs — so a
        .                   // white creature pays a {W} the cost wants — falling back to {1}.
        .                   // Improvise / waterbend helpers (colorless artifacts) always pay {1}.
        .                   for cid in convoke_creatures {
        .                       let colors: Vec<crate::mana::Color> = self
-- line 6953 ----------------------------------------
-- line 6962 ----------------------------------------
        .                           .copied()
        .                           .find(|c| self.cost_still_needs_color(p, &cost, *c))
        .                       {
        .                           Some(color) => self.players[p].mana_pool.add(color, 1),
        .                           None => self.players[p].mana_pool.add_colorless(1),
        .                       }
        .                   }
        .           
   14,740 ( 0.00%)          let forced_only = self.players[p].manual_mana;
        .                   // Spell kind gates spend-restricted mana ("spend only to cast …")
        .                   // by the cast card's types — see `CardDefinition::spell_kind`.
   14,740 ( 0.00%)          let spell_kind = card.definition.spell_kind();
7,793,210 ( 0.25%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::spell_kind (7,370x)
        .                   // CR 601.2g — float-spend confirmation. If the caster has pre-existing
        .                   // floating mana that the cost could *either* spend *or* avoid (untapped
        .                   // sources can cover it), ask before auto-spending it instead of silently
        .                   // consuming mana they may have been holding. Skipped for convoke/delve
        .                   // casts (their pool already carries cost-reduction mana). Nothing has
        .                   // been tapped yet on this no-convoke path, so we abort by simply
        .                   // returning the card to hand; the cast re-runs from the top on answer.
   14,740 ( 0.00%)          if forced_only
        .                       && spend_float_choice.is_none()
        .                       && convoke_creatures.is_empty()
        .                       && delve_cards.is_empty()
        .                       && self.float_spend_is_optional(p, &cost, &spell_kind)
        .                   {
        .                       let float_summary = self.protectable_float(p, &cost).summary();
        .                       let name = card.definition.name;
        .                       self.players[p].hand.push(card);
-- line 6989 ----------------------------------------
-- line 7010 ----------------------------------------
        .                           resume: crate::game::types::ResumeContext::ActionFloatConfirm {
        .                               actor: p,
        .                               action: Box::new(action),
        .                           },
        .                       });
        .                       return Ok(vec![]);
        .                   }
        .           
  103,180 ( 0.00%)          let receipt = match self.try_pay_after_snapshot_mode(p, &cost, snapshot, forced_only, &spell_kind, spend_float_choice) {
419,069,531 (13.45%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode (7,370x)
   14,092 ( 0.00%)              Ok(r) => r,
  211,380 ( 0.01%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (7,046x)
      648 ( 0.00%)              Err(e) => {
      972 ( 0.00%)                  self.players[p].hand.push(card);
    9,720 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (324x)
    1,944 ( 0.00%)                  return Err(e);
        .                       }
        .                   };
   28,184 ( 0.00%)          self.pay_life_cost(p, receipt.side_effects.life_lost);
  105,690 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::pay_life_cost (7,046x)
   49,322 ( 0.00%)          self.note_cast_payment_riders(&receipt, &card.definition.spell_kind());
6,558,242 ( 0.21%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::spell_kind (7,046x)
   63,414 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::note_cast_payment_riders (7,046x)
        .                   // "Pay X life" additional cost — paid on cast alongside the mana
        .                   // (CR 601.2h); it stays paid if the spell is countered.
   28,184 ( 0.00%)          self.pay_life_cost(p, pay_x_life);
  105,690 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::pay_life_cost (7,046x)
        .           
        .                   // CR 702.150 — Compleated: remember the life paid to Phyrexian pips so
        .                   // the planeswalker enters with that much less loyalty.
   14,092 ( 0.00%)          if receipt.side_effects.life_lost > 0
        .                       && card.definition.keywords.contains(&crate::card::Keyword::Compleated)
        .                   {
        .                       card.compleated_life_paid = receipt.side_effects.life_lost;
        .                   }
        .           
        .                   // Delve payment succeeded — exile the chosen graveyard cards now
        .                   // (CR 702.66: they're exiled as part of paying the cost). Bumps the
        .                   // per-turn exile tally so "if cards were exiled this turn" payoffs see
-- line 7041 ----------------------------------------
-- line 7045 ----------------------------------------
        .                           self.exile.push(exiled);
        .                           self.players[p].cards_exiled_this_turn += 1;
        .                       }
        .                   }
        .           
        .                   // Compute converge: count distinct colors of mana drained from the
        .                   // pool by paying the cost. Convoke pips contribute generic only,
        .                   // so they don't raise this count.
   49,322 ( 0.00%)          let converged_value = converge_count(&receipt.pool_before, &self.players[p].mana_pool);
  105,690 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination::game::actions::converge_count (7,046x)
        .                   // Total mana spent — `pool_before.total() - pool_after.total()`.
        .                   // Read by `Value::CastSpellManaSpent` for Increment / Opus payoffs.
        .                   let mana_spent = receipt
        .                       .pool_before
        .                       .total()
        .                       .saturating_sub(self.players[p].mana_pool.total());
   42,276 ( 0.00%)          card.cast_mana_spent_by_color =
        .                       spent_by_color(&receipt.pool_before, &self.players[p].mana_pool);
        .           
        .                   // Chorus of the Conclave — "as an additional cost to cast creature
        .                   // spells, you may pay any amount of mana." Offered after the printed
        .                   // cost is paid, capped by what's still floating; the amount rides
        .                   // `pending_etb_counters` to the battlefield.
   21,138 ( 0.00%)          if card.definition.is_creature()
  101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
   19,510 ( 0.00%)              && self.battlefield.iter().any(|c| {
  642,642 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (3,902x)
  127,908 ( 0.00%)                  c.controller == p
        .                               && c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::CreatureSpellsMayPayExtraForCounters
        .                                   )
        .                               })
        .                       })
        .                   {
-- line 7077 ----------------------------------------
-- line 7088 ----------------------------------------
        .                           if extra > 0 {
        .                               self.players[p].mana_pool.spend_generic(extra);
        .                               card.pending_etb_counters
        .                                   .push((crate::card::CounterType::PlusOnePlusOne, extra));
        .                           }
        .                       }
        .                   }
        .           
   28,184 ( 0.00%)          let mut auto_events = receipt.auto_events;
   63,414 ( 0.00%)          auto_events.push(GameEvent::SpellCast {
        .                       player: p,
        .                       card_id,
    7,046 ( 0.00%)              face: self.pending_cast_face,
        .                   });
        .           
        .                   // CR 601.2h — pay the additional cast costs now (during casting), so
        .                   // sacrifice/discard triggers fire before the spell resolves. A
        .                   // sacrifice reports the fodder's power, which becomes the spell's X
        .                   // for "X = the sacrificed creature's power" riders (Tend the Pests).
        .                   let mut sac_x = None;
   14,092 ( 0.00%)          if !additional_costs.is_empty() {
        .                       // CR 701.59 — stamp whether the collect-evidence cost will be paid
        .                       // before payment mutates the graveyard (auto-collects when able), so
        .                       // `Predicate::SpellCollectedEvidence` reads it at resolution.
        .                       if additional_costs.iter().any(|c| matches!(c,
        .                           crate::card::AdditionalCastCost::CollectEvidence { amount, .. }
        .                               if self.graveyard_can_collect_evidence(p, *amount)))
        .                       {
        .                           card.cast_collected_evidence = true;
-- line 7116 ----------------------------------------
-- line 7139 ----------------------------------------
        .                               toughness: tough,
        .                               count: self.sacrificed_count,
        .                               mana_value: mv,
        .                               card: sac_card,
        .                               body: Box::new(def.effect.clone()),
        .                           };
        .                       }
        .                   }
   35,230 ( 0.00%)          let events = auto_events;
        .                   let final_x = x_value.unwrap_or(0).max(sac_x.unwrap_or(0));
        .           
        .                   // CR 701.67 — record that the waterbend additional cost was paid, so
        .                   // "if its additional cost was paid" riders can branch on resolution.
        .                   if waterbend.is_some_and(|a| a > 0) {
        .                       card.cast_via_waterbend = true;
        .                   }
   77,506 ( 0.00%)          self.finalize_cast(
171,551,124 ( 5.50%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::finalize_cast (7,046x)
        .                       p,
    7,046 ( 0.00%)              card,
        .                       target,
        .                       additional_targets,
        .                       mode,
        .                       final_x,
        .                       converged_value,
        .                       mana_spent,
        .                       true,
        .                   );
        .           
   42,276 ( 0.00%)          Ok(events)
  111,086 ( 0.00%)      }
  191,620 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination_base::card::AdditionalCastCost>> (7,370x)
   70,460 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::mana::ManaPool> (7,046x)
        .           
        .               /// Is `sac_id` a legal answer to `card_id`'s "sacrifice a permanent"
        .               /// additional cast cost? True iff the card is in `caster`'s hand, the
        .               /// card has such a cost, and `caster` controls `sac_id` and it matches
        .               /// the cost's filter. Used by the `CastSacrifice` resume to vet a
        .               /// client's chosen sacrifice before re-running the cast.
        .               pub(crate) fn cast_sacrifice_choice_is_legal(
        .                   &self,
-- line 7176 ----------------------------------------
-- line 7894 ----------------------------------------
        .               /// 5. Resets priority to the active player so the cast can be responded
        .               ///    to.
        .               // The argument list is wide because each cast path's resolution-time
        .               // context (`x_value`, `converged_value`, `mana_spent`) must reach
        .               // `finalize_cast` without round-tripping through a struct. The three
        .               // callers (regular cast, flashback, alternative cast) all hand off
        .               // the scalars directly from local variables computed during payment.
        .               #[allow(clippy::too_many_arguments)]
   98,644 ( 0.00%)      pub(crate) fn finalize_cast(
        .                   &mut self,
        .                   p: usize,
        .                   card: crate::card::CardInstance,
        .                   target: Option<Target>,
        .                   additional_targets: Vec<Target>,
        .                   mode: Option<usize>,
        .                   x_value: u32,
        .                   converged_value: u32,
        .                   mana_spent: u32,
        .                   from_hand: bool,
        .               ) {
        .                   let card_id = card.id;
        .                   // "The next [filter] spell you cast this turn can't be countered" is a
        .                   // one-shot: the matching cast consumes its grant.
   21,138 ( 0.00%)          if !self.players[p].next_spell_uncounterable.is_empty() {
        .                       let hit = self.players[p]
        .                           .next_spell_uncounterable
        .                           .iter()
        .                           .position(|f| self.evaluate_requirement_on_card(f, &card, p));
        .                       if let Some(i) = hit {
        .                           self.players[p].next_spell_uncounterable.remove(i);
        .                       }
        .                   }
    7,046 ( 0.00%)          self.spells_cast_this_turn += 1;
        .                   // Mana Maze reads the turn's most recent cast (CR 601.2 restriction).
   49,322 ( 0.00%)          self.last_cast_spell_colors = card.definition.printed_colors();
1,306,118 ( 0.04%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_colors (7,046x)
   14,092 ( 0.00%)          self.players[p].spells_cast_this_turn += 1;
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
   14,092 ( 0.00%)          self.players[p].spells_cast_this_game_turn += 1;
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                   if card.definition.card_types.contains(&crate::card::CardType::Sorcery) {
    1,746 ( 0.00%)              self.players[p].sorceries_cast_this_turn += 1;
   17,460 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (582x)
        .                   }
        .                   // Arboria — "cast a spell … during their last turn".
        .                   self.note_acted_on_own_turn(p);
        .                   for (seat, pl) in self.players.iter_mut().enumerate() {
   28,184 ( 0.00%)              if seat != p {
   28,184 ( 0.00%)                  pl.opponent_cast_spell_since_your_turn = true;
18,072,046 ( 0.58%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                       }
        .                   }
        .                   // Per-turn cast-name log (Grim Reminder's "cast a spell this turn with
        .                   // the same name").
    7,046 ( 0.00%)          self.players[p]
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                       .spell_names_cast_this_turn
   14,092 ( 0.00%)              .push(card.definition.name);
   35,230 ( 0.00%)          self.players[p].spell_ids_cast_this_turn.push(card.id);
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                   // "First noncreature spell of a turn" tally (Nullstone Gargoyle). An
        .                   // Adventure/Omen half cast is a noncreature spell regardless of the
        .                   // card's front face.
   35,230 ( 0.00%)          if card.casting_alt_half() || !card.definition.is_creature() {
  101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
    3,144 ( 0.00%)              self.noncreature_spells_cast_this_turn += 1;
        .                   }
        .                   // Per-name lifetime tally — "you've cast another spell named X this
        .                   // game" (Approach of the Second Sun).
   14,092 ( 0.00%)          *self
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                       .players[p]
        .                       .spells_cast_by_name_this_game
   14,092 ( 0.00%)              .entry(card.definition.name)
        .                       .or_insert(0) += 1;
   14,092 ( 0.00%)          if from_hand {
   14,092 ( 0.00%)              self.players[p].spells_cast_from_hand_this_turn += 1;
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                   }
        .                   // CR 715 / 702.183 — when cast as its Adventure/Omen half the card is an
        .                   // instant/sorcery spell, not a creature spell, so the spell-type
        .                   // tallies (Magecraft / Prowess) read the half's types.
   14,092 ( 0.00%)          let alt_types = card.alt_spell_half().map(|h| &h.card_types);
        .                   let is_instant_or_sorcery = match alt_types {
        .                       Some(types) => {
        .                           types.contains(&CardType::Instant) || types.contains(&CardType::Sorcery)
        .                       }
        .                       None => {
        .                           card.definition.card_types.contains(&CardType::Instant)
        .                               || card.definition.card_types.contains(&CardType::Sorcery)
        .                       }
        .                   };
        .                   // Refine the spell-type tallies. Both gates default to 0 on
        .                   // snapshot back-compat (player.rs `#[serde(default)]`).
        .                   if is_instant_or_sorcery {
    5,416 ( 0.00%)              self.players[p].instants_or_sorceries_cast_this_turn += 1;
   81,240 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (2,708x)
        .                   }
   35,230 ( 0.00%)          if card.definition.cost.colors().len() >= 2 {
1,980,712 ( 0.06%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (7,046x)
        .                       self.players[p].multicolored_spells_cast_this_turn += 1;
        .                   }
   35,230 ( 0.00%)          if !card.casting_alt_half() && card.definition.is_creature() {
  101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
    7,804 ( 0.00%)              self.players[p].creatures_cast_this_turn += 1;
  117,060 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,902x)
        .                   }
        .                   // Spell-type tallies for the per-turn lock pieces (Deafening Silence,
        .                   // Ethersworn Canonist). Read the cast half's types so an Adventure/Omen
        .                   // instant-or-sorcery half counts as a noncreature spell.
    7,046 ( 0.00%)          let cast_types = alt_types.unwrap_or(&card.definition.card_types);
        .                   if !cast_types.contains(&CardType::Creature) {
    6,288 ( 0.00%)              self.players[p].noncreature_spells_cast_this_game_turn += 1;
   94,320 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,144x)
        .                   }
        .                   if !cast_types.contains(&CardType::Artifact) {
   14,092 ( 0.00%)              self.players[p].nonartifact_spells_cast_this_game_turn += 1;
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
        .                   }
        .                   // Veil of Summer gate: note when a player casts a blue or black
        .                   // spell (color read off the printed mana cost). The full profile
        .                   // (colors + cast half's types) backs the Trap alternative costs.
        .                   {
   21,138 ( 0.00%)              let colors = card.definition.cost.colors();
1,519,068 ( 0.05%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaCost::colors (7,046x)
        .                       if colors.contains(&crate::mana::Color::Blue)
        .                           || colors.contains(&crate::mana::Color::Black)
        .                       {
    7,076 ( 0.00%)                  self.players[p].cast_blue_or_black_this_turn = true;
  106,140 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,538x)
        .                       }
   70,460 ( 0.00%)              self.players[p].spell_casts_this_turn.push(crate::game::types::CastProfile {
  211,380 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (7,046x)
    7,046 ( 0.00%)                  colors,
        .                           card_types: cast_types.clone(),
        .                       });
        .                   }
        .                   consume_first_spell_tax(self, p);
        .           
   14,092 ( 0.00%)          let on_cast_triggers = if card.casting_alt_half() {
        .                       Vec::new()
        .                   } else {
        .                       collect_self_cast_triggers(&card)
        .                   };
   35,230 ( 0.00%)          let uncounterable = self.caster_grants_uncounterable_with_x(p, &card, x_value)
4,813,218 ( 0.15%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::caster_grants_uncounterable_with_x (7,046x)
        .                       || std::mem::take(&mut self.cast_paid_uncounterable);
        .           
   21,138 ( 0.00%)          let was_creature_spell = !card.casting_alt_half() && card.definition.is_creature();
  101,030 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,046x)
        .                   // CR 702.146e — casting a daybound spell while it's neither day nor
        .                   // night makes it day as the spell is put onto the stack.
        .                   let casts_daybound = card.definition.keywords.contains(&Keyword::Daybound);
        .                   // CR 702.40 — Storm: when this spell is cast, copy it for each spell
        .                   // cast before it this turn. `spells_cast_this_turn` already includes
        .                   // this spell (bumped above), so prior spells = count - 1. Capture the
        .                   // bits needed to mint copies before `card` is moved onto the stack.
        .                   // Statics-granted storm ("Instant and sorcery spells you cast have
        .                   // storm" — Prismari, the Inspiration): granted at cast time so the
        .                   // copy count is the true CR 702.40 storm count.
   42,944 ( 0.00%)          let granted_storm = (card.definition.is_instant() || card.definition.is_sorcery())
  106,358 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_instant (7,046x)
   76,974 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_sorcery (4,920x)
    2,708 ( 0.00%)              && self.battlefield.iter().any(|c| {
   98,080 ( 0.00%)                  c.controller == p
        .                               && c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(sa.effect, crate::effect::StaticEffect::GrantStormToISSpells)
        .                               })
        .                       });
   28,184 ( 0.00%)          let storm_copies = (card
        .                       .definition
        .                       .keywords
        .                       .contains(&Keyword::Storm)
        .                       || granted_storm)
        .                       .then(|| {
        .                           (
        .                               card.definition.clone(),
        .                               self.spells_cast_this_turn.saturating_sub(1),
        .                           )
        .                       })
        .                       // CR 702.69 — Gravestorm: copy for each permanent put into a
        .                       // graveyard from the battlefield this turn (counted before this
        .                       // spell resolves, so its own future death isn't included).
   14,092 ( 0.00%)              .or_else(|| {
        .                           card.definition
        .                               .keywords
        .                               .contains(&Keyword::Gravestorm)
        .                               .then(|| (card.definition.clone(), self.permanents_to_graveyard_this_turn))
        .                       })
        .                       // "Copy it for each OTHER instant and sorcery spell you've cast
        .                       // this turn" (Show of Confidence). The caster's I/S counter has
        .                       // already been bumped for this cast, so subtract it back out.
-- line 8062 ----------------------------------------
-- line 8070 ----------------------------------------
        .                                       self.players[p]
        .                                           .instants_or_sorceries_cast_this_turn
        .                                           .saturating_sub(1),
        .                                   )
        .                               })
        .                       })
        .                       // Caster-chosen copy count via the cast's X (Plumb the
        .                       // Forbidden — one copy per additional-cost sacrifice).
    7,046 ( 0.00%)              .or_else(|| {
        .                           card.definition
        .                               .copies_on_cast_x
        .                               .then(|| (card.definition.clone(), x_value))
        .                       });
        .           
        .                   // CR 608.2b — remember whether the primary target is a battlefield
        .                   // permanent right now; resolution re-checks its legality.
   14,092 ( 0.00%)          let mut card = card;
   28,184 ( 0.00%)          card.cast_target_was_battlefield = matches!(
   21,138 ( 0.00%)              &target,
    2,266 ( 0.00%)              Some(Target::Permanent(tid)) if self.battlefield_find(*tid).is_some()
        .                   );
   14,092 ( 0.00%)          if self.cast_kick_count > 0 {
        .                       card.kicked = true;
        .                       card.kick_count = self.cast_kick_count;
        .                   }
  197,288 ( 0.01%)          self.stack.push(StackItem::Spell {
   14,092 ( 0.00%)              card: Box::new(card),
        .                       caster: p,
        .                       target: target.clone(),
        .                       additional_targets: additional_targets.clone(),
        .                       mode,
        .                       x_value,
        .                       converged_value,
        .                       mana_spent,
        .                       uncounterable,
        .                   });
        .                   // Push Storm copies above the original (they resolve first, CR 702.40).
        .                   // Each is a token copy that can't be countered, inheriting target/mode.
   28,184 ( 0.00%)          if let Some((def, n)) = storm_copies {
        .                       for _ in 0..n {
        .                           let new_id = self.next_id();
        .                           let mut copy_inst = crate::card::CardInstance::new(new_id, def.clone(), p);
        .                           copy_inst.is_token = true;
        .                           copy_inst.cast_target_was_battlefield = matches!(
        .                               &target,
        .                               Some(Target::Permanent(tid)) if self.battlefield_find(*tid).is_some()
        .                           );
-- line 8116 ----------------------------------------
-- line 8122 ----------------------------------------
        .                               mode,
        .                               x_value,
        .                               converged_value,
        .                               mana_spent: 0,
        .                               uncounterable: true,
        .                           });
        .                       }
        .                   }
    7,046 ( 0.00%)          self.randomize_single_target_on_stack();
1,509,416 ( 0.05%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack (7,046x)
   21,138 ( 0.00%)          self.push_on_cast_triggers_x(card_id, p, on_cast_triggers, x_value);
        .                   // SpellCast / YourControl triggers (Prowess, Magecraft, Repartee, …)
        .                   // fire *at cast time*, before the spell resolves. The trigger goes
        .                   // on the stack above the spell so it resolves first (and still
        .                   // fires if the spell itself is countered in response). Filters
        .                   // (e.g. CastSpellTargetsMatch) read the just-cast spell's target
        .                   // from the stack while the spell still sits there.
        .                   //
        .                   // Threads `mana_spent` (and X / Converge) into the trigger context
        .                   // so Increment / Opus payoffs reading `Value::CastSpellManaSpent`
        .                   // observe the actual amount paid for *this* spell.
        .                   self.fire_spell_cast_triggers(p, card_id, !was_creature_spell, mana_spent, converged_value);
        .                   // CR 702.146e — a daybound spell cast while neither day nor night
        .                   // makes it day.
    7,046 ( 0.00%)          if casts_daybound && self.day_night.is_none() {
        .                       let mut day_evs = Vec::new();
        .                       self.set_day_night(crate::game::types::DayNight::Day, &mut day_evs);
        .                   }
        .                   // CR 700.14 — Expend. Bump the caster's running spell-mana total and
        .                   // dispatch an `Expended` event so "Whenever you expend N" triggers
        .                   // fire on the cost-payment that first reaches their threshold.
   14,092 ( 0.00%)          if mana_spent > 0 {
   14,092 ( 0.00%)              self.expend_prev_total = self.mana_spent_on_spells_this_turn;
    7,046 ( 0.00%)              self.mana_spent_on_spells_this_turn =
        .                           self.mana_spent_on_spells_this_turn.saturating_add(mana_spent);
        .                       let total = self.mana_spent_on_spells_this_turn;
   49,322 ( 0.00%)              self.dispatch_triggers_for_events(&[GameEvent::Expended { player: p, total }]);
29,098,330 ( 0.93%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (7,046x)
        .                   }
        .                   // CR 702.62 suspend accelerants (Deep-Sea Kraken): an opponent's cast
        .                   // ticks a time counter off their suspended accelerant cards.
    7,046 ( 0.00%)          self.process_suspend_accelerants(p);
  563,172 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_suspend_accelerants (7,046x)
        .                   // CR 702.21: Ward triggers on each chosen target permanent the caster
        .                   // doesn't control. Pushed last so Ward sits on top of the caster's
        .                   // own SpellCast triggers (Magecraft, Prowess) — correct APNAP order
        .                   // since the caster is the active player and Ward belongs to a
        .                   // nonactive player. Ward resolves first and may counter the spell
        .                   // unless the caster pays the Ward cost.
        .                   self.push_ward_triggers_for_cast(p, card_id);
        .                   // BecameTarget triggers — fired through the unified dispatcher so
        .                   // APNAP order is correct and the trigger's `EventSpec.filter` is
        .                   // honored. One event per permanent target on the just-pushed
        .                   // spell. Used by SOS Tenured Concocter's "may draw" trigger.
        .                   self.dispatch_became_target_events_for_cast(p, card_id);
        .                   // CR 700.13 — committing a crime by targeting an opponent / their
        .                   // permanents / cards with this spell (Kaervek, Gisa).
    7,046 ( 0.00%)          let crime_targets: Vec<Target> = self
        .                       .stack
        .                       .iter()
        .                       .rev()
   14,268 ( 0.00%)              .find_map(|si| match si {
   14,092 ( 0.00%)                  StackItem::Spell { card, target, additional_targets, .. } if card.id == card_id => {
        .                               Some(target.iter().cloned().chain(additional_targets.iter().cloned()).collect())
        .                           }
        .                           _ => None,
        .                       })
        .                       .unwrap_or_default();
        .                   self.dispatch_crime_for_targets(p, &crime_targets);
        .                   self.give_priority_to_active();
   56,368 ( 0.00%)      }
        .           
        .               /// CR 700.13 — does choosing `t` as a target for a spell/ability cast by
        .               /// `caster` constitute a crime? True when `t` is an opponent, a permanent
        .               /// an opponent controls, a spell an opponent controls, or any card an
        .               /// opponent owns (graveyard / hand / library / exile).
        .               pub(crate) fn target_is_crime(&self, caster: usize, t: &Target) -> bool {
    5,156 ( 0.00%)          match t {
    2,308 ( 0.00%)              Target::Player(p) => *p != caster && self.players.get(*p).is_some(),
        .                       Target::Permanent(id) => {
    2,278 ( 0.00%)                  if let Some(c) = self.battlefield_find(*id) {
    1,614 ( 0.00%)                      return c.controller != caster;
        .                           }
        .                           for si in &self.stack {
    1,368 ( 0.00%)                      if let StackItem::Spell { card, caster: sc, .. } = si
    1,368 ( 0.00%)                          && card.id == *id
        .                               {
      440 ( 0.00%)                          return *sc != caster;
        .                               }
        .                           }
      448 ( 0.00%)                  self.find_card_anywhere(*id).is_some_and(|c| c.owner != caster)
   31,798 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (224x)
        .                       }
        .                   }
       16 ( 0.00%)      }
        .           
        .               /// CR 700.13 — dispatch a single `CommittedCrime` event for `caster` if any
        .               /// of `targets` qualifies as a crime. Fires once per spell/ability, not per
        .               /// qualifying target.
        .               pub(crate) fn dispatch_crime_for_targets(&mut self, caster: usize, targets: &[Target]) {
        .                   if targets.iter().any(|t| self.target_is_crime(caster, t)) {
    2,204 ( 0.00%)              self.players[caster].committed_crime_this_turn = true;
   33,060 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,102x)
    6,612 ( 0.00%)              self.dispatch_triggers_for_events(&[GameEvent::CommittedCrime { player: caster }]);
4,728,536 ( 0.15%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (1,102x)
        .                   }
        .               }
        .           
        .               /// Walk the just-pushed `StackItem::Spell` and emit one
        .               /// `GameEvent::BecameTarget` for every permanent target slot, then
        .               /// dispatch the events through the unified trigger pipeline.
        .               pub(crate) fn dispatch_became_target_events_for_cast(
        .                   &mut self,
        .                   caster: usize,
        .                   cast_card_id: CardId,
        .               ) {
    7,046 ( 0.00%)          let (target, additional_targets): (Option<Target>, Vec<Target>) = match self
        .                       .stack
        .                       .iter()
        .                       .rev()
   14,268 ( 0.00%)              .find_map(|si| match si {
        .                           StackItem::Spell { card, target, additional_targets, .. }
   14,092 ( 0.00%)                      if card.id == cast_card_id =>
        .                           {
   28,184 ( 0.00%)                      Some((target.clone(), additional_targets.clone()))
        .                           }
        .                           _ => None,
        .                       }) {
        .                       Some(t) => t,
        .                       None => return,
        .                   };
        .                   let slots: Vec<Target> = target.into_iter().chain(additional_targets).collect();
   14,092 ( 0.00%)          if slots.is_empty() {
        .                       return;
        .                   }
        .                   // CR 601.2c — one "chose targets" event for the whole announcement,
        .                   // plus the per-object `BecameTarget`s.
   12,810 ( 0.00%)          let mut events = vec![GameEvent::ChoseTargets { chooser: caster, object: cast_card_id }];
   10,248 ( 0.00%)          events.extend(slots.into_iter().filter_map(|t| match t {
        .                       Target::Permanent(id) => {
    6,798 ( 0.00%)                  Some(GameEvent::BecameTarget { target: id, caster, by: Some(cast_card_id) })
        .                       }
        .                       _ => None,
        .                   }));
    2,562 ( 0.00%)          self.dispatch_triggers_for_events(&events);
11,007,661 ( 0.35%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (2,562x)
    5,124 ( 0.00%)      }
  400,126 ( 0.01%)  => ???:__rustc::__rust_dealloc (2,562x)
        .           
        .               /// CR 702.21 — push a Ward triggered ability onto the stack for each
        .               /// target permanent (controlled by another player) that has
        .               /// `Keyword::Ward(WardCost)`. Each trigger is `Effect::CounterUnless`
        .               /// aimed at the just-cast spell. At resolution the engine auto-pays
        .               /// on the spell controller's behalf if affordable; otherwise the
        .               /// spell is countered.
        .               ///
-- line 8269 ----------------------------------------
-- line 8271 ----------------------------------------
        .               /// `StackItem::Spell` (so this must run after `finalize_cast`'s push).
        .               /// Trivial Ward variants (e.g. `WardCost::Mana` with an empty/zero
        .               /// cost) are skipped — a $0 pay is always affordable and the visible
        .               /// outcome is identical to no Ward at all, so we save the stack churn.
        .               pub(crate) fn push_ward_triggers_for_cast(&mut self, caster: usize, cast_card_id: CardId) {
        .                   // Locate the just-pushed spell and pull its targets out as owned
        .                   // values — we can't hold an immutable borrow while we push new
        .                   // stack items below.
    7,046 ( 0.00%)          let (target, additional_targets): (Option<Target>, Vec<Target>) = match self
        .                       .stack
        .                       .iter()
        .                       .rev()
   14,268 ( 0.00%)              .find_map(|si| match si {
        .                           StackItem::Spell { card, target, additional_targets, .. }
   14,092 ( 0.00%)                      if card.id == cast_card_id =>
        .                           {
   28,184 ( 0.00%)                      Some((target.clone(), additional_targets.clone()))
        .                           }
        .                           _ => None,
        .                       }) {
        .                       Some(t) => t,
        .                       // Spell isn't on the stack (e.g. countered before this hook).
        .                       None => return,
        .                   };
        .           
        .                   let all_targets: Vec<Target> = target
        .                       .into_iter()
        .                       .chain(additional_targets)
        .                       .collect();
   28,184 ( 0.00%)          self.push_ward_triggers_for_targets(caster, cast_card_id, &all_targets);
7,182,172 ( 0.23%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_targets (7,046x)
        .               }
        .           
        .               /// Shared core for Ward enforcement: walk `targets`, and for each
        .               /// permanent target controlled by a player other than `actor` whose
        .               /// `Keyword::Ward(WardCost)` is non-trivial, push a Ward trigger
        .               /// above whatever is currently on top of the stack. The trigger's
        .               /// `target` carries `target_for_trigger` — the spell card-id (for
        .               /// casts) or the source permanent's id (for activated abilities) —
        .               /// so `Effect::CounterUnless` can walk the stack for the topmost
        .               /// matching `Spell` or `Trigger`.
   63,558 ( 0.00%)      pub(crate) fn push_ward_triggers_for_targets(
        .                   &mut self,
        .                   actor: usize,
        .                   target_for_trigger: CardId,
        .                   targets: &[Target],
        .               ) {
        .                   use crate::card::{Keyword, WardCost};
        .                   use crate::effect::Selector;
        .           
        .                   for tgt in targets {
    5,156 ( 0.00%)              let perm_id = match tgt {
    2,278 ( 0.00%)                  Target::Permanent(id) => *id,
        .                           _ => continue,
        .                       };
        .                       self.push_first_targeting_counter(perm_id, target_for_trigger);
    2,278 ( 0.00%)              let (ward_cost, ward_controller) = match self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == perm_id)
        .                       {
    3,228 ( 0.00%)                  Some(c) if c.controller != actor => {
    1,112 ( 0.00%)                      let computed = self.computed_permanent(perm_id);
  710,244 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (278x)
        .                               let cost: Option<WardCost> = computed
        .                                   .as_ref()
        .                                   .map(|cp| cp.keywords.as_slice())
        .                                   .unwrap_or(&c.definition.keywords)
        .                                   .iter()
      566 ( 0.00%)                          .find_map(|k| match k {
        .                                       Keyword::Ward(cost) => Some(cost.clone()),
      126 ( 0.00%)                              _ => None,
        .                                   });
        .                               match cost {
        .                                   Some(cc) if !ward_cost_is_trivial(&cc) => (cc, c.controller),
        .                                   _ => continue,
        .                               }
        .                           }
        .                           _ => continue,
        .                       };
-- line 8348 ----------------------------------------
-- line 8352 ----------------------------------------
        .                           cost: ward_cost,
        .                       };
        .                       self.stack.push(
        .                           TriggerPush::new(perm_id, ward_controller, effect)
        .                               .target(Some(Target::Permanent(target_for_trigger)))
        .                               .build(),
        .                       );
        .                   }
   56,496 ( 0.00%)      }
        .           
        .               /// `Keyword::CounterFirstTargetingEachTurn` (the Glasskite cycle, and
        .               /// Kira's granted copy): the first spell or ability to target `perm_id`
        .               /// each turn is countered. Rides the Ward push convention — the trigger's
        .               /// slot 0 carries the targeting spell / ability source. The per-turn flag
        .               /// reuses `triggered_once_per_turn_used` under a `usize::MAX` slot, which
        .               /// no printed trigger index can collide with.
        .               fn push_first_targeting_counter(&mut self, perm_id: CardId, target_for_trigger: CardId) {
        .                   use crate::card::Keyword;
        .                   let key = (perm_id, usize::MAX);
    2,278 ( 0.00%)          if self.triggered_once_per_turn_used.contains(&key) {
        .                       return;
        .                   }
    1,614 ( 0.00%)          let Some(controller) = self.battlefield_find(perm_id).map(|c| c.controller) else {
        .                       return;
        .                   };
        .                   let has = self
    4,842 ( 0.00%)              .computed_permanent(perm_id)
5,202,562 ( 0.17%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (1,614x)
      730 ( 0.00%)              .map(|cp| cp.keywords.to_vec())
        .                       .unwrap_or_else(|| {
        .                           self.battlefield_find(perm_id)
        .                               .map(|c| c.definition.keywords.clone())
        .                               .unwrap_or_default()
        .                       })
        .                       .contains(&Keyword::CounterFirstTargetingEachTurn);
    1,614 ( 0.00%)          if !has {
        .                       return;
        .                   }
        .                   self.triggered_once_per_turn_used.insert(key);
        .                   self.stack.push(
        .                       TriggerPush::new(
        .                           perm_id,
        .                           controller,
        .                           Effect::CounterSpellOrAbility { what: crate::effect::Selector::Target(0) },
-- line 8394 ----------------------------------------
-- line 8399 ----------------------------------------
        .               }
        .           
        .               /// CR 702.21 — Ward enforcement on activated-ability targeting. Hooked
        .               /// into `activate_ability` immediately after the ability is pushed
        .               /// onto the stack as a `StackItem::Trigger`. The Ward trigger's
        .               /// `Effect::CounterUnless` walks the stack for the topmost matching
        .               /// `Trigger` whose `source` is the activating permanent (identifying
        .               /// the ability to counter).
      112 ( 0.00%)      pub(crate) fn push_ward_triggers_for_activated_ability(
        .                   &mut self,
        .                   activator: usize,
        .                   ability_source: CardId,
        .                   target: Option<Target>,
        .               ) {
        .                   let targets: Vec<Target> = target.into_iter().collect();
       48 ( 0.00%)          self.push_ward_triggers_for_targets(activator, ability_source, &targets);
   77,892 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_targets (16x)
        .               }
        .           
        .               /// Push pre-collected `SpellCast`/`SelfSource` triggers from the
        .               /// just-cast card onto the stack as `Trigger` items, so they resolve
        .               /// before the spell itself. Caller is responsible for collecting the
        .               /// effect list before the card moves into the stack item.
        .               /// Push a card's "when you cast this spell" triggers, carrying the cast's
        .               /// `x_value` so an on-cast trigger body can read `Value::XFromCost`
        .               /// (Hydroid Krasis's "gain half X life and draw half X cards").
        .               /// Grip of Chaos — "Whenever a spell or ability is put onto the stack, if
        .               /// it has a single target, reselect its target at random." Called right
        .               /// after a push; a no-op unless a Grip is on the battlefield.
   49,434 ( 0.00%)      pub(crate) fn randomize_single_target_on_stack(&mut self) {
        .                   use crate::effect::StaticEffect;
        .                   use rand::seq::IteratorRandom;
    7,062 ( 0.00%)          if !self.battlefield.iter().any(|c| {
        .                       c.definition
        .                           .static_abilities
        .                           .iter()
        .                           .any(|sa| matches!(sa.effect, StaticEffect::RandomizeSingleTargets))
        .                   }) {
        .                       return;
        .                   }
        .                   let Some(top) = self.stack.last() else { return };
-- line 8438 ----------------------------------------
-- line 8454 ----------------------------------------
        .                       self.enumerate_legal_targets_with_source(&effect, controller, source);
        .                   let Some(pick) = choices.into_iter().choose(&mut self.rng.draw()) else { return };
        .                   match self.stack.last_mut() {
        .                       Some(StackItem::Spell { target, .. }) | Some(StackItem::Trigger { target, .. }) => {
        .                           *target = Some(pick);
        .                       }
        .                       None => {}
        .                   }
   56,496 ( 0.00%)      }
        .           
        .               pub(crate) fn push_on_cast_triggers_x(
        .                   &mut self,
        .                   source: CardId,
        .                   controller: usize,
        .                   triggers: Vec<(Option<crate::card::Predicate>, Effect)>,
        .                   cast_x: u32,
        .               ) {
   35,230 ( 0.00%)          for (filter, effect) in triggers {
        .                       // Evaluate the trigger's filter (Infusion's
        .                       // LifeGainedThisTurnAtLeast, etc.) before pushing. The ctx
        .                       // is the on-cast context where `trigger_source` points to
        .                       // the cast card. If the filter rejects, drop the trigger
        .                       // silently — matches the "won't trigger unless the
        .                       // condition is met" wording.
        .                       if let Some(pred) = &filter {
        .                           let ctx = crate::game::effects::EffectContext {
-- line 8479 ----------------------------------------
-- line 10292 ----------------------------------------
        .               /// True if `player` is restricted to sorcery-only spell timing by an
        .               /// opponent's `StaticEffect::OpponentsSorceryTimingOnly` (Teferi, Time
        .               /// Raveler's static "Each opponent can cast spells only any time they
        .               /// could cast a sorcery"). Walked by `cast_spell` ahead of the
        .               /// is-instant-speed timing check.
        .               pub(crate) fn player_locked_to_sorcery_timing(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
  595,736 ( 0.02%)              c.controller != player
        .                           && c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::OpponentsSorceryTimingOnly)
        .                           })
        .                   })
    2,510 ( 0.00%)      }
        .           
        .               /// CR 702.8 — true if a permanent `player` controls grants `card` flash via
        .               /// a `ControllerSpellsHaveFlash` (filtered) or `ControllerSorceriesAsFlash`
        .               /// (Teferi, Time Raveler; Hypersonic Dragon) static. Excludes the
        .               /// card-intrinsic `SelfFlashIf` path, which each cast site handles inline.
        .               /// CR 601.2b — "you may cast this spell as though it had flash if you pay
        .               /// [cost] more". Returns the surcharge only when the caster is actually
        .               /// outside sorcery timing, so a main-phase cast pays the printed cost.
   22,110 ( 0.00%)      pub fn flash_surcharge_for<'a>(
        .                   &self,
        .                   player: usize,
        .                   card: &'a CardInstance,
        .               ) -> Option<&'a crate::mana::ManaCost> {
        .                   let extra = card.definition.flash_surcharge.as_ref()?;
        .                   (!self.can_cast_sorcery_speed(player) && !self.player_locked_to_sorcery_timing(player))
        .                       .then_some(extra)
   29,480 ( 0.00%)      }
        .           
        .               pub(crate) fn battlefield_grants_flash(&self, player: usize, card: &CardInstance) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   if self.player_locked_to_sorcery_timing(player) {
        .                       return false;
        .                   }
        .                   self.battlefield.iter().any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| match &sa.effect {
-- line 10330 ----------------------------------------
-- line 10347 ----------------------------------------
        .               /// or has Protection from the caster's color identity. For player targets,
        .               /// also checks the `ControllerHasHexproof` static (Leyline of Sanctity).
        .               ///
        .               /// **Ward** — if the target permanent has `Keyword::Ward(n)` and the caster
        .               /// is an opponent, the caster must have `{n}` generic mana available.
        .               /// This check is read-only; use `pay_ward_cost` after a successful check
        .               /// to actually deduct the mana.
        .               pub fn check_target_legality(&self, target: &Target, caster: usize) -> Result<(), GameError> {
   13,112 ( 0.00%)          self.check_target_legality_with_source(target, caster, None)
4,444,326 ( 0.14%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source (1,306x)
        .               }
        .           
        .               /// Same as [`check_target_legality`] but also enforces CR 115.5 — "a
        .               /// spell or ability on the stack is an illegal target for itself" —
        .               /// when `source_card_id` is provided. Used by the cast pipeline for
        .               /// spells like Stifle/Squelch that target stack spells/abilities;
        .               /// passing the casting spell's own `CardId` rejects a self-target
        .               /// at cast time.
        .               /// The body reads the layer system several times over (Shroud,
        .               /// Hexproof, the Artifact Ward keyword scan), each of which would
        .               /// otherwise re-gather every continuous effect in the game; the whole
        .               /// check is `&self`, so one freeze scope covers it.
   76,960 ( 0.00%)      pub(crate) fn check_target_legality_with_source(
        .                   &self,
        .                   target: &Target,
        .                   caster: usize,
        .                   source_card_id: Option<CardId>,
        .               ) -> Result<(), GameError> {
        .                   self.with_frozen_layers(|s| s.check_target_legality_inner(target, caster, source_card_id))
   47,360 ( 0.00%)      }
        .           
        .               fn check_target_legality_inner(
        .                   &self,
        .                   target: &Target,
        .                   caster: usize,
        .                   source_card_id: Option<CardId>,
        .               ) -> Result<(), GameError> {
        .                   // CR 801.4 — objects and players outside the caster's range of
        .                   // influence can't be targeted by their spells or abilities.
   11,840 ( 0.00%)          let out_of_range = match target {
    4,150 ( 0.00%)              Target::Player(p) => !self.player_in_range_of(caster, *p),
   24,900 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::player_in_range_of (830x)
    5,090 ( 0.00%)              Target::Permanent(c) => !self.object_in_range_of(caster, *c),
        .                   };
    1,660 ( 0.00%)          if out_of_range {
        .                       return Err(GameError::InvalidTarget);
        .                   }
        .                   // CR 702.26b — a phased-out permanent is treated as though it doesn't
        .                   // exist, so nothing can target it.
        .                   if let Target::Permanent(c) = target
    5,090 ( 0.00%)              && self.phased_out.iter().any(|p| p.id == *c)
        .                   {
        .                       return Err(GameError::InvalidTarget);
        .                   }
        .                   let cid = match target {
        .                       Target::Player(p) => {
        .                           if self.player_has_static_shroud(*p)
    1,660 ( 0.00%)                      || (*p != caster
        .                                   && self.player_has_static_hexproof(*p)
        .                                   && !self.player_ignores_hexproof(caster))
        .                           {
        .                               return Err(GameError::TargetHasHexproof(crate::card::CardId(0)));
        .                           }
        .                           // Protection from everything (The One Ring) — can't be
        .                           // targeted by any spell or ability.
    2,490 ( 0.00%)                  if self.players[*p].protected_from_everything {
        .                               return Err(GameError::InvalidTarget);
        .                           }
        .                           return Ok(());
        .                       }
        .                       Target::Permanent(c) => c,
        .                   };
        .                   // CR 115.5: A spell or ability on the stack is an illegal target
        .                   // for itself. When the cast pipeline passes its own source id,
        .                   // reject a target matching that id. Catches Spell Burst /
        .                   // hypothetical "counter target spell" trying to point at itself
        .                   // mid-cast.
   10,180 ( 0.00%)          if let Some(src) = source_card_id
        .                       && *cid == src
        .                   {
        .                       return Err(GameError::InvalidTarget);
        .                   }
        .                   let Some(card) = self.battlefield_find(*cid) else {
        .                       // Underworld Cerberus — cards in graveyards can't be targeted.
        .                       if self.graveyard_cards_untargetable()
        .                           && self.players.iter().any(|pl| pl.graveyard.iter().any(|c| c.id == *cid))
-- line 10430 ----------------------------------------
-- line 10431 ----------------------------------------
        .                       {
        .                           return Err(GameError::InvalidTarget);
        .                       }
        .                       return Ok(());
        .                   };
        .                   // Read layer-computed keywords (CR 613) so granted *and* stripped
        .                   // Hexproof/Shroud are honored — e.g. Nowhere to Run removing an
        .                   // opponent's hexproof makes the creature targetable.
    8,328 ( 0.00%)          let controller = card.controller;
        .                   // CR 702.18 — Autumn Willow's waiver makes it targetable by one
        .                   // player's spells and abilities as though it had no shroud.
        .                   // Peace Talks — nothing at all can be targeted for its two turns.
        .                   if self.truce_active() {
        .                       return Err(GameError::TargetHasShroud(*cid));
        .                   }
   24,984 ( 0.00%)          if self.permanent_has_keyword(*cid, &Keyword::Shroud)
11,735,678 ( 0.38%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::permanent_has_keyword (4,164x)
        .                       && !self.shroud_waivers.contains(&(*cid, caster))
        .                   {
        .                       return Err(GameError::TargetHasShroud(*cid));
        .                   }
   24,984 ( 0.00%)          if self.permanent_has_keyword(*cid, &Keyword::Hexproof)
  491,982 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::permanent_has_keyword (4,164x)
        .                       && controller != caster
        .                       && !self.player_ignores_creature_hexproof(caster)
        .                   {
        .                       return Err(GameError::TargetHasHexproof(*cid));
        .                   }
        .                   // Tomik — a player's lands can't be targeted by an opponent's spells
        .                   // or abilities.
   12,492 ( 0.00%)          if card.definition.is_land()
   63,324 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (4,164x)
        .                       && controller != caster
    6,426 ( 0.00%)              && self.player_lands_untargetable_by_opponents(controller)
  150,926 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::actions::<impl crabomination::game::GameState>::player_lands_untargetable_by_opponents (918x)
        .                   {
        .                       return Err(GameError::InvalidTarget);
        .                   }
        .                   // Artifact Ward — "can't be the target of abilities from [filter]
        .                   // sources". Only ability sources reach here with a `source_card_id` on
        .                   // the battlefield; a spell's own cast gate is upstream.
   10,782 ( 0.00%)          if let Some(src) = source_card_id
    4,908 ( 0.00%)              && self
    7,362 ( 0.00%)                  .computed_permanent(*cid)
  152,148 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (2,454x)
        .                           .into_iter()
    1,126 ( 0.00%)                  .flat_map(|cp| cp.keywords.to_vec())
    8,042 ( 0.00%)                  .any(|k| match k {
        .                               Keyword::CantBeTargetedByAbilitiesFromMatching(f) => self
        .                                   .evaluate_requirement_static(&f, &Target::Permanent(src), controller, Some(src)),
        .                               _ => false,
    7,118 ( 0.00%)                  })
   41,718 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::iter::adapters::flatten::FlatMap<core::option::IntoIter<alloc::sync::Arc<crabomination::game::layers::ComputedPermanent>>,alloc::vec::Vec<crabomination_base::card::Keyword>,crabomination::game::GameState::damage_from_source_prevented_by_keyword::{{closure}}>> (2,454x)
        .                   {
        .                       return Err(GameError::InvalidTarget);
        .                   }
        .                   // Ward is enforced via triggered abilities on the stack (CR 702.21a),
        .                   // not as a pre-flight targeting restriction. The caster CAN target a
        .                   // Ward creature — the Ward trigger fires and counters the spell unless
        .                   // the caster pays the Ward cost at resolution time.
        .                   Ok(())
-- line 10485 ----------------------------------------
-- line 10509 ----------------------------------------
        .                   })
        .               }
        .           
        .               /// True while `player` controls a `LandsUntargetableByOpponents` source
        .               /// (Tomik, Distinguished Advokist).
        .               pub(crate) fn player_lands_untargetable_by_opponents(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
   30,060 ( 0.00%)              c.controller == player
        .                           && c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::LandsUntargetableByOpponents)
        .                           })
        .                   })
      918 ( 0.00%)      }
        .           
        .               /// CR 601.2c — Flagbearer. Every battlefield Flagbearer `actor` could
        .               /// legally target, when an opponent of `actor` has the restriction up.
        .               /// Empty when the restriction is inactive, so callers can treat "no
        .               /// candidates" as "no restriction".
        .               pub(crate) fn flagbearer_candidates(&self, actor: usize) -> Vec<CardId> {
        .                   use crate::effect::StaticEffect;
   26,452 ( 0.00%)          let restricted = self.battlefield.iter().any(|c| {
        .                       c.definition
        .                           .static_abilities
        .                           .iter()
        .                           .any(|sa| matches!(sa.effect, StaticEffect::FlagbearersMustBeTargeted))
        .                           && !self.same_team(c.controller, actor)
        .                   });
        .                   if !restricted {
        .                       return Vec::new();
-- line 10538 ----------------------------------------
-- line 10545 ----------------------------------------
        .                               && self.check_target_legality(&Target::Permanent(c.id), actor).is_ok()
        .                       })
        .                       .map(|c| c.id)
        .                       .collect()
        .               }
        .           
        .               /// CR 601.2c — true when `chosen` skips an available Flagbearer that one of
        .               /// `slot_filters` would have accepted ("must choose at least one … if able").
  308,544 ( 0.01%)      pub(crate) fn flagbearer_violation(
        .                   &self,
        .                   actor: usize,
        .                   chosen: &[Target],
        .                   slot_filters: &[Option<crate::card::SelectionRequirement>],
        .               ) -> bool {
        .                   let candidates = self.flagbearer_candidates(actor);
        .                   if candidates.is_empty() {
        .                       return false;
-- line 10561 ----------------------------------------
-- line 10568 ----------------------------------------
        .                   slot_filters.iter().any(|f| {
        .                       candidates.iter().any(|&fb| match f {
        .                           Some(filter) => {
        .                               self.evaluate_requirement(filter, &Target::Permanent(fb), actor)
        .                           }
        .                           None => true,
        .                       })
        .                   })
  231,408 ( 0.01%)      }
        .           
        .               /// True while `player` controls a `ControllerCantCastPermanentSpells`
        .               /// source (Codie, Vociferous Codex).
        .               pub(crate) fn player_cant_cast_permanent_spells(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
  150,204 ( 0.00%)              c.controller == player
        .                           && c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::ControllerCantCastPermanentSpells)
        .                           })
        .                   })
        .               }
        .           
        .               /// True while `player` controls a `ControllerCantCastNoncreatureSpells`
        .               /// static (Nikya of the Old Ways).
   16,600 ( 0.00%)      pub(crate) fn player_cant_cast_noncreature_spells(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.seat_static_sources(player).any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| {
        .                           matches!(sa.effect, StaticEffect::ControllerCantCastNoncreatureSpells)
        .                       })
        .                   })
   19,920 ( 0.00%)      }
        .           
        .               /// True while `player` controls a `ControllerCantCastInstantsOrSorceries`
        .               /// static (Hymn of the Wilds, from the command zone).
   14,310 ( 0.00%)      pub(crate) fn player_cant_cast_instants_or_sorceries(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.seat_static_sources(player).any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| {
        .                           matches!(sa.effect, StaticEffect::ControllerCantCastInstantsOrSorceries)
        .                       })
        .                   })
   17,172 ( 0.00%)      }
        .           
        .               /// CR 506 — Hand to Hand: while any copy is in play and the turn is in a
        .               /// combat step, instants can't be cast and non-mana abilities can't be
        .               /// activated.
   16,184 ( 0.00%)      pub(crate) fn combat_spell_lock_active(&self) -> bool {
        .                   use crate::effect::StaticEffect;
    2,312 ( 0.00%)          self.step.is_combat_phase()
      590 ( 0.00%)              && self.battlefield.iter().any(|c| {
        .                           c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(
        .                                   self.active_static(&sa.effect, c),
        .                                   Some(StaticEffect::NoInstantsOrAbilitiesDuringCombat)
        .                               )
        .                           })
        .                       })
   18,496 ( 0.00%)      }
        .           
        .               /// True when an opponent of `player` controls an
        .               /// `OpponentsCantCastMatching` static whose filter matches `card`
        .               /// (Llawan, Cephalid Empress — "your opponents can't cast blue creature
        .               /// spells").
        .               pub(crate) fn opponent_locks_cast_of(
        .                   &self,
        .                   player: usize,
        .                   card: &crate::card::CardInstance,
        .               ) -> bool {
        .                   use crate::effect::StaticEffect;
    7,456 ( 0.00%)          self.battlefield.iter().any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| {
        .                           matches!(
        .                               &sa.effect,
        .                               StaticEffect::OpponentsCantCastMatching { filter }
        .                                   if self.evaluate_requirement_on_card(filter, card, player)
        .                           )
        .                       }) && !self.same_team(c.controller, player)
        .                   })
        .               }
        .           
        .               /// City in a Bottle — a symmetric `PlayersCantPlayMatching` static in play
        .               /// whose filter matches `card`. Binds every seat, its controller included.
        .               pub(crate) fn play_locked_for_all(&self, player: usize, card: &crate::card::CardInstance) -> bool {
        .                   use crate::effect::StaticEffect;
   24,098 ( 0.00%)          self.battlefield.iter().any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| {
        .                           matches!(
        .                               &sa.effect,
        .                               StaticEffect::PlayersCantPlayMatching { filter }
        .                                   if self.evaluate_requirement_on_card(filter, card, player)
        .                           )
        .                       })
        .                   })
        .               }
        .           
        .               /// True while `player` controls a `ControllerCantCastCreatureSpells`
        .               /// static (Grid Monitor).
        .               pub(crate) fn player_cant_cast_creature_spells(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield.iter().any(|c| {
  132,940 ( 0.00%)              c.controller == player
        .                           && c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::ControllerCantCastCreatureSpells)
        .                           })
        .                   })
    4,136 ( 0.00%)      }
        .           
        .               /// Gaddock Teeg lock — true if `card` is a noncreature spell barred from
        .               /// being cast by some `NoncreatureSpellsCantBeCastIf` static anywhere on
        .               /// the battlefield (global, all players).
   37,280 ( 0.00%)      pub fn noncreature_spell_cast_locked(&self, card: &crate::card::CardDefinition) -> bool {
        .                   use crate::effect::StaticEffect;
   29,824 ( 0.00%)          if card.is_creature() {
  106,888 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (7,456x)
        .                       return false;
        .                   }
    9,960 ( 0.00%)          let mv = card.cost.cmc();
   81,086 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (3,320x)
    6,640 ( 0.00%)          let has_x = card.cost.has_x();
   68,992 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (3,320x)
        .                   self.battlefield.iter().any(|c| {
        .                       c.definition.static_abilities.iter().any(|sa| match sa.effect {
        .                           StaticEffect::NoncreatureSpellsCantBeCastIf { min_mana_value, or_has_x } => {
        .                               mv >= min_mana_value || (or_has_x && has_x)
        .                           }
        .                           // Sanctum Prelate — locked only at the exact chosen mana value.
        .                           StaticEffect::NoncreatureSpellsWithChosenManaValueCantBeCast => {
        .                               c.chosen_number == Some(mv)
        .                           }
        .                           _ => false,
        .                       })
        .                   })
   29,824 ( 0.00%)      }
        .           
        .               /// CR 702.16c — a permanent with protection from a quality can't be the
        .               /// target of an *ability* from a source with that quality. `source` is the
        .               /// ability's source permanent. Mirrors the cast-time spell gate but reads
        .               /// the source's qualities (color / creature-ness / creature type) rather
        .               /// than a spell's colors.
      342 ( 0.00%)      pub fn ability_target_has_protection(&self, target: &Target, source: CardId) -> bool {
       76 ( 0.00%)          let Target::Permanent(tid) = target else {
        .                       // Player target: only the turn-scoped hexproof-from-color grant
        .                       // (Veil of Summer) applies — and only against opponents' abilities.
        .                       if let Target::Player(tp) = target {
        .                           let src_controller = self.battlefield_find(source).map(|c| c.controller);
        .                           if let Some(srcc) = src_controller
       42 ( 0.00%)                      && srcc != *tp
       42 ( 0.00%)                      && !self.players[*tp].hexproof_from_colors_this_turn.is_empty()
       14 ( 0.00%)                      && let Some(src) = self.computed_permanent(source)
        .                           {
        .                               return self.players[*tp]
        .                                   .hexproof_from_colors_this_turn
        .                                   .iter()
        .                                   .any(|c| src.colors.contains(c));
        .                           }
        .                       }
        .                       return false;
        .                   };
      144 ( 0.00%)          let Some(tgt) = self.computed_permanent(*tid) else { return false };
   72,108 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (24x)
       24 ( 0.00%)          let tgt_controller = tgt.controller;
        .                   let src_is_opponent = self
        .                       .battlefield_find(source)
       24 ( 0.00%)              .is_some_and(|c| c.controller != tgt_controller);
        .                   // CR 702.11d — "hexproof from activated and triggered abilities":
        .                   // an opponent's ability simply can't target it (Volatile Stormdrake).
       24 ( 0.00%)          if src_is_opponent
        .                       && tgt.keywords.iter().any(|kw| matches!(kw, Keyword::HexproofFromAbilities))
        .                   {
        .                       return true;
        .                   }
        .                   let printed_hexproof_color = tgt.keywords.iter().any(|kw| {
        .                       matches!(
        .                           kw,
        .                           Keyword::HexproofFromColor(_)
        .                               | Keyword::HexproofFromMonocolored
        .                               | Keyword::HexproofFromMulticolored
        .                       )
        .                   });
       24 ( 0.00%)          let turn_hexproof_color = !self.players[tgt_controller]
        .                       .hexproof_from_colors_this_turn
        .                       .is_empty();
        .                   if !tgt.keywords.iter().any(|kw| {
        .                       matches!(
        .                           kw,
        .                           Keyword::Protection(_)
        .                               | Keyword::ProtectionFromCreatures
        .                               | Keyword::ProtectionFromCreatureType(_)
-- line 10752 ----------------------------------------
-- line 10755 ----------------------------------------
        .                               | Keyword::ProtectionFromManaValueParity { .. }
        .                               | Keyword::ProtectionFromMulticolored
        .                               | Keyword::ProtectionFromMonocolored
        .                               | Keyword::ProtectionFromCardType(_)
        .                               | Keyword::ProtectionFromOwnColors
        .                               | Keyword::ProtectionFromEverything
        .                               | Keyword::HexproofExceptColors(_)
        .                       )
       72 ( 0.00%)          }) && !(src_is_opponent && (printed_hexproof_color || turn_hexproof_color))
        .                   {
        .                       return false;
        .                   }
        .                   let Some(src) = self.computed_permanent(source) else { return false };
        .                   let src_is_creature = src.card_types.contains(&CardType::Creature);
        .                   let src_mv = self
        .                       .battlefield_find(source)
        .                       .map(|c| c.definition.cost.cmc())
-- line 10771 ----------------------------------------
-- line 10811 ----------------------------------------
        .                       Keyword::ProtectionFromEverything => true,
        .                       // "Abilities from nongreen sources opponents control can't target
        .                       // this" (Thrun) — opponent's source sharing none of the colors.
        .                       Keyword::HexproofExceptColors(colors) => {
        .                           src_is_opponent && !colors.iter().any(|c| src.colors.contains(c))
        .                       }
        .                       _ => false,
        .                   })
      304 ( 0.00%)      }
        .           
        .               /// Card types `player` has protection from via
        .               /// `StaticEffect::YouAndCreaturesProtectionFromChosenCardType`
        .               /// permanents they control (Serra's Emissary).
        .               pub(crate) fn player_protection_card_types(&self, player: usize) -> Vec<crate::card::CardType> {
        .                   use crate::effect::StaticEffect;
        .                   self.battlefield
        .                       .iter()
  177,570 ( 0.01%)              .filter(|c| c.controller == player)
        .                       .filter(|c| {
        .                           c.definition.static_abilities.iter().any(|sa| {
        .                               matches!(sa.effect, StaticEffect::YouAndCreaturesProtectionFromChosenCardType)
        .                           })
        .                       })
        .                       .filter_map(|c| c.chosen_card_type.clone())
        .                       .collect()
        .               }
        .           
        .               /// True if `player` controls any permanent granting "you have hexproof"
        .               /// via `StaticEffect::ControllerHasHexproof` (Leyline of Sanctity).
        .               pub fn player_has_static_hexproof(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
    1,660 ( 0.00%)          self.players.get(player).is_some_and(|p| p.hexproof_until_next_turn)
        .                       || self.battlefield.iter().any(|c| {
   15,288 ( 0.00%)                  c.controller == player
        .                               && c.definition
        .                                   .static_abilities
        .                                   .iter()
        .                                   .any(|sa| matches!(sa.effect, StaticEffect::ControllerHasHexproof))
        .                       })
        .               }
        .           
        .               /// Peace Talks — true while its two-turn truce is live: no creature can
-- line 10852 ----------------------------------------
-- line 10860 ----------------------------------------
        .               /// spells and abilities, and no ignore-hexproof static pierces it.
        .               pub fn player_has_static_shroud(&self, player: usize) -> bool {
        .                   use crate::effect::StaticEffect;
        .                   // Peace Talks blankets every player for its two turns.
        .                   if self.truce_active() {
        .                       return true;
        .                   }
        .                   // Gilded Light's turn-scoped grant rides the same check.
    1,660 ( 0.00%)          if self.players.get(player).is_some_and(|p| p.shroud_this_turn) {
        .                       return true;
        .                   }
        .                   self.battlefield.iter().any(|c| {
   15,288 ( 0.00%)              c.controller == player
        .                           && c.definition
        .                               .static_abilities
        .                               .iter()
        .                               .any(|sa| matches!(sa.effect, StaticEffect::ControllerHasShroud))
        .                   })
        .               }
        .           
        .               /// True if `player` controls a permanent granting "ignore opponents'
-- line 10880 ----------------------------------------
-- line 10966 ----------------------------------------
        .                   _is_noncreature: bool,
        .                   mana_spent: u32,
        .                   converged_value: u32,
        .               ) {
        .                   use crate::effect::{EventKind, EventScope};
        .                   // CR 603.7e — one-shot "when you cast your next spell this turn"
        .                   // delayed triggers (Codie). Fire each matching watcher once, with
        .                   // the cast spell bound as the trigger source, and consume it.
    7,046 ( 0.00%)          let cast_is_is = self.find_card_anywhere(cast_card).is_some_and(|c| {
3,864,234 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,046x)
        .                       c.definition.card_types.contains(&crate::card::CardType::Instant)
        .                           || c.definition.card_types.contains(&crate::card::CardType::Sorcery)
        .                   });
   63,414 ( 0.00%)          let (next_cast, rest): (Vec<_>, Vec<_>) = std::mem::take(&mut self.delayed_triggers)
        .                       .into_iter()
    7,046 ( 0.00%)              .partition(|dt| {
  479,128 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:core::iter::traits::iterator::Iterator::partition (7,046x)
        .                           dt.controller == controller
        .                               && (matches!(
        .                                   dt.kind,
        .                                   crate::game::types::DelayedKind::YourNextSpellCastThisTurn
        .                               ) || (cast_is_is
        .                                   && matches!(
        .                                       dt.kind,
        .                                       crate::game::types::DelayedKind::YourNextInstantSorceryCastThisTurn
        .                                   )))
        .                       });
   28,184 ( 0.00%)          self.delayed_triggers = rest;
        .                   // Expose the cast spell's mana value so bodies can gate on it
        .                   // (Vivien, Monsters' Advocate — "a creature card with lesser mana
        .                   // value" via `ManaValueLessThanEventAmount`).
        .                   let cast_mv = self
   21,138 ( 0.00%)              .find_card_anywhere(cast_card)
3,864,234 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,046x)
   14,092 ( 0.00%)              .map(|c| c.definition.cost.cmc())
  194,740 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (7,046x)
        .                       .unwrap_or(0);
   56,368 ( 0.00%)          for dt in next_cast {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .trigger_source(Some(crate::game::effects::EntityRef::Card(cast_card)))
        .                               .event_amount(cast_mv)
        .                               .build(),
        .                       );
        .                       // Repeating watchers ("whenever you cast a spell this turn",
        .                       // Rediscover the Way III) survive until cleanup clears them.
-- line 11007 ----------------------------------------
-- line 11009 ----------------------------------------
        .                           self.delayed_triggers.push(dt);
        .                       }
        .                   }
        .                   // CR 603.7e (name-gated) — "when you cast a spell with the chosen name
        .                   // for the first time this turn" (Medomai's Prophecy III). Only a cast
        .                   // whose name matches the watching source's `named_card` consumes the
        .                   // one-shot; other casts leave it armed.
        .                   let cast_name = self
    7,046 ( 0.00%)              .find_card_anywhere(cast_card)
3,864,234 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (7,046x)
   14,092 ( 0.00%)              .map(|c| c.definition.name.to_string());
   56,368 ( 0.00%)          let (named_fire, rest): (Vec<_>, Vec<_>) = std::mem::take(&mut self.delayed_triggers)
        .                       .into_iter()
   63,414 ( 0.00%)              .partition(|dt| {
  472,082 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs:core::iter::traits::iterator::Iterator::partition (7,046x)
        .                           dt.controller == controller
        .                               && matches!(dt.kind, crate::game::types::DelayedKind::YourNextNamedSpellThisTurn)
        .                               && self
        .                                   .battlefield_find(dt.source)
        .                                   .and_then(|s| s.named_card.clone())
        .                                   == cast_name
        .                       });
   28,184 ( 0.00%)          self.delayed_triggers = rest;
   56,368 ( 0.00%)          for dt in named_fire {
        .                       self.stack.push(
        .                           TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                               .trigger_source(Some(crate::game::effects::EntityRef::Card(cast_card)))
        .                               .build(),
        .                       );
        .                   }
        .                   // One battlefield pass for the ability-strip presence gate and both
        .                   // grant lists (the dispatcher's `DispatchScan`), instead of one walk
        .                   // each. CR 113.10b — permanents under a "loses all abilities"
        .                   // continuous effect (Mercurial Transformation / Turn to Frog) don't
        .                   // fire printed Magecraft / spell-cast triggers, so the stripped set
        .                   // drops those listeners below; it is empty on almost every board,
        .                   // which a `Vec` answers with a length check and a `HashSet` answers
        .                   // with a hash.
   14,092 ( 0.00%)          let scan = self.dispatch_board_scan();
3,037,874 ( 0.10%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_board_scan (7,046x)
   28,184 ( 0.00%)          let stripped = self.permanents_with_abilities_removed(scan.strip_on_battlefield);
  838,474 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::permanents_with_abilities_removed (7,046x)
        .                   // Whether the cast spell came from its caster's hand, read off the
        .                   // stack item. Stamped into the trigger context so
        .                   // `Predicate::CastFromHand` (Quandrix, the Proof) reflects the
        .                   // actual cast: cascade / flashback / exile casts read `false`,
        .                   // which stops "spells you cast from your hand have cascade" from
        .                   // re-triggering on the spells it cascades into.
    7,046 ( 0.00%)          let cast_from_hand = self
        .                       .stack
        .                       .iter()
   15,356 ( 0.00%)              .find_map(|item| match item {
    7,672 ( 0.00%)                  crate::game::types::StackItem::Spell { card, .. } if card.id == cast_card => {
    7,046 ( 0.00%)                      Some(card.cast_from_hand)
        .                           }
        .                           _ => None,
        .                       })
        .                       .unwrap_or(true);
        .                   // Walk every permanent on the battlefield — `YourControl` triggers
        .                   // fire from the caster's permanents, while `OpponentControl` triggers
        .                   // fire from non-caster permanents (Wandering Archaic etc.). The
        .                   // ability's effective controller is its own permanent's controller,
        .                   // *not* the spell-caster's index.
        .                   // A SpellCast trigger applies iff its scope matches the caster.
      402 ( 0.00%)          let scope_matches = |scope: EventScope, c_controller: usize| match scope {
      402 ( 0.00%)              EventScope::YourControl => c_controller == controller,
        .                       EventScope::OpponentControl => c_controller != controller,
        .                       EventScope::AnyPlayer => true,
        .                       _ => false,
        .                   };
        .                   // Walk printed triggers AND statics-/equip-granted ones (Red Mage's
        .                   // Rapier's "equipped creature has 'whenever you cast a noncreature
        .                   // spell, …'", Sliver-granted Magecraft): granted abilities fire as
        .                   // though printed on the host. Granted triggers use a sentinel index and
        .                   // are never once-per-turn.
        .                   #[allow(clippy::type_complexity)]
        .                   let mut candidates: Vec<(CardId, usize, Effect, Option<crate::effect::Predicate>, usize, bool)> =
        .                       Vec::new();
   28,184 ( 0.00%)          let trigger_grants = scan.trigger_grants;
        .                   let equip_grants = scan.equip_grants;
        .                   // Presence gates for the two per-card grant walks: on a board that
        .                   // grants nothing each returns empty for every permanent (the
        .                   // dispatcher's device). The station leg of
        .                   // `statics_granted_triggers_with` is per-card, so it stays per-card.
   21,138 ( 0.00%)          let any_static_grant = !trigger_grants.is_empty() || !self.turn_granted_triggers.is_empty();
        .                   let any_equip_grant = !equip_grants.is_empty();
    7,046 ( 0.00%)          for c in self.battlefield.iter() {
        .                       if stripped.contains(&c.id) {
        .                           continue;
        .                       }
  242,412 ( 0.01%)              let (cid, c_controller) = (c.id, c.controller);
        .                       for (idx, t) in c.definition.triggered_abilities.iter().enumerate() {
    1,206 ( 0.00%)                  if t.event.kind == EventKind::SpellCast && scope_matches(t.event.scope, c_controller) {
    3,610 ( 0.00%)                      candidates.push((cid, c_controller, t.effect.clone(), t.event.filter.clone(), idx, t.event.once_per_turn));
   20,330 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (380x)
   28,690 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (190x)
        .                           }
        .                       }
  484,824 ( 0.02%)              let static_granted = if any_static_grant || !c.definition.station.is_empty() {
        .                           self.statics_granted_triggers_with(c, &trigger_grants)
        .                       } else {
        .                           Vec::new()
        .                       };
  121,206 ( 0.00%)              let equip_granted = if any_equip_grant {
   25,486 ( 0.00%)                  self.equip_granted_triggers_with(c, &equip_grants)
        .                       } else {
        .                           Vec::new()
        .                       };
1,575,678 ( 0.05%)              for t in static_granted.into_iter().chain(equip_granted) {
        .                           if t.event.kind == EventKind::SpellCast && scope_matches(t.event.scope, c_controller) {
        .                               candidates.push((cid, c_controller, t.effect, t.event.filter, usize::MAX, false));
        .                           }
        .                       }
        .                   }
        .                   // CR 902.5 — a Vanguard avatar's cast trigger fires from the command
        .                   // zone (Serra Angel Avatar's "whenever you cast a spell, gain 2 life").
        .                   for (seat, pl) in self.players.iter().enumerate() {
   28,184 ( 0.00%)              for c in pl.command.iter().filter(|c| c.command_zone_abilities_active()) {
        .                           for t in &c.definition.triggered_abilities {
        .                               if t.event.kind == EventKind::SpellCast && scope_matches(t.event.scope, seat) {
        .                                   candidates.push((
        .                                       c.id,
        .                                       seat,
        .                                       t.effect.clone(),
        .                                       t.event.filter.clone(),
        .                                       usize::MAX,
-- line 11128 ----------------------------------------
-- line 11139 ----------------------------------------
        .                   let gy_casters: Vec<(CardId, usize)> = self
        .                       .players
        .                       .iter()
        .                       .enumerate()
        .                       .flat_map(|(owner, pl)| pl.graveyard.iter().map(move |c| (c.id, owner)))
        .                       .filter(|&(_, owner)| owner == controller)
        .                       .collect();
        .                   for (cid, owner) in gy_casters {
   73,088 ( 0.00%)              let Some(c) = self.players[owner].graveyard.iter().find(|c| c.id == cid) else {
        .                           continue;
        .                       };
        .                       for t in &c.definition.triggered_abilities {
        .                           if t.event.kind == EventKind::SpellCast
      284 ( 0.00%)                      && matches!(t.event.scope, EventScope::FromYourGraveyard)
        .                           {
        .                               candidates.push((cid, owner, t.effect.clone(), t.event.filter.clone(), usize::MAX, false));
        .                           }
        .                       }
        .                   }
        .           
   58,838 ( 0.00%)          for (source, listener_controller, effect, filter, trig_idx, once_per_turn) in candidates {
   20,330 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (380x)
        .                       // CR 603.3d — "This ability triggers only once each turn"
        .                       // (Whispering Wizard, Welcoming Vampire-style SpellCast payoffs).
        .                       let once_key = (source, trig_idx);
      380 ( 0.00%)              if once_per_turn && self.triggered_once_per_turn_used.contains(&once_key) {
        .                           continue;
        .                       }
    1,710 ( 0.00%)              if let Some(filter) = filter {
    5,700 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (190x)
    5,700 ( 0.00%)                  let ctx = crate::game::effects::EffectContext {
        .                               controller: listener_controller,
        .                               source: Some(source),
        .                               targets: vec![],
        .                               trigger_source: Some(crate::game::effects::EntityRef::Card(cast_card)),
        .                               mode: 0,
        .                               x_value: 0,
        .                               converged_value,
        .                               mana_spent,
-- line 11175 ----------------------------------------
-- line 11182 ----------------------------------------
        .                               kick_count: 0,
        .                               bargained: false,
        .                               cast_via_mayhem: false,
        .                               cast_via_waterbend: false,
        .                               cast_collected_evidence: false,
        .                               entwined: false,
        .                               spree_modes: Vec::new(),
        .                           };
      950 ( 0.00%)                  if !self.evaluate_predicate(&filter, &ctx) {
  201,255 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_predicate (190x)
        .                               continue;
        .                           }
      482 ( 0.00%)              }
   31,920 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Predicate> (190x)
        .                       // The trigger is firing this cast — consume its once-per-turn slot.
      176 ( 0.00%)              if once_per_turn {
        .                           self.triggered_once_per_turn_used.insert(once_key);
        .                       }
        .                       let auto_target = self.auto_target_for_effect_avoiding(
        .                           &effect,
        .                           listener_controller,
        .                           Some(source),
        .                       );
        .                       // CR 700.2b — pick the mode at push time if the trigger is modal.
        .                       // Powers Prismari Apprentice's modal Magecraft (Scry 1 / +1/+0 EOT):
        .                       // AutoDecider picks mode 0 (Scry); ScriptedDecider::new([Mode(1)])
        .                       // exercises the pump branch.
      528 ( 0.00%)              let mode = self.pick_trigger_mode(&effect, source, listener_controller);
    4,048 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pick_trigger_mode (88x)
        .                       // The cast spell's mana value, so "where X is that spell's mana
        .                       // value" riders scale (Shark Typhoon).
       88 ( 0.00%)              let spell_mv = self
        .                           .stack
        .                           .iter()
      200 ( 0.00%)                  .find_map(|si| match si {
       94 ( 0.00%)                      StackItem::Spell { card, .. } if card.id == cast_card => {
      352 ( 0.00%)                          Some(card.definition.cost.cmc())
    1,584 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:crabomination_base::mana::ManaCost::cmc (88x)
        .                               }
        .                               _ => None,
        .                           })
        .                           .unwrap_or(0);
        .                       // CR 603.x — Harmonic Prodigy / Veyran / Katara: a SpellCast
        .                       // (Magecraft) trigger of a matching-subtype permanent fires an
        .                       // additional time per doubler the controller controls.
       88 ( 0.00%)              let fires = 1 + ally_trigger_extra_fires(self, listener_controller, source);
  211,672 ( 0.01%)  => crabomination/src/game/actions.rs:crabomination::game::actions::ally_trigger_extra_fires (88x)
        .                       for _ in 0..fires {
      176 ( 0.00%)                  self.stack.push(
      264 ( 0.00%)                      TriggerPush::new(source, listener_controller, effect.clone())
   13,288 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (88x)
        .                                   .target(auto_target.clone())
        .                                   .mode(mode)
        .                                   // The cast spell's converge count, so per-cast
        .                                   // `Value::ConvergedValue` reads the iterated spell
        .                                   // (Magmablood / Wildgrowth Archaic).
        .                                   .converged_value(converged_value)
        .                                   // Preserve the cast spell's id for Effect::CopySpell /
        .                                   // Selector::CastSpellTarget.
        .                                   .trigger_source(Some(crate::game::effects::EntityRef::Card(cast_card)))
        .                                   .mana_spent(mana_spent)
        .                                   .event_amount(spell_mv)
        .                                   .build(),
        .                           );
        .                       }
      380 ( 0.00%)          }
   11,970 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (190x)
        .               }
        .           
        .               // ── Payment snapshot / restore ───────────────────────────────────────────
        .           
        .               /// Capture mana pool + tapped state of every permanent owned by `payer`.
        .               /// Used by the cast/activate/counter paths so a payment that fails
        .               /// mid-way (after auto-tap has already tapped lands) can be reverted to
        .               /// pristine state.
   80,028 ( 0.00%)      pub(crate) fn snapshot_payment_state(&self, payer: usize) -> PaymentSnapshot {
  160,056 ( 0.01%)          PaymentSnapshot {
        .                       pool: self.players[payer].mana_pool.clone(),
        .                       tapped: self
        .                           .battlefield
        .                           .iter()
  333,132 ( 0.01%)                  .filter(|c| c.owner == payer)
  227,484 ( 0.01%)                  .map(|c| (c.id, c.tapped))
        .                           .collect(),
        .                   }
   71,136 ( 0.00%)      }
        .           
        .               /// Restore the mana pool and tapped state captured by a prior
        .               /// `snapshot_payment_state`. Skips cards that have since left the
        .               /// battlefield (the caller is responsible for any zone-change rollback).
    2,916 ( 0.00%)      pub(crate) fn restore_payment_state(&mut self, payer: usize, snapshot: PaymentSnapshot) {
    6,156 ( 0.00%)          self.players[payer].mana_pool = snapshot.pool;
    9,720 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (324x)
    1,296 ( 0.00%)          for (id, was_tapped) in snapshot.tapped {
        .                       if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == id) {
    4,132 ( 0.00%)                  c.tapped = was_tapped;
        .                       }
        .                   }
        .               }
        .           
        .               /// Snapshot, auto-tap, and pay `cost` atomically. Returns the auto-tap
        .               /// events plus the pre-payment pool snapshot (callers like `cast_spell`
        .               /// use it to compute converge). On payment failure the snapshot is
        .               /// restored — pool and tapped flags revert to the pre-call state.
        .               /// CR 702.8 — may `p` cast `card` at instant speed? A card-intrinsic
        .               /// `SelfFlashIf`, a battlefield grant (Sigarda's Aid), a flash *surcharge*
        .               /// (Vedalken Orrery-style taxes), or Winding Canyons' turn-scoped
        .               /// creature-spell permission. The six cast entry points share this.
   82,016 ( 0.00%)      pub(crate) fn flash_granted_for(&self, p: usize, card: &crate::card::CardInstance) -> bool {
    7,456 ( 0.00%)          let self_flash = !self.player_locked_to_sorcery_timing(p)
        .                       && card.definition.static_abilities.iter().any(|sa| {
        .                           if let crate::effect::StaticEffect::SelfFlashIf { condition } = &sa.effect {
        .                               let ctx = crate::game::effects::EffectContext::for_spell(p, None, 0, 0);
        .                               self.evaluate_predicate(condition, &ctx)
        .                           } else {
        .                               false
        .                           }
        .                       });
        .                   self_flash
        .                       || self.battlefield_grants_flash(p, card)
        .                       || self.flash_surcharge_for(p, card).is_some()
   22,368 ( 0.00%)              || (self.players[p].creature_spells_as_flash_this_turn
        .                           && card.definition.is_creature())
   59,648 ( 0.00%)      }
        .           
        .               pub(crate) fn try_pay_with_auto_tap(
        .                   &mut self,
        .                   payer: usize,
        .                   cost: &crate::mana::ManaCost,
        .               ) -> Result<PaymentReceipt, GameError> {
        .                   self.try_pay_with_auto_tap_mode(payer, cost, false)
        .               }
-- line 11305 ----------------------------------------
-- line 11322 ----------------------------------------
        .               /// spend-restricted mana recognize what it's funding.
        .               pub(crate) fn try_pay_with_auto_tap_kind(
        .                   &mut self,
        .                   payer: usize,
        .                   cost: &crate::mana::ManaCost,
        .                   forced_only: bool,
        .                   kind: &crate::mana::SpellKind,
        .               ) -> Result<PaymentReceipt, GameError> {
    6,088 ( 0.00%)          let snapshot = self.snapshot_payment_state(payer);
2,301,475 ( 0.07%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::snapshot_payment_state (1,522x)
        .                   // These paths don't pose the float-spend confirmation, so pass `None`.
   16,742 ( 0.00%)          self.try_pay_after_snapshot_mode(payer, cost, snapshot, forced_only, kind, None)
7,470,643 ( 0.24%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::try_pay_after_snapshot_mode (1,522x)
        .               }
        .           
        .               /// CR 601.2g — the pre-existing floating mana that paying `cost` would sweep
        .               /// onto the *generic* portion: the player's pool minus the mana that matches
        .               /// the cost's colored / `{C}` pips (which they'd spend on those pips anyway).
        .               /// This "excess" is the only float that's discretionary — what the spend
        .               /// confirmation is actually about ("keep your leftover {R}, or spend it?").
        .               pub(crate) fn protectable_float(
-- line 11340 ----------------------------------------
-- line 11427 ----------------------------------------
        .               ///   *could have contributed to this cost* remains. If a relevant
        .               ///   untapped source is left over, the player had a real choice, so the
        .               ///   cast is rejected with `ManualTapRequired` (rolled back) and they
        .               ///   tap manually before re-submitting.
        .               ///
        .               /// `forced_only` is false for bots, scripted tests, and engine-driven
        .               /// auto-pays (Counter-unless-paid, "pay X or sacrifice"), which keep
        .               /// the original full auto-tap behavior.
  124,488 ( 0.00%)      pub(crate) fn try_pay_after_snapshot_mode(
        .                   &mut self,
        .                   payer: usize,
        .                   cost: &crate::mana::ManaCost,
        .                   snapshot: PaymentSnapshot,
        .                   forced_only: bool,
        .                   kind: &crate::mana::SpellKind,
        .                   // CR 601.2g float-spend choice: `Some(false)` = the player declined to
        .                   // spend their pre-existing floating mana, so pay from freshly-tapped
-- line 11443 ----------------------------------------
-- line 11446 ----------------------------------------
        .                   // `forced_only` (human) path.
        .                   spend_float: Option<bool>,
        .               ) -> Result<PaymentReceipt, GameError> {
        .                   // CR 609.4b — Mycosynth Lattice's "players may spend mana as though it
        .                   // were mana of any color": relax the coloured pips before anything
        .                   // downstream (auto-tap, float protection, `pay_for_spell`) reads them.
        .                   let relaxed = self.relax_cost_colors_for_spell(Some(payer), cost, kind);
        .                   let cost: &crate::mana::ManaCost = &relaxed;
   26,676 ( 0.00%)          if forced_only {
        .                       // "Keep my leftover floating mana": lift out only the *excess*
        .                       // float (the off-pip mana that would hit the generic), pay the cost
        .                       // from the remaining pip-matching float + freshly-tapped sources,
        .                       // then restore the excess. Pip-matching float is still spent on its
        .                       // pip (auto-tap accounts for the pool). We only reach here after the
        .                       // cast confirmed this is payable (see `float_spend_is_optional`); if
        .                       // it somehow can't, restore the excess and fall through to spending
        .                       // it (it was forced after all).
-- line 11462 ----------------------------------------
-- line 11556 ----------------------------------------
        .                           }
        .                           Err(e) => {
        .                               self.restore_payment_state(payer, snapshot);
        .                               Err(GameError::Mana(e))
        .                           }
        .                       };
        .                   }
        .           
   26,676 ( 0.00%)          let auto_events = self.auto_tap_for_cost_filtered(payer, cost, kind.creature_mana_only);
        .                   // Snapshot the pool *after* auto-tap so `pool_before` reflects the
        .                   // mana actually available to `pay()`. Without this, a player who
        .                   // starts with an empty pool and auto-taps lands to cover the cost
        .                   // shows mana_spent = 0 (pre-auto-tap 0 → post-pay 0), which silently
        .                   // breaks Increment / Opus / converge payoffs that read the
        .                   // difference. The original snapshot is still used for rollback.
    8,892 ( 0.00%)          let pool_after_auto_tap = self.players[payer].mana_pool.clone();
  106,704 ( 0.00%)          match self.players[payer].mana_pool.pay_for_spell(cost, kind) {
5,014,157 ( 0.16%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (8,892x)
3,578,998 ( 0.11%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::pay_for_spell (8,892x)
   68,544 ( 0.00%)              Ok(side_effects) => Ok(PaymentReceipt {
  257,040 ( 0.01%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (8,568x)
   34,272 ( 0.00%)                  auto_events,
        .                           side_effects,
  128,520 ( 0.00%)                  pool_before: pool_after_auto_tap,
        .                       }),
        .                       Err(e) => {
        .                           // Channel — convert life 1:1 into the colorless shortfall
        .                           // ("you may pay 1 life: add {C}", active until end of turn).
      972 ( 0.00%)                  if self.players[payer].channel_life_for_mana {
        .                               let life = self.players[payer].life.max(0) as u32;
        .                               for n in 1..=life {
        .                                   let mut probe = self.players[payer].mana_pool.clone();
        .                                   probe.add_colorless(n);
        .                                   if probe.pay_for_spell(cost, kind).is_err() {
        .                                       continue;
        .                                   }
        .                                   self.pay_life_cost(payer, n);
-- line 11589 ----------------------------------------
-- line 11591 ----------------------------------------
        .                                   let pool_before = self.players[payer].mana_pool.clone();
        .                                   let side_effects = self.players[payer]
        .                                       .mana_pool
        .                                       .pay_for_spell(cost, kind)
        .                                       .expect("probe covered the cost a line ago");
        .                                   return Ok(PaymentReceipt { auto_events, side_effects, pool_before });
        .                               }
        .                           }
    1,296 ( 0.00%)                  self.restore_payment_state(payer, snapshot);
3,121,583 ( 0.10%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::restore_payment_state (324x)
    2,916 ( 0.00%)                  Err(GameError::Mana(e))
        .                       }
        .                   }
   80,352 ( 0.00%)      }
        .           
        .               /// True if `player` controls an untapped mana source that *could have
        .               /// contributed* to `cost` — used by the forced-only payment path to
        .               /// detect that a manual tapping choice existed. A source is relevant
        .               /// when the cost has a generic (or monocolored-hybrid) pip that any
        .               /// mana satisfies, or when the source can produce a color the cost
        .               /// requires.
        .               fn untapped_relevant_source_exists(&self, player: usize, cost: &crate::mana::ManaCost) -> bool {
-- line 11611 ----------------------------------------
-- line 11848 ----------------------------------------
        .               /// "spend only mana produced by creatures" cast (Myr Superion) never
        .               /// strands itself by tapping lands it can't spend.
        .               pub fn auto_tap_for_cost_filtered(
        .                   &mut self,
        .                   player: usize,
        .                   cost: &crate::mana::ManaCost,
        .                   creature_only: bool,
        .               ) -> Vec<GameEvent> {
    8,892 ( 0.00%)          let prev_priority = self.priority.player_with_priority;
   17,784 ( 0.00%)          self.priority.player_with_priority = player;
   17,784 ( 0.00%)          let events = self.auto_tap_for_cost_inner(player, cost, creature_only);
404,835,635 (12.99%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::auto_tap_for_cost_inner (8,892x)
    8,892 ( 0.00%)          self.priority.player_with_priority = prev_priority;
        .                   events
        .               }
        .           
        .               /// Preference rank for an auto-tap mana source: lower is tapped first.
        .               /// Plain "{T}: add mana" sources cost nothing but the tap; life and
        .               /// self-consuming sources (painlands, Lotus Petal, Chromatic Star) are
        .               /// held back so a generic pip never eats a one-shot artifact or a
        .               /// chunk of life while an ordinary land sits untapped.
        .               fn mana_source_cost_rank(a: &crate::effect::ActivatedAbility) -> u8 {
  280,728 ( 0.01%)          if a.sac_cost || a.exile_self_cost || a.discard_cost.is_some() {
        .                       2
   93,576 ( 0.00%)          } else if a.life_cost > 0 || a.energy_cost > 0 {
        .                       1
        .                   } else {
        .                       0
        .                   }
        .               }
        .           
        .               /// One pass over the battlefield collecting every untapped mana
        .               /// source this player controls, with the colours each can make.
-- line 11879 ----------------------------------------
-- line 11908 ----------------------------------------
        .                       .collect()
        .               }
        .           
        .               /// Frozen: every untapped permanent asks `effective_mana_abilities`, and
        .               /// each of those runs `printed_land_mana_ability_lost_with` and
        .               /// `intrinsic_land_mana_abilities`, both of which take a layer pass. One
        .               /// scope makes the whole table share one gather and one
        .               /// `ComputedPermanent` per card.
   66,330 ( 0.00%)      fn mana_source_table(&self, player: usize, creature_only: bool) -> Vec<ManaSourceInfo> {
        .                   self.with_frozen_layers(|g| g.mana_source_table_inner(player, creature_only))
   51,590 ( 0.00%)      }
        .           
        .               fn mana_source_table_inner(&self, player: usize, creature_only: bool) -> Vec<ManaSourceInfo> {
        .                   // One board-level grant scan for the whole table instead of one per
        .                   // untapped permanent (see `grant_scan`).
   22,110 ( 0.00%)          let scan = self.grant_scan();
5,851,077 ( 0.19%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::grant_scan (7,370x)
    7,370 ( 0.00%)          self.battlefield
        .                       .iter()
  376,056 ( 0.01%)              .filter(|c| c.controller == player && !c.tapped)
  109,140 ( 0.00%)              .filter(|c| !creature_only || self.permanent_is_creature(c.id))
        .                       .filter_map(|c| {
  109,140 ( 0.00%)                  let abilities = self.effective_mana_abilities_with(c.id, &scan);
67,284,555 ( 2.16%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_mana_abilities_with (54,570x)
        .                           let (first_idx, first) = abilities.first()?;
        .                           let mut colors = crate::mana::ColorSet::empty();
        .                           let mut color_idx = [0usize; 5];
        .                           for col in ManaColor::ALL {
        .                               if let Some((i, _)) =
  701,820 ( 0.02%)                          abilities.iter().find(|(_, a)| effect_produces_color(&a.effect, col))
9,965,844 ( 0.32%)  => crabomination/src/game/actions.rs:crabomination::game::actions::effect_produces_color (233,940x)
        .                               {
        .                                   colors.insert(col);
   46,788 ( 0.00%)                          color_idx[color_index(col)] = *i;
        .                               }
        .                           }
  655,032 ( 0.02%)                  Some(ManaSourceInfo {
        .                               id: c.id,
        .                               first_idx: *first_idx,
        .                               rank: Self::mana_source_cost_rank(first),
        .                               colors,
        .                               color_idx,
        .                           })
        .                       })
        .                       .collect()
        .               }
        .           
   97,812 ( 0.00%)      fn auto_tap_for_cost_inner(
        .                   &mut self,
        .                   player: usize,
        .                   cost: &crate::mana::ManaCost,
        .                   creature_only: bool,
        .               ) -> Vec<GameEvent> {
        .                   let mut events = Vec::new();
        .                   // Off, the two selection loops below fall back to "cheapest
        .                   // activation, then battlefield order" — the historical behaviour,
        .                   // kept as the ladder control.
   26,676 ( 0.00%)          let smart = self.players[player].smart_tap;
        .           
        .                   // Deduct what the pool already covers before deciding what to tap.
        .                   // We track a "virtual" pool snapshot so we don't mutate the real pool here.
        .                   let pool = &self.players[player].mana_pool;
        .                   // Five fixed keys indexed by `color_index`, not a hash table: this
        .                   // runs once per auto-tap and every cost symbol probed it.
        .                   let mut avail = [0u32; 5];
        .                   for c in ManaColor::ALL {
   17,784 ( 0.00%)              avail[color_index(c)] = pool.amount(c);
        .                   }
        .                   let mut avail_colorless = pool.colorless_amount();
        .           
        .                   let mut still_need_colors: Vec<ManaColor> = Vec::new();
        .                   // Hybrid pips are resolved after the fixed-color pass so the
        .                   // pool is drained by the more-constrained colored pips first.
        .                   let mut hybrids: Vec<(ManaColor, ManaColor)> = Vec::new();
        .                   let mut generic: u32 = 0;
        .           
        .                   for sym in &cost.symbols {
   90,972 ( 0.00%)              match sym {
        .                           ManaSymbol::Colored(c) => {
    9,766 ( 0.00%)                      let have = &mut avail[color_index(*c)];
   29,298 ( 0.00%)                      if *have > 0 { *have -= 1; } else { still_need_colors.push(*c); }
        .                           }
        .                           ManaSymbol::Hybrid(a, b) => hybrids.push((*a, *b)),
        .                           ManaSymbol::Phyrexian(c) => {
        .                               // Pool covers it if available; otherwise paid with life — no tapping.
        .                               let have = &mut avail[color_index(*c)];
        .                               if *have > 0 { *have -= 1; }
        .                           }
        .                           ManaSymbol::PhyrexianHybrid(a, b) => {
-- line 11993 ----------------------------------------
-- line 12001 ----------------------------------------
        .                               }
        .                           }
        .                           ManaSymbol::MonoHybrid(n, c) => {
        .                               // {n/C}: spend a matching colored mana if on hand;
        .                               // otherwise treat the pip as {n} generic to tap for.
        .                               let have = &mut avail[color_index(*c)];
        .                               if *have > 0 { *have -= 1; } else { generic += n; }
        .                           }
    6,308 ( 0.00%)                  ManaSymbol::Generic(n) => generic += n,
        .                           ManaSymbol::Colorless(n) => {
        .                               // {C} must be paid from the colorless bucket.
        .                               avail_colorless = avail_colorless.saturating_sub(*n);
        .                               // If colorless bucket can't cover it fully, we'd need to tap a colorless
        .                               // source — skip that complexity for now (generic fallback handles it).
        .                           }
        .                           ManaSymbol::Snow | ManaSymbol::X => {}
        .                       }
-- line 12017 ----------------------------------------
-- line 12023 ----------------------------------------
        .                   // limited board isn't spent on the wrong half: {W/B}{W/B} with a
        .                   // Plains + a Swamp must split W and B, and {W/B} with only a Swamp
        .                   // must tap the Swamp rather than hunting for a white source (the
        .                   // previous code always tried color A and stranded the cast).
        .                   // `reach` = pool mana + untapped sources that can make the color.
        .                   fn reach(c: ManaColor, avail: &[u32; 5], prod: &[u32; 5]) -> u32 {
        .                       avail[color_index(c)] + prod[color_index(c)]
        .                   }
   17,784 ( 0.00%)          if !hybrids.is_empty() {
        .                       let mut prod = [0u32; 5];
        .                       for c in ManaColor::ALL {
        .                           prod[color_index(c)] = self.untapped_producers_of(player, c);
        .                       }
        .                       while !hybrids.is_empty() {
        .                           let idx = hybrids
        .                               .iter()
        .                               .position(|(a, b)| (reach(*a, &avail, &prod) > 0) ^ (reach(*b, &avail, &prod) > 0))
-- line 12039 ----------------------------------------
-- line 12056 ----------------------------------------
        .                               // Neither color reachable — push anyway; the tap loop
        .                               // no-ops and the cast fails downstream as before.
        .                               still_need_colors.push(pick);
        .                           }
        .                       }
        .                   }
        .           
        .                   // Remaining pool total after colored deductions covers generic pips.
   53,352 ( 0.00%)          let pool_total_left: u32 = avail.iter().sum::<u32>() + avail_colorless;
        .                   let generic_to_tap = generic.saturating_sub(pool_total_left);
        .           
        .                   // Nothing to tap: the two loops below are no-ops and `events` is
        .                   // still empty, so return before building the source table. That
        .                   // table is a layer pass per untapped permanent
        .                   // (`mana_source_table`, 3.83 % of the profile over 8,892 calls) and
        .                   // every caller whose pool already covers the cost was paying for it.
   20,828 ( 0.00%)          if still_need_colors.is_empty() && generic_to_tap == 0 {
    7,610 ( 0.00%)              return events;
        .                   }
        .           
        .                   // Built once; the selection loops below re-check `tapped` live but
        .                   // read colours and costs from here. See `mana_source_table`.
   36,850 ( 0.00%)          let sources = self.mana_source_table(player, creature_only);
112,195,973 ( 3.60%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_source_table (7,370x)
        .           
        .                   // Tap a color-matched source for each still-needed colored pip.
        .                   // For abilities that produce `AnyOneColor` (Black Lotus, Birds of
        .                   // Paradise, Mox Diamond, etc.) the source's own resolver asks the
        .                   // installed `Decider` which color to add. We temporarily swap in a
        .                   // `ScriptedDecider` that answers with `color`, so the chosen color
        .                   // matches the pip we're trying to satisfy. (Without this, the
        .                   // default `AutoDecider` always picks White and leaves the requested
        .                   // color unfilled.)
   46,616 ( 0.00%)          for color in still_need_colors {
        .                       // `controller` not `owner`: a permanent you've stolen
        .                       // (Threaten / Mind Control) is a tap-for-mana source for
        .                       // you, regardless of its original ownership — `sources` is
        .                       // filtered on controller for the same reason.
        .                       //
        .                       // Among sources that make this colour, spend the *least*
        .                       // flexible one: a Swamp pays {B} before a Dimir dual does,
        .                       // leaving the dual free for whichever colour comes up next.
        .                       // Same reasoning as the "dedicated first" reservation in
        .                       // `manual_tap_is_a_real_choice`. Cheapest first, then
        .                       // narrowest; `min_by_key` keeps the first of equal keys, so
        .                       // battlefield order still breaks remaining ties as before.
        .                       let source = sources
        .                           .iter()
   66,174 ( 0.00%)                  .filter(|s| !self.battlefield_find(s.id).is_some_and(|c| c.tapped))
        .                           .filter_map(|s| {
   63,848 ( 0.00%)                      let idx = s.colors.contains(color).then(|| s.color_idx[color_index(color)])?;
   63,132 ( 0.00%)                      let breadth = if smart { s.colors.len() } else { 0 };
   31,566 ( 0.00%)                      Some((s.rank, breadth, s.id, idx))
        .                           })
        .                           .min_by_key(|&(rank, breadth, ..)| (rank, breadth))
        .                           .map(|(_, _, id, idx)| (id, idx));
        .                       if let Some((id, idx)) = source {
        .                           let scripted = crate::decision::ScriptedDecider::new([
    9,416 ( 0.00%)                      crate::decision::DecisionAnswer::Color(color),
        .                           ]);
        .                           let prev_decider = std::mem::replace(
        .                               &mut self.decider,
        .                               Box::new(scripted),
        .                           );
        .                           // Force synchronous resolution: if the player normally wants
        .                           // a UI prompt for `AnyOneColor`, auto-tap must still finish
        .                           // inline (otherwise the cast aborts mid-payment with a
        .                           // pending decision). The scripted decider already supplies
        .                           // the right answer.
   28,248 ( 0.00%)                  let prev_wants_ui = self.players[player].wants_ui;
   18,832 ( 0.00%)                  self.players[player].wants_ui = false;
  282,480 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (9,416x)
   94,160 ( 0.00%)                  let result = self.activate_ability(id, idx, None, Vec::new(), None, None);
134,416,203 ( 4.31%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability (9,416x)
   47,080 ( 0.00%)                  self.decider = prev_decider;
   18,832 ( 0.00%)                  self.players[player].wants_ui = prev_wants_ui;
  282,480 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (9,416x)
   46,948 ( 0.00%)                  if let Ok(mut evs) = result {
        .                               events.append(&mut evs);
        .                           }
      132 ( 0.00%)              }
      880 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::result::Result<alloc::vec::Vec<crabomination::game::types::GameEvent>,crabomination::game::types::GameError>> (44x)
        .                   }
        .           
        .                   // Tap any mana source for remaining generic pips, spending the
        .                   // most replaceable ones first — see `source_redundancy`.
        .                   //
        .                   // `redundancy` reads `sources`, which this loop never mutates
        .                   // (tapping writes the battlefield, and the table's colours can't
        .                   // change from a tap — that is why it is built once at all), so the
        .                   // ranking is constant across the pips and is computed once here.
        .                   // It used to be recomputed per live source per pip. Gated on there
        .                   // being a generic pip at all: most costs have none, and building the
        .                   // ranking is O(sources² × colours).
    9,572 ( 0.00%)          let keep_by_idx: Vec<u32> = if smart && generic_to_tap > 0 {
        .                       sources.iter().map(|s| s.redundancy(&sources)).collect()
        .                   } else {
        .                       Vec::new()
        .                   };
        .                   for _ in 0..generic_to_tap {
        .                       // Same controller-vs-owner fix as the colored-pip loop.
        .                       let source = sources
        .                           .iter()
        .                           .enumerate()
   64,604 ( 0.00%)                  .filter(|(_, s)| !self.battlefield_find(s.id).is_some_and(|c| c.tapped))
        .                           .map(|(i, s)| {
  100,784 ( 0.00%)                      let keep = if smart { keep_by_idx[i] } else { 0 };
   91,860 ( 0.00%)                      (s.rank, std::cmp::Reverse(keep), s.id, s.first_idx)
        .                           })
        .                           .min_by_key(|&(rank, keep, ..)| (rank, keep))
        .                           .map(|(_, _, id, idx)| (id, idx));
        .                       let Some((id, idx)) = source else { break };
  116,012 ( 0.00%)              if let Ok(mut evs) = self.activate_ability(id, idx, None, Vec::new(), None, None) {
124,696,330 ( 4.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability (8,924x)
        .                           events.append(&mut evs);
        .                       } else {
        .                           break;
        .                       }
        .                   }
        .           
   36,850 ( 0.00%)          events
   71,136 ( 0.00%)      }
        .           
        .               // ── Activate ability ──────────────────────────────────────────────────────
        .           
        .               /// Collect the activated abilities granted to the permanent `card_id`
        .               /// by `StaticEffect::GrantActivatedAbility` statics in play (Galazeth
        .               /// Prismari, Cryptolith Rite). Each grant's `applies_to` filter is
        .               /// evaluated from the static source's controller so "you control"
        .               /// clauses scope to that player's permanents. Returned in battlefield
-- line 12179 ----------------------------------------
-- line 12201 ----------------------------------------
        .                   Self::intrinsic_land_mana_abilities_with(card, &computed.subtypes.land_types)
        .               }
        .           
        .               /// [`intrinsic_land_mana_abilities`](Self::intrinsic_land_mana_abilities)
        .               /// against a computed land-type list the caller already holds.
        .               /// `effective_mana_abilities_with` reads the same card's computed types
        .               /// for its printed-ability check, and a `computed_permanent` outside a
        .               /// freeze scope is a whole-game gather.
  436,560 ( 0.01%)      pub(crate) fn intrinsic_land_mana_abilities_with(
        .                   card: &crate::card::CardInstance,
        .                   computed_land_types: &[crate::card::LandType],
        .               ) -> Vec<crate::effect::ActivatedAbility> {
        .                   use crate::card::LandType;
        .                   let printed: &[LandType] = &card.definition.subtypes.land_types;
        .                   let mut out = Vec::new();
        .                   for lt in computed_land_types {
        .                       if printed.contains(lt) {
-- line 12217 ----------------------------------------
-- line 12231 ----------------------------------------
        .                           tap_cost: true,
        .                           effect: Effect::AddMana {
        .                               who: crate::effect::PlayerRef::You,
        .                               pool: ManaPayload::Colors(vec![color]),
        .                           },
        .                           ..Default::default()
        .                       });
        .                   }
  381,990 ( 0.01%)          out
  436,560 ( 0.01%)      }
        .           
        .               /// CR 305.6 / CR 612 — a basic land's printed `{T}: Add <color>` is really
        .               /// the intrinsic ability its type line grants, so it goes away once a type
        .               /// rewrite takes that basic type off the *computed* type line (Spreading
        .               /// Seas, Blood Moon, a Trait Doctoring color/land-word change). Printed
        .               /// mana abilities that aren't a basic type's intrinsic ability are real
        .               /// rules text and survive.
        .               ///
-- line 12248 ----------------------------------------
-- line 12251 ----------------------------------------
        .               /// fetched its own (a whole-game gather per call) is gone. See
        .               /// [`intrinsic_land_mana_abilities_with`](Self::intrinsic_land_mana_abilities_with).
        .               pub(crate) fn printed_land_mana_ability_lost_with(
        .                   card: &crate::card::CardInstance,
        .                   index: usize,
        .                   computed_land_types: &[crate::card::LandType],
        .               ) -> bool {
        .                   use crate::card::LandType;
  195,522 ( 0.01%)          if !card.definition.is_land() {
  239,858 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,386x)
        .                       return false;
        .                   }
        .                   let Some(ability) = card.definition.activated_abilities.get(index) else { return false };
  289,692 ( 0.01%)          let color = match &ability.effect {
  190,110 ( 0.01%)              Effect::AddMana { pool: ManaPayload::Colors(cs), .. } if cs.len() == 1 => cs[0],
        .                       _ => return false,
        .                   };
        .                   let basic = match color {
        .                       ManaColor::White => LandType::Plains,
        .                       ManaColor::Blue => LandType::Island,
        .                       ManaColor::Black => LandType::Swamp,
        .                       ManaColor::Red => LandType::Mountain,
        .                       ManaColor::Green => LandType::Forest,
-- line 12272 ----------------------------------------
-- line 12288 ----------------------------------------
        .                   card_id: CardId,
        .               ) -> Vec<(usize, std::borrow::Cow<'_, crate::effect::ActivatedAbility>)> {
        .                   self.effective_mana_abilities_with(card_id, &self.grant_scan())
        .               }
        .           
        .               /// [`effective_mana_abilities`](Self::effective_mana_abilities) against a
        .               /// prebuilt [`grant_scan`](Self::grant_scan) — `mana_source_table_inner`
        .               /// asks this per untapped permanent, so the scan is built once per table.
  654,840 ( 0.02%)      pub(crate) fn effective_mana_abilities_with(
        .                   &self,
        .                   card_id: CardId,
        .                   scan: &GrantScan<'_>,
        .               ) -> Vec<(usize, std::borrow::Cow<'_, crate::effect::ActivatedAbility>)> {
        .                   use std::borrow::Cow;
        .                   let Some(card) = self.battlefield_find(card_id) else {
        .                       return vec![];
        .                   };
-- line 12304 ----------------------------------------
-- line 12311 ----------------------------------------
        .                   //
        .                   // And the layer pass is skipped outright when nothing in scope can
        .                   // rewrite a land type: exactly three modifications write
        .                   // `subtypes.land_types` (see `rewrites_land_types`), and with none of
        .                   // them present the computed type line *is* the printed one. Inside a
        .                   // freeze scope the effect list is already gathered, so the test is a
        .                   // walk of it; outside one, `frozen_effects` returns `None` and the
        .                   // old path stands rather than paying a gather to save a gather.
  272,850 ( 0.01%)          let computed = match self.frozen_effects() {
17,705,437 ( 0.57%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::frozen_effects (54,570x)
  109,140 ( 0.00%)              Some(fx) if !fx.iter().any(rewrites_land_types) => None,
        .                       _ => self.computed_permanent(card_id),
        .                   };
  218,280 ( 0.01%)          let computed_land_types: &[crate::card::LandType] = match &computed {
        .                       // `card` came out of `battlefield_find`, so `computed_permanent`
        .                       // found it too — a `None` here is the gate above, not a miss.
        .                       Some(cp) => &cp.subtypes.land_types,
        .                       None => &card.definition.subtypes.land_types,
        .                   };
        .                   // The printed abilities are borrowed from `card.definition`, which
        .                   // outlives the call — only `granted_abilities_for` and
        .                   // `intrinsic_land_mana_abilities` synthesize, and all three callers
        .                   // read `.effect` and the cost fields. Cloning them was one
        .                   // `ActivatedAbility` deep copy per printed mana ability per untapped
        .                   // permanent per `auto_tap_for_cost_inner`.
        .                   let mut out: Vec<(usize, Cow<'_, crate::effect::ActivatedAbility>)> = Vec::new();
        .                   for (i, a) in card.definition.activated_abilities.iter().enumerate() {
  140,888 ( 0.00%)              if is_mana_ability(&a.effect)
   46,788 ( 0.00%)                  && !Self::printed_land_mana_ability_lost_with(card, i, computed_land_types)
        .                       {
        .                           out.push((i, Cow::Borrowed(a)));
        .                       }
        .                   }
  218,280 ( 0.01%)          let granted = self.granted_abilities_of(card, scan);
6,811,062 ( 0.22%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::granted_abilities_of (54,570x)
        .                   let gc = granted.len();
  272,850 ( 0.01%)          for (j, a) in granted.into_iter().enumerate() {
        .                       if is_mana_ability(&a.effect) {
        .                           out.push((printed_count + j, Cow::Owned(a)));
        .                       }
        .                   }
  600,270 ( 0.02%)          for (k, a) in Self::intrinsic_land_mana_abilities_with(card, computed_land_types)
2,461,158 ( 0.08%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::intrinsic_land_mana_abilities_with (54,570x)
        .                       .into_iter()
        .                       .enumerate()
        .                   {
        .                       out.push((printed_count + gc + k, Cow::Owned(a)));
        .                   }
  272,850 ( 0.01%)          out
  491,130 ( 0.02%)      }
        .           
        .               /// CR 702.61a — true while any spell on the stack has split second.
        .               pub(crate) fn stack_has_split_second(&self) -> bool {
   21,064 ( 0.00%)          self.stack.iter().any(|si| match si {
        .                       crate::game::types::StackItem::Spell { card, .. } => {
        .                           card.definition.keywords.contains(&crate::card::Keyword::SplitSecond)
        .                       }
        .                       _ => false,
        .                   })
        .               }
        .           
        .               /// CR 702.61 — which actions a split-second lock forbids: every cast,
-- line 12369 ----------------------------------------
-- line 12417 ----------------------------------------
        .               /// it costs three whole-battlefield walks and both graveyards — the same
        .               /// walks [`granted_abilities_for`] used to run *per call*, from three
        .               /// per-card loops (`bot::usable_abilities`, `effective_mana_abilities`,
        .               /// `bot::available_mana`). Build one per loop and hand it to
        .               /// [`granted_abilities_with`](Self::granted_abilities_with).
        .               ///
        .               /// Bound to `&self`, so it is invalid the moment the board changes; there
        .               /// is deliberately no cached copy on `GameState`.
  255,600 ( 0.01%)      pub(crate) fn grant_scan(&self) -> GrantScan<'_> {
        .                   use crate::effect::{Selector, StaticEffect};
        .                   let mut scan = GrantScan::default();
        .                   // CR 315.5 — a face-up conspiracy grants from the command zone too.
        .                   for src in self.all_static_sources() {
        .                       for sa in &src.definition.static_abilities {
        .                           // CR 611.2 — a grant may sit under a duration/predicate
        .                           // wrapper ("Threshold — this creature has '…'"); unwrap it
        .                           // and honour the gate.
-- line 12433 ----------------------------------------
-- line 12449 ----------------------------------------
        .                           }
        .                           scan.statics.push((applies_to, ability, src));
        .                       }
        .                   }
        .                   // Riftstone Portal — "as long as this card is in your graveyard,
        .                   // lands you control have '…'". The grant is live from the graveyard,
        .                   // scoped to the owning seat's permanents.
        .                   for (seat, pl) in self.players.iter().enumerate() {
  204,968 ( 0.01%)              for src in pl.graveyard.iter() {
        .                           for sa in &src.definition.static_abilities {
        .                               let StaticEffect::GrantActivatedAbilityFromGraveyard { applies_to, ability } =
        .                                   &sa.effect
        .                               else {
        .                                   continue;
        .                               };
        .                               let Selector::EachPermanent(req) = applies_to else { continue };
        .                               scan.graveyard.push((req, &**ability, seat, src.id));
        .                           }
        .                       }
        .                   }
   28,400 ( 0.00%)          for src in self.battlefield.iter() {
        .                       // CR 702.95 — Soulbond-granted activated abilities (Deadeye
        .                       // Navigator's flicker). A paired creature carrying a
        .                       // `soulbond_bonus` with `activated_abilities` grants them to BOTH
        .                       // itself and its partner.
1,287,312 ( 0.04%)              if let Some(bonus) = &src.definition.soulbond_bonus
        .                           && !bonus.activated_abilities.is_empty()
        .                           && let Some(partner) = src.soulbond_partner
        .                           && self.battlefield.iter().any(|c| c.id == partner)
        .                       {
        .                           scan.soulbond.push((src.id, partner, &bonus.activated_abilities));
        .                       }
        .                       // CR 702.6e — Equipment/Aura-granted activated abilities, matched
        .                       // per card by `attached_to`.
  865,366 ( 0.03%)              if src.attached_to.is_some() && src.definition.equipped_bonus.is_some() {
        .                           scan.equipment.push(src);
        .                       }
        .                   }
  369,200 ( 0.01%)          scan
  227,200 ( 0.01%)      }
        .           
        .               pub fn granted_abilities_for(
        .                   &self,
        .                   card_id: CardId,
        .               ) -> Vec<crate::effect::ActivatedAbility> {
        .                   self.granted_abilities_with(card_id, &self.grant_scan())
        .               }
        .           
-- line 12496 ----------------------------------------
-- line 12517 ----------------------------------------
        .                   self.granted_abilities_of(me, scan)
        .               }
        .           
        .               /// [`granted_abilities_with`](Self::granted_abilities_with) for a
        .               /// battlefield permanent the caller already holds. The `CardId` form opens
        .               /// with a `battlefield_find`, and its hot callers — `available_mana`,
        .               /// `effective_mana_abilities_with`, the bot's activatable sweep — are all
        .               /// already iterating the battlefield, so that scan is pure duplication.
2,220,174 ( 0.07%)      pub(crate) fn granted_abilities_of(
        .                   &self,
        .                   me: &CardInstance,
        .                   scan: &GrantScan<'_>,
        .               ) -> Vec<crate::effect::ActivatedAbility> {
        .                   use crate::effect::{Selector, StaticEffect};
  403,668 ( 0.01%)          let card_id = me.id;
  403,668 ( 0.01%)          let tgt = Target::Permanent(card_id);
        .                   let mut out = Vec::new();
        .                   // Which of the "has the activated abilities of …" statics this
        .                   // permanent carries, in one pass of its static abilities instead of
        .                   // one pass per block below.
        .                   let (mut welder, mut ooze, mut marvin, mut kraj, mut safehouse) =
        .                       (false, false, false, false, false);
        .                   for sa in &me.definition.static_abilities {
        .                       match sa.effect {
-- line 12540 ----------------------------------------
-- line 12550 ----------------------------------------
        .                   }
        .                   // Instance-granted abilities first (Urza's Saga chapters) — the
        .                   // client view lists printed + instance-granted in this order, so
        .                   // their indices must come before the battlefield-static grants.
        .                   out.extend(me.granted_activated_abilities.iter().cloned());
        .                   out.extend(me.granted_activated_eot.iter().cloned());
        .                   // Myr Welder — "has all activated abilities of all cards exiled
        .                   // with it", read live off the imprint pile.
  403,668 ( 0.01%)          if welder {
        .                       for imp in self.exile.iter().filter(|e| e.exiled_with == Some(card_id)) {
        .                           out.extend(imp.definition.activated_abilities.iter().cloned());
        .                       }
        .                   }
        .                   // CR 804.2 — the deploy creatures option gives every creature
        .                   // "{T}: Target teammate gains control of this creature. Activate
        .                   // only as a sorcery."
  403,668 ( 0.01%)          if self.deploy_creatures && me.definition.is_creature() {
        .                       out.push(crate::effect::ActivatedAbility {
        .                           tap_cost: true,
        .                           sorcery_speed: true,
        .                           effect: Effect::GainControl {
        .                               what: Selector::This,
        .                               to: Some(crate::effect::PlayerRef::EachTeammate),
        .                               duration: crate::effect::Duration::Permanent,
        .                           },
-- line 12574 ----------------------------------------
-- line 12613 ----------------------------------------
        .                           out.push((*ability).clone());
        .                       }
        .                   }
        .                   // Necrotic Ooze — a permanent with
        .                   // `HasActivatedAbilitiesOfGraveyardCreatures` gains every battlefield-
        .                   // usable activated ability of every creature card in every graveyard.
        .                   // Skip graveyard-only activations (`from_graveyard` / `exile_self_cost`)
        .                   // — those function only from the graveyard, so the Ooze can't use them.
  403,668 ( 0.01%)          if ooze {
        .                       for pl in &self.players {
        .                           for card in &pl.graveyard {
        .                               if !card.definition.is_creature() {
        .                                   continue;
        .                               }
        .                               for ab in &card.definition.activated_abilities {
        .                                   if ab.from_graveyard || ab.exile_self_cost {
        .                                       continue;
-- line 12629 ----------------------------------------
-- line 12630 ----------------------------------------
        .                                   }
        .                                   out.push(ab.clone());
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // Marvin, Murderous Mimic — every activated ability of each creature
        .                   // its controller controls whose name differs from Marvin's.
  403,668 ( 0.01%)          if marvin {
        .                       let (seat, name) = (me.controller, me.definition.name);
        .                       for other in &self.battlefield {
        .                           if other.controller != seat
        .                               || other.definition.name == name
        .                               || !other.definition.is_creature()
        .                           {
        .                               continue;
        .                           }
        .                           out.extend(other.definition.activated_abilities.iter().cloned());
        .                       }
        .                   }
        .                   // Experiment Kraj — every activated ability of each *other* creature
        .                   // carrying a +1/+1 counter.
  403,668 ( 0.01%)          if kraj {
        .                       for other in &self.battlefield {
        .                           if other.id == card_id
        .                               || !other.definition.is_creature()
        .                               || other.counter_count(crate::card::CounterType::PlusOnePlusOne) == 0
        .                           {
        .                               continue;
        .                           }
        .                           out.extend(other.definition.activated_abilities.iter().cloned());
        .                       }
        .                   }
        .                   // Mirran Safehouse — every battlefield-usable activated ability of
        .                   // every land card in every graveyard.
  403,668 ( 0.01%)          if safehouse {
        .                       for pl in &self.players {
        .                           for card in &pl.graveyard {
        .                               if !card.definition.is_land() {
        .                                   continue;
        .                               }
        .                               for ab in &card.definition.activated_abilities {
        .                                   if ab.from_graveyard || ab.exile_self_cost {
        .                                       continue;
-- line 12673 ----------------------------------------
-- line 12696 ----------------------------------------
        .                                   out.push(ab.clone());
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // Agatha's Soul Cauldron — a creature you control with a +1/+1
        .                   // counter has all activated abilities of creature cards exiled
        .                   // with any Cauldron its controller controls.
  605,502 ( 0.02%)          if me.definition.is_creature()
3,122,682 ( 0.10%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (201,834x)
  142,216 ( 0.00%)              && me.counter_count(crate::card::CounterType::PlusOnePlusOne) > 0
  355,540 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (35,554x)
        .                   {
        .                       let cauldrons: Vec<CardId> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.controller == me.controller
        .                                   && c.definition.static_abilities.iter().any(|sa| {
        .                                       matches!(
-- line 12713 ----------------------------------------
-- line 12738 ----------------------------------------
        .                       if *src_id == card_id || *partner == card_id {
        .                           out.extend(abilities.iter().cloned());
        .                       }
        .                   }
        .                   // CR 702.6e — Equipment-granted activated abilities. An Equipment whose
        .                   // `equipped_bonus.activated_abilities` is non-empty and is attached to
        .                   // this creature grants them (Wrench's "{3}, {T}: Tap target creature").
        .                   for eq in &scan.equipment {
  137,140 ( 0.00%)              if eq.attached_to != Some(card_id) {
        .                           continue;
        .                       }
    9,318 ( 0.00%)              if let Some(bonus) = &eq.definition.equipped_bonus {
        .                           out.extend(bonus.activated_abilities.iter().cloned());
        .                           // Host-conditional grants ("as long as enchanted creature is a
        .                           // Wizard, it has …" — Lavamancer's Skill).
        .                           for cond in &bonus.conditional {
        .                               if cond.activated_abilities.is_empty() {
        .                                   continue;
        .                               }
        .                               if self.evaluate_requirement_on_card(&cond.host_filter, me, eq.controller) {
-- line 12757 ----------------------------------------
-- line 12758 ----------------------------------------
        .                                   out.extend(cond.activated_abilities.iter().cloned());
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // CR 721.2a — Station `{N+}` activated-ability bands. While the source's
        .                   // charge-counter count meets a band threshold, that band's activated
        .                   // abilities are usable (a Planet's `12+ | {cost}: …`).
  403,668 ( 0.01%)          if !me.definition.station.is_empty() {
        .                       let charges = me.counter_count(crate::card::CounterType::Charge);
        .                       for band in me.definition.station.iter().filter(|b| charges >= b.min) {
        .                           out.extend(band.activated.iter().cloned());
        .                       }
        .                   }
1,009,170 ( 0.03%)          out
1,614,672 ( 0.05%)      }
        .           
        .               /// Activate an ability whose cost accepts tapped helpers: CR 701.67
        .               /// Waterbend (`ActivatedAbility.waterbend` — artifacts or creatures, each
        .               /// paying {1} of the generic) or CR 702.51 convoke on the ability
        .               /// (`.convoke` — creatures only, each paying one generic *or* one colored
        .               /// pip of its own color). Floats the helper mana, then defers to
        .               /// `activate_ability` for the full payment.
        .               pub(crate) fn activate_ability_waterbend(
-- line 12781 ----------------------------------------
-- line 12874 ----------------------------------------
        .                   r
        .               }
        .           
        .               #[allow(clippy::too_many_arguments)]
        .               /// CR 106.6b — mana a *creature* produces carries provenance so a
        .               /// "spend only mana produced by creatures" cost (Myr Superion) can find
        .               /// it. Tagging the pool delta here catches printed, granted (Cryptolith
        .               /// Rite) and intrinsic mana abilities in one place.
  386,106 ( 0.01%)      pub(crate) fn activate_ability(
        .                   &mut self,
        .                   card_id: CardId,
        .                   ability_index: usize,
        .                   target: Option<Target>,
        .                   additional_targets: Vec<Target>,
        .                   x_value: Option<u32>,
        .                   chosen_mode: Option<usize>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
   18,386 ( 0.00%)          let p = self.priority.player_with_priority;
        .                   self.check_free_activation_loop(card_id, ability_index)?;
        .                   // Mana produced by a *creature* is marked as such (CR 106.12
        .                   // restrictions). Whether the source is one is a layer read, and
        .                   // `_inner` already computes that permanent for the ability lookup —
        .                   // so it hands back the pre-activation pool when the source is a
        .                   // creature, instead of this frame paying a second whole-game gather.
        .                   // Nothing has moved mana at the point it fills this in.
   18,386 ( 0.00%)          let mut before: Option<crate::mana::ManaPool> = None;
  220,632 ( 0.01%)          let out = self.activate_ability_inner(
232,423,409 ( 7.46%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::activate_ability_inner (18,386x)
        .                       card_id,
        .                       ability_index,
        .                       target,
        .                       additional_targets,
        .                       x_value,
        .                       chosen_mode,
        .                       &mut before,
        .                   );
   36,772 ( 0.00%)          if let Some(before) = before {
    1,400 ( 0.00%)              let pool = &mut self.players[p].mana_pool;
   98,501 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (280x)
        .                       for c in ManaColor::ALL {
        .                           let d = pool.amount(c).saturating_sub(before.amount(c));
    2,800 ( 0.00%)                  if d > 0 {
      570 ( 0.00%)                      pool.mark_from_creature(Some(c), d);
    4,750 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::mark_from_creature (190x)
        .                           }
        .                       }
        .                       let d = pool.colorless_amount().saturating_sub(before.colorless_amount());
      560 ( 0.00%)              if d > 0 {
        .                           pool.mark_from_creature(None, d);
        .                       }
        .                   }
   91,930 ( 0.00%)          out
  147,088 ( 0.00%)      }
        .           
        .               #[allow(clippy::too_many_arguments)]
  349,334 ( 0.01%)      fn activate_ability_inner(
        .                   &mut self,
        .                   card_id: CardId,
        .                   ability_index: usize,
        .                   target: Option<Target>,
        .                   additional_targets: Vec<Target>,
        .                   x_value: Option<u32>,
        .                   chosen_mode: Option<usize>,
        .                   // Out: the caller's pre-activation mana pool, filled only when the
        .                   // source is a creature — see `activate_ability`.
        .                   creature_mana_before: &mut Option<crate::mana::ManaPool>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
   36,772 ( 0.00%)          let p = self.priority.player_with_priority;
        .           
        .                   // CR 801.6 — a player can't activate abilities of an object outside
        .                   // their range of influence.
        .                   if !self.object_in_range_of(p, card_id) {
        .                       return Err(GameError::OutOfRange);
        .                   }
        .           
        .                   // Consume any "another permanent" cost picks from an
        .                   // `ActivateAbilityChoice` resume up front, so a failure anywhere below
        .                   // can't leak them onto the next activation. `None` on the first attempt
        .                   // (may suspend for the choice); `Some` on the replay (used in lieu of
        .                   // the auto-pick).
        .                   // Abeyance — "that player can't activate abilities that aren't mana
        .                   // abilities" this turn.
   73,544 ( 0.00%)          if self.players[p].cant_activate_nonmana_abilities_this_turn
        .                       && let Some(a) = self
        .                           .find_card_anywhere(card_id)
        .                           .and_then(|c| c.definition.activated_abilities.get(ability_index))
        .                       && !is_mana_ability(&a.effect)
        .                   {
        .                       return Err(GameError::AbilityConditionNotMet);
        .                   }
        .           
-- line 12961 ----------------------------------------
-- line 12968 ----------------------------------------
        .                   let spend_float = self.pending_cast_spend_float.take();
        .           
        .                   // Source zone: battlefield by default, the controller's graveyard
        .                   // when the ability is flagged `from_graveyard`, or the controller's
        .                   // hand when flagged `from_hand` (Spirit Guides' exile-to-pitch mana
        .                   // abilities). We scan battlefield first; if missing, fall back to
        .                   // graveyards then hands (any player's; ownership is verified below).
        .                   let (source_in_gy, source_in_hand, source_in_exile, source_in_command, source_owner) = {
   55,158 ( 0.00%)              let on_bf = self.battlefield.iter().any(|c| c.id == card_id);
        .                       if on_bf {
        .                           (false, false, false, false, None)
        .                       } else if let Some(o) = self
        .                           .players
        .                           .iter()
        .                           .position(|pl| pl.graveyard.iter().any(|c| c.id == card_id))
        .                       {
        .                           (true, false, false, false, Some(o))
-- line 12984 ----------------------------------------
-- line 13004 ----------------------------------------
        .                       }
        .                   };
        .           
        .                   // This card's computed view, taken once by the battlefield branch
        .                   // below and read again by the CR 602.5 gates further down. Nothing
        .                   // between the two points mutates a layer input (the one write is the
        .                   // `{X}` prompt, which returns), so the second read reuses this
        .                   // instead of taking its own whole-game gather.
   18,386 ( 0.00%)          let mut bf_cp = None;
        .                   let ability: crate::effect::ActivatedAbility = if source_in_gy {
        .                       let owner = source_owner.unwrap();
        .                       let card = self.players[owner].graveyard.iter()
        .                           .find(|c| c.id == card_id)
        .                           .ok_or(GameError::AbilityIndexOutOfBounds)?;
        .                       let printed_count = card.definition.activated_abilities.len();
        .                       if ability_index < printed_count {
        .                           card.definition.activated_abilities[ability_index].clone()
-- line 13020 ----------------------------------------
-- line 13070 ----------------------------------------
        .                       // The grant/intrinsic halves are indexed only at
        .                       // `ability_index >= printed_count`, and the printed count is a
        .                       // definition read with no layer in it — so decide it first and
        .                       // skip both for a printed index. That is nearly every
        .                       // activation (a land tapping for mana is index 0), and each one
        .                       // was a whole-board `grant_scan` plus a second
        .                       // `computed_permanent`.
        .                       let printed_count = self.battlefield[pos].definition.activated_abilities.len();
   91,930 ( 0.00%)              let want_extra = ability_index >= printed_count;
        .                       let (stripped, is_creature, granted, intrinsic, land_mana_lost, cp) =
        .                           self.with_frozen_layers(|g| {
   73,544 ( 0.00%)                      let cp = g.computed_permanent(card_id);
53,734,084 ( 1.72%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::computed_permanent (18,386x)
        .                               // CR 305.6 / 612 — the same computed view answers whether a
        .                               // basic's printed mana ability survived its type line, so
        .                               // the check below reads it here instead of taking its own
        .                               // gather (`printed_land_mana_ability_lost` did, 1.28 % of
        .                               // the simulator). Cheap when the index isn't a printed
        .                               // single-colour mana ability: `_with` bails on the printed
        .                               // shape.
   36,772 ( 0.00%)                      let land_mana_lost = match (&cp, g.battlefield_find(card_id)) {
   18,386 ( 0.00%)                          (Some(c), Some(card)) => Self::printed_land_mana_ability_lost_with(
        .                                       card,
        .                                       ability_index,
        .                                       &c.subtypes.land_types,
        .                                   ),
        .                                   _ => false,
        .                               };
        .                               (
   18,386 ( 0.00%)                          cp.as_ref().map(|c| c.lost_all_abilities).unwrap_or(false),
        .                                   cp.as_ref().is_some_and(|c| {
        .                                       c.card_types.contains(&crate::card::CardType::Creature)
        .                                   }),
   18,386 ( 0.00%)                          if want_extra { g.granted_abilities_for(card_id) } else { Vec::new() },
        .                                   if want_extra { g.intrinsic_land_mana_abilities(card_id) } else { Vec::new() },
        .                                   land_mana_lost,
        .                                   cp,
        .                               )
        .                           });
   18,386 ( 0.00%)              bf_cp = cp;
   36,772 ( 0.00%)              if is_creature {
    5,880 ( 0.00%)                  *creature_mana_before = Some(self.players[p].mana_pool.clone());
        .                       }
   18,386 ( 0.00%)              if ability_index < printed_count {
   18,386 ( 0.00%)                  let raw = self.battlefield[pos]
        .                               .definition
        .                               .activated_abilities[ability_index]
   18,386 ( 0.00%)                      .clone();
13,534,270 ( 0.43%)  => /home/user/Crabomination/crabomination_base/src/effect/abilities.rs:<crabomination_base::effect::abilities::ActivatedAbility as core::clone::Clone>::clone (18,386x)
   36,772 ( 0.00%)                  if stripped && !is_mana_ability(&raw.effect) {
        .                               return Err(GameError::AbilityIndexOutOfBounds);
        .                           }
        .                           // CR 305.6 / 612 — a basic's intrinsic mana ability follows its
        .                           // *computed* type line, so a rewritten type takes it away.
   36,772 ( 0.00%)                  if land_mana_lost {
        .                               return Err(GameError::AbilityIndexOutOfBounds);
        .                           }
        .                           raw
   18,386 ( 0.00%)              } else if ability_index < printed_count + granted.len() {
        .                           let g = granted[ability_index - printed_count].clone();
        .                           // Stripped permanents keep granted mana abilities only.
        .                           if stripped && !is_mana_ability(&g.effect) {
        .                               return Err(GameError::AbilityIndexOutOfBounds);
        .                           }
        .                           g
        .                       } else if ability_index < printed_count + granted.len() + intrinsic.len() {
        .                           // Intrinsic basic-land mana abilities survive stripping.
-- line 13134 ----------------------------------------
-- line 13139 ----------------------------------------
        .                   };
        .           
        .                   // {X} activation costs ({X}, {T}: … — Berta, Imbraham): a
        .                   // hand-paying activator who didn't send an X picks one via a
        .                   // `ChooseAmount` modal (suspend + clean replay — nothing has been
        .                   // paid yet). The auto path keeps `x_value` as-is (unwrapped to 0
        .                   // downstream, the historical behavior). [`manual_mana`] rather than
        .                   // `wants_ui` for the same reason as the cast_spell X prompt.
   55,158 ( 0.00%)          if ability.mana_cost.has_x()
  202,246 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (18,386x)
        .                       && x_value.is_none()
        .                       && self.players[p].manual_mana
        .                   {
        .                       let max = self.max_prompt_x(p, &ability.mana_cost);
        .                       let source_name = self
        .                           .find_card_anywhere(card_id)
        .                           .map(|c| c.definition.name.to_string())
        .                           .unwrap_or_default();
-- line 13155 ----------------------------------------
-- line 13170 ----------------------------------------
        .                           },
        .                       });
        .                       return Ok(vec![]);
        .                   }
        .           
        .                   // Spend context for restricted mana: an ArtifactOnly pool entry
        .                   // (Power Depot) may fund abilities of artifact sources.
        .                   let ability_spend_kind = {
   36,772 ( 0.00%)              let def = if source_in_gy {
        .                           self.players[source_owner.unwrap()].graveyard.iter()
        .                               .find(|c| c.id == card_id).map(|c| &c.definition)
   36,772 ( 0.00%)              } else if source_in_hand {
        .                           self.players[source_owner.unwrap()].hand.iter()
        .                               .find(|c| c.id == card_id).map(|c| &c.definition)
   36,772 ( 0.00%)              } else if source_in_exile {
        .                           self.exile.iter().find(|c| c.id == card_id).map(|c| &c.definition)
   36,772 ( 0.00%)              } else if source_in_command {
        .                           self.players[source_owner.unwrap()].command.iter()
        .                               .find(|c| c.id == card_id).map(|c| &c.definition)
        .                       } else {
   18,386 ( 0.00%)                  self.battlefield.iter().find(|c| c.id == card_id).map(|c| &c.definition)
        .                       };
   18,386 ( 0.00%)              def.map(|d| d.ability_spend_kind()).unwrap_or_default()
1,103,160 ( 0.04%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::ability_spend_kind (18,386x)
        .                   };
        .           
        .                   // For graveyard/hand activations, reject if the ability isn't flagged
        .                   // for that zone. This prevents activating a card's printed
        .                   // battlefield-only ability from another zone accidentally.
   55,158 ( 0.00%)          if source_in_gy && !ability.from_graveyard {
        .                       return Err(GameError::CardNotOnBattlefield(card_id));
        .                   }
   36,772 ( 0.00%)          if source_in_hand && !ability.from_hand {
        .                       return Err(GameError::CardNotOnBattlefield(card_id));
        .                   }
   36,772 ( 0.00%)          if source_in_exile && !ability.from_exile {
        .                       return Err(GameError::CardNotOnBattlefield(card_id));
        .                   }
   73,544 ( 0.00%)          if source_in_command && !ability.from_command_zone {
        .                       return Err(GameError::CardNotOnBattlefield(card_id));
        .                   }
        .           
        .                   // Only the controller (or graveyard/hand owner) can activate abilities,
        .                   // except abilities flagged `opponents_only` (CR 602.5 — Detention
        .                   // Vortex's escape clause), which only an opponent of the controller may.
  128,702 ( 0.00%)          if source_in_gy || source_in_hand || source_in_exile || source_in_command {
        .                       if source_owner != Some(p) {
        .                           return Err(GameError::NotYourPriority);
        .                       }
        .                   } else {
   36,772 ( 0.00%)              let controller = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == card_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(card_id))?
        .                           .controller;
   36,772 ( 0.00%)              if ability.opponents_only {
        .                           if self.same_team(controller, p) {
        .                               return Err(GameError::NotYourPriority);
        .                           }
   73,544 ( 0.00%)              } else if !ability.any_player && controller != p {
        .                           return Err(GameError::NotYourPriority);
        .                       }
        .                   }
        .           
        .                   // CR 201.3 — Pithing Needle / Phyrexian Revoker: a permanent that
        .                   // named this source's card name shuts off its activated abilities
        .                   // unless they're mana abilities. The suppression is global (affects
        .                   // every player's matching sources), so we scan the whole battlefield
        .                   // for a `named_card` matching this source's printed name.
   55,066 ( 0.00%)          if !is_mana_ability(&ability.effect) {
       92 ( 0.00%)              let source_name = if source_in_gy {
        .                           self.players[source_owner.unwrap()].graveyard.iter()
        .                               .find(|c| c.id == card_id)
        .                               .map(|c| c.definition.name)
        .                       } else {
       46 ( 0.00%)                  self.battlefield.iter().find(|c| c.id == card_id).map(|c| c.definition.name)
        .                       };
        .                       if let Some(name) = source_name
      184 ( 0.00%)                  && self.battlefield.iter().any(|c| c.named_card.as_deref() == Some(name))
    9,772 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (46x)
        .                       {
        .                           return Err(GameError::AbilitySuppressedByNamedCard);
        .                       }
        .                   }
        .           
        .                   // Hand to Hand — no non-mana activations during combat.
   36,910 ( 0.00%)          if !is_mana_ability(&ability.effect) && self.combat_spell_lock_active() {
      966 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::combat_spell_lock_active (46x)
        .                       return Err(GameError::AbilitySuppressedByNamedCard);
        .                   }
        .           
        .                   // Interdict — "that permanent's activated abilities can't be
        .                   // activated this turn". Mana abilities are unaffected.
   36,726 ( 0.00%)          if !is_mana_ability(&ability.effect) && self.abilities_locked_this_turn.contains(&card_id) {
        .                       return Err(GameError::AbilitySuppressedByNamedCard);
        .                   }
        .           
        .                   // Collector Ouphe / Karn lock: non-mana activated abilities of
        .                   // artifacts can't be activated while a `ArtifactActivatedAbilitiesLocked`
        .                   // static is in play (global — affects every player). A source on the
        .                   // battlefield is checked for its artifact type; gy/hand sources of an
        .                   // artifact (rare) are caught the same way.
   36,680 ( 0.00%)          if !is_mana_ability(&ability.effect) {
       92 ( 0.00%)              let src_is_artifact = if source_in_gy {
        .                           self.players[source_owner.unwrap()].graveyard.iter()
        .                               .find(|c| c.id == card_id)
        .                               .is_some_and(|c| c.definition.is_artifact())
       92 ( 0.00%)              } else if source_in_hand {
        .                           self.players[source_owner.unwrap()].hand.iter()
        .                               .find(|c| c.id == card_id)
        .                               .is_some_and(|c| c.definition.is_artifact())
        .                       } else {
       92 ( 0.00%)                  self.battlefield_find(card_id).is_some_and(|c| c.definition.is_artifact())
      736 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_artifact (46x)
        .                       };
      184 ( 0.00%)              if src_is_artifact
        .                           && self.battlefield.iter().flat_map(|c| &c.definition.static_abilities).any(|sa| {
        .                               matches!(sa.effect, crate::effect::StaticEffect::ArtifactActivatedAbilitiesLocked)
        .                           })
        .                       {
        .                           return Err(GameError::AbilitySuppressedByNamedCard);
        .                       }
        .                   }
        .           
        .                   // Karn, the Great Creator — activated abilities (mana abilities
        .                   // included) of artifacts controlled by an opponent of Karn's
        .                   // controller can't be activated.
        .                   {
   36,772 ( 0.00%)              let src_artifact_on_bf = !source_in_gy
        .                           && !source_in_hand
   36,772 ( 0.00%)                  && self.battlefield_find(card_id).is_some_and(|c| c.definition.is_artifact());
  294,176 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_artifact (18,386x)
   36,772 ( 0.00%)              if src_artifact_on_bf
        .                           && self.battlefield.iter().any(|c| {
        .                               c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::OpponentsCantActivateArtifactAbilities
        .                                   )
        .                               }) && !self.same_team(c.controller, p)
        .                           })
-- line 13306 ----------------------------------------
-- line 13309 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // Grand Abolisher — during the active player's turn, that player's
        .                   // opponents can't activate abilities of artifacts, creatures, or
        .                   // enchantments (mana abilities included). Lands and other permanents
        .                   // are unaffected.
        .                   {
   55,158 ( 0.00%)              let active = self.active_player_idx;
        .                       let src_is_ace = !source_in_gy
        .                           && !source_in_hand
   55,158 ( 0.00%)                  && self.battlefield_find(card_id).is_some_and(|c| {
   91,930 ( 0.00%)                      c.definition.is_artifact()
  294,176 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_artifact (18,386x)
        .                                   || c.definition.card_types.contains(&crate::card::CardType::Creature)
        .                                   || c.definition.card_types.contains(&crate::card::CardType::Enchantment)
   36,772 ( 0.00%)                  });
   55,158 ( 0.00%)              if src_is_ace
      840 ( 0.00%)                  && p != active
      130 ( 0.00%)                  && !self.same_team(p, active)
      910 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::same_team (26x)
      130 ( 0.00%)                  && self.battlefield.iter().any(|c| {
    5,220 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (26x)
    1,056 ( 0.00%)                      c.controller == active
        .                                   && c.definition.static_abilities.iter().any(|sa| {
        .                                       matches!(
        .                                           sa.effect,
        .                                           crate::effect::StaticEffect::OpponentsCantActDuringYourTurn
        .                                       )
        .                                   })
        .                           })
        .                       {
-- line 13337 ----------------------------------------
-- line 13339 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // CR 701.35 — a detained permanent's activated abilities can't be
        .                   // activated (all of them, mana abilities included) until the detainer's
        .                   // next turn.
        .                   if !source_in_gy
        .                       && !source_in_hand
   18,386 ( 0.00%)              && self.battlefield_find(card_id).is_some_and(|c| c.detained_by.is_some())
        .                   {
        .                       return Err(GameError::AbilitySuppressedByNamedCard);
        .                   }
        .           
        .                   // Cursed Totem / Damping Matrix lock: non-mana activated abilities of
        .                   // creatures can't be activated while a
        .                   // `CreatureActivatedAbilitiesLocked` static is in play (global).
   36,680 ( 0.00%)          if !is_mana_ability(&ability.effect) {
       92 ( 0.00%)              let src_is_creature = if source_in_gy {
        .                           self.players[source_owner.unwrap()].graveyard.iter()
        .                               .find(|c| c.id == card_id)
        .                               .is_some_and(|c| c.definition.is_creature())
       92 ( 0.00%)              } else if source_in_hand {
        .                           self.players[source_owner.unwrap()].hand.iter()
        .                               .find(|c| c.id == card_id)
        .                               .is_some_and(|c| c.definition.is_creature())
        .                       } else {
       92 ( 0.00%)                  self.battlefield_find(card_id).is_some_and(|c| c.definition.is_creature())
      598 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (46x)
        .                       };
      138 ( 0.00%)              if src_is_creature
      138 ( 0.00%)                  && self.battlefield.iter().flat_map(|c| &c.definition.static_abilities).any(|sa| {
        .                               matches!(sa.effect, crate::effect::StaticEffect::CreatureActivatedAbilitiesLocked)
        .                           })
        .                       {
        .                           return Err(GameError::AbilitySuppressedByNamedCard);
        .                       }
        .                   }
        .           
        .                   // The three CR 602.5 gates below all read one bit of *this* card's
        .                   // computed view, nothing between them touches a layer input, and this
        .                   // is a `&mut self` path — so each `computed_permanent` was its own
        .                   // whole-game gather. Take one, under the same conditions that decide
        .                   // whether any of them can fire. On the battlefield that one is
        .                   // `bf_cp`, already taken by the ability lookup above; each gate
        .                   // re-checks its own condition, so handing it a view the old gate
        .                   // would have skipped cannot change an answer.
   36,772 ( 0.00%)          let tap_gated = ability.tap_cost || ability.untap_self_cost;
   36,772 ( 0.00%)          let on_battlefield = !source_in_gy && !source_in_hand && !source_in_command;
   36,772 ( 0.00%)          let cp = if on_battlefield {
   36,772 ( 0.00%)              bf_cp
        .                   } else {
        .                       tap_gated.then(|| self.computed_permanent(card_id)).flatten()
        .                   };
        .           
        .                   // CR 602.5 — "activated abilities with {T} in their costs can't be
        .                   // activated" (Serra Bestiary). Read off the computed keyword set so a
        .                   // granted restriction applies immediately.
   55,158 ( 0.00%)          if tap_gated
  128,702 ( 0.00%)              && cp.as_ref().is_some_and(|cp| {
        .                           cp.keywords.contains(&Keyword::CantActivateTapAbilities)
  110,316 ( 0.00%)              })
        .                   {
        .                       return Err(GameError::AbilityAlreadyUsedThisTurn);
        .                   }
        .           
        .                   // CR 602.5g/h — a creature's ability with a {T} or {Q} cost can't be
        .                   // activated while the creature is summoning-sick, unless it has haste
        .                   // or its controller has a Tyvar-style "as though they had haste" static.
   36,772 ( 0.00%)          if tap_gated && on_battlefield {
  147,088 ( 0.00%)              let sick = self.battlefield_find(card_id).is_some_and(|c| {
   36,772 ( 0.00%)                  c.summoning_sick
        .                               && cp.as_ref().is_some_and(|cp| {
        .                                   cp.card_types.contains(&crate::card::CardType::Creature)
        .                                       && !cp.keywords.contains(&Keyword::Haste)
        .                               })
  128,702 ( 0.00%)              });
   36,772 ( 0.00%)              if sick {
      208 ( 0.00%)                  let exempt = self.battlefield.iter().any(|c| {
    5,626 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (52x)
    1,072 ( 0.00%)                      c.controller == p
        .                                   && c.definition.static_abilities.iter().any(|sa| {
        .                                       matches!(
        .                                           sa.effect,
        .                                           crate::effect::StaticEffect::ControllerCreatureAbilitiesAsThoughHaste
        .                                       )
        .                                   })
        .                           });
      104 ( 0.00%)                  if !exempt {
      416 ( 0.00%)                      return Err(GameError::SummoningSickness(card_id));
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 602.5c — a permanent whose *computed* keyword set carries
        .                   // `CantActivateAbilities` (Detention Vortex's Aura grant, etc.) can't
        .                   // activate its non-mana abilities. Battlefield sources only.
   36,668 ( 0.00%)          if on_battlefield
   36,668 ( 0.00%)              && !is_mana_ability(&ability.effect)
      494 ( 0.00%)              && cp.as_ref().is_some_and(|c| c.keywords.contains(&Keyword::CantActivateAbilities))
        .                   {
        .                       return Err(GameError::AbilitySuppressedByNamedCard);
        .                   }
        .           
        .                   // Once-per-turn: reject if this ability index has already been
        .                   // used since the most recent turn-cleanup. The ability is recorded
        .                   // as "used" *after* successful activation below so failed mana
        .                   // payments / illegal targets don't burn the per-turn budget.
        .                   // (Graveyard activations don't track per-card once-per-turn state
        .                   // since the card may move between zones; the gate is no-op.)
        .                   // CR 602.5f — "activate only once each turn" and its "no more than N
        .                   // times each turn" generalization share the permanent's tally.
   36,668 ( 0.00%)          let per_turn_cap = ability
        .                       .max_activations_per_turn
   18,334 ( 0.00%)              .or(ability.once_per_turn.then_some(1));
        .                   if !source_in_gy
        .                       && !source_in_hand
        .                       && !source_in_exile
        .                       && !source_in_command
        .                       && let Some(cap) = per_turn_cap
        .                   {
        .                       let perm = self
        .                           .battlefield
-- line 13458 ----------------------------------------
-- line 13463 ----------------------------------------
        .                       if used as u32 >= cap {
        .                           return Err(GameError::AbilityAlreadyUsedThisTurn);
        .                       }
        .                   }
        .                   // CR 702.56 — Forecast / other hand-activated "once each turn"
        .                   // abilities (the card stays in hand). The hand instance's
        .                   // per-turn budget rides the global `triggered_once_per_turn_used`
        .                   // set, which is cleared at turn cleanup.
   55,002 ( 0.00%)          if (source_in_hand || source_in_command)
        .                       && ability.once_per_turn
        .                       && self.triggered_once_per_turn_used.contains(&(card_id, ability_index))
        .                   {
        .                       return Err(GameError::AbilityAlreadyUsedThisTurn);
        .                   }
        .           
        .                   // CR 702.177 — Exhaust: an exhaust ability can be activated only once
        .                   // per game. `exhausted_abilities` (never cleared at turn start) records
        .                   // spent indices on the source permanent.
  165,006 ( 0.01%)          if !source_in_gy && !source_in_hand && !source_in_exile && !source_in_command && (ability.exhaust || ability.activate_once) {
        .                       let perm = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == card_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(card_id))?;
        .                       if perm.exhausted_abilities.contains(&ability_index) {
        .                           return Err(GameError::AbilityAlreadyUsedThisTurn);
        .                       }
-- line 13489 ----------------------------------------
-- line 13491 ----------------------------------------
        .           
        .                   // Sorcery-speed gate: reject the activation if the ability is
        .                   // flagged sorcery-speed and the controller can't currently
        .                   // cast sorceries (not their main phase, or stack non-empty).
        .                   // Used by cards with printed "Activate only as a sorcery" — e.g.
        .                   // SOS Summoned Dromedary, Stone Docent, Cauldron of Essence's
        .                   // reanimation. The pre-fix flow let upkeep activations leak
        .                   // through silently.
   36,668 ( 0.00%)          if ability.sorcery_speed && !self.can_cast_sorcery_speed(p) {
        .                       return Err(GameError::SorcerySpeedOnly);
        .                   }
        .           
        .                   // Per-card "activate only if …" gate. Evaluated against the
        .                   // controller/source context before any cost is paid. Used by
        .                   // cards like Resonating Lute (`{T}: Draw a card. Activate only
        .                   // if you have seven or more cards in your hand.`), Potioner's
        .                   // Trove (`{T}: gain 2 life. Activate only if you've cast an IS
        .                   // spell this turn.`), and similar conditional activations.
   55,002 ( 0.00%)          if let Some(cond) = &ability.condition {
        .                       let ctx = crate::game::effects::EffectContext {
        .                           controller: p,
        .                           source: Some(card_id),
        .                           targets: vec![],
        .                           trigger_source: None,
        .                           mode: 0,
        .                           // The gate can read the announced X (Soul Foundry's "X is the
        .                           // mana value of that card").
-- line 13517 ----------------------------------------
-- line 13535 ----------------------------------------
        .                       if !self.evaluate_predicate(cond, &ctx) {
        .                           return Err(GameError::AbilityConditionNotMet);
        .                       }
        .                   }
        .           
        .                   // Reject the activation if the chosen target has hexproof / shroud /
        .                   // protection / Leyline-of-Sanctity-style player hexproof. Mana-only
        .                   // and self-targeting abilities don't pass a target so they bypass.
  110,004 ( 0.00%)          if let Some(tgt) = &target {
       38 ( 0.00%)              self.check_target_legality(tgt, p)?;
      266 ( 0.00%)              if self.ability_target_has_protection(tgt, card_id) {
   83,690 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::ability_target_has_protection (38x)
        .                           return Err(GameError::TargetHasProtection(card_id));
        .                       }
        .                       // Ward enforcement happens via push_ward_triggers_for_cast
        .                       // after finalize_cast, not as a synchronous cost payment.
        .                       let _ = tgt; let _ = p;
        .                   }
        .           
        .                   // Enforce the ability's own target selection requirement (e.g.
        .                   // Wasteland's "destroy target nonbasic land", Goblin Bombardment's
        .                   // "any target"). Spell casts already validate this in `cast_spell`;
        .                   // activated abilities went unchecked, which let bots/UIs aim a
        .                   // Wasteland at a Plains. Mirror the cast-side gate for parity.
        .                   if let Some(tgt) = &target
       76 ( 0.00%)              && let Some(filter) = ability
        .                           .effect
      152 ( 0.00%)                  .target_filter_for_slot_in_mode(0, chosen_mode)
    3,458 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode (38x)
        .                           .map(|f| f.resolve_x(x_value.unwrap_or(0)))
        .                       && !self.evaluate_requirement_static(&filter, tgt, p, Some(card_id))
        .                   {
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // Two-target activated abilities (Autumn-Tail): validate slots 1+ the
        .                   // same way — legality (hexproof/shroud/…) plus the per-slot filter.
-- line 13569 ----------------------------------------
-- line 13581 ----------------------------------------
        .                           return Err(GameError::SelectionRequirementViolated);
        .                       }
        .                   }
        .                   // CR 601.2c — Flagbearer applies to activated abilities too.
        .                   {
        .                       let chosen: Vec<Target> =
        .                           target.iter().cloned().chain(additional_targets.iter().cloned()).collect();
        .                       let slots: Vec<Option<crate::card::SelectionRequirement>> = (0..chosen.len())
      190 ( 0.00%)                  .map(|i| ability.effect.target_filter_for_slot_in_mode(i as u8, chosen_mode).cloned())
    3,458 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode (38x)
        .                           .collect();
  128,338 ( 0.00%)              if self.flagbearer_violation(p, &chosen, &slots) {
4,364,760 ( 0.14%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::flagbearer_violation (18,334x)
        .                           return Err(GameError::InvalidTarget);
        .                       }
   55,002 ( 0.00%)          }
  425,900 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<core::option::Option<crabomination_base::card::SelectionRequirement>>> (18,334x)
        .                   // Reject when the effect still references a target slot we weren't
        .                   // given (a two-target ability invoked with too few targets — the
        .                   // bot / affordance path passes only slot 0). Without this the extra
        .                   // slot resolves to nothing and the effect half-fires.
        .                   // A divided-damage / "up to N targets" ability's slots past its minimum
        .                   // are optional (CR 115.3), so only a genuinely required slot rejects.
        .                   let next_slot = 1 + additional_targets.len() as u8;
   18,334 ( 0.00%)          if target.is_some()
      304 ( 0.00%)              && ability.effect.target_filter_for_slot_in_mode(next_slot, chosen_mode).is_some()
    3,458 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode (38x)
        .                       && !ability.effect.target_slot_optional_x(
        .                           next_slot,
        .                           chosen_mode,
        .                           x_value.unwrap_or(0),
        .                       )
        .                   {
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
-- line 13611 ----------------------------------------
-- line 13613 ----------------------------------------
        .                   // Graveyard-card targets (reanimation abilities — Cauldron of
        .                   // Essence's "Return target creature card from your graveyard"):
        .                   // the in-scene cursor can't select graveyard cards, so clients
        .                   // activate with `target: None`. Bind slot 0 here — a `wants_ui`
        .                   // activator with a real choice picks via a `ChooseCards` modal
        .                   // (suspend + clean replay, like the cost picks below); otherwise
        .                   // auto-pick. Without this the effect went on the stack unbound
        .                   // and silently no-opped.
   73,336 ( 0.00%)          let mut target = target;
        .                   if target.is_none()
   91,480 ( 0.00%)              && ability.effect.prefers_graveyard_target()
  530,584 ( 0.02%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::prefers_graveyard_target (18,296x)
        .                       && let Some(filter) = ability
        .                           .effect
        .                           .target_filter_for_slot(0)
        .                           .map(|f| f.resolve_x(x_value.unwrap_or(0)))
        .                   {
        .                       let candidates: Vec<CardId> = self
        .                           .players
        .                           .iter()
-- line 13631 ----------------------------------------
-- line 13695 ----------------------------------------
        .                           .map(Target::Permanent);
        .                   }
        .           
        .                   // Pre-flight life-cost gate: reject activation cleanly when the
        .                   // controller doesn't have enough life. Mirror the mana-cost
        .                   // pre-pay check (we want a clean error, not a "you can't pay
        .                   // and just lost a tap" surprise). Activation that gets past
        .                   // this point will deduct the life after tap/mana succeed.
   55,002 ( 0.00%)          if ability.life_cost > 0 && self.players[p].life < ability.life_cost as i32 {
        .                       return Err(GameError::InsufficientLife);
        .                   }
        .                   // Pre-flight variable life-cost gate ("Pay X life", CR 107.16): the
        .                   // spend equals the activation's chosen `x_value`. Reject cleanly when
        .                   // short so tap/mana aren't burned (Krumar Initiate).
   36,668 ( 0.00%)          if ability.x_life_cost && self.players[p].life < x_value.unwrap_or(0) as i32 {
        .                       return Err(GameError::InsufficientLife);
        .                   }
        .                   // "Pay half your life, rounded up" (CR 118.4 — Lurking Evil).
   36,668 ( 0.00%)          if ability.half_life_cost && self.players[p].life <= 0 {
        .                       return Err(GameError::InsufficientLife);
        .                   }
        .           
        .                   // Pre-flight {E} gate (CR 107.16): reject cleanly when the controller
        .                   // lacks the energy. Mirrors the mana/life pre-pay checks; the spend
        .                   // happens after tap/mana/life succeed.
   55,002 ( 0.00%)          if ability.energy_cost > 0 && self.players[p].energy < ability.energy_cost {
        .                       return Err(GameError::InsufficientEnergy);
        .                   }
        .                   // Pre-flight variable-{E} gate: "Pay X {E}" abilities spend the
        .                   // activation's chosen `x_value` energy (CR 107.16); reject cleanly when
        .                   // short. The same X gates the target filter (ManaValueExactlyXFromCost).
   36,668 ( 0.00%)          if ability.energy_x_cost && self.players[p].energy < x_value.unwrap_or(0) {
        .                       return Err(GameError::InsufficientEnergy);
        .                   }
        .           
        .                   // Pre-flight collect-evidence gate (CR 701.59): reject cleanly when the
        .                   // graveyard can't supply the required total mana value, so tap/mana
        .                   // aren't burned. The exile happens after payment succeeds.
   36,668 ( 0.00%)          if let Some(amount) = ability.collect_evidence_cost
        .                       && !self.graveyard_can_collect_evidence(p, amount)
        .                   {
        .                       return Err(GameError::SelectionRequirementViolated);
        .                   }
        .           
        .                   // Pre-flight exile-other-from-gy gate: confirm `count` graveyard
        .                   // cards matching the cost's filter exist, *excluding* the source
        .                   // itself for graveyard activations where source_in_gy is true. If
-- line 13741 ----------------------------------------
-- line 13983 ----------------------------------------
        .                           return Ok(vec![]);
        .                       } else {
        .                           self.auto_pick_lowest_power(&candidates, count)
        .                       }
        .                   } else {
        .                       Vec::new()
        .                   };
        .           
  110,004 ( 0.00%)          let mut sac_other_picks = sac_other_picks;
        .           
        .                   // CR 602.5b — "…and any number of [filter] you control". Zero is a
        .                   // legal payment, so there is no candidate-count gate; a hand-paying
        .                   // activator picks the subset, everything else sacrifices all of them
        .                   // (Sword of the Ages is a finisher — a partial auto-pick has no
        .                   // sensible size).
        .                   if let Some(filter) = ability.sac_any_number_filter.as_ref() {
        .                       let candidates: Vec<CardId> = self
-- line 13999 ----------------------------------------
-- line 14046 ----------------------------------------
        .                           }
        .                           None => sac_other_picks.extend(candidates),
        .                       }
        .                   }
        .           
        .                   // CR 602.5b — statics that bolt an extra "Sacrifice a [filter]" onto
        .                   // matching permanents' activated abilities (Brutal Suppression).
        .                   // Mana abilities are exempt, matching the other activation taxes.
   36,592 ( 0.00%)          if !is_mana_ability(&ability.effect) {
       38 ( 0.00%)              let taxes: Vec<crate::card::SelectionRequirement> = self
        .                           .battlefield
        .                           .iter()
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .filter_map(|sa| match &sa.effect {
        .                               crate::effect::StaticEffect::ActivationAdditionalSacrifice {
        .                                   filter,
        .                                   sacrifice,
        .                               } => Some((filter.clone(), sacrifice.clone())),
-- line 14063 ----------------------------------------
-- line 14064 ----------------------------------------
        .                               _ => None,
        .                           })
        .                           .filter(|(filter, _)| {
        .                               self.battlefield_find(card_id)
        .                                   .is_some_and(|c| self.evaluate_requirement_on_card(filter, c, p))
        .                           })
        .                           .map(|(_, sacrifice)| sacrifice)
        .                           .collect();
      152 ( 0.00%)              for sacrifice in taxes {
        .                           let pick = self
        .                               .battlefield
        .                               .iter()
        .                               .filter(|c| c.controller == p && !sac_other_picks.contains(&c.id))
        .                               .find(|c| self.evaluate_requirement_on_card(&sacrifice, c, p))
        .                               .map(|c| c.id);
        .                           match pick {
        .                               Some(id) => sac_other_picks.push(id),
-- line 14080 ----------------------------------------
-- line 14215 ----------------------------------------
        .                       }
        .                       self.auto_pick_lowest_power(&candidates, count)
        .                   } else {
        .                       Vec::new()
        .                   };
        .           
        .                   // Pre-flight "put a card from your hand on top of your library" gate
        .                   // (CR 602.5b — Hidden Retreat). Lowest mana value, like discard costs.
   73,336 ( 0.00%)          let library_top_pick: Option<CardId> = if ability.put_hand_on_library_cost {
        .                       match self.players[p]
        .                           .hand
        .                           .iter()
        .                           .filter(|c| c.id != card_id)
        .                           .min_by_key(|c| c.definition.cost.cmc())
        .                       {
        .                           Some(c) => Some(c.id),
        .                           None => return Err(GameError::SelectionRequirementViolated),
-- line 14231 ----------------------------------------
-- line 14303 ----------------------------------------
        .                       }
        .                   } else {
        .                       Vec::new()
        .                   };
        .           
        .                   // Pre-flight process gate: "Put N cards an opponent owns from exile
        .                   // into that player's graveyard:" is a real cost, so too few eligible
        .                   // exile cards means the ability can't be activated (Cryptic Cruiser).
   36,668 ( 0.00%)          let process_picks: Vec<CardId> = match ability.process_cost {
        .                       Some(count) => {
        .                           let opponents = self.opponents_of(p);
        .                           let picks: Vec<CardId> = self
        .                               .exile
        .                               .iter()
        .                               .filter(|c| opponents.contains(&c.owner))
        .                               .map(|c| c.id)
        .                               .take(count as usize)
-- line 14319 ----------------------------------------
-- line 14408 ----------------------------------------
        .                           .unwrap_or(0);
        .                       if have < x_value.unwrap_or(0) {
        .                           return Err(GameError::SelectionRequirementViolated);
        .                       }
        .                   }
        .           
        .                   // Pre-flight remove-counters-from-among gate (Hopeful Initiate): the
        .                   // matching permanents you control must together carry `count` counters.
  110,004 ( 0.00%)          if let Some((kinds, count, filter)) = counter_drain_cost(&ability) {
  476,684 ( 0.02%)  => crabomination/src/game/actions.rs:crabomination::game::actions::counter_drain_cost (18,334x)
        .                       let have: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p)
        .                           .filter(|c| self.evaluate_requirement_static(&filter, &Target::Permanent(c.id), p, Some(card_id)))
        .                           .map(|c| drainable_counters(c, kinds.as_deref()))
        .                           .sum();
        .                       if have < count {
-- line 14424 ----------------------------------------
-- line 14443 ----------------------------------------
        .                   }
        .           
        .                   // Apply self-counter cost reduction (Strixhaven Book artifacts).
        .                   // Subtracts one generic pip per counter of the specified kind on
        .                   // the source permanent. Clamped at the printed generic total via
        .                   // `ManaCost::reduce_generic`. Only applies when the source is on
        .                   // the battlefield (graveyard-activations skip; the source carries
        .                   // no live counter pool there).
   55,002 ( 0.00%)          let mut effective_mana_cost = if ability.mana_cost.has_x() {
  201,674 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (18,334x)
        .                       // Bind the X value from the action into the printed cost so
        .                       // the player has to actually pay X generic mana. Used by
        .                       // Pernicious Deed's `{X}, Sacrifice this: …` activation,
        .                       // future Walking Ballista-style `{X}` activations.
        .                       match ability.x_mana_color {
        .                           // CR 601.2g — "spend only [colour] mana on X".
        .                           Some(c) => ability.mana_cost.with_x_value_colored(x_value.unwrap_or(0), c),
        .                           None => ability.mana_cost.with_x_value(x_value.unwrap_or(0)),
-- line 14459 ----------------------------------------
-- line 14460 ----------------------------------------
        .                       }
        .                   } else {
        .                       ability.mana_cost.clone()
        .                   };
        .                   // A generic cost whose amount the game state defines rather than the
        .                   // activator (Bargaining Table's "X is the number of cards in an
        .                   // opponent's hand"). Folded into the printed generic; the body reads
        .                   // it back via `Value::XFromCost`.
   91,670 ( 0.00%)          let state_defined_x = ability.generic_cost_value.as_ref().map(|v| {
        .                       let ctx = crate::game::effects::EffectContext::for_trigger(card_id, p, None, 0);
        .                       self.evaluate_value(v, &ctx).max(0) as u32
        .                   });
        .                   if let Some(n) = state_defined_x {
        .                       effective_mana_cost.add_generic(n);
        .                   }
        .                   let activated_x = if let Some(n) = state_defined_x {
        .                       n
   55,002 ( 0.00%)          } else if ability.mana_cost.has_x()
  201,674 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::mana::ManaCost::has_x (18,334x)
  110,004 ( 0.00%)              || ability.sac_other_x
        .                       || ability.exile_other_x
        .                       || ability.remove_counter_x.is_some()
   91,670 ( 0.00%)              || ability.remove_counter_among_x.is_some()
        .                       || ability.energy_x_cost
        .                       || ability.x_life_cost
        .                   {
        .                       x_value.unwrap_or(0)
        .                   } else {
        .                       0
        .                   };
   91,670 ( 0.00%)          if let Some(kind) = ability.self_counter_cost_reduction
        .                       && !source_in_gy
        .                       && let Some(src) = self.battlefield_find(card_id)
        .                   {
        .                       let count = src.counter_count(kind);
        .                       if count > 0 {
        .                           effective_mana_cost.reduce_generic(count);
        .                       }
        .                   }
        .                   // "Pay {1} for each [kind] counter on this creature" (Skeleton
        .                   // Scavengers) — the surcharge mirror of the reduction above.
   55,002 ( 0.00%)          if let Some(kind) = ability.mana_cost_per_self_counter
        .                       && let Some(src) = self.battlefield_find(card_id)
        .                   {
        .                       let count = src.counter_count(kind);
        .                       if count > 0 {
        .                           effective_mana_cost.add_generic(count);
        .                       }
        .                   }
        .                   // "Costs {1} less for each [kind] counter on [filter] you control"
        .                   // (Deepwood Denizen).
   36,668 ( 0.00%)          if let Some((kind, filter)) = &ability.cost_reduction_per_counter {
        .                       let count: u32 = self.with_frozen_layers(|g| {
        .                           g.battlefield
        .                               .iter()
        .                               .filter(|c| {
        .                                   c.controller == p
        .                                       && g.evaluate_requirement_static(
        .                                           filter,
        .                                           &Target::Permanent(c.id),
-- line 14518 ----------------------------------------
-- line 14523 ----------------------------------------
        .                               .map(|c| c.counter_count(*kind))
        .                               .sum()
        .                       });
        .                       if count > 0 {
        .                           effective_mana_cost.reduce_generic(count);
        .                       }
        .                   }
        .                   // "Costs {1} less for each [filter] you control" (channel lands).
   36,668 ( 0.00%)          if let Some(filter) = &ability.cost_reduction_per {
        .                       let count = self.with_frozen_layers(|g| {
        .                           g.battlefield
        .                               .iter()
        .                               .filter(|c| {
        .                                   c.controller == p
        .                                       && g.evaluate_requirement_static(
        .                                           filter,
        .                                           &Target::Permanent(c.id),
-- line 14539 ----------------------------------------
-- line 14544 ----------------------------------------
        .                               .count() as u32
        .                       });
        .                       if count > 0 {
        .                           effective_mana_cost.reduce_generic(count);
        .                       }
        .                   }
        .                   // "Costs {X} less to activate, where X is this creature's power"
        .                   // (The Dominion Bracelet, granted to its bearer).
   36,668 ( 0.00%)          if ability.cost_reduction_per_equipped_power {
        .                       let power = self
        .                           .computed_permanent(card_id)
        .                           .map(|c| c.power.max(0) as u32)
        .                           .unwrap_or(0);
        .                       if power > 0 {
        .                           effective_mana_cost.reduce_generic(power);
        .                       }
        .                   }
        .                   // "Costs {1} less for each [filter] card in your graveyard"
        .                   // (Battlefield Butcher).
   36,668 ( 0.00%)          if let Some(filter) = &ability.cost_reduction_per_graveyard {
        .                       let count = self.players[p]
        .                           .graveyard
        .                           .iter()
        .                           .filter(|c| crate::game::layers::requirement_matches_card(filter, c, p))
        .                           .count() as u32;
        .                       if count > 0 {
        .                           effective_mana_cost.reduce_generic(count);
        .                       }
        .                   }
        .                   // Zirda — non-mana activated abilities cost {N} less (generic only),
        .                   // floored at one mana of the printed cost.
   36,668 ( 0.00%)          if !is_mana_ability(&ability.effect) && !effective_mana_cost.symbols.is_empty() {
        .                       let total: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p)
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::ActivationCostReduction { amount } => amount,
        .                               _ => 0,
-- line 14583 ----------------------------------------
-- line 14586 ----------------------------------------
        .                       if total > 0 {
        .                           let max_cut = effective_mana_cost.cmc().saturating_sub(1);
        .                           effective_mana_cost.reduce_generic(total.min(max_cut));
        .                       }
        .                   }
        .                   // Biomancer's Familiar / Training Grounds — activated abilities of
        .                   // creatures you control cost {N} less (generic-only, floored at one
        .                   // mana of the printed cost).
   18,334 ( 0.00%)          let source_is_your_creature = self
        .                       .battlefield_find(card_id)
   73,336 ( 0.00%)              .is_some_and(|c| c.controller == p && c.definition.is_creature());
  292,660 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (18,334x)
   37,124 ( 0.00%)          if source_is_your_creature && !effective_mana_cost.symbols.is_empty() {
        .                       let total: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p)
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::YourCreatureActivatedAbilitiesCostLess { amount } => amount,
        .                               _ => 0,
-- line 14605 ----------------------------------------
-- line 14607 ----------------------------------------
        .                           .sum();
        .                       if total > 0 {
        .                           let max_cut = effective_mana_cost.cmc().saturating_sub(1);
        .                           effective_mana_cost.reduce_generic(total.min(max_cut));
        .                       }
        .                   }
        .                   // Power Artifact — the enchanted permanent's activated abilities cost
        .                   // {N} less (generic-only, floored at one mana of the printed cost).
   36,668 ( 0.00%)          if !effective_mana_cost.symbols.is_empty() {
        .                       let total: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.attached_to == Some(card_id))
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::AttachedActivatedAbilitiesCostLess { amount } => {
        .                                   amount
-- line 14623 ----------------------------------------
-- line 14627 ----------------------------------------
        .                           .sum();
        .                       if total > 0 {
        .                           let max_cut = effective_mana_cost.cmc().saturating_sub(1);
        .                           effective_mana_cost.reduce_generic(total.min(max_cut));
        .                       }
        .                   }
        .                   // Boom Scholar — exhaust abilities of your *other* permanents cost {N}
        .                   // less (generic-only, floored at one mana of the printed cost).
   36,668 ( 0.00%)          if ability.exhaust && !effective_mana_cost.symbols.is_empty() {
        .                       let total: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p && c.id != card_id)
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::OtherExhaustActivationCostReduction { amount } => {
        .                                   amount
-- line 14643 ----------------------------------------
-- line 14647 ----------------------------------------
        .                           .sum();
        .                       if total > 0 {
        .                           let max_cut = effective_mana_cost.cmc().saturating_sub(1);
        .                           effective_mana_cost.reduce_generic(total.min(max_cut));
        .                       }
        .                   }
        .                   // Skyseer's Chariot — abilities of sources with the named card name
        .                   // cost {N} more. Unlike the taxes below this one covers mana abilities.
   36,668 ( 0.00%)          if let Some(name) = self.battlefield_find(card_id).map(|c| c.definition.name) {
        .                       let tax: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.named_card.as_deref() == Some(name))
        .                           .flat_map(|c| c.definition.static_abilities.iter())
        .                           .map(|sa| match sa.effect {
        .                               crate::effect::StaticEffect::NamedSourcesActivationTax { amount } => amount,
        .                               _ => 0,
        .                           })
        .                           .sum();
   36,668 ( 0.00%)              if tax > 0 {
        .                           effective_mana_cost.symbols.push(crate::mana::ManaSymbol::Generic(tax));
        .                       }
        .                   }
        .           
        .                   // Suppression Field — non-mana activated abilities cost {N} more,
        .                   // for every player's activations. Tithe Taker adds the same tax but
        .                   // only to opponents' activations on its controller's turn.
   36,592 ( 0.00%)          if !is_mana_ability(&ability.effect) {
       38 ( 0.00%)              let tax: u32 = self
        .                           .battlefield
        .                           .iter()
        .                           .flat_map(|c| {
    4,136 ( 0.00%)                      let opp_your_turn = c.controller != p && c.controller == self.active_player_idx;
        .                               let attached_here = c.attached_to == Some(card_id);
        .                               c.definition.static_abilities.iter().map(move |sa| match sa.effect {
        .                                   crate::effect::StaticEffect::ActivationTax { amount } => amount,
        .                                   crate::effect::StaticEffect::AttachedActivationTax { amount }
        .                                       if attached_here =>
        .                                   {
        .                                       amount
        .                                   }
-- line 14687 ----------------------------------------
-- line 14689 ----------------------------------------
        .                                       if opp_your_turn =>
        .                                   {
        .                                       amount
        .                                   }
        .                                   _ => 0,
        .                               })
        .                           })
        .                           .sum();
       76 ( 0.00%)              if tax > 0 {
        .                           effective_mana_cost.symbols.push(crate::mana::ManaSymbol::Generic(tax));
        .                       }
        .                   }
        .           
        .                   // CR 601.2g — float-spend confirmation. Before tapping anything, if the
        .                   // activator has pre-existing floating mana the mana cost could either
        .                   // spend or avoid (untapped sources can cover it), ask first. Nothing is
        .                   // mutated yet at this point (tap-cost applies below), so suspending is a
-- line 14705 ----------------------------------------
-- line 14707 ----------------------------------------
        .                   // CR 601.2g float-spend confirmation is a *mana payment* question,
        .                   // so it keys on `manual_mana` — the flag that exists for exactly this
        .                   // rule — rather than `wants_ui`, which bot seats also set. Prompting
        .                   // a bot here is the same livelock as the {X} and additional-cost
        .                   // modals: the suspend returns `Ok`, so the probe reports the cast as
        .                   // legal, and the failed replay is rolled back with the decision
        .                   // restored. Latent rather than observed only because the bot stopped
        .                   // floating mana when it stopped pre-tapping its board.
   18,334 ( 0.00%)          if spend_float.is_none()
   55,002 ( 0.00%)              && self.players[p].manual_mana
        .                       && !effective_mana_cost.symbols.is_empty()
        .                       && self.float_spend_is_optional(p, &effective_mana_cost, &ability_spend_kind)
        .                   {
        .                       let float_summary = self.protectable_float(p, &effective_mana_cost).summary();
        .                       let name = self
        .                           .battlefield_find(card_id)
        .                           .map(|c| c.definition.name)
        .                           .unwrap_or("this ability");
-- line 14724 ----------------------------------------
-- line 14747 ----------------------------------------
        .                   // payment rolls back both the auto-tap of mana sources AND the
        .                   // tap-cost on the source itself.
        .                   let needs_payment = !effective_mana_cost.symbols.is_empty();
        .                   let pre_snapshot = needs_payment.then(|| self.snapshot_payment_state(p));
        .           
        .                   // Pay tap cost. Graveyard activations can't tap (the source is not
        .                   // a permanent), so we reject any `tap_cost: true` ability from a
        .                   // graveyard source as a guard against malformed card definitions.
   36,668 ( 0.00%)          if ability.tap_cost {
   36,668 ( 0.00%)              if source_in_gy || source_in_hand || source_in_exile || source_in_command {
        .                           return Err(GameError::CardIsTapped(card_id));
        .                       }
        .                       let perm = self
        .                           .battlefield
        .                           .iter_mut()
        .                           .find(|c| c.id == card_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(card_id))?;
   36,668 ( 0.00%)              if perm.tapped {
        .                           return Err(GameError::CardIsTapped(card_id));
        .                       }
   18,312 ( 0.00%)              perm.tapped = true;
        .                   }
        .                   // CR 107.17 — pay a {Q} cost: the source must be tapped; untap it.
   36,624 ( 0.00%)          if ability.untap_self_cost {
        .                       if source_in_gy || source_in_hand || source_in_exile {
        .                           return Err(GameError::CardIsTapped(card_id));
        .                       }
        .                       let perm = self
        .                           .battlefield
        .                           .iter_mut()
        .                           .find(|c| c.id == card_id)
        .                           .ok_or(GameError::CardNotOnBattlefield(card_id))?;
-- line 14778 ----------------------------------------
-- line 14781 ----------------------------------------
        .                       }
        .                       perm.tapped = false;
        .                   }
        .           
        .                   let mut auto_mana_events = Vec::new();
        .                   // CR 106.6 — per-colour breakdown of what actually funded this
        .                   // activation, threaded to the resolving body (Protective Sphere).
        .                   let mut activation_mana_colors = Vec::new();
   54,936 ( 0.00%)          if let Some(snapshot) = pre_snapshot {
        .                       let forced_only = self.players[p].manual_mana;
        .                       // Restricted mana may fund this only per the source's spend
        .                       // context (e.g. ArtifactOnly mana for an artifact's ability).
        .                       let receipt = self.try_pay_after_snapshot_mode(
        .                           p,
        .                           &effective_mana_cost,
        .                           snapshot,
        .                           forced_only,
-- line 14797 ----------------------------------------
-- line 14803 ----------------------------------------
        .                       self.pay_life_cost(p, receipt.side_effects.life_lost);
        .                       auto_mana_events = receipt.auto_events;
        .                   }
        .           
        .                   // Pay the life cost. Tap and mana are committed; the life
        .                   // payment is now safe (the pre-flight gate above guaranteed
        .                   // sufficient life). Emits a LifeLost event so trigger / replay
        .                   // observers see the cost.
   54,936 ( 0.00%)          if ability.life_cost > 0 {
        .                       let applied = self.adjust_life_applied(p, -(ability.life_cost as i32));
        .                       if applied < 0 {
        .                           auto_mana_events.push(GameEvent::LifeLost {
        .                               player: p,
        .                               amount: (-applied) as u32,
        .                           });
        .                       }
        .                   }
   36,624 ( 0.00%)          if ability.half_life_cost {
        .                       let half = self.players[p].life.div_euclid(2) + self.players[p].life.rem_euclid(2);
        .                       let applied = self.adjust_life_applied(p, -half);
        .                       if applied < 0 {
        .                           auto_mana_events
        .                               .push(GameEvent::LifeLost { player: p, amount: (-applied) as u32 });
        .                       }
        .                   }
   36,624 ( 0.00%)          if ability.x_life_cost {
        .                       let paid = x_value.unwrap_or(0) as i32;
        .                       let applied = self.adjust_life_applied(p, -paid);
        .                       if applied < 0 {
        .                           auto_mana_events.push(GameEvent::LifeLost {
        .                               player: p,
        .                               amount: (-applied) as u32,
        .                           });
        .                       }
        .                   }
        .           
        .                   // Spend the {E} cost (CR 107.16). Tap/mana/life are committed; the
        .                   // pre-flight gate above guaranteed sufficient energy. Like the
        .                   // `Effect::PayEnergy` spend path, no event is emitted.
   54,936 ( 0.00%)          if ability.energy_cost > 0 {
        .                       self.spend_energy(p, ability.energy_cost);
        .                   }
   36,624 ( 0.00%)          if ability.energy_x_cost {
        .                       self.spend_energy(p, x_value.unwrap_or(0));
        .                   }
        .                   // Pay the collect-evidence cost (CR 701.59). Tap/mana/life/energy are
        .                   // committed; the pre-flight gate guaranteed the graveyard can afford it.
   36,624 ( 0.00%)          if let Some(amount) = ability.collect_evidence_cost {
        .                       let mut ev = self.collect_evidence_from_graveyard(p, amount);
        .                       auto_mana_events.append(&mut ev);
        .                   }
        .           
   73,248 ( 0.00%)          let mut events = auto_mana_events;
        .                   // The paid tap-cost is a "becomes tapped" event (Vampire Envoy,
        .                   // Vorinclex's opponent-land lock). Emitted after the mana payment
        .                   // succeeded so a rolled-back activation never announces a tap.
   36,624 ( 0.00%)          if ability.tap_cost {
  146,496 ( 0.00%)              events.push(GameEvent::PermanentTapped { card_id, actor: None, as_attacker: false });
        .                       // CR 605 — a mana ability's tap is also a "tapped for mana" event
        .                       // (Extraplanar Lens), distinct from the plain tap above.
   36,592 ( 0.00%)              if is_mana_ability(&ability.effect) {
  128,072 ( 0.00%)                  events.push(GameEvent::TappedForMana { card_id, player: p });
   73,184 ( 0.00%)                  if self.battlefield_find(card_id).is_some_and(|c| c.definition.is_land()) {
  238,418 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (18,296x)
   36,212 ( 0.00%)                      self.players[p].tapped_land_for_mana_this_turn = true;
  543,180 ( 0.02%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (18,106x)
        .                           }
        .                       }
        .                   }
   36,624 ( 0.00%)          if ability.untap_self_cost {
        .                       events.push(GameEvent::PermanentUntapped { card_id });
        .                   }
        .                   // Mana abilities don't emit the activation event — every printed
        .                   // "whenever an opponent activates an ability" trigger carves them
        .                   // out (Flamescroll Celebrant, CR 605.1), and the log skips the
        .                   // tap-for-mana spam.
   36,592 ( 0.00%)          if !is_mana_ability(&ability.effect) {
      128 ( 0.00%)              events.push(GameEvent::AbilityActivated {
        .                           source: card_id,
       16 ( 0.00%)                  exhaust: ability.exhaust,
       32 ( 0.00%)                  adapt: ability.effect.is_adapt(),
      112 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::is_adapt (16x)
       64 ( 0.00%)                  tap_cost: ability.tap_cost || ability.untap_self_cost,
        .                       });
        .                   }
        .           
        .                   // Mark the ability as used for the once-per-turn budget. (After
        .                   // tap/mana cost validation succeeds, before sacrifice or stack
        .                   // queueing — all of which are guaranteed to commit if we get here.)
  238,056 ( 0.01%)          if (ability.once_per_turn || ability.max_activations_per_turn.is_some())
        .                       && !source_in_gy
        .                       && !source_in_hand
        .                       && let Some(card) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       card.once_per_turn_used.push(ability_index);
        .                   }
        .                   // CR 702.56 / 902.5 — record a hand (Forecast) or command-zone
        .                   // (Vanguard) once-per-turn activation.
-- line 14897 ----------------------------------------
-- line 14898 ----------------------------------------
        .                   if ability.once_per_turn && (source_in_hand || source_in_command) {
        .                       self.triggered_once_per_turn_used.insert((card_id, ability_index));
        .                   }
        .                   // CR 603.4 — "when you next activate an exhaust ability that isn't a
        .                   // mana ability this turn" (Pit Automaton). Claimed here so the
        .                   // activation consumes exactly one watcher; pushed after the ability
        .                   // itself lands on the stack so the copy resolves first.
        .                   let exhaust_watchers: Vec<crate::game::types::DelayedTrigger> =
   54,936 ( 0.00%)              if ability.exhaust && !is_mana_ability(&ability.effect) {
        .                           let (watchers, rest): (Vec<_>, Vec<_>) = std::mem::take(&mut self.delayed_triggers)
        .                               .into_iter()
        .                               .partition(|dt| {
        .                                   dt.controller == p
        .                                       && matches!(
        .                                           dt.kind,
        .                                           crate::game::types::DelayedKind::YourNextExhaustActivationThisTurn
        .                                       )
        .                               });
        .                           self.delayed_triggers = rest;
        .                           watchers
        .                       } else {
        .                           Vec::new()
        .                       };
        .                   // CR 702.177 — record the exhaust activation (never cleared this game).
   91,560 ( 0.00%)          if (ability.exhaust || ability.activate_once)
        .                       && !source_in_gy
        .                       && let Some(card) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       card.exhausted_abilities.push(ability_index);
        .                   }
        .           
        .                   // P/T of a creature sacrificed as part of this activation's cost,
        .                   // re-stamped at resolution via `Effect::WithSacrificedPt` so
-- line 14930 ----------------------------------------
-- line 14950 ----------------------------------------
        .                   // reference the source by id will miss it on the battlefield —
        .                   // which matches the Oracle (sac is part of the activation cost,
        .                   // so the source is in the graveyard by the time the ability
        .                   // resolves). Cards whose effect references self after sacrifice
        .                   // (Greater Good's "draw cards equal to its power") need to
        .                   // capture that data via `Effect::SacrificeAndRemember` instead.
        .                   // CR 702.6 — "Unattach this Equipment" as a cost (Sunforger): the
        .                   // source must be attached, and it detaches before the effect runs.
   36,624 ( 0.00%)          if ability.unattach_cost {
        .                       // On an equipment-GRANTED ability (Blinding Powder, Shuriken) the
        .                       // source is the equipped creature, so the cost detaches the granter
        .                       // instead (CR 702.6e).
        .                       let detach = match self.battlefield.iter().find(|c| c.id == card_id) {
        .                           Some(c) if c.attached_to.is_some() => Some(card_id),
        .                           Some(_) => self
        .                               .battlefield
        .                               .iter()
-- line 14966 ----------------------------------------
-- line 14976 ----------------------------------------
        .                       };
        .                       match detach.and_then(|id| self.battlefield.iter_mut().find(|c| c.id == id)) {
        .                           Some(c) => c.attached_to = None,
        .                           None => return Err(GameError::SelectionRequirementViolated),
        .                       }
        .                   }
        .                   // "Exile [this Equipment]" as a cost on an ability the Equipment grants
        .                   // its bearer: the cost exiles the granter, not the payer.
   36,624 ( 0.00%)          if ability.exile_attachment_cost {
        .                       let granter = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| {
        .                               c.attached_to == Some(card_id)
        .                                   && c.definition
        .                                       .equipped_bonus
        .                                       .as_ref()
-- line 14992 ----------------------------------------
-- line 14995 ----------------------------------------
        .                           .map(|c| c.id)
        .                           .ok_or(GameError::SelectionRequirementViolated)?;
        .                       self.remove_from_battlefield_to_exile(granter);
        .                       events.push(GameEvent::PermanentExiled { card_id: granter });
        .                   }
        .                   // "Return N [filter] you control to their owner's hand" as a cost
        .                   // (Floodbringer). Bounce the cheapest matches so a bot doesn't throw
        .                   // away its best permanents.
   36,624 ( 0.00%)          if let Some((filter, n)) = &ability.return_permanent_cost {
        .                       let mut pool: Vec<(CardId, u32)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.controller == p
        .                                   && self.evaluate_requirement_static(
        .                                       filter,
        .                                       &Target::Permanent(c.id),
-- line 15011 ----------------------------------------
-- line 15027 ----------------------------------------
        .                               &ctx,
        .                               &mut events,
        .                           );
        .                       }
        .                   }
        .                   // "Tap N untapped [filter] you control" as a cost (Crookclaw Elder).
        .                   // Taps the least useful matches first (lowest power) so a bot doesn't
        .                   // tap out its best attackers to draw a card.
   36,624 ( 0.00%)          if let Some((filter, n)) = &ability.tap_others_cost {
        .                       let mut pool: Vec<(CardId, i32)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.controller == p
        .                                   && !c.tapped
        .                                   && c.id != card_id
        .                                   && self.evaluate_requirement_static(
-- line 15043 ----------------------------------------
-- line 15059 ----------------------------------------
        .                           }
        .                           events.push(GameEvent::PermanentTapped {
        .                               card_id: id,
        .                               actor: Some(p),
        .                               as_attacker: false,
        .                           });
        .                       }
        .                   }
   36,624 ( 0.00%)          if ability.sac_cost {
        .                       let is_creature = self.permanent_is_creature(card_id);
        .                       // The activator is the player paying the cost; the
        .                       // sacrifice attribution should match the controller of the
        .                       // sacrificed permanent, which is `p` (priority player) for
        .                       // activated abilities since you only activate abilities of
        .                       // permanents you control.
        .                       let sac_who = self
        .                           .battlefield_find(card_id)
-- line 15075 ----------------------------------------
-- line 15126 ----------------------------------------
        .                       // creatures.
        .                       events.push(GameEvent::PermanentSacrificed { card_id, who: sac_who });
        .                       let mut die_evs = self.remove_to_graveyard_with_triggers(card_id);
        .                       events.append(&mut die_evs);
        .                   }
        .                   // Bounce-self-as-cost (CR 602.5b "…and return it to its owner's hand:"
        .                   // — Magosi, the Waterveil). Paid after tap/mana/counter costs, so the
        .                   // ability is already on its way to the stack when the source leaves.
   36,624 ( 0.00%)          if ability.bounce_self_cost
        .                       && let Some(snap) = self.battlefield_find(card_id).cloned()
        .                   {
        .                       self.leaves_bf_lki.insert(card_id, snap);
        .                       let mut evs = self.remove_from_battlefield_to_hand(card_id);
        .                       events.append(&mut evs);
        .                   }
        .           
        .                   // Return-self-as-cost: with tap/mana/life paid, bounce the source
        .                   // back to its owner's hand (CR 602.5b). Applied before the effect
        .                   // resolves, mirroring `sac_cost`. The mana ability / spell-copy then
        .                   // runs with the source already in hand (Grinning Ignus, Rootha).
   36,624 ( 0.00%)          if ability.return_self_cost
        .                       && let Some(owner) = self.battlefield_find(card_id).map(|c| c.owner)
        .                   {
        .                       let ctx = crate::game::effects::EffectContext::for_spell(owner, None, 0, 0);
        .                       self.move_card_to(
        .                           card_id,
        .                           &crate::effect::ZoneDest::Hand(crate::effect::PlayerRef::Seat(owner)),
        .                           &ctx,
        .                           &mut events,
        .                       );
        .                   }
        .           
        .                   // "Exile a [filter] you control:" as a cost (Food Chain). Stamps the
        .                   // last exiled permanent's mana value for `Value::ExiledForCostManaValue`.
   18,312 ( 0.00%)          self.exiled_for_cost_mana_value = None;
   73,248 ( 0.00%)          for cid in exile_permanent_picks {
        .                       let mv = self.battlefield_find(cid).map(|c| c.definition.cost.cmc() as i32);
        .                       self.move_card_to(
        .                           cid,
        .                           &crate::effect::ZoneDest::Exile,
        .                           &crate::game::effects::EffectContext::for_ability(card_id, p, None),
        .                           &mut events,
        .                       );
        .                       if mv.is_some() {
-- line 15169 ----------------------------------------
-- line 15192 ----------------------------------------
        .                   // creature: …`, Korlash, Heir to Blackblade's `{B}, Sacrifice a
        .                   // Swamp: …`, Witherbloom Harvester-style "sac another creature
        .                   // for an effect" activations.
        .                   // CR 602.5b — the whole cost-sacrifice batch feeds
        .                   // `Value::Sacrificed{Count,TotalPower}` (Sword of the Ages sacrifices
        .                   // any number of creatures and reads their total power). Seed from the
        .                   // source's own `sac_cost` so an earlier resolution's tally can't leak
        .                   // in but this activation's does.
   36,624 ( 0.00%)          self.sacrificed_count = cost_sac_count;
   18,312 ( 0.00%)          self.sacrificed_total_power = cost_sac_total_power;
  128,184 ( 0.00%)          self.cost_sacrificed_batch = if ability.sac_cost { vec![card_id] } else { Vec::new() };
        .                   self.cost_sacrificed_batch.extend(sac_other_picks.iter().copied());
   91,560 ( 0.00%)          for other_cid in sac_other_picks {
        .                       let sac_power = self.battlefield_find(other_cid).map(|c| c.power()).unwrap_or(0);
        .                       self.sacrificed_count += 1;
        .                       self.sacrificed_total_power += sac_power;
        .                       cost_sac_count += 1;
        .                       cost_sac_total_power += sac_power;
        .                       let is_creature = self
        .                           .battlefield_find(other_cid)
        .                           .map(|c| c.definition.is_creature())
-- line 15212 ----------------------------------------
-- line 15247 ----------------------------------------
        .                       let mut die_evs = self.remove_to_graveyard_with_triggers(other_cid);
        .                       events.append(&mut die_evs);
        .                   }
        .           
        .                   // Remove-counter-as-cost (CR 602.5b): with tap/mana/life paid, strip
        .                   // the cost-picked counters off the source (validated by the pre-flight
        .                   // gate). Walking Ballista's `Remove a +1/+1 counter from this: deal 1
        .                   // damage` runs here before the ping resolves.
   54,936 ( 0.00%)          if let Some((kind, count)) = ability.remove_counter_cost
        .                       && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       let ctrl = c.controller;
        .                       c.remove_counters(kind, count);
        .                       if kind == crate::card::CounterType::Oil {
        .                           self.players[ctrl].oil_activity_this_turn = true;
        .                       }
        .                   }
        .           
        .                   // Remove-ALL-counters-as-cost: strip them and stamp the tally so the
        .                   // body's `Value::CountersRemovedAsCost` can scale off it.
   18,312 ( 0.00%)          self.counters_removed_as_cost = 0;
   54,936 ( 0.00%)          if let Some(kind) = ability.remove_all_counters_cost
        .                       && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       let had = c.counter_count(kind);
        .                       c.remove_counters(kind, had);
        .                       self.counters_removed_as_cost = had;
        .                   }
        .           
        .                   // Tap-permanents-as-cost (CR 602.5b): tap the pre-flight picks now that
-- line 15276 ----------------------------------------
-- line 15284 ----------------------------------------
        .                               as_attacker: false,
        .                           });
        .                       }
        .                   }
        .           
        .                   // Add-counter-as-cost (CR 602.5b "Put a verse counter on this:"):
        .                   // paid before the ability goes on the stack, so a body reading the
        .                   // source's counters sees the new total. Yisan.
   54,936 ( 0.00%)          if let Some((kind, count)) = ability.add_counter_cost
        .                       && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       c.add_counters(kind, count);
        .                       events.push(GameEvent::CounterAdded {
        .                           card_id,
        .                           counter_type: kind,
        .                           count,
        .                       });
        .                   }
        .           
        .                   // Exile-top-of-library-as-cost (CR 602.5b — Arc-Slogger). Paid after
        .                   // tap/mana, before the ability resolves; a short library exiles what
        .                   // it has.
   54,936 ( 0.00%)          if ability.exile_top_cost > 0 {
        .                       for _ in 0..ability.exile_top_cost {
        .                           if self.players[p].library.is_empty() {
        .                               break;
        .                           }
        .                           let card = self.players[p].library.remove(0);
        .                           self.place_card_in_dest(card, p, &crate::effect::ZoneDest::Exile, &mut events);
        .                       }
        .                   }
        .           
        .                   // Remove-X-counters-as-cost (Arcbound Javelineer): strip the paid X.
   54,936 ( 0.00%)          if let Some(kind) = ability.remove_counter_x
        .                       && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                   {
        .                       c.remove_counters(kind, x_value.unwrap_or(0));
        .                   }
        .           
        .                   // Remove-counters-from-among-cost (Hopeful Initiate): drain `count`
        .                   // counters distributed across matching permanents you control. The
        .                   // auto-picker takes them lowest-power-first (validated pre-flight).
   54,936 ( 0.00%)          if let Some((kinds, count, filter)) = counter_drain_cost(&ability) {
  476,112 ( 0.02%)  => crabomination/src/game/actions.rs:crabomination::game::actions::counter_drain_cost (18,312x)
        .                       let mut picks: Vec<(CardId, i32)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.controller == p && drainable_counters(c, kinds.as_deref()) > 0)
        .                           .filter(|c| self.evaluate_requirement_static(&filter, &Target::Permanent(c.id), p, Some(card_id)))
        .                           .map(|c| (c.id, c.power()))
        .                           .collect();
        .                       // Weakest first, but the source itself last: an ability whose body
-- line 15334 ----------------------------------------
-- line 15377 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // Discard-as-cost (CR 602.5b): with tap/mana/life paid, discard each
        .                   // cost-picked hand card (validated via the `discard_picks` pre-flight).
        .                   // Fauna Shaman's "Discard a creature card:" cost runs here.
        .                   let mut discarded_for_cost_mv = None;
        .                   let mut discarded_for_cost = 0u32;
   91,560 ( 0.00%)          for cid in discard_picks {
        .                       let mv = self.players[p]
        .                           .hand
        .                           .iter()
        .                           .find(|c| c.id == cid)
        .                           .map(|c| c.definition.cost.cmc());
        .                       if self.discard_card(p, cid, &mut events) {
        .                           discarded_for_cost += 1;
        .                       }
        .                       discarded_for_cost_mv = discarded_for_cost_mv.max(mv);
        .                   }
        .                   // CR 701.9 — the batch fires for a cost payment too; nothing routes a
        .                   // cost through `resolve_effect`, so emit it here.
   36,624 ( 0.00%)          if discarded_for_cost > 0 {
        .                       events.push(GameEvent::DiscardedBatch { player: p, count: discarded_for_cost });
        .                   }
        .                   // Slumbering Tora reads the cost-discarded card's mana value at
        .                   // resolution; `last_discarded_mana_value` is per-resolution scratch and
        .                   // would be cleared before the body runs.
   36,624 ( 0.00%)          self.cost_discarded_mana_value = discarded_for_cost_mv;
        .           
        .                   // Process-as-cost: the pre-flight-picked exile cards go to their
        .                   // owners' graveyards (CR 614.6 hate redirects still apply).
        .                   for cid in process_picks {
        .                       if let Some(card) = Self::take_card(&mut self.exile, cid) {
        .                           self.route_to_graveyard(card, &mut events);
        .                       }
        .                   }
        .           
        .                   // Discard-your-hand-as-cost (Diamond Lion / Lion's Eye Diamond).
   36,624 ( 0.00%)          if ability.discard_hand_cost {
        .                       let hand: Vec<CardId> = self.players[p].hand.iter().map(|c| c.id).collect();
        .                       let mut dumped = 0u32;
        .                       for cid in hand {
        .                           if self.discard_card(p, cid, &mut events) {
        .                               dumped += 1;
        .                           }
        .                       }
        .                       if dumped > 0 {
-- line 15423 ----------------------------------------
-- line 15426 ----------------------------------------
        .                   }
        .           
        .                   // Tap-another-as-cost (CR 602.5b): with tap/mana/life paid, tap the
        .                   // pre-selected untapped permanent. Opposition's "Tap an untapped
        .                   // creature you control" cost runs here. Capture its power first so a
        .                   // Station ability (CR 702.184a) can stamp the counter count at
        .                   // resolution via `Effect::WithTappedPower`.
        .                   let mut tap_other_power: Option<i32> = None;
   36,624 ( 0.00%)          if let Some(other_cid) = tap_other_pick
        .                       && let Some(c) = self.battlefield.iter_mut().find(|c| c.id == other_cid)
        .                   {
        .                       tap_other_power = Some(c.power());
        .                       c.tapped = true;
        .                   }
        .           
        .                   // Tap-N-as-cost (CR 602.5b): with tap/mana/life paid, tap each
        .                   // pre-selected untapped permanent. Heritage Druid's "Tap three
        .                   // untapped Elves you control" cost runs here.
  146,496 ( 0.00%)          self.tapped_for_cost = tap_n_picks.clone();
  604,296 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (18,312x)
   54,936 ( 0.00%)          for other_cid in tap_n_picks {
        .                       if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == other_cid) {
        .                           c.tapped = true;
        .                       }
        .                   }
        .           
        .                   // Exile-a-spell-you-control-as-cost (CR 602.5b): with tap/mana/life
        .                   // paid, pull the cost-picked spell off the stack and exile it — it
        .                   // won't resolve. Nivmagus Elemental.
   18,312 ( 0.00%)          if let Some(spell_id) = exile_spell_pick
        .                       && let Some(pos) = self.stack.iter().position(|item| {
        .                           matches!(item, StackItem::Spell { card, .. } if card.id == spell_id)
        .                       })
        .                       && let StackItem::Spell { card, .. } = self.stack.remove(pos)
        .                   {
        .                       // The spell leaves the stack without resolving; it isn't
        .                       // "countered" (no counter-a-spell payoff should fire).
        .                       self.exile.push(*card);
        .                       self.players[p].cards_exiled_this_turn =
        .                           self.players[p].cards_exiled_this_turn.saturating_add(1);
        .                   }
        .           
        .                   // Return-another-to-hand-as-cost (CR 602.5b): with tap/mana/life paid,
        .                   // bounce each cost-picked permanent to its owner's hand. Quirion
        .                   // Ranger, Wirewood Symbiote.
   73,248 ( 0.00%)          for other_cid in bounce_other_picks {
        .                       self.move_card_to(
        .                           other_cid,
        .                           &crate::effect::ZoneDest::Hand(crate::effect::PlayerRef::OwnerOfMoved),
        .                           &crate::game::effects::EffectContext::for_ability(card_id, p, None),
        .                           &mut events,
        .                       );
        .                   }
        .           
-- line 15478 ----------------------------------------
-- line 15505 ----------------------------------------
        .                   // each cost-picked graveyard card (already validated to exist via
        .                   // the pre-flight `exile_other_picks` lookup). Used by cards like
        .                   // Postmortem Professor's `{1}{B}, Exile an instant or sorcery
        .                   // card from your graveyard: …` (count 1), Lorehold Pledgemage's
        .                   // `{2}{R}{W}, Exile a card from your graveyard: +1/+1 EOT`
        .                   // (count 1), and Grim Lavamancer's `{R}, {T}, Exile two cards
        .                   // from your graveyard` (count 2).
        .                   self.cost_exiled_cards.clear();
   73,248 ( 0.00%)          for other_cid in exile_other_picks {
        .                       if let Some(card) = Self::take_card(&mut self.players[p].graveyard, other_cid) {
        .                           self.exile.push(card);
        .                           self.cost_exiled_cards.push(other_cid);
        .                           self.players[p].cards_exiled_this_turn = self.players[p]
        .                               .cards_exiled_this_turn
        .                               .saturating_add(1);
        .                           events.push(GameEvent::CardLeftGraveyard {
        .                               player: p,
-- line 15521 ----------------------------------------
-- line 15524 ----------------------------------------
        .                           self.players[p].cards_left_graveyard_this_turn = self.players[p]
        .                               .cards_left_graveyard_this_turn
        .                               .saturating_add(1);
        .                       }
        .                   }
        .           
        .                   // Exile-a-card-from-hand-as-cost: stamp `exiled_with` so the body can
        .                   // read the exiled card back (Holistic Wisdom's shared-card-type gate).
   18,312 ( 0.00%)          if let Some(hand_cid) = exile_from_hand_pick
        .                       && let Some(mut card) = Self::take_card(&mut self.players[p].hand, hand_cid)
        .                   {
        .                       card.exiled_with = Some(card_id);
        .                       self.exile.push(card);
        .                       self.players[p].cards_exiled_this_turn =
        .                           self.players[p].cards_exiled_this_turn.saturating_add(1);
        .                       events.push(GameEvent::PermanentExiled { card_id: hand_cid });
        .                   }
        .           
        .                   // Exile-self-as-cost (graveyard activations): with tap/mana/life
        .                   // paid, exile the source from the graveyard. This is the cost
        .                   // line for cards like Stone Docent and Eternal Student that read
        .                   // "Exile this card from your graveyard:". The effect then
        .                   // resolves *after* the source is in exile, mirroring `sac_cost`
        .                   // for battlefield sources.
   36,624 ( 0.00%)          if ability.exile_self_cost && source_in_gy {
        .                       let owner = source_owner.unwrap();
        .                       if let Some(mut card) = Self::take_card(&mut self.players[owner].graveyard, card_id) {
        .                           card.controller = owner;
        .                           self.exile.push(card);
        .                           self.players[owner].cards_exiled_this_turn = self.players[owner]
        .                               .cards_exiled_this_turn
        .                               .saturating_add(1);
        .                           // Emit CardLeftGraveyard so Lorehold "cards leave your gy" payoffs fire.
-- line 15556 ----------------------------------------
-- line 15560 ----------------------------------------
        .                               .saturating_add(1);
        .                       }
        .                   }
        .           
        .                   // Exile-self-as-cost (hand activations): the "Exile this card from
        .                   // your hand:" cost line of the Spirit Guides. Exile happens after
        .                   // the (typically empty) mana cost is paid, before the mana ability
        .                   // resolves.
   36,624 ( 0.00%)          if ability.exile_self_cost && source_in_hand {
        .                       let owner = source_owner.unwrap();
        .                       if let Some(mut card) = Self::take_card(&mut self.players[owner].hand, card_id) {
        .                           card.controller = owner;
        .                           self.exile.push(card);
        .                           self.players[owner].cards_exiled_this_turn = self.players[owner]
        .                               .cards_exiled_this_turn
        .                               .saturating_add(1);
        .                       }
        .                   }
        .           
        .                   // Exile-self-as-cost (battlefield activations): the "Exile this
        .                   // creature:" cost line on a permanent (Hanged Executioner). With
        .                   // tap/mana/life paid, route the source battlefield → exile via the
        .                   // shared move funnel so linked-exile returns / leaves triggers /
        .                   // combat removal stay consistent, mirroring `return_self_cost`.
   73,248 ( 0.00%)          if ability.exile_self_cost
        .                       && !source_in_gy
        .                       && !source_in_hand
        .                       && let Some(owner) = self.battlefield_find(card_id).map(|c| c.owner)
        .                   {
        .                       let ctx = crate::game::effects::EffectContext::for_spell(owner, None, 0, 0);
        .                       self.move_card_to(card_id, &crate::effect::ZoneDest::Exile, &ctx, &mut events);
        .                   }
        .           
        .                   // Put-a-card-on-top-as-cost: the pre-flight pick leaves the hand once
        .                   // the rest of the cost is paid.
   36,624 ( 0.00%)          if let Some(cid) = library_top_pick
        .                       && let Some(card) = Self::take_card(&mut self.players[p].hand, cid)
        .                   {
        .                       self.players[p].library.insert(0, card);
        .                   }
        .           
        .                   // Discard-self-as-cost (hand activations): the "Discard this card:"
        .                   // cost line of Elemental Masterpiece. Routes hand → graveyard via the
        .                   // shared discard path (CardDiscarded event, madness, etc.) after mana
        .                   // payments succeed but before the effect resolves.
   36,624 ( 0.00%)          if ability.discard_self_cost && source_in_hand {
        .                       let owner = source_owner.unwrap();
        .                       self.discard_card(owner, card_id, &mut events);
        .                   }
        .           
        .                   // Mana abilities resolve immediately (no stack, no priority reset).
        .                   let is_mana_ab = is_mana_ability(&ability.effect);
        .           
   54,888 ( 0.00%)          if is_mana_ab {
        .                       // Carry a cost-sacrificed permanent's stats into the inline
        .                       // resolution (resolve_effect resets the scratch) — Slobad's
        .                       // "add {R} equal to the sacrificed artifact's mana value".
   36,592 ( 0.00%)              let effect = match cost_sac_pt {
        .                           Some((power, toughness)) => Effect::WithSacrificedPt {
        .                               power,
        .                               total_power: cost_sac_total_power,
        .                               toughness,
        .                               count: cost_sac_count,
        .                               mana_value: cost_sac_mv,
        .                               card: cost_sac_card,
        .                               body: Box::new(ability.effect.clone()),
        .                           },
   36,592 ( 0.00%)                  None => ability.effect.clone(),
3,908,446 ( 0.13%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (18,296x)
        .                       };
        .                       // CR 701.10f / 614.5 — "tap a permanent for mana" multipliers
        .                       // (Mana Reflection ×2, Nyxbloom Ancient ×3). Stamp the transient
        .                       // multiplier so the `AddMana` resolver scales pip output; clear
        .                       // it afterward. Only tapping qualifies per the printed text.
   54,888 ( 0.00%)              self.mana_production_multiplier =
   91,480 ( 0.00%)                  if ability.tap_cost { self.mana_production_multiplier_for(p) } else { 1 };
3,039,268 ( 0.10%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::mana_production_multiplier_for (18,296x)
  109,776 ( 0.00%)              let resolved = self.continue_ability_resolution_x(
30,043,758 ( 0.96%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::continue_ability_resolution_x (18,296x)
        .                           card_id,
        .                           p,
   36,592 ( 0.00%)                  effect,
1,408,792 ( 0.05%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (18,296x)
        .                           target.clone(),
        .                           x_value.unwrap_or(0),
        .                       );
   18,296 ( 0.00%)              self.mana_production_multiplier = 1;
   73,184 ( 0.00%)              let mut resolved = resolved?;
        .                       // CR 605.1b — triggered mana abilities ("Whenever a land is
        .                       // tapped for mana, … adds …") don't use the stack; they resolve
        .                       // here, right after the tapping ability.
   36,592 ( 0.00%)              if ability.tap_cost {
  128,072 ( 0.00%)                  self.resolve_extra_mana_on_land_tap(card_id, p, &resolved, &mut events);
9,126,406 ( 0.29%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::resolve_extra_mana_on_land_tap (18,296x)
        .                       }
        .                       events.append(&mut resolved);
   18,296 ( 0.00%)          } else {
2,268,704 ( 0.07%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>> (18,296x)
        .                       // Non-mana activated ability goes on the stack.
        .                       let ability_target = target.clone();
        .                       // Carry a cost-sacrificed creature's P/T into resolution
        .                       // (intervening resolutions reset the scratch).
       32 ( 0.00%)              let mut queued_effect = match cost_sac_pt {
        .                           Some((power, toughness)) => Effect::WithSacrificedPt {
        .                               power,
        .                               total_power: cost_sac_total_power,
        .                               toughness,
        .                               count: cost_sac_count,
        .                               mana_value: cost_sac_mv,
        .                               card: cost_sac_card,
        .                               body: Box::new(ability.effect),
        .                           },
       48 ( 0.00%)                  None => ability.effect,
    1,232 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (16x)
        .                       };
        .                       // Carry the Station-tapped creature's power into resolution
        .                       // (CR 702.184a) so `Value::TappedForCostPower` reads it.
       32 ( 0.00%)              if let Some(power) = tap_other_power {
        .                           queued_effect = Effect::WithTappedPower { power, body: Box::new(queued_effect) };
        .                       }
        .                       // CR 601.2b — a modal activated ability's mode is chosen as part
        .                       // of the activation (Shifting Ceratops's reach/trample/haste).
        .                       // A submitted mode is authoritative — it's the only path by which
        .                       // a UI seat can pick a mode whose body takes a target.
       32 ( 0.00%)              let mode = match chosen_mode {
        .                           Some(m) => Some(clamp_activated_mode(&queued_effect, m)),
       96 ( 0.00%)                  None => self.pick_trigger_mode(&queued_effect, card_id, p),
      688 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pick_trigger_mode (16x)
        .                       };
       32 ( 0.00%)              self.stack.push(
       32 ( 0.00%)                  TriggerPush::new(card_id, p, queued_effect)
        .                               .target(target)
       16 ( 0.00%)                      .additional_targets(additional_targets.clone())
      464 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (16x)
        .                               .mode(mode)
        .                               .x_value(activated_x)
        .                               .activated(true)
        .                               .mana_spent_by_color(activation_mana_colors)
       16 ( 0.00%)                      .build(),
    4,864 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::TriggerPush::build (16x)
        .                       );
       16 ( 0.00%)              self.randomize_single_target_on_stack();
    5,020 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::randomize_single_target_on_stack (16x)
        .                       // Pit Automaton — the claimed exhaust watchers go above the
        .                       // ability they copy, so each resolves before its original.
        .                       for dt in &exhaust_watchers {
        .                           self.stack.push(
        .                               TriggerPush::new(dt.source, dt.controller, dt.effect.clone())
        .                                   .trigger_source(Some(crate::game::effects::EntityRef::Permanent(card_id)))
        .                                   .build(),
        .                           );
        .                       }
        .                       // CR 702.21: Ward also fires on activated abilities targeting
        .                       // an opp's Ward permanent (the "or ability" half of 702.21a).
        .                       // Push Ward triggers above the just-queued ability so they
        .                       // resolve first.
       80 ( 0.00%)              self.push_ward_triggers_for_activated_ability(p, card_id, ability_target.clone());
   82,218 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::push_ward_triggers_for_activated_ability (16x)
        .                       // BecameTarget — fire per permanent target the activation
        .                       // chose (CR 603.x). The unified dispatcher handles APNAP and
        .                       // the trigger filter.
       32 ( 0.00%)              if let Some(Target::Permanent(target_id)) = &ability_target {
        .                           let evs =
       96 ( 0.00%)                      vec![GameEvent::BecameTarget { target: *target_id, caster: p, by: Some(card_id) }];
       48 ( 0.00%)                  self.dispatch_triggers_for_events(&evs);
   57,328 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (12x)
       12 ( 0.00%)              }
    1,656 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::GameEvent>> (12x)
        .                       // CR 601.2c — one "chose targets" event per activation. Queued
        .                       // rather than dispatched inline: an out-of-band dispatch here
        .                       // would drain the sacrifice-cost death events before
        .                       // `perform_action` folds them in.
       48 ( 0.00%)              if ability_target.is_some() || !additional_targets.is_empty() {
      112 ( 0.00%)                  events.push(GameEvent::ChoseTargets { chooser: p, object: card_id });
        .                       }
        .                       // CR 700.13 — activating a targeted ability against an opponent /
        .                       // their permanents is also a crime. Queue the event alongside the
        .                       // activation's other events (e.g. a sacrifice-cost death trigger)
        .                       // so `perform_action` dispatches them together, in order, rather
        .                       // than firing this out-of-band mid-activation.
       16 ( 0.00%)              if let Some(t) = &ability_target
      112 ( 0.00%)                  && self.target_is_crime(p, t)
    1,976 ( 0.00%)  => crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::target_is_crime (16x)
        .                       {
       48 ( 0.00%)                  self.players[p].committed_crime_this_turn = true;
   42,104 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (16x)
       80 ( 0.00%)                  events.push(GameEvent::CommittedCrime { player: p });
        .                       }
        .                       self.give_priority_to_active();
        .                   }
        .           
  128,184 ( 0.00%)          Ok(events)
  624,534 ( 0.02%)      }
2,763,352 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (18,296x)
  549,360 ( 0.02%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<alloc::vec::Vec<crabomination::game::types::DelayedTrigger>> (18,312x)
   91,560 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<core::option::Option<(alloc::vec::Vec<crabomination_base::card::CounterType>,u32,crabomination_base::card::SelectionRequirement)>> (18,312x)
   12,538 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::abilities::ActivatedAbility> (74x)
        .           }
        .           
        .           /// Pre-payment state captured by `snapshot_payment_state` so a failed
        .           /// payment can revert mana pool and tap-state mutations.
        .           pub(crate) struct PaymentSnapshot {
        .               pub pool: crate::mana::ManaPool,
        .               pub tapped: Vec<(CardId, bool)>,
        .           }
-- line 15744 ----------------------------------------
-- line 15752 ----------------------------------------
        .               pub pool_before: crate::mana::ManaPool,
        .           }
        .           
        .           impl GameState {
        .               /// CR 118.8 / 119.3c — apply `life` paid as a cost (Phyrexian pips,
        .               /// "pay N life" riders) through the life funnel and queue the loss as
        .               /// a `LifeLost` event (drained after the action) so paid life fires
        .               /// life-loss triggers.
   84,552 ( 0.00%)      pub(crate) fn pay_life_cost(&mut self, p: usize, life: u32) {
   28,184 ( 0.00%)          if life == 0 {
        .                       return;
        .                   }
        .                   // CR 118.8 — "whenever you pay life" (Font of Agonies) sees the amount
        .                   // paid, whether or not the life reduction was itself replaced.
        .                   self.pending_cost_events
        .                       .push(GameEvent::PaidLife { player: p, amount: life });
        .                   let applied = self.adjust_life_applied(p, -(life as i32));
        .                   if applied < 0 {
        .                       self.pending_cost_events
        .                           .push(GameEvent::LifeLost { player: p, amount: (-applied) as u32 });
        .                   }
   98,644 ( 0.00%)      }
        .           
        .               /// `WardCost::RemoveCounterFromPermanent` — take one counter off a
        .               /// permanent `p` controls. Auto-pick prefers the most-stacked kind on the
        .               /// least valuable permanent, and never touches loyalty (removing it is a
        .               /// real cost the auto-payer shouldn't volunteer). Returns false — the cost
        .               /// is unpayable — when nothing they control carries a removable counter.
        .               pub(crate) fn remove_one_counter_from_own_permanent(
        .                   &mut self,
-- line 15781 ----------------------------------------
-- line 15804 ----------------------------------------
        .                   true
        .               }
        .           }
        .           
        .           /// Optional-cost cast variants threaded through
        .           /// `GameState::cast_spell_with_convoke` (Kicker / Buyback / Bestow /
        .           /// Entwine). Each flag only sticks when the card carries the matching
        .           /// keyword.
   37,280 ( 0.00%)  #[derive(Debug, Clone, Copy, Default)]
        .           pub(crate) struct CastFlags {
        .               pub kicked: bool,
        .               pub buyback: bool,
        .               pub bestow: bool,
        .               pub entwine: bool,
        .               /// CR 709.5 — Room door being cast (`Some(0)` left, `Some(1)` right).
        .               pub room_door: Option<u8>,
        .               /// CR 702.165 — the spell's Gift was promised to an opponent.
-- line 15820 ----------------------------------------

9,763,838 ( 0.31%)  <counts for unidentified lines in crabomination/src/game/actions.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination_base/src/card.rs
--------------------------------------------------------------------------------
Ir                  

-- line 5 ----------------------------------------
         .           pub use crate::effect::{
         .               ActivatedAbility, Effect, EventKind, EventScope, EventSpec, LoyaltyAbility, OpeningHandEffect,
         .               Predicate, Selector, StateTriggeredAbility, StaticAbility, StaticEffect, TriggeredAbility,
         .               Value,
         .           };
         .           use crate::mana::{Color, ManaCost};
         .           
         .           /// Unique runtime ID for a card instance within a game.
31,746,352 ( 1.02%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize)]
   376,810 ( 0.01%)  pub struct CardId(pub u32);
         .           
         .           /// A single card type. Cards may have multiple types (e.g. Enchantment + Creature).
 2,882,118 ( 0.09%)  #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum CardType {
         .               Land,
         .               Creature,
         .               Artifact,
         .               Enchantment,
         .               Planeswalker,
         .               Battle,
         .               Instant,
-- line 25 ----------------------------------------
-- line 41 ----------------------------------------
         .               /// (CR 901.4). Its abilities function from there (CR 901.7).
         .               Plane,
         .               /// CR 312 — a Phenomenon. Like a Plane, but its "when you encounter this"
         .               /// trigger leaving the stack makes its controller planeswalk (CR 704.6f).
         .               Phenomenon,
         .           }
         .           
         .           /// Supertypes that modify a card's identity and rules interactions.
    40,010 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum Supertype {
         .               Basic,
         .               Legendary,
         .               Snow,
         .               World,
         .               /// CR 904.11 — an Ongoing scheme stays face up in the command zone until
         .               /// something abandons it, rather than being swept by the CR 904.10 SBA.
         .               Ongoing,
         .           }
         .           
         .           /// Creature subtypes (race/class).
    17,320 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum CreatureType {
         .               Human, Elf, Goblin, Merfolk, Moonfolk, Zombie, Vampire, Angel, Demon, Dragon,
         .               Knight, Soldier, Wizard, Cleric, Rogue, Warrior, Beast, Bird, Soltari, Dauthi,
         .               Elemental, Djinn, Efreet, Horror, Specter, Cat, Insect, Spider, Wurm, Worm, Alien,
         .               Armadillo, Nautilus,
         .               Bear, Ape, Rat, Fungus, Snail, Treefolk, Giant, Ogre, Orgg, Shaman, Druid,
         .               Monk, Archer, Berserker, Barbarian, Artificer, Pirate, Scout, Mongoose, Clown, Dalek, Nomad,
         .               Balloon,
-- line 69 ----------------------------------------
-- line 236 ----------------------------------------
         .               Wombat,
         .               // Fifth Dawn (the Bringer cycle, Summoning Station's Pincher token).
         .               Bringer, Pincher,
         .               // Coldsnap (the Aurochs herd).
         .               Aurochs,
         .           }
         .           
         .           /// Land subtypes (basic land types + others).
   172,004 ( 0.01%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum LandType {
         .               Plains, Island, Swamp, Mountain, Forest,
         .               Desert, Gate, Locus, Mine, Tower, PowerPlant, Urza,
         .               // Tarkir: Dragonstorm planeswalking dual lands.
         .               Omenpath,
         .               // Edge of Eternities — "Land — Planet" (Station-bearing utility lands).
         .               Planet,
         .               // Lost Caverns of Ixalan — "Land — Cave" (Caves-matter payoffs).
-- line 252 ----------------------------------------
-- line 287 ----------------------------------------
         .               Spacecraft, Lander,
         .               // Final Fantasy: Book (grimoire equipment).
         .               Book,
         .               // Unfinity: Attraction (CR 717) — lives in the Attraction deck, not the deck.
         .               Attraction,
         .           }
         .           
         .           /// Enchantment subtypes.
    10,344 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum EnchantmentSubtype {
         .               Aura, Saga, Shrine, Cartouche, Curse, Room, Class, Case, Background, Role,
         .           }
         .           
         .           /// Spell subtypes (for instants/sorceries).
         .           #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum SpellSubtype {
         .               Adventure, Lesson, Trap, Arcane,
-- line 303 ----------------------------------------
-- line 341 ----------------------------------------
         .               // ONE planeswalkers.
         .               Koth, Kaya, Tyvar, Kaito,
         .               // WAR planeswalkers.
         .               Tibalt, Teyo, Wanderer, Nixilis, Jaya, Angrath, Huatli, Kiora, Samut, Dovin,
         .               Davriel, Arlinn, Sarkhan, Yanggu,
         .           }
         .           
         .           /// All subtype categories collected into one struct for CardDefinition.
     4,488 ( 0.00%)  #[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]
         .           pub struct Subtypes {
         .               pub creature_types: Vec<CreatureType>,
         .               pub land_types: Vec<LandType>,
         .               pub artifact_subtypes: Vec<ArtifactSubtype>,
         .               pub enchantment_subtypes: Vec<EnchantmentSubtype>,
         .               pub spell_subtypes: Vec<SpellSubtype>,
         .               pub planeswalker_subtypes: Vec<PlaneswalkerSubtype>,
         .               #[serde(default)]
-- line 357 ----------------------------------------
-- line 702 ----------------------------------------
         .               Ore,
         .               /// Bogardan Phoenix — set once its recursion has been used.
         .               Death,
         .               /// Corrosion — an artifact dies once its mana value is at most its rust count.
         .               Rust,
         .           }
         .           
         .           /// Every zone a card can occupy.
       128 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum Zone {
         .               Library,
         .               Hand,
         .               Battlefield,
         .               Graveyard,
         .               Exile,
         .               Stack,
         .               Command,
-- line 718 ----------------------------------------
-- line 878 ----------------------------------------
         .           /// CR 122 — the counters on a permanent, in the order they were first added.
         .           ///
         .           /// A `Vec` for the same two reasons [`KeywordCounters`] is: `HashMap`'s
         .           /// iteration order is reseeded per process by `RandomState`, and a permanent
         .           /// carries one or two kinds when it carries any at all. The clone cost is the
         .           /// measured half — `CardData` is deep-copied ~1.09 M times per six bench games
         .           /// by the CoW unshare, and an empty `Vec` clone allocates nothing where an
         .           /// empty `hashbrown` table clone still walks its control bytes.
     3,300 ( 0.00%)  #[derive(Debug, Clone, Default, PartialEq, Eq)]
         .           pub struct CounterBag(Vec<(CounterType, u32)>);
         .           
         .           impl CounterBag {
         .               pub fn get(&self, ct: &CounterType) -> Option<&u32> {
         .                   self.0.iter().find(|(k, _)| k == ct).map(|(_, n)| n)
         .               }
         .               pub fn get_mut(&mut self, ct: &CounterType) -> Option<&mut u32> {
         .                   self.0.iter_mut().find(|(k, _)| k == ct).map(|(_, n)| n)
-- line 894 ----------------------------------------
-- line 975 ----------------------------------------
         .           /// Deliberately a `Vec`, not a `HashMap`. `layers.rs` walks this to push one
         .           /// granted keyword per counter kind into the computed keyword list, so a
         .           /// `HashMap`'s iteration order — reseeded per process by `RandomState` —
         .           /// reached game state, and two processes on the same seed could disagree.
         .           /// A permanent carries one or two kinds when it carries any at all, so the
         .           /// linear scans are also cheaper than hashing a `Keyword` (which owns a
         .           /// boxed `SelectionRequirement` in several variants).
         .           #[derive(Debug, Clone, Default, PartialEq, Eq)]
   108,036 ( 0.00%)  pub struct KeywordCounters(Vec<(Keyword, u32)>);
 3,673,224 ( 0.12%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .           
         .           impl KeywordCounters {
         .               pub fn get(&self, kw: &Keyword) -> Option<&u32> {
         .                   self.0.iter().find(|(k, _)| k == kw).map(|(_, n)| n)
         .               }
         .               /// Add `n` counters of `kw`, creating the entry at 0 first.
         .               pub fn add(&mut self, kw: Keyword, n: u32) {
         .                   match self.0.iter_mut().find(|(k, _)| *k == kw) {
-- line 991 ----------------------------------------
-- line 1009 ----------------------------------------
         .                   match self.0.iter_mut().find(|(k, _)| *k == kw) {
         .                       Some((_, have)) => *have = n,
         .                       None => self.0.push((kw, n)),
         .                   }
         .               }
         .               pub fn remove(&mut self, kw: &Keyword) {
         .                   self.0.retain(|(k, _)| k != kw);
         .               }
    42,852 ( 0.00%)      pub fn clear(&mut self) {
         .                   self.0.clear();
    42,852 ( 0.00%)      }
         .               pub fn len(&self) -> usize {
         .                   self.0.len()
         .               }
         .               pub fn is_empty(&self) -> bool {
         .                   self.0.is_empty()
         .               }
         .               pub fn iter(&self) -> impl Iterator<Item = (&Keyword, &u32)> {
         .                   self.0.iter().map(|(k, n)| (k, n))
-- line 1027 ----------------------------------------
-- line 1056 ----------------------------------------
         .                   for (k, n) in iter {
         .                       out.add(k, n);
         .                   }
         .                   out
         .               }
         .           }
         .           
         .           /// Keyword abilities supported by the engine.
15,272,954 ( 0.49%)  #[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum Keyword {
         .               Flying,
         .               Reach,
         .               Menace,
         .               Unblockable,
         .               Shadow,
         .               Horsemanship,
         .               /// Landwalk (CR 702.15) — this creature can't be blocked as long as the
-- line 1072 ----------------------------------------
-- line 2054 ----------------------------------------
         .               Life,
         .               CardsInHand,
         .               CreaturesControlled,
         .               LandsControlled,
         .               NonbasicLandsControlled,
         .               CreatureCardsInGraveyard,
         .           }
         .           
   253,986 ( 0.01%)  #[derive(Debug, Clone, Default, PartialEq, Eq, Hash, Serialize, Deserialize)]
         .           pub enum SelectionRequirement {
         .               #[default]
         .               Any,
         .               Player,
         .               /// A player who is an opponent of the effect's controller ("target
         .               /// opponent").
         .               OpponentPlayer,
         .               /// The effect's controller, as a player target ("targets you" —
-- line 2070 ----------------------------------------
-- line 2786 ----------------------------------------
         .               /// i.e. it's a double-faced card (MDFC).
         .               HasBackFace,
         .               /// True when the candidate is a preparation card — a creature
         .               /// carrying an inset prepare spell (`prepare_spell.is_some()`).
         .               /// Enforces the SOS Prepare reminder "(Only creatures with prepare
         .               /// spells can become prepared.)" on Biblioplex Tomekeeper /
         .               /// Skycoach Waypoint's target filters.
         .               HasPrepareSpell,
    10,164 ( 0.00%)      And(Box<SelectionRequirement>, Box<SelectionRequirement>),
   437,857 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone (2,904x)
         .               Or(Box<SelectionRequirement>, Box<SelectionRequirement>),
     4,926 ( 0.00%)      Not(Box<SelectionRequirement>),
    19,190 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs:<alloc::boxed::Box<T,A> as core::clone::Clone>::clone (190x)
         .           }
         .           
         .           impl SelectionRequirement {
         .               /// `Any`, as a function so it can be a `#[serde(default)]` for filter
         .               /// fields added to existing effect variants.
         .               pub fn any() -> Self {
         .                   SelectionRequirement::Any
         .               }
         .           
       224 ( 0.00%)      pub fn and(self, other: Self) -> Self {
       280 ( 0.00%)          Self::And(Box::new(self), Box::new(other))
       196 ( 0.00%)      }
         .               pub fn or(self, other: Self) -> Self {
         .                   Self::Or(Box::new(self), Box::new(other))
         .               }
       140 ( 0.00%)      pub fn negate(self) -> Self {
        56 ( 0.00%)          Self::Not(Box::new(self))
       140 ( 0.00%)      }
         .           
         .               /// Rewrite every `IsSource` atom to a constant. Used when a filter is
         .               /// evaluated against a card that has already left the battlefield, where
         .               /// the "is this the granting source?" answer is known up front but the
         .               /// card-only evaluator can't look it up (CR 603.10a death LKI).
         .               pub fn resolve_is_source(&self, is_source: bool) -> Self {
         .                   match self {
         .                       Self::IsSource => {
-- line 2822 ----------------------------------------
-- line 2929 ----------------------------------------
         .                       other => other.clone(),
         .                   }
         .               }
         .           
         .               /// Replace X-dependent atoms with concrete values
         .               /// (`ManaValueAtMostXFromCost` → `ManaValueAtMost(x)`), recursing
         .               /// through And/Or/Not. Called by `Effect::Search` with the resolving
         .               /// spell's paid X (Chord of Calling, CR 601.2b).
    34,128 ( 0.00%)      pub fn resolve_x(&self, x: u32) -> Self {
    28,116 ( 0.00%)          match self {
         .                       Self::ManaValueAtMostXFromCost => Self::ManaValueAtMost(x),
         .                       Self::ManaValueExactlyXFromCost => Self::ManaValueExactly(x),
         .                       Self::PowerAtMostXFromCost => Self::PowerAtMost(x as i32),
         .                       Self::ToughnessAtMostXFromCost => Self::ToughnessAtMost(x as i32),
     8,940 ( 0.00%)              Self::And(a, b) => Self::And(Box::new(a.resolve_x(x)), Box::new(b.resolve_x(x))),
   154,931 ( 0.00%)  => crabomination_base/src/card.rs:crabomination_base::card::SelectionRequirement::resolve_x'2 (1,788x)
         .                       Self::Or(a, b) => Self::Or(Box::new(a.resolve_x(x)), Box::new(b.resolve_x(x))),
     4,470 ( 0.00%)              Self::Not(inner) => Self::Not(Box::new(inner.resolve_x(x))),
    35,760 ( 0.00%)  => crabomination_base/src/card.rs:crabomination_base::card::SelectionRequirement::resolve_x'2 (894x)
     7,800 ( 0.00%)              other => other.clone(),
    33,792 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::SelectionRequirement as core::clone::Clone>::clone (2,112x)
         .                   }
    43,716 ( 0.00%)      }
         .           
         .               /// Concretize `ManaValueAtMostSourcePower` against the source's power
         .               /// (Velomachus Lorehold's "with mana value less than or equal to this
         .               /// creature's power"), so source-less card matchers (library walks)
         .               /// evaluate a plain `ManaValueAtMost`. Same shape as `resolve_x`.
         .               pub fn resolve_source_power(&self, power: i32) -> Self {
         .                   match self {
         .                       Self::ManaValueAtMostSourcePower => Self::ManaValueAtMost(power.max(0) as u32),
-- line 2956 ----------------------------------------
-- line 3171 ----------------------------------------
         .           ///
         .           /// `Default` is derived so card constructors can use
         .           /// `..Default::default()` to skip boilerplate fields. Default values:
         .           /// `name = ""`, `cost = ManaCost::default()` (free), all `Vec`s empty,
         .           /// `effect = Effect::Noop`, and all `Option`s = `None`. Numeric fields
         .           /// (`power`, `toughness`, `base_loyalty`) default to 0. New cards
         .           /// usually only need to set `name`, `cost`, `card_types`, P/T (for
         .           /// creatures), and the relevant ability/effect fields.
    87,048 ( 0.00%)  #[derive(Debug, Clone, Default, Serialize, Deserialize)]
         .           #[serde(bound = "")]
         .           pub struct CardDefinition {
         .               #[serde(with = "crate::static_str_serde")]
         .               pub name: crate::static_str_serde::StaticStr,
         .               pub cost: ManaCost,
         .               pub supertypes: Vec<Supertype>,
         .               pub card_types: Vec<CardType>,
         .               pub subtypes: Subtypes,
-- line 3187 ----------------------------------------
-- line 4394 ----------------------------------------
         .               OpponentGainsLife { amount: u32 },
         .           }
         .           
         .           /// The static bonus an Equipment confers on the creature it's attached to.
         .           /// Mirrors the printed "Equipped creature gets +P/+T and has [keywords]"
         .           /// clause. Stored on `CardDefinition.equipped_bonus`; applied by
         .           /// `compute_battlefield` only while the Equipment is attached to a creature
         .           /// that's currently on the battlefield.
       656 ( 0.00%)  #[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]
         .           pub struct EquipBonus {
         .               /// Power modification granted to the equipped creature (layer 7c).
         .               pub power: i32,
         .               /// Toughness modification granted to the equipped creature (layer 7c).
         .               pub toughness: i32,
         .               /// Keywords granted to the equipped creature (layer 6).
         .               pub keywords: Vec<Keyword>,
         .               /// Keywords REMOVED from the equipped creature (layer 6 — CR 613.1f).
-- line 4410 ----------------------------------------
-- line 5092 ----------------------------------------
         .               pub warp: bool,
         .               /// CR 702.162 / 701.28 — More Than Meets the Eye: the spell is cast
         .               /// *converted*, so the permanent enters with its back face up.
         .               #[serde(default)]
         .               pub converted: bool,
         .           }
         .           
         .           impl CardDefinition {
   459,354 ( 0.01%)      pub fn is_land(&self) -> bool { self.card_types.contains(&CardType::Land) }
 1,108,816 ( 0.04%)      pub fn is_creature(&self) -> bool { self.card_types.contains(&CardType::Creature) }
    42,030 ( 0.00%)      pub fn is_instant(&self) -> bool { self.card_types.contains(&CardType::Instant) }
    23,578 ( 0.00%)      pub fn is_sorcery(&self) -> bool { self.card_types.contains(&CardType::Sorcery) }
    44,274 ( 0.00%)      pub fn is_artifact(&self) -> bool { self.card_types.contains(&CardType::Artifact) }
     3,784 ( 0.00%)      pub fn is_enchantment(&self) -> bool { self.card_types.contains(&CardType::Enchantment) }
   107,940 ( 0.00%)      pub fn is_planeswalker(&self) -> bool { self.card_types.contains(&CardType::Planeswalker) }
     2,838 ( 0.00%)      pub fn is_battle(&self) -> bool { self.card_types.contains(&CardType::Battle) }
         .               pub fn is_vanguard(&self) -> bool { self.card_types.contains(&CardType::Vanguard) }
         .               pub fn is_conspiracy(&self) -> bool { self.card_types.contains(&CardType::Conspiracy) }
         .               /// CR 314.1 — a scheme card.
         .               pub fn is_scheme(&self) -> bool { self.card_types.contains(&CardType::Scheme) }
         .               /// CR 311.1 — a plane card.
         .               pub fn is_plane(&self) -> bool { self.card_types.contains(&CardType::Plane) }
         .               /// CR 312.1 — a phenomenon card.
         .               pub fn is_phenomenon(&self) -> bool { self.card_types.contains(&CardType::Phenomenon) }
-- line 5115 ----------------------------------------
-- line 5123 ----------------------------------------
         .                   self.keywords.is_empty()
         .                       && self.static_abilities.is_empty()
         .                       && self.triggered_abilities.is_empty()
         .                       && self.activated_abilities.is_empty()
         .                       && self.state_trigger.is_none()
         .               }
         .               pub fn is_permanent(&self) -> bool {
         .                   self.card_types.iter().any(|t| {
    16,474 ( 0.00%)              matches!(
         .                           t,
         .                           CardType::Land
         .                               | CardType::Creature
         .                               | CardType::Enchantment
         .                               | CardType::Artifact
         .                               | CardType::Planeswalker
         .                               | CardType::Battle
         .                       )
         .                   })
    16,474 ( 0.00%)      }
    37,280 ( 0.00%)      pub fn is_instant_speed(&self) -> bool {
         .                   self.is_instant() || self.keywords.contains(&Keyword::Flash)
    52,192 ( 0.00%)      }
         .           
         .               /// True if any activated ability could add {C} ("could produce {C}" —
         .               /// Break the Ice, Obsidian Charmaw). Walks `AddMana` payloads, seeing
         .               /// through `Restricted` wrappers and `Seq` bodies.
         .               pub fn produces_colorless(&self) -> bool {
         .                   fn payload_colorless(p: &crate::effect::ManaPayload) -> bool {
         .                       use crate::effect::ManaPayload as MP;
         .                       match p {
-- line 5152 ----------------------------------------
-- line 5169 ----------------------------------------
         .               }
         .           
         .               /// Printed colors from the mana cost's colored pips (CR 105.2), with
         .               /// the Devoid CDA (CR 702.114) yielding colorless.
         .               ///
         .               /// Allocation-free; the `Vec` form is [`Self::printed_colors`]. The
         .               /// layer pass calls this ~1.2 M times per six bench games, so it stays
         .               /// on the bitmask.
 1,989,232 ( 0.06%)      pub fn printed_color_set(&self) -> crate::mana::ColorSet {
         .                   use crate::mana::{ColorSet, ManaSymbol};
   568,352 ( 0.02%)          if let Some(forced) = &self.color_override {
         .                       return forced.iter().collect();
         .                   }
         .                   if self.keywords.contains(&Keyword::Devoid) {
         .                       return ColorSet::empty();
         .                   }
         .                   let mut colors = ColorSet::empty();
         .                   // CR 105.2c — a color indicator defines color for objects (tokens,
         .                   // back faces) whose mana cost can't.
         .                   for c in &self.color_indicator {
         .                       colors.insert(*c);
         .                   }
         .                   for sym in &self.cost.symbols {
 4,032,298 ( 0.13%)              match sym {
         .                           ManaSymbol::Colored(c) | ManaSymbol::Phyrexian(c) | ManaSymbol::MonoHybrid(_, c) => {
   302,768 ( 0.01%)                      colors.insert(*c)
         .                           }
         .                           ManaSymbol::Hybrid(a, b) => {
         .                               colors.insert(*a);
         .                               colors.insert(*b);
         .                           }
         .                           _ => {}
         .                       }
         .                   }
         .                   colors
 1,705,056 ( 0.05%)      }
         .           
         .               /// [`Self::printed_color_set`] in WUBRG order as a `Vec`.
    35,384 ( 0.00%)      pub fn printed_colors(&self) -> Vec<crate::mana::Color> {
    78,632 ( 0.00%)          self.printed_color_set().to_vec()
 1,375,514 ( 0.04%)  => crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::printed_color_set (17,692x)
         .               }
         .           
         .               pub fn is_legendary(&self) -> bool { self.supertypes.contains(&Supertype::Legendary) }
    37,942 ( 0.00%)      pub fn is_basic(&self) -> bool { self.supertypes.contains(&Supertype::Basic) }
         .               pub fn is_snow(&self) -> bool { self.supertypes.contains(&Supertype::Snow) }
         .               pub fn has_creature_type(&self, ct: CreatureType) -> bool {
         .                   self.subtypes.creature_types.contains(&ct)
         .               }
         .               pub fn has_land_type(&self, lt: LandType) -> bool {
         .                   self.subtypes.land_types.contains(&lt)
         .               }
         .           
         .               /// Spend-restriction context for casting this card as a spell — gates
         .               /// which restricted mana [`crate::mana::ManaPool::pay_for_spell`] may drain.
   129,744 ( 0.00%)      pub fn spell_kind(&self) -> crate::mana::SpellKind {
   432,480 ( 0.01%)          crate::mana::SpellKind {
         .                       instant_or_sorcery: self.is_instant() || self.is_sorcery(),
         .                       artifact: self.is_artifact(),
         .                       creature_types: if self.is_creature() {
         .                           self.subtypes.creature_types.clone()
         .                       } else {
         .                           Vec::new()
         .                       },
         .                       changeling: self.is_creature() && self.keywords.contains(&Keyword::Changeling),
-- line 5232 ----------------------------------------
-- line 5238 ----------------------------------------
         .                       lesson: self.subtypes.spell_subtypes.contains(&crate::card::SpellSubtype::Lesson),
         .                       devoid: self.keywords.contains(&Keyword::Devoid),
         .                       equipment: self.is_equipment(),
         .                       colorless: self.printed_colors().is_empty(),
         .                       mana_value: self.cost.cmc(),
         .                       has_x: self.cost.has_x(),
         .                       omen: false,
         .                       enchantment: self.is_enchantment(),
    14,416 ( 0.00%)              multicolored: self.printed_colors().len() >= 2,
         .                       planeswalker: self.is_planeswalker(),
         .                       legendary: self.supertypes.contains(&Supertype::Legendary),
    14,416 ( 0.00%)              creature_mana_only: self.spend_only_creature_mana,
         .                       room_or_door: self.subtypes.enchantment_subtypes.contains(&EnchantmentSubtype::Room),
    28,832 ( 0.00%)              name: Some(self.name),
         .                       face_down: false,
         .                       turning_face_up: false,
         .                   }
   129,744 ( 0.00%)      }
         .           
         .               /// Spend-restriction context for activating an ability of this card
         .               /// ("… or activate abilities of artifacts" — Power Depot).
    18,386 ( 0.00%)      pub fn ability_spend_kind(&self) -> crate::mana::SpellKind {
   275,790 ( 0.01%)          crate::mana::SpellKind {
         .                       artifact: self.is_artifact(),
         .                       land_ability: self.is_land(),
         .                       creature_ability: self.is_creature(),
         .                       activating_ability: true,
         .                       equipment: self.is_equipment(),
         .                       ..Default::default()
         .                   }
    18,386 ( 0.00%)      }
         .           
         .               // Vehicles (CR 301.7) carry printed P/T even though they aren't
         .               // creatures until crewed, so their base P/T must survive into the layer
         .               // system — when a Crew activation animates them via a layer-4
         .               // AddCardType(Creature), the printed power/toughness is what the new
         .               // creature uses. A non-crewed Vehicle is still not a creature, so the
         .               // base P/T is inert for combat / "creatures you control" purposes.
         .               pub fn base_power(&self) -> i32 {
   188,528 ( 0.01%)          if self.is_creature() || self.is_vehicle() { self.power } else { 0 }
   237,652 ( 0.01%)      }
         .               pub fn base_toughness(&self) -> i32 {
   216,488 ( 0.01%)          if self.is_creature() || self.is_vehicle() { self.toughness } else { 0 }
   237,652 ( 0.01%)      }
         .           
         .               pub fn is_equipment(&self) -> bool {
         .                   self.subtypes.artifact_subtypes.contains(&ArtifactSubtype::Equipment)
    17,402 ( 0.00%)      }
         .               pub fn is_vehicle(&self) -> bool {
         .                   self.subtypes.artifact_subtypes.contains(&ArtifactSubtype::Vehicle)
         .               }
         .               /// Returns the Crew cost (required total power) if this card has
         .               /// `Keyword::Crew(N)`.
         .               pub fn crew_cost(&self) -> Option<u32> {
         .                   self.keywords.iter().find_map(|kw| {
    11,944 ( 0.00%)              if let Keyword::Crew(n) = kw { Some(*n) } else { None }
         .                   })
    33,486 ( 0.00%)      }
         .               /// Returns the Saddle cost (required total power) if this card has
         .               /// `Keyword::Saddle(N)` (CR 702.171).
         .               pub fn saddle_cost(&self) -> Option<u32> {
         .                   self.keywords.iter().find_map(|kw| {
     1,240 ( 0.00%)              if let Keyword::Saddle(n) = kw { Some(*n) } else { None }
         .                   })
     2,920 ( 0.00%)      }
         .               /// Returns the Casualty number (minimum power of the creature to
         .               /// sacrifice) if this card has `Keyword::Casualty(N)` (CR 702.153).
         .               pub fn casualty_cost(&self) -> Option<u32> {
         .                   self.keywords.iter().find_map(|kw| {
         .                       if let Keyword::Casualty(n) = kw { Some(*n) } else { None }
         .                   })
         .               }
         .               pub fn is_aura(&self) -> bool {
         .                   self.subtypes.enchantment_subtypes.contains(&EnchantmentSubtype::Aura)
    95,496 ( 0.00%)      }
         .               /// CR 716 — a Class enchantment (enters at level 1, gains levels at
         .               /// sorcery speed via its `Level N` activated abilities).
         .               pub fn is_class(&self) -> bool {
         .                   self.subtypes.enchantment_subtypes.contains(&EnchantmentSubtype::Class)
     2,174 ( 0.00%)      }
         .           
         .               /// The "enchant ___" restriction an Aura attaches under, recovered from
         .               /// its resolution `Effect::Attach { to: TargetFiltered { filter, .. } }`
         .               /// (CR 303.4a). Returns `None` when the attach target can't be read as a
         .               /// single filtered selector (bestow, scripted attaches), so callers that
         .               /// re-check legality (the 704.5n SBA) can conservatively skip it.
    10,794 ( 0.00%)      pub fn aura_enchant_filter(&self) -> Option<&SelectionRequirement> {
         .                   fn from_effect(e: &crate::effect::Effect) -> Option<&SelectionRequirement> {
         .                       use crate::effect::{Effect, Selector};
    21,588 ( 0.00%)              match e {
         .                           Effect::Attach { to: Selector::TargetFiltered { filter, .. }, .. } => Some(filter),
         .                           Effect::Seq(inner) => inner.iter().find_map(from_effect),
         .                           _ => None,
         .                       }
         .                   }
         .                   from_effect(&self.effect)
    17,990 ( 0.00%)      }
         .           
         .               pub fn has_flashback(&self) -> Option<&ManaCost> {
         .                   self.keywords.iter().find_map(|kw| {
         .                       if let Keyword::Flashback(cost) = kw { Some(cost) } else { None }
         .                   })
         .               }
         .               /// Returns the number of creatures that must be tapped to flashback
         .               /// this card if it has `Keyword::FlashbackTap`. None otherwise.
-- line 5342 ----------------------------------------
-- line 5602 ----------------------------------------
         .           // ── Runtime card instance ─────────────────────────────────────────────────────
         .           
         .           /// A card in play.  Tracks mutable game state layered on top of the static definition.
         .           ///
         .           /// CR 508.1a — a pending or active "can't attack during its controller's
         .           /// next turn" ban (Wall of Dust). Set to `Pending` when the ban lands; the
         .           /// bearer's untap step promotes it to `Active`, and that turn's cleanup
         .           /// clears it.
     8,464 ( 0.00%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize, Deserialize)]
         .           pub enum AttackBan {
         .               #[default]
         .               None,
         .               Pending,
         .               Active,
         .           }
         .           
         .           /// A card's mutable per-object state. Reached only through
         .           /// [`CardInstance`], which owns it behind an `Arc` — see that type for why.
 5,617,872 ( 0.18%)  #[derive(Debug, Clone)]
         .           pub struct CardData {
   108,036 ( 0.00%)      pub id: CardId,
         .               /// Static blueprint, shared behind an `Arc` so materializing a
         .               /// `CardData` (which the CoW handle does only for a card actually being
         .               /// written) doesn't deep-copy the definition's ~two dozen `Vec` fields.
         .               /// The definition is immutable for the common case; the handful of
         .               /// effects that rewrite it
         .               /// (MDFC face-swap, "loses all abilities", overload effect override,
         .               /// keyword grants) go through `Arc::make_mut`, which clones lazily only
         .               /// when the `Arc` is actually shared.
-- line 5630 ----------------------------------------
-- line 5647 ----------------------------------------
         .               /// CR 303.4a — the seat this Aura enchants, for "enchant player" Auras
         .               /// (the Curse cycle, Psychic Possession). Mutually exclusive with
         .               /// `attached_to`.
         .               pub attached_to_player: Option<usize>,
         .               /// CR 702.95 — the creature this one is Soulbond-paired with, if any.
         .               /// Either member of a pair points at the other. Cleared when either
         .               /// creature leaves the battlefield (SBA in `stack.rs`).
         .               pub soulbond_partner: Option<CardId>,
   108,036 ( 0.00%)      pub kicked: bool,
         .               /// Activated abilities granted to this specific permanent by resolved
         .               /// effects (`Effect::GainActivatedAbility` — Urza's Saga chapters I/II).
         .               /// Cleared when the permanent leaves the battlefield (a new object,
         .               /// CR 400.7). Surfaced after the printed abilities in both
         .               /// `activate_ability` index space and the client view.
   324,108 ( 0.01%)      pub granted_activated_abilities: Vec<ActivatedAbility>,
 3,457,152 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// "Until end of turn" sibling of `granted_activated_abilities` — cleared
         .               /// by `clear_end_of_turn_effects` (Lightning Volley, Retraction Helix).
   324,108 ( 0.01%)      pub granted_activated_eot: Vec<ActivatedAbility>,
 3,457,152 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// CR 702.33c — how many times this spell's Multikicker cost was paid.
         .               /// Persists onto the permanent so `Value::TimesKicked` can read it
         .               /// (Everflowing Chalice's enters-with-counters). Defaults to 0.
         .               pub kick_count: u32,
         .               /// CR 702.157 — how many times this spell's optional Squad cost was paid.
         .               /// Persists onto the permanent so its ETB mints one token copy per payment
         .               /// (`Value::SquadCount`). Defaults to 0 (no Squad / not paid).
         .               pub squad_count: u32,
-- line 5672 ----------------------------------------
-- line 5696 ----------------------------------------
         .               /// CR 702.172 — Spree mode indices chosen at cast time (empty for
         .               /// non-Spree spells). Stamped by `GameAction::CastSpellSpree` and read at
         .               /// resolution by `Effect::Spree`.
         .               pub spree_modes: Vec<u8>,
         .               /// CR 702.47 — rules text gained by splicing cards onto this spell,
         .               /// resolved after the main effect (each entry reads its target from the
         .               /// matching `additional_targets` slot). Cleared when the spell leaves
         .               /// the stack (702.47e).
   324,108 ( 0.01%)      pub spliced_effects: Vec<Effect>,
 3,457,152 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// Names of the cards spliced onto this spell, parallel to
         .               /// `spliced_effects` — Minamo's Meddling discards by name. Cleared with
         .               /// them when the spell leaves the stack.
   324,108 ( 0.01%)      pub spliced_names: Vec<String>,
 3,565,188 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// CR 702.27 — true if this spell was cast paying its optional Buyback
         .               /// cost. On resolution the resolver returns the card to its owner's
         .               /// hand instead of the graveyard.
   216,072 ( 0.01%)      pub bought_back: bool,
         .               /// CR 702.41 — true if this spell was cast paying its optional Entwine
         .               /// cost: its `ChooseMode` runs every mode in order.
         .               pub entwined: bool,
         .               /// CR 702.165 — true if this spell was cast with its Gift promised: on
         .               /// resolution it runs `definition.gift.gifted_effect` instead of `effect`.
         .               pub gift_promised: bool,
         .               /// CR 702.103 — true while this permanent is on the battlefield as a
         .               /// bestowed Aura. It's an Aura (not a creature) for as long as this is
         .               /// set; cleared by the SBA when the enchanted creature leaves, at which
         .               /// point it reverts to a creature.
         .               pub bestowed: bool,
   108,036 ( 0.00%)      pub face_down: bool,
         .               /// CR 712 — true while this double-faced permanent is showing its back
         .               /// face. `definition` is swapped to the active face; `front_face` stashes
         .               /// the front so `Effect::Transform` can toggle back. Reconstructed on
         .               /// snapshot load from the front name + this flag.
   108,036 ( 0.00%)      pub transformed: bool,
         .               /// CR 712 — the front-face definition, kept while `transformed` so the
         .               /// permanent can flip back. In-memory only (not serialized): the serde
         .               /// wire stores the front name and rebuilds this on load.
         .               pub front_face: Option<Arc<CardDefinition>>,
         .               /// CR 702.160 — the printed (full, colorless) definition, kept while
         .               /// `cast_as_prototype` so the permanent reverts to its full mana cost,
         .               /// color, and size when it leaves the battlefield. In-memory only: the
         .               /// wire keeps the name + `cast_as_prototype` and rebuilds this on load.
         .               pub prototype_printed: Option<Arc<CardDefinition>>,
         .               /// CR 710 — true while this flip card is showing its flipped (bottom) face.
         .               /// `definition` is swapped to the flip face; `unflipped_def` stashes the
         .               /// top so it can be restored on zone change. Reconstructed on snapshot load
         .               /// from the top name + this flag.
   108,036 ( 0.00%)      pub flipped: bool,
         .               /// CR 710 — the unflipped (top) definition, kept while `flipped`. In-memory
         .               /// only: the serde wire stores the top name + the `flipped` flag.
         .               pub unflipped_def: Option<Arc<CardDefinition>>,
         .               /// CR 708 — while this permanent is on the battlefield face down (morph /
         .               /// manifest), `definition` is swapped to the vanilla 2/2 face-down
         .               /// definition and the real card is stashed here so it can be turned face
         .               /// up (and is restored as the card leaves the battlefield). In-memory
         .               /// only: the serde wire stores the real name + a `face_down_permanent`
-- line 5751 ----------------------------------------
-- line 5937 ----------------------------------------
         .               /// CR 702.177 — indices of `exhaust` activated abilities already used.
         .               /// Unlike `once_per_turn_used`, this is **never** cleared (once per game).
         .               pub exhausted_abilities: Vec<usize>,
         .               /// Keywords granted with `Duration::EndOfTurn` via `Effect::GrantKeyword`.
         .               /// Cleared at the Cleanup step alongside `power_bonus`/`toughness_bonus`.
         .               /// Stored separately from `definition.keywords` so the printed-Oracle
         .               /// keywords aren't permanently mutated by an EOT pump (engine fix —
         .               /// push modern_decks batch 24). `has_keyword` checks both vectors.
   324,108 ( 0.01%)      pub granted_keywords_eot: Vec<Keyword>,
 3,565,188 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// Per-grant layer timestamps for `granted_keywords_eot` (same indices;
         .               /// CR 613.7). Lets the layer walk order an EOT grant against
         .               /// RemoveKeyword / RemoveAllAbilities effects instead of always
         .               /// pre-merging it. Grants pushed without a timestamp (tests, legacy
         .               /// snapshots) default to 0 — ordered before every tracked effect,
         .               /// matching the old pre-merge behavior.
         .               pub granted_keywords_eot_ts: Vec<u64>,
         .               /// Keywords removed until end of turn via `Effect::LoseKeyword`
         .               /// (Shadowspear's "creatures your opponents control lose hexproof and
         .               /// indestructible until end of turn"). Removal beats printed/granted/
         .               /// counter sources for the turn; cleared at Cleanup.
   324,108 ( 0.01%)      pub removed_keywords_eot: Vec<Keyword>,
 3,565,188 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// Keywords removed indefinitely (`Duration::Permanent`) — Ageless
         .               /// Sentinels' "it loses defender". Survives cleanup.
   324,142 ( 0.01%)      pub removed_keywords: Vec<Keyword>,
 3,565,188 ( 0.11%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (108,036x)
         .               /// CR 122.1b — Keyword counters. Each entry maps a keyword to its
         .               /// count; the host gets the keyword while one or more such counters
         .               /// are on it. Applied as a layer-6 keyword addition during
         .               /// `compute_battlefield`. Distinct from `definition.keywords`
         .               /// (printed) and `granted_keywords_eot` (transient EOT grants) so
         .               /// the printed/granted/counter sources can be inspected separately
         .               /// (e.g., for "remove all abilities" effects). Defaults to empty.
         .               /// Push (modern_decks batch 183): added per CR 122.1b.
   108,036 ( 0.00%)      pub keyword_counters: KeywordCounters,
         .               /// "You may cast/play this card without paying its mana cost" permission
         .               /// granted by Practiced Scrollsmith, Suspend Aggression, Nita, …
         .               /// Set by `Effect::GrantMayPlay`; consumed by
         .               /// `GameAction::CastFromZoneWithoutPaying`; cleared on expiry by
         .               /// `clean_per_turn_state` / next-turn cleanup, and also cleared
         .               /// whenever the card changes zones (the grantor's "that card" stops
         .               /// referring to it once it moves).
         .               pub may_play_until: Option<MayPlayPermission>,
-- line 5977 ----------------------------------------
-- line 6273 ----------------------------------------
         .           ///
         .           /// `Serialize`/`Deserialize` are implemented manually below — `definition`
         .           /// is round-tripped by *card name* (via `catalog::lookup_by_name`) rather
         .           /// than by serializing the full `CardDefinition` tree, which would force
         .           /// the parent's `Deserialize<'de>` impl to bound `'de: 'static` because
         .           /// of the `&'static str` name field. Manual impls let `CardInstance` be
         .           /// `Deserialize<'de>` for any `'de`, which is a hard requirement for
         .           /// containers like `Box<CardInstance>` inside `StackItem`.
    42,068 ( 0.00%)  #[derive(Clone)]
         .           pub struct CardInstance(Arc<CardData>);
         .           
         .           impl std::ops::Deref for CardInstance {
         .               type Target = CardData;
         .               #[inline]
         .               fn deref(&self) -> &CardData {
         .                   &self.0
         .               }
         .           }
         .           
         .           /// The unshare point: every `&mut` reach into a card goes through here, so a
         .           /// shared card is cloned exactly once and then written in place.
         .           impl std::ops::DerefMut for CardInstance {
         .               #[inline]
   272,288 ( 0.01%)      fn deref_mut(&mut self) -> &mut CardData {
   833,144 ( 0.03%)          Arc::make_mut(&mut self.0)
   324,600 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs:alloc::sync::Arc<T,A>::make_mut (10,820x)
   306,324 ( 0.01%)      }
         .           }
         .           
         .           impl std::fmt::Debug for CardInstance {
         .               fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
         .                   std::fmt::Debug::fmt(&*self.0, f)
         .               }
         .           }
         .           
         .           impl From<CardData> for CardInstance {
     1,980 ( 0.00%)      fn from(data: CardData) -> Self {
         .                   Self(Arc::new(data))
     2,640 ( 0.00%)      }
         .           }
         .           
         .           impl CardInstance {
         .               /// The instant/sorcery alternate half (Adventure or Omen) this card is
         .               /// currently on the stack as, if any. While set, the card resolves down the
         .               /// spell path with the half's card types and effect (CR 715 / CR 702.183).
         .               pub fn alt_spell_half(&self) -> Option<&Adventure> {
    24,386 ( 0.00%)          if self.adventuring {
         .                       self.definition.adventure.as_deref()
    17,340 ( 0.00%)          } else if self.omen_casting {
         .                       self.definition.omen.as_deref()
         .                   } else {
         .                       None
         .                   }
         .               }
         .           
         .               /// True while this card is on the stack as an instant/sorcery alternate
         .               /// half (Adventure or Omen) — i.e. it resolves as a noncreature spell.
         .               pub fn casting_alt_half(&self) -> bool {
    68,392 ( 0.00%)          self.adventuring || self.omen_casting
         .               }
         .           
     8,580 ( 0.00%)      pub fn new(id: CardId, definition: impl Into<Arc<CardDefinition>>, owner: usize) -> Self {
       660 ( 0.00%)          let definition = definition.into();
     1,320 ( 0.00%)          let summoning_sick = definition.is_creature();
     9,840 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (660x)
       660 ( 0.00%)          let base_loyalty = definition.base_loyalty;
     1,320 ( 0.00%)          let is_planeswalker = definition.is_planeswalker();
    10,560 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (660x)
         .                   let mut counters = CounterBag::default();
     1,320 ( 0.00%)          if is_planeswalker && base_loyalty > 0 {
         .                       counters.insert(CounterType::Loyalty, base_loyalty);
         .                   }
         .                   // CR 310.7 — a Battle enters with defense counters equal to its printed
         .                   // defense (re-seeded on reanimate/blink in `move_card_to`).
     2,640 ( 0.00%)          if definition.is_battle() && definition.defense > 0 {
    10,560 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_battle (660x)
         .                       counters.insert(CounterType::Defense, definition.defense);
         .                   }
    82,500 ( 0.00%)          CardData {
         .                       id,
         .                       definition,
         .                       owner,
         .                       controller: owner,
         .                       tapped: false,
         .                       damage: 0,
         .                       summoning_sick,
         .                       power_bonus: 0,
         .                       toughness_bonus: 0,
         .                       perm_power_bonus: 0,
         .                       perm_toughness_bonus: 0,
     2,640 ( 0.00%)              counters,
         .                       attached_to: None,
         .                       attached_to_player: None,
         .                       soulbond_partner: None,
         .                       kicked: false,
         .                       granted_activated_abilities: Vec::new(),
         .                       granted_activated_eot: Vec::new(),
         .                       kick_count: 0,
         .                       squad_count: 0,
-- line 6367 ----------------------------------------
-- line 6490 ----------------------------------------
         .                       compleated_life_paid: 0,
         .                       meld_parts: Vec::new(),
         .                       mutate_stack: Vec::new(),
         .                       mutate_onto: None,
         .                       protected_by: None,
         .                       resolve_riders: None,
         .                   }
         .                   .into()
     5,280 ( 0.00%)      }
         .           
         .               pub fn new_token(id: CardId, definition: impl Into<Arc<CardDefinition>>, owner: usize) -> Self {
         .                   let mut instance = Self::new(id, definition, owner);
         .                   instance.is_token = true;
         .                   instance
         .               }
         .           
         .               /// CR 613.7a — the timestamp this object's static-ability effects order
         .               /// by. Id-order fallback for objects never stamped (direct test pushes,
         .               /// pre-stamp snapshots).
         .               pub fn object_timestamp(&self) -> u64 {
   383,648 ( 0.01%)          if self.battlefield_timestamp != 0 {
         .                       self.battlefield_timestamp
         .                   } else {
         .                       self.id.0 as u64
         .                   }
         .               }
         .           
    62,574 ( 0.00%)      pub fn power(&self) -> i32 {
         .                   let plus = self.counter_count(CounterType::PlusOnePlusOne) as i32;
         .                   let minus = self.counter_count(CounterType::MinusOneMinusOne) as i32;
         .                   let minus_one_zero = self.counter_count(CounterType::MinusOneMinusZero) as i32;
         .                   let plus_one_zero = self.counter_count(CounterType::PlusOnePlusZero) as i32;
         .                   let plus_two_zero = self.counter_count(CounterType::PlusTwoPlusZero) as i32;
         .                   let plus_two_two = self.counter_count(CounterType::PlusTwoPlusTwo) as i32;
   166,864 ( 0.01%)          self.definition.base_power() + self.power_bonus + self.perm_power_bonus + plus
         .                       - minus
         .                       - minus_one_zero
         .                       + plus_one_zero
         .                       + 2 * plus_two_zero
         .                       + 2 * plus_two_two
    83,432 ( 0.00%)      }
         .           
   195,272 ( 0.01%)      pub fn toughness(&self) -> i32 {
         .                   let plus = self.counter_count(CounterType::PlusOnePlusOne) as i32;
         .                   let minus = self.counter_count(CounterType::MinusOneMinusOne) as i32;
         .                   let minus_zero_one = self.counter_count(CounterType::MinusZeroMinusOne) as i32;
         .                   let minus_zero_two = self.counter_count(CounterType::MinusZeroMinusTwo) as i32;
         .                   let plus_zero_one = self.counter_count(CounterType::PlusZeroPlusOne) as i32;
         .                   let plus_zero_two = self.counter_count(CounterType::PlusZeroPlusTwo) as i32;
         .                   let plus_two_two = self.counter_count(CounterType::PlusTwoPlusTwo) as i32;
   439,362 ( 0.01%)          self.definition.base_toughness() + self.toughness_bonus + self.perm_toughness_bonus + plus
         .                       - minus
         .                       - minus_zero_one
         .                       - 2 * minus_zero_two
         .                       + plus_zero_one
         .                       + 2 * plus_zero_two
         .                       + 2 * plus_two_two
   244,090 ( 0.01%)      }
         .           
         .               pub fn counter_count(&self, ct: CounterType) -> u32 {
         .                   self.counters.get(&ct).copied().unwrap_or(0)
   108,506 ( 0.00%)      }
         .           
         .               /// CR 122.1 — a counter kind is on a permanent or it is not; a stored
         .               /// count of zero is neither, and readers that ask "does this map have
         .               /// entries" (CR 700.9 `IsModified`) can't tell the difference. These two
         .               /// maintain the invariant: no zero-valued entry ever lands in `counters`.
         .               pub fn add_counters(&mut self, ct: CounterType, n: u32) {
         .                   if n == 0 {
         .                       return;
-- line 6559 ----------------------------------------
-- line 6575 ----------------------------------------
         .               }
         .           
         .               pub fn is_dead(&self) -> bool {
         .                   if !self.definition.is_creature() { return false; }
         .                   if self.has_keyword(&Keyword::Indestructible) { return false; }
         .                   self.damage as i32 >= self.toughness()
         .               }
         .           
 1,132,644 ( 0.04%)      pub fn has_keyword(&self, kw: &Keyword) -> bool {
         .                   // Printed keyword, EOT-granted, or keyword counter (CR 122.1b)
         .                   // all qualify. The keyword-counter check requires at least one
         .                   // counter of the matching type to be present.
   188,774 ( 0.01%)          if self.removed_keywords_eot.contains(kw) || self.removed_keywords.contains(kw) {
         .                       return false;
         .                   }
         .                   self.definition.keywords.contains(kw)
         .                       || self.granted_keywords_eot.contains(kw)
         .                       || self.keyword_counters.get(kw).copied().unwrap_or(0) > 0
 1,132,644 ( 0.04%)      }
         .           
         .               /// True if this permanent has Toxic N for any N (printed or EOT-granted).
         .               /// The value-agnostic sibling of `has_keyword(&Toxic(n))`.
         .               pub fn has_toxic(&self) -> bool {
         .                   self.definition.keywords.iter().chain(self.granted_keywords_eot.iter())
         .                       .any(|k| matches!(k, Keyword::Toxic(_)))
         .               }
         .           
-- line 6601 ----------------------------------------
-- line 6603 ----------------------------------------
         .               pub fn has_modular(&self) -> bool {
         .                   self.definition.keywords.iter().chain(self.granted_keywords_eot.iter())
         .                       .any(|k| matches!(k, Keyword::Modular(_)))
         .               }
         .           
         .               /// True if this permanent can't be destroyed — either the
         .               /// Indestructible keyword (printed, granted, or via a keyword counter)
         .               /// or one or more `CounterType::Indestructible` counters (CR 122.1).
    99,316 ( 0.00%)      pub fn is_indestructible(&self) -> bool {
   297,948 ( 0.01%)          self.has_keyword(&Keyword::Indestructible)
 3,799,950 ( 0.12%)  => crabomination_base/src/card.rs:crabomination_base::card::CardInstance::has_keyword (49,658x)
         .                       || self.counter_count(CounterType::Indestructible) > 0
    99,316 ( 0.00%)      }
         .           
         .               pub fn has_protection_from(&self, color: Color) -> bool {
         .                   // Granted/stripped EOT keywords count, same as `has_keyword`.
         .                   self.has_keyword(&Keyword::Protection(color))
         .               }
         .           
         .               /// CR 708 — flip this permanent face down: stash the real definition in
         .               /// CR 902.5 / 315.5 — this command-zone card's abilities function from
-- line 6622 ----------------------------------------
-- line 6659 ----------------------------------------
         .               }
         .           
         .               /// CR 708.5/708.10 — flip this permanent face up (or restore the real
         .               /// card as it leaves the battlefield), restoring its real definition.
         .               /// Returns the real definition's name when a flip actually happened.
         .               /// CR 709.5c — unlocked-door designations are battlefield-only; clear
         .               /// them (and restore the printed no-abilities definition) as a Room
         .               /// permanent leaves the battlefield.
    31,056 ( 0.00%)      pub fn reset_room_doors(&mut self) {
    11,646 ( 0.00%)          if self.unlocked_doors != 0 && self.definition.room.is_some() {
         .                       self.unlocked_doors = 0;
         .                       self.definition = Arc::new(self.definition.room_definition_with(0));
         .                   }
    19,410 ( 0.00%)      }
         .           
         .               /// CR 716.2 — the Class level is battlefield-only; clear it as a Class
         .               /// leaves the battlefield so it re-enters at level 1.
         .               pub fn reset_class_level(&mut self) {
         .                   self.class_level = 0;
         .               }
         .           
         .               /// MKM — the solved designation is battlefield-only; clear it (and restore
         .               /// the printed always-on definition) as a Case leaves the battlefield.
    58,230 ( 0.00%)      pub fn reset_case(&mut self) {
    11,646 ( 0.00%)          if self.case_solved && self.definition.case.is_some() {
         .                       self.case_solved = false;
         .                       // Rebuild from the case data on the current (solved) def by dropping
         .                       // the appended solved abilities: re-derive from the un-appended base.
         .                       // The base always-on abilities are those minus the solved set.
         .                       if let Some(case) = self.definition.case.clone() {
         .                           let mut d = (*self.definition).clone();
         .                           let (t, a, s) = (&mut d.triggered_abilities, &mut d.activated_abilities, &mut d.static_abilities);
         .                           t.truncate(t.len().saturating_sub(case.solved_triggered.len()));
         .                           a.truncate(a.len().saturating_sub(case.solved_activated.len()));
         .                           s.truncate(s.len().saturating_sub(case.solved_static.len()));
         .                           self.definition = Arc::new(d);
         .                       }
         .                   }
    31,056 ( 0.00%)      }
         .           
         .               /// MKM — mark this Case solved and rebuild its live definition to switch on
         .               /// the "Solved —" abilities. Returns false if not a Case or already solved.
         .               pub fn solve_case(&mut self) -> bool {
         .                   if self.case_solved || self.definition.case.is_none() {
         .                       return false;
         .                   }
         .                   self.case_solved = true;
-- line 6705 ----------------------------------------
-- line 6769 ----------------------------------------
         .               /// [`make_licid_aura`]: Self::make_licid_aura
         .               pub fn undo_licid_aura(&mut self) {
         .                   if let Some(def) = self.licid_creature_def.take() {
         .                       self.definition = def;
         .                       self.attached_to = None;
         .                   }
         .               }
         .           
    27,174 ( 0.00%)      pub fn turn_face_up(&mut self) -> Option<&'static str> {
     3,882 ( 0.00%)          let real = self.face_up_def.take()?;
   116,460 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (3,882x)
         .                   let name = real.name;
         .                   self.definition = real;
         .                   self.face_down = false;
         .                   self.cloaked = false;
         .                   Some(name)
    34,938 ( 0.00%)      }
         .           
         .               /// CR 710.2 — flip this permanent to its flipped (bottom) face in place.
         .               /// No-op if already flipped or it has no flip face. Returns the flip
         .               /// face's name when a flip happened.
         .               pub fn flip(&mut self) -> Option<&'static str> {
         .                   if self.flipped {
         .                       return None;
         .                   }
-- line 6792 ----------------------------------------
-- line 6802 ----------------------------------------
         .                   self.unflipped_def = Some(self.definition.clone());
         .                   self.definition = Arc::new(flip);
         .                   self.flipped = true;
         .                   Some(name)
         .               }
         .           
         .               /// CR 710.2 — outside the battlefield only the unflipped characteristics
         .               /// exist; restore the top face as the card changes zones.
    20,050 ( 0.00%)      pub fn revert_flip(&mut self) {
    12,030 ( 0.00%)          if let Some(top) = self.unflipped_def.take() {
   120,300 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (4,010x)
         .                       self.definition = top;
         .                       self.flipped = false;
         .                   }
    20,050 ( 0.00%)      }
         .           
         .               /// CR 712.4 — a transforming double-faced permanent that leaves the
         .               /// battlefield has only its front-face characteristics in the new zone.
         .               /// Restore the front face (no-op for a permanent showing its front).
    19,410 ( 0.00%)      pub fn revert_transform(&mut self) {
    11,646 ( 0.00%)          if let Some(front) = self.front_face.take() {
   116,460 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (3,882x)
         .                       self.definition = front;
         .                       self.transformed = false;
         .                   }
    19,410 ( 0.00%)      }
         .           
         .               /// CR 702.160c — a permanent cast for its prototype cost has only its
         .               /// printed (full, colorless) characteristics once it leaves the
         .               /// battlefield. Restore the printed definition as the card changes zones.
    19,410 ( 0.00%)      pub fn revert_prototype(&mut self) {
    11,646 ( 0.00%)          if let Some(printed) = self.prototype_printed.take() {
   116,460 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (3,882x)
         .                       self.definition = printed;
         .                       self.cast_as_prototype = false;
         .                   }
    19,410 ( 0.00%)      }
         .           
         .               /// CR 702.140e — merge `incoming` onto this host permanent. `on_top`
         .               /// puts the incoming card's characteristics on top of the merged pile.
         .               /// The host keeps its id, counters, damage, and summoning sickness; only
         .               /// its printed characteristics + abilities change.
         .               pub fn apply_mutate(&mut self, incoming: CardInstance, on_top: bool) {
         .                   if self.mutate_stack.is_empty() {
         .                       // Seed the pile with a bare component carrying the host's printed
-- line 6843 ----------------------------------------
-- line 6886 ----------------------------------------
         .                       && !self.tapped
         .                       && (!self.has_keyword(&Keyword::Defender)
         .                           || self.has_keyword(&Keyword::AttacksAsThoughNoDefender))
         .                       && !self.has_keyword(&Keyword::CantAttack)
         .                       && (!self.summoning_sick || self.has_keyword(&Keyword::Haste))
         .               }
         .           
         .               pub fn can_block(&self) -> bool {
    15,284 ( 0.00%)          self.definition.is_creature() && !self.tapped
    91,070 ( 0.00%)      }
         .           
         .               /// The full Ward cost, if any (CR 702.21). Engine ward enforcement
         .               /// matches on the `WardCost` variants directly; this is the inspection
         .               /// helper.
         .               pub fn ward(&self) -> Option<&WardCost> {
         .                   self.definition.keywords.iter().find_map(|kw| {
         .                       if let Keyword::Ward(cost) = kw {
         .                           Some(cost)
-- line 6903 ----------------------------------------
-- line 6915 ----------------------------------------
         .                       _ => None,
         .                   }
         .               }
         .           
         .               /// CR 702.146e — true for a transformed Disturb card (a back face whose
         .               /// front prints Disturb): "if it would be put into a graveyard from
         .               /// anywhere, exile it instead." Consulted at the graveyard funnels.
         .               pub fn disturb_back_exiles(&self) -> bool {
    11,744 ( 0.00%)          self.transformed
         .                       && self
         .                           .front_face
         .                           .as_ref()
         .                           .is_some_and(|f| f.keywords.iter().any(|k| matches!(k, Keyword::Disturb(_))))
     5,872 ( 0.00%)      }
         .           
         .               /// Fold `amount` damage from a source named `name` into this turn's
         .               /// per-name tally (Blazing Effigy).
        44 ( 0.00%)      pub fn record_damage_from_named(&mut self, name: &'static str, amount: u32) {
         4 ( 0.00%)          match self.damage_by_source_name_this_turn.iter_mut().find(|(n, _)| *n == name) {
       120 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (4x)
         .                       Some((_, n)) => *n += amount,
        16 ( 0.00%)              None => self.damage_by_source_name_this_turn.push((name, amount)),
       120 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (4x)
         .                   }
        32 ( 0.00%)      }
         .           
         .               /// Fold `amount` damage from source instance `src` into this turn's
         .               /// per-source tally (Ogre Enforcer).
    35,632 ( 0.00%)      pub fn record_damage_from(&mut self, src: CardId, amount: u32) {
     4,454 ( 0.00%)          match self.damage_by_source_this_turn.iter_mut().find(|(s, _)| *s == src) {
   133,620 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (4,454x)
         .                       Some((_, n)) => *n += amount,
    21,646 ( 0.00%)              None => self.damage_by_source_this_turn.push((src, amount)),
   133,620 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (4,454x)
         .                   }
    26,724 ( 0.00%)      }
         .           
         .               /// The largest damage tally any single source has marked on this permanent
         .               /// this turn.
         .               pub fn max_damage_from_single_source(&self) -> u32 {
         .                   self.damage_by_source_this_turn.iter().map(|(_, n)| *n).max().unwrap_or(0)
         .               }
         .           
         .               /// True when every field [`clear_end_of_turn_effects`] resets already
         .               /// holds its cleared value.
         .               ///
         .               /// Read through `Deref`, so the cleanup sweep never takes a `&mut` — and
         .               /// never unshares the CoW `Arc` — for a permanent with nothing to clear,
         .               /// which is most of them on most turns. **Every field the clear writes
         .               /// must appear here**, or it is skipped while it still matters.
         .               fn end_of_turn_effects_are_clear(&self) -> bool {
    64,956 ( 0.00%)          self.power_bonus == 0
    64,852 ( 0.00%)              && self.toughness_bonus == 0
    64,852 ( 0.00%)              && self.loyalty_uses_this_turn == 0
    64,852 ( 0.00%)              && !self.loyalty_twice_this_turn
    64,852 ( 0.00%)              && self.once_per_turn_used.is_empty()
    64,852 ( 0.00%)              && self.granted_keywords_eot.is_empty()
    64,852 ( 0.00%)              && self.granted_keywords_eot_ts.is_empty()
    64,852 ( 0.00%)              && self.granted_activated_eot.is_empty()
    64,852 ( 0.00%)              && self.removed_keywords_eot.is_empty()
    32,426 ( 0.00%)              && self.granted_flashback_eot.is_none()
    32,426 ( 0.00%)              && self.granted_harmonize_eot.is_none()
    32,426 ( 0.00%)              && self.granted_alt_cast_cost_eot.is_none()
    32,426 ( 0.00%)              && self.granted_cast_surcharge_eot.is_none()
    64,852 ( 0.00%)              && !self.dealt_deathtouch_damage
    64,852 ( 0.00%)              && !self.dealt_damage_this_turn
    64,228 ( 0.00%)              && self.damage_dealt_to_this_turn == 0
    64,228 ( 0.00%)              && self.damaged_by_this_turn.is_empty()
    64,228 ( 0.00%)              && self.damage_by_source_name_this_turn.is_empty()
    64,228 ( 0.00%)              && self.damage_by_source_this_turn.is_empty()
    64,228 ( 0.00%)              && self.regeneration_shields == 0
    64,228 ( 0.00%)              && self.regeneration_control_grant.is_none()
    64,228 ( 0.00%)              && !self.cant_regenerate_this_turn
    64,228 ( 0.00%)              && !self.damage_prevention_off_eot
    64,228 ( 0.00%)              && !self.saddled
    64,228 ( 0.00%)              && self.saddled_by.is_empty()
         .                       && self.crewed_by.is_empty()
         .               }
         .           
   227,346 ( 0.01%)      pub fn clear_end_of_turn_effects(&mut self) {
         .                   // See `end_of_turn_effects_are_clear`: the sweep runs over every
         .                   // battlefield and phased-out permanent each turn, and each of the
         .                   // writes below is a `DerefMut` on the CoW handle.
    64,592 ( 0.00%)          if self.end_of_turn_effects_are_clear() {
         .                       return;
         .                   }
     1,092 ( 0.00%)          self.power_bonus = 0;
    87,973 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.toughness_bonus = 0;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.loyalty_uses_this_turn = 0;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.loyalty_twice_this_turn = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.once_per_turn_used.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.granted_keywords_eot.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.granted_keywords_eot_ts.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.granted_activated_eot.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.removed_keywords_eot.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.granted_flashback_eot = None;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.granted_harmonize_eot = None;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.granted_alt_cast_cost_eot = None;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     5,096 ( 0.00%)          self.granted_cast_surcharge_eot = None;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.dealt_deathtouch_damage = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.dealt_damage_this_turn = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.damage_dealt_to_this_turn = 0;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.damaged_by_this_turn.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.damage_by_source_name_this_turn.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.damage_by_source_this_turn.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
         .                   // CR 701.15g — unused regeneration shields expire at end of turn,
         .                   // and so does the "can't be regenerated this turn" lock.
     1,092 ( 0.00%)          self.regeneration_shields = 0;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.regeneration_control_grant = None;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.cant_regenerate_this_turn = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
     1,092 ( 0.00%)          self.damage_prevention_off_eot = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
         .                   // CR 702.171 — "saddled until end of turn" ends here.
     1,092 ( 0.00%)          self.saddled = false;
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.saddled_by.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
       728 ( 0.00%)          self.crewed_by.clear();
    10,920 ( 0.00%)  => crabomination_base/src/card.rs:<crabomination_base::card::CardInstance as core::ops::deref::DerefMut>::deref_mut (364x)
   194,868 ( 0.01%)      }
         .           
         .               /// The flashback cost this card can currently be cast with from a
         .               /// graveyard — its printed `Keyword::Flashback`, or an until-end-of-turn
         .               /// grant (the SOS "Flashback" instant). `None` if neither applies.
         .               pub fn effective_flashback(&self) -> Option<&ManaCost> {
         .                   self.definition
         .                       .has_flashback()
         .                       .or(self.granted_flashback_eot.as_ref())
-- line 7034 ----------------------------------------

13,236,370 ( 0.42%)  <counts for unidentified lines in crabomination_base/src/card.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/decision.rs
--------------------------------------------------------------------------------
Ir               

-- line 33 ----------------------------------------
      .               Scry,
      .               /// Surveil — unkept cards go to the graveyard.
      .               Surveil,
      .               /// Rearrange (Index, Spire Owl) — every card stays on top; no second
      .               /// bucket, the player only reorders.
      .               Rearrange,
      .           }
      .           
  2,830 ( 0.00%)  #[derive(Debug, Clone, Serialize, Deserialize)]
      .           pub enum Decision {
      .               /// Pick a target satisfying the ability's selector.
      .               ///
      .               /// `source_name` is the printed name of the source card; `description`
      .               /// is a short human-readable rendering of the effect ("exile target
      .               /// card from a graveyard"). Both are populated at decision-construction
      .               /// time so the UI can render a "<name> — <description>" prompt without
      .               /// re-deriving them from the effect tree. Empty `description` is fine
-- line 49 ----------------------------------------
-- line 111 ----------------------------------------
      .                   source: CardId,
      .                   description: String,
      .               },
      .           
      .               /// Choose `count` cards from hand to put on top of the library.
      .               /// The order of the returned IDs determines library order: index 0 ends up
      .               /// on top.
      .               PutOnLibrary {
     12 ( 0.00%)          player: usize,
      .                   count: usize,
     24 ( 0.00%)          hand: Vec<(CardId, String)>,
 15,862 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (12x)
      .               },
      .           
      .               /// Opening-hand keep-or-mulligan decision. The player sees their current
      .               /// hand and decides whether to keep, shuffle back and draw again, or use
      .               /// a Serum Powder to exile the hand and draw a fresh seven (the
      .               /// `serum_powders` field lists the mulligan-helper card IDs in hand —
      .               /// empty when none are present).
      .               Mulligan {
    186 ( 0.00%)          player: usize,
    186 ( 0.00%)          hand: Vec<(CardId, String)>,
 99,937 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (62x)
      .                   mulligans_taken: usize,
      .                   serum_powders: Vec<CardId>,
      .               },
      .           
      .               /// "As this enters, choose a creature type." Cavern of Souls.
      .               ChooseCreatureType {
      .                   /// Source permanent that's asking for the type.
      .                   source: CardId,
-- line 139 ----------------------------------------
-- line 228 ----------------------------------------
      .               /// blocking one attacker; combat damage is then assigned in that order
      .               /// (each blocker must be dealt lethal before the next gets any). The
      .               /// decider answers `DamageOrder(ordered_blocker_ids)`; `AutoDecider`
      .               /// keeps the engine's default (declaration / CardId order). Any ids the
      .               /// answer omits are appended in their original order, so a partial or
      .               /// empty answer is always legal.
      .               CombatDamageOrder {
      .                   attacker: CardId,
     16 ( 0.00%)          blockers: Vec<(CardId, String)>,
  2,748 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (8x)
      .               },
      .           
      .               /// CR 700.2d — choose `count` distinct modes (indices into `0..num_modes`)
      .               /// for a "choose N" modal spell/ability (Charms, the Strixhaven Command
      .               /// cycle). The decider answers `Modes(indices)`; `AutoDecider` returns the
      .               /// card's sensible `default` picks unchanged.
      .               ChooseModes {
      .                   source: CardId,
-- line 244 ----------------------------------------
-- line 295 ----------------------------------------
      .               /// assign more than `lethal` to any blocker (e.g. to deny trample) but
      .               /// must assign at least `lethal` to a blocker before any later blocker
      .               /// (or trample-over) receives damage. The decider answers
      .               /// `CombatDamageAssignment(amounts)`; `AutoDecider` returns an empty
      .               /// answer = the engine's default lethal-to-each-then-trample split.
      .               AssignCombatDamage {
      .                   attacker: CardId,
      .                   attacker_power: u32,
     16 ( 0.00%)          blockers: Vec<(CardId, String, u32)>,
  2,780 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:<alloc::vec::Vec<T,A> as core::clone::Clone>::clone (8x)
      .               },
      .           
      .               /// CR 704.5j — when a player controls two or more legendary permanents
      .               /// with the same name, that player chooses one to keep; the rest are put
      .               /// into their owners' graveyards. `duplicates` lists the tied permanents
      .               /// (id + name) sharing `name`. The decider answers `KeptLegend(id)` with
      .               /// the permanent to keep; an id not in `duplicates` (or `AutoDecider`'s
      .               /// default) keeps the newest (highest id).
-- line 311 ----------------------------------------
-- line 482 ----------------------------------------
      .                   answers: Vec<DecisionAnswer>,
      .                   asked: Vec<Decision>,
      .               },
      .           }
      .           
      .           impl DeciderKind {
      .               /// Materialize back into a boxed decider trait object.
      .               pub fn into_boxed(self) -> Box<dyn Decider + Send + Sync> {
134,408 ( 0.00%)          match self {
      .                       DeciderKind::Auto => Box::new(AutoDecider),
      .                       DeciderKind::Scripted { answers, asked } => Box::new(ScriptedDecider {
      .                           answers: answers.into_iter().collect(),
      .                           asked,
      .                       }),
      .                   }
      .               }
      .           }
      .           
      .           /// Default decider: picks the first/simplest legal answer. Good for tests
      .           /// where the choice doesn't matter and for engine bootstrapping.
      .           #[derive(Debug, Default, Clone, Copy)]
      .           pub struct AutoDecider;
      .           
      .           impl Decider for AutoDecider {
 67,204 ( 0.00%)      fn kind(&self) -> DeciderKind {
 33,602 ( 0.00%)          DeciderKind::Auto
 33,602 ( 0.00%)      }
  3,720 ( 0.00%)      fn decide(&mut self, decision: &Decision) -> DecisionAnswer {
  3,100 ( 0.00%)          match decision {
      .                       // Empty `legal` shouldn't be raised, but the server rope path
      .                       // calls this for stalled seats — decline (consumers fall back on
      .                       // a non-Target answer) instead of panicking.
      .                       Decision::ChooseTarget { legal, .. } => match legal.first() {
      .                           Some(t) => DecisionAnswer::Target(t.clone()),
      .                           None => DecisionAnswer::Bool(false),
      .                       },
      .                       Decision::ChooseMode { .. } => DecisionAnswer::Mode(0),
-- line 518 ----------------------------------------
-- line 587 ----------------------------------------
      .                       // that's 10. ScriptedDecider can script any specific face
      .                       // for testing branch coverage of result tables.
      .                       Decision::DieRoll { sides, .. } => {
      .                           let midpoint = (*sides as u32).max(1).div_ceil(2);
      .                           DecisionAnswer::DieRoll(midpoint as u8)
      .                       }
      .                       // CR 510.1c — keep the engine's default order (empty answer is
      .                       // treated as "all blockers in their original order").
  1,860 ( 0.00%)              Decision::CombatDamageOrder { .. } => DecisionAnswer::DamageOrder(vec![]),
      .                       // CR 700.2d — keep the card's sensible default mode picks.
      .                       Decision::ChooseModes { default, .. } => DecisionAnswer::Modes(default.clone()),
      .                       // CR 701.45 — prefer fetching a Lesson (card advantage) over
      .                       // rummaging; the decision is only surfaced when a Lesson exists.
      .                       Decision::Learn { lessons, .. } => DecisionAnswer::Learn(
      .                           lessons
      .                               .first()
      .                               .map(|(id, _)| LearnChoice::FetchLesson(*id))
-- line 603 ----------------------------------------
-- line 606 ----------------------------------------
      .                       // CR 603.3b — keep the engine's default order (empty answer).
      .                       Decision::OrderTriggers { .. } => DecisionAnswer::TriggerOrder(vec![]),
      .                       // CR 601.2d — spread the damage as evenly as possible.
      .                       Decision::DivideDamage { total, targets, .. } => {
      .                           DecisionAnswer::DamageDivision(even_damage_split(*total, targets.len()))
      .                       }
      .                       // CR 510.1c-d — keep the engine's default lethal-to-each split.
      .                       Decision::AssignCombatDamage { .. } => {
  1,860 ( 0.00%)                  DecisionAnswer::CombatDamageAssignment(vec![])
      .                       }
      .                       // CR 704.5j — keep the newest (highest id); the engine treats an
      .                       // out-of-set id this way, so any sentinel works.
      .                       Decision::ChooseLegendToKeep { duplicates, .. } => DecisionAnswer::KeptLegend(
      .                           duplicates.iter().map(|(id, _)| *id).max().unwrap_or(CardId(0)),
      .                       ),
      .                       // Conservative default — never pay life / sacrifice unprompted.
      .                       // ScriptedDecider supplies a positive amount.
-- line 622 ----------------------------------------
-- line 627 ----------------------------------------
      .                       // ScriptedDecider / the bot supply a positive subset.
      .                       // Forced "choose exactly N" (min > 0) auto-picks the first N
      .                       // candidates so a non-interactive decider still pays the cost;
      .                       // the "up to" case (min == 0, Devious Cover-Up) chooses nothing.
      .                       Decision::ChooseCards { candidates, min, .. } => DecisionAnswer::Cards(
      .                           candidates.iter().take(*min as usize).map(|(id, _)| *id).collect(),
      .                       ),
      .                   }
  4,960 ( 0.00%)      }
      .           }
      .           
      .           /// Pre-scripted decider for tests. Pops answers in FIFO order; falls back to
      .           /// `AutoDecider` when exhausted. Records every decision it saw for assertions.
      .           #[derive(Debug, Default)]
      .           pub struct ScriptedDecider {
      .               answers: std::collections::VecDeque<DecisionAnswer>,
      .               pub asked: Vec<Decision>,
      .           }
      .           
      .           impl ScriptedDecider {
      .               pub fn new(answers: impl IntoIterator<Item = DecisionAnswer>) -> Self {
 56,496 ( 0.00%)          Self {
      .                       answers: answers.into_iter().collect(),
      .                       asked: Vec::new(),
      .                   }
      .               }
      .           }
      .           
      .           impl Decider for ScriptedDecider {
      .               fn kind(&self) -> DeciderKind {
-- line 656 ----------------------------------------

135,142 ( 0.00%)  <counts for unidentified lines in crabomination/src/decision.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/team.rs
--------------------------------------------------------------------------------
Ir                 

-- line 7 ----------------------------------------
        .           //! the unit for "target opponent" / "each opponent" semantics.
        .           //!
        .           //! `GameState::new` populates `teams` with one singleton team per seat
        .           //! automatically. Team formats call `assign_teams` to re-partition seats
        .           //! after construction.
        .           
        .           use serde::{Deserialize, Serialize};
        .           
1,198,704 ( 0.04%)  #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
   10,464 ( 0.00%)  pub struct TeamId(pub usize);
        .           
        .           #[derive(Debug, Clone, Serialize, Deserialize)]
        .           pub struct Team {
        .               pub id: TeamId,
        .               /// Seat indices of the players on this team.
        .               pub members: Vec<usize>,
        .               /// Shared life pool. `Some(n)` means the team's members share a
        .               /// single life total (Two-Headed Giant — CR 810.8). `None` means
-- line 24 ----------------------------------------

  100,136 ( 0.00%)  <counts for unidentified lines in crabomination/src/team.rs>

--------------------------------------------------------------------------------
-- Auto-annotated source: crabomination/src/game/stack.rs
--------------------------------------------------------------------------------
Ir                 

-- line 5 ----------------------------------------
        .           use crate::game::types::{DelayedKind, DelayedTrigger};
        .           
        .           /// A collected death/leaves trigger to fire from a dying permanent:
        .           /// `(source, effect, controller, intervening/subject filter)`.
        .           type DeathTrigger = (CardId, Effect, usize, Option<crate::card::Predicate>);
        .           
        .           /// Presence flags for the rare state-based actions, taken in one battlefield
        .           /// pass by [`GameState::sba_board_scan`]. See that method for the contract.
   53,350 ( 0.00%)  #[derive(Debug, Default, Clone, Copy)]
        .           struct SbaBoardScan {
        .               flip_keyword: bool,
        .               flip_predicate: bool,
        .               sacrifice_when: bool,
        .               state_trigger: bool,
        .               steal_penalty: bool,
        .               no_other: bool,
        .               persist_undying: bool,
-- line 21 ----------------------------------------
-- line 68 ----------------------------------------
        .               ///
        .               /// The picked index is clamped to `modes.len() - 1` to guard against
        .               /// a misbehaving decider returning an out-of-range mode. Effects that
        .               /// nest `ChooseMode` inside `Seq`/`If`/`ForEach` are not addressed
        .               /// here — those would need a recursive walk and an N-tuple of picks;
        .               /// the printed Magic cards in scope today (Prismari Apprentice,
        .               /// future Tempted by the Oriq Magecraft rider) all have a top-level
        .               /// `ChooseMode` so the simple walk is sufficient.
    1,456 ( 0.00%)      pub(crate) fn pick_trigger_mode(
        .                   &mut self,
        .                   effect: &Effect,
        .                   source: CardId,
        .                   controller: usize,
        .               ) -> Option<usize> {
      364 ( 0.00%)          if let Effect::ChooseMode(modes) = effect {
      182 ( 0.00%)              if modes.is_empty() {
        .                           return None;
        .                       }
        .                       // A `wants_ui` controller picks through the client modal at
        .                       // resolution time instead of the synchronous decider (which
        .                       // would silently take mode 0 — Riot creatures never hasty).
        .                       // Deferral is gated on no mode requiring a target, since target
        .                       // slots are assigned at push time, before the pick exists.
        .                       if self.players.get(controller).is_some_and(|p| p.wants_ui)
-- line 91 ----------------------------------------
-- line 104 ----------------------------------------
        .                       return None;
        .                   }
        .                   // CR 603.7 — a modal buried behind a reflexive payment (Voltstorm
        .                   // Angel's "pay {E}{E}. When you do, choose one …") owns its pick at
        .                   // resolution, *after* the payment succeeds. Defer it: the mode isn't
        .                   // read until the wrappers run, and `MODE_PICK_DEFERRED` routes a UI
        .                   // seat to the client modal and a bot to its decider (both post-payment,
        .                   // since the payment only ever runs when accepted).
      728 ( 0.00%)          if let Some(modes) = Self::governing_modal(effect)
    3,774 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::governing_modal (182x)
        .                       && !modes.is_empty()
        .                       && modes.iter().all(|m| !m.requires_target())
        .                   {
        .                       return Some(crate::game::types::MODE_PICK_DEFERRED);
        .                   }
        .                   None
    1,638 ( 0.00%)      }
        .           
        .               /// Unwrap reflexive-payment wrappers (`MayDo`/`MayPay*`/`PayEnergy*`) and
        .               /// the cost-capture wrappers the activation path adds
        .               /// (`WithSacrificedPt`/`WithTappedPower`) to find a nested `ChooseMode`.
        .               /// Only descends single-child bodies — a `Seq` or branching effect is left
        .               /// to its own resolution-time handling.
      910 ( 0.00%)      pub(crate) fn governing_modal(effect: &Effect) -> Option<&Vec<Effect>> {
    1,772 ( 0.00%)          match effect {
        .                       Effect::ChooseMode(modes) => Some(modes),
        .                       Effect::MayDo { body, .. }
        .                       | Effect::PayEnergy { then: body, .. }
        .                       | Effect::PayEnergyValue { then: body, .. }
        .                       | Effect::WithSacrificedPt { body, .. }
        .                       | Effect::WithTappedPower { body, .. } => Self::governing_modal(body),
        .                       Effect::MayPay { body, .. } | Effect::MayPayLife { body, .. } => {
        .                           Self::governing_modal(body)
        .                       }
        .                       // A modal step inside a sequence still governs the whole
        .                       // resolution's mode slot — Urza's Avenger's "-1/-1 and gains your
        .                       // choice of …". The first modal step wins.
        .                       Effect::Seq(steps) => steps.iter().find_map(Self::governing_modal),
        .                       _ => None,
        .                   }
      910 ( 0.00%)      }
        .           }
        .           
        .           impl GameState {
        .               // ── Pass priority ─────────────────────────────────────────────────────────
        .           
        .               /// True when the next [`pass_priority`](Self::pass_priority) will take its
        .               /// early return: not every player has passed yet, so it bumps the pass
        .               /// counter, hands priority to the next seat and stops. That branch is
        .               /// infallible and writes two integers — nothing to roll back — which is
        .               /// what lets `perform_action` skip its transaction checkpoint for it.
        .               /// Every other branch resolves the stack or advances the step and can
        .               /// fail, so it keeps the checkpoint.
        .               pub(crate) fn pass_priority_is_trivial(&self) -> bool {
   57,534 ( 0.00%)          self.priority.consecutive_passes + 1 < self.alive_count()
        .               }
        .           
  492,174 ( 0.02%)      pub fn pass_priority(&mut self) -> Result<Vec<GameEvent>, GameError> {
        .                   let alive = self.alive_count();
  164,058 ( 0.01%)          self.priority.consecutive_passes += 1;
        .           
  109,372 ( 0.00%)          if self.priority.consecutive_passes < alive {
        .                       // Move priority to the next non-eliminated player.
   26,736 ( 0.00%)              self.priority.player_with_priority =
  106,944 ( 0.00%)                  self.next_alive_seat(self.priority.player_with_priority);
  106,944 ( 0.00%)              return Ok(vec![]);
        .                   }
        .           
        .                   // All players passed — either resolve top of stack or advance the step.
   27,950 ( 0.00%)          self.priority.consecutive_passes = 0;
        .           
   83,850 ( 0.00%)          if !self.stack.is_empty() {
    8,248 ( 0.00%)              let events = self.resolve_top_of_stack()?;
146,121,817 ( 4.69%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack (4,124x)
        .                       // After resolution, active player gets priority again.
        .                       self.give_priority_to_active();
        .                       return Ok(events);
        .                   }
        .           
        .                   // Stack is empty — advance to next step.
        .           
        .                   // MTG rule 500.4: mana pools empty at the end of each step and phase
        .                   // (Kruphix converts to colorless instead, CR 106.4 override).
   47,652 ( 0.00%)          self.empty_mana_pools();
2,741,392 ( 0.09%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::empty_mana_pools (22,108x)
        .           
        .                   // Auto-declare empty blockers if no one blocked.
        .                   let mut events = vec![];
   47,652 ( 0.00%)          if self.step == TurnStep::DeclareBlockers
   10,368 ( 0.00%)              && !self.attacking.is_empty()
        .                       && !self.blockers_declared
        .                   {
       14 ( 0.00%)              self.blockers_declared = true;
        .                       // CR 509.3g — every attacker went unblocked; "attacks and isn't
        .                       // blocked" triggers fire on this path too, not just on an
        .                       // explicit DeclareBlockers action. Dispatch inline: if anything
        .                       // triggered, stop here (priority round for the trigger) instead
        .                       // of advancing into combat damage in the same pass.
        .                       let unblocked: Vec<GameEvent> = self
        .                           .attacking
        .                           .iter()
        .                           .map(|atk| GameEvent::AttackerWentUnblocked { attacker: atk.attacker })
        .                           .collect();
        .                       // Dispatched here, not via the returned events — the caller
        .                       // (`perform_action`) re-dispatches returned events, which would
        .                       // double-fire the triggers.
       14 ( 0.00%)              let stack_before = self.stack.len();
       56 ( 0.00%)              self.dispatch_triggers_for_events(&unblocked);
  123,663 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (14x)
       70 ( 0.00%)              if self.stack.len() > stack_before || self.pending_decision.is_some() {
        .                           self.give_priority_to_active();
        .                           return Ok(events);
        .                       }
        .                   }
        .           
   42,496 ( 0.00%)          if self.step == TurnStep::Cleanup {
        .                       // All players passed during a CR 514.3a cleanup priority round
        .                       // with an empty stack — another cleanup round happens. It may
        .                       // suspend (UI discard), grant priority again (more triggers), or
        .                       // finish the turn.
        .                       return match self.do_cleanup(&mut events) {
        .                           CleanupOutcome::Suspended | CleanupOutcome::PriorityGranted => Ok(events),
        .                           CleanupOutcome::TurnOver => self.advance_step(events),
        .                       };
        .                   }
        .           
        .                   self.advance_step(events)
  492,174 ( 0.02%)      }
        .           
        .               /// CR 614 — Fasting: "if you would begin your draw step, you may skip
        .               /// that step instead. If you do, you gain N life." Returns true when the
        .               /// active player takes the skip; the life rider is paid here.
        .               fn offer_optional_draw_step_skip(&mut self, events: &mut Vec<GameEvent>) -> bool {
    1,718 ( 0.00%)          let active = self.active_player_idx;
    1,718 ( 0.00%)          let offers: Vec<(crate::card::CardId, &'static str, u32)> = self
        .                       .battlefield
        .                       .iter()
   97,434 ( 0.00%)              .filter(|c| c.controller == active)
        .                       .flat_map(|c| {
        .                           c.definition.static_abilities.iter().filter_map(move |sa| {
        .                               match sa.effect {
        .                                   crate::effect::StaticEffect::ControllerMaySkipDrawStepForLife { life } => {
        .                                       Some((c.id, c.definition.name, life))
        .                                   }
        .                                   _ => None,
        .                               }
-- line 245 ----------------------------------------
-- line 265 ----------------------------------------
        .                   }
        .                   false
        .               }
        .           
        .               /// Compute and enter the step following the current one, running its
        .               /// turn-based entry actions (untap, draw, combat resolution, step
        .               /// triggers, …). Split out of `pass_priority` so the cleanup discard
        .               /// resume path can re-run it after a suspended discard is answered.
  306,528 ( 0.01%)      pub fn advance_step(
        .                   &mut self,
        .                   mut events: Vec<GameEvent>,
        .               ) -> Result<Vec<GameEvent>, GameError> {
        .                   // CR 702.94 — a step-bounded Miracle window ("cast it now or lose
        .                   // the chance") dies at the step transition; the alt-cost shares the
        .                   // permission's lifetime.
        .                   self.clear_step_bounded_may_play();
        .                   // Skip FirstStrikeDamage if no first/double-strike creatures are in combat.
   25,544 ( 0.00%)          let mut next = self.step.next();
    5,184 ( 0.00%)          if next == TurnStep::FirstStrikeDamage && !self.has_first_strikers() {
        .                       next = next.next(); // skip directly to CombatDamage
        .                   }
        .                   // CR 506.1 — "The declare blockers and combat damage steps are
        .                   // skipped if no creatures are declared as attackers or put onto
        .                   // the battlefield attacking." When the DeclareAttackers step
        .                   // ends with no attackers, advance straight past DeclareBlockers /
        .                   // FirstStrikeDamage / CombatDamage to EndCombat. Trigger windows
        .                   // for "at the beginning of combat" still fire at BeginCombat
        .                   // since that step is unaffected.
    9,558 ( 0.00%)          if self.step == TurnStep::DeclareAttackers && self.attacking.is_empty() {
        .                       next = TurnStep::EndCombat;
        .                   }
        .           
        .                   // CR 506 — skip the active player's combat phase (Stonehorn
        .                   // Dignitary). When we'd enter Begin Combat with a banked skip charge,
        .                   // consume it and jump straight to the postcombat main — no
        .                   // begin-combat triggers, declares, or damage. (The post-main extra
        .                   // combat below is unaffected; only the scheduled combat is skipped.)
        .                   if next == TurnStep::BeginCombat
        .                       && self.step == TurnStep::PreCombatMain
   10,456 ( 0.00%)              && self.players[self.active_player_idx].skip_next_combat > 0
        .                   {
        .                       self.players[self.active_player_idx].skip_next_combat -= 1;
        .                       next = TurnStep::PostCombatMain;
        .                   }
        .           
        .                   // CR 505.1b — additional combat phase. When the active player leaves
        .                   // End of Combat with a banked extra phase, loop back to Begin Combat
        .                   // (a fresh combat) instead of advancing to the postcombat main.
   34,560 ( 0.00%)          if self.step == TurnStep::EndCombat && self.additional_combat_phases > 0 {
        .                       self.additional_combat_phases -= 1;
        .                       next = TurnStep::BeginCombat;
        .                   }
        .           
        .                   // CR 505.1b — "after this main phase, an additional combat phase
        .                   // followed by an additional main phase" (Relentless Assault). When
        .                   // the active player leaves the postcombat main with one banked,
        .                   // enter Begin Combat; EndCombat → PostCombatMain then supplies the
        .                   // extra main. (A precombat-main cast banks the phase until after
        .                   // the turn's scheduled combat rather than inserting it before.)
   48,304 ( 0.00%)          if self.step == TurnStep::PostCombatMain && self.additional_post_main_combats > 0 {
        .                       self.additional_post_main_combats -= 1;
        .                       next = TurnStep::BeginCombat;
        .                   }
        .           
        .                   // CR 500.7 — additional end step. When the active player leaves the
        .                   // End step with one banked, loop back to another End step instead of
        .                   // advancing to cleanup (Y'shtola Rhul).
   54,292 ( 0.00%)          if self.step == TurnStep::End && self.additional_end_steps > 0 {
        .                       self.additional_end_steps -= 1;
        .                       next = TurnStep::End;
        .                   }
        .           
        .                   // CR 500.9 — additional upkeep step. When the active player leaves
        .                   // the Upkeep with one banked, loop back to another Upkeep instead of
        .                   // advancing to Draw (Paradox Haze).
   56,034 ( 0.00%)          if self.step == TurnStep::Upkeep && self.additional_upkeep_steps > 0 {
        .                       self.additional_upkeep_steps -= 1;
        .                       next = TurnStep::Upkeep;
        .                   }
        .           
        .                   // CR 511.2 — "Effects that last 'until end of combat' expire at the
        .                   // end of the combat phase." Sweep `UntilEndOfCombat` continuous
        .                   // effects whenever we leave EndCombat — including into an additional
        .                   // combat phase, since each combat phase has its own end.
   15,512 ( 0.00%)          if self.step == TurnStep::EndCombat {
        .                       self.expire_end_of_combat_effects();
    6,912 ( 0.00%)              self.revert_temporary_control(&[crate::effect::Duration::EndOfCombat]);
2,347,822 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_control (1,728x)
    6,912 ( 0.00%)              self.revert_temporary_copies(&[crate::effect::Duration::EndOfCombat]);
  276,480 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_copies (1,728x)
    1,728 ( 0.00%)              let mut cleanup = self.process_attacking_token_cleanup();
32,088,098 ( 1.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_attacking_token_cleanup (1,728x)
        .                       events.append(&mut cleanup);
        .                   }
        .           
   25,544 ( 0.00%)          self.step = next;
        .                   // Per-step draw tallies reset at every step boundary (Orcish
        .                   // Bowmasters' "first card drawn in the draw step" exemption).
   51,088 ( 0.00%)          for pl in &mut self.players {
  153,264 ( 0.00%)              pl.cards_drawn_this_step = 0;
1,326,480 ( 0.04%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (44,216x)
        .                   }
   51,088 ( 0.00%)          events.push(GameEvent::StepChanged(next));
        .           
        .                   // CR 614.10 — skip-step replacements (Eon Hub / Stasis family). A
        .                   // skipped upkeep or draw step never occurs: no turn-based actions,
        .                   // step triggers, or priority — advance straight through. (A skipped
        .                   // untap is handled in the Untap arm so the turn still starts.)
   76,632 ( 0.00%)          if matches!(next, TurnStep::Upkeep | TurnStep::Draw)
    3,484 ( 0.00%)              && self.step_skipped_for(self.active_player_idx, next)
        .                   {
        .                       return self.advance_step(events);
        .                   }
        .           
  153,264 ( 0.00%)          match next {
        .                       // Untap has no priority window — auto-execute and move on.
        .                       TurnStep::Untap => {
        .                           // CR 615 — "until your next turn" damage locks (Kiora's +1)
        .                           // end as the granting player's turn begins.
    1,718 ( 0.00%)                  let ap = self.active_player_idx;
        .                           self.damage_locked_until_turn_of.retain(|(_, seat)| *seat != ap);
        .                           // Oracle en-Vec's mandate arms as its victim's turn begins.
   10,308 ( 0.00%)                  for m in self.attack_mandates.iter_mut().filter(|m| m.seat == ap) {
        .                               m.armed = true;
        .                           }
        .                           // Arboria — the active player's "acted during their last turn"
        .                           // flag starts over as their new turn begins.
    3,436 ( 0.00%)                  if self.acted_on_own_turn.len() < self.players.len() {
        .                               self.acted_on_own_turn.resize(self.players.len(), false);
        .                           }
    1,718 ( 0.00%)                  self.acted_on_own_turn[ap] = false;
        .                           // CR 614.10 — a skipped untap step skips its turn-based
        .                           // actions (untapping, phasing, day/night), but the turn
        .                           // itself still begins.
    1,718 ( 0.00%)                  if !self.step_skipped_for(self.active_player_idx, TurnStep::Untap) {
        .                               // CR 502.2 — day/night turn-based check. Runs BEFORE
        .                               // do_untap so an extra turn (previous active == active)
        .                               // reads the real previous-turn spell count rather than
        .                               // the counter do_untap is about to reset. A transition
        .                               // here fires "day becomes night …" triggers (Brimstone
        .                               // Vandal) — dispatch them before priority is given out.
        .                               let mut dn_evs = Vec::new();
        .                               self.check_day_night_transition(&mut dn_evs);
        .                               if !dn_evs.is_empty() {
        .                                   self.dispatch_triggers_for_events(&dn_evs);
        .                                   events.append(&mut dn_evs);
        .                               }
    3,436 ( 0.00%)                      self.do_untap();
48,937,412 ( 1.57%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_untap (1,718x)
        .                           }
        .                           // CR 611.2b — "until the next turn" / "until your next turn"
        .                           // continuous effects end as the relevant turn begins.
    6,872 ( 0.00%)                  let (active, turn) = (self.active_player_idx, self.turn_number);
    6,872 ( 0.00%)                  self.continuous_effects.retain(|e| match e.duration {
   42,950 ( 0.00%)  => /home/user/Crabomination/crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut (1,718x)
   30,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (1,718x)
        .                               crate::game::layers::EffectDuration::UntilNextTurn => false,
        .                               crate::game::layers::EffectDuration::UntilYourNextTurn {
        .                                   player,
        .                                   installed_turn,
        .                               } => !(player == active && turn > installed_turn),
        .                               _ => true,
        .                           });
        .                           // CR 801.2c — ranges of influence are determined as each turn
        .                           // begins, so a player leaving only shifts them now.
    1,718 ( 0.00%)                  self.refresh_range_matrix();
  118,542 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::refresh_range_matrix (1,718x)
        .                           // CR 723.1 — a pending player-control effect applies to the
        .                           // next turn its target actually takes, and expires here.
    3,436 ( 0.00%)                  self.apply_pending_player_control(self.active_player_idx);
   88,296 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::apply_pending_player_control (1,718x)
    5,154 ( 0.00%)                  events.push(GameEvent::TurnStarted {
    1,718 ( 0.00%)                      player: self.active_player_idx,
    1,718 ( 0.00%)                      turn: self.turn_number,
        .                           });
        .                           // No priority in Untap (CR 502.4) — immediately advance to
        .                           // Upkeep. Seed the pass count so the single pass below counts
        .                           // as everyone passing rather than needing a phantom extra
        .                           // client pass.
    3,436 ( 0.00%)                  self.priority.player_with_priority = self.active_player_idx;
    3,436 ( 0.00%)                  self.priority.consecutive_passes = self.alive_count().saturating_sub(1);
   10,308 ( 0.00%)                  let mut upkeep_events = self.pass_priority()?;
13,073,045 ( 0.42%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pass_priority'2 (1,718x)
        .                           events.append(&mut upkeep_events);
    6,872 ( 0.00%)                  return Ok(events);
        .                       }
        .                       TurnStep::Draw => {
        .                           // CR 103.7a — only the *starting player's first* draw step is
        .                           // skipped. Consuming the flag unconditionally handed the skip
        .                           // to whoever reached a draw step first, so a game advanced
        .                           // past turn 1 (every test fixture) robbed the wrong seat.
    1,742 ( 0.00%)                  let skips_draw_step = self.battlefield.iter().any(|c| {
   64,956 ( 0.00%)                      c.controller == self.active_player_idx
        .                                   && c.definition.static_abilities.iter().any(|sa| {
        .                                       sa.effect == crate::effect::StaticEffect::ControllerSkipsDrawStep
        .                                   })
        .                           });
        .                           let first_draw_skip =
        .                               std::mem::take(&mut self.skip_first_draw) && self.turn_number == 1;
        .                           // CR 614.13 — a "skip your next draw step" charge is only spent
        .                           // when it's the effect doing the skipping; another skip already
        .                           // in force leaves the charge waiting for the next draw step.
    3,532 ( 0.00%)                  let charged_skip = !first_draw_skip
        .                               && !skips_draw_step
    5,154 ( 0.00%)                      && self.players[self.active_player_idx].skip_next_draw_step > 0;
    1,718 ( 0.00%)                  if charged_skip {
        .                               self.players[self.active_player_idx].skip_next_draw_step -= 1;
        .                           }
        .                           // CR 614 — Fasting's optional draw-step replacement. Only
        .                           // asked when the step would otherwise happen; taking it pays
        .                           // the printed life rider.
        .                           let optional_skip = !(first_draw_skip || skips_draw_step || charged_skip)
        .                               && self.offer_optional_draw_step_skip(&mut events);
        .                           if first_draw_skip || skips_draw_step || charged_skip || optional_skip {
        .                           } else {
        .                               // CR 805.4b / 805.6a — each player on the active team
        .                               // draws, in seat order. A singleton team is just the
        .                               // active player.
    1,718 ( 0.00%)                      for p in self.active_team_members() {
  177,960 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::active_team_members (1,718x)
        .                                   // CR 504.1 — the turn-based draw-step draw is exempt from
        .                                   // Notion Thief's redirect ("except the first one they draw").
    1,718 ( 0.00%)                          self.in_turn_based_draw = true;
    6,872 ( 0.00%)                          let drew = self.draw_one(p, &mut events);
8,405,703 ( 0.27%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::draw_one (1,718x)
    1,718 ( 0.00%)                          self.in_turn_based_draw = false;
    3,436 ( 0.00%)                          if !drew {
        .                                       // CR 104.3c (or the Lab-Man win override). Game-over
        .                                       // check happens inside SBA.
        .                                       self.lose_to_empty_draw(p);
        .                                       let mut sba = self.check_state_based_actions();
        .                                       events.append(&mut sba);
        .                                       if self.is_game_over() {
        .                                           return Ok(events);
        .                                       }
        .                                   }
        .                               }
        .                           }
        .                           // CR 504.1 — "at the beginning of your draw step" triggers go
        .                           // on the stack after the turn-based draw (Armageddon Clock).
    3,484 ( 0.00%)                  self.fire_step_triggers(TurnStep::Draw);
2,540,444 ( 0.08%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (1,742x)
        .                           self.give_priority_to_active();
        .                       }
        .                       TurnStep::Upkeep => {
    3,484 ( 0.00%)                  self.upkeep_steps_this_turn = self.upkeep_steps_this_turn.saturating_add(1);
        .                           // CR 611.2b — "until your next upkeep" ends as that upkeep
        .                           // begins, before any of this step's turn-based actions.
    6,968 ( 0.00%)                  let (active, turn) = (self.active_player_idx, self.turn_number);
    6,968 ( 0.00%)                  self.continuous_effects.retain(|e| match e.duration {
    8,444 ( 0.00%)  => /home/user/Crabomination/crabomination/src/cow.rs:<crabomination::cow::CowBox<T> as core::ops::deref::DerefMut>::deref_mut (24x)
      432 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (24x)
        .                               crate::game::layers::EffectDuration::UntilYourNextUpkeep {
        .                                   player,
        .                                   installed_turn,
        .                               } => !(player == active && turn > installed_turn),
        .                               _ => true,
        .                           });
        .                           // CR 702.32 / 702.62 — Fading / Vanishing tick down as a
        .                           // turn-based action at upkeep, before step triggers.
    1,742 ( 0.00%)                  let mut fv = self.process_fading_vanishing();
    1,752 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_fading_vanishing (24x)
        .                           events.append(&mut fv);
        .                           // CR 702.62d/e — Suspend time counters tick at the owner's
        .                           // upkeep; the spell is cast for free when the last comes off.
    3,484 ( 0.00%)                  let mut susp = self.process_suspend();
    2,832 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_suspend (24x)
    3,484 ( 0.00%)                  susp.append(&mut self.process_delayed_spells());
    1,728 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_delayed_spells (24x)
        .                           events.append(&mut susp);
        .                           // Uvilda — hone counters tick down at the owner's upkeep.
    3,484 ( 0.00%)                  let mut hone = self.process_hone();
    1,776 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_hone (24x)
        .                           events.append(&mut hone);
        .                           // CR 726.2 — "At the beginning of the upkeep of the player who
        .                           // has the initiative, that player ventures into Undercity."
        .                           if self.initiative == Some(self.active_player_idx) {
        .                               self.push_undercity_venture(self.active_player_idx);
        .                           }
        .                           // All Hallow's Eve — exile fuses tick at the owner's upkeep.
    3,484 ( 0.00%)                  let mut fuse = self.process_exile_countdowns();
    1,728 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_exile_countdowns (24x)
        .                           events.append(&mut fuse);
        .                           // CR 702.24 — cumulative upkeep: age counter + pay-or-sacrifice.
    3,484 ( 0.00%)                  let mut cu = self.process_cumulative_upkeep();
   26,318 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_cumulative_upkeep (24x)
        .                           events.append(&mut cu);
        .                           // CR 702.29 — echo: pay-or-sacrifice on the first upkeep.
    3,484 ( 0.00%)                  let mut echo = self.process_echo();
    1,944 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_echo (24x)
        .                           events.append(&mut echo);
        .                           // CR 702.50a — Epic spells copy at the controller's upkeep.
    3,484 ( 0.00%)                  let mut epic = self.process_epic();
      984 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::process_epic (24x)
        .                           events.append(&mut epic);
    5,226 ( 0.00%)                  self.fire_step_triggers(TurnStep::Upkeep);
   13,464 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (24x)
        .                           self.give_priority_to_active();
        .                       }
        .                       TurnStep::PreCombatMain => {
        .                           // CR 728.2 — rad-counter mill is a turn-based action that
        .                           // happens as the precombat main phase begins, before
        .                           // players receive priority (and thus before step triggers).
        .                           let mut rad = self.do_rad_counters();
        .                           events.append(&mut rad);
        .                           // CR 714.2b — as the active player's precombat main phase
        .                           // begins, put a lore counter on each Saga they control,
        .                           // firing that chapter's ability.
    1,742 ( 0.00%)                  let sagas: Vec<CardId> = self
        .                               .battlefield
        .                               .iter()
        .                               .filter(|c| {
   64,956 ( 0.00%)                          c.controller == self.active_player_idx
        .                                       && !c.definition.saga_chapters.is_empty()
        .                               })
        .                               .map(|c| c.id)
        .                               .collect();
        .                           for id in sagas {
        .                               self.saga_advance(id);
        .                           }
        .                           // CR 703.4g / 717.4 — right after the lore counters, an active
        .                           // player who controls an Attraction rolls to visit them.
    3,484 ( 0.00%)                  let mut visits = self.roll_to_visit_attractions(self.active_player_idx);
        .                           events.append(&mut visits);
        .                           // CR 904.9 — immediately after the archenemy's precombat main
        .                           // phase begins, they set the top scheme in motion. A turn-based
        .                           // action, so it precedes the step triggers.
        .                           if self.archenemy == Some(self.active_player_idx) {
        .                               self.set_scheme_in_motion(self.active_player_idx);
        .                           }
    5,226 ( 0.00%)                  self.fire_step_triggers(TurnStep::PreCombatMain);
2,540,444 ( 0.08%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (1,742x)
        .                           self.give_priority_to_active();
        .                       }
        .                       TurnStep::BeginCombat => {
    5,228 ( 0.00%)                  self.combat_phases_this_turn = self.combat_phases_this_turn.saturating_add(1);
   10,456 ( 0.00%)                  self.fire_step_triggers(TurnStep::BeginCombat);
3,774,224 ( 0.12%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (2,614x)
        .                           self.give_priority_to_active();
        .                       }
        .                       TurnStep::FirstStrikeDamage => {
      468 ( 0.00%)                  let mut fs_events = self.resolve_first_strike_damage()?;
10,423,684 ( 0.33%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_first_strike_damage (78x)
        .                           events.append(&mut fs_events);
        .                           // Combat damage may suspend on a `wants_ui` player's ordering /
        .                           // assignment choice; leave priority alone until it's answered.
        .                           if self.pending_decision.is_none() {
        .                               self.give_priority_to_active();
        .                           }
        .                       }
        .                       TurnStep::CombatDamage => {
   15,456 ( 0.00%)                  let mut combat_events = self.resolve_combat()?;
335,561,787 (10.77%)  => /home/user/Crabomination/crabomination/src/game/combat.rs:crabomination::game::combat::<impl crabomination::game::GameState>::resolve_combat (2,576x)
        .                           events.append(&mut combat_events);
        .                           if self.pending_decision.is_none() {
        .                               self.give_priority_to_active();
        .                           }
        .                       }
        .                       TurnStep::End => {
    3,436 ( 0.00%)                  self.end_steps_this_turn = self.end_steps_this_turn.saturating_add(1);
        .                           // Oracle en-Vec — the mandate's payoff: every chosen creature
        .                           // that sat out is destroyed, then the mandate expires.
    1,718 ( 0.00%)                  let seat = self.active_player_idx;
   12,026 ( 0.00%)                  if let Some(chosen) = self.armed_attack_mandate_for(seat) {
   48,104 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::armed_attack_mandate_for (1,718x)
        .                               self.attack_mandates.retain(|m| !(m.seat == seat && m.armed));
        .                               for id in chosen {
        .                                   if self
        .                                       .battlefield_find(id)
        .                                       .is_some_and(|c| !c.attacked_this_turn)
        .                                   {
        .                                       self.destroy_permanent(id, false, &mut events);
        .                                   }
-- line 608 ----------------------------------------
-- line 624 ----------------------------------------
        .                                       },
        .                                   )
        .                                   .build(),
        .                               );
        .                           }
        .                           // CR 702.183 — Impending time counters tick at the beginning of
        .                           // the controller's end step; the permanent becomes a creature
        .                           // when the last is gone.
    1,718 ( 0.00%)                  let mut imp = self.process_impending();
  676,386 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::process_impending (1,718x)
        .                           events.append(&mut imp);
        .                           // MKM — solve any Cases whose condition is now met (before end-step
        .                           // triggers so a Case File Auditor sees "whenever you solve a Case").
    5,154 ( 0.00%)                  self.process_case_solves(&mut events);
  412,250 ( 0.01%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::process_case_solves (1,718x)
    5,154 ( 0.00%)                  self.fire_step_triggers(TurnStep::End);
2,526,604 ( 0.08%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (1,718x)
        .                           self.give_priority_to_active();
        .                       }
        .                       TurnStep::Cleanup => {
        .                           // Reset per-turn spell counter and the Gravestorm
        .                           // permanents-died tally. Snapshot the turn's total first so
        .                           // the classic werewolf "no spells cast last turn" check (read
        .                           // at the next upkeep) sees it.
        .                           // CR 514.3 — the cleanup discard has no causing spell or
        .                           // ability, so no earlier resolution may be attributed to it.
    1,718 ( 0.00%)                  self.resolution_causer = None;
    3,436 ( 0.00%)                  self.spells_cast_last_turn = self.spells_cast_this_turn;
    1,718 ( 0.00%)                  self.spells_cast_this_turn = 0;
        .                           self.last_cast_spell_colors.clear();
    1,718 ( 0.00%)                  self.noncreature_spells_cast_this_turn = 0;
    6,872 ( 0.00%)                  self.players[self.active_player_idx].opponent_cast_spell_since_your_turn = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                           for pl in &mut self.players {
   10,308 ( 0.00%)                      pl.spells_cast_this_game_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.sorceries_cast_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.noncreature_spells_cast_this_game_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.nonartifact_spells_cast_this_game_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.multicolored_spells_cast_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)                      pl.spell_names_cast_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)                      pl.spell_ids_cast_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.forage_graveyard_casts_turn = None;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.spells_cast_from_hand_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.oil_activity_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.channel_life_for_mana = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                               // CR 603.7e — unused "your next creature spell this turn"
        .                               // riders expire with the turn.
    6,872 ( 0.00%)                      pl.pending_creature_etb_counters.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)                      pl.pending_creature_etb_keywords.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                               // The Chain Veil's banked activations and the
        .                               // "did you activate a loyalty ability" flag are per-turn.
   10,308 ( 0.00%)                      pl.extra_loyalty_activations = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.activated_loyalty_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)                      pl.tapped_land_for_mana_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                           }
    1,718 ( 0.00%)                  self.mana_spent_on_spells_this_turn = 0;
    1,718 ( 0.00%)                  self.permanents_to_graveyard_this_turn = 0;
        .                           // CR 514.3 — "at the beginning of the next cleanup step"
        .                           // delayed triggers (Waylay) go on the stack here; the
        .                           // `finish_cleanup` check below then grants priority.
    3,436 ( 0.00%)                  self.fire_step_triggers(TurnStep::Cleanup);
2,526,420 ( 0.08%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (1,718x)
        .                           // CR 514.3 — no player receives priority during cleanup
        .                           // unless its turn-based actions fire triggers or SBAs act;
        .                           // run them immediately on entering the step.
    5,154 ( 0.00%)                  match self.do_cleanup(&mut events) {
122,934,524 ( 3.94%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_cleanup (1,718x)
        .                               CleanupOutcome::Suspended | CleanupOutcome::PriorityGranted => {}
        .                               CleanupOutcome::TurnOver => return self.advance_step(events),
        .                           }
        .                       }
        .                       TurnStep::EndCombat => {
        .                           // CR 511.1 — "at end of combat" triggers, including the
        .                           // `DelayedKind::EndOfCombat` queue (Triton Tactics, Fortune).
   12,744 ( 0.00%)                  self.fire_step_triggers(TurnStep::EndCombat);
4,650,748 ( 0.15%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::fire_step_triggers (3,186x)
        .                           self.give_priority_to_active();
        .                       }
        .                       // Master Warcraft — the declaration steps hand priority to the
        .                       // outside chooser so it, not the active/defending player, submits
        .                       // the declaration. Without a chooser these behave like `_`.
    5,228 ( 0.00%)              TurnStep::DeclareAttackers if self.combat_chooser.is_some() => {
        .                           self.priority.player_with_priority =
        .                               self.combat_chooser.unwrap_or(self.active_player_idx);
        .                           self.priority.consecutive_passes = 0;
        .                       }
        .                       TurnStep::DeclareBlockers if self.block_chooser().is_some() => {
        .                           self.priority.player_with_priority =
        .                               self.block_chooser().unwrap_or(self.active_player_idx);
        .                           self.priority.consecutive_passes = 0;
        .                       }
        .                       _ => {
        .                           self.give_priority_to_active();
        .                       }
        .                   }
        .           
  132,648 ( 0.00%)          Ok(events)
  229,896 ( 0.01%)      }
        .           
        .               /// MKM — at the beginning of the active player's end step, each of their
        .               /// unsolved Cases whose "To solve" condition holds becomes solved. Emits a
        .               /// `GameEvent::CaseSolved` per newly-solved Case and dispatches "whenever
        .               /// you solve a Case" triggers (Case File Auditor).
   13,744 ( 0.00%)      pub fn process_case_solves(&mut self, events: &mut Vec<GameEvent>) {
    5,154 ( 0.00%)          let active = self.active_player_idx;
    1,718 ( 0.00%)          let candidates: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
   98,992 ( 0.00%)              .filter(|c| c.controller == active && !c.case_solved && c.definition.case.is_some())
        .                       .map(|c| c.id)
        .                       .collect();
        .                   let mut solved_events = Vec::new();
        .                   for id in candidates {
        .                       let Some(pred) = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
-- line 733 ----------------------------------------
-- line 740 ----------------------------------------
        .                           continue;
        .                       }
        .                       if let Some(card) = self.battlefield.iter_mut().find(|c| c.id == id)
        .                           && card.solve_case()
        .                       {
        .                           solved_events.push(GameEvent::CaseSolved { case: id, controller: active });
        .                       }
        .                   }
    3,436 ( 0.00%)          if !solved_events.is_empty() {
        .                       self.dispatch_triggers_for_events(&solved_events);
        .                       events.extend(solved_events);
        .                   }
   13,744 ( 0.00%)      }
        .           
        .               /// Push step-based triggers onto the stack for the given step.
        .               /// Fires `EventKind::StepBegins(step)` triggers. Scope controls which
        .               /// players' permanents' triggers fire: `ActivePlayer` is default for
        .               /// "at the beginning of your upkeep"; `AnyPlayer` fires for everyone.
        .               /// Also processes any `delayed_triggers` whose kind matches this step
        .               /// (e.g. Pact upkeep cost, Goryo's exile-at-end-step).
  130,158 ( 0.00%)      pub fn fire_step_triggers(&mut self, step: TurnStep) {
   43,386 ( 0.00%)          let active = self.active_player_idx;
        .                   let kind = EventKind::StepBegins(step);
        .                   // Collect candidate (source, effect, controller, filter) tuples for
        .                   // each battlefield permanent's matching trigger. We snapshot the
        .                   // optional `event.filter` predicate alongside the effect so we can
        .                   // re-check it after gathering — predicate evaluation needs
        .                   // `&self.evaluate_predicate(...)` which can't run inside the inner
        .                   // closure due to the `iter` borrow.
        .                   let scope_matches = |scope: &EventScope, controller: usize| match scope {
-- line 769 ----------------------------------------
-- line 781 ----------------------------------------
        .                       | EventScope::OpponentSourceDamagedYou
        .                       | EventScope::YourOtherSourceDamagedOpponent
        .                       | EventScope::YouTapped => false, // event-based
        .                       EventScope::ControllerAttackedByOpponent
        .                       | EventScope::ControllerPlaneswalkerAttackedByOpponent => false, // combat-based
        .                   };
        .                   // One board-level scan for the whole walk: the per-card shim rebuilds
        .                   // it, so asking it per battlefield permanent is O(cards²).
   28,924 ( 0.00%)          let trigger_grants = self.trigger_grant_sources();
6,753,278 ( 0.22%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::trigger_grant_sources (14,462x)
        .                   // Presence gate for the per-card grant walk (the dispatcher's device):
        .                   // on a board that grants nothing it returns empty for every permanent.
   43,386 ( 0.00%)          let any_static_grant = !trigger_grants.is_empty() || !self.turn_granted_triggers.is_empty();
        .                   let mut candidates: Vec<(CardId, Effect, usize, Option<crate::card::Predicate>)> =
        .                       Vec::new();
   14,462 ( 0.00%)          for c in self.battlefield.iter() {
        .                       // Printed triggers plus statics-granted ones (Kataki's "All
        .                       // artifacts have '…upkeep…'"), both firing off `c`. Filter on the
        .                       // borrowed ability and clone only the survivors: this runs on every
        .                       // step of every turn and a `TriggeredAbility` clone is an `Effect`
        .                       // tree plus the event's filter predicate.
   19,376 ( 0.00%)              for t in c.definition.triggered_abilities.iter() {
        .                           if t.event.kind == kind && scope_matches(&t.event.scope, c.controller) {
        .                               candidates.push((c.id, t.effect.clone(), c.controller, t.event.filter.clone()));
        .                           }
        .                       }
1,076,984 ( 0.03%)              if any_static_grant || !c.definition.station.is_empty() {
        .                           for t in self.statics_granted_triggers_with(c, &trigger_grants) {
        .                               if t.event.kind == kind && scope_matches(&t.event.scope, c.controller) {
        .                                   candidates.push((c.id, t.effect, c.controller, t.event.filter));
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // CR 702.6e / 303.4 — step triggers granted to a permanent by an
        .                   // attached Aura/Equipment's `equipped_bonus` fire as though printed on
        .                   // the host ("Enchanted creature has 'At the beginning of your upkeep,
        .                   // you lose 1 life'" — Pillory of the Sleepless). Source is the host
        .                   // (unless `triggers_on_equipment`); "your" scope keys on the host's
        .                   // controller. Combat-damage/dies equip triggers use other kinds and so
        .                   // are untouched here.
        .                   for eq in &self.battlefield {
  538,492 ( 0.02%)              let Some(host_id) = eq.attached_to else { continue };
    9,976 ( 0.00%)              let Some(bonus) = &eq.definition.equipped_bonus else { continue };
    4,988 ( 0.00%)              let Some(host) = self.battlefield.iter().find(|c| c.id == host_id) else { continue };
        .                       for t in &bonus.triggered_abilities {
        .                           if t.event.kind == kind && scope_matches(&t.event.scope, host.controller) {
        .                               let source = if bonus.triggers_on_equipment { eq.id } else { host_id };
        .                               candidates.push((source, t.effect.clone(), host.controller, t.event.filter.clone()));
        .                           }
        .                       }
        .                   }
        .                   // Walk the active player's graveyard for `FromYourGraveyard`
        .                   // step triggers (Ichorid's "at the beginning of your upkeep").
        .                   if let Some(player) = self.players.get(active) {
   14,462 ( 0.00%)              for c in &player.graveyard {
    3,420 ( 0.00%)                  for t in &c.definition.triggered_abilities {
        .                               if t.event.kind == kind
        .                                   && matches!(t.event.scope, EventScope::FromYourGraveyard)
        .                               {
        .                                   candidates.push((c.id, t.effect.clone(), c.owner, t.event.filter.clone()));
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // CR 902.5 — a Vanguard avatar's step triggers fire from the command
        .                   // zone ("at the beginning of your upkeep" — Arcbound Overseer Avatar).
        .                   for (seat, player) in self.players.iter().enumerate() {
   57,848 ( 0.00%)              for c in player
        .                           .command
        .                           .iter()
        .                           .filter(|c| c.command_zone_abilities_active() || c.definition.is_scheme())
        .                       {
        .                           // CR 901.6 — a plane's controller is the planar controller,
        .                           // normally the active player, so its "your" triggers fire on
        .                           // every turn regardless of who owns the planar deck.
        .                           let plane = c.definition.is_plane();
-- line 856 ----------------------------------------
-- line 868 ----------------------------------------
        .                               }
        .                           }
        .                       }
        .                   }
        .                   // CR 114 — step-keyed emblem triggers ("at the beginning of your
        .                   // upkeep, draw a card"). "Your" scope fires only on the emblem
        .                   // owner's step; `AnyPlayer` fires for every player's step.
        .                   for (seat, player) in self.players.iter().enumerate() {
   28,924 ( 0.00%)              for em in &player.emblems {
        .                           for t in &em.triggered {
        .                               let scoped_to_owner = matches!(
        .                                   t.event.scope,
        .                                   EventScope::YourControl | EventScope::ActivePlayer | EventScope::SelfSource
        .                               );
        .                               if t.event.kind == kind
        .                                   && (matches!(t.event.scope, EventScope::AnyPlayer)
        .                                       || (scoped_to_owner && seat == active))
-- line 884 ----------------------------------------
-- line 900 ----------------------------------------
        .                   // ability resolves — is now also wired (see
        .                   // `triggers_with_filter` below + the resolver's `intervening_if`
        .                   // branch).
        .                   // Single filter pass that keeps both halves of CR 603.4 alive: drop
        .                   // triggers whose intervening-if predicate is false right now (the
        .                   // trigger-time check), and preserve the predicate on the survivors
        .                   // so the resolver can re-check at resolution time.
        .                   let triggers_with_filter: Vec<(CardId, Effect, usize, Option<crate::card::Predicate>)> =
   43,386 ( 0.00%)              candidates
        .                           .into_iter()
        .                           .filter(|(src, _eff, ctrl, filter)| {
        .                               let Some(pred) = filter else { return true };
        .                               let ctx = crate::game::effects::EffectContext::for_trigger(
        .                                   *src, *ctrl, None, 0,
        .                               );
        .                               self.evaluate_predicate(pred, &ctx)
        .                           })
-- line 916 ----------------------------------------
-- line 924 ----------------------------------------
        .                       CardId,
        .                       Effect,
        .                       usize,
        .                       Option<Target>,
        .                       Option<crate::game::effects::EntityRef>,
        .                   );
        .                   let mut delayed_to_fire: Vec<DelayedFire> = Vec::new();
        .                   let mut keep: Vec<DelayedTrigger> = Vec::new();
   72,310 ( 0.00%)          for dt in std::mem::take(&mut self.delayed_triggers) {
        .                       let matches = match (&dt.kind, step) {
        .                           (DelayedKind::YourNextUpkeep, TurnStep::Upkeep) => dt.controller == active,
        .                           (DelayedKind::EachPlayersUpkeep, TurnStep::Upkeep) => true,
        .                           (DelayedKind::NextUpkeep { after_turn }, TurnStep::Upkeep) => {
        .                               self.turn_number > *after_turn
        .                           }
        .                           (DelayedKind::YourNextMainPhase, TurnStep::PreCombatMain) => {
        .                               dt.controller == active
-- line 940 ----------------------------------------
-- line 966 ----------------------------------------
        .                           ));
        .                           if !dt.fires_once {
        .                               keep.push(dt);
        .                           }
        .                       } else {
        .                           keep.push(dt);
        .                       }
        .                   }
  115,696 ( 0.00%)          self.delayed_triggers = keep;
        .           
        .                   // Build a single queue (delayed triggers first, then step
        .                   // triggers; APNAP-sorted below) so `drain_trigger_queue` can surface
        .                   // `Decision::ChooseTarget` for wants_ui controllers instead of
        .                   // silently auto-targeting them.
        .                   let mut queue: Vec<PendingTriggerPush> = Vec::new();
  115,696 ( 0.00%)          for (source, effect, controller, captured_target, subject) in delayed_to_fire {
        .                       let mode = self.pick_trigger_mode(&effect, source, controller);
        .                       // Delayed triggers may have captured a target at registration
        .                       // time (e.g. Pact's "lose the game"). If so, push immediately
        .                       // with that target — we already passed the targeting moment.
        .                       if captured_target.is_some() {
        .                           self.push_pending_trigger(
        .                               PendingTriggerPush {
        .                                   from_mana_ability: false,
-- line 989 ----------------------------------------
-- line 1007 ----------------------------------------
        .                           controller,
        .                           effect,
        .                           subject,
        .                           event_amount: 0,
        .                           mode,
        .                           intervening_if: None,
        .                       });
        .                   }
  144,620 ( 0.00%)          for (source, effect, controller, intervening_if) in triggers_with_filter {
        .                       let mode = self.pick_trigger_mode(&effect, source, controller);
        .                       queue.push(PendingTriggerPush {
        .                           from_mana_ability: false,
        .                               actor: None,
        .                           source,
        .                           controller,
        .                           effect,
        .                           subject: None,
-- line 1023 ----------------------------------------
-- line 1025 ----------------------------------------
        .                           mode,
        .                           intervening_if,
        .                       });
        .                   }
        .                   // CR 603.3b — APNAP order: the active player's triggers push first
        .                   // (resolving last). Battlefield-Vec order is otherwise preserved as
        .                   // each controller's chosen same-controller order (stable sort).
        .                   let n_players = self.players.len();
   43,386 ( 0.00%)          let apnap_rank = |seat: usize| -> usize {
        .                       (seat + n_players - active) % n_players.max(1)
        .                   };
        .                   queue.sort_by_key(|t| apnap_rank(t.controller));
  101,234 ( 0.00%)          self.drain_trigger_queue(queue);
  838,796 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::drain_trigger_queue (14,462x)
  115,696 ( 0.00%)      }
        .           
        .               /// CR 714 — put a lore counter on a Saga and fire the chapter ability/ies
        .               /// for the new lore-counter total. Called on ETB (chapter I) and as a
        .               /// turn-based action at the start of the controller's precombat main.
        .               /// CR 714.2b — a Saga entering the battlefield gets its first lore
        .               /// counter. CR 702.155 (Read Ahead): a read-ahead Saga instead enters with
        .               /// a chosen number of lore counters (1..final chapter), firing only the
        .               /// chosen chapter. Non-read-ahead Sagas defer to [`saga_advance`].
-- line 1046 ----------------------------------------
-- line 1210 ----------------------------------------
        .                       .filter(|c| c.definition.is_plane() || c.definition.is_phenomenon())
        .                       .map(|c| c.id)
        .                       .collect()
        .               }
        .           
        .               /// CR 901.6 — the planar controller: normally the active player, but the
        .               /// owner of the face-up plane when the active player has no planar deck of
        .               /// their own (a two-player game where only one seat brought one).
  366,324 ( 0.01%)      pub fn planar_controller(&self) -> usize {
  156,996 ( 0.01%)          if !self.players[self.active_player_idx].planar_deck.is_empty()
        .                       || self.players[self.active_player_idx]
        .                           .command
        .                           .iter()
        .                           .any(|c| c.definition.is_plane() || c.definition.is_phenomenon())
        .                   {
        .                       return self.active_player_idx;
        .                   }
        .                   self.players
        .                       .iter()
        .                       .position(|p| {
  209,328 ( 0.01%)                  !p.planar_deck.is_empty()
        .                               || p.command
        .                                   .iter()
        .                                   .any(|c| c.definition.is_plane() || c.definition.is_phenomenon())
        .                       })
        .                       .unwrap_or(self.active_player_idx)
  470,988 ( 0.02%)      }
        .           
        .               /// CR 901.5 — turn the top of `seat`'s planar deck face up as the starting
        .               /// plane, skipping phenomena to the bottom. No abilities trigger.
        .               pub fn set_starting_plane(&mut self, seat: usize) -> Option<CardId> {
        .                   for _ in 0..self.players[seat].planar_deck.len() {
        .                       let card = self.players[seat].planar_deck.remove(0);
        .                       if card.definition.is_plane() {
        .                           let id = card.id;
-- line 1244 ----------------------------------------
-- line 1344 ----------------------------------------
        .               pub(crate) fn sweep_finished_phenomena(&mut self) {
        .                   let phenomena: Vec<CardId> = self
        .                       .players
        .                       .iter()
        .                       .flat_map(|p| p.command.iter())
        .                       .filter(|c| c.definition.is_phenomenon())
        .                       .map(|c| c.id)
        .                       .collect();
   21,340 ( 0.00%)          if phenomena.is_empty()
        .                       || self.stack.iter().any(|item| {
        .                           matches!(item, StackItem::Trigger { source, .. } if phenomena.contains(source))
        .                       })
        .                   {
        .                       return;
        .                   }
        .                   let seat = self.planar_controller();
        .                   self.planeswalk(seat);
-- line 1360 ----------------------------------------
-- line 1370 ----------------------------------------
        .                           return;
        .                       }
        .                   }
        .               }
        .           
        .               /// CR 904.10 — a face-up non-ongoing scheme is abandoned the next time a
        .               /// player would receive priority, once no scheme trigger is on the stack.
        .               pub(crate) fn sweep_finished_schemes(&mut self) {
   10,670 ( 0.00%)          if self.stack.iter().any(|item| self.stack_item_is_scheme_trigger(item)) {
        .                       return;
        .                   }
        .                   let done: Vec<CardId> = self
        .                       .players
        .                       .iter()
        .                       .flat_map(|p| p.command.iter())
        .                       .filter(|c| c.definition.is_scheme() && !c.definition.is_ongoing_scheme())
        .                       .map(|c| c.id)
        .                       .collect();
   10,670 ( 0.00%)          for id in done {
        .                       self.abandon_scheme(id);
        .                   }
        .               }
        .           
        .               /// Is this stack item a triggered ability whose source is a face-up scheme?
        .               fn stack_item_is_scheme_trigger(&self, item: &StackItem) -> bool {
      960 ( 0.00%)          let StackItem::Trigger { source, .. } = item else { return false };
        .                   self.players
        .                       .iter()
        .                       .flat_map(|p| p.command.iter())
        .                       .any(|c| c.id == *source && c.definition.is_scheme())
        .               }
        .           
        .               /// CR 714.2b — "when one or more lore counters are put onto this Saga, if
        .               /// the number of lore counters on it was less than N and became at least
-- line 1403 ----------------------------------------
-- line 1462 ----------------------------------------
        .               /// trace. Non-free abilities always pass and clear the watch: paying a
        .               /// cost is itself a game choice that moves the state.
        .               pub(crate) fn check_free_activation_loop(
        .                   &mut self,
        .                   card_id: CardId,
        .                   ability_index: usize,
        .               ) -> Result<(), GameError> {
        .                   let free = self
   55,158 ( 0.00%)              .find_card_anywhere(card_id)
  815,500 ( 0.03%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (18,386x)
        .                       .and_then(|c| c.definition.activated_abilities.get(ability_index))
   18,386 ( 0.00%)              .is_some_and(|a| a.is_free());
25,656,976 ( 0.82%)  => /home/user/Crabomination/crabomination_base/src/effect/abilities.rs:crabomination_base::effect::abilities::ActivatedAbility::is_free (18,386x)
   36,772 ( 0.00%)          if !free {
   55,158 ( 0.00%)              self.free_activation_watch = (0, None, 0);
        .                       return Ok(());
        .                   }
        .                   let fp = self.loop_fingerprint();
        .                   let key = Some((card_id, ability_index));
        .                   if self.free_activation_watch.0 == fp && self.free_activation_watch.1 == key {
        .                       if self.free_activation_watch.2 >= Self::FREE_ACTIVATION_REPEAT_CAP {
        .                           return Err(GameError::LoopMustBeBroken);
        .                       }
        .                       self.free_activation_watch.2 += 1;
        .                   } else {
        .                       self.free_activation_watch = (fp, key, 1);
        .                   }
        .                   Ok(())
        .               }
        .           
   37,116 ( 0.00%)      pub fn resolve_top_of_stack(&mut self) -> Result<Vec<GameEvent>, GameError> {
        .                   // A spell resolution consumed a cast, so only abilities can loop with
        .                   // nothing spent: every triggered ability, plus activations whose cost
        .                   // line is empty (`{0}:` with no tap/sac/cap — `ActivatedAbility::is_free`).
   14,436 ( 0.00%)          let was_trigger = match self.stack.last() {
        .                       Some(StackItem::Trigger { activated: false, .. }) => true,
        .                       Some(StackItem::Trigger { source, activated: true, .. }) => self
       24 ( 0.00%)                  .find_card_anywhere(*source)
    1,204 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination::game::GameState::find_card_anywhere (8x)
       32 ( 0.00%)                  .is_some_and(|c| c.definition.activated_abilities.iter().any(|a| a.is_free())),
    9,752 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/abilities.rs:crabomination_base::effect::abilities::ActivatedAbility::is_free (8x)
        .                       _ => false,
        .                   };
   24,744 ( 0.00%)          let mut events = self.resolve_top_of_stack_inner()?;
144,063,225 ( 4.62%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::resolve_top_of_stack_inner (4,124x)
    8,756 ( 0.00%)          if was_trigger && self.game_over.is_none() {
    1,016 ( 0.00%)              let fp = self.loop_fingerprint();
1,840,264 ( 0.06%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::loop_fingerprint (508x)
    1,016 ( 0.00%)              if fp == self.mandatory_loop_watch.0 {
        .                           self.mandatory_loop_watch.1 += 1;
        .                           if self.mandatory_loop_watch.1 >= Self::MANDATORY_LOOP_DRAW_REPEATS {
        .                               self.game_over = Some(None);
        .                               events.push(GameEvent::GameOver { winner: None });
        .                           }
        .                       } else {
    1,016 ( 0.00%)                  self.mandatory_loop_watch = (fp, 0);
        .                       }
        .                   } else {
    3,616 ( 0.00%)              self.mandatory_loop_watch = (0, 0);
        .                   }
   20,620 ( 0.00%)          Ok(events)
   37,116 ( 0.00%)      }
        .           
        .               /// Cheap game-state digest for the CR 104.4b loop watchdog: everything a
        .               /// progressing trigger chain would move (life, zone sizes, counters, the
        .               /// stack's shape). Deliberately coarse — a false *negative* just means the
        .               /// draw isn't detected, while a false positive would end a live game.
    4,572 ( 0.00%)      fn loop_fingerprint(&self) -> u64 {
        .                   use std::hash::{Hash, Hasher};
        .                   let mut h = std::collections::hash_map::DefaultHasher::new();
        .                   self.turn_number.hash(&mut h);
        .                   self.stack.len().hash(&mut h);
        .                   for p in &self.players {
        .                       p.life.hash(&mut h);
        .                       p.hand.len().hash(&mut h);
        .                       p.library.len().hash(&mut h);
-- line 1531 ----------------------------------------
-- line 1536 ----------------------------------------
        .                   self.battlefield.len().hash(&mut h);
        .                   for c in &self.battlefield {
        .                       c.id.hash(&mut h);
        .                       c.tapped.hash(&mut h);
        .                       c.damage.hash(&mut h);
        .                       c.counters.values().sum::<u32>().hash(&mut h);
        .                   }
        .                   h.finish()
    4,064 ( 0.00%)      }
        .           
   70,108 ( 0.00%)      fn resolve_top_of_stack_inner(&mut self) -> Result<Vec<GameEvent>, GameError> {
    4,124 ( 0.00%)          let Some(item) = self.stack.pop() else {
        .                       return Ok(vec![]);
        .                   };
        .                   let mut events = vec![];
        .           
   16,496 ( 0.00%)          match item {
        .                       StackItem::Spell {
    3,608 ( 0.00%)                  card,
    7,216 ( 0.00%)                  caster,
    7,216 ( 0.00%)                  target,
   18,040 ( 0.00%)                  additional_targets,
    7,216 ( 0.00%)                  mode,
    7,216 ( 0.00%)                  x_value,
    7,216 ( 0.00%)                  converged_value,
    7,216 ( 0.00%)                  mana_spent,
        .                           uncounterable: _,
        .                       } => {
    7,216 ( 0.00%)                  let card = *card;
    3,608 ( 0.00%)                  let card_id = card.id;
        .                           // CR 706 — stash the copy-relevant fields before anything else
        .                           // runs; a "copy this spell" rider resolves after the stack
        .                           // entry is already popped (the Chain cycle).
    7,216 ( 0.00%)                  self.resolving_spell_snapshot =
   61,336 ( 0.00%)                      Some(Box::new(crate::game::ResolvingSpell {
        .                                   definition: card.definition.clone(),
        .                                   target: target.clone(),
        .                                   additional_targets: additional_targets.clone(),
        .                                   mode,
        .                                   x_value,
        .                                   converged_value,
        .                               }));
        .           
        .                           // CR 702.140 — a creature with mutate merges onto its host
        .                           // permanent instead of entering on its own. If the host is no
        .                           // longer a legal mutate target it enters as a normal creature
        .                           // (CR 702.140i), so we only divert when the host is legal.
   14,432 ( 0.00%)                  if let Some((host_id, on_top)) = card.mutate_onto {
        .                               let legal_host = self.battlefield.iter().any(|c| {
        .                                   c.id == host_id
        .                                       && c.controller == caster
        .                                       && c.definition.is_creature()
        .                                       && !c
        .                                           .definition
        .                                           .has_creature_type(crate::card::CreatureType::Human)
        .                               });
-- line 1591 ----------------------------------------
-- line 1634 ----------------------------------------
        .                                   return Ok(events);
        .                               }
        .                           }
        .           
        .                           // CR 715 / 702.183 — while cast as its Adventure/Omen half the
        .                           // card is its instant/sorcery half, so it resolves down the
        .                           // spell path (not onto the battlefield) regardless of its
        .                           // creature card type.
   10,824 ( 0.00%)                  let is_noncreature = card.casting_alt_half() || !card.definition.is_creature();
   51,896 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,608x)
        .           
        .                           // CR 608.2b — an Aura spell re-checks its enchant target as
        .                           // it tries to resolve; if the target is illegal (gone,
        .                           // filter mismatch, granted Hexproof/Shroud) the spell
        .                           // doesn't resolve — countered into its owner's graveyard,
        .                           // never entering the battlefield (no ETB). Bestowed casts
        .                           // are exempt: CR 702.103e resolves them as the creature.
   10,824 ( 0.00%)                  if card.definition.is_aura()
   33,162 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_aura (3,608x)
      460 ( 0.00%)                      && !card.bestowed
      690 ( 0.00%)                      && !card.casting_alt_half()
        .                               && let Some(t) = &target
        .                           {
      460 ( 0.00%)                      let gone = matches!(t, Target::Permanent(tid)
      230 ( 0.00%)                          if self.battlefield_find(*tid).is_none());
      230 ( 0.00%)                      let filter_fail = card
        .                                   .definition
        .                                   .effect
    1,150 ( 0.00%)                          .target_filter_for_slot_in_mode_kicked(0, mode, card.kicked)
   22,770 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::target_filter_for_slot_in_mode_kicked (230x)
        .                                   .is_some_and(|f| {
    2,300 ( 0.00%)                              !self.evaluate_requirement_static(f, t, caster, Some(card.id))
1,153,739 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/effects/eval.rs:crabomination::game::effects::eval::<impl crabomination::game::GameState>::evaluate_requirement_static (230x)
        .                                   });
        .                               let untargetable = self
    1,610 ( 0.00%)                          .check_target_legality_with_source(t, caster, Some(card.id))
1,340,686 ( 0.04%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::check_target_legality_with_source (230x)
        .                                   .is_err();
      920 ( 0.00%)                      if gone || filter_fail || untargetable {
        .                                   if !card.is_token {
        .                                       self.route_to_graveyard(card, &mut events);
        .                                   }
        .                                   return Ok(events);
        .                               }
        .                           }
        .           
   15,172 ( 0.00%)                  if card.definition.is_permanent() && !card.casting_alt_half() {
   51,206 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_permanent (3,608x)
        .                               // Collect ETB triggers before moving card into battlefield.
        .                               // `mut` so the enters-as-copy path can swap in the
        .                               // copied object's ETB triggers (CR 707.5).
        .                               // Carry each self-ETB trigger's intervening-`if`
        .                               // (`event.filter`) so it can be re-evaluated once the card
        .                               // is on the battlefield (CR 603.4) — the inline resolution
        .                               // path used to drop it, firing filtered self-ETB triggers
        .                               // unconditionally (Corrupted / kicker-gated ETBs).
        .                               let mut etb_triggers: Vec<(Effect, Option<crate::card::Predicate>)> = card
        .                                   .definition
        .                                   .triggered_abilities
        .                                   .iter()
        .                                   .filter(|t| t.event.kind == EventKind::EntersBattlefield
       78 ( 0.00%)                              && matches!(t.event.scope, EventScope::SelfSource))
      624 ( 0.00%)                          .map(|t| (t.effect.clone(), t.event.filter.clone()))
    8,190 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (78x)
    6,006 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (78x)
        .                                   .collect();
    2,174 ( 0.00%)                      let evoked = card.evoked;
    2,174 ( 0.00%)                      let dashed = card.dashed;
        .                               // CR 614.12 — capture the "enters with N counters"
        .                               // replacement before the card moves to the battlefield;
        .                               // we apply the counters immediately after pushing,
        .                               // BEFORE the next state-based-action sweep, so a printed
        .                               // 0/0 body (Pterafractyl, Symmathematics) survives ETB.
        .                               let enters_spec = card.definition.enters_with_counters.clone();
        .                               // CR 614.12 — Patched Plaything's "enters with two -1/-1
        .                               // counters if you cast it from your hand" reads the cast
        .                               // zone via `Predicate::CastFromHand`.
    4,348 ( 0.00%)                      let cast_from_hand = card.cast_from_hand;
    2,174 ( 0.00%)                      let mut card = card;
        .                               // CR 608.3a — a permanent spell enters under the control
        .                               // of its caster (matters for casts of opponent-owned
        .                               // cards: Gonti, Hostage Taker).
    2,174 ( 0.00%)                      card.controller = caster;
    4,348 ( 0.00%)                      card.controller = self.apply_etb_control_replacement(&card, card.controller);
        .                               // Stamp the cast cost so ETB riders can read it after the
        .                               // spell leaves the stack (Astelli Reclaimer's MV-≤-X return).
    2,174 ( 0.00%)                      card.cast_mana_spent = mana_spent;
        .                               // Stamp the cast's X so ETB *triggered* abilities can read
        .                               // it (Dune Drifter's MV-≤-X graveyard return).
    2,174 ( 0.00%)                      card.cast_x_value = x_value;
        .                               // CR 702.150c — a Compleated planeswalker cast with life
        .                               // enters with two fewer loyalty per pip paid with life.
    4,348 ( 0.00%)                      if card.compleated_life_paid > 0 && card.definition.is_planeswalker() {
        .                                   let loyalty = card
        .                                       .definition
        .                                       .base_loyalty
        .                                       .saturating_sub(card.compleated_life_paid);
        .                                   card.compleated_life_paid = 0;
        .                                   if loyalty > 0 {
        .                                       card.counters
        .                                           .insert(crate::card::CounterType::Loyalty, loyalty);
-- line 1726 ----------------------------------------
-- line 1731 ----------------------------------------
        .                               let room_door = card.definition.room.as_ref().map(|_| {
        .                                   usize::from(card.split_cast == Some(1))
        .                               });
        .                               // `Effect::CopySpellWithRiders` — the copy's stamped
        .                               // riders apply as it resolves into a permanent.
        .                               let resolve_riders = card.resolve_riders.take();
        .                               // This permanent entered because its spell was cast (CR
        .                               // 400.7 new object) — powers "if you cast it" ETB gates.
    4,348 ( 0.00%)                      card.entered_by_cast = true;
    8,696 ( 0.00%)                      self.battlefield.push(card);
    6,522 ( 0.00%)                      if let Some((grant_haste, sacrifice_eot)) = resolve_riders {
        .                                   if grant_haste {
        .                                       self.grant_keyword_eot(card_id, crate::card::Keyword::Haste);
        .                                   }
        .                                   if sacrifice_eot {
        .                                       self.delayed_triggers.push(crate::game::types::DelayedTrigger {
        .                                           controller: caster,
        .                                           source: card_id,
        .                                           kind: crate::game::types::DelayedKind::NextEndStep,
-- line 1749 ----------------------------------------
-- line 1754 ----------------------------------------
        .                                           fires_once: true,
        .                                           expires_after_turn: None,
        .                                       });
        .                                   }
        .                               }
        .                               // CR 310.6 — a cast Siege's controller chooses an opponent
        .                               // to protect it (the lone opponent in 2-player; multiplayer
        .                               // choice is a follow-up).
    2,174 ( 0.00%)                      if let Some(c) = self.battlefield.iter().find(|c| c.id == card_id)
    6,522 ( 0.00%)                          && c.definition.is_battle()
   34,784 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_battle (2,174x)
        .                                   && c.protected_by.is_none()
        .                               {
        .                                   let ctrl = c.controller;
        .                                   let protector = (0..self.players.len())
        .                                       .find(|&pl| pl != ctrl && self.players[pl].is_alive());
        .                                   if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id) {
        .                                       c.protected_by = protector;
        .                                   }
        .                               }
        .                               // CR 614 — control-setting and enters-tapped replacements
        .                               // (Captive Audience; Imposing Sovereign, Urabrask) apply to
        .                               // cast permanents too.
    6,522 ( 0.00%)                      self.apply_enters_under_opponent_control(card_id);
  269,452 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_under_opponent_control (2,174x)
    6,522 ( 0.00%)                      self.apply_enters_tapped_replacement(card_id);
  863,348 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::apply_enters_tapped_replacement (2,174x)
        .                               // CR 709.5d — a Room enters with the cast door unlocked
        .                               // (709.5h: its unlock trigger fires).
    2,174 ( 0.00%)                      if let Some(door) = room_door {
        .                                   self.set_room_door_unlocked(card_id, door == 1, &mut events);
        .                               }
        .                               // CR 614 — "As this permanent enters, [effect]" (Ixidron).
        .                               // Before the first SBA sweep so a `*/*` body sized off the
        .                               // effect never dies as a 0/0, and before the counter specs
        .                               // below so an enters-with count can read what it did
        .                               // (Mimeoplasm's three counters per card it exiled).
    6,522 ( 0.00%)                      self.apply_as_enters_effect(card_id);
  256,408 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::apply_as_enters_effect (2,174x)
        .                               // Collect the printed `enters_with_counters` spec and
        .                               // any active `ExtraEtbCountersForCreatureCasts` static
        .                               // effects controlled by the caster. The static fires
        .                               // only for creature spells (we gate on the resolving
        .                               // card's type).
    2,174 ( 0.00%)                      let is_creature_resolve = self
        .                                   .battlefield
        .                                   .iter()
        .                                   .find(|c| c.id == card_id)
    2,174 ( 0.00%)                          .map(|c| c.definition.is_creature())
   28,952 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (2,174x)
        .                                   .unwrap_or(false);
        .                               let mut counter_specs: Vec<(crate::card::CounterType, crate::effect::Value)> =
        .                                   Vec::new();
   10,870 ( 0.00%)                      if let Some(spec) = enters_spec {
        .                                   counter_specs.push(spec);
        .                               }
    4,348 ( 0.00%)                      if is_creature_resolve {
        .                                   for src in &self.battlefield {
   61,956 ( 0.00%)                              if src.controller != caster {
        .                                           continue;
        .                                       }
        .                                       for sa in &src.definition.static_abilities {
        .                                           if let crate::effect::StaticEffect::ExtraEtbCountersForCreatureCasts {
        .                                               kind,
        .                                               value,
        .                                           } = &sa.effect
        .                                           {
-- line 1815 ----------------------------------------
-- line 1817 ----------------------------------------
        .                                           }
        .                                       }
        .                                   }
        .                               }
        .                               // Metallic Mimic-style chosen-type ETB counters (any matching
        .                               // creature entry the caster controls) — plus Oath of
        .                               // Gideon's loyalty rider, which is the planeswalker case
        .                               // the same collector serves.
      230 ( 0.00%)                      if is_creature_resolve || self.battlefield.iter().any(|c| {
    4,740 ( 0.00%)                          c.id == card_id && c.definition.is_planeswalker()
    3,680 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_planeswalker (230x)
        .                               }) {
    1,944 ( 0.00%)                          for (kind, n) in self.chosen_type_etb_counter_specs(card_id, caster) {
1,899,616 ( 0.06%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::chosen_type_etb_counter_specs (1,944x)
        .                                       counter_specs.push((kind, crate::effect::Value::Const(n as i32)));
        .                                   }
        .                               }
        .                               // CR 603.7e — one-shot "your next creature spell enters
        .                               // with N counters / these keywords" riders (FIN "Summon"
        .                               // saga chapters — Fenrir II counters, Brynhildr Gestalt haste).
    8,696 ( 0.00%)                      if is_creature_resolve {
        .                                   for (kind, n) in
    1,944 ( 0.00%)                              std::mem::take(&mut self.players[caster].pending_creature_etb_counters)
  928,003 ( 0.03%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,944x)
        .                                   {
        .                                       counter_specs.push((kind, crate::effect::Value::Const(n as i32)));
        .                                   }
        .                                   let kws =
    1,944 ( 0.00%)                              std::mem::take(&mut self.players[caster].pending_creature_etb_keywords);
   58,320 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,944x)
    7,776 ( 0.00%)                          for kw in kws {
        .                                       self.grant_keyword_eot(card_id, kw);
        .                                   }
        .                               }
        .                               // Cast-time ETB-counter riders stamped on the instance
        .                               // (Noctis's graveyard-cast finality counter).
        .                               if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id) {
        .                                   for (kind, n) in std::mem::take(&mut c.pending_etb_counters) {
        .                                       counter_specs.push((kind, crate::effect::Value::Const(n as i32)));
        .                                   }
        .                               }
        .                               // CR 702.44 — Sunburst: one counter per color of mana
        .                               // spent (a +1/+1 counter when it enters as a creature, a
        .                               // charge counter otherwise). The cast's converge count is
        .                               // exactly "colors of mana spent".
    4,348 ( 0.00%)                      if converged_value > 0
    2,174 ( 0.00%)                          && self.battlefield.iter().any(|c| {
   35,488 ( 0.00%)                              c.id == card_id
        .                                           && c.definition.keywords.contains(&crate::card::Keyword::Sunburst)
        .                                   })
        .                               {
        .                                   let kind = if is_creature_resolve {
        .                                       crate::card::CounterType::PlusOnePlusOne
        .                                   } else {
        .                                       crate::card::CounterType::Charge
        .                                   };
        .                                   counter_specs
        .                                       .push((kind, crate::effect::Value::Const(converged_value as i32)));
        .                               }
        .                               // CR 122.1 — Solemnity drops enters-with-counters.
        .                               if self.counters_locked() { counter_specs.clear(); }
   17,392 ( 0.00%)                      for (kind, value) in counter_specs {
        .                                   let mut etb_ctx = crate::game::effects::EffectContext::for_spell_with_source(
        .                                       card_id,
        .                                       self.battlefield
        .                                           .iter()
        .                                           .find(|c| c.id == card_id)
        .                                           .map(|c| c.definition.name)
        .                                           .unwrap_or(""),
        .                                       caster,
-- line 1882 ----------------------------------------
-- line 1913 ----------------------------------------
        .                                       events.push(GameEvent::CounterAdded {
        .                                           card_id,
        .                                           counter_type: kind,
        .                                           count: n,
        .                                       });
        .                                   }
        .                               }
        .                               // CR 702.32 / 702.62 — Fading / Vanishing enter-with-counters.
    2,174 ( 0.00%)                      self.apply_fading_vanishing_etb(card_id, &mut events);
  300,270 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::apply_fading_vanishing_etb (2,174x)
        .                               // CR 701.28 — a spell cast converted (More Than Meets the
        .                               // Eye) enters with its back face up.
        .                               self.apply_cast_converted_etb(card_id, &mut events);
        .                               // CR 702.183 — Impending: a permanent cast for its impending
        .                               // cost enters with N time counters (and isn't a creature
        .                               // until they tick off).
        .                               self.apply_impending_etb(card_id, &mut events);
        .                               // CR 614 — "As this enters, it becomes your choice of …"
-- line 1929 ----------------------------------------
-- line 1932 ----------------------------------------
        .                               self.apply_enters_as_choice(card_id);
        .                               // CR 614 — "As this enters, choose [mode A] or [mode B]"
        .                               // (the Tarkir Siege cycle). Bakes the chosen mode's
        .                               // abilities onto the permanent as it enters.
        .                               self.apply_enters_mode_choice(card_id);
        .                               // CR 707 — "enters as a copy of [filter]" replacement.
        .                               // Applied here, before the first SBA sweep, so a 0/0
        .                               // copier (Clone, Phantasmal Image) never dies as a 0/0.
   13,044 ( 0.00%)                      if self.apply_enters_as_copy(card_id, caster, &mut events) {
  278,148 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/effects/mod.rs:crabomination::game::effects::<impl crabomination::game::GameState>::apply_enters_as_copy (2,174x)
        .                                   // CR 616.1c/616.1g — the copy replacement outranks the
        .                                   // enters-tapped one, so re-decide tappedness against the
        .                                   // copied characteristics.
        .                                   self.reapply_enters_tapped_after_copy(card_id);
        .                                   // CR 707.5 — the copy's own ETB triggers fire. The
        .                                   // list collected above was the copier's (usually
        .                                   // empty); re-read it from the post-copy definition.
        .                                   etb_triggers = self
-- line 1948 ----------------------------------------
-- line 1961 ----------------------------------------
        .                                       .unwrap_or_default();
        .                               }
        .           
        .                               // Statics-granted ETBs ("Slivers you control have 'When
        .                               // this enters…'" — Lavabelly) fire as though printed;
        .                               // gathered now that the entrant is on the battlefield.
        .                               if let Some(c) = self.battlefield_find(card_id) {
        .                                   etb_triggers.extend(
    2,174 ( 0.00%)                              self.statics_granted_triggers_for(c)
1,064,826 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::statics_granted_triggers_for (2,174x)
        .                                           .into_iter()
        .                                           .filter(|t| {
        .                                               t.event.kind == EventKind::EntersBattlefield
        .                                                   && matches!(t.event.scope, EventScope::SelfSource)
        .                                           })
        .                                           .map(|t| (t.effect, t.event.filter)),
        .                                   );
        .                               }
        .           
    4,348 ( 0.00%)                      events.push(GameEvent::PermanentEntered { card_id });
        .           
        .                               // CR 702.165 — a permanent spell cast with its Gift promised
        .                               // gives the gift as it enters. Emit once for "whenever you
        .                               // give a gift" payoffs (Jolly Gerbils).
   17,392 ( 0.00%)                      if let Some(gifted) = self.battlefield_find(card_id).and_then(|c| {
        .                                   c.gift_promised
        .                                       .then(|| c.definition.gift.as_ref().map(|g| g.gifted_effect.clone()))
        .                                       .flatten()
    8,696 ( 0.00%)                      }) {
        .                                   events.push(GameEvent::GiftGiven { player: caster });
        .                                   // The gifted half of a permanent's Gift ("when it
        .                                   // enters, they create …, then attach it") resolves as
        .                                   // it enters, with the permanent as the source.
        .                                   let mut ctx = crate::game::effects::EffectContext::for_spell(
        .                                       caster, target.clone(), 0, 0,
        .                                   );
        .                                   ctx.source = Some(card_id);
        .                                   if let Ok(mut sub) = self.resolve_effect(&gifted, &ctx) {
        .                                       events.append(&mut sub);
        .                                   }
        .                               }
        .           
        .                               // CR 702.146e — a daybound permanent entering while it's
        .                               // neither day nor night makes it day.
    2,174 ( 0.00%)                      if self.day_night.is_none()
        .                                   && self
        .                                       .battlefield_find(card_id)
        .                                       .is_some_and(|c| c.definition.keywords.contains(&Keyword::Daybound))
        .                               {
        .                                   self.set_day_night(crate::game::types::DayNight::Day, &mut events);
        .                               }
        .           
        .                               // CR 303.4f / 303.4h — an Aura permanent spell enters
-- line 2012 ----------------------------------------
-- line 2015 ----------------------------------------
        .                               // Aura's `equipped_bonus` (P/T via layer 7c, keywords
        .                               // via layer 6) flow onto the enchanted creature, and the
        .                               // stale-link SBA in this file moves the Aura to the
        .                               // graveyard if its host ever leaves.
        .                               // Also attaches a bestowed enchantment-creature (CR
        .                               // 702.103) cast as an Aura, even though its printed type
        .                               // line isn't an Aura.
        .                               // CR 303.4a — "enchant player" Auras anchor to a seat.
    4,808 ( 0.00%)                      if let Some(crate::game::types::Target::Player(seat)) = target
        .                                   && let Some(aura) =
        .                                       self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                                   && aura.definition.is_aura()
        .                               {
        .                                   aura.attached_to_player = Some(seat);
        .                               }
    2,174 ( 0.00%)                      if self
        .                                   .battlefield
        .                                   .iter()
   42,010 ( 0.00%)                          .any(|c| c.id == card_id && (c.definition.is_aura() || c.bestowed))
   20,256 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_aura (2,174x)
      920 ( 0.00%)                          && let Some(crate::game::types::Target::Permanent(tid)) = target
      230 ( 0.00%)                          && self.battlefield.iter().any(|c| c.id == tid)
        .                                   && let Some(aura) =
        .                                       self.battlefield.iter_mut().find(|c| c.id == card_id)
        .                               {
      690 ( 0.00%)                          aura.attached_to = Some(tid);
        .                                   // CR 303.4 — fire "an Aura you control became attached"
        .                                   // triggers (Siona). Only for true Auras, not bestowed
        .                                   // creature spells (which entered as creatures).
    5,660 ( 0.00%)                          if self.battlefield.iter().any(|c| c.id == card_id && c.definition.is_aura())
   44,360 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs:<core::slice::iter::Iter<T> as core::iter::traits::iterator::Iterator>::any (230x)
        .                                   {
    1,380 ( 0.00%)                              events.push(GameEvent::AuraAttached { aura: card_id, attached_to: tid });
        .                                   }
        .                               }
        .           
        .                               // Evoke: schedule a self-sacrifice trigger that resolves
        .                               // AFTER the ETB triggers (so the ETB exile happens first,
        .                               // then the creature sacrifices itself).
    6,522 ( 0.00%)                      if evoked {
        .                                   self.stack.push(
        .                                   TriggerPush::new(card_id, caster, Effect::Move {
        .                                       what: crate::effect::Selector::This,
        .                                       to: crate::effect::ZoneDest::Graveyard,
        .                                   })
        .                                   .build(),
        .                                   );
        .                               }
        .           
        .                               // Dash (CR 702.110): the dashed creature gains haste and
        .                               // returns to its owner's hand at the beginning of the next
        .                               // end step. Grant haste on the entering instance and arm
        .                               // the delayed bounce.
    4,348 ( 0.00%)                      if dashed
        .                                   && self.battlefield.iter().any(|c| c.id == card_id)
        .                               {
        .                                   self.grant_keyword_eot(card_id, Keyword::Haste);
        .                                   self.delayed_triggers.push(crate::game::types::DelayedTrigger {
        .                                       controller: caster,
        .                                       source: card_id,
        .                                       kind: crate::game::types::DelayedKind::NextEndStep,
        .                                       effect: Effect::Move {
-- line 2074 ----------------------------------------
-- line 2086 ----------------------------------------
        .                                       expires_after_turn: None,
        .                                   });
        .                               }
        .           
        .                               // Blitz (CR 702.152): the blitzed creature gains haste and
        .                               // "When this creature dies, draw a card," and is sacrificed
        .                               // at the beginning of the next end step. Grant haste on the
        .                               // entering instance and arm the two delayed triggers.
   37,662 ( 0.00%)                      if self.battlefield.iter().any(|c| c.id == card_id && c.blitzed) {
        .                                   self.grant_keyword_eot(card_id, Keyword::Haste);
        .                                   self.delayed_triggers.push(crate::game::types::DelayedTrigger {
        .                                       controller: caster,
        .                                       source: card_id,
        .                                       kind: crate::game::types::DelayedKind::WhenCardDies(card_id),
        .                                       effect: Effect::Draw {
        .                                           who: crate::effect::Selector::You,
        .                                           amount: crate::effect::Value::Const(1),
-- line 2102 ----------------------------------------
-- line 2120 ----------------------------------------
        .                                   });
        .                               }
        .           
        .                               // Warp (EOE): a permanent cast for its warp cost is exiled
        .                               // at the beginning of the next end step and may be recast
        .                               // from exile later for its full cost. Arm a delayed
        .                               // exile-then-grant-may-play; clear the flag once consumed.
        .                               if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
    4,348 ( 0.00%)                          && c.warped
        .                               {
        .                                   c.warped = false;
        .                                   self.delayed_triggers.push(crate::game::types::DelayedTrigger {
        .                                       controller: caster,
        .                                       source: card_id,
        .                                       kind: crate::game::types::DelayedKind::NextEndStep,
        .                                       effect: Effect::Seq(vec![
        .                                           Effect::Move {
-- line 2136 ----------------------------------------
-- line 2153 ----------------------------------------
        .                                       expires_after_turn: None,
        .                                   });
        .                               }
        .           
        .                               // Suspend (CR 702.62f): a creature cast off its last time
        .                               // counter gains haste. The flag rode the instance from
        .                               // exile; clear it once consumed.
        .                               if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
    4,348 ( 0.00%)                          && c.cast_from_suspend
        .                               {
        .                                   c.cast_from_suspend = false;
        .                                   self.grant_keyword_eot(card_id, Keyword::Haste);
        .                               }
        .           
        .                               // Push ETB triggers onto the stack — Elesh Norn
        .                               // replacement adjusts the trigger count (0 = suppressed
        .                               // by opponent's Norn, 1+N = each of your Norns adds an
        .                               // extra fire). The spell's `x_value` is threaded so
        .                               // ETB-trigger expressions like `Effect::AddCounter
        .                               // { amount: Value::XFromCost }` (Pterafractyl, Static
        .                               // Prison) read the actual paid X.
   10,870 ( 0.00%)                      let base_mult = crate::game::actions::etb_trigger_multiplier(
1,666,766 ( 0.05%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::etb_trigger_multiplier (2,174x)
        .                                   self,
        .                                   caster,
        .                                   Some(card_id),
        .                               );
        .                               // Subtype/supertype-keyed trigger doublers (Harmonic
        .                               // Prodigy, Annie Joins Up) apply to the entrant's own ETB
        .                               // on the cast path too, not only to `fire_self_etb_triggers`.
    6,522 ( 0.00%)                      let etb_multiplier = if base_mult == 0 {
        .                                   0
        .                               } else {
    4,348 ( 0.00%)                          base_mult
    2,174 ( 0.00%)                              + crate::game::actions::ally_trigger_extra_fires(self, caster, card_id)
8,495,978 ( 0.27%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::ally_trigger_extra_fires (2,174x)
        .                               };
   18,328 ( 0.00%)                      for (effect, filter) in etb_triggers {
    8,346 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (156x)
        .                                   // CR 603.4 — honor the trigger's intervening-`if`
        .                                   // (`event.filter`) now that the source is on the
        .                                   // battlefield; a false condition skips the fire.
      156 ( 0.00%)                          if let Some(pred) = &filter {
        .                                       let mut ctx = crate::game::effects::EffectContext::for_trigger(
        .                                           card_id, caster, None, 0,
        .                                       );
        .                                       // Carry the source's cast-mode flags so cast-property
        .                                       // intervening-`if`s (SpellWasKicked / SpellWasBargained
        .                                       // / …) read true when the mode was paid.
        .                                       if let Some(c) = self.battlefield_find(card_id) {
        .                                           ctx.kicked = c.kicked;
-- line 2200 ----------------------------------------
-- line 2204 ----------------------------------------
        .                                           ctx.cast_via_mayhem = c.cast_via_mayhem;
        .                                       }
        .                                       if !self.evaluate_predicate(pred, &ctx) {
        .                                           continue;
        .                                       }
        .                                   }
        .                                   // Strict Proctor's CR 614 tax — pay {amount} or
        .                                   // sacrifice the source. Applied once per fire.
      468 ( 0.00%)                          if !crate::game::actions::apply_etb_trigger_tax(
   10,100 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::apply_etb_trigger_tax (78x)
        .                                       self, card_id, caster,
        .                                   ) {
        .                                       // Source sacrificed; remaining ETB triggers moot.
        .                                       break;
        .                                   }
        .                                   // CR 700.2b — modal ETB trigger mode pick at
        .                                   // push-time (Biblioplex Tomekeeper's "choose up
        .                                   // to one — prepare / unprepare").
      546 ( 0.00%)                          let mode = self.pick_trigger_mode(&effect, card_id, caster);
    3,588 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::pick_trigger_mode (78x)
        .                                   // Per-copy target choice for doubled fires
        .                                   // (CR 603.3d): each copy prefers a target the prior
        .                                   // copies didn't pick, so the second Solitude exile
        .                                   // under Elesh Norn aims at a fresh creature.
       78 ( 0.00%)                          let mut avoid = vec![card_id];
        .                                   for _ in 0..etb_multiplier {
        .                                       // Converge-aware: an ETB trigger reading the
        .                                       // cast's converge count (Sundering Archaic)
        .                                       // must pick its target under the concrete cap.
      624 ( 0.00%)                              let auto_target = self.auto_target_for_effect_avoiding_set_xc(
   94,073 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/targeting.rs:crabomination::game::effects::targeting::<impl crabomination::game::GameState>::auto_target_for_effect_avoiding_set_xc (78x)
        .                                           &effect,
        .                                           caster,
        .                                           &avoid,
        .                                           x_value,
        .                                           converged_value,
        .                                       );
      546 ( 0.00%)                              if let Some(Target::Permanent(tid)) = &auto_target {
        .                                           avoid.push(*tid);
        .                                       }
        .                                       // CR 115.1c — an engine-resolved "up to N target"
        .                                       // ETB ability (Azorius Justiciar's detain-two)
        .                                       // maximizes its targets; fill slots 1.. the way the
        .                                       // cast path threads `additional_targets`.
      546 ( 0.00%)                              let additional = self.auto_extra_targets_for(
   12,090 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::auto_extra_targets_for (78x)
        .                                           &effect, card_id, caster, auto_target.clone(),
        .                                       );
        .                                       for t in &additional {
        .                                           if let Target::Permanent(tid) = t {
        .                                               avoid.push(*tid);
        .                                           }
        .                                       }
       78 ( 0.00%)                              self.stack.push(
      234 ( 0.00%)                                  TriggerPush::new(card_id, caster, effect.clone())
    8,190 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (78x)
        .                                               .target(auto_target.clone())
        .                                               .additional_targets(additional)
        .                                               .mode(mode)
        .                                               .x_value(x_value)
        .                                               .converged_value(converged_value)
        .                                               .trigger_source(Some(
        .                                           crate::game::effects::EntityRef::Permanent(card_id),
        .                                       ))
        .                                               .mana_spent(mana_spent)
      312 ( 0.00%)                                      .build(),
   23,712 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/types.rs:crabomination::game::types::TriggerPush::build (78x)
        .                                       );
        .                                       // CR 603 — a triggered ability that DECLARES a
        .                                       // target slot (printed "target …" wording) fires
        .                                       // "becomes the target" listeners (Tenured
        .                                       // Concocter), same as the cast/activated paths.
      468 ( 0.00%)                              if effect.requires_target()
    5,070 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect/query.rs:crabomination_base::effect::query::<impl crabomination_base::effect::Effect>::requires_target (78x)
        .                                           && let Some(Target::Permanent(tid)) = &auto_target
        .                                           && self.battlefield_find(*tid).is_some()
        .                                       {
        .                                           let evs = vec![
        .                                               GameEvent::ChoseTargets { chooser: caster, object: card_id },
        .                                               GameEvent::BecameTarget { target: *tid, caster, by: Some(card_id) },
        .                                           ];
        .                                           self.dispatch_triggers_for_events(&evs);
        .                                       }
        .                                   }
      156 ( 0.00%)                      }
    3,588 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (78x)
        .           
        .                               // CR 714.2b — a Saga enters with its first lore counter;
        .                               // chapter I fires off the same lore-counter placement.
    2,174 ( 0.00%)                      let is_saga = self
        .                                   .battlefield
        .                                   .iter()
   35,488 ( 0.00%)                          .any(|c| c.id == card_id && !c.definition.saga_chapters.is_empty());
        .                               if is_saga {
        .                                   self.saga_enter_advance(card_id);
        .                               }
        .           
        .                               // CR 716.2 — a Class enters the battlefield at level 1.
        .                               if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == card_id)
    6,522 ( 0.00%)                          && c.definition.is_class()
   20,946 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_class (2,174x)
        .                               {
        .                                   c.class_level = 1;
        .                               }
        .           
        .                               // AnotherOfYours creature-ETB triggers are dispatched
        .                               // by the unified event pipeline (`dispatch_triggers_
        .                               // for_events` reading the `PermanentEntered` event).
        .                               // The synchronous push that used to live here was a
-- line 2303 ----------------------------------------
-- line 2310 ----------------------------------------
        .                               // the sole source of truth.
        .                           } else {
        .                               let chosen_mode = mode.unwrap_or(0);
        .                               // CR 702.15 — Radiant Scrollwielder: while this instant/
        .                               // sorcery resolves, its controller gains life from any
        .                               // damage it deals. Stamp the seat so `deal_damage_to_from`
        .                               // can credit it, then clear after resolution.
        .                               let is_is = card.definition.card_types.iter().any(|t| {
    2,868 ( 0.00%)                          matches!(t, crate::card::CardType::Instant | crate::card::CardType::Sorcery)
        .                               });
        .                               // Judith, Carnage Connoisseur — a per-spell grant onto this
        .                               // stack item counts the same as the blanket static.
    2,868 ( 0.00%)                      let granted_ll = self.spell_granted_keyword(card.id, &Keyword::Lifelink);
        .                               let granted_dt = self.spell_granted_keyword(card.id, &Keyword::Deathtouch);
    1,434 ( 0.00%)                      self.spell_keyword_grants.retain(|(id, _)| *id != card.id);
    8,604 ( 0.00%)                      self.resolving_spell_lifelink_seat = (is_is
    5,736 ( 0.00%)                          && (granted_ll || self.controller_grants_spell_lifelink(caster)))
        .                               .then_some(caster);
        .                               // Pestilent Spirit — the resolving I/S has deathtouch while
        .                               // its controller has the granting static.
    1,434 ( 0.00%)                      self.resolving_spell_deathtouch_seat = (is_is
    5,736 ( 0.00%)                          && (granted_dt || self.controller_grants_spell_deathtouch(caster)))
        .                               .then_some(caster);
        .                               // Track the I/S caster so damage the spell deals can fire
        .                               // "whenever an instant or sorcery you control deals damage"
        .                               // (Blaze Commando), once per resolution.
    2,868 ( 0.00%)                      self.resolving_spell_caster = is_is.then_some(caster);
    1,434 ( 0.00%)                      self.spell_damage_trigger_fired = false;
   21,510 ( 0.00%)                      let mut spell_events = self.continue_spell_resolution(
28,326,940 ( 0.91%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::continue_spell_resolution (1,434x)
    2,868 ( 0.00%)                          card,
        .                                   caster,
        .                                   target,
    7,170 ( 0.00%)                          additional_targets,
        .                                   chosen_mode,
        .                                   x_value,
        .                                   converged_value,
        .                                   mana_spent,
    1,434 ( 0.00%)                          None,
        .                               )?;
    1,434 ( 0.00%)                      self.resolving_spell_lifelink_seat = None;
    1,434 ( 0.00%)                      self.resolving_spell_deathtouch_seat = None;
    1,434 ( 0.00%)                      self.resolving_spell_caster = None;
        .                               events.append(&mut spell_events);
    1,434 ( 0.00%)                      if self.pending_decision.is_some() {
        .                                   return Ok(events);
        .                               }
        .                           }
        .           
        .                           // SpellCast / YourControl triggers (Prowess, Magecraft,
        .                           // Repartee, …) fire at *cast time* now (see
        .                           // `finalize_cast`). The post-resolve fire here would
        .                           // double-fire them. Kept the call site as a placeholder
        .                           // for any future "after spell resolves" trigger types.
        .                           let _ = (caster, card_id, is_noncreature);
        .                       }
        .                       StackItem::Trigger {
    1,548 ( 0.00%)                  source,
    1,032 ( 0.00%)                  controller,
    1,032 ( 0.00%)                  effect,
    1,032 ( 0.00%)                  target,
      516 ( 0.00%)                  mode,
    1,032 ( 0.00%)                  x_value,
    1,032 ( 0.00%)                  converged_value,
    1,548 ( 0.00%)                  trigger_source,
    1,032 ( 0.00%)                  mana_spent,
    1,032 ( 0.00%)                  event_amount,
    2,580 ( 0.00%)                  intervening_if,
   15,480 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (516x)
    3,096 ( 0.00%)                  additional_targets,
        .                           activated: _,
    1,548 ( 0.00%)                  trigger_player,
        .                           mana_spent_by_color,
        .                       } => {
    2,580 ( 0.00%)                  self.activation_mana_colors_scratch = mana_spent_by_color;
        .                           // CR 603.4 — re-check the intervening 'if' clause as the
        .                           // ability resolves. "If the condition isn't true at that
        .                           // time, the ability is removed from the stack and does
        .                           // nothing." We pop the trigger off the stack the same way
        .                           // (the resolver caller already drained the StackItem) but
        .                           // skip running its `effect`.
    1,032 ( 0.00%)                  if let Some(pred) = &intervening_if {
        .                               let mut ctx = crate::game::effects::EffectContext::for_trigger(
        .                                   source,
        .                                   controller,
        .                                   target.clone(),
        .                                   mode.unwrap_or(0),
        .                               );
        .                               ctx.trigger_source = trigger_source;
        .                               ctx.event_amount = event_amount;
-- line 2397 ----------------------------------------
-- line 2404 ----------------------------------------
        .                               }
        .                           }
        .                           let chosen_mode = mode.unwrap_or(0);
        .                           // CR 603.10 — scope the leaves-battlefield LKI read to this
        .                           // resolution so `Value::PowerOf(This)` on a "when this dies"
        .                           // body sees the dying object's last on-battlefield P/T.
        .                           let had_lki = self.leaves_bf_lki.contains_key(&source);
        .                           if had_lki {
      440 ( 0.00%)                      self.resolving_lki_source = Some(source);
        .                           }
        .                           // CR 603.10 — scope the dead *subject*'s LKI read too, so
        .                           // Jenova's "draw cards equal to its power" reads the dying
        .                           // Mutant's counter-boosted power (not its printed value).
    1,548 ( 0.00%)                  let lki_subject = match trigger_source {
        .                               Some(crate::game::effects::EntityRef::Card(c))
        .                               | Some(crate::game::effects::EntityRef::Permanent(c))
      134 ( 0.00%)                          if c != source && self.leaves_bf_lki.contains_key(&c) =>
        .                               {
        .                                   self.resolving_lki_subject = Some(c);
        .                                   Some(c)
        .                               }
        .                               _ => None,
        .                           };
   13,932 ( 0.00%)                  let mut trig_events = self.continue_trigger_resolution_with_source(
2,550,428 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::continue_trigger_resolution_with_source (516x)
        .                               source,
        .                               controller,
    2,580 ( 0.00%)                      *effect,
   39,600 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (516x)
        .                               target,
        .                               chosen_mode,
        .                               x_value,
        .                               converged_value,
        .                               mana_spent,
        .                               trigger_source,
        .                               event_amount,
        .                               trigger_player,
    3,096 ( 0.00%)                      additional_targets,
        .                           )?;
    1,548 ( 0.00%)                  if had_lki {
      110 ( 0.00%)                      self.resolving_lki_source = None;
        .                               self.leaves_bf_lki.remove(&source);
        .                           }
    1,032 ( 0.00%)                  if let Some(sid) = lki_subject {
        .                               self.resolving_lki_subject = None;
        .                               self.leaves_bf_lki.remove(&sid);
        .                           }
        .                           events.append(&mut trig_events);
      516 ( 0.00%)                  if self.pending_decision.is_some() {
        .                               return Ok(events);
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 724 — Effect::EndTheTurn fired during this resolution: exile
        .                   // the rest of the stack, clear combat, and jump to cleanup.
    8,248 ( 0.00%)          if self.end_turn_requested {
        .                       self.end_turn_requested = false;
        .                       return self.do_end_the_turn(events);
        .                   }
        .           
    8,248 ( 0.00%)          let mut sba = self.check_state_based_actions();
78,859,346 ( 2.53%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (4,124x)
        .                   events.append(&mut sba);
        .           
   16,496 ( 0.00%)          Ok(events)
   32,992 ( 0.00%)      }
        .           
        .               /// CR 724.1 — end the turn: exile every spell and ability still on the
        .               /// stack (real cards go to exile; trigger items and token copies cease),
        .               /// remove everything from combat, then advance from the end step
        .               /// straight into cleanup (damage wear-off, "this turn" expiry, and the
        .               /// discard-to-hand-size all happen there as normal).
        .               pub(crate) fn do_end_the_turn(
        .                   &mut self,
-- line 2475 ----------------------------------------
-- line 2496 ----------------------------------------
        .               }
        .           
        .               // ── Automatic step effects ────────────────────────────────────────────────
        .           
        .               /// CR 702.50a — copy each of the active player's resolved Epic spells
        .               /// onto the stack at the beginning of their upkeep. The copy keeps the
        .               /// original targets while they're legal, else auto-picks fresh ones
        .               /// ("you may choose new targets"; AutoDecider keeps/repairs).
   27,872 ( 0.00%)      pub fn process_epic(&mut self) -> Vec<GameEvent> {
    1,742 ( 0.00%)          let p = self.active_player_idx;
        .                   let mut events = Vec::new();
    5,226 ( 0.00%)          if self.players[p].epic_spells.is_empty() {
        .                       return events;
        .                   }
        .                   let epics = self.players[p].epic_spells.clone();
        .                   for e in epics {
        .                       let Some(def) = crate::catalog::lookup_by_name(&e.name) else { continue };
        .                       let new_id = self.next_id();
        .                       let mut copy = crate::card::CardInstance::new(new_id, def.clone(), p);
        .                       // CR 707.10a — a copy of a spell ceases to exist off the stack.
-- line 2515 ----------------------------------------
-- line 2537 ----------------------------------------
        .                           x_value: e.x_value,
        .                           converged_value: 0,
        .                           mana_spent: 0,
        .                           uncounterable: true, // copies can't be countered
        .                       });
        .                       events.push(GameEvent::SpellsCopied { original: new_id, count: 1, controller: p });
        .                   }
        .                   events
   15,678 ( 0.00%)      }
        .           
        .               /// CR 728.2 / 122.1i — rad-counter turn-based action. As the active
        .               /// player begins their precombat main phase, if they have any rad
        .               /// counters they mill that many cards; for each *nonland* card milled
        .               /// this way they lose 1 life and remove one rad counter.
        .               /// CR 701.52a — roll a d6; every Attraction `p` controls with that number
        .               /// lit up is visited, firing its visit ability (CR 717.5). A turn-based
        .               /// action: it doesn't use the stack, but the visit triggers do.
        .               pub(crate) fn roll_to_visit_attractions(&mut self, p: usize) -> Vec<GameEvent> {
        .                   let attractions: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
   64,956 ( 0.00%)              .filter(|c| c.controller == p && !c.definition.attraction_lights.is_empty())
        .                       .map(|c| c.id)
        .                       .collect();
    3,484 ( 0.00%)          if attractions.is_empty() {
        .                       return Vec::new();
        .                   }
        .                   let result = self.roll_one_die(p, 6);
        .                   let mut events = vec![GameEvent::RolledToVisitAttractions { player: p, result }];
        .                   for id in attractions {
        .                       let lit = self
        .                           .battlefield_find(id)
        .                           .is_some_and(|c| c.definition.attraction_lights.contains(&result));
-- line 2569 ----------------------------------------
-- line 2571 ----------------------------------------
        .                           events.push(GameEvent::AttractionVisited { card_id: id });
        .                       }
        .                   }
        .                   events
        .               }
        .           
        .               pub(crate) fn do_rad_counters(&mut self) -> Vec<GameEvent> {
        .                   use crate::card::CardType;
    1,742 ( 0.00%)          let p = self.active_player_idx;
        .                   let mut events = Vec::new();
    5,226 ( 0.00%)          let n = self.players[p].rad_counters;
    3,484 ( 0.00%)          if n == 0 {
    8,710 ( 0.00%)              return events;
        .                   }
        .                   for _ in 0..n {
        .                       if self.players[p].library.is_empty() {
        .                           break;
        .                       }
        .                       let card = self.players[p].library.remove(0);
        .                       let cid = card.id;
        .                       let is_land = card.definition.card_types.contains(&CardType::Land);
-- line 2591 ----------------------------------------
-- line 2607 ----------------------------------------
        .           
        .               /// CR 702.26 / 502.1 — phasing turn-based action at the start of the
        .               /// untap step (before permanents untap). Permanents the active player
        .               /// controls that are phased out phase in; permanents they control with
        .               /// phasing (plus anything attached to them) phase out. Phasing in/out is
        .               /// not a zone change: no ETB/LTB triggers fire and all state is retained,
        .               /// modelled by moving the `CardInstance` between `battlefield` and
        .               /// `phased_out` rather than re-creating it.
   13,744 ( 0.00%)      pub fn do_phasing(&mut self) {
    5,154 ( 0.00%)          let p = self.active_player_idx;
        .                   // Direct phasers currently in play (computed *before* phase-in so a
        .                   // permanent that phases in this step doesn't immediately phase back
        .                   // out), plus any Aura/Equipment attached to one of them.
        .                   // Computed keywords so layer-granted Phasing (Teferi's Veil-style
        .                   // statics) phases out too, not just the printed/EOT-granted keyword.
        .                   // `CantPhaseOut` only ever shrinks the set, so the presence gate on
        .                   // Phasing alone is exact — and it is `false` on nearly every board,
        .                   // which is the whole-board layer pass this step used to take per turn.
        .                   let mut to_phase_out: crate::fxhash::HashSet<crate::card::CardId> =
        .                       self.with_frozen_layers(|g| {
   10,308 ( 0.00%)                  if !g.board_keyword_in_scope(&[crate::card::Keyword::Phasing]) {
4,665,355 ( 0.15%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::board_keyword_in_scope (1,718x)
        .                               return crate::fxhash::HashSet::default();
        .                           }
        .                           g.compute_battlefield()
        .                               .iter()
        .                               .filter(|c| {
        .                                   c.controller == p
        .                                       && c.keywords.contains(&crate::card::Keyword::Phasing)
        .                                       // CR 702.26 — Spatial Binding pins a permanent in phase.
        .                                       && !c.keywords.contains(&crate::card::Keyword::CantPhaseOut)
        .                               })
        .                               .map(|c| c.id)
        .                               .collect()
        .                       });
    3,436 ( 0.00%)          if !to_phase_out.is_empty() {
        .                       let attached: Vec<crate::card::CardId> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.attached_to.is_some_and(|h| to_phase_out.contains(&h)))
        .                           .map(|c| c.id)
        .                           .collect();
        .                       to_phase_out.extend(attached);
        .                   }
        .                   // Phase IN: every phased-out permanent this player controls returns —
        .                   // except "until [source] leaves" phase-outs (CR 702.26 — Out of
        .                   // Time), which `on_left_battlefield` returns instead.
        .                   let mut phased_in: Vec<crate::card::CardId> = Vec::new();
        .                   let mut i = 0;
    5,154 ( 0.00%)          while i < self.phased_out.len() {
        .                       if self.phased_out[i].controller == p && self.phased_out[i].phased_out_by.is_none() {
        .                           let c = self.phased_out.remove(i);
        .                           phased_in.push(c.id);
        .                           self.battlefield.push(c);
        .                       } else {
        .                           i += 1;
        .                       }
        .                   }
        .                   // CR 702.26 — "when this phases out" fires while the permanent is still
        .                   // on the battlefield, so its own trigger can be found.
    3,436 ( 0.00%)          if !to_phase_out.is_empty() {
        .                       let evs: Vec<GameEvent> = to_phase_out
        .                           .iter()
        .                           .map(|&card_id| GameEvent::PermanentPhasedOut { card_id })
        .                           .collect();
        .                       self.dispatch_triggers_for_events(&evs);
        .                   }
        .                   // Phase OUT the set captured above.
        .                   if !to_phase_out.is_empty() {
-- line 2674 ----------------------------------------
-- line 2679 ----------------------------------------
        .                               self.phased_out.push(c);
        .                           } else {
        .                               idx += 1;
        .                           }
        .                       }
        .                   }
        .                   // CR 702.26 — "when this phases in" triggers. Phasing in isn't an ETB,
        .                   // so we dispatch a dedicated `PermanentPhasedIn` event for each.
    3,436 ( 0.00%)          if !phased_in.is_empty() {
        .                       let evs: Vec<GameEvent> = phased_in
        .                           .into_iter()
        .                           .map(|card_id| GameEvent::PermanentPhasedIn { card_id })
        .                           .collect();
        .                       self.dispatch_triggers_for_events(&evs);
        .                   }
   13,744 ( 0.00%)      }
        .           
        .               /// CR 502.3 / 122.1d — whether `card_id` is currently stopped from untapping
        .               /// during its controller's next untap step by a continuous prevention (a
        .               /// `PreventUntap` static in any of its selector forms, or a stun counter
        .               /// waiting to be removed). A read-only sibling of the set `do_untap`
        .               /// computes, surfaced through the server view so the UI can flag locked
        .               /// permanents. Player-scoped skips (Yosei, Bontu's) aren't included — those
        .               /// are one-shot flags, not a property of the permanent.
        .               /// Blind Fury — creature-to-creature combat damage is doubled once per
        .               /// active doubler this turn (CR 614.x). Player-facing and noncombat damage
        .               /// are untouched.
        .               pub fn double_creature_combat_damage(&self, amount: u32) -> u32 {
   13,350 ( 0.00%)          amount.saturating_mul(1u32 << self.creature_combat_damage_doublers.min(8))
        .               }
        .           
        .               /// CR 510.1a — this creature assigns no combat damage for the rest of the
        .               /// turn, whether by a printed keyword (Master of Cruelties) or a
        .               /// turn-scoped effect (Kukemssa Pirates). Both damage-assignment sites
        .               /// consult the same two sources; this is the read-only mirror the client
        .               /// badge uses.
        .               pub fn assigns_no_combat_damage(&self, card_id: crate::card::CardId) -> bool {
-- line 2715 ----------------------------------------
-- line 2799 ----------------------------------------
        .                                   )
        .                               })
        .                           }
        .                           _ => false,
        .                       })
        .                   })
        .               }
        .           
   13,744 ( 0.00%)      pub fn do_untap(&mut self) {
        .                   // CR 502.1 — phasing happens first, as a turn-based action.
    1,718 ( 0.00%)          self.do_phasing();
5,304,397 ( 0.17%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::do_phasing (1,718x)
    3,436 ( 0.00%)          let p = self.active_player_idx;
        .                   // Untap permanents YOU CONTROL on your untap step, not just
        .                   // those you originally owned. A creature you've stolen
        .                   // (Threaten / Mind Control) untaps on your turn; one of yours
        .                   // that's been stolen does not. Filtering by `owner` here would
        .                   // leave stolen permanents permanently tapped (or, conversely,
        .                   // un-tap a stolen permanent on the wrong player's turn).
        .                   //
        .                   // CR 701.46a / 122.1d — Stun counters interpose on the untap
-- line 2818 ----------------------------------------
-- line 2834 ----------------------------------------
        .                   use crate::card::SelectionRequirement;
        .                   use crate::effect::StaticEffect;
        .                   // CR 502.3 — Seedborn Muse: any player *other* than the active player
        .                   // who controls an `UntapAllYoursEachUntapStep` permanent also untaps
        .                   // their permanents during this untap step.
        .                   // CR 502.3 — a player who's been made to skip their next untap step
        .                   // (Yosei) doesn't untap; consume one charge. Seedborn-style other
        .                   // untappers still untap their own permanents.
   10,308 ( 0.00%)          let active_skips_untap = if self.players[p].skip_next_untap_step > 0 {
        .                       self.players[p].skip_next_untap_step -= 1;
        .                       true
        .                   } else {
        .                       false
        .                   };
        .                   // Bontu's Last Reckoning — the active player's lands skip this untap.
    8,590 ( 0.00%)          let active_lands_skip_untap = if self.players[p].lands_dont_untap_next_untap > 0 {
        .                       self.players[p].lands_dont_untap_next_untap -= 1;
        .                       true
        .                   } else {
        .                       false
        .                   };
        .                   // Blinding Beam — the active player's creatures skip this untap.
        .                   let active_creatures_skip_untap =
   10,308 ( 0.00%)              if self.players[p].creatures_dont_untap_next_untap > 0 {
        .                           self.players[p].creatures_dont_untap_next_untap -= 1;
        .                           true
        .                       } else {
        .                           false
        .                       };
        .                   // CR 502.4 — Mist of Stagnation: nothing untaps during any untap step.
    6,872 ( 0.00%)          if self.battlefield.iter().chain(self.command_zone_sources()).any(|c| {
        .                       c.definition
        .                           .static_abilities
        .                           .iter()
        .                           .any(|sa| matches!(sa.effect, StaticEffect::PermanentsDontUntap))
        .                   }) {
        .                       for c in self.battlefield.iter_mut().filter(|c| c.controller == p) {
        .                           c.summoning_sick = false;
        .                       }
        .                       return;
        .                   }
        .                   let untappers: Vec<usize> = {
   12,026 ( 0.00%)              let mut u = if active_skips_untap { vec![] } else { vec![p] };
        .                       for c in &self.battlefield {
   97,434 ( 0.00%)                  if c.controller != p
        .                               && !u.contains(&c.controller)
        .                               && c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(
        .                                       self.active_static(&sa.effect, c),
        .                                       Some(StaticEffect::UntapAllYoursEachUntapStep)
        .                                   )
        .                               })
        .                           {
        .                               u.push(c.controller);
        .                           }
        .                       }
    6,872 ( 0.00%)              u
        .                   };
        .                   // CR 502.3 — the filtered sibling (Prophet of Kruphix): an off-turn
        .                   // controller untaps only the permanents matching the static's filter.
        .                   let filtered_untap: crate::fxhash::HashSet<crate::card::CardId> = {
    1,718 ( 0.00%)              let filters: Vec<(usize, SelectionRequirement)> = self
        .                           .battlefield
        .                           .iter()
   97,434 ( 0.00%)                  .filter(|c| c.controller != p && !untappers.contains(&c.controller))
        .                           .flat_map(|c| {
        .                               c.definition.static_abilities.iter().filter_map(|sa| {
        .                                   match self.active_static(&sa.effect, c) {
        .                                       Some(StaticEffect::UntapYoursEachUntapStepFiltered(f)) => {
        .                                           Some((c.controller, f.clone()))
        .                                       }
        .                                       _ => None,
        .                                   }
        .                               })
        .                           })
        .                           .collect();
        .                       let mut set = crate::fxhash::HashSet::default();
        .                       // Endbringer — "untap this during each other player's untap step".
        .                       for c in &self.battlefield {
   97,434 ( 0.00%)                  if c.controller != p
        .                               && c.definition
        .                                   .static_abilities
        .                                   .iter()
        .                                   .any(|sa| matches!(sa.effect, StaticEffect::UntapSelfEachOtherUntapStep))
        .                           {
        .                               set.insert(c.id);
        .                           }
        .                       }
-- line 2921 ----------------------------------------
-- line 2927 ----------------------------------------
        .                                       &crate::game::types::Target::Permanent(c.id),
        .                                       *seat,
        .                                   )
        .                               {
        .                                   set.insert(c.id);
        .                               }
        .                           }
        .                       }
    6,872 ( 0.00%)              set
        .                   };
        .                   // Storage Matrix — while an untapped one is out, each untapping player
        .                   // picks artifact / creature / land and untaps only that type this step.
        .                   // The auto pick is whichever type would free the most permanents.
        .                   let matrix_choice: crate::fxhash::HashMap<usize, crate::card::CardType> = {
    1,718 ( 0.00%)              let live = self.battlefield.iter().any(|c| {
   64,956 ( 0.00%)                  !c.tapped
        .                               && c.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(sa.effect, StaticEffect::UntapOnlyChosenTypeWhileUntapped)
        .                               })
        .                       });
        .                       let mut out = crate::fxhash::HashMap::default();
        .                       if live {
        .                           use crate::card::CardType;
        .                           for seat in &untappers {
-- line 2950 ----------------------------------------
-- line 2960 ----------------------------------------
        .                               };
        .                               let best = [CardType::Land, CardType::Creature, CardType::Artifact]
        .                                   .into_iter()
        .                                   .max_by_key(|t| count(t))
        .                                   .unwrap();
        .                               out.insert(*seat, best);
        .                           }
        .                       }
    6,872 ( 0.00%)              out
        .                   };
        .                   let prevented: crate::fxhash::HashSet<crate::card::CardId> = {
        .                       let mut blocked = crate::fxhash::HashSet::default();
        .                       // Storage Matrix's off-type permanents are simply blocked.
    3,436 ( 0.00%)              if !matrix_choice.is_empty() {
        .                           for c in &self.battlefield {
        .                               if let Some(t) = matrix_choice.get(&c.controller)
        .                                   && !c.definition.card_types.contains(t)
        .                               {
        .                                   blocked.insert(c.id);
        .                               }
        .                           }
        .                       }
-- line 2981 ----------------------------------------
-- line 3008 ----------------------------------------
        .                                       if let Some(host) = c.attached_to {
        .                                           blocked.insert(host);
        .                                       }
        .                                   }
        .                                   _ => {}
        .                               }
        .                           }
        .                       }
    3,436 ( 0.00%)              if global_prevent {
        .                           for id in self.battlefield.iter().map(|c| c.id).collect::<Vec<_>>() {
        .                               if self.untap_prevented_by_static(id) {
        .                                   blocked.insert(id);
        .                               }
        .                           }
        .                       }
    3,436 ( 0.00%)              if !prevent_filters.is_empty() {
        .                           for c in &self.battlefield {
        .                               if !untappers.contains(&c.controller) {
        .                                   continue;
        .                               }
        .                               for req in &prevent_filters {
        .                                   if self.evaluate_requirement(
        .                                       req,
        .                                       &crate::game::types::Target::Permanent(c.id),
-- line 3031 ----------------------------------------
-- line 3033 ----------------------------------------
        .                                   ) {
        .                                       blocked.insert(c.id);
        .                                       break;
        .                                   }
        .                               }
        .                           }
        .                       }
        .                       // Bontu's Last Reckoning — block the active player's lands.
    1,718 ( 0.00%)              if active_lands_skip_untap {
        .                           for c in &self.battlefield {
        .                               if c.controller == p && c.definition.is_land() {
        .                                   blocked.insert(c.id);
        .                               }
        .                           }
        .                       }
        .                       // Blinding Beam — block the active player's creatures.
    1,718 ( 0.00%)              if active_creatures_skip_untap {
        .                           for c in &self.battlefield {
        .                               if c.controller == p && c.definition.is_creature() {
        .                                   blocked.insert(c.id);
        .                               }
        .                           }
        .                       }
    6,872 ( 0.00%)              blocked
        .                   };
        .                   // Entrancing Lyre tap-lock: gather the ids referenced as a lock source
        .                   // and the set of currently-tapped permanents. A lock source keeps
        .                   // itself tapped (the "you may choose not to untap this artifact"
        .                   // clause, modeled as: stay tapped while it still locks a creature); a
        .                   // locked permanent skips its untap while its source remains tapped.
        .                   // Vedalken Shackles joins the same set: while it holds a stolen
        .                   // creature it stays tapped ("you may choose not to untap this").
        .                   // Only a source that actually prints "you may choose not to untap
        .                   // this" holds itself down; one without the clause (Kill Switch)
        .                   // untaps normally and releases its lock.
    1,718 ( 0.00%)          let lock_sources: crate::fxhash::HashSet<crate::card::CardId> = self
        .                       .battlefield
        .                       .iter()
        .                       .filter_map(|c| c.untap_locked_by)
        .                       .chain(
    1,718 ( 0.00%)                  self.temporary_control
        .                               .iter()
        .                               .filter(|tc| tc.while_source_tapped)
        .                               .filter_map(|tc| tc.source),
        .                       )
        .                       .filter(|id| {
        .                           self.battlefield_find(*id).is_some_and(|c| {
        .                               c.definition.keywords.contains(&crate::card::Keyword::MayChooseNotToUntap)
        .                           })
        .                       })
        .                       .collect();
        .                   let tapped_now_set: crate::fxhash::HashSet<crate::card::CardId> =
   10,760 ( 0.00%)              self.battlefield.iter().filter(|c| c.tapped).map(|c| c.id).collect();
        .                   // Shipbreaker Kraken — a presence-lock holds while its source is still
        .                   // on the battlefield.
        .                   let on_battlefield: crate::fxhash::HashSet<crate::card::CardId> =
   34,196 ( 0.00%)              self.battlefield.iter().map(|c| c.id).collect();
        .                   // CR 502.3 — "Players can't untap more than one [filter] during their
        .                   // untap steps" (Winter Moon, Imi Statue). Pre-resolve each cap's
        .                   // matching permanents (the untap loop below borrows the battlefield
        .                   // mutably), then let each player untap at most one per cap.
    1,718 ( 0.00%)          let untap_caps: Vec<(crate::fxhash::HashSet<crate::card::CardId>, u32)> = self
        .                       .battlefield
        .                       .iter()
        .                       .flat_map(|c| c.definition.static_abilities.iter().map(move |sa| (c, sa)))
        .                       .filter_map(|(c, sa)| match self.active_static(&sa.effect, c) {
        .                           Some(StaticEffect::MaxOneUntapPerStep { filter }) => Some((filter, 1)),
        .                           Some(StaticEffect::MaxUntapsPerStep { filter, max }) => Some((filter, *max)),
        .                           _ => None,
        .                       })
-- line 3102 ----------------------------------------
-- line 3117 ----------------------------------------
        .                           (set, max)
        .                       })
        .                       .collect();
        .                   let mut capped_untaps: crate::fxhash::HashMap<(usize, usize), u32> =
        .                       crate::fxhash::HashMap::default();
        .                   // CR 502.1 — ask each `MayChooseNotToUntap` permanent's controller
        .                   // before the loop (which borrows the battlefield mutably).
        .                   let may_decline: crate::fxhash::HashSet<crate::card::CardId> = {
    1,718 ( 0.00%)              let asking: Vec<(crate::card::CardId, usize, &'static str)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| {
   64,956 ( 0.00%)                      c.tapped
        .                                   && untappers.contains(&c.controller)
        .                                   && c.definition.keywords.contains(&crate::card::Keyword::MayChooseNotToUntap)
        .                           })
        .                           .map(|c| (c.id, c.controller, c.definition.name))
        .                           .collect();
        .                       let mut set = crate::fxhash::HashSet::default();
        .                       for (id, _seat, name) in asking {
        .                           if let crate::decision::DecisionAnswer::Bool(true) =
-- line 3137 ----------------------------------------
-- line 3138 ----------------------------------------
        .                               self.decider.decide(&crate::decision::Decision::OptionalTrigger {
        .                                   source: id,
        .                                   description: format!("Leave {name} tapped?"),
        .                               })
        .                           {
        .                               set.insert(id);
        .                           }
        .                       }
    6,872 ( 0.00%)              set
        .                   };
        .                   // CR 502.3 / 613 — both untap locks must be read off the *computed*
        .                   // keywords, so a granted one counts too (Temporal Distortion's
        .                   // hourglass counters, Steel Dromedary's own; Tangle Kelp's aura
        .                   // grant of "doesn't untap if it attacked"). One whole-board layer
        .                   // pass answers both: `computed_permanent` outside a freeze scope
        .                   // re-gathers every continuous effect in the game per call, and these
        .                   // were two such calls per battlefield permanent per untap step.
        .                   // Neither lock is on a bench board, so the pass rides a presence gate
        .                   // on the two keywords it looks for.
        .                   let (counter_locked, attack_locked) = self.with_frozen_layers(|g| {
        .                       let mut counter_locked: Vec<crate::card::CardId> = Vec::new();
        .                       let mut attack_locked: Vec<crate::card::CardId> = Vec::new();
    5,154 ( 0.00%)              if !g.board_keyword_matching(|k| {
        .                           matches!(
        .                               k,
        .                               crate::card::Keyword::DoesntUntapWhileCounter(_)
        .                                   | crate::card::Keyword::DoesntUntapIfAttackedLastTurn
        .                           )
        .                       }) {
        .                           return (counter_locked, attack_locked);
        .                       }
-- line 3168 ----------------------------------------
-- line 3183 ----------------------------------------
        .                   // fire CR 702.108 Inspired ("becomes untapped") triggers afterward.
        .                   let mut untapped_now: Vec<crate::card::CardId> = Vec::new();
        .                   for card in &mut self.battlefield {
        .                       if untappers.contains(&card.controller) || filtered_untap.contains(&card.id) {
        .                           // Summoning sickness clears only for the *active* player at the
        .                           // turn boundary (CR 302.1 / 506.4). A Seedborn-untapped
        .                           // permanent (controlled by another player) untaps but does not
        .                           // shed sickness on someone else's turn.
   30,944 ( 0.00%)                  let active = card.controller == p;
        .                           if prevented.contains(&card.id) {
        .                               // CR 502.3 — untap is prevented. Summoning sickness still
        .                               // clears per CR 506.4 (the turn-boundary tag, not the
        .                               // untap event).
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // CR 702.83 — an exerted creature skips this untap. The flag
        .                           // is one-shot: clear it so the creature untaps normally next
        .                           // turn. No tapped→untapped flip, so no Inspired trigger.
   30,944 ( 0.00%)                  if card.skip_next_untap {
        .                               card.skip_next_untap = false;
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // Entrancing Lyre — a lock source keeps itself tapped while it
        .                           // still locks a creature.
   46,416 ( 0.00%)                  if card.tapped && lock_sources.contains(&card.id) {
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // Goblin Rock Sled / Tangle Kelp — "doesn't untap … if it
        .                           // attacked during your last turn". `attacked_own_turn` still
        .                           // holds the previous own-turn attack; the roll-over into
        .                           // `attacked_last_turn` happens after this loop.
   30,944 ( 0.00%)                  if card.attacked_own_turn && attack_locked.contains(&card.id) {
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // Steel Dromedary / Temporal Distortion — "doesn't untap …
        .                           // if it has a [kind] counter on it" (printed or granted).
        .                           if counter_locked.contains(&card.id) {
-- line 3231 ----------------------------------------
-- line 3232 ----------------------------------------
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // CR 502.1 — "you may choose not to untap this" (Hisoka's
        .                           // Guard). Asked as "leave tapped?", so a headless seat's
        .                           // default `false` untaps normally.
   30,944 ( 0.00%)                  if card.tapped && may_decline.contains(&card.id) {
        .                               if active {
        .                                   card.summoning_sick = false;
        .                               }
        .                               continue;
        .                           }
        .                           // A locked permanent skips its untap while its source is still
        .                           // tapped on the battlefield; otherwise the lock releases.
   30,944 ( 0.00%)                  if let Some(src) = card.untap_locked_by {
        .                               if tapped_now_set.contains(&src) {
        .                                   if active {
        .                                       card.summoning_sick = false;
        .                                   }
        .                                   continue;
        .                               }
        .                               card.untap_locked_by = None;
        .                           }
   30,944 ( 0.00%)                  if let Some(src) = card.untap_locked_while_present {
        .                               if on_battlefield.contains(&src) {
        .                                   if active {
        .                                       card.summoning_sick = false;
        .                                   }
        .                                   continue;
        .                               }
        .                               card.untap_locked_while_present = None;
        .                           }
        .                           // CR 502.3 — a tapped permanent beyond the first this player
        .                           // untaps under any active cap stays tapped.
   30,944 ( 0.00%)                  if card.tapped
        .                               && let Some(i) = untap_caps.iter().position(|(s, _)| s.contains(&card.id))
        .                           {
        .                               let n = capped_untaps.entry((i, card.controller)).or_insert(0);
        .                               if *n >= untap_caps[i].1 {
        .                                   if active {
        .                                       card.summoning_sick = false;
        .                                   }
        .                                   continue;
        .                               }
        .                               *n += 1;
        .                           }
   77,360 ( 0.00%)                  if card.counter_count(CounterType::Stun) > 0 {
  154,720 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (15,472x)
        .                               card.remove_counters(CounterType::Stun, 1);
        .                           } else {
   30,944 ( 0.00%)                      if card.tapped {
    4,370 ( 0.00%)                          untapped_now.push(card.id);
        .                               }
   15,472 ( 0.00%)                      card.tapped = false;
        .                           }
        .                           if active {
        .                               card.summoning_sick = false;
        .                           }
        .                       }
        .                   }
        .                   // CR 502.1 — Undiscovered Paradise: the bounce is part of the untap
        .                   // turn-based action, so it happens here rather than as a trigger.
    1,718 ( 0.00%)          let bouncing: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
   64,956 ( 0.00%)              .filter(|c| c.bounce_at_next_untap && untappers.contains(&c.controller))
        .                       .map(|c| c.id)
        .                       .collect();
        .                   for id in bouncing {
        .                       if let Some(c) = self.battlefield_find_mut(id) {
        .                           c.bounce_at_next_untap = false;
        .                       }
        .                       let ctx = crate::game::effects::EffectContext::for_ability(id, p, None);
        .                       let mut evs = Vec::new();
-- line 3306 ----------------------------------------
-- line 3334 ----------------------------------------
        .                       } else if card.tapped {
        .                           untapped_now.push(card.id);
        .                           card.tapped = false;
        .                       }
        .                   }
        .                   // CR 502.3 — an Aura's "Enchanted land untaps during each other
        .                   // player's untap step" (Urban Burgeoning). Collect the hosts whose
        .                   // aura carries the static and whose controller isn't the untapper.
    1,718 ( 0.00%)          let untap_hosts: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
        .                       .filter(|src| {
        .                           !untappers.contains(&src.controller)
        .                               && src.definition.static_abilities.iter().any(|sa| {
        .                                   matches!(sa.effect, StaticEffect::UntapAttachedEachUntapStep)
        .                               })
        .                       })
-- line 3350 ----------------------------------------
-- line 3366 ----------------------------------------
        .                   for card in &mut self.battlefield {
        .                       card.goaded_by.retain(|&g| g != p);
        .                       // CR 701.35 — detain lasts "until your next turn"; lift it when the
        .                       // detaining player's (= active player p's) turn begins.
        .                       if card.detained_by == Some(p) {
        .                           card.detained_by = None;
        .                       }
        .                       // CR 702.142 — "attacked this turn" (Boast gate) resets each turn.
   32,478 ( 0.00%)              card.attacked_this_turn = false;
   32,478 ( 0.00%)              card.blocked_this_turn = false;
        .                       card.blocked_attackers_this_turn.clear();
   97,434 ( 0.00%)              if card.controller == p {
        .                           // "…during your last turn" rolls over as its controller's turn
        .                           // begins (Giant Turtle), and so does a pending Wall of Dust
        .                           // attack ban — armed on the previous turn, live on this one.
   30,944 ( 0.00%)                  card.attacked_last_turn = card.attacked_own_turn;
   15,472 ( 0.00%)                  card.attacked_own_turn = false;
   61,888 ( 0.00%)                  card.attack_ban = match card.attack_ban {
        .                               crate::card::AttackBan::Pending => crate::card::AttackBan::Active,
        .                               _ => crate::card::AttackBan::None,
        .                           };
        .                       }
        .                   }
    5,154 ( 0.00%)          self.players[p].lands_played_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    3,436 ( 0.00%)          self.players[p].graveyard_cast_types_this_turn.clear();
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // "Protection from everything until your next turn" expires as that
        .                   // player's turn begins (The One Ring).
    5,154 ( 0.00%)          self.players[p].protected_from_everything = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // "Opponents' spells cost more until your next turn" expires too
        .                   // (Elspeth Conquers Death II).
    1,718 ( 0.00%)          self.turn_scoped_spell_taxes.retain(|t| t.controller != p);
        .                   // "Opponents can't cast spells named X until your next turn"
        .                   // (Academic Probation mode 0) expires as the lock owner's turn begins.
    3,436 ( 0.00%)          self.players[p].opponents_cant_cast_named.clear();
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Stagger damage-doubling windows expire as the registrant's turn
        .                   // begins (Lightning, Army of One).
        .                   self.staggered_damage_players.retain(|(_, reg)| *reg != p);
        .                   // "Until your next turn, whenever a creature attacks you…" floating
        .                   // triggers (Tamiyo +2) expire as their controller's turn begins.
        .                   self.delayed_triggers.retain(|dt| {
        .                       !(matches!(
        .                           dt.kind,
        .                           crate::game::types::DelayedKind::CreatureAttacksYouUntilYourNextTurn
        .                       ) && dt.controller == p)
        .                   });
    5,154 ( 0.00%)          self.players[p].extra_land_plays = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // CR 702.179 — "speed increases once on each of your turns": clear the
        .                   // active player's per-turn speed-bump flag as their turn begins.
    5,154 ( 0.00%)          self.players[p].speed_increased_this_turn = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Raid (CR 702.108): the active player hasn't attacked yet this turn.
    5,154 ( 0.00%)          self.players[p].attacked_this_turn = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // "Until your next turn" player grants expire at their owner's untap
        .                   // (Blossoming Calm's hexproof).
    5,154 ( 0.00%)          self.players[p].hexproof_until_next_turn = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].creatures_attacked_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].spells_cast_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].spells_cast_from_hand_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Reset the Bloodthirst "damaged this turn" flag for *every* player
        .                   // CR 701.38 — a vote-control grant lasts only for its own turn.
    1,718 ( 0.00%)          self.vote_controller_this_turn = None;
        .                   // CR 725 — snapshot the crown for "…as the turn began" riders.
    3,436 ( 0.00%)          self.monarch_at_turn_start = self.monarch;
        .                   // at the turn boundary (not just the active player) so a creature
        .                   // cast on your turn reads damage dealt since this turn began.
        .                   for pl in &mut self.players {
   10,308 ( 0.00%)              pl.was_dealt_damage_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.damage_taken_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.combat_damage_taken_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.token_copy_replacement_used_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.poison_capped_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.lost_life_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.life_lost_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.creatures_that_damaged_me_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.lands_entered_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creature_spell_countered_by_opponent_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.noncreature_destroyed_by_opponent_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.prowl_types_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.prowl_any_type_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // Veil of Summer's "this turn" riders clear at the turn boundary
        .                       // for every seat (CR 514.2 cleanup-scope grants).
    6,872 ( 0.00%)              pl.next_draw_replacements.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 901.9 — the planar-die roll surcharge resets each turn.
   10,308 ( 0.00%)              pl.planar_die_rolls_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.spells_uncounterable_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creature_spells_uncounterable_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.hexproof_from_colors_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.cast_blue_or_black_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.spell_casts_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.statics_ignored_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.cant_cast_noncreature_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.free_spells_from_hand_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.play_from_graveyard_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.graveyard_bound_exiled_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.silenced_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.warped_spell_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.searched_library_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.shroud_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.cards_to_graveyard_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creature_cards_to_graveyard_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.graveyard_ids_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.descended_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.descend_count_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.discarded_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.permanents_sacrificed_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.artifacts_sacrificed_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 702.179 — Freerunning's combat-damage gate is per-turn.
   10,308 ( 0.00%)              pl.dealt_combat_damage_to_player_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // Quest for Pure Flame's turn-scoped source-damage doubling.
   10,308 ( 0.00%)              pl.double_your_source_damage_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // Turf Wound's land-play lock is turn-scoped.
   10,308 ( 0.00%)              pl.cant_play_lands_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.cant_cast_matching_this_turn.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.cant_activate_nonmana_abilities_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creature_spells_as_flash_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.cast_from_graveyard_top_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              pl.next_spell_uncounterable.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 700.13 — "committed a crime this turn" resets each turn.
   10,308 ( 0.00%)              pl.committed_crime_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 708 — "entered face down / turned face up this turn" resets.
   10,308 ( 0.00%)              pl.face_down_activity_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // Warped Space's once-per-turn free cast from exile.
   10,308 ( 0.00%)              pl.free_exile_cast_used_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creatures_exiled_from_control_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 401.6 — turn-scoped play-from-top permission ends at cleanup.
   10,308 ( 0.00%)              pl.play_from_top_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // Johann's once-per-turn top-of-library cast resets each turn.
   10,308 ( 0.00%)              pl.cast_from_library_top_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // Reset Infusion / "if you gained life this turn" tracking for the
        .                   // active player at the start of their turn. Other players' counters
        .                   // tick down only at their own untaps so symmetric "this turn"
        .                   // checks remain accurate per-player. (Same convention as
        .                   // `lands_played_this_turn` and `spells_cast_this_turn`.)
    5,154 ( 0.00%)          self.players[p].life_gained_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // "For the first time each turn" life-gain gates key on the turn
        .                   // boundary itself, so this flag resets for EVERY player at each
        .                   // untap (unlike the per-player-turn tallies above).
    3,436 ( 0.00%)          for pl in &mut self.players {
   10,308 ( 0.00%)              pl.gained_life_earlier_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // Reset cards-drawn tally for the active player. Powers Quandrix
        .                   // scaling cards (Fractal Anomaly's "X = cards drawn this turn"
        .                   // and similar). Other players' tallies advance independently
        .                   // and are reset on their own untap.
    5,154 ( 0.00%)          self.players[p].cards_drawn_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].last_drawn_card = None;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Reset the per-turn {E}-spent tally (Izzet Generatorium's draw gate).
    5,154 ( 0.00%)          self.players[p].energy_spent_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Reset the "cards left your graveyard this turn" tally; powers
        .                   // Lorehold "if a card left your graveyard this turn" payoffs
        .                   // (Living History, Primary Research, Wilt in the Heat) per turn.
    5,154 ( 0.00%)          self.players[p].cards_left_graveyard_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Reset the "creatures died under your control this turn" tally;
        .                   // powers Witherbloom "if a creature died under your control this
        .                   // turn" end-step payoffs (Essenceknit Scholar).
    5,154 ( 0.00%)          self.players[p].creatures_died_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   self.creature_deaths_this_turn.clear();
    5,154 ( 0.00%)          self.players[p].zuberas_died_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Reset the Revolt (CR 702.139) "permanent left the battlefield under
        .                   // your control this turn" flag for the active player.
    5,154 ( 0.00%)          self.players[p].permanent_left_battlefield_this_turn = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // EOE Void — reset the game-wide "a nonland permanent left this turn"
        .                   // flag at the turn boundary.
    1,718 ( 0.00%)          self.nonland_permanent_left_bf_this_turn = false;
        .                   // Reset the "cards exiled this turn" tally; powers Strixhaven
        .                   // "if one or more cards were put into exile this turn" payoffs
        .                   // (Ennis the Debate Moderator) per turn.
    5,154 ( 0.00%)          self.players[p].cards_exiled_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].cards_discarded_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // CR 609.4b — North Star's permission lasts the turn, for every seat
        .                   // that was granted one.
    3,436 ( 0.00%)          for pl in self.players.iter_mut() {
   10,308 ( 0.00%)              pl.may_spend_any_color_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // Reset per-spell-type tallies (instant/sorcery vs creature
        .                   // casts). These refine `spells_cast_this_turn` for cards that
        .                   // need exact-type filtering (Potioner's Trove "instant or
        .                   // sorcery only" gate, future Magecraft variants).
    5,154 ( 0.00%)          self.players[p].instants_or_sorceries_cast_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // One-shot IS-spell discounts are keyed off that tally, so they must
        .                   // be cleared in lockstep with it (a stale `granted_at == 0` entry
        .                   // would otherwise re-match after the reset).
    3,436 ( 0.00%)          self.players[p].pending_is_discounts.clear();
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    3,436 ( 0.00%)          self.players[p].pending_spell_discounts.clear();
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
    5,154 ( 0.00%)          self.players[p].creatures_cast_this_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Clear Teferi, Time Raveler's "you may cast sorceries as though they
        .                   // had flash" flag — it expires on the start of your next turn.
    5,154 ( 0.00%)          self.players[p].sorceries_as_flash = false;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Clear "this turn" lifegain locks across **every player** — CR
        .                   // "this turn" means the current turn, so a Skullcrack-style
        .                   // lock set during the previous turn expires before priority
        .                   // hits the new active player. Scoped wider than the per-player
        .                   // counters above because the lock applies to whichever player
        .                   // was targeted, not to the active player.
        .                   for q in 0..self.players.len() {
    6,872 ( 0.00%)              self.players[q].cannot_gain_life_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              self.players[q].life_locked_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // CR 104.3d — Angel's Grace's protections end with the turn.
    6,872 ( 0.00%)              self.players[q].cant_lose_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              self.players[q].damage_floor_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // Forbidding Spirit's "until your next turn" attack tax expires when
        .                   // the taxed player's own turn begins.
    6,872 ( 0.00%)          self.players[self.active_player_idx].attack_tax_until_your_turn = 0;
   51,540 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (1,718x)
        .                   // Deep Water / Dark Sphere / Blood of the Martyr all expire with the
        .                   // turn they were made in.
        .                   for pl in &mut self.players {
   10,308 ( 0.00%)              pl.lands_produce_color_this_turn = None;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.half_damage_shields = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.damage_mirrors = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.creature_damage_to_you_this_turn = false;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // CR 702.108 — fire "becomes untapped" (Inspired) triggers for every
        .                   // permanent that flipped tapped→untapped this step.
    3,436 ( 0.00%)          if !untapped_now.is_empty() {
    2,500 ( 0.00%)              let events: Vec<GameEvent> = untapped_now
        .                           .into_iter()
        .                           .map(|card_id| GameEvent::PermanentUntapped { card_id })
        .                           .collect();
    5,000 ( 0.00%)              self.dispatch_triggers_for_events(&events);
5,683,522 ( 0.18%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (1,250x)
        .                   }
   13,744 ( 0.00%)      }
        .           
        .               /// CR 514 cleanup step turn-based actions (514.1 discard-down, 514.2
        .               /// wear-off, 514.3 priority check). See `CleanupOutcome` for how callers
        .               /// continue.
   15,462 ( 0.00%)      pub fn do_cleanup(&mut self, events: &mut Vec<GameEvent>) -> CleanupOutcome {
        .                   // CR 514.1 — First, if the active player's hand contains more cards
        .                   // than their maximum hand size (normally seven), they discard
        .                   // enough cards to reduce their hand size to that number. This
        .                   // turn-based action doesn't use the stack.
        .                   //
        .                   // For a `wants_ui` player we surface an interactive
        .                   // `Decision::Discard` so the player picks which cards to pitch (and
        .                   // suspend). For non-UI seats (tests, the bot's AutoDecider fallback)
        .                   // we keep the deterministic first-card dump, routed through the
        .                   // centralized `discard_card` path so the discard fires
        .                   // `CardDiscarded` (CR 514.3 lets discard-matters triggers fire from
        .                   // cleanup) and honors Madness (CR 702.35).
    1,718 ( 0.00%)          let active = self.active_player_idx;
        .                   // CR 402.2 — "Each player's maximum hand size is normally seven
        .                   // cards. A player may have any number of cards in their hand, but as
        .                   // part of their cleanup step, the player must discard excess cards
        .                   // down to the maximum hand size." `Player.max_hand_size` is `None`
        .                   // for "no maximum hand size" effects (skip entirely) and `Some(n)`
        .                   // otherwise (discard down to `n`).
    6,872 ( 0.00%)          if let Some(max) = self.effective_max_hand_size(active)
2,339,194 ( 0.08%)  => /home/user/Crabomination/crabomination/src/game/actions.rs:crabomination::game::actions::<impl crabomination::game::GameState>::effective_max_hand_size (1,718x)
    6,872 ( 0.00%)              && self.players[active].hand.len() > max
        .                   {
        .                       if self.players[active].wants_ui {
        .                           let excess = (self.players[active].hand.len() - max) as u32;
        .                           self.set_cleanup_discard_decision(active, excess);
        .                           return CleanupOutcome::Suspended;
        .                       }
        .                       let mut cleanup_events = Vec::new();
        .                       let mut discarded = 0u32;
-- line 3621 ----------------------------------------
-- line 3634 ----------------------------------------
        .                       }
        .                       if !cleanup_events.is_empty() {
        .                           // Dispatched here only — the caller re-dispatches whatever it
        .                           // returns, so appending these would double-fire the triggers.
        .                           self.dispatch_triggers_for_events(&cleanup_events);
        .                       }
        .                   }
        .           
    5,154 ( 0.00%)          self.finish_cleanup(events)
120,535,200 ( 3.87%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::finish_cleanup (1,718x)
   13,744 ( 0.00%)      }
        .           
        .               /// Pose the CR 514.1 cleanup discard-down as an interactive decision:
        .               /// the active player picks exactly `excess` cards from hand to discard.
        .               pub(crate) fn set_cleanup_discard_decision(&mut self, player: usize, excess: u32) {
        .                   let hand: Vec<(CardId, String)> = self.players[player]
        .                       .hand
        .                       .iter()
        .                       .map(|c| (c.id, c.definition.name.to_string()))
-- line 3651 ----------------------------------------
-- line 3654 ----------------------------------------
        .                       decision: Decision::Discard { player, count: excess, hand },
        .                       resume: crate::game::types::ResumeContext::CleanupDiscard { player },
        .                   });
        .               }
        .           
        .               /// CR 514.2 onward — the part of cleanup that runs after the discard-down
        .               /// step (which may have suspended for a UI player): wear-off, then the
        .               /// CR 514.3 priority check, then (if nothing is pending) the turn end.
   15,462 ( 0.00%)      pub(crate) fn finish_cleanup(&mut self, events: &mut Vec<GameEvent>) -> CleanupOutcome {
        .                   self.cleanup_wear_off();
        .                   // CR 514.3a — check state-based actions and triggered abilities. If
        .                   // anything is waiting, players receive priority in the cleanup step;
        .                   // once they all pass with an empty stack, another cleanup happens.
    3,436 ( 0.00%)          let mut sba = self.check_state_based_actions();
34,412,626 ( 1.10%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::check_state_based_actions (1,718x)
        .                   events.append(&mut sba);
   10,308 ( 0.00%)          if !self.stack.is_empty() || self.pending_decision.is_some() {
        .                       if self.pending_decision.is_none() {
        .                           self.give_priority_to_active();
        .                       }
        .                       return CleanupOutcome::PriorityGranted;
        .                   }
        .                   // CR 514.3 — normally no player receives priority during cleanup;
        .                   // the turn simply ends.
        .                   self.end_turn();
        .                   CleanupOutcome::TurnOver
   15,462 ( 0.00%)      }
        .           
        .               /// CR 514.2 — the cleanup wear-off: clears "until end of turn" / "this
        .               /// turn" state and removes marked damage.
        .               fn cleanup_wear_off(&mut self) {
        .                   // Bubbling Muck's floating "Swamps add an extra {B}" grant is a
        .                   // "until end of turn" effect (CR 514.2).
        .                   self.extra_mana_on_land_tap_this_turn.clear();
        .                   // CR 514.2 — Second, the following actions happen simultaneously:
        .                   // all damage marked on permanents is removed and all "until end of
        .                   // turn" and "this turn" effects end.
        .                   // Clear temporary pump effects (CardInstance-level bonuses still used
        .                   // as base). Phased-out permanents are still on the battlefield for
        .                   // this purpose (CR 702.26e), so they expire on the same schedule.
    6,872 ( 0.00%)          for card in self.battlefield.iter_mut().chain(self.phased_out.iter_mut()) {
   32,478 ( 0.00%)              card.clear_end_of_turn_effects();
2,538,373 ( 0.08%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::clear_end_of_turn_effects (32,478x)
        .                   }
        .                   // Until-end-of-turn flashback grants (SOS "Flashback") live on
        .                   // graveyard cards, which `clear_end_of_turn_effects` above doesn't
        .                   // reach — expire them here so the window closes at end of turn.
        .                   for player in &mut self.players {
   16,154 ( 0.00%)              for card in &mut player.graveyard {
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   22,680 ( 0.00%)                  card.granted_flashback_eot = None;
   22,680 ( 0.00%)                  card.granted_harmonize_eot = None;
        .                       }
        .                       // "[Filter] spells cost {N} less this turn" grants end (CR 514.2).
    6,872 ( 0.00%)              player.turn_spell_discounts.clear();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              player.face_down_discount_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                       // "Until end of turn" +1/+1 counter bonus (Prairie Dog) ends.
   10,308 ( 0.00%)              player.extra_plus_one_counters_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              player.extra_etb_p1p1_counters_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // Expire UntilEndOfTurn continuous effects from the layer system
        .                   self.expire_end_of_turn_effects();
        .                   // Snap control of EOT-stolen permanents (Act of Treason / Threaten)
        .                   // back to their pre-steal controllers (CR 800.4).
    8,590 ( 0.00%)          self.revert_temporary_control(&[
  187,262 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_control (1,718x)
        .                       crate::effect::Duration::EndOfTurn,
        .                       crate::effect::Duration::UntilNextTurn,
        .                   ]);
        .                   // CR 707 — "becomes a copy ... until end of turn" swaps snap back.
    6,872 ( 0.00%)          self.revert_temporary_copies(&[
  274,880 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::revert_temporary_copies (1,718x)
        .                       crate::effect::Duration::EndOfTurn,
        .                       crate::effect::Duration::UntilNextTurn,
        .                   ]);
        .                   // CR 702.143b — foretold-this-turn cards become castable next turn.
        .                   // Autumn Willow's "until end of turn" shroud waiver (CR 514.2).
        .                   self.shroud_waivers.clear();
        .                   self.abilities_locked_this_turn.clear();
        .                   self.foretold_this_turn.clear();
-- line 3728 ----------------------------------------
-- line 3730 ----------------------------------------
        .                   self.entered_from_graveyard_this_turn.clear();
        .                   self.entered_from_exile_this_turn.clear();
        .                   // CR 603.3d — "triggers only once each turn" abilities reset.
        .                   self.triggered_once_per_turn_used.clear();
        .                   self.per_subject_trigger_uses.clear();
        .                   // Single Combat — the creature/planeswalker cast lock ends at the end
        .                   // of the registerer's first turn strictly after it was registered
        .                   // ("until the end of your next turn").
    3,436 ( 0.00%)          let (active, turn) = (self.active_player_idx, self.turn_number);
        .                   self.creature_pw_cast_locks.retain(|(reg, t)| !(*reg == active && turn > *t));
        .                   // CR 505.1b — discard any unconsumed additional combat phases so they
        .                   // don't bleed into the next turn (e.g. the turn ended before combat).
        .                   self.additional_combat_phases = 0;
    1,718 ( 0.00%)          self.combat_chooser = None;
    6,872 ( 0.00%)          self.additional_post_main_combats = 0;
        .                   self.combat_phases_this_turn = 0;
    1,718 ( 0.00%)          self.permanents_enter_tapped_this_turn = false;
        .                   self.additional_end_steps = 0;
        .                   self.end_steps_this_turn = 0;
        .                   self.additional_upkeep_steps = 0;
        .                   self.upkeep_steps_this_turn = 0;
        .                   self.graveyard_from_battlefield_this_turn.clear();
        .                   // CR 514.2 — all damage marked on permanents is removed, phased-out
        .                   // ones included (they're treated as nonexistent, not as gone).
   15,842 ( 0.00%)          for card in self.battlefield.iter_mut().chain(self.phased_out.iter_mut()) {
   32,478 ( 0.00%)              card.damage = 0;
        .                   }
        .                   // Clear the per-turn "permanents gained a counter this turn"
        .                   // tracker (used by Fractal Tender's end-step trigger). Resetting
        .                   // at cleanup is the canonical "until end of turn" scope.
        .                   self.permanents_gained_counter_this_turn.clear();
        .                   self.permanents_amplified_counter_this_turn.clear();
        .                   // CR 603-style "Nth time this turn" escalation counters reset.
        .                   self.ability_resolutions_this_turn.clear();
-- line 3763 ----------------------------------------
-- line 3768 ----------------------------------------
        .                   // (Wilt in the Heat).
        .                   self.dies_to_exile_eot.clear();
        .                   self.damage_exiles_victim_eot.clear();
        .                   self.damage_denies_regen_eot.clear();
        .                   // Expire event-keyed "when [card] dies this turn" delayed triggers
        .                   // that never fired (CR 603.4 — the "this turn" window closes). A
        .                   // watcher carrying an explicit `expires_after_turn` runs on its own
        .                   // clock instead ("until the end of your next turn").
    3,436 ( 0.00%)          let turn = self.turn_number;
    6,872 ( 0.00%)          self.delayed_triggers.retain(|dt| match dt.expires_after_turn {
   30,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (1,718x)
        .                       Some(last) => turn < last,
        .                       None => true,
        .                   });
    3,436 ( 0.00%)          self.delayed_triggers.retain(|dt| {
   30,924 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs:alloc::vec::Vec<T,A>::retain (1,718x)
        .                       dt.expires_after_turn.is_some()
        .                           || !matches!(
        .                               dt.kind,
        .                               crate::game::types::DelayedKind::WhenCardDies(_)
        .                                   | crate::game::types::DelayedKind::WhenCardLeavesBattlefieldThisTurn(_)
        .                                   | crate::game::types::DelayedKind::CreatureYouControlEntersThisTurn
        .                                   | crate::game::types::DelayedKind::CreatureYouControlDiesThisTurn
        .                                   | crate::game::types::DelayedKind::MatchingCreatureDiesThisTurn(_)
-- line 3789 ----------------------------------------
-- line 3798 ----------------------------------------
        .                                   | crate::game::types::DelayedKind::YouGainLifeThisTurn
        .                                   | crate::game::types::DelayedKind::CardEntersOpponentGraveyardThisTurn
        .                                   | crate::game::types::DelayedKind::OpponentCausesYouToDiscardThisTurn
        .                           )
        .                   });
        .                   // CR 514.2 / CR 615.1 — "this turn" combat damage prevention
        .                   // (Owlin Shieldmage's ETB, Holy Day-style fogs) expires at
        .                   // cleanup along with the other until-end-of-turn flags.
    1,718 ( 0.00%)          self.prevent_combat_damage_this_turn = false;
    3,436 ( 0.00%)          self.prevent_combat_damage_except = None;
        .                   self.combat_damage_prevented_creatures.clear();
        .                   self.combat_damage_prevented_to_this_turn.clear();
        .                   self.combat_damage_prevented_by_this_turn.clear();
        .                   self.all_damage_prevented_by_this_turn.clear();
    1,718 ( 0.00%)          self.life_gain_punish_this_turn = 0;
        .                   self.draws_redirected_this_turn.clear();
    1,718 ( 0.00%)          self.damage_becomes_this_turn = None;
        .                   self.combat_damage_prevented_to_players_this_turn.clear();
       40 ( 0.00%)          self.auras_at_death.clear();
        .                   self.creature_etb_steal_this_turn.clear();
        .                   self.search_tax_paid_this_turn.clear();
        .                   self.damage_prevented_sources.clear();
        .                   self.land_mana_replacements_this_turn.retain(|r| r.indefinite);
        .                   self.targeting_damage_prevented_this_turn.clear();
        .                   self.colored_mana_becomes_this_turn.clear();
        .                   self.blocks_declared_this_turn.clear();
        .                   self.turn_granted_triggers.clear();
        .                   self.cant_block_pairs.clear();
        .                   self.cant_block_this_turn.clear();
        .                   self.attack_despite_defender_this_turn.clear();
        .                   // CR 615 — prevention shields and the "can't be prevented" rider
        .                   // are "this turn" effects; they expire at cleanup too.
        .                   self.prevention_shields.clear();
    1,718 ( 0.00%)          self.damage_cant_be_prevented_this_turn = false;
    1,718 ( 0.00%)          self.attack_tax_this_turn = 0;
        .                   self.block_tax_this_turn = 0;
        .                   self.damage_redirect_this_turn.clear();
        .                   self.next_damage_redirect.clear();
        .                   self.turn_damage_redirect.clear();
        .                   self.next_combat_damage_to_controller.clear();
        .                   self.next_combat_damage_redirect.clear();
        .                   self.spell_damage_to_controller.clear();
        .                   self.sorcery_damage_this_turn.clear();
        .                   self.artifact_damage_to_players_this_turn.clear();
        .                   self.combat_damage_redirect_this_turn.clear();
        .                   self.doubled_damage_sources_this_turn.clear();
        .                   self.assigns_no_combat_damage_this_turn.clear();
    1,718 ( 0.00%)          self.creature_combat_damage_doublers = 0;
        .                   self.damage_sources_this_turn.clear();
        .                   self.noncombat_damage_bonus_this_turn.clear();
        .                   // Desperate Gambit's unspent doubler expires with the turn.
        .                   self.double_next_damage_from.clear();
    1,718 ( 0.00%)          self.damaged_creatures_die_this_turn = false;
        .                   self.creature_deaths_drain_toughness_this_turn = false;
    1,718 ( 0.00%)          self.no_search_this_turn = false;
        .                   self.skipped_steps_this_turn.clear();
        .                   self.cant_attack_player_this_turn.clear();
    1,718 ( 0.00%)          self.graveyard_play_pooled_for = None;
    1,718 ( 0.00%)          self.block_poison_this_turn = 0;
        .                   // CR 500.4 — "kept this turn" mana (Savage Ventmaw) expires now, so the
        .                   // final empty of the turn actually removes it.
        .                   for p in self.players.iter_mut() {
   10,308 ( 0.00%)              p.kept_mana_this_turn.empty();
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   65,284 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/mana.rs:crabomination_base::mana::ManaPool::empty (3,436x)
        .                   }
        .                   // Empty mana pools (Kruphix converts to colorless instead).
    5,154 ( 0.00%)          self.empty_mana_pools();
  213,032 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::empty_mana_pools (1,718x)
        .               }
        .           
        .               /// The end of the turn: consume extra turns / skip-turn debt, advance the
        .               /// active player and turn number, and sweep expired play permissions.
        .               fn end_turn(&mut self) {
        .                   // Rotate the per-turn creature-entry log (Ephara reads "last turn"
        .                   // at each upkeep, so the rotation is per game turn, not per player).
        .                   for pl in &mut self.players {
   27,488 ( 0.00%)              pl.creatures_entered_last_turn = std::mem::take(&mut pl.creatures_entered_this_turn);
  206,160 ( 0.01%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (6,872x)
   10,308 ( 0.00%)              pl.artifacts_entered_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.planeswalkers_entered_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.nonland_permanents_entered_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
   10,308 ( 0.00%)              pl.mounts_vehicles_entered_this_turn = 0;
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   // CR 500.7 — extra turns. If the active player banked an extra
        .                   // turn (Time Walk, Ral Zarek's -7 emblem), keep the turn instead
        .                   // of passing: consume one charge and just bump the turn number.
    1,718 ( 0.00%)          let active = self.active_player_idx;
        .                   // Remember the just-ended turn's active player for the CR 502.2
        .                   // day/night turn-based check at the next untap.
    3,436 ( 0.00%)          self.previous_turn_active = Some(active);
    8,590 ( 0.00%)          if self.players[active].is_alive() && self.players[active].extra_turns > 0 {
        .                       self.players[active].extra_turns -= 1;
        .                       self.current_turn_is_extra = true;
        .                       self.turn_number += 1;
        .                   } else {
    5,154 ( 0.00%)              self.current_turn_is_extra = false;
        .                       // Advance to the next non-eliminated player's turn (TurnStarted
        .                       // fires on Untap entry). If the next player has pending skip
        .                       // turns (Ral Zarek's -7), decrement and skip past them — keep
        .                       // walking until we find a player with no skip-turn debt.
        .                       // Safety cap at `players.len()` iterations to avoid an
        .                       // infinite loop in pathological "everyone skips" scenarios.
        .                       let n_players = self.players.len();
        .                       for _ in 0..n_players.max(1) {
    3,436 ( 0.00%)                  self.active_player_idx = self.next_alive_seat(self.active_player_idx);
    6,872 ( 0.00%)                  self.turn_number += 1;
    3,436 ( 0.00%)                  let skipped = self.players[self.active_player_idx].skip_turns;
    5,154 ( 0.00%)                  if skipped == 0 {
        .                               break;
        .                           }
        .                           self.players[self.active_player_idx].skip_turns = skipped - 1;
        .                           // Loop again — the current player's turn was just consumed
        .                           // by the skip and we advance to the next.
        .                       }
        .                   }
        .                   // Sweep expired `may_play_until` permissions across every zone.
-- line 3910 ----------------------------------------
-- line 3913 ----------------------------------------
        .                   // completed. EndOfThisTurn → expires after one bump (elapsed
        .                   // ≥ 1). EndOfControllersNextTurn → expires after one full
        .                   // controller-turn loop (elapsed ≥ player_count) — in a 2p game
        .                   // that's 2 turn bumps later, i.e. the controller's *next*
        .                   // cleanup.
        .                   let player_count = self.players.len() as u32;
        .                   let turn_number = self.turn_number;
        .                   let sweep = |c: &mut crate::card::CardInstance| {
  285,520 ( 0.01%)              if let Some(perm) = c.may_play_until {
        .                           let elapsed = turn_number.saturating_sub(perm.granted_turn);
        .                           let expired = match perm.duration {
        .                               crate::card::MayPlayDuration::EndOfThisTurn => elapsed >= 1,
        .                               crate::card::MayPlayDuration::EndOfControllersNextTurn => {
        .                                   elapsed >= player_count.max(1)
        .                               }
        .                               crate::card::MayPlayDuration::WhileExiled => false,
        .                               // Step-bounded miracle windows are also dead by turn end.
-- line 3929 ----------------------------------------
-- line 3932 ----------------------------------------
        .                           if expired {
        .                               c.may_play_until = None;
        .                               // The miracle alt-cost shares the permission's lifetime.
        .                               c.granted_alt_cast_cost_eot = None;
        .                           }
        .                       }
        .                   };
        .                   for c in self.battlefield.iter_mut() { sweep(c); }
    1,718 ( 0.00%)          for c in self.exile.iter_mut() { sweep(c); }
        .                   for p in self.players.iter_mut() {
    6,872 ( 0.00%)              for c in p.hand.iter_mut() { sweep(c); }
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              for c in p.graveyard.iter_mut() { sweep(c); }
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
    6,872 ( 0.00%)              for c in p.library.iter_mut() { sweep(c); }
  103,080 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,436x)
        .                   }
        .                   self.give_priority_to_active();
        .               }
        .           
        .               // ── State-based actions ───────────────────────────────────────────────────
        .           
        .               /// CR 701.15 — apply a regeneration shield: remove one shield, tap the
        .               /// permanent, remove it from combat (as both attacker and blocker), and
-- line 3952 ----------------------------------------
-- line 3983 ----------------------------------------
        .               }
        .           
        .               /// CR 800.4a — handle a player leaving the game: all cards/tokens they
        .               /// own leave with them (every zone), and permanents they controlled but
        .               /// don't own revert to their owners' control. Objects leaving this way
        .               /// are removed directly (not via the death/exile pipelines) since a
        .               /// departing player's objects "cease to exist" rather than being
        .               /// destroyed or sacrificed.
    1,840 ( 0.00%)      pub(crate) fn objects_leave_with_player(&mut self, p: usize) {
    4,056 ( 0.00%)          self.battlefield.retain(|c| c.owner != p);
      368 ( 0.00%)          let reverts: Vec<(CardId, usize)> = self
        .                       .battlefield
        .                       .iter()
    5,850 ( 0.00%)              .filter(|c| c.controller == p)
        .                       .map(|c| (c.id, c.owner))
        .                       .collect();
      184 ( 0.00%)          for (id, owner) in reverts {
        .                       self.change_control(id, owner); // control-changing effects end
        .                   }
      318 ( 0.00%)          self.exile.retain(|c| c.owner != p);
      368 ( 0.00%)          self.players[p].hand.clear();
    5,520 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (184x)
      368 ( 0.00%)          self.players[p].library.clear();
    5,520 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (184x)
      368 ( 0.00%)          self.players[p].graveyard.clear();
    5,520 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (184x)
        .                   // CR 725.4 — if the monarch leaves the game, the active player
        .                   // becomes the monarch; if the active player is the one leaving (or
        .                   // there is no active player), the next player in turn order who can
        .                   // still become the monarch does.
        .                   if self.monarch == Some(p) {
        .                       self.monarch = if self.active_player_idx != p && !self.players[self.active_player_idx].eliminated {
        .                           Some(self.active_player_idx)
        .                       } else {
-- line 4013 ----------------------------------------
-- line 4027 ----------------------------------------
        .                           Some(self.active_player_idx)
        .                       } else {
        .                           let n = self.players.len();
        .                           (1..n)
        .                               .map(|off| (self.active_player_idx + off) % n)
        .                               .find(|&q| q != p && !self.players[q].eliminated)
        .                       };
        .                   }
    1,472 ( 0.00%)      }
        .           
        .           
        .               /// CR 704.5u / 702.158c — while a space-sculptor permanent is on the
        .               /// battlefield, every creature without a sector designation gets one; its
        .               /// controller chooses, opponents of the sculptor's controller first. When
        .               /// the last sculptor leaves, the designations clear (CR 702.158b).
        .               /// Auto-assignment spreads a player's creatures round-robin.
        .               pub(crate) fn assign_sectors(&mut self) {
        .                   use crate::card::{Keyword, Sector};
        .                   // Hot path: this runs on every SBA check, so bail before allocating
        .                   // when no sculptor is out and nothing carries a designation.
        .                   let mut sculptors: Vec<usize> = Vec::new();
        .                   let mut any_sector = false;
        .                   let mut any_unassigned = false;
   21,340 ( 0.00%)          for c in &self.battlefield {
        .                       if c.definition.keywords.contains(&Keyword::SpaceSculptor)
        .                           && !sculptors.contains(&c.controller)
        .                       {
        .                           sculptors.push(c.controller);
        .                       }
        .                       any_sector |= c.sector.is_some();
  762,464 ( 0.02%)              any_unassigned |= c.sector.is_none() && c.definition.is_creature();
2,932,508 ( 0.09%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (190,616x)
        .                   }
   21,340 ( 0.00%)          if sculptors.is_empty() {
   21,340 ( 0.00%)              if any_sector {
        .                           for c in &mut self.battlefield {
        .                               c.sector = None;
        .                           }
        .                       }
        .                       return;
        .                   }
        .                   if !any_unassigned {
        .                       return;
-- line 4068 ----------------------------------------
-- line 4086 ----------------------------------------
        .               }
        .           
        .               /// CR 613 layer 1 — keep every `copies_top_graveyard_creature` permanent
        .               /// (Volrath's Shapeshifter) matched to the top creature card of its
        .               /// controller's graveyard, reverting to its printed self when the top card
        .               /// isn't a creature. The printed definition lives on a `shapeshifter`
        .               /// `TempCopy` entry, so a snapshot round-trip recovers it by name.
        .               fn sync_graveyard_shapeshifters(&mut self) {
   10,670 ( 0.00%)          let ids: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
        .                       .filter(|c| {
  381,232 ( 0.01%)                  c.definition.copies_top_graveyard_creature
        .                               || self
        .                                   .temporary_copies
        .                                   .iter()
        .                                   .any(|tc| tc.card == c.id && tc.shapeshifter)
        .                       })
        .                       .map(|c| c.id)
        .                       .collect();
        .                   for id in ids {
-- line 4106 ----------------------------------------
-- line 4163 ----------------------------------------
        .                   }
        .               }
        .           
        .               /// One battlefield pass answering "is this SBA's shape present at all?"
        .               /// for every rare state-based action, so the sweep pays one walk instead
        .               /// of ~20 `filter(...).collect()` passes that come back empty. Every flag
        .               /// is an over-approximation of its block's own filter: false means the
        .               /// block provably does nothing, true re-runs the original code unchanged.
   74,690 ( 0.00%)      fn sba_board_scan(&self) -> SbaBoardScan {
        .                   use crate::card::{CounterType, Keyword, Supertype};
        .                   use crate::effect::StaticEffect;
        .                   let mut s = SbaBoardScan::default();
        .                   for c in self.battlefield.iter() {
        .                       let d = &c.definition;
  211,956 ( 0.01%)              s.flip_keyword |= d.flip_when_has_keyword.is_some() && !c.flipped;
  391,902 ( 0.01%)              s.flip_predicate |= d.flip_when_predicate.is_some() && !c.flipped;
   10,670 ( 0.00%)              s.sacrifice_when |= d.sacrifice_when.is_some();
   21,340 ( 0.00%)              s.state_trigger |= d.state_trigger.is_some();
  391,902 ( 0.01%)              s.steal_penalty |= d.sacrifice_and_burn_when_stolen.is_some() && c.controller != c.owner;
   10,670 ( 0.00%)              s.no_other |= d.sacrifice_when_you_control_no_other.is_some();
   10,670 ( 0.00%)              s.max_counters |= d.max_counters_of_kind.is_some();
   21,340 ( 0.00%)              s.saga |= !d.saga_chapters.is_empty();
  211,956 ( 0.01%)              s.bestowed |= c.bestowed;
  201,286 ( 0.01%)              s.soulbond |= c.soulbond_partner.is_some();
        .                       // One walk per vector rather than one per flag.
1,334,312 ( 0.04%)              if !c.counters.is_empty() {
  138,710 ( 0.00%)                  s.pm_both |= c.counter_count(CounterType::PlusOnePlusOne) > 0
        .                               && c.counter_count(CounterType::MinusOneMinusOne) > 0;
        .                       }
        .                       for kw in d.keywords.iter() {
  711,572 ( 0.02%)                  match kw {
        .                               Keyword::Persist | Keyword::Undying => s.persist_undying = true,
        .                               Keyword::StartYourEngines => s.start_engines = true,
        .                               _ => {}
        .                           }
        .                       }
        .                       for st in d.supertypes.iter() {
1,625,338 ( 0.05%)                  match st {
        .                               Supertype::Legendary => s.legendary = true,
        .                               Supertype::World => s.world = true,
        .                               _ => {}
        .                           }
        .                       }
  190,616 ( 0.01%)              for t in d.card_types.iter() {
  953,080 ( 0.03%)                  match t {
        .                               crate::card::CardType::Planeswalker => s.planeswalker = true,
        .                               crate::card::CardType::Battle => s.battle |= d.defense > 0,
        .                               _ => {}
        .                           }
        .                       }
        .                       for es in d.subtypes.enchantment_subtypes.iter() {
   33,678 ( 0.00%)                  match es {
        .                               crate::card::EnchantmentSubtype::Aura => s.aura = true,
        .                               crate::card::EnchantmentSubtype::Role => s.role = true,
        .                               _ => {}
        .                           }
        .                       }
  461,332 ( 0.01%)              s.equipment_attached |= c.attached_to.is_some() && d.is_equipment();
   33,678 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_equipment (3,742x)
        .                       for sa in &d.static_abilities {
        .                           match sa.effect {
        .                               StaticEffect::AllNonlandPermanentsAreLegendary => s.supertype_grant = true,
        .                               StaticEffect::LegendRuleDoesntApply => s.legend_rule_off = true,
        .                               StaticEffect::LethalDamageByPower { .. } => s.lethal_by_power = true,
        .                               _ => {}
        .                           }
        .                       }
        .                   }
        .                   // CR 704.5j — the Ring's emblem grants the supertype without a
        .                   // battlefield source.
   64,020 ( 0.00%)          s.supertype_grant |= self.players.iter().any(|p| p.ring_temptations >= 1);
        .                   s
   85,360 ( 0.00%)      }
        .           
  170,720 ( 0.01%)      pub fn check_state_based_actions(&mut self) -> Vec<GameEvent> {
        .                   let mut events = vec![];
        .           
        .                   self.assign_sectors();
        .           
        .                   // CR 904.10 — a face-up non-ongoing scheme is abandoned once no scheme
        .                   // trigger is left on the stack.
        .                   self.sweep_finished_schemes();
        .                   // CR 704.6f — a face-up phenomenon whose encounter trigger has left the
-- line 4244 ----------------------------------------
-- line 4245 ----------------------------------------
        .                   // stack makes its controller planeswalk.
        .                   self.sweep_finished_phenomena();
        .           
        .                   self.sync_graveyard_shapeshifters();
        .           
        .                   // One pass answering "which of the rare SBAs can fire on this board".
        .                   // Retaken below wherever the sweep can change the answer (a flip swaps
        .                   // a definition; Persist/Undying puts a permanent back).
   10,670 ( 0.00%)          let mut scan = self.sba_board_scan();
23,915,320 ( 0.77%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::sba_board_scan (10,670x)
        .                   let mut flipped = false;
        .           
        .                   // CR 603.8 — state-triggered flip (Student of Elements: "When this
        .                   // creature has flying, flip it"). Cheap guard so the common board pays
        .                   // nothing; only compute the layer view when an unflipped state-flip
        .                   // card is present, then flip any whose *computed* keywords now satisfy
        .                   // the condition. Flipping clears the condition, so it fires once.
   21,340 ( 0.00%)          if scan.flip_keyword {
        .                       let computed = self.compute_battlefield();
        .                       let to_flip: Vec<CardId> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter_map(|c| {
        .                               let kw = c.definition.flip_when_has_keyword.as_ref()?;
        .                               let has = computed.iter().find(|p| p.id == c.id)?.keywords.contains(kw);
        .                               (has && !c.flipped).then_some(c.id)
-- line 4269 ----------------------------------------
-- line 4273 ----------------------------------------
        .                           self.flip_permanent(id, &mut events);
        .                           flipped = true;
        .                       }
        .                   }
        .           
        .                   // CR 603.8 — state-triggered flip on a board predicate (Rune-Tail's
        .                   // "when you have 30 or more life"). Same once-only shape as the
        .                   // keyword variant above.
   21,340 ( 0.00%)          if scan.flip_predicate {
        .                       let flip_when: Vec<(CardId, usize)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.definition.flip_when_predicate.is_some() && !c.flipped)
        .                           .map(|c| (c.id, c.controller))
        .                           .collect();
        .                       for (id, ctrl) in flip_when {
        .                           let Some(pred) =
-- line 4289 ----------------------------------------
-- line 4302 ----------------------------------------
        .                   // A flip swaps the permanent's definition, so every flag below is
        .                   // re-read from the post-flip board.
        .                   if flipped {
        .                       scan = self.sba_board_scan();
        .                   }
        .           
        .                   // CR 603.8 — "when [condition], sacrifice this" state trigger
        .                   // (Phylactery Lich). Evaluated from each carrier's own controller.
   32,010 ( 0.00%)          if scan.sacrifice_when {
        .                       let sacrifice_when: Vec<(CardId, usize)> = self
        .                           .battlefield
        .                           .iter()
        .                           .filter(|c| c.definition.sacrifice_when.is_some())
        .                           .map(|c| (c.id, c.controller))
        .                           .collect();
        .                       for (id, ctrl) in sacrifice_when {
        .                           let Some(pred) =
-- line 4318 ----------------------------------------
-- line 4325 ----------------------------------------
        .                               self.sacrifice_one(id, ctrl, &mut events);
        .                           }
        .                       }
        .                   }
        .           
        .                   // CR 603.8 — the general state trigger: "When [condition], [effect]."
        .                   // Latched per permanent so it goes on the stack once while the
        .                   // condition holds and re-arms once it's false again.
   21,340 ( 0.00%)          let state_triggers: Vec<(CardId, usize)> = if scan.state_trigger {
        .                       self.battlefield
        .                           .iter()
        .                           .filter(|c| c.definition.state_trigger.is_some())
        .                           .map(|c| (c.id, c.controller))
        .                           .collect()
        .                   } else {
        .                       Vec::new()
        .                   };
-- line 4341 ----------------------------------------
-- line 4359 ----------------------------------------
        .                           );
        .                       }
        .                   }
        .           
        .                   // CR 603.8 — "when a player other than its owner controls it" state
        .                   // trigger (Bronze Bombshell): that player sacrifices it, then it deals
        .                   // N damage to them. Latched in `steal_penalty_armed` so it fires once
        .                   // per control change; the latch clears when control returns to owner.
   21,340 ( 0.00%)          let steal_penalties: Vec<(CardId, usize, u32)> = if scan.steal_penalty {
        .                       self.battlefield
        .                           .iter()
        .                           .filter_map(|c| {
        .                               let dmg = c.definition.sacrifice_and_burn_when_stolen?;
        .                               (c.controller != c.owner).then_some((c.id, c.controller, dmg))
        .                           })
        .                           .collect()
        .                   } else {
        .                       Vec::new()
        .                   };
        .                   // Only an *armed* id can be disarmed, so walk the latch set rather
        .                   // than the whole battlefield — the old loop allocated a Vec of every
        .                   // id and did an O(n) `battlefield_find` per id to reach a `remove`
        .                   // that is a no-op on the empty set every ordinary board carries.
   53,350 ( 0.00%)          if !self.steal_penalty_armed.is_empty() {
        .                       let disarm: Vec<CardId> = self
        .                           .steal_penalty_armed
        .                           .iter()
        .                           .copied()
        .                           .filter(|&id| {
        .                               self.battlefield_find(id).is_some_and(|c| c.controller == c.owner)
        .                           })
        .                           .collect();
        .                       for id in disarm {
        .                           self.steal_penalty_armed.remove(&id);
        .                       }
        .                   }
   53,350 ( 0.00%)          for (id, thief, dmg) in steal_penalties {
        .                       if self.steal_penalty_armed.insert(id) {
        .                           self.push_pending_trigger(
        .                               crate::game::types::PendingTriggerPush {
        .                                   from_mana_ability: false,
        .                               actor: None,
        .                                   source: id,
        .                                   controller: thief,
        .                                   effect: Effect::Seq(vec![
-- line 4403 ----------------------------------------
-- line 4415 ----------------------------------------
        .                               None,
        .                           );
        .                       }
        .                   }
        .           
        .                   // CR 611.2c — "for as long as this artifact remains tapped" (Vedalken
        .                   // Shackles) / "for as long as that Aura is attached to it" (Eriette)
        .                   // steals end the moment the source stops meeting the clause.
   10,670 ( 0.00%)          if self.temporary_control.iter().any(|tc| tc.while_source_tapped || tc.while_source_attached)
        .                   {
        .                       let mut kept = Vec::new();
        .                       for tc in std::mem::take(&mut self.temporary_control) {
        .                           let holds = (!tc.while_source_tapped
        .                               || tc.source.and_then(|s| self.battlefield_find(s)).is_some_and(|c| c.tapped))
        .                               && (!tc.while_source_attached
        .                                   || tc.source.and_then(|s| self.battlefield_find(s)).is_some_and(|c| {
        .                                       c.attached_to == Some(tc.card)
-- line 4431 ----------------------------------------
-- line 4436 ----------------------------------------
        .                               self.change_control(tc.card, tc.original_controller);
        .                           }
        .                       }
        .                       self.temporary_control = kept;
        .                   }
        .           
        .                   // Same clause on the continuous-effect side (Hisoka's Guard's shroud
        .                   // grant): drop it once the granting source is untapped or gone.
   21,340 ( 0.00%)          if self
        .                       .continuous_effects
        .                       .iter()
        .                       .any(|e| e.duration == crate::game::layers::EffectDuration::WhileSourceTapped)
        .                   {
        .                       let still_tapped: crate::fxhash::HashSet<CardId> =
        .                           self.battlefield.iter().filter(|c| c.tapped).map(|c| c.id).collect();
        .                       self.continuous_effects.retain(|e| {
        .                           e.duration != crate::game::layers::EffectDuration::WhileSourceTapped
-- line 4452 ----------------------------------------
-- line 4470 ----------------------------------------
        .                       self.continuous_effects.retain(|e| {
        .                           e.duration != crate::game::layers::EffectDuration::WhileSourceAttached
        .                               || attached.contains(&e.source)
        .                       });
        .                   }
        .                   // CR 611.2c — "for as long as this remains attached to it"
        .                   // (Assimilation Aegis): the copy ends the moment the Equipment
        .                   // unattaches or leaves.
   10,670 ( 0.00%)          if self
        .                       .temporary_copies
        .                       .iter()
        .                       .any(|tc| tc.duration == crate::effect::Duration::WhileSourceAttached)
        .                   {
        .                       let lapsed: Vec<CardId> = self
        .                           .temporary_copies
        .                           .iter()
        .                           .filter(|tc| {
-- line 4486 ----------------------------------------
-- line 4509 ----------------------------------------
        .                           scan = self.sba_board_scan();
        .                       }
        .                   }
        .           
        .                   // CR 603.8 state trigger — "When you control no other [filter],
        .                   // sacrifice this permanent" (Synod Centurion). Latched in
        .                   // `no_other_sacrifice_armed` so it queues once while the condition
        .                   // holds; the latch clears as soon as a match is back.
   21,340 ( 0.00%)          let no_other: Vec<(CardId, usize, crate::card::SelectionRequirement)> = if scan.no_other {
        .                       self.battlefield
        .                           .iter()
        .                           .filter_map(|c| {
        .                               let f = c.definition.sacrifice_when_you_control_no_other.clone()?;
        .                               Some((c.id, c.controller, f))
        .                           })
        .                           .collect()
        .                   } else {
        .                       Vec::new()
        .                   };
   53,350 ( 0.00%)          for (id, seat, filter) in no_other {
        .                       let alone = !self.battlefield.iter().any(|o| {
        .                           o.id != id
        .                               && o.controller == seat
        .                               && self.evaluate_requirement_static(
        .                                   &filter,
        .                                   &crate::game::types::Target::Permanent(o.id),
        .                                   seat,
        .                                   Some(id),
-- line 4536 ----------------------------------------
-- line 4560 ----------------------------------------
        .                   // sweep reports last-known information from the state *before* any of
        .                   // the sweep's actions ran. Snapshot the ±1/±1 counts here, ahead of the
        .                   // 122.3 annihilation, so Persist/Undying read the pre-sweep pile
        .                   // (Young Wolf with a +1/+1 counter that takes three -1/-1 counters
        .                   // dies for good). Only the Persist/Undying return reads it, and only
        .                   // for a card whose *printed* keywords carry one, so the map holds
        .                   // exactly those cards; every other lookup misses and is unused.
        .                   let pre_sba_pm_counters: crate::fxhash::HashMap<CardId, (u32, u32)> =
   21,340 ( 0.00%)              if scan.persist_undying {
        .                           self.battlefield
        .                               .iter()
        .                               .filter(|c| {
        .                                   c.definition.keywords.contains(&Keyword::Persist)
        .                                       || c.definition.keywords.contains(&Keyword::Undying)
        .                               })
        .                               .map(|c| {
        .                                   (
-- line 4576 ----------------------------------------
-- line 4585 ----------------------------------------
        .                       } else {
        .                           crate::fxhash::HashMap::default()
        .                       };
        .           
        .                   // +1/+1 and -1/-1 counters cancel each other out (CR 122.3 — the
        .                   // SBA removes `N` of each kind, where `N` is the smaller count).
        .                   // The `&mut` walk unshares the zone, so skip it unless a permanent
        .                   // actually holds both kinds.
   21,340 ( 0.00%)          if scan.pm_both {
        .                       for card in &mut self.battlefield {
        .                           let plus = card
        .                               .counters
        .                               .get(&crate::card::CounterType::PlusOnePlusOne)
        .                               .copied()
        .                               .unwrap_or(0);
        .                           let minus = card
        .                               .counters
-- line 4601 ----------------------------------------
-- line 4611 ----------------------------------------
        .                       }
        .                   }
        .           
        .                   // CR 122.4 — "An effect can set the maximum number of counters of a
        .                   // kind that a permanent can have." If a permanent has more than its
        .                   // printed cap, the SBA prunes the excess down to the cap. Uses the
        .                   // new `CardDefinition.max_counters_of_kind: Option<(CounterType,
        .                   // u32)>` field — None ⇒ no cap, the default.
   21,340 ( 0.00%)          if scan.max_counters {
        .                       for card in &mut self.battlefield {
        .                           if let Some((kind, max)) = card.definition.max_counters_of_kind {
        .                               let current = card.counters.get(&kind).copied().unwrap_or(0);
        .                               if current > max {
        .                                   // Through the accessor so a cap of 0 drops the entry
        .                                   // rather than storing a zero (CR 122.1).
        .                                   card.remove_counters(kind, current - max);
        .                               }
-- line 4627 ----------------------------------------
-- line 4636 ----------------------------------------
        .                   let legend_groups = {
        .                       let mut order: Vec<(usize, &str)> = Vec::new();
        .                       let mut groups: crate::fxhash::HashMap<(usize, &str), Vec<(CardId, String)>> =
        .                           crate::fxhash::HashMap::default();
        .                       // CR 704.5j reads *current* supertypes, so a continuous grant of
        .                       // the Legendary supertype (Leyline of Singularity, the Ring's
        .                       // emblem) counts. Only pay for the layer computation when such a
        .                       // grant is live; otherwise the printed supertype is authoritative.
   32,010 ( 0.00%)              let supertype_grant_active = scan.supertype_grant;
   32,010 ( 0.00%)              let is_legendary = |c: &CardInstance| -> bool {
        .                           c.definition.supertypes.contains(&Supertype::Legendary)
        .                               || (supertype_grant_active
        .                                   && self
        .                                       .computed_permanent(c.id)
        .                                       .is_some_and(|cp| cp.supertypes.contains(&Supertype::Legendary)))
        .                       };
        .                       // CR 704.5j — Mirror Gallery turns the legend rule off entirely.
        .                       // Only *this* SBA is switched off; the rest of the sweep (deaths,
        .                       // loss conditions, the Aura/Equipment sweeps) still runs.
        .                       let mut out = Vec::new();
   74,690 ( 0.00%)              if !scan.legend_rule_off && (scan.legendary || supertype_grant_active) {
        .                           // Walk descending by id so each group's vec is newest-first.
        .                           let mut by_id: Vec<_> = self
        .                               .battlefield
        .                               .iter()
        .                               .filter(|c| is_legendary(c))
        .                               // Aeve — "isn't legendary if it's a token".
        .                               .filter(|c| !(c.is_token && c.definition.nonlegendary_as_token))
        .                               .collect();
-- line 4664 ----------------------------------------
-- line 4689 ----------------------------------------
        .                                   out.push((k.0, k.1.to_string(), dups));
        .                               }
        .                           }
        .                       }
        .                       out
        .                   };
        .                   let legend_victims: Vec<CardId> = {
        .                       let mut victims = Vec::new();
   53,350 ( 0.00%)              for (player, name, duplicates) in legend_groups {
        .                           // Ask the controller which to keep; default keeps newest.
        .                           // Auto default: keep the copy carrying the most accumulated
        .                           // board state (counters + attachments), tiebreak newest —
        .                           // "keep newest" alone sacrificed the aura'd/countered copy
        .                           // to a freshly cast vanilla one. Scripted deciders steer.
        .                           let kept = match self.decider.kind() {
        .                               crate::decision::DeciderKind::Auto => duplicates
        .                                   .iter()
-- line 4705 ----------------------------------------
-- line 4732 ----------------------------------------
        .                               },
        .                           };
        .                           for (id, _) in &duplicates {
        .                               if *id != kept {
        .                                   victims.push(*id);
        .                               }
        .                           }
        .                       }
   42,680 ( 0.00%)              victims
        .                   };
        .                   for id in legend_victims {
        .                       // Cache snapshot before zone change so AnotherOfYours-scope
        .                       // triggers off legend-rule deaths see the right player AND
        .                       // can introspect the dying card's printed types. Only a *creature*
        .                       // dies (CR 700.4) — a legend-ruled planeswalker/artifact/enchant
        .                       // leaves the battlefield without a CreatureDied event.
        .                       if let Some(c) = self.battlefield.iter().find(|c| c.id == id) {
-- line 4748 ----------------------------------------
-- line 4754 ----------------------------------------
        .                       self.remove_from_battlefield_to_graveyard_raw(id);
        .                   }
        .           
        .                   // World rule (CR 704.5k): if two or more permanents have the World
        .                   // supertype, all except the one with the newest timestamp go to their
        .                   // owners' graveyards; on a timestamp tie ALL of them go. Unlike the
        .                   // legend rule this is global, not per-controller.
        .                   let world_victims: Vec<CardId> = {
   21,340 ( 0.00%)              let worlds: Vec<(CardId, u64)> = if scan.world {
        .                           self.battlefield
        .                               .iter()
        .                               .filter(|c| c.definition.supertypes.contains(&Supertype::World))
        .                               .map(|c| (c.id, c.battlefield_timestamp))
        .                               .collect()
        .                       } else {
        .                           Vec::new()
        .                       };
-- line 4770 ----------------------------------------
-- line 4789 ----------------------------------------
        .           
        .                   // Saga rule (CR 714.4 / 704.5x): a Saga whose lore counters have
        .                   // reached its final chapter number is sacrificed — unless one of its
        .                   // chapter abilities is still a trigger on the stack (so the last
        .                   // chapter resolves before the Saga leaves). CR 714.4 has no creature
        .                   // exemption: FIN's "Legendary Enchantment Creature — Saga" back faces
        .                   // survive by exiling and re-entering front face up on their last
        .                   // chapter, which resets the lore count before the SBA can see it.
   21,340 ( 0.00%)          let saga_victims: Vec<CardId> = if !scan.saga {
        .                       Vec::new()
        .                   } else {
        .                       self.battlefield
        .                       .iter()
        .                       .filter(|c| {
        .                           let Some(final_ch) = c.definition.saga_chapters.iter().map(|(n, _)| *n).max() else {
        .                               return false;
        .                           };
-- line 4805 ----------------------------------------
-- line 4825 ----------------------------------------
        .                       self.remove_from_battlefield_to_graveyard_raw(id);
        .                   }
        .           
        .                   // Collect dead creatures using layer-computed toughness. Only
        .                   // creatures can be in `dead`, and the `find(id)` misses below already
        .                   // fall back to the printed type, so the layer pass skips the
        .                   // permanents no card-type-changing effect can animate.
        .                   let computed = self.compute_battlefield_creatures();
   10,670 ( 0.00%)          let dead: Vec<CardId> = self
        .                       .battlefield
        .                       .iter()
   10,670 ( 0.00%)              .filter(|c| {
        .                           // CR 704.5g/CR 305.7 — use the *computed* type so an animated
        .                           // land (earthbend, creature-lands granted Creature via layers)
        .                           // dies to lethal damage / toughness ≤ 0 like any creature, not
        .                           // just cards printed as creatures.
  190,616 ( 0.01%)                  let cp = computed.iter().find(|cp| cp.id == c.id);
        .                           let is_creature = cp
        .                               .map(|cp| cp.card_types.contains(&crate::card::CardType::Creature))
  151,500 ( 0.00%)                      .unwrap_or_else(|| c.definition.is_creature());
2,424,000 ( 0.08%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (151,500x)
  303,000 ( 0.01%)                  if !is_creature {
        .                               return false;
        .                           }
        .                           // Indestructible stops destruction by damage but NOT by toughness ≤ 0.
  117,348 ( 0.00%)                  let computed_toughness = cp.map(|cp| cp.toughness).unwrap_or(c.toughness());
2,659,888 ( 0.09%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::toughness (39,116x)
        .                           // Toughness ≤ 0 kills even indestructible creatures.
   78,232 ( 0.00%)                  if computed_toughness <= 0 {
        .                               return true;
        .                           }
        .                           // CR 704.5g: lethal damage = damage >= toughness.
        .                           // CR 704.5h: any damage from a deathtouch source is lethal.
        .                           // Indestructible creatures don't die to either rule. Read the
        .                           // *computed* keyword so a layer-6 grant (Aura / Equipment /
        .                           // anthem — Shielded by Faith) counts, not just the printed
        .                           // keyword + indestructible counter on the instance.
        .                           let indestructible = cp
        .                               .map(|cp| cp.keywords.contains(&crate::card::Keyword::Indestructible))
        .                               .unwrap_or(false)
   78,232 ( 0.00%)                      || c.is_indestructible();
3,697,748 ( 0.12%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::is_indestructible (39,116x)
  229,732 ( 0.01%)                  if indestructible {
        .                               return false;
        .                           }
        .                           // Zilortha — lethal is measured against power, not toughness,
        .                           // for any creature a LethalDamageByPower static matches. The
        .                           // power threshold can be 0, so gate on actual damage being
        .                           // marked (a 0-power creature dies only once it's been dealt
        .                           // damage; an undamaged one survives — CR 704.5g ruling).
  117,348 ( 0.00%)                  let lethal_threshold = if scan.lethal_by_power && self.lethal_damage_by_power(c.id) {
        .                               computed
        .                                   .iter()
        .                                   .find(|cp| cp.id == c.id)
        .                                   .map(|cp| cp.power)
        .                                   .unwrap_or(c.power())
        .                           } else {
        .                               computed_toughness
        .                           };
-- line 4880 ----------------------------------------
-- line 4882 ----------------------------------------
        .                           // single source's tally reaching the threshold does.
        .                           let needs_single_source = cp
        .                               .map(|cp| {
        .                                   cp.keywords.contains(&crate::card::Keyword::SurvivesSplitLethalDamage)
        .                               })
        .                               .unwrap_or(false);
        .                           let single_source_lethal = !needs_single_source
        .                               || (c.max_damage_from_single_source() as i32) >= lethal_threshold;
  126,856 ( 0.00%)                  if c.damage > 0 && (c.damage as i32) >= lethal_threshold && single_source_lethal {
        .                               return true;
        .                           }
        .                           // Shriveling Rot — "whenever a creature is dealt damage,
        .                           // destroy it" for the rest of the turn. Indestructible has
        .                           // already returned above, matching "destroy".
   39,444 ( 0.00%)                  if self.damaged_creatures_die_this_turn && c.damage > 0 {
        .                               return true;
        .                           }
   72,112 ( 0.00%)                  c.dealt_deathtouch_damage && c.damage > 0
        .                       })
    3,420 ( 0.00%)              .map(|c| c.id)
        .                       .collect();
        .           
        .                   // Set when Persist/Undying puts a permanent back: the only way the
        .                   // death loop can *add* to the board, and so the only reason the
        .                   // presence scan has to be retaken below.
        .                   let mut board_grew = false;
        .           
        .                   // Hushbringer (CR 614): suppress creature-death triggers while a
        .                   // `SuppressCreatureEtbTriggers { also_dies }` static is in play. Read
        .                   // only inside the death loop, so the whole-battlefield scan is gated
        .                   // on there being one — most SBA sweeps kill nothing.
        .                   let dies_suppressed =
   32,010 ( 0.00%)              !dead.is_empty() && crate::game::actions::creature_dies_triggers_suppressed(self);
        .           
   39,796 ( 0.00%)          for id in dead {
        .                       // CR 701.15 — regeneration shields replace destruction by
        .                       // *damage* (lethal damage / deathtouch), but never destruction
        .                       // from toughness ≤ 0 (that's a separate SBA, not a "destroy").
        .                       // A surviving shield taps the creature, removes it from combat,
        .                       // and heals marked damage instead of letting it die.
    3,420 ( 0.00%)              let dies_by_lethal_toughness = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
        .                           .map(|c| {
        .                               let ct = computed
        .                                   .iter()
        .                                   .find(|cp| cp.id == id)
    3,420 ( 0.00%)                          .map(|cp| cp.toughness)
        .                                   .unwrap_or_else(|| c.toughness());
    6,840 ( 0.00%)                      ct <= 0
        .                           })
        .                           .unwrap_or(false);
        .                       let has_regen = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
    6,840 ( 0.00%)                  .map(|c| c.regeneration_shields > 0 && !c.cant_regenerate_this_turn)
        .                           .unwrap_or(false);
        .                       if has_regen && !dies_by_lethal_toughness {
        .                           self.apply_regeneration(id, &mut events);
        .                           continue;
        .                       }
        .                       // CR 702.89 — umbra armor replaces destruction (not the
        .                       // toughness-≤-0 SBA, which isn't destruction).
   23,940 ( 0.00%)              if !dies_by_lethal_toughness && self.apply_umbra_armor(id, &mut events) {
  578,836 ( 0.02%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::apply_umbra_armor (3,420x)
        .                           continue;
        .                       }
        .           
    6,840 ( 0.00%)              self.apply_death_toughness_drain(id);
   10,260 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::apply_death_toughness_drain (3,420x)
        .                       events.push(GameEvent::CreatureDied { card_id: id });
        .                       // Cache the dying card's snapshot so AnotherOfYours-scope
        .                       // triggers AND printed-type filter predicates fire reliably
        .                       // even for tokens. CR 111.7c's
        .                       // "ceases to exist" SBA removes the token from every zone in
        .                       // the same sweep — by dispatch time the zone-walking lookup
        .                       // returns None. The cached `CardInstance` survives the sweep
        .                       // and is consulted by `event_matches_spec` (controller lookup)
        .                       // and `evaluate_requirement_static` (type/keyword/counter
        .                       // filter). Cleared after `dispatch_triggers_for_events`.
   17,100 ( 0.00%)              if let Some(c) = self.dying_snapshot(id) {
12,870,804 ( 0.41%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dying_snapshot (3,420x)
        .                           self.died_card_snapshots.insert(id, c);
        .                       }
        .                       // Collect Dies triggers and Persist/Undying info before removing from battlefield.
        .                       let (
   10,260 ( 0.00%)                  mut die_triggers,
        .                           has_persist,
        .                           has_undying,
        .                           minus_count,
        .                           plus_count,
        .                           owner,
        .                           controller_idx,
    3,420 ( 0.00%)              ) = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
        .                           .map(|c| {
        .                               // CR 603.10a — "leaves-the-battlefield" triggers look
        .                               // back in time at the dying card. Only fire the dying
        .                               // card's own die-triggers whose scope says they can
        .                               // fire from self — i.e. SelfSource or YourControl /
-- line 4982 ----------------------------------------
-- line 4983 ----------------------------------------
        .                               // AnyPlayer. AnotherOfYours / OpponentControl /
        .                               // FromYourGraveyard are NOT self-fire scopes; skipping
        .                               // them here matches the printed Oracle semantics for
        .                               // "Whenever another creature you control dies" (must
        .                               // be another, not this dying card).
        .                               // Walk printed Dies triggers + any granted transient
        .                               // ones (Rabid Attack EOT "this creature gains 'die →
        .                               // draw a card'" grants ride on `granted_triggers_eot`).
    3,420 ( 0.00%)                      let granted: &[crate::card::TriggeredAbility] = self
        .                                   .granted_triggers_eot
        .                                   .get(&c.id)
        .                                   .map(Vec::as_slice)
        .                                   .unwrap_or(&[]);
        .                               let triggers: Vec<(CardId, Effect, usize, Option<crate::card::Predicate>)> = c
        .                                   .definition
        .                                   .triggered_abilities
        .                                   .iter()
        .                                   .chain(granted)
        .                                   // PermanentLeavesBattlefield ("when this leaves the
        .                                   // battlefield") fires on any departure, including a
        .                                   // lethal-damage death (Thought-Knot Seer); CreatureDied
        .                                   // is suppressed by Hushbringer-style statics.
    1,010 ( 0.00%)                          .filter(|t| match t.event.kind {
        .                                       EventKind::CreatureDied => !dies_suppressed,
        .                                       EventKind::PermanentLeavesBattlefield => true,
        .                                       _ => false,
        .                                   })
        .                                   .filter(|t| matches!(
        .                                       t.event.scope,
        .                                       crate::effect::EventScope::SelfSource
        .                                           | crate::effect::EventScope::YourControl
-- line 5013 ----------------------------------------
-- line 5021 ----------------------------------------
        .                                   // control dies" (YourControl) must not fire when the
        .                                   // dying source itself fails the filter.
        .                                   .map(|t| (c.id, t.effect.clone(), c.controller, t.event.filter.clone()))
        .                                   .collect();
        .                               let has_persist = c.definition.keywords.contains(&Keyword::Persist);
        .                               let has_undying = c.definition.keywords.contains(&Keyword::Undying);
        .                               // CR 704.8 — read the pre-sweep pile, not the post-122.3 one.
        .                               let (minus, plus) = pre_sba_pm_counters.get(&c.id).copied().unwrap_or((
   10,260 ( 0.00%)                          c.counter_count(crate::card::CounterType::MinusOneMinusOne),
   34,200 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (3,420x)
   10,260 ( 0.00%)                          c.counter_count(crate::card::CounterType::PlusOnePlusOne),
   34,200 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (3,420x)
        .                               ));
        .                               (
   10,260 ( 0.00%)                          triggers,
        .                                   has_persist,
        .                                   has_undying,
        .                                   minus,
        .                                   plus,
    6,840 ( 0.00%)                          c.owner,
   10,260 ( 0.00%)                          c.controller,
        .                               )
        .                           })
        .                           .unwrap_or_default();
        .                       // CR 702.6e — Equipment-granted "dies" triggers fire as though
        .                       // printed on the equipped creature (Skullclamp). Collect them
        .                       // while the creature is still attached (pre-removal). Source is
        .                       // the dying creature so `Selector::This` reads its last-known
        .                       // info; controller is the creature's controller.
    6,840 ( 0.00%)              for eq in &self.battlefield {
    1,664 ( 0.00%)                  if eq.attached_to != Some(id) {
        .                               continue;
        .                           }
      512 ( 0.00%)                  let Some(bonus) = &eq.definition.equipped_bonus else { continue };
        .                           for ta in &bonus.triggered_abilities {
        .                               if ta.event.kind == EventKind::CreatureDied && !dies_suppressed {
        .                                   die_triggers.push((id, ta.effect.clone(), controller_idx, ta.event.filter.clone()));
        .                               }
        .                           }
        .                       }
        .                       // Bump the controller's per-turn died-creature tally for
        .                       // Witherbloom "if a creature died under your control this
        .                       // turn" payoffs (Essenceknit Scholar).
   13,680 ( 0.00%)              if controller_idx < self.players.len() {
   10,260 ( 0.00%)                  self.players[controller_idx].creatures_died_this_turn =
  125,028 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,420x)
    6,840 ( 0.00%)                      self.players[controller_idx].creatures_died_this_turn.saturating_add(1);
    3,420 ( 0.00%)                  self.creatures_died_this_resolution =
    3,420 ( 0.00%)                      self.creatures_died_this_resolution.saturating_add(1);
    6,840 ( 0.00%)                  self.note_creature_death(id);
  794,096 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::note_creature_death (3,420x)
        .                           // Zubera cycle: count Zubera deaths separately (read off the
        .                           // still-present dying creature's subtypes).
   76,916 ( 0.00%)                  if self.battlefield.iter().any(|c| c.id == id
        .                               && c.definition.subtypes.creature_types.contains(&crate::card::CreatureType::Zubera))
        .                           {
        .                               self.players[controller_idx].zuberas_died_this_turn =
        .                                   self.players[controller_idx].zuberas_died_this_turn.saturating_add(1);
        .                           }
        .                       }
        .                       // CR 603.10 — stash an LKI snapshot before the creature leaves
        .                       // so a "deals damage / makes tokens equal to its power" dies
        .                       // body reads its counter-boosted P/T (Goldvein Hydra). Removed
        .                       // when the trigger resolves (`resolve_top_of_stack`).
   10,260 ( 0.00%)              if !die_triggers.is_empty()
        .                           && let Some(c) = self.battlefield.iter().find(|c| c.id == id)
        .                       {
        .                           self.leaves_bf_lki.insert(id, c.clone());
        .                       }
    3,420 ( 0.00%)              let was_land = self
        .                           .battlefield
        .                           .iter()
        .                           .find(|c| c.id == id)
    3,420 ( 0.00%)                  .is_some_and(|c| c.definition.is_land());
   54,720 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (3,420x)
    6,840 ( 0.00%)              self.remove_from_battlefield_to_graveyard_raw(id);
23,489,958 ( 0.75%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw (3,420x)
        .                       // CR 700 — emit the graveyard-arrival event (only when the card
        .                       // actually landed there; Finality / RIP redirects don't count).
        .                       // Emrakul-style "put into a graveyard from anywhere" triggers
        .                       // listen for this.
    6,840 ( 0.00%)              if self.players[owner].graveyard.iter().any(|c| c.id == id) {
   13,680 ( 0.00%)                  events.push(GameEvent::CardPutIntoGraveyard {
        .                               player: owner,
        .                               card_id: id,
        .                               is_land: was_land,
        .                           });
        .                       }
        .                       // Push Dies triggers to the stack for resolution.
        .                       // CR 603.10a — a self-death "this or another creature you control
        .                       // dies" trigger (YourControl scope) reads the dying creature's own
        .                       // mana value through the event amount, so an event-amount-relative
        .                       // target filter (`ManaValueLessThanEventAmount` — Jackdaw Savior)
        .                       // can find a legal target during enumeration and at resolution.
        .                       let died_ev_amount =
   27,360 ( 0.00%)                  self.event_amount_for(&GameEvent::CreatureDied { card_id: id });
  235,540 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::event_amount_for (3,420x)
   37,620 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination::game::types::GameEvent> (3,420x)
    3,420 ( 0.00%)              self.trigger_event_amount_scratch = died_ev_amount;
   30,780 ( 0.00%)              for (source, effect, controller, filter) in die_triggers {
        .                           // CR 603.4/603.10a — a filtered self-death trigger ("this or
        .                           // another [type] you control dies") checks its intervening
        .                           // filter against the dying creature, bound as TriggerSource via
        .                           // the cached death snapshot, before firing.
        .                           if let Some(pred) = &filter {
        .                               let mut ctx = crate::game::effects::EffectContext::for_trigger(
        .                                   source, controller, None, 0,
        .                               );
-- line 5120 ----------------------------------------
-- line 5133 ----------------------------------------
        .                               TriggerPush::new(source, controller, effect)
        .                                   .target(auto_target)
        .                                   .event_amount(died_ev_amount)
        .                                   .build(),
        .                           );
        .                       }
        .                       // Persist / Undying return (CR 702.79 / 702.92), shared with the
        .                       // destroy / sacrifice funnels via `return_persist_undying`.
   17,100 ( 0.00%)              self.return_persist_undying(
   95,760 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::return_persist_undying (3,420x)
   27,360 ( 0.00%)                  id, owner, (has_persist, has_undying, minus_count, plus_count), &mut events,
        .                       );
   13,680 ( 0.00%)              if (has_persist || has_undying) && self.battlefield.iter().any(|c| c.id == id) {
        .                           board_grew = true;
        .                       }
        .                       let _ = controller_idx; // used via closure above
        .                   }
        .           
        .                   // Every flag is an over-approximation and the sweep so far can only
        .                   // have *removed* permanents, so the scan taken above still covers the
        .                   // post-death board. Only a Persist/Undying return grows it.
   64,020 ( 0.00%)          let scan = if board_grew { self.sba_board_scan() } else { scan };
        .           
        .                   // Planeswalkers with 0 loyalty die (CR 704.5i).
   21,340 ( 0.00%)          let pw_dead: Vec<CardId> = if !scan.planeswalker {
        .                       Vec::new()
        .                   } else {
        .                       self.battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.definition.is_planeswalker()
        .                                   && c.counter_count(crate::card::CounterType::Loyalty) == 0
        .                           })
        .                           .map(|c| c.id)
        .                           .collect()
        .                   };
   10,670 ( 0.00%)          for id in pw_dead {
        .                       events.push(GameEvent::PlaneswalkerDied { card_id: id });
        .                       self.remove_from_battlefield_to_graveyard_raw(id);
        .                   }
        .           
        .                   // CR 310.10 / 704.5x — a battle with no defense counters is defeated.
   21,340 ( 0.00%)          let defeated_battles: Vec<CardId> = if !scan.battle {
        .                       Vec::new()
        .                   } else {
        .                       self.battlefield
        .                           .iter()
        .                           .filter(|c| {
        .                               c.definition.is_battle()
        .                                   && c.definition.defense > 0
        .                                   && c.counter_count(crate::card::CounterType::Defense) == 0
-- line 5182 ----------------------------------------
-- line 5184 ----------------------------------------
        .                           .map(|c| c.id)
        .                           .collect()
        .                   };
        .                   let any_defeated = !defeated_battles.is_empty();
        .                   for id in defeated_battles {
        .                       self.defeat_battle(id, &mut events);
        .                   }
        .                   // A defeated battle's back face re-enters the battlefield.
   10,670 ( 0.00%)          let scan = if any_defeated { self.sba_board_scan() } else { scan };
        .           
        .                   // CR 702.103f — a bestowed permanent that is unattached, or attached
        .                   // to an illegal object, ceases to be bestowed: it stays in play and
        .                   // reverts to a creature (the rule's stated exception to 704.5m, so it
        .                   // never hits the graveyard). Run before the orphan-Aura sweep so it
        .                   // isn't swept away.
   21,340 ( 0.00%)          let unbestowed: Vec<CardId> = if !scan.bestowed {
        .                       Vec::new()
        .                   } else {
        .                       self.battlefield
        .                       .iter()
        .                       .filter(|c| c.bestowed)
        .                       .filter(|c| match c.attached_to {
        .                           None => true,
        .                           Some(host) => {
-- line 5207 ----------------------------------------
-- line 5227 ----------------------------------------
        .                   for id in unbestowed {
        .                       if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == id) {
        .                           c.bestowed = false;
        .                           c.attached_to = None;
        .                       }
        .                   }
        .           
        .                   // Auras with no valid attachment target go to their owner's graveyard (CR 704.5n/5q).
   21,340 ( 0.00%)          let orphaned_auras: Vec<CardId> = if !scan.aura {
        .                       Vec::new()
        .                   } else {
    2,168 ( 0.00%)              self.battlefield
        .                       .iter()
  134,442 ( 0.00%)              .filter(|c| c.definition.is_aura() && c.attached_to_player.is_none())
  414,552 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_aura (44,814x)
        .                       .filter(|c| {
    7,484 ( 0.00%)                  match c.attached_to {
        .                               None => true, // not attached to anything
    3,742 ( 0.00%)                      Some(attached_id) => !self.battlefield.iter().any(|b| b.id == attached_id),
        .                           }
        .                       })
      144 ( 0.00%)              .map(|c| c.id)
        .                       .collect()
        .                   };
        .                   for id in orphaned_auras {
        .                       // Record (aura → host) before the Aura leaves, so "whenever an
        .                       // enchanted creature dies" payoffs can count the Auras that were
        .                       // on the dying host (Hateful Eidolon, Dawn Evangel). Only meaningful
        .                       // when the lost host is gone (the common death case).
      144 ( 0.00%)              if let Some(aura) = self.battlefield.iter().find(|c| c.id == id)
      432 ( 0.00%)                  && let Some(host) = aura.attached_to
        .                           && !self.battlefield.iter().any(|b| b.id == host)
        .                       {
      288 ( 0.00%)                  let (aura_controller, aura_snapshot) = (aura.controller, aura.clone());
      268 ( 0.00%)                  self.auras_at_death.entry(host).or_default().push((id, aura_controller));
        .                           // Snapshot the leaving Aura so its "when enchanted creature
        .                           // dies" trigger (EnchantedBySource) can fire via LKI even
        .                           // though the Aura itself is gone (Minion's Return).
      124 ( 0.00%)                  self.died_card_snapshots.insert(id, aura_snapshot);
        .                       }
        .                       // Fire any leaves-the-battlefield triggers on the Aura itself
        .                       // (CR 603.6d) — e.g. Rancor's "return it to its owner's hand".
      576 ( 0.00%)              events.append(&mut self.remove_to_graveyard_with_triggers(id));
14,060,401 ( 0.45%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_to_graveyard_with_triggers (144x)
        .                   }
        .           
        .                   // CR 704.5n / 704.5m / 303.4f — an Aura attached to an object it can
        .                   // no longer legally enchant (host lost the required type, a "you
        .                   // control" Aura's host changed controllers, or the host gained
        .                   // protection from the Aura) is put into its owner's graveyard.
        .                   // The filter half is only checked when the Aura's "enchant ___"
        .                   // filter is recoverable — distinct from the missing-host sweep
        .                   // above. Bestowed Auras are exempt (their host loss reverts them
        .                   // to creatures, handled earlier).
   21,340 ( 0.00%)          let illegally_attached: Vec<CardId> = if !scan.aura {
        .                       Vec::new()
        .                   } else {
    2,168 ( 0.00%)              self.battlefield
        .                       .iter()
  134,010 ( 0.00%)              .filter(|c| c.definition.is_aura() && !c.bestowed)
  412,824 ( 0.01%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_aura (44,670x)
        .                       .filter_map(|c| {
        .                           let host = c.attached_to?;
    3,598 ( 0.00%)                  if !self.battlefield.iter().any(|b| b.id == host) {
        .                               return None; // missing host: handled by the sweep above
        .                           }
        .                           // CR 704.5m — protection covers "can't be Enchanted by": a
        .                           // host with protection from the Aura's qualities (its color,
        .                           // card type, "everything", …) sheds the Aura.
        .                           // CR 702.16k — Spectra Ward's "this effect doesn't remove
        .                           // Auras": a `protection_keeps_auras` Aura on the host makes
        .                           // the whole host immune to the protection shed.
        .                           let keeps_auras = self.battlefield.iter().any(|a| {
    7,790 ( 0.00%)                      a.attached_to == Some(host)
        .                                   && a.definition
        .                                       .equipped_bonus
        .                                       .as_ref()
        .                                       .is_some_and(|b| b.protection_keeps_auras)
        .                           });
        .                           // CR 702.16k (narrow) — "this effect doesn't remove this
        .                           // Aura": the granting Aura exempts only itself.
        .                           let keeps_self = c
        .                               .definition
        .                               .equipped_bonus
        .                               .as_ref()
        .                               .is_some_and(|b| b.protection_keeps_self);
   17,990 ( 0.00%)                  if !keeps_auras && !keeps_self && self.is_protected_from(c.id, host) {
        .                               return Some(c.id);
        .                           }
    3,598 ( 0.00%)                  let filter = c.definition.aura_enchant_filter()?;
   57,568 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardDefinition::aura_enchant_filter (3,598x)
   17,990 ( 0.00%)                  if !self.evaluate_requirement(filter, &Target::Permanent(host), c.controller) {
        .                               Some(c.id)
        .                           } else {
        .                               None
        .                           }
        .                       })
        .                       .collect()
        .                   };
        .                   for id in illegally_attached {
        .                       events.append(&mut self.remove_to_graveyard_with_triggers(id));
        .                   }
        .           
        .                   // CR 704.5y — if a permanent has more than one Role controlled by
        .                   // the same player attached, each but the newest (by battlefield
        .                   // timestamp, CardId tiebreak) goes to its owner's graveyard.
   21,340 ( 0.00%)          let stale_roles: Vec<CardId> = if !scan.role {
        .                       Vec::new()
        .                   } else {
        .                       let mut by_host: crate::fxhash::HashMap<(CardId, usize), Vec<(u64, CardId)>> =
        .                           crate::fxhash::HashMap::default();
        .                       for c in self.battlefield.iter().filter(|c| {
        .                           c.definition
        .                               .subtypes
        .                               .enchantment_subtypes
-- line 5337 ----------------------------------------
-- line 5356 ----------------------------------------
        .                   };
        .                   for id in stale_roles {
        .                       events.append(&mut self.remove_to_graveyard_with_triggers(id));
        .                   }
        .           
        .                   // CR 704.5z — a player who controls a "Start your engines!" permanent
        .                   // and has no speed gets speed 1. (The self-ETB path also seeds it;
        .                   // this SBA covers blink/control-change/token-copy arrivals.)
   21,340 ( 0.00%)          if scan.start_engines {
        .                       for seat in 0..self.players.len() {
        .                           if self.players[seat].speed == 0
        .                               && self.battlefield.iter().any(|c| {
        .                                   c.controller == seat
        .                                       && c.definition
        .                                           .keywords
        .                                           .contains(&crate::card::Keyword::StartYourEngines)
        .                               })
-- line 5372 ----------------------------------------
-- line 5378 ----------------------------------------
        .           
        .                   // CR 704.5n — "If an Equipment or Fortification is attached to an
        .                   // illegal permanent or to a player, it becomes unattached from
        .                   // that permanent or player. It remains on the battlefield."
        .                   // Illegal here means the attached card isn't on the battlefield
        .                   // anymore (e.g. equipped creature died) OR the target permanent
        .                   // is no longer a legal target (no creature subtype for Equipment).
        .                   // The Equipment itself stays in play — only the link is cleared.
   21,340 ( 0.00%)          let stale_equipment_links: Vec<CardId> = if !scan.equipment_attached {
        .                       Vec::new()
        .                   } else {
        .                       self.battlefield
        .                       .iter()
        .                       .filter(|c| c.definition.is_equipment())
        .                       .filter_map(|c| {
        .                           let attached = c.attached_to?;
        .                           // Layer-aware: an animated land (Quirion Druid) is a legal
-- line 5394 ----------------------------------------
-- line 5411 ----------------------------------------
        .                   for id in stale_equipment_links {
        .                       if let Some(c) = self.battlefield.iter_mut().find(|c| c.id == id) {
        .                           c.attached_to = None;
        .                       }
        .                   }
        .           
        .                   // CR 702.95h — Soulbond pairs break when either creature leaves the
        .                   // battlefield. Clear any link that points at a card no longer in play.
   21,340 ( 0.00%)          if scan.soulbond {
        .                       let on_bf: crate::fxhash::HashSet<CardId> =
        .                           self.battlefield.iter().map(|c| c.id).collect();
        .                       for c in &mut self.battlefield {
        .                           if let Some(p) = c.soulbond_partner
        .                               && !on_bf.contains(&p)
        .                           {
        .                               c.soulbond_partner = None;
        .                           }
-- line 5427 ----------------------------------------
-- line 5432 ----------------------------------------
        .                   // Dies / leaves-battlefield triggers have already fired by this point
        .                   // (they queue into the events vec before this scan), so dropping the
        .                   // token from its post-bf zone now matches the timing real MTG would
        .                   // produce. Without this, dead tokens linger in graveyards (and would
        .                   // count toward graveyard-size effects, mill prompts, etc.).
        .                   // Each `retain` unshares its CoW zone and the seat's `PlayerData`, so
        .                   // read first: an off-battlefield token is the rare case.
        .                   for seat in 0..self.players.len() {
   42,680 ( 0.00%)              if self.players[seat].graveyard.iter().any(|c| c.is_token) {
        .                           self.players[seat].graveyard.retain(|c| !c.is_token);
        .                       }
   42,680 ( 0.00%)              if self.players[seat].hand.iter().any(|c| c.is_token) {
        .                           self.players[seat].hand.retain(|c| !c.is_token);
        .                       }
   42,680 ( 0.00%)              if self.players[seat].library.iter().any(|c| c.is_token) {
        .                           self.players[seat].library.retain(|c| !c.is_token);
        .                       }
        .                   }
   21,340 ( 0.00%)          if self.exile.iter().any(|c| c.is_token) {
        .                       self.exile.retain(|c| !c.is_token);
        .                   }
        .           
        .                   // Player loss conditions (CR 704.5a/b/c). Eliminated players are
        .                   // removed from turn/priority rotation; the game ends when ≤ 1
        .                   // team alive (see surviving-teams check below).
        .                   //
        .                   // Phase F: `effective_life(i)` collapses the solo-life and
-- line 5458 ----------------------------------------
-- line 5460 ----------------------------------------
        .                   // both teammates' effective life is `n`, so dropping the pool
        .                   // to ≤ 0 eliminates both members simultaneously (CR 810.8 +
        .                   // 704.5a). CR 810.5/810.8d — poison is likewise a shared team
        .                   // resource in 2HG: the members' counters are summed and the
        .                   // threshold is fifteen (`effective_poison` /
        .                   // `poison_loss_threshold` collapse both modes).
        .                   let mut newly_eliminated: Vec<usize> = Vec::new();
        .                   for i in 0..self.players.len() {
   64,020 ( 0.00%)              if self.players[i].eliminated {
        .                           continue;
        .                       }
        .                       // Phase M: 21-commander-damage SBA (CR 704.5v). Any
        .                       // single (this-player, commander) entry of ≥ 21 in
        .                       // `commander_damage` loses the game for this player. We
        .                       // collect the check separately from life / poison so
        .                       // the cause is debuggable.
   21,318 ( 0.00%)              let lost_to_commander = self
        .                           .commander_damage
        .                           .iter()
        .                           .any(|((victim, _), amt)| *victim == i && *amt >= 21);
        .                       // Phyrexian Unlife — the life half of the loss SBA is skipped
        .                       // (poison / commander losses still apply).
        .                       let unlife = self.player_unlife_active(i);
  149,226 ( 0.00%)              let lost = (self.effective_life(i) <= 0 && !unlife)
  948,442 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (21,318x)
  105,670 ( 0.00%)                  || self.effective_poison(i) >= self.poison_loss_threshold(i)
  919,006 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_poison (21,134x)
        .                           || lost_to_commander;
        .                       // CR 104.3d — a player who can't lose (Angel's Grace, Platinum
        .                       // Angel, an opponent's Abyssal Persecutor) skips the loss SBAs;
        .                       // the qualifying state (life ≤ 0, poison ≥ 10) persists.
        .                       // CR 704.7 — a single loss replacement (Lich's Mirror) covers
        .                       // every SBA that would end the game for this player at once.
    1,472 ( 0.00%)              if lost && !self.player_cant_lose_game(i) && !self.apply_loss_reset(i) {
   45,160 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::player_cant_lose_game (184x)
   41,654 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::apply_loss_reset (184x)
        .                           // Stamp the authoritative cause most-specific-first, matching
        .                           // the SBA order that would fire (life, then poison, then
        .                           // commander damage — CR 704.5a/c/v).
        .                           use crate::player::LossCause;
    1,104 ( 0.00%)                  let cause = if self.effective_life(i) <= 0 && !unlife {
    8,302 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::effective_life (184x)
        .                               LossCause::LifeDepleted
        .                           } else if self.effective_poison(i) >= self.poison_loss_threshold(i) {
        .                               LossCause::Poison
        .                           } else {
        .                               LossCause::CommanderDamage
        .                           };
      368 ( 0.00%)                  self.players[i].eliminated = true;
    5,520 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (184x)
      368 ( 0.00%)                  self.players[i].loss_cause.get_or_insert(cause);
    5,520 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (184x)
        .                           newly_eliminated.push(i);
        .                       }
        .                   }
        .                   // CR 809.5b — a team loses when its emperor loses; the generals go
        .                   // out with them and leave under the same CR 800.4a rules.
        .                   newly_eliminated.extend(self.apply_emperor_losses());
        .                   // CR 800.4a — when a player leaves the game, every card and token
        .                   // they own leaves with them, and permanents they controlled but
        .                   // didn't own revert to their owners' control. (Stack items the
        .                   // departed player controlled ceasing to exist is a remaining gap;
        .                   // tracked in TODO.md.)
      184 ( 0.00%)          for &p in &newly_eliminated {
      184 ( 0.00%)              self.objects_leave_with_player(p);
  605,747 ( 0.02%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::objects_leave_with_player (184x)
        .                   }
        .           
        .                   // CR 104.2 / 810.7: the game ends when only one *team* has
        .                   // players remaining (in solo-team formats — 1v1, FFA — a team
        .                   // is one seat, so this reduces to "only one alive player").
        .                   // Pre-Phase-G this checked alive seats directly, which in 2HG
        .                   // would have ended the match as soon as one of the four
        .                   // players died even though their teammate was still in.
   10,670 ( 0.00%)          if self.game_over.is_none() {
        .                       let alive: Vec<usize> = (0..self.players.len())
        .                           .filter(|i| !self.players[*i].eliminated)
        .                           .collect();
        .                       let mut surviving_teams: Vec<crate::team::TeamId> = alive
        .                           .iter()
   42,256 ( 0.00%)                  .map(|&s| self.team_of(s))
  316,920 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::team_of (21,128x)
        .                           .collect();
        .                       surviving_teams.sort_by_key(|t| t.0);
        .                       surviving_teams.dedup();
   42,656 ( 0.00%)              match surviving_teams.len() {
        .                           0 => {
        .                               self.game_over = Some(None);
        .                               events.push(GameEvent::GameOver { winner: None });
        .                           }
        .                           1 => {
        .                               // Report the winning team's first alive seat (by
        .                               // seat number) as the `winner`. For solo-team
        .                               // formats this is the literal winner; for 2HG it
        .                               // identifies the surviving team via a
        .                               // representative member, which is enough to let
        .                               // the server / UI resolve to a team result.
      400 ( 0.00%)                      let winner_team = surviving_teams[0];
        .                               let mut reps: Vec<usize> = alive
        .                                   .iter()
        .                                   .copied()
      600 ( 0.00%)                          .filter(|&s| self.team_of(s) == winner_team)
    3,000 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::team_of (200x)
        .                                   .collect();
        .                               reps.sort();
      200 ( 0.00%)                      let winner = reps[0];
      400 ( 0.00%)                      self.game_over = Some(Some(winner));
        .                               // CR 407.2 — the winner becomes the owner of everything
        .                               // in the ante zone.
      400 ( 0.00%)                      if self.playing_for_ante {
        .                                   self.award_ante_to(winner);
        .                               }
      600 ( 0.00%)                      events.push(GameEvent::GameOver { winner: Some(winner) });
        .                           }
        .                           _ => {}
        .                       }
        .                   }
        .           
   53,350 ( 0.00%)          events
   85,360 ( 0.00%)      }
        .           
        .               /// CR 506.4 — A permanent is removed from combat if it leaves the
        .               /// battlefield. Called by every battlefield-removal path
        .               /// (`move_card_to`, `remove_from_battlefield_to_graveyard_raw`,
        .               /// `remove_from_battlefield_to_exile`, etc.) so the post-removal
        .               /// combat state stays consistent. Prunes `self.attacking` (the
        .               /// attacker slot) and `self.block_map` (both blocker keys and
        .               /// attacker values).
   38,820 ( 0.00%)      pub(crate) fn remove_from_combat(&mut self, id: CardId) {
        .                   self.attacking.retain(|a| a.attacker != id);
        .                   // CR 702.22f — a creature removed from combat leaves its band.
        .                   for band in self.attack_bands.iter_mut() {
        .                       band.retain(|m| *m != id);
        .                   }
        .                   self.attack_bands.retain(|b| b.len() > 1);
      886 ( 0.00%)          self.block_map.remove(&id);
        .                   self.block_map.retain(|_, atks| {
        .                       atks.retain(|a| *a != id);
        .                       !atks.is_empty()
        .                   });
   31,056 ( 0.00%)      }
        .           
        .               /// **Raw** battlefield→graveyard move: zone change + replacements only.
        .               /// Fires NO dies/LTB triggers, no Persist/Undying, no sacrifice events —
        .               /// callers must handle those themselves (the SBA paths do). New effect
        .               /// arms should use `remove_to_graveyard_with_triggers` or
        .               /// `sacrifice_one` instead (audit P3: death-funnel bypass family).
   34,254 ( 0.00%)      pub fn remove_from_battlefield_to_graveyard_raw(&mut self, id: CardId) {
   15,224 ( 0.00%)          if let Some(mut card) = Self::take_card(&mut self.battlefield, id) {
        .                       self.remove_effects_from_source(id);
   11,418 ( 0.00%)              self.remove_from_combat(id);
1,040,086 ( 0.03%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_combat (3,806x)
   11,418 ( 0.00%)              self.collect_leaver_counters(&card);
  159,276 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::collect_leaver_counters (3,806x)
        .                       // Churning Reservoir: an oil-countered permanent bound for a
        .                       // graveyard flips its controller's oil-activity flag.
   15,224 ( 0.00%)              if card.counter_count(crate::card::CounterType::Oil) > 0 {
   38,060 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (3,806x)
        .                           self.players[card.controller].oil_activity_this_turn = true;
        .                       }
        .                       // CR 122.1h — Finality counters redirect Battlefield →
        .                       // Graveyard to Battlefield → Exile. Wilt in the Heat's "if
        .                       // that creature would die this turn, exile it instead" rides
        .                       // the same redirect via `dies_to_exile_eot`. We must check
        .                       // both here because the card has been removed from the
        .                       // battlefield before `resolve_zone_change` walks for it.
        .                       // CR 614 — "If a nontoken creature an opponent controls would die,
        .                       // exile it instead" (Valentin, Dean of the Vein). Detect an active
        .                       // `ExileDyingOpponentCreatures` static controlled by an opponent of
        .                       // the dying creature, redirect to exile, and capture its reflexive
        .                       // "when you do" effect to fire after placement.
   15,224 ( 0.00%)              let valentin_redirect: Option<(CardId, usize, Option<Effect>)> = if card.definition.is_creature()
   49,910 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,806x)
    7,324 ( 0.00%)                  && !card.is_token
        .                       {
    3,662 ( 0.00%)                  self.battlefield.iter().find_map(|src| {
        .                               src.definition.static_abilities.iter().find_map(|sa| {
        .                                   if let crate::effect::StaticEffect::ExileDyingOpponentCreatures { when_you_do } =
        .                                       &sa.effect
        .                                       && src.controller != card.controller
        .                                   {
        .                                       Some((src.id, src.controller, when_you_do.as_deref().cloned()))
        .                                   } else {
        .                                       None
        .                                   }
        .                               })
        .                           })
        .                       } else {
      432 ( 0.00%)                  None
        .                       };
        .                       // CR 614 — "If this permanent would be put into a graveyard, put
        .                       // it on top of its owner's library instead" (Pulmonic Sliver's
        .                       // Sliver-wide grant; the "may" is auto-taken).
        .                       // The dying permanent is already off the battlefield here, so a
        .                       // card that replaces its OWN death (Gravebane Zombie) has to be
        .                       // read off its snapshot alongside the battlefield grants.
    3,806 ( 0.00%)              let self_statics = std::iter::once((&card.definition, card.controller));
    3,806 ( 0.00%)              let library_top_redirect = self
        .                           .battlefield
        .                           .iter()
        .                           .map(|src| (&src.definition, src.controller))
        .                           .chain(self_statics)
        .                           .any(|(def, ctrl)| {
        .                               def.static_abilities.iter().any(|sa| {
        .                                   matches!(&sa.effect,
        .                                       crate::effect::StaticEffect::DiesToLibraryTopInstead { filter }
        .                                       if crate::game::layers::requirement_matches_card(filter, &card, ctrl))
        .                               })
        .                           });
        .                       // CR 614 — "…would die, return it to its owner's hand instead"
        .                       // (Necromancer's Magemark). Same site as the library-top redirect.
    3,806 ( 0.00%)              let hand_redirect = self
        .                           .battlefield
        .                           .iter()
        .                           .filter_map(|src| {
        .                               src.definition.static_abilities.iter().find_map(|sa| match &sa.effect {
        .                                   crate::effect::StaticEffect::DiesToOwnersHandInstead { filter } => {
        .                                       Some((filter.clone(), src.controller))
        .                                   }
        .                                   _ => None,
-- line 5665 ----------------------------------------
-- line 5675 ----------------------------------------
        .                               _ => None,
        .                           }))
        .                           .collect::<Vec<_>>()
        .                           .iter()
        .                           // The stateful evaluator, not `requirement_matches_card` — the
        .                           // filter is typically `IsEnchanted`, which has to scan the
        .                           // battlefield for the Aura still attached to the dying card.
        .                           .any(|(filter, ctrl)| self.evaluate_requirement_on_card(filter, &card, *ctrl));
   15,224 ( 0.00%)              let initial_to = if card.counter_count(crate::card::CounterType::Finality) > 0
   38,060 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (3,806x)
    3,806 ( 0.00%)                  || self.dies_to_exile_eot.contains(&id)
   15,224 ( 0.00%)                  || card.definition.dies_to_exile
        .                           || valentin_redirect.is_some()
        .                       {
        .                           crate::card::Zone::Exile
   15,224 ( 0.00%)              } else if library_top_redirect || card.definition.dies_to_library_bottom {
        .                           crate::card::Zone::Library
        .                       } else if hand_redirect {
        .                           crate::card::Zone::Hand
        .                       } else {
        .                           crate::card::Zone::Graveyard
        .                       };
   15,224 ( 0.00%)              let resolved = self.resolve_zone_change(
  384,406 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::resolve_zone_change (3,806x)
        .                           id,
        .                           crate::card::Zone::Battlefield,
        .                           initial_to,
        .                       );
        .                       // CR 702.69 — bump the turn's "permanents put into a graveyard
        .                       // from the battlefield" tally for Gravestorm. Only when the
        .                       // card actually landed in a graveyard (Finality / dies-to-exile
        .                       // redirects don't count).
   11,418 ( 0.00%)              if resolved == crate::card::Zone::Graveyard {
    3,806 ( 0.00%)                  self.permanents_to_graveyard_this_turn =
    3,806 ( 0.00%)                      self.permanents_to_graveyard_this_turn.saturating_add(1);
    7,612 ( 0.00%)                  self.graveyard_from_battlefield_this_turn.insert(id);
        .                           // CR 700.4 — record the death for the batched `PermanentDied`
        .                           // synthesis (dispatch drains this into "creature or artifact
        .                           // you control dies" triggers). CreatureDied already covers
        .                           // creatures at every site; this backfills non-creature deaths.
        .                           self.pending_permanent_deaths.push((
        .                               id,
    3,806 ( 0.00%)                      card.controller,
    3,806 ( 0.00%)                      card.definition.is_creature(),
   49,910 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (3,806x)
        .                               card.definition.card_types.contains(&crate::card::CardType::Artifact),
    3,806 ( 0.00%)                      self.resolution_causer,
        .                           ));
        .                       }
        .                       // CR 702.139 — Revolt: a permanent left the battlefield under its
        .                       // controller this turn.
   11,418 ( 0.00%)              if card.controller < self.players.len() {
    7,612 ( 0.00%)                  self.players[card.controller].permanent_left_battlefield_this_turn = true;
  114,180 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,806x)
        .                       }
        .                       // EOE Void — a nonland permanent left the battlefield this turn.
   15,224 ( 0.00%)              if !card.definition.is_land() {
   60,896 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (3,806x)
    3,806 ( 0.00%)                  self.nonland_permanent_left_bf_this_turn = true;
        .                       }
        .                       // Stamp `exiled_with` so the static's controller can recur the
        .                       // card later (Gisa, Glorious Resurrector's upkeep mass-reanimate).
    3,806 ( 0.00%)              if let (Some((src_id, _, _)), crate::card::Zone::Exile) =
    7,612 ( 0.00%)                  (&valentin_redirect, resolved)
        .                       {
        .                           card.exiled_with = Some(*src_id);
        .                       }
        .                       // Vren, the Relentless counts creatures exiled from under each
        .                       // player's control this turn — including via a death replacement.
    7,612 ( 0.00%)              if matches!(resolved, crate::card::Zone::Exile)
        .                           && card.definition.is_creature()
        .                           && card.controller < self.players.len()
        .                       {
        .                           let seat = card.controller;
        .                           self.players[seat].creatures_exiled_from_control_this_turn =
        .                               self.players[seat].creatures_exiled_from_control_this_turn.saturating_add(1);
        .                       }
   11,418 ( 0.00%)              self.place_card_at_resolved_zone(card, resolved);
14,434,270 ( 0.46%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::place_card_at_resolved_zone (3,806x)
        .                       let mut events = Vec::new();
   15,224 ( 0.00%)              self.on_left_battlefield(id, &mut events);
4,093,066 ( 0.13%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::on_left_battlefield (3,806x)
        .                       // Fire Valentin's reflexive "when you do, …" for the static's
        .                       // controller (CR 603.x reflexive trigger off the replacement).
   15,224 ( 0.00%)              if let Some((_src, controller, Some(effect))) = valentin_redirect {
        .                           let auto_target =
        .                               self.auto_target_for_effect_avoiding(&effect, controller, None);
        .                           self.stack.push(
        .                               TriggerPush::new(id, controller, effect)
        .                                   .target(auto_target)
        .                                   .build(),
        .                           );
        .                       }
        .                   }
   30,448 ( 0.00%)      }
        .           
        .               /// Return a permanent to its owner's hand, unwinding combat/attachments and
        .               /// emitting the leave-the-battlefield events. Used by cost lines that bounce
        .               /// the source (`ActivatedAbility::bounce_self_cost` — Magosi).
        .               pub fn remove_from_battlefield_to_hand(&mut self, id: CardId) -> Vec<GameEvent> {
        .                   let mut events = Vec::new();
        .                   let Some(mut card) = Self::take_card(&mut self.battlefield, id) else {
        .                       return events;
-- line 5770 ----------------------------------------
-- line 5790 ----------------------------------------
        .                   self.on_left_battlefield(id, &mut events);
        .                   if let Some((card_id, controller)) = leaver {
        .                       events.push(GameEvent::CreatureLeftWithoutDying { card_id, controller });
        .                   }
        .                   events.push(GameEvent::PermanentReturnedToHand { card_id: id, player: owner });
        .                   events
        .               }
        .           
      684 ( 0.00%)      pub fn remove_from_battlefield_to_exile(&mut self, id: CardId) {
      228 ( 0.00%)          if let Some(card) = Self::take_card(&mut self.battlefield, id) {
        .                       self.remove_effects_from_source(id);
      228 ( 0.00%)              self.remove_from_combat(id);
   11,780 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_combat (76x)
      228 ( 0.00%)              self.collect_leaver_counters(&card);
    3,192 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::collect_leaver_counters (76x)
      456 ( 0.00%)              let resolved = self.resolve_zone_change(
    7,676 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::resolve_zone_change (76x)
        .                           id,
        .                           crate::card::Zone::Battlefield,
        .                           crate::card::Zone::Exile,
        .                       );
        .                       // CR 702.139 — Revolt: a permanent left the battlefield this turn.
      228 ( 0.00%)              if card.controller < self.players.len() {
      152 ( 0.00%)                  self.players[card.controller].permanent_left_battlefield_this_turn = true;
   39,386 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (76x)
        .                       }
        .                       // EOE Void — a nonland permanent left the battlefield this turn.
      228 ( 0.00%)              if !card.definition.is_land() {
    1,216 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_land (76x)
       76 ( 0.00%)                  self.nonland_permanent_left_bf_this_turn = true;
        .                       }
        .                       // CR 603.6 — exile is a non-graveyard exit: a creature leaves
        .                       // without dying (Dour Port-Mage / Three Tree Scribe watchers).
        .                       let leaver =
      304 ( 0.00%)                  (card.definition.is_creature()).then_some((card.id, card.controller));
      988 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (76x)
      304 ( 0.00%)              self.place_card_at_resolved_zone(card, resolved);
  194,726 ( 0.01%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::place_card_at_resolved_zone (76x)
        .                       let mut events = Vec::new();
      228 ( 0.00%)              self.on_left_battlefield(id, &mut events);
   97,932 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/effects/movement.rs:crabomination::game::effects::movement::<impl crabomination::game::GameState>::on_left_battlefield (76x)
      228 ( 0.00%)              if let Some((card_id, controller)) = leaver {
      228 ( 0.00%)                  events.push(GameEvent::CreatureLeftWithoutDying { card_id, controller });
        .                       }
      228 ( 0.00%)              self.dispatch_triggers_for_events(&events);
  253,371 ( 0.01%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::dispatch_triggers_for_events (76x)
        .                   }
      608 ( 0.00%)      }
        .           
        .               /// Internal: drop `card` into `zone` (the result of a replacement
        .               /// resolver walk). Handles the terminal-zone shapes; for
        .               /// `Zone::Command` falls back to graveyard with a debug-assert
        .               /// until Phase I adds the per-player command zone storage.
        .               /// `Zone::Battlefield` / `Zone::Stack` likewise fall back —
        .               /// those shouldn't appear as legitimate redirect targets.
   34,938 ( 0.00%)      pub(crate) fn place_card_at_resolved_zone(
        .                   &mut self,
        .                   card: CardInstance,
        .                   zone: crate::card::Zone,
        .               ) {
        .                   use crate::card::Zone;
    3,882 ( 0.00%)          let owner = card.owner;
        .                   // CR 712.16/712.17 — a melded permanent leaves the battlefield as
        .                   // its two component cards.
    3,882 ( 0.00%)          let mut card = card;
   11,646 ( 0.00%)          if !card.meld_parts.is_empty() && zone != Zone::Battlefield {
        .                       for part in std::mem::take(&mut card.meld_parts) {
        .                           self.place_card_at_resolved_zone(part, zone);
        .                       }
        .                       return;
        .                   }
        .                   // CR 702.140e — a merged (mutated) permanent leaves as its components.
   11,646 ( 0.00%)          if !card.mutate_stack.is_empty() && zone != Zone::Battlefield {
        .                       for part in std::mem::take(&mut card.mutate_stack) {
        .                           self.place_card_at_resolved_zone(part, zone);
        .                       }
        .                       return;
        .                   }
        .                   // CR 702.95h — a card leaving the battlefield is no longer Soulbond-
        .                   // paired. Clear its own link so a later re-entry can re-pair cleanly
        .                   // (the SBA in `check_state_based_actions` clears the partner's side).
    7,764 ( 0.00%)          card.soulbond_partner = None;
        .                   // CR 708.10 — a face-down permanent is turned face up as it leaves
        .                   // the battlefield (no-op unless it carries a stashed real definition).
    3,882 ( 0.00%)          card.turn_face_up();
  201,864 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::turn_face_up (3,882x)
        .                   // The graveyard→exile redirect (Rest in Peace / Leyline / Disturb back
        .                   // face, CR 614.6 / 702.146e) and its void-counter rider read the back
        .                   // face, so capture them *before* the CR 712.4 front-face revert.
   15,528 ( 0.00%)          let exile_on_graveyard = self.graveyard_exiled_for(&card) || card.disturb_back_exiles();
   19,410 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::disturb_back_exiles (3,882x)
   11,646 ( 0.00%)          let (_, void_counter_on_exile, exile_stamped_by) =
   15,528 ( 0.00%)              self.graveyard_exile_redirects(&card);
  952,992 ( 0.03%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::graveyard_exile_redirects (3,882x)
        .                   // CR 710.4 / 712.4 — flip cards and transformed DFCs revert to their
        .                   // unflipped / front face off the battlefield.
    3,882 ( 0.00%)          card.revert_flip();
  174,690 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::revert_flip (3,882x)
    3,882 ( 0.00%)          card.revert_transform();
  174,690 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::revert_transform (3,882x)
        .                   // CR 702.160c — a prototype permanent has only its printed
        .                   // (full, colorless) characteristics off the battlefield.
    3,882 ( 0.00%)          card.revert_prototype();
  174,690 ( 0.01%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::revert_prototype (3,882x)
        .                   // CR 709.5c — Room unlocked designations are battlefield-only.
    3,882 ( 0.00%)          card.reset_room_doors();
   62,112 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::reset_room_doors (3,882x)
        .                   // MKM — a Case's solved designation is battlefield-only.
    3,882 ( 0.00%)          card.reset_case();
  100,932 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/card.rs:crabomination_base::card::CardInstance::reset_case (3,882x)
        .                   // CR 707 — a temporary copy reverts as it leaves.
    7,764 ( 0.00%)          self.revert_copy_on_leave(&mut card);
   58,230 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::revert_copy_on_leave (3,882x)
        .                   // CR 717.6 — an Astrotorium-backed Attraction card bound for anywhere
        .                   // but the battlefield, exile, or the command zone goes to its owner's
        .                   // face-up junkyard pile instead.
    7,764 ( 0.00%)          if !card.definition.attraction_lights.is_empty()
        .                       && matches!(zone, Zone::Hand | Zone::Library | Zone::Graveyard)
        .                   {
        .                       card.counters.clear();
        .                       card.keyword_counters.clear();
        .                       card.controller = owner;
        .                       self.players[owner].attraction_junkyard.push(card);
        .                       return;
        .                   }
   19,410 ( 0.00%)          match zone {
        .                       // CR 614.6 — "shuffle into its owner's library instead"
        .                       // (Darksteel Colossus); the card never touches the graveyard.
    7,612 ( 0.00%)              Zone::Graveyard if card.definition.shuffles_into_library_instead => {
        .                           use rand::seq::SliceRandom;
        .                           self.players[owner].library.push(card);
        .                           let mut rng = self.rng.draw();
        .                           self.players[owner].library.shuffle(&mut rng);
        .                       }
        .                       // CR 614.6 — Rest in Peace / Leyline of the Void redirect the
        .                       // graveyard arrival to exile; CR 702.146e — so does a Disturb
        .                       // back face.
    7,612 ( 0.00%)              Zone::Graveyard if exile_on_graveyard => {
        .                           let mut card = card;
        .                           if void_counter_on_exile {
        .                               card.add_counters(crate::card::CounterType::Void, 1);
        .                           }
        .                           if let Some(src) = exile_stamped_by {
        .                               card.exiled_with = Some(src);
        .                           }
        .                           self.exile.push(card)
        .                       }
   11,418 ( 0.00%)              Zone::Graveyard => self.players[owner].send_to_graveyard(card),
  114,180 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (3,806x)
      228 ( 0.00%)              Zone::Exile => self.exile.push(card),
        .                       Zone::Hand => self.players[owner].hand.push(card),
        .                       // CR 614.6 — "put it on the bottom of its owner's library
        .                       // instead" (Nissa's Chosen).
        .                       Zone::Library if card.definition.dies_to_library_bottom => {
        .                           let owner = card.owner;
        .                           self.players[owner].library.push(card);
        .                       }
        .                       // Top of owner's library. Replacement effects don't carry
-- line 5928 ----------------------------------------
-- line 5946 ----------------------------------------
        .                           // by the time we reach here. Fall back to graveyard.
        .                           debug_assert!(
        .                               false,
        .                               "replacement redirect to Battlefield/Stack is unsupported"
        .                           );
        .                           self.players[owner].send_to_graveyard(card);
        .                       }
        .                   }
   31,056 ( 0.00%)      }
        .           
        .               /// Remove a permanent from the battlefield to its graveyard and collect any
        .               /// `Dies` triggered abilities, returning them as events after the fact.
        .               /// (This is the version used by destroy/damage effects that want to fire triggers.)
        .               /// Shriveling Rot mode 2 — while
        .               /// `creature_deaths_drain_toughness_this_turn` is set, a creature leaving
        .               /// the battlefield for the graveyard drains its controller for its
        .               /// *computed* toughness. Called from both death funnels (the SBA lethal
        .               /// sweep and the destroy/sacrifice route) before the card is removed.
        .               pub(crate) fn apply_death_toughness_drain(&mut self, id: CardId) {
    7,324 ( 0.00%)          if !self.creature_deaths_drain_toughness_this_turn {
        .                       return;
        .                   }
        .                   let Some(controller) = self.battlefield.iter().find(|c| c.id == id).map(|c| c.controller)
        .                   else {
        .                       return;
        .                   };
        .                   let toughness = self
        .                       .computed_permanent(id)
        .                       .map(|cp| cp.toughness)
        .                       .or_else(|| self.battlefield.iter().find(|c| c.id == id).map(|c| c.toughness()))
        .                       .unwrap_or(0);
        .                   if toughness > 0 {
        .                       self.adjust_life(controller, -toughness);
        .                   }
    3,662 ( 0.00%)      }
        .           
    4,246 ( 0.00%)      pub fn remove_to_graveyard_with_triggers(&mut self, id: CardId) -> Vec<GameEvent> {
        .                   // Collect both `CreatureDied` and `PermanentLeavesBattlefield`
        .                   // self-source triggers off the leaving permanent. CreatureDied
        .                   // only matters for creatures (Solitude evoke-sac etc.);
        .                   // PermanentLeavesBattlefield is the broader "when this leaves the
        .                   // battlefield" hook used by Chromatic Star, Roomba-style cards,
        .                   // and any future non-creature die-trigger.
        .                   // Hushbringer (CR 614): creature-death triggers are suppressed while
        .                   // a `SuppressCreatureEtbTriggers { also_dies }` static is in play.
-- line 5990 ----------------------------------------
-- line 5994 ----------------------------------------
        .                   // Void, void counters) the card never dies, so its own dies triggers,
        .                   // equipment-granted dies triggers, and the died tally are suppressed
        .                   // (Persist/Undying already no-op — the card never reaches the
        .                   // graveyard to be returned from).
        .                   let exiled_instead = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
    2,316 ( 0.00%)              .is_some_and(|c| self.graveyard_exiled_for(c) || c.disturb_back_exiles());
    1,930 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::disturb_back_exiles (386x)
    1,158 ( 0.00%)          let dies_suppressed = dies_suppressed || exiled_instead;
        .                   // CR 603.10 — record the Auras riding this permanent before it
        .                   // leaves, so `EventScope::EnchantedBySource` triggers fire off a
        .                   // destroy/sacrifice death too, not only the SBA lethal-damage path
        .                   // (Puppet Master). The orphaned-Aura SBA records the same pair; the
        .                   // dedup below keeps a single entry.
      386 ( 0.00%)          let riders: Vec<(CardId, usize)> = self
        .                       .battlefield
        .                       .iter()
        .                       .filter(|c| c.attached_to == Some(id))
        8 ( 0.00%)              .map(|c| (c.id, c.controller))
        .                       .collect();
    1,154 ( 0.00%)          if !riders.is_empty() {
        4 ( 0.00%)              let seen = self.auras_at_death.entry(id).or_default();
       12 ( 0.00%)              for r in riders {
        .                           if !seen.contains(&r) {
        .                               seen.push(r);
        .                           }
        .                       }
        .                   }
      386 ( 0.00%)          let (leave_triggers, dying_creature_controller): (Vec<DeathTrigger>, Option<usize>) = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
        .                       .map(|c| {
    1,158 ( 0.00%)                  let is_creature = c.definition.is_creature();
    5,450 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs:crabomination_base::card::CardDefinition::is_creature (386x)
        .                           // Walk printed SelfSource LTB triggers + any transient
        .                           // granted ones (Rabid Attack-style "this creature gains
        .                           // 'when this creature dies, draw a card'" grants ride
        .                           // on `granted_triggers_eot[c.id]`).
      386 ( 0.00%)                  let granted: &[crate::card::TriggeredAbility] = self
        .                               .granted_triggers_eot
        .                               .get(&c.id)
        .                               .map(Vec::as_slice)
        .                               .unwrap_or(&[]);
        .                           let triggers = c.definition
        .                               .triggered_abilities
        .                               .iter()
        .                               .chain(granted)
        .                               // CR 603.10a — the dying creature's own self-fire death
        .                               // triggers. SelfSource always, plus the self-inclusive
        .                               // scopes ("this or another … you control dies" —
        .                               // YourControl) so a destroyed/sacrificed aristocrat drains
        .                               // for its own death, matching the SBA lethal-damage path.
      132 ( 0.00%)                      .filter(|t| matches!(
      264 ( 0.00%)                          t.event.scope,
        .                                   EventScope::SelfSource
        .                                       | EventScope::YourControl
        .                                       | EventScope::AnyPlayer
        .                                       | EventScope::ActivePlayer,
        .                               ))
    1,056 ( 0.00%)                      .filter(|t| match t.event.kind {
        .                                   EventKind::PermanentLeavesBattlefield => true,
        .                                   EventKind::CreatureDied => is_creature && !dies_suppressed,
        .                                   // CR 700.4 — "when this is put into a graveyard from the
        .                                   // battlefield" for a non-creature (Wicked Role token's
        .                                   // death-drain). Suppressed under a graveyard→exile
        .                                   // replacement just like CreatureDied.
        .                                   EventKind::PermanentDied => !dies_suppressed,
        .                                   _ => false,
        .                               })
    1,584 ( 0.00%)                      .map(|t| (c.id, t.effect.clone(), c.controller, t.event.filter.clone()))
   27,456 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (132x)
   10,164 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (132x)
        .                               .collect();
    1,014 ( 0.00%)                  let creature_controller = if is_creature { Some(c.controller) } else { None };
    1,060 ( 0.00%)                  (triggers, creature_controller)
        .                       })
        .                       .unwrap_or_default();
        .                   // CR 702.6e — Equipment-granted "dies" triggers fire as though
        .                   // printed on the equipped creature (Skullclamp). The SBA lethal
        .                   // path collects these too; without this the Destroy / sacrifice
        .                   // funnels dropped them.
    1,204 ( 0.00%)          let mut leave_triggers = leave_triggers;
        .                   if let Some(controller) = dying_creature_controller
      726 ( 0.00%)              && !dies_suppressed
        .                   {
        .                       for eq in &self.battlefield {
       12 ( 0.00%)                  if eq.attached_to != Some(id) {
        .                               continue;
        .                           }
        8 ( 0.00%)                  let Some(bonus) = &eq.definition.equipped_bonus else { continue };
        .                           for ta in &bonus.triggered_abilities {
        .                               if ta.event.kind == EventKind::CreatureDied {
        .                                   leave_triggers.push((id, ta.effect.clone(), controller, ta.event.filter.clone()));
        .                               }
        .                           }
        .                       }
        .                   }
        .                   if dying_creature_controller.is_some() {
      726 ( 0.00%)              self.apply_death_toughness_drain(id);
      726 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::apply_death_toughness_drain (242x)
        .                   }
        .                   // Capture Persist/Undying info before the card leaves the battlefield.
      386 ( 0.00%)          let (persist_has, undying_has, persist_minus, persist_plus, persist_owner) = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
        .                       .map(|c| {
        .                           (
        .                               c.definition.keywords.contains(&Keyword::Persist),
        .                               c.definition.keywords.contains(&Keyword::Undying),
    1,930 ( 0.00%)                      c.counter_count(crate::card::CounterType::MinusOneMinusOne),
    3,860 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (386x)
    1,544 ( 0.00%)                      c.counter_count(crate::card::CounterType::PlusOnePlusOne),
    3,860 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs:crabomination_base::card::CardInstance::counter_count (386x)
      386 ( 0.00%)                      c.owner,
        .                           )
        .                       })
        .                       .unwrap_or((false, false, 0, 0, 0));
        .                   // Bump the controller's per-turn died-creature tally for
        .                   // Witherbloom payoffs (Essenceknit Scholar). This path is the
        .                   // standard destroy / damage-lethal route that bypasses the SBA
        .                   // dies handler in `apply_state_based_actions`; we duplicate the
        .                   // bump so all destroy paths agree.
    1,302 ( 0.00%)          if let Some(controller_idx) = dying_creature_controller
    1,210 ( 0.00%)              && controller_idx < self.players.len()
        .                       && !exiled_instead
        .                   {
      726 ( 0.00%)              self.players[controller_idx].creatures_died_this_turn =
  109,132 ( 0.00%)  => /home/user/Crabomination/crabomination/src/player.rs:<crabomination::player::Player as core::ops::deref::DerefMut>::deref_mut (242x)
      484 ( 0.00%)                  self.players[controller_idx].creatures_died_this_turn.saturating_add(1);
      242 ( 0.00%)              self.creatures_died_this_resolution =
      242 ( 0.00%)                  self.creatures_died_this_resolution.saturating_add(1);
      484 ( 0.00%)              self.note_creature_death(id);
   79,440 ( 0.00%)  => /home/user/Crabomination/crabomination/src/game/mod.rs:crabomination::game::GameState::note_creature_death (242x)
    4,740 ( 0.00%)              if self.battlefield.iter().any(|c| c.id == id
        .                           && c.definition.subtypes.creature_types.contains(&crate::card::CreatureType::Zubera))
        .                       {
        .                           self.players[controller_idx].zuberas_died_this_turn =
        .                               self.players[controller_idx].zuberas_died_this_turn.saturating_add(1);
        .                       }
        .                   }
        .                   // Snapshot the card if it carries a SelfSource `PermanentSacrificed`
        .                   // trigger so the dispatcher can fire it from last-known info after the
        .                   // permanent has left (the sacrifice funnels push a `PermanentSacrificed`
        .                   // event but the source is already gone by dispatch time — Carrot Cake).
      386 ( 0.00%)          let has_sac_self_trigger = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
        .                       .map(|c| {
        .                           let granted: &[crate::card::TriggeredAbility] = self
        .                               .granted_triggers_eot
        .                               .get(&c.id)
        .                               .map(Vec::as_slice)
        .                               .unwrap_or(&[]);
        .                           c.definition
        .                               .triggered_abilities
        .                               .iter()
        .                               .chain(granted)
        .                               .any(|t| {
      132 ( 0.00%)                          matches!(t.event.scope, EventScope::SelfSource)
        .                                       && matches!(t.event.kind, EventKind::PermanentSacrificed)
        .                               })
        .                       })
        .                       .unwrap_or(false);
        .                   // Last-known-info snapshot (CR 603.10): a "when this leaves the
        .                   // battlefield" trigger resolves after the permanent is gone, so cache
        .                   // its pre-removal state for selectors that read it (e.g. an Aura's
        .                   // `AttachedTo(This)` → enchanted creature — Parallax Dementia).
      386 ( 0.00%)          if (!leave_triggers.is_empty() || has_sac_self_trigger)
        .                       && let Some(c) = self.battlefield.iter().find(|c| c.id == id)
        .                   {
      264 ( 0.00%)              self.died_card_snapshots.insert(id, c.clone());
        .                       // CR 603.10 — keep a longer-lived LKI snapshot so a
        .                       // "deals damage / makes tokens equal to its power" body reads
        .                       // the counter-boosted P/T at resolution (Goldvein Hydra,
        .                       // Cacophony Scamp). Removed when the trigger resolves.
        .                       self.leaves_bf_lki.insert(id, c.clone());
        .                   }
        .                   // Capture owner + land-ness before removal so we can emit a
        .                   // `CardPutIntoGraveyard` event (CR 700 — "put into a graveyard from
        .                   // the battlefield") once we confirm the card actually landed in the
        .                   // graveyard (Finality / dies-to-exile redirects send it elsewhere).
      264 ( 0.00%)          let gy_info = self
        .                       .battlefield
        .                       .iter()
        .                       .find(|c| c.id == id)
      386 ( 0.00%)              .map(|c| (c.owner, c.definition.card_types.contains(&crate::card::CardType::Land)));
      772 ( 0.00%)          self.remove_from_battlefield_to_graveyard_raw(id);
2,582,028 ( 0.08%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::remove_from_battlefield_to_graveyard_raw (386x)
        .                   let mut out = Vec::new();
      386 ( 0.00%)          if let Some((owner, is_land)) = gy_info
      772 ( 0.00%)              && self.players[owner].graveyard.iter().any(|c| c.id == id)
        .                   {
    1,544 ( 0.00%)              out.push(GameEvent::CardPutIntoGraveyard { player: owner, card_id: id, is_land });
        .                   }
    3,128 ( 0.00%)          for (source, effect, controller, filter) in leave_triggers {
   14,124 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (264x)
        .                       // CR 603.4/603.10a — a filtered self-death trigger checks its
        .                       // intervening filter against the dying creature (bound as
        .                       // TriggerSource via the cached death snapshot) before firing.
      264 ( 0.00%)              if let Some(pred) = &filter {
        .                           let mut ctx = crate::game::effects::EffectContext::for_trigger(
        .                               source, controller, None, 0,
        .                           );
        .                           ctx.trigger_source = crate::game::effects::event_subject(
        .                               &GameEvent::CreatureDied { card_id: id },
        .                               &EventKind::CreatureDied,
        .                           );
        .                           ctx.event_amount = self.event_amount_for(&GameEvent::CreatureDied { card_id: id });
        .                           if !self.evaluate_predicate(pred, &ctx) {
        .                               continue;
        .                           }
        .                       }
        .                       // Drivnod, Carnage Dominus — a creature dying causes this trigger,
        .                       // so it fires an additional time per Drivnod its controller runs.
      264 ( 0.00%)              let fires = 1 + if dying_creature_controller.is_some() {
        .                           self.battlefield
        .                               .iter()
        .                               .filter(|c| c.controller == controller)
        .                               .flat_map(|c| &c.definition.static_abilities)
        .                               .filter(|sa| {
        .                                   matches!(
        .                                       sa.effect,
        .                                       crate::effect::StaticEffect::DoubleControllerDeathTriggers
-- line 6210 ----------------------------------------
-- line 6213 ----------------------------------------
        .                               .count()
        .                       } else {
        .                           0
        .                       };
        .                       for _ in 0..fires {
        .                           let auto_target =
        .                               self.auto_target_for_effect_avoiding(&effect, controller, Some(source));
        .                           self.stack.push(
      396 ( 0.00%)                      TriggerPush::new(source, controller, effect.clone())
   27,456 ( 0.00%)  => /home/user/Crabomination/crabomination_base/src/effect.rs:<crabomination_base::effect::Effect as core::clone::Clone>::clone (132x)
        .                                   .target(auto_target)
        .                                   .build(),
        .                           );
        .                       }
      264 ( 0.00%)          }
   22,836 ( 0.00%)  => /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs:core::ptr::drop_in_place<crabomination_base::effect::Effect> (132x)
        .                   // CR 702.79 / 702.92 — Persist / Undying apply on *any* death, not
        .                   // just lethal-damage SBA. The destroy / sacrifice funnels route
        .                   // through here, so return the creature now if it qualifies.
    2,316 ( 0.00%)          self.return_persist_undying(
   10,808 ( 0.00%)  => crabomination/src/game/stack.rs:crabomination::game::stack::<impl crabomination::game::GameState>::return_persist_undying (386x)
    3,088 ( 0.00%)              id, persist_owner, (persist_has, undying_has, persist_minus, persist_plus), &mut out,
        .                   );
    2,316 ( 0.00%)          out // dies-trigger events are on the stack; Persist returns are in `out`.
    3,860 ( 0.00%)      }
        .           
        .               /// CR 702.79 / 702.92 — Persist / Undying return. Shared by every death
        .               /// funnel (SBA lethal damage, `Effect::Destroy`, sacrifice) so a creature
        .               /// with Persist/Undying returns regardless of how it died. `owner` and the
        .               /// counter counts must be captured from the dying card *before* it left the
        .               /// battlefield.
        .               /// `info` bundles `(has_persist, has_undying, minus_counter_count,
        .               /// plus_counter_count)` captured from the dying card before removal.
   45,672 ( 0.00%)      pub(crate) fn return_persist_undying(
        .                   &mut self,
        .                   id: CardId,
        .                   owner: usize,
        .                   info: (bool, bool, u32, u32),
        .                   events: &mut Vec<GameEvent>,
        .               ) {
        .                   use crate::card::CounterType;
        .                   let (has_persist, has_undying, minus_count, plus_count) = info;
   11,418 ( 0.00%)          let kind = if has_persist && minus_count == 0 {
        .                       CounterType::MinusOneMinusOne
   11,418 ( 0.00%)          } else if has_undying && plus_count == 0 {
        .                       CounterType::PlusOnePlusOne
        .                   } else {
        .                       return;
        .                   };
        .                   if let Some(mut returned) = Self::take_card(&mut self.players[owner].graveyard, id) {
        .                       self.players[owner].cards_left_graveyard_this_turn =
        .                           self.players[owner].cards_left_graveyard_this_turn.saturating_add(1);
        .                       returned.damage = 0;
        .                       returned.summoning_sick = true;
        .                       returned.add_counters(kind, 1);
        .                       let rid = returned.id;
        .                       events.push(GameEvent::CardLeftGraveyard { player: owner, card_id: rid });
        .                       self.battlefield.push(returned);
        .                       events.push(GameEvent::PermanentEntered { card_id: rid });
        .                   }
   30,448 ( 0.00%)      }
        .           }

8,486,550 ( 0.27%)  <counts for unidentified lines in crabomination/src/game/stack.rs>

--------------------------------------------------------------------------------
The following files chosen for auto-annotation could not be found:
--------------------------------------------------------------------------------
  ./elf/../sysdeps/x86_64/dl-machine.h
  ./elf/./elf/dl-tunables.c
  ./elf/./elf/do-rel.h
  ./malloc/../string/bits/string_fortified.h
  ./malloc/./malloc/arena.c
  ./malloc/./malloc/malloc.c
  ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S
  ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S
  /rust/deps/hashbrown-0.16.1/src/control/bitmask.rs
  /rust/deps/hashbrown-0.16.1/src/control/tag.rs
  /rust/deps/hashbrown-0.16.1/src/map.rs
  /rust/deps/hashbrown-0.16.1/src/raw/alloc.rs
  /rust/deps/hashbrown-0.16.1/src/raw/mod.rs
  /rust/deps/hashbrown-0.16.1/src/rustc_entry.rs
  /rust/deps/hashbrown-0.16.1/src/set.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/alloc.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/borrow.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/boxed.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/collections/vec_deque/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/raw_vec/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/slice.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/string.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/sync.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/in_place_collect.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/into_iter.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/set_len_on_drop.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_extend.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/alloc/src/vec/spec_from_iter_nested.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/../../stdarch/crates/core_arch/src/x86/sse2.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/alloc/layout.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/bool.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/cmp.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/hash/sip.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/intrinsics/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/chain.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/cloned.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/copied.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/enumerate.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/filter_map.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/flatten.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/fuse.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/map.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/adapters/take.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/range.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/accum.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/exact_size.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/iter/traits/iterator.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/macros/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_dangling.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/maybe_uninit.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/mem/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/int_macros.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/num/uint_macros.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/control_flow.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/function.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ops/range.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/option.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/alignment.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/const_ptr.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/mut_ptr.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/ptr/non_null.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/result.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/cmp.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/index.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/iter/macros.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/shared/smallsort.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/slice/sort/stable/mod.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/core/src/sync/atomic.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/alloc.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/map.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/collections/hash/set.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/panicking.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sync/poison.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/alloc/unix.rs
  /rustc/59807616e1fa2540724bfbac14d7976d7e4a3860/library/std/src/sys/sync/mutex/futex.rs

--------------------------------------------------------------------------------
Ir                   
--------------------------------------------------------------------------------
525,927,091 (16.88%)  events annotated

